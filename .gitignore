node_modules/
.next/
.env
.env.local
.env*.local
*.tsbuildinfo
next-env.d.ts
.DS_Store
.worktrees/
.gstack/
.playwright-mcp/
docs/designs/
latest-screenshots/
dist/

# Session / tooling scratch
.context/
.superpowers/
.claude/
packages/*.zip
packages/editor.zip

# Designer-facing spec mirror (reference docs, not runtime code).
# Lives at packages/editor/src/project/ as a dev-local snapshot of the spec drop.
# Excluded from git so it can't drift into the runtime tree, and from DS gates
# via --exclude-dir=project (see packages/editor/scripts/ds-grep-gates.sh).
packages/editor/src/project/

# Per-developer dev-only artifacts (not source, not committed)
packages/editor/src/editor-live.jpeg
packages/editor/src/project/
