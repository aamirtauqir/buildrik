/**
 * Aquibra Styles Panel — L0 stub
 * Sub-modules (components, configs, hooks, styles, types) not yet migrated to canonical.
 * @license BSD-3-Clause
 */
import * as React from "react";

export interface StylesPanelProps {
  composer?: unknown;
  selectedElement?: unknown;
}

export const StylesPanel: React.FC<StylesPanelProps> = () => null;

export default StylesPanel;
