/**
 * editor/sidebar — Left sidebar panel with 9-tab navigation system
 * Integration: L2 — fully wired (tab routing, keyboard shortcuts, panel state)
 *
 * Public API: LeftSidebar component + props type.
 * Tab components and hooks are internal to this module.
 *
 * @license BSD-3-Clause
 */

// Main sidebar component (the entire left panel)
export { LeftSidebar } from "./LeftSidebar";
export type { LeftSidebarProps } from "./LeftSidebar";
