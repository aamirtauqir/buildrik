# Teaching notes — preferences & working state

## How this learner wants to be taught
- **Roman-Urdu-friendly** explanation; one clear recommendation, not a menu. Surface cost/effort.
- **Drive, don't quiz** with abstract Q&A — give a clear lead + confirm.
- **Learn by auditing the REAL app** (Buildrik editor `/edit/:id`, dashboard). Abstract examples feel too far away.
- Solo builder, ships fast to `main`. Lessons must be short + immediately applicable.

## Context
- Building Buildrik: agency-first web builder, Webflow/Framer competitor. Editor (Vite, mounted in Next at `/edit/:id`) + dashboard (Next). Brand locked in DESIGN.md (cobalt #2D6DFF, no purple).
- Just shipped a big editor redesign (inspector → "You are editing" + 3-reach + plain labels; topbar → Exit/breadcrumb/Client view) after a codex review. The learner felt the product was still incoherent — that frustration is what triggered this course.

## Course shape
- Spine = NN/g 10 heuristics + severity + the audit loop. Reference: `reference/heuristics-cheatsheet.html`.
- L1 (done): teach the naming muscle via a quiz on the learner's own complaints. Assignment = write 3 findings on one editor task.
- L2 (done): rate + prioritise. Built as a WORKED example — 3 real, fresh editor findings (breadcrumb no agency›client #2 sev2 · +Invite first-class #8 sev1 · "0/7 done" pill unlabelled #6 sev2), each rated with the why (freq×impact×persistence), then ranked (severity sets order, effort breaks ties). Introduced "expected result in one line". `lessons/0002-rate-and-prioritise.html`.
- L3 (done): "What goes where" — prominence = frequency × importance + the loudest button = the product's purpose. Built from the learner's OWN two objections to a topbar mockup (breadcrumb depth + Publish vs Ask AI as hero). `lessons/0003-what-goes-where.html`.
- L4 (done): "From design to build — the handoff." The 4-part spec (WHAT / WHERE / DON'T-BREAK / DONE) + the loop (inventory → decide → handoff → build → next). Driven by the learner's own workflow question ("design each thing then build — what do I tell the coder?"). `lessons/0004-design-to-build-handoff.html`. Real artifact produced: `docs/reviews/audit-mockups/topbar-build-spec.md`.
- L5 (done): "The topbar is a bouncer — default no." The earns-its-way-in test (ready? · global? · always/at-a-glance?) + subtraction > addition. Driven by the learner asking "what else can we add to the topbar?" (collaboration). Taught the inverse instinct + flagged collab is demo-only (don't promote a broken feature). `lessons/0005-the-topbar-is-a-bouncer.html`.
- L6 (done): "Name it, write it down" — design principles vs taste; the upgrade = one shared page; why a shared rule-set is what makes a whole product consistent. Driven by the learner's meta-question (name it / improve it / scale it). `lessons/0006-name-it-write-it-down.html`.
- **KEY ARTIFACT born:** `reference/buildrik-design-principles.html` — the **Buildrik Design Constitution** (12 numbered principles from L1–L5, each with rule + use + test). This is now the course spine + the SSOT every decision/builder cites. Living doc — revise with the learner.
- L7 (done): "Design every state" — the happy path is ~10%; design empty/loading/error/edge + app states. Plus the system hierarchy (Principles → Patterns → Components → Tokens) — situational rules are PATTERNS, not new principles. Driven by the learner's two observations (we didn't design all states / 12 won't be enough, rules are situational). `lessons/0007-design-every-state.html`.
- Constitution grew: added **#13 Design every state** + a "Principles vs Patterns" note. New reference: `reference/states-checklist.html` (full state list + 3-question completeness test).
- L8 (done): "Build without breaking" — re-lay-out not re-wire (presentation ≠ logic; "move the boxes, keep the wires") + the 5-rung verification ladder (types → tests → walk states → click actions → acceptance). Driven by the learner's 3 build questions (how to implement / without breaking logic / how to know it's 100% right). `lessons/0008-build-without-breaking.html`. New reference: `reference/definition-of-done.html`.
- Topbar design is now COMPLETE: resting + spec + all 9 states (`docs/reviews/audit-mockups/topbar-states.html`). Ready to build.
- **TOPBAR BUILD COMPLETE** (`bda5fddd`, 2026-06-21): real editor topbar now matches `topbar-final.html` — 3 labelled zones (Navigate · View · Status+Ship), breadcrumb removed from topbar, Ask AI icon, Preview + Publish(/Export/Send-for-review) hero, and a ⋯ overflow holding Invite/⌘K/Preview-as-client/Help/Account (handlers untouched). CSS grid → `auto 1fr auto` + `.bd-topbar__zone` rules. All 4 rungs verified live (tsc, shell tests, design match, ⋯ opens + Command-palette fires). Dev diffs vs the mock: no presence (collab gated), "Export" not "Publish" (VITE_FEATURE_PUBLISH=false locally). Follow-up: the breadcrumb's bottom-bar home (StudioFooter) is a separate surface; status/publish state styles already exist (bd-topbar__status--saving/warn/error).
- L9 / earlier slice 1 (`a10bd63c`): Ask AI labelled Button → IconButton (constitution #5). **All 4 rungs ✓** — tsc, shell tests, icon renders (32px icon-only, DOM + screenshot), and click opens the AI bar (`✨ Content|Layout Write a headline…` appears bottom-center). Verified live 2026-06-21 after the browser extension recovered + a fresh saqib magic-link re-auth (dev server had died again → restarted). Next: slice 2 = 3-zone grid regroup + ⋯ overflow (Invite/⌘K/Help/Account) + Preview-as-client dropdown; slice 3 = wire status/publish states. Topbar.tsx uses a CSS-grid `VibcoderTopbar`; the 3-zone regroup touches that grid CSS too. (Publish button is feature-flagged off in dev — VITE_FEATURE_PUBLISH=false — so it won't show locally.)
- **Honesty win to reinforce:** held the line — did NOT claim "done/100%" with rungs 3–4 unrun. "Safe ≠ verified." Good teaching moment (definition-of-done in practice).

## Course structure now
- **Constitution** (`reference/buildrik-design-principles.html`, 13 principles) = the spine. **States checklist** (`reference/states-checklist.html`) = the completeness companion. Lessons 1–7 build to these. Method = principle-driven design; deliverable per surface = a complete prototype (all states) built via the 5-step loop.

## Mission status
- **MISSION UPDATED 2026-06-21** (learning-record 0004): now "become a pro product builder & designer (vibcoder)" — user research + measurement + prioritisation moved INTO scope (the "pro frontier"). Capstone L9 (`lessons/0009-becoming-a-pro.html`) + roadmap (`reference/the-path.html`) written. Confirm wording with the learner if they push back; otherwise proceed.
- **RESEARCH DONE (learner ran it):** real users gave harsh feedback — not easy/user-friendly, no error/success feedback in processes, "broken features" (collab), "backend exists but not integrated with frontend", "it's shit / make another version." Turned into `docs/reviews/user-feedback-fixplan-20260621.md` (UF-1..4 rated). Key diagnosis: **product is HALF-WIRED, not broken** — backend exists, frontend missing states + integration. NOT a rewrite.
- L10 (done): "Fix the spine, hide the rest" — the recovery method (hide half-baked → feedback everywhere → walk the core loop). `lessons/0010-fix-the-spine-hide-the-rest.html`. Full plan: `docs/reviews/buildrik-recovery-roadmap-20260621.md` (Phase 0 gate-broken → 1 feedback layer → 2 walk core loop → 3 wire/hide ghosts → 4 re-test → 5 redesign by pain).
- **NEXT ACTION (recommend):** Phase 0 — gate collaboration (constitution #3; cheap, instant credibility) → then Phase 1 feedback layer (extend the existing `addToast`/save-status pattern to every action). Both are real code work the learner asked to do "fast."
- L11 (done): "Write a goal the AI can't misread" — the build-prompt skill (check-first + handoff + verify-before-done + phrasing as a /goal). `lessons/0011-write-a-goal-the-ai-cant-misread.html` + reusable `reference/build-prompt-template.html` (has the ready Phase-0 prompt). Driven by the learner asking how to start Phase 0 / what to add / what prompt to write.
- **Grounding found:** collab flag ALREADY exists — `FEATURE_COLLAB` (off by default) in shared/utils/featureFlags.ts + runtimeEnv; StudioHeader CTA already gated (`isFeatureEnabled("collab")`). So Phase 0 collab work = AUDIT all collab UI respects the flag (find leaks: topbar collaborationSlot, PresenceIndicators, ConnectionQualityIndicator) + sweep/hide other half-baked. Not "add a flag."
- **NEXT ACTION:** run Phase 0 (the ready prompt) — audit collab gating + hide broken, verified. Learner may paste it as /goal or say "do Phase 0."
- Course now: 11 lessons + 8 references + 4 learning records. Mission = become a pro product builder; current arc = recover Buildrik via the roadmap (Phase 0 next).

- L12 (done): "Reverse-wireframe — read the backend, find the half-wired gap." `lessons/0012-reverse-wireframe-the-backend.html` + reusable `reference/reverse-wireframe-inventory.html` (pre-seeded with the real 25 routers). Driven by the learner asking (2026-06-22, Roman Urdu) "can we wireframe the existing app's current/backend functionality, and do we run plan-design-review / plan-ceo-review for it?" Taught: forward (wish) vs reverse (reality) wireframing; backend = source of truth (grounded: 25 routers · 50+ services · 58 models); the capability-inventory method with 3 verdicts (Ghost = no UI → wireframe / Half = missing states → wireframe states / Wired = leave); the Ghost+Half rows ARE the worklist (no guessing). **Correction taught:** plan-design-review / plan-ceo-review do NOT generate wireframes — they pressure-test a PLAN at step 4. Sequence = inventory → wireframe gaps → draft plan → THEN reviews → build. Directly feeds recovery roadmap Phase 3 (wire/hide ghosts). NEXT ACTION offered: "run the inventory" = walk the 25 routers vs live app, fill Ghost/Half/Wired in one pass.
- **INVENTORY RUN (learner said "go with the recommendation"), 2026-06-22:** done via CODE-evidence (grep `trpc.<router>.` in dashboard + editor `client.<router>.` services), not live browse. Result doc: `docs/reviews/reverse-wireframe-inventory-20260622.md`. **Headline: ZERO router-level Ghosts** — all 25 routers have a real frontend consumer. `forms` (guessed "Ghost" in L12) turned out Wired (overview-tab `SubmissionDrawer`+`listSubmissions`+pagination) — inventory corrected the guess, the lesson's point. 19 Wired / 6 Half-verify (reviews, comments, clients, theme, integrations, apiTokens). **Key reframe:** half-wiring ≠ missing screens; it's missing STATES + feedback inside existing surfaces (matches users' "no feedback" + recovery P1). Honesty limit flagged: static evidence proves surface-exists, not states — Half-vs-Wired needs a live state-audit (recovery Phase 4). Editor uses a vanilla `createTRPCClient` (`api-client.ts`/`AiTrpcClient.ts`) hitting `/api/trpc`, NOT the dashboard's `trpc.` React symbol — so editor-owned routers (ai/pages/cms/actions) look 0 in a dashboard-only grep; must grep editor `client.*` separately. NEXT: wireframe the 6 Half rows (reviews+comments first = agency↔client loop, highest leverage) OR run the live state-audit of the 19 Wired.
- **REVIEW-LOOP WIREFRAME DONE (learner: "wireframe the review loop"), 2026-06-22:** reverse-wireframed from real backend (`ReviewRequest` status+changeSummary · `Comment` x/y+targetSelector · `ShareLink` token+password+expiry). Artifacts: `docs/reviews/audit-mockups/review-loop-wireframe.html` (6 frames + 8 states, chips ✓/◐/✗) + `review-loop-build-spec.md` (L4 4-part handoff). **Real gap found by reading code, not guessing:** agency side WIRED (`ReviewService.submit`, `review-queue.tsx` resolve+toast) but the whole CLIENT half is broken — `share/[token]/page.tsx` is just a password gate that REDIRECTS to the read-only published site (no comment layer, no approve/request-changes for the client), AND no review notification trigger exists (silent = users' "no feedback"). **Hard decision surfaced for the plan-reviews:** published site is a separate deploy → can't overlay comments cross-origin → v1 = path A (Buildrik-hosted review preview, same-origin overlay), not path B (overlay live URL). Sequence now at step 3 done (plan drafted as the spec); NEXT = step 4 = run plan-design-review + plan-ceo-review on the spec to test path A vs B + scope, THEN build via 5-rung ladder.

- **L13 (done): "One home, grouped by the job"** — taught 2026-06-23 (Roman Urdu). The learner asked to redesign the existing features "first": too many clusters, duplicates, two surfaces (dashboard+editor), broken nav/hierarchy, "samajh nahi aata kahan rakhun", wants a step plan UX-first. Named it correctly = **information architecture**, not taste/polish/rewrite. Root cause: `feature-inventory.md` is grouped into 17 PRD/backend-module clusters = the SYSTEM's shape, not the user's. Taught 3 rules: (1) group by user job not module → ~6 agency jobs; (2) one home per feature = new constitution **#14**; (3) the scope test ("one site → editor, across sites/business → dashboard") decides every home. `lessons/0013-one-home-group-by-the-job.html`. Two fresh ground-truth docs drove the worked example: `docs/reviews/feature-inventory.md` (~100 features) + `docs/reviews/feature-backend-map.md` (every feature's WORKING/PARTIAL/STUB/BROKEN/NO-BACKEND status). **Constitution grew to 14** (added #14 "Every feature has one home", new group "Structure"). **New reference: `reference/ia-home-map.html`** — the SSOT worklist, seeded: 6 jobs × home × verdict (keep/merge/cut/hide/fix), grounded in backend-map. Real calls surfaced: dedup Site-settings×3 / Media×2 / SEO×2 / DS×3; **hide** AI-create-site + stock + collab + billing-upgrade + localization (#3); **cut** Export HTML (anti-retention). **Step plan confirmed (learner's instinct right): 1 Structure(IA) → 2 Navigation → 3 Flows/states → 4 Visual polish(later).** NEXT ACTION offered: "run the home-map" = fill all ~100 features + confirm each verdict vs live code in one pass → that output IS the Step-2 nav skeleton. Learning-record 0006.
- **L14 (done): "Structure becomes navigation"** — taught 2026-06-23. Learner pasted the editor-rail skeleton back and asked (Roman Urdu) "explain me all of this." Taught the Step1→Step2 transition: home-map (IA = what-goes-where) → navigation (how it's shown/clicked); IA ≠ navigation, good nav just mirrors IA. `lessons/0014-structure-becomes-navigation.html`. Core points: (1) editor nav = TWO layers, two questions — topbar = whole-site *actions* (always present), rail = the *jobs* inside one site (one at a time); mixing them = "messy". (2) the 4-mode rail was already correct — fix was the *contents*. (3) Design mode = two altitudes: Inspector (this element) vs Brand (the system). (4) 3 merges all #14 (Brand 3→1, Media 2→1, Settings 3→1). (5) 4 removals = #3 + product judgment (collab/locale/stock not-ready; export cut=anti-retention); "hide ≠ delete code." **No new principle — pure application of #5/#7/#10/#14/#3** (good signal the constitution is now load-bearing). Learning-record 0007. NEXT offered: clickable editor wireframe (rail+topbar, all states) OR open the dashboard sidebar the same way → then L12-sequence step 4 (plan-design-review/plan-ceo-review) on the drafted redesign.
- **L15 (done): "You're not stuck, you're in the gap"** — taught 2026-06-23. After several turns iterating the left rail, the learner had a confidence wobble (Roman Urdu): "I'm not a designer, can't design this properly, help me get good." This is the exact wall record 0004 predicted (craft-on-own-guesses → endless second-guessing). Taught: (1) **they ARE designing** — list the real wins this session (breadcrumb catch, the #14 Site-group-duplicates-topbar bug in the team's E3 plan, the cluster/grab-bag smell, "frequently-used → left bar" = #4); discomfort = taste firing. (2) **Ira Glass "The Gap"** (added to RESOURCES) — taste outruns skill at first; close with reps not harder thinking. (3) **Krug** — "make it testable, not right"; converge → test → fix. (4) **The unstick:** lock fewest-issue rail (Model C + dissolve Settings: Insert·Media·Pages·Design) → build clickable mockup (like topbar-final.html) → walk the core job (self + 3-5 users) → fix what trips. Decision > perfection. `lessons/0015-youre-not-stuck-youre-in-the-gap.html`. Learning-record 0008. **grab-bag vs palette reframe** (keep): can't kill grouping in a 100-feature tool; goal = each group is ONE job (palette) not several (grab-bag); Insert=palette (fine), Settings=the real grab-bag (dissolve). NEXT (offered): "lock C + build the mockup" → then run the 5-user walk on the core job and let use break the rail ties (4-vs-5, Media-in/out). Confidence is now a teaching variable — lead with their wins, then method.
- **Rail design state (mid-brainstorm, /brainstorming → Approach A, unit 1 = left rail):** converging on **Model C + dissolve Settings**: rail = ➕Insert(elements/sections/components) · 🖼Media · 📄Pages(+layers) · 🎨Design(brand). OUT of rail: Publish/Undo/History/AI = topbar. Live code today = 11-tab zoned rail (`tabsConfig.ts` GROUPED_TABS_CONFIG); E3 target in code = 4 tools Insert/Pages/Styles/Site + AI→topbar + Layers→footer (mapped, NOT built). Open ties to resolve by USE not debate: Media sub-tab vs own slot; Layers in Pages vs footer ⌗. Topbar already shipped (`topbar-final.html`).
- Course now: **17 lessons + 10 references + 10 learning records.** Constitution = 15 principles (no new principle in L17 — pure application).
- **L17 (done): "Wireframe it — job first, then blocks"** — taught 2026-06-25 (Roman Urdu, via /teach). Learner pasted a JTBD→module/block wireframing prompt and asked to apply it to the REAL app + produce **"a complete wireframing of the whole editor."** Lands exactly on the long-standing NEXT-action (clickable editor wireframe; L14/L15/L16 all pointed here). Taught: **two passes on one screen** — Pass 1 Job (JTBD, decides *prominence*: biggest job = loudest box, ties to #4) → Pass 2 Modules (cut into blocks, grey-box on a grid). The learner's exact **5-step recipe** (Screen→Job→Modules→Wireframe→Why) taught as the method. Low-fi discipline: monochrome (prominence by size/position not colour), bars not copy, ✕-box for images, align to grid; Krug's "make it *testable*, not *right*." Worked example = the editor shell, inline grey-box. `lessons/0017-wireframe-job-first-then-blocks.html`. **No new principle** (good signal — pure use of #1/#4/#5/#9/#13/#15) → constitution stays 15. **DELIVERABLE = `reference/editor-wireframe.html`** (the "complete editor wireframe"): 11 sections — shell + Insert/Media/Pages/Design panels + Inspector(selected/empty) + topbar + footer + states(empty/loading/error) + decisions-table + open-ties — every surface grey-boxed via the 5-step recipe, each with numbered "why" notes citing constitution #. Grounded in the live shell (mapped via Explore: `AquibraStudio.tsx`/`Topbar.tsx`/`ProInspector.tsx`/`StudioFooter.tsx`/`tabsConfig.ts` — today = 11-tab zoned rail; target = 4-tool) + `ia-home-map.html`. New reusable asset: **`assets/wireframe.css`** (the grey-box kit — wf-box/line/img/slot/shell/tag/why; reused by both lesson + reference; the workspace's wireframing component). **Open ties drawn but flagged for the 5-user walk, not debate** (L15): Media own-slot vs Insert sub-tab · Layers footer-⌗ vs Pages · Components sub-tab vs slot · presence-slot reserved-but-gated (collab demo-only, #3). Learning-record 0010. NEXT offered: "walk the wireframe" (frame-by-frame on the core job: build→publish) OR turn it into a clickable HTML mockup like `topbar-final.html`, then test on 3–5 users (L15 sequence).
- **LEFT-SIDE STRUCTURE LOCKED (learner: "left side achi nahi lag rahi, structure pehle decide karo"), 2026-06-25:** offered 3 left-side structures via AskUserQuestion w/ ASCII previews (A two-col icon rail + drawer / B single labelled collapsible panel = my rec / C labelled icon rail + drawer). **Learner OVERRODE my rec → chose A (icon rail + drawer, Webflow density).** Defensible (drawer collapses → 48px rail → canvas full; pro feel) — another applied-judgment override (cf. record 0002). Taught: the "achi nahi lag rahi" feeling = *within-structure execution*, not the 1-vs-2-column structure. 3 fixes applied to the wireframe inside the chosen structure: **(1) labels under rail icons** (48→64px; #1 recognition — the main "feels-bad" cause) · **(2) drawer collapsible** (active icon re-click → canvas-first; #9·#6) · **(3) drawer title always on top** (#2 orientation). Updated `reference/editor-wireframe.html` shell (labelled rail + drawer title + a collapsed-state mini-frame + decisions-table row) and `assets/wireframe.css` (`.wf-railitem` + `.wf-drawer-title`). Method note reinforced: structure (1-vs-2-col) is the IA/Step-2 call; label/collapse/title are navigation polish — don't conflate. NEXT unchanged: walk the wireframe OR clickable mockup → 5-user test.
- **L18 (done): "Cover every surface — don't sketch a few"** — taught 2026-06-26 (Roman Urdu, /teach). Learner pushed back: *"wireframe meri existing functionality ke hisaab se nahi ban rahi? features ki tarah bhi nahi — sirf functionality dikhao, poora editor redesign, kyunki pehla product theek design nahi hua. ab samajh aati hai aap iske liye skills bhi run karte ho."* THREE corrections: (1) wireframe must come from REAL functionality (reverse-wireframe L12), not invented; (2) **feature ≠ surface ≠ screen** — group ~100 features into ~12–15 functional *surfaces* (by job), wireframe the surface, don't catalog features; (3) redesign the WHOLE editor (v1 designed wrong = the course's founding wound). Diagnosed the real gap in my own work: the editor-wireframe was **sketch not cover** — shell + 4 panels + inspector basic, but ~21 surfaces missing. Taught the completeness discipline: a **coverage map** makes "done" countable (to-draw → 0), not a feeling — definition-of-done applied to wireframing. `lessons/0018-cover-every-surface-not-a-sketch.html`. **NEW reference (the grounding SSOT the learner demanded): `reference/editor-functionality-map.html`** — every real editor functional surface (code-grounded from `complete-feature-list-20260623.md` + `editor-orphans-20260624.md` + `wiring-matrix-codex`), grouped by 6 jobs, with status (✅🟡🔵) + redesign-home + **drawn/partial/to-draw**. Headline count: ≈35 surfaces · **11 drawn (basic) · 3 partial · 21 to-draw.** This is the build checklist + proof the wireframe is grounded in THEIR functionality, not guessed. No new constitution principle. Learning-record 0011. **Method-sequence reinforced (L12):** inventory→**confirm the map**→wireframe each to-draw surface (L17 recipe)→plan-design-review→build. Deliberately did NOT mass-produce 21 screens this turn — showed the coverage map FIRST so the learner can verify I captured their functionality correctly (their exact complaint = grounding) before investing in drawing. NEXT offered: confirm the map (anything missing?) → then "draw the to-draw surfaces" one batch at a time until to-draw=0.
- **L19 (done): "Redesign is a pipeline, not a button"** — taught 2026-06-26 (Roman Urdu, /teach). Learner asked the PROCESS question: can we redesign the whole product using `plan-ceo-review` (to "fix features"), `design-review` (to "make user flow + wireframing"), a `/goal` skill + `codex`, every screen checked, job+module like Webflow — and should old flows be kept or all-new? **Two key corrections taught:** (1) **what the skills REALLY do** — plan-ceo/design/eng-review *pressure-test a written PLAN* (don't draw/generate); `design-review` *QAs BUILT UI* (spacing/slop, post-build), NOT a wireframe generator; `codex` reviews code/plan; `/goal` is autonomous *build*. So "design-review se wireframing banwate hain" is the wrong model — **wireframes = me + their eye (stage 2); skills validate (stage 5) / QA-built (stage 7) / build (stage 6).** Same correction lineage as L12. (2) **keep vs new flow** = L8 "re-lay-out, not re-wire": keep+reskin flows that work (status ✅), new flow only for broken/confusing/missing (status 🟡🔵) — the decision falls straight out of the coverage-map status column. **Taught the 8-stage pipeline in 3 lanes:** Design (1 inventory✓ · 2 wireframe←now · 3 check-every-screen) → Validate (4 write plan · 5 autoplan+codex) → Build (6 /goal+codex+5-rung · 7 design-review built UI · 8 5-user walk). **Honest limit:** stages 2–3 need their judgment (AI guesses→slop); /goal shines at build — "ek /goal poora product design nahi karega, woh aap ka design build karega." Job+module = Webflow's model (top-level jobs, fixed modules = #15 spine). `lessons/0019-the-redesign-pipeline.html` + **new reference `reference/redesign-pipeline.html`** (the playbook: skill-roles table + keep/new rule + 8 stages + honest autonomous-vs-judgment split). New RESOURCES: Webflow University (job+module reference, fills the long-noted agency-tool gap). `autoplan` surfaced as the skill that runs ceo+design+eng reviews together. No new constitution principle. Learning-record 0012. **Reinforced: we're at Stage 2 — draw surfaces BEFORE running reviews** (can't review an unwritten plan; can't write a plan from undrawn surfaces). Course now: 19 lessons + 12 references + 12 learning records. NEXT: "draw the to-draw surfaces" (batch 1: CMS·Components·Inspector-full·Forms·Publish-flow) each with a stage-3 heuristics check.
- **STAGE-2 BATCH 1 DRAWN (learner: "kya karna hai ab" → drove it), 2026-06-26:** drew 5 deep surfaces into `reference/editor-wireframe.html` as §10–14 (CMS two-pane · Components library+instance · Inspector-full 3-tab section list [covers Interactions+Animations] · Forms build+submissions · Publish-flow 4 states). Each = 5-step recipe + numbered why-notes (constitution #) + a one-line **Stage-3 heuristics check** (the learner's "har screen check ho", done pre-build = cheap). Renumbered Decisions §10→§15, added 5 index entries. Updated `editor-functionality-map.html`: **~40 surfaces · 13 drawn · 5 partial · 22 to-draw** (was 11/3/21). **22 remaining**, next batches by job: B2 sign-off loop (Preview·Request-review·Comments·Share — the agency↔client differentiator, highest leverage) · B3 settings/ship (SEO·Site-settings·Domains·Redirects·Version-history) · B4 AI (assistant·propose·inline) · B5 J3 misc (Layers·Canvas-controls·Inline-text·Onboarding·Command-palette·Image-editor·Stock·My-Templates·DS-push·Custom-CSS). Pipeline stays: finish Stage-2 (to-draw→0) before Stage-4 plan + Stage-5 autoplan/codex. NEXT: "next batch" → B2 sign-off loop.
- **L20 (done): "Tools live near the work" + CONSTITUTION #16, 2026-06-26** (Roman Urdu, /teach). Learner proposed a structural rule: canvas-related controls (undo/redo · history · breakpoints · zoom — frequent, used *while* on the canvas) should live WITH the canvas, not the topbar; and keep the topbar clean — only *infrequent* things there. Recognised as a real principle: **control locality / proximity** — frequency decides not just how *loud* a control is (#4) but *where* it lives. Grounding = **Fitts's Law** (distance to target = effort) + the Figma/Framer "canvas toolbar" pattern. **Refines L5 (bouncer):** old test "topbar default NO"; new sharper filter "frequent canvas-op? → not topbar, it hugs the canvas." Resolved the apparent conflict: Publish is *infrequent* (build long, publish occasionally) so it stays the topbar hero — "topbar=rare/global" and "Publish=hero" don't clash. **Constitution grew to 16** (#16 "Tools live near the work", Structure group, after #15). `lessons/0020-tools-live-near-the-work.html`. **Wireframe updated** (`editor-wireframe.html`): §0 shell — slimmed topbar (Exit · Share · Publish · ⋯) + added a **canvas toolbar** strip at top of canvas (undo/redo · history ⏱ · device-switch centered · zoom); §7 topbar rewritten "slim, rare/global only" with an L8 re-lay-out note (shipped topbar still has undo/breakpoints → move buttons, keep handlers); decisions-table +#16 row. RESOURCES +Fitts's Law (NN/g + Laws of UX). Learning-record 0013. AI ✨ kept in topbar (it's a global helper, not a direct canvas-manipulation control — out of #16's scope; flagged as a possible later reconsideration). NEXT unchanged: "next batch" → B2 sign-off loop (Preview·Review·Comments·Share).
- **STAGE-2 BATCH 2 DRAWN ("okay" → drove it), 2026-06-26:** drew the J5 sign-off loop into `editor-wireframe.html` §15–18 (Preview-as-client clean overlay · Request-review w/ change-summary + the missing notify trigger · Comments canvas-overlay pins+threads+resolve · Share-link+password w/ honesty badge for the 🟡 decorative-gate). Grounded in the broken-client-side gap (share page just redirects) + the existing `review-loop-wireframe.html` (path A: same-origin Buildrik-hosted review preview). Each = 5-step + why-notes + stage-3 check. Renumbered Decisions §15→§19, +4 index entries. Map: **17 drawn · 5 partial · 18 to-draw**. **18 remaining:** B3 settings/ship (SEO·Site-settings·Domains·Redirects·Version-history) · B4 AI (assistant·propose·inline) · B5 J3 misc (Layers·Canvas-controls·Inline-text·Onboarding·Command-palette·Image-editor·Stock·DS-push·Custom-CSS·My-Templates). NEXT: "next batch" → B3.
- **CODEX REVIEW PASS on the wireframing, 2026-06-26** (learner ran `/codex review the wireframing`). Codex (read-only, ~1.1M tok) found **11 real issues** (7 P1 / 4 P2) — almost all **cross-reference drift** from incremental edits: after #16 moved undo/redo·breakpoints·zoom to the canvas toolbar, functionality-map still listed them in topbar/footer rows; zoom had two homes; surface count said ~35 (pipeline) vs ~40 (map) vs ~12–15 (L18); several stage-3 "pass" lines claimed undrawn states (sent/awaiting, components badge, forms validation). Plus a **real HTML bug** (functionality-map callout `<div>` closed with `</p>`) that I had wrongly dismissed as `tidy` noise — a div-balance + raw-byte check proved codex right. **All 12 fixes applied** (Edit was blocked under /codex read-only → applied via a python script; the callout fix landed via Edit after plan-mode exit). Re-verified: 5 files tag-balanced, map cells **16 drawn / 7 partial / 18 to-draw = 41** matching the summary. Learning-record 0014. **Process takeaway: run a consistency pass (codex or a script) after each wireframe batch — counts, control-homes, and "drawn" claims drift first; and verify a finding against raw bytes before calling it a false-positive.** Coverage now: **41 surfaces · 16 drawn · 7 partial · 18 to-draw** (Interactions/Animations honestly downgraded ✓→◐). NEXT unchanged: "next batch" → B3 (SEO·Site-settings·Domains·Redirects·Version-history).
- **STAGE-2 BATCH 3 DRAWN ("next batch"), 2026-06-26:** drew J6 settings/ship into `editor-wireframe.html` §20–24 (Per-page SEO w/ live search-preview + char-count · Site-settings one-home section-nav + auto-save · Custom domains w/ pending/verified/failed states + honesty badge 🟡 · Redirects w/ "stored-not-live" honesty badge 🟡 · Version-history timeline+restore, "cloud-backed ✓" now true). Each 5-step + why + stage-3. Renumbered Decisions §19→§25, +5 index entries. Map: **21 drawn · 7 partial · 13 to-draw = 41**, re-verified tag-balanced. **13 remaining:** B4 AI (assistant·propose·inline) · B5 J3 misc (Layers·Canvas-controls·Inline-text·Onboarding·Command-palette·Image-editor·Stock·DS-push·Custom-CSS·My-Templates). NEXT: "next batch" → B4 (AI) then B5.
- **CODEX REVIEW on Batch 3 ("codex reviw on this batch 3"), 2026-06-26 — GATE PASS after 2 fix rounds.** Codex (read-only, high effort, ~425k tok) found **7 issues** (5 P1 / 2 P2) on §20–24, same drift class as the round-1 review: **stage-3 "pass" lines claiming undrawn states** + 2 genuinely-missing capabilities. (1) §20 SEO — claimed over-limit edge + AI no-key, neither drawn → **drew** an edge-states strip (red `172/160 ⚠` + `✨ needs key`). (2) §21 Site-settings — nav drew 5 rows but copy promised 6, AND falsely claimed localization is gated (feature-list:85 — localization *settings* ARE wired; only *runtime* engine is locale-unaware) → **drew 6th row, removed the fake gate**, why-note now states the accurate runtime caveat. (3) §22 Domains — missing `set-primary` flow (real per :101; dead-ended w/ multiple domains) → **drew a verified-secondary row + Set-primary action** (4 states now). (4) §23 Redirects — claimed empty state, none drawn → **drew empty-state ghost**. (5) §24 Version-history — claimed empty + restore-confirm + compare, only timeline drawn → **drew empty + restore-confirm box; compare needed a 2nd round** (codex round-2 caught it was still text-only `select 2 →`) → **drew visual ☑ selection on 2 rows + active `Compare 2`**. 3 codex passes total (high→medium→low effort, quota-aware): round-1 found 7, round-2 confirmed 6/7 + flagged §24 compare, round-3 (§24 only) **GATE PASS**. Re-verified: div 516/516 · section 28/28 · ol 24/24 · li 67/67 balanced; map unchanged 21/7/13/41 (site-settings ✓ now honest = 6 wired sections). **Reinforces learning-record 0014: every "pass" line must be backed by a *visually drawn* state — "described in text" ≠ drawn (codex's round-2 catch); and a list/timeline surface's missing capability (set-primary, empty state) hides behind a populated happy-path.** NEXT unchanged: "next batch" → B4 (AI) then B5.
- **STAGE-2 BATCH 4 DRAWN + codex GATE PASS ("next batch"), 2026-06-26:** drew the 3 AI surfaces (cross-cutting, not a job — no rail slot, #5/#8) into `editor-wireframe.html` §25–27: **AI Assistant** (topbar ✨ → right drawer: chat thread · quota meter · Chat·Plan toggle · edit-command "applied" chip · 🟡 live-unverified badge · no-key/thinking/streaming/error/quota-out states) · **Propose-action** (propose→confirm→execute card + target highlight + ADMIN badge + 1-use token + executing/applied/rejected chips — the trust/safety surface, never auto-mutates #5) · **Inline AI** (✨ on selection: Rewrite/Shorten/Tone + Accept/↻ · image alt-text · generating/failed states — clean **#16 "tools near work"** application, L20's AI form). **Gave the deferred `ai.getQuotaStatus` orphan its home** (it was ⏸ "needs a home in the AI panel" — this redesign IS that home; quota meter in the assistant header). Renumbered Decisions §25→§28, +3 index entries, +3 decision rows. Map: 3 AI rows ◻→✓, **24 drawn · 7 partial · 10 to-draw = 41**. **Codex caught the SAME drift class AGAIN** (1st pass GATE FAIL, 5 P1/3 P2): pass-lines claimed states (streaming/error/quota-out/executing/done/rejected/loading) + capabilities (edit-commands/plan) that were prose-only, not drawn. Fix = **drew every claimed state as a visible chip** (not trimmed claims) → 2nd pass GATE PASS. Verified: div 562/562 · section 32/32 · ol 27/27 · li 78/78 balanced; map td-cells 24/7/10=41 (the bare-regex 25/8/11 counts the 3 legend spans — use `<td class=` anchor). **This is now a 3-batch pattern: stage-3 "pass" lines drift into claiming undrawn states EVERY batch — the discipline (LR 0014) must be applied at draw-time, not caught at review-time.** 4 codex passes this turn (high→med→med→low, quota-aware). **10 surfaces left:** B5 J3 misc (Command-palette · Onboarding · Layers · Canvas-controls · Inline-text · Image-editor · Stock · DS-push · Custom-CSS · My-Templates). NEXT: "next batch" → B5.
- **STAGE-2 BATCH 5 DRAWN + codex GATE PASS — COVERAGE COMPLETE, to-draw = 0 ("next batch"), 2026-06-26.** Drew the final 10 into `editor-wireframe.html` §29–38: Command-palette (⌘⇧P overlay) · Onboarding (dismissible card) · Layers (footer ⌗ popover, w/ rename ✎ + drag-reorder ⠿) · **Canvas-controls** (on-canvas selection overlay — 4 corner handles + drag ✛ + group + **edit-scope picker** element-vs-master; distinct from the already-drawn §0 canvas *toolbar*) · Inline-text (on-canvas format bar + ✨) · Image-editor (Media modal crop/aspect/versions) · Stock browser (🔵→✅ env-gated, no-key gate + Insert CTA) · **Shared DS push ★** (the agency wedge — multi-select client sites + **blast-radius confirm "N sites will update"** + per-site result/partial-fail, the UX-audit cross-client gap) · Custom-CSS (inspector advanced, Pro-lock + invalid-CSS error) · My-Templates (🟡→✅ server-backed, grid + recents + empty). Renumbered Decisions §28→§39, +10 index entries, +4 decision rows. Map: **34 drawn · 7 partial · 0 to-draw = 41 — COVERAGE COMPLETE.** (The 7 ◐ partials = Insert/Pages/Media/Interactions/Animations/Brand/States panels — drawn but want deeper state-sets = Stage-3 polish, not Stage-2 coverage.) **★ THE WIN: LR-0015 LANDED.** I applied the corrective (draw states FIRST, write the "pass" line OFF the screen) and codex confirmed verbatim: *"Stage-3 state-pass lines §29–38: no mismatches found. This batch fixes the prior 'claims a state that isn't drawn' problem."* — the defect that recurred 3 straight batches is GONE once the work-order changed (move the check upstream, not try-harder-at-review). Codex 1st pass still GATE FAIL on **cross-doc drift** (LR-0014 class): a stale "Stock = hidden" decision row + §2 Media "stock chhupa hua" both contradicted §35 (Stock graduated 🔵→✅ shipped/env-gated) — reconciled both; + 4 P2 prose-named-not-drawn capabilities (layers rename/reorder, canvas 8-handles, stock Insert, my-templates recents) all drawn. 3 codex passes (med→low→low). Final: div 696/696 balanced, map td-cells 34/7/0=41, anchors resolve, GATE PASS. **STAGE 2 DONE.** Per the redesign pipeline (`reference/redesign-pipeline.html` / L19): next is the **Validate lane** — Stage 4 (write the redesign plan from the coverage map's keep/new column) → Stage 5 (autoplan = ceo+design+eng reviews together, + codex on the plan). Course now: 20 lessons + 12 references + 15 learning records. NEXT: assemble the Stage-4 plan, OR deepen the 7 ◐ partials (Stage-3 polish) — learner's call.
- **STAGE 4 — REDESIGN PLAN BUILT + codex GATE PASS ("go with your recommendation"), 2026-06-26.** Recommended + did the next pipeline step: turned the coverage map into an ordered build plan → **new reference `reference/redesign-plan.html`**. Method taught (the lesson): a plan is *read off the status column* in 3 moves — (1) keep-vs-new = the status icon (✅ keep+reskin / 🟡🔵 new-flow, L8 re-lay-out-not-re-wire); (2) that split is also the **effort estimate** (keep=cheap hours, new=days); (3) order = dependency-first (spine #15) then **pull the differentiator forward** (agency wedge). **Honest headline: 30 keep+reskin · 3 graduated · 8 new-flow = 41 → ~80% re-lay-out, ~20% new build.** 6 phases: P0 spine → P1 editing-loop (J3) → P2 wedge+brand (J4, DS-push ★ pulled fwd) → P3 agency sign-off loop (J5, all-but-Preview new) → P4 ship (J6) → P5 AI; + woven Substrate (save/undo/version/states). Collab explicitly EXCLUDED (demo-only, #3). 4 named risks for Stage-5 to argue (sequencing/honesty-vs-fix/effort/coherence). Codex caught the SAME honesty families as the wireframe batches (LR-0014/0015 are scale-free): a P3 'all new-flow' header contradicting a keep-tagged Preview row, + an unprovable '3 graduated' headline (Version-history was prose-only substrate) → fixed by tagging substrate + a **tally-proof line** (every headline number derivable from the body). 2 codex passes (med→low). div 24/24 balanced, all cross-links resolve (caught + fixed a wrong lesson-link: re-lay-out = L0008 not L0015). Learning-record 0016. **Course: 20 lessons · 14 references (+redesign-plan) · 16 learning-records.** Closes the loop the learner opened 3 turns ago ("redesign using plan-ceo-review/design-review/goal+codex?") — answer played out: wireframes human-authored (St2), plan authored from map (St4), review-skills only NOW the right tool (they need a plan to review). NEXT (Stage 5, Validate lane): run `autoplan` (or plan-ceo/design/eng-review individually) on `redesign-plan.html` → then codex on the plan → then Build (Stage 6).
- **STAGE 5 — AUTOPLAN RAN, caught "build the wrong thing well"; founder chose VALIDATE-FIRST ("chala do autoplan"), 2026-06-26.** Ran autoplan on `redesign-plan.html`: 3 independent Claude subagents (CEO=ce-product-lens, design=ce-design-lens, eng=ce-feasibility) in parallel + 1 codex code-check. Plan was craft-excellent but **GATE FAIL on critical strategic+classification holes**, converging across lenses. **Cross-phase themes (2+ lenses agreed):** (1) ★ **wedge mis-everything** — CEO: it's retention/scale not acquisition (can't feel on client-site #1); Eng+codex: it's a NEW BUILD not keep+reskin (editor has ZERO `theme.*` wiring, dashboard-only, no themeSync.ts) + live clobber hazard (editor reads dsSchemaVersion only at load → open client editor LWW clobbers a fresh push). (2) **keep/new=status-icon too coarse** — "works"≠"flow right"; dedup "one home" merge (the lever!) tagged cheap; icon drops sub-statuses (Components 🔴, Comments cross-origin, Animations engine DELETED 05-18). (3) **demand unvalidated until Stage 8** (after whole build). (4) **Share=security gap not honesty-badge** — codex-confirmed password redirects to PUBLIC url. (5) **5 surfaces block build** (CMS-entry, Interactions-builder, Animations-timeline, Components master-mode, P3 client-comment) misclassified "deepen states", no primary UI drawn. (6) Inspector in P2 breaks P1 #12 gate → split chrome P0/content P2. Codex cross-checked 3 critical code claims: wedge-zero-editor-wiring ✓, share-redirects-public ✓, animations-engine-deleted ✓ (all hold). **User Challenge (never auto-decided) → founder chose "Validate demand first"** (CEO+Eng rec). Plan REVISED: added **Phase −1 demand gate** (pick ONE differentiator → thin-vertical on CURRENT chrome → 3-5 real agencies → demand pulls → fund P0-P5) + **7 required revisions** recorded on the plan (re-tag wedge new-flow+clobber-guard → tally 30/3/8→29/3/9, fix keep/new 2-axis method, split Inspector, draw 5 blockers, fix Share backend, reclassify Animations, move demand-gate before build). Learning-record 0017 (the capstone). **Course: 20 lessons · 14 references · 17 learning-records.** **THE PAYOFF: the whole "can we redesign using these skills?" loop closed — Stage 5 just earned its keep by catching a quarter-saving strategic error no more drawing/planning would find.** Skills pressure-test, don't draw/build. NEXT: not more design — put the one differentiator in front of real agencies (Phase −1). Founder picks the differentiator: DS-push (acquisition?) vs review-loop (CEO lens: sharper retention pain).
- **WIREFRAME → WIREFLOWS PIVOT (founder ran `/codex` on the wireframe, caught it was surface-not-job), 2026-06-26.** Founder critique (Roman Urdu): "wireframing deeply explain ni hai, structure STILL module-approach pe, user-flow completely miss, job-base ni, screen pe job decided ni, AI slop hai." Ran codex consult on `editor-wireframe.html` (high effort, 247k tok) vs the JTBD lesson + #10/#14. **Codex sided with the founder:** claim #2 (still module/surface-based) **TRUE** — index = `0 shell·1 Insert·2 Media·3 Pages·4 Design` = backend panels, violates #14; claim #6 (AI slop) **TRUE** — the repeated `Job·Numayan·Dheema·Modules·Why` formula + `✓ Stage-3 check…pass` self-stamps + `to-draw=0` = "completeness theater, generated, sounding exhaustive"; claims #1/#3/#4/#5 PARTIALLY-TRUE (flow exists in only 2 of 38 §s — §14, §26; rest = surface dumps; job is a *label* on a control inventory, not the driver). **Single biggest fix codex gave:** replace top-level surface-inventory with ONE canonical wireflow per journey; demote panel specs to appendix. **DID IT:** new **`reference/wireflows.html`** = 5 job journeys as left→right storyboards (J1 Make-a-page · J2 On-brand · J3 CMS · J4 Sign-off ★ · J5 Ship), 26 steps total, each step = mini editor-snapshot (active zone cobalt-hot) + **Karta→Dikhta** caption + `→ §N` ref into the appendix. Panels now appear *inside* flows as blocks, not the spine — real "job-first THEN blocks" (L17), in structure not just label. **Slop-drained the appendix:** removed all **27** `Stage-3-pass` self-certification lines + the `to-draw=0` tag from `editor-wireframe.html`; reframed it "Surface appendix — start with the journeys" banner → wireflows.html. **The deep lesson (capstone): I built completeness (37 surfaces, to-draw=0) and dressed it with job labels + pass-stamps — but completeness ≠ job-first; a coverage list with "job:" labels is still module-shaped. The founder felt it for 10 turns ("Webflow-like, job-based"); codex proved it. Flow IS the unit, not the screen.** Learning-record 0018. Also fixed a CSS bug this session: `.wf-why li` was `display:grid` → scattered the tag-number + text across cells (per-word wrapping); changed to block paragraphs w/ absolute tag gutter. **Course: 20 lessons · 15 references (+wireflows) · 18 learning-records.** NEXT: founder review the 5 flows; if shape holds, this becomes the canonical wireframe (the build plan's Phase −1 / the eventual /goal input).
- **WIREFLOWS HARDENED — /goal "complete the process one by one", 2026-06-26.** Founder ran `/goal copmate all the process one by one and do not stop`. Finished the codex-PARTIAL residue from the pivot turn: codex had confirmed the *structure* fix landed (sequences are top-level) but flagged a NEW problem — the fix doc had become **audit-response prose** ("Why this exists / the fix", "Koi ✓ pass theater nahi", "Appendix slop-drained: 27 lines…", "Fix driven by the codex JTBD/flow audit"), and the appendix still carried **stale process language** (Batch-N dividers, "A codex audit found…", "to-draw → 0"). **Drained all of it** → wireflows + appendix now read as pure artifact (intro = "The journey is the unit", source = "the journey is the unit, not the screen"; appendix banner = plain "top-level structure is the 5 journeys", no audit narration). Removed 5 `id="batch1..5"` dividers from the appendix + repaired the map's orphaned `#batch` links. **★ Caught + fixed real cross-ref drift (LR-0014 class) the founder's eye would've hit:** the wireflow `→ §N` refs were plain text authored against an OLD appendix order and had drifted — SEO §20→§19, domains §22→§21, version-history §24→§23, canvas §32→§30 (and its id is `canvas-controls`, not `canvas`), inline-text §33→§31. The *names* were the stable key; the *numbers* lied. Fix = converted all **26** refs to **clickable anchors** (`editor-wireframe.html#<id>`) with corrected numbers → "drop into a §N for the panel detail" now literally clicks through. Verified: 26/26 anchors resolve to existing appendix ids, 0 broken inter-doc links across all 5 docs (file + fragment checked), all tag-balanced, artifact-prose residue = 0. **Process takeaway: a self-fix can introduce its own slop — after restructuring, re-read the doc as a *reader who wasn't in the room*; defensive "look I fixed the thing" prose is itself the AI-slop pattern (LR-0018). And text cross-refs rot silently — make them real links so drift becomes a broken-link the validator catches (LR-0014).** Course unchanged: 20 lessons · 15 references · 18 learning-records. NEXT: founder review the 5 flows (now clickable into the appendix); if shape holds → canonical wireframe → Phase −1 demand gate.
- **JOURNEY SET COMPLETED + pre-Figma cleanup (founder: "Figma MCP se karenge, par pehle sari cheezein sahi karo"), 2026-06-29.** Founder dropped SVG, chose Figma-MCP recreate, but ordered: clean the source artifacts FIRST. Scope confirmed via AskUserQuestion: **complete the 4 reference artifacts (NOT the 108 dashboard screens); rail stays object-named (CONFIRMED).** Drew the **4 journeys codex flagged missing** into `wireflows.html`: **J1 Run-business** (sign-in→client→team→Vercel) · **J2 Start-site** (sites→new→editor handoff) · **J6 Run&measure** (live→analytics→fix→re-publish, the "run it" half) · **J6 Forms→leads** (drop→configure→submit→leads). **wireflows ab 10 journeys, saare 6 jobs covered** (★ wedge + J1 + J2 + J3×2 + J4 + J5 + J6×3). Counts propagated (6→10) to editor-wireframe banner + by-journey index (added J6-run/forms rows + J1/J2 dashboard note) + functionality-map. **D4 rail = CONFIRMED object-named** (shape-check report updated: settled, not reversible-pending). `wireflows.standalone.html` subordinated w/ stale banner (nothing consumes it; flagged not hand-synced). L24 stale cell fixed + founder's MCP-over-SVG decision noted in lesson. **Verified:** 10 journeys, 0 broken §N anchors, tags balanced (672/672 etc.), "10 journeys" consistent across set. Learning-record 0025. **Course: 24 lessons · 16 references · 25 learning-records.** Principle: clean upstream before forking to a 2nd tool (the gap you skip upstream you rebuild downstream). Still NOT done = correctness (10 coherent journeys, still no user contact — L23). NEXT: Figma-MCP recreate (incremental, Starter-cap aware), or remaining cleanup if any.
- **2nd CODEX AUDIT of the "clean" set caught a P1 dead-end + 3 drifts (founder ran `/codex` on wireflows + standalone + ia-tree), 2026-06-29.** I closed last turn calling the 10-journey set "complete + consistent + verified" — but my verification only ran *mechanical* invariants (anchor resolution, tag balance, journey-count). Codex's independent pass found **4 real issues those checks can't see**: **[P1]** J6 "Ship it" *ended on a dead-end* — `● Live` was step 4, "Restore safety net" was step 5, so a Ship journey terminated on "if it broke, restore." Fixed by reorder: safety-net = step 4 (honest, cloud snapshots are continuous), `● Live — the goal` = terminal step-5 hero. **[P2]** J5 jumped Agency-resolve→approved with no client-return/status-flip → sharpened step 5 to name the `In-review ➜ ✓ Approved` flip. **[P2]** surface-count drift: both wireflow files said "41 per-surface specs" but the appendix merged §6+§12 into §5 → **39** (my count-sweeps last turn checked the *journey* count, never the *surface* count). **[P2]** ia-tree self-contradiction: rail prose said locked spine `Insert·Pages·Styles·Site` but a J3 table row still said "4-mode editor (Build/AI/Design/Settings) — this IS the rail" → aligned to the spine, AI excluded. Codex confirmed OK: coverage (10 journeys / all 6 jobs), all anchors resolve, standalone stale-banner honest, ia-tree job-first. **Re-verified after fixes:** div 672/672 + 466/466, 19 anchors all resolve, no stray "41", no old rail string, "10 journeys" consistent, J6 ends on Live. Learning-record 0026. **Course: 24 lessons · 16 references · 26 learning-records.** Lesson: a *mechanical-invariant* pass and a *coherence* pass are different audits — passing the first ≠ the second; and the author's own coherence pass is blind to the author's framing ("of course Ship ends on live"). "Verified" was scoped to the checks I wrote, not absolute. Per `feedback_codex_iterate_until_clean`, the set isn't codex-clean until a pass returns nothing — a confirming 2nd codex pass is offered, not auto-spent (Figma work needs the ChatGPT-Plus quota). NEXT: optional confirm-pass, else Figma-MCP recreate.
- **CONFIRM PASS ran (founder: "confirm karo"), 2026-06-29.** 2nd codex pass CONFIRMED F3+F4 + canonical wireflows.html F1/F2 landed, and found **3 more**: standalone copy still had the **same J6 dead-end + J5 no-flip** (I'd only fixed canonical, leaned on the standalone's "stale" banner as cover), + **NEW** stale printed cross-ref `§12 inspector-full` in *both* files (anchor `#inspector-full` resolves — lives in §5 after §6+§12 merge — but the "§12" label drifted, LR-0014 class). All 3 fixed: standalone J6 → ends on `● Live`, J5 flip added, `§12`→`§5` both files. **Re-verified:** div 672/672 + 466/466, anchors 19 + 18 all resolve, no `§12`/`§6` printed refs, no "41", standalone J6 terminal = Live, canonical no regression. Deeper lesson (added to LR-0026): **"stale snapshot" excuses missing *content*, not a *broken journey* the copy still contains** — a fix in the canonical but not its derived copy is half-done in two files (refines LR-0020). **Course: 24 lessons · 16 references · 26 learning-records.** Per `feedback_codex_iterate_until_clean` (stop after 2 clean passes), this pass was NOT clean (3 found) → strictly wants a 3rd; but the 3 fixes were mechanical replays of already-validated changes (same J6 reorder codex OK'd on canonical + a one-token §-number), self-verified solid. 3rd codex pass = low marginal value, high quota cost. NEXT: Figma-MCP recreate (save remaining quota), unless founder wants the 3rd pass for certainty.
- **3rd CODEX PASS ran (founder: "3rd pass"), 2026-06-29.** CONFIRMED G1 (standalone J6 → ends on ● Live), G2 (standalone J5 flip), G3 (`§5 inspector-full` both wireflow files); mechanical sweep = 0 broken anchors, 0 §N mismatches in either wireflow file. **Found 1 [P2]:** the SAME §12 drift's last instance — `editor-wireframe.html:74` by-journey index (which I built in the D3 fix) still printed `§12 reach` for `#inspector-full`. Fixed → `§5 reach`. **Final whole-set sweep (my own, reproduces codex's checks):** editor-wireframe internal anchors — every printed §N matches its section heading (NONE mismatched); both wireflows — 0 broken anchors + 0 §N label-mismatch; only `§6/§12` string left is the intentional merge-doc prose at editor-wireframe:397. div 672/672 + 466/466 + 758/758. **The §12 merge-drift is now fully drained across all 3 files** (wireflows.html, standalone, editor-wireframe index) — root cause closed, not whack-a-mole. **Course unchanged: 24 lessons · 16 references · 26 learning-records.** Tally across 3 passes: pass-1 found 4 · pass-2 found 3 · pass-3 found 1 (the converging tail of one root cause). A 4th pass would near-certainly return AUDIT CLEAN (my sweep already shows zero §N mismatches anywhere) → not run, low value vs quota. The 4 reference artifacts are now codex-audited-clean for coherence. STILL open = correctness (no real-agency contact — L23). NEXT: Figma-MCP recreate.
- **PIVOT: design DONE → VALIDATE (founder asked "next kya karna," then chose "validate" over Figma), 2026-06-29.** Laid out the real fork honestly: **polish (Figma) vs prove (validate)**. Recommended prove — the design is done + codex-clean, every prior review (autoplan LR-0017 + L23) says the open risk is *correctness*, not coherence, and Figma polishes an unproven artifact (the "craft on guesses" / "build the wrong thing well" pattern). Founder chose **validate**. Built two artifacts: **`reference/agency-validation-plan.html`** = runnable 1-page demand-gate (who: 3-5 real agencies ≥3 client sites · Mom-Test method: their-life-not-your-idea, past-specifics-not-future-opinions, compliments≠data · 5 past-tense questions, zero "would you use Buildrik" · show design LAST · pull-vs-no-pull table · **gate: ≥3/5 spent-money-or-time pain → fund P0 build; <3 → wedge isn't the wedge, pivot to review-loop or stop**). **`lessons/0025-validate-with-real-agencies.html`** teaches the method (3-Q quiz, Mom-Test grounded — already in RESOURCES line 21). Key honesty: founder needs NO new artifact to run it — `design-handoff.html` (217 screens) + wedge journey already exist; Figma deferred (prettier, not truer). Learning-record 0027. **Course: 25 lessons · 17 references · 26 learning-records.** This is the arc finally crossing from *making* to *proving* — the rung L23 named but never taught HOW. NEXT (founder's to run, not mine): 3-5 agency calls → read pull → go/kill the wedge build. Figma only if a formal share/handoff is needed later.
- **TEACHING-STYLE SIGNAL: founder prefers worked examples over live practice, 2026-06-29.** Offered a live roleplay (I=skeptical agency "Sara", founder drives the 5 Mom-Test questions, I coach on slips). Founder declined to drive **three times** ("tum khud hi poch lo" → "is ko ap auto khud hi chala lo" → "okay bana lo ap jo bhee bana hai"). Read it honestly: he learns by *watching a complete annotated example*, not by type-practicing live. So I (1) ran the full Sara call myself end-to-end with inline `[why]`/`[coach]` annotations — Q1-Q5 + the pitch-trap moment + design-reveal-last + PULL verdict (1 of 5) + the 5 moves; then (2) made it a **reusable reference**: `reference/model-agency-interview.html` (course-styled, annotated, "re-read before each real call"), cross-linked into the validation plan + Lesson 25. This is validation-*enablement* (a model he rereads before real calls), not polish-avoidance. **Course: 25 lessons · 18 references · 26 learning-records.** Saved as memory `feedback_founder_prefers_worked_examples`. Apply going forward: teach via complete annotated worked-examples, not "now you try" live drills. NEXT unchanged: founder runs 5 real calls → bring notes back → read pull vs politeness together.
- **FIGMA RECREATE DONE — all 4 artifacts in Figma via MCP (founder: "Figma mein add karo, MCP ke saath, do not stop until done"), 2026-06-29.** Founder pivoted back to the deferred Figma work. I surfaced the "Starter cap" cost one more time, then started — **and the cap was wrong.** Two Figma MCP servers are connected: `claude_ai_Figma` (aamir/Starter, capped — source of the old memory) vs `plugin_figma_figma` (**saqib/Pro**, Full seat, no cap). `whoami` returned tier "pro". All 4 reference artifacts (ia-home-map · ia-tree · wireflows · editor-wireframe) captured into ONE file: **https://www.figma.com/design/RmtnWGlZX9Z3idP6f5vmLq** (Page 1, nodes 1:2–4:2, ~7k native-editable nodes), zero rate-limit hit. **Method:** `generate_figma_design` HTML-capture — served the artifacts on localhost, injected Figma's `capture.js` into temp copies, `open`ed each with a capture-hash, polled till complete; rect→Rectangle, text→Text, NOT a flat image. Cleanup: temp copies removed, server stopped, source + git untouched. Memory `reference_figma_mcp_starter_cap` rewritten (two servers, use Pro one, + recipe); MEMORY.md pointer updated; L24 lesson got a ✓ DONE note. Learning-record 0028. **Course: 25 lessons · 18 references · 28 learning-records.** Lesson: the days-old "cap" blocker was an **account/server mix-up, not a real limit** — `whoami` first before generalizing a tool's cap; and "export not rebuild" is literal (a localhost HTML render → native Figma layers). Figma now = the shareable/zoom-out view the founder wanted. STILL open (unchanged): correctness — real-agency validation (L23), founder's to run.
- **CODEX FORWARD-AUDIT of the design set (founder: "audit the Figma file, find broken ends/gaps, ensure we can add features"), 2026-06-30.** Honest constraint surfaced: codex is read-only sandboxed at the repo — can't reach the remote Figma URL; but the Figma = a capture of the 4 in-repo HTML artifacts, so auditing those = auditing the Figma. New lens (NOT the 3 coherence passes): broken-ends · gaps · extensibility · enhancements. Codex also spot-checked editor/dashboard/shared code. **Headline: structure sound, NO orphan problem — blockers are OUTCOME CLOSURE, not coherence.** 3 P1 dead-ends (a user starts but can't reach done): **J5 sign-off** (client approve not built; comments in dashboard iframe; share UI absent in editor), **CMS dynamic pages** (records stuck draft → unreachable via UI), **J6 custom domains** (verify broken). These are **product build work, not design-doc bugs** — and exactly what the agency-validation gates. Extensibility: spine has room (billing/AI-gen/marketplace fit J1/J2/J3) BUT `Site` rail = junk-drawer risk, CMS-under-Pages will sprawl, J5/J6 fractured across editor+dashboard+live. **My code-verification tempered 2 codex code-claims:** (1) "version history server-backed now" = UNVERIFIED (`useVersionHistory.ts` just wraps `composer.versions`; persistence not provable from the hook — left the design's IndexedDB note as-is, `feedback_phantom_bugs_static_analysis`); (2) shared-theme push = REAL+dashboard-homed and the backend even has **dry-run/rollback/preset schemas** (`theme.ts` D1/D2/D4) — design §36 already honestly says "ships in dashboard not editor", so NOT a stale note; the only real design gap = §36 wireframe under-draws what the backend already supports. **No stale-broken design notes to fix** (my initial "2 stale notes" guess was wrong — verified). Audit recorded: `docs/reviews/design-gaps-extensibility-audit-20260630.md`. Course unchanged (no new lesson/record — this is a review artifact). Lesson: a forward-looking audit on a coherence-clean set surfaces PRODUCT gaps (build backlog), not doc bugs — and verify codex's build-state claims against code before acting (it over-claimed version-history). NEXT: the 3 P1s are build backlog gated by validation; the only cheap design action = enrich §36 dry-run/rollback states (optional).
- **L24 (done): "Get your whole design into Figma (the quota-free route)"** — taught 2026-06-29 (Roman Urdu). Founder: "Figma mein saara design kaise banaoon jis se sab dekh sakoon?" Two reframes: **(1) export, not build** — design already exists as HTML (217 screens + IA + flows); Figma is an export target, redrawing = wasting finished work. **(2) On Starter plan, route = SVG-import, NOT MCP** — Figma MCP hard-caps (whoami+create+~1 write exhausts; founder's own `editor-wireframe.figma.README.md` confirms it was blocked → SVG route adopted). HTML→SVG→drag into Figma → rect/text/g become native-editable Rectangle/Text/Group, zero quota. **(3) "See everything" already solved** — `design-handoff.html` links all 217 + clickable prototype; Figma's distinct value = infinite-canvas zoom-out bird's-eye + comment + designer hand-off, the one thing HTML can't do. Plan = one Figma file, one page per artifact-set. **Status:** `editor-wireframe.figma.svg` ready (38 surfaces) · `wireflows.svg` STALE (5 flows, wedge missing) · ia-tree + 108 dashboard = no SVG · generator (`gen-*-figma-svg.mjs`) lost (scratchpad cleaned). `lessons/0024-whole-design-into-figma.html`. Learning-record 0024. RESOURCES += Figma SVG-import. **Course: 24 lessons · 16 references · 24 learning-records.** NEXT offered: rebuild generator → regen wireflows.svg (with wedge) + ia-tree.svg + dashboard SVGs; founder just drags into Figma.
- **WEDGE JOURNEY DRAWN (founder: "wedge journey draw karo"), 2026-06-29.** Drew the codex-audit #1 gap: a new **★ agency-wedge journey** at the TOP of `wireflows.html` (+ `.standalone.html`), 7 steps: clients/sites (dashboard) → master brand (§4) → push: select client sites (§36) → **HERO "5 sites will update" blast-radius confirm** (§36) → per-site result 4✓/1⚠ (§36) → **sign-off queue status-flip** (§16 + dashboard queue, the J5 gap codex added) → **HERO publish-all→live** (§14). Badge = **★** (not a J#) because it's the cross-job SPINE threading J1/J2→J4→J5→J6, not one of the 6 jobs. One journey collapses gaps #1 (wedge e2e) + #2 (J4 cross-site push, fixing the "wrong job" altitude) + J5-queue half of #4. **§36 ds-push orphan FIXED**: 0→3 journey refs. Updated scope note + footer (5→6 journeys) in both copies; added a ★ wedge row to editor-wireframe's by-journey index (resolves the §36 orphan there too). **Verified:** 6 journeys present, standalone synced, div tags balanced (465/465 both), 0 broken →§N anchors. **⚠ `wireflows.svg` is now STALE** (vector export, still 5 flows — can't hand-author a 7-step flow's coordinates; needs regen from HTML). Honesty kept in the flow-why: editor `theme.*` wiring still zero (LR-0017), so this is redesign-target not shipped, and correctness still needs real agencies (L23). The wedge journey materializes the L22 candidate-7th-job ("manage brand across all client sites" = the agency's true core job, bigger than single-site J4). NEXT: founder review the wedge flow; then the correctness proof = put it in front of agencies.
- **CODEX JOURNEY-COVERAGE AUDIT (founder: "codex deeply check karo, kis ki journey ni hai"), 2026-06-29.** Ran codex consult (high effort, 461k tokens, read actual files) on which jobs/flows have NO journey. Report = `docs/reviews/journey-coverage-audit-20260629.md`. **Headline: of 6 jobs, only J3 (Build) is correctly + completely journeyed.** Codex confirmed my L23 baseline, **added 1 gap I missed (J5 dashboard review-queue/status-flip — IA's actual success condition, never drawn)**, and **sharpened J4**: the J4 journey isn't missing a step — it's the WRONG JOB. wireflows:217 = "consistent across the whole *site*" (single-site); ia-tree:222 = propagation to "every selected *client site*" (cross-client). So J4 journeys single-site consistency while the real job is cross-client brand push (the wedge). **#1 gap = the agency wedge end-to-end** (ia-tree:142 "first slice") — codex: *"the doc that claims to prove the differentiator never draws the differentiator."* 7 real gaps ranked: 1 wedge-e2e · 2 J4 cross-site push · 3 J1 Run-business · 4 J5 dashboard queue · 5 J2 Start-site · 6 J6 run/measure · 7 Forms build→leads. **Corrections codex made:** §21a = tracker config not analytics-view (my citation sloppy); AI-inside-job = LEGIT (cross-cutting, no journey needed); onboarding splits (dashboard first-run real, editor onboarding legit); flagged my double-counting (wedge-e2e ≈ J4-push ≈ J1/J2-entry = one product bet at different zooms). Learning-record 0023. **Course: 23 lessons · 16 references · 23 learning-records.** Lesson: a coherence audit by the author finds missing *steps*; an independent reviewer (codex) finds the *mis-aimed journey* — same artifact, deeper error class (global-shape, LR-0018/0017). Countable coverage (5 journeys) hid correctness (1/6 jobs journeyed right) AGAIN. **NEXT (recommended): draw the agency-wedge end-to-end journey** — collapses gaps #1+#2+J5-queue into one artifact + forces J1/J2 entry; highest-leverage missing piece. Then (still) validate with real agencies.
- **L23 (done): "Audit your jobs with your wireframes — coherence, not correctness"** — taught 2026-06-29 (Roman Urdu). Founder pushed: "jobs ko wireframing se audit nahi kar sakte?" Answer = qualified yes. **Coherence audit (wireframe, runnable now):** 5 tests — orphan surface · empty job · seam · merge · dead-end. **Correctness (right jobs?): only users** — wireframes derived FROM jobs, so auditing jobs against them = circular. **Ran it live + found a big hole:** ★ **§36 Shared DS push (the agency wedge, #1 differentiator) is journey-ORPHANED** — `ds-push` appears in 0 of 5 wireflows. So J4 lost its biggest step (it only journeys one-site consistency, never cross-site push), OR there's a hidden 7th job "keep all clients on-brand from one place." Also **J6 seam**: journey shows only Ship (SEO→domain→publish→live), never "run it"/grow — the L22 grow-candidate with evidence. **The coherence audit's value: it can't prove the right jobs, but its structural breaks point at missing/miscut jobs** (orphan wedge = the product's core bet has no flow). `lessons/0023-audit-jobs-with-wireframes.html`. Learning-record 0022. **Course: 23 lessons · 16 references · 22 learning-records.** Ties to LR-0017 (wedge under-BUILT: zero theme.* editor wiring) — now also under-DESIGNED in the journeys. NEXT offered: draw the wedge as a 6th journey (capture→select clients→blast-radius confirm→push→per-site result), then decide J4-step vs 7th-job, then validate with users.
- **L22 (done): "Is six jobs right? — what counts as a job"** — taught 2026-06-29 (Roman Urdu). Founder asked the question the whole IA rests on: "sirf 6 jobs hain ya aur bhi?" Answer = two layers. **Well-formed?** Yes — each of the 6 is an outcome, one home, top-level 5-6; the list is 6 because AI = cross-cutting tool (not a job) and auth/save/states/confirm = substrate (not jobs). Test taught = **"I came here to ___"** (blank = job only if it's a UI-free outcome; step/tool/plumbing ≠ job). **Complete/correct?** Unknown — the 6 were **reverse-engineered from the backend-map (code), not users**, which is why they map so cleanly onto modules. 3 candidate 7th jobs folded in: grow/measure (J6), maintain-a-live-site (J3+J6 loop), publish-content-regularly (J3 CMS); splitting now = empty job (LR-0005 ghost). **Biggest surfaced thread: the entire IA→flows→wireframe chain sits on a job list that has never met a user** — the only proof is Krug's 5-user test (mission pro-frontier). `lessons/0022-is-six-jobs-right.html`. Learning-record 0021. **Course: 22 lessons · 16 references · 21 learning-records.** NEXT offered: write a 5-user test script to actually validate the 6-job carve, OR draw a candidate 7th-job test.
- **JOB-FIRST SHAPE-CHECK RUN + ALL DRIFTS FIXED (/goal "do not stop until fixed+verified"), 2026-06-29.** Ran the L21 shape-check across all 4 artifacts (ia-home-map · ia-tree · wireflows · editor-wireframe) vs the 6-job SSOT. **Finding: structure already job-first everywhere (LR-0018 fix held) — the real defects were 4 *consistency* drifts** (each doc job-first alone, but not one shared job vocabulary). Flag/fix list = `docs/reviews/shape-check-job-first-20260629.md`. **D1 (HIGH, LR-0014 class):** wireflows numbered its flows `J1–J5` but the IA uses `J1–J6` with different meanings (wf "J1 Make-a-page" = IA "J3 Build"; IA "J1" = Run-the-business) — same key, different referent. wireflows was the lone outlier (editor-wireframe + both IA docs already used J3-J6). Fixed: renumbered to `J3·J3·J4·J5·J6` across **3 copies** (wireflows.html · .standalone.html · .svg) + fixed the editor-wireframe "(J4/J5)" back-ref → "(J5)". **D2 (MED):** wireflows had no scope note → added ("4 editor jobs here; 2 dashboard jobs in docs/reviews/wireframes/"). **D3 (LOW):** editor-wireframe appendix index was pure module A–Z → added a "By journey (job-first)" index above it (5 flows → their §N). **D4 (MED, reversible design fork):** ia-tree's editor rail was stale (`Build·Design·AI·Settings`) vs the code-locked spine (`Insert·Pages·Styles·Site`), and listed **AI as a rail slot** against its own "AI is not a place" → aligned to the locked spine, AI → topbar ✨ (cross-cutting), added "job-grouping lives in the journeys, not the rail labels" (Webflow model: rail=tool/object, jobs=flows). **Verified:** 0 unresolved anchors (all wireflow §N + by-journey links resolve), all tags balanced (div/section), no stale J-numbers, one J# = one job across every doc. Learning-record 0020. **Course: 21 lessons · 15 references · 20 learning-records.** Key rule: once structure is job-first, the next failure mode is *vocabulary* (numbers are cross-refs and rot like links); a shape-check separates the mechanical fix (D1-D3) from the design fork (D4 — surfaced, not silently decided). NEXT (founder call): keep the object/tool rail (recommended, Webflow-aligned) or pursue a job-named rail (`Build·Brand`, a real redesign).
- **L21 (done): "The design spine — Jobs → IA → Flows → Wireframe"** — taught 2026-06-29 (Roman Urdu). Founder came back lost ("existing app ki IA/flow/wireframe professional banani hai bina code — kaun sa goal run karoon?"). The question wants a single button; none exists. Diagnosis = **lost through-line, not missing work** (LR-0015 again): the founder had already produced all four design artifacts across the arc (ia-home-map=jobs · ia-tree=IA · wireflows=flows · editor-wireframe=wireframe). Taught the design lane as ONE 4-rung spine where each rung is the fruit of the one above + a "you are here" table marking all 4 done → so the professional finish is a **job-first shape-check, not a rebuild**. Reinforced the capstone (LR-0018): module-first (`Insert·Media·Pages·Design`) = slop; job-first (build·brand·sign-off·ship) = pro; a `Job:` label on a control inventory is still module-shaped. `lessons/0021-the-design-spine-jobs-to-wireframe.html`. Learning-record 0019. **Course: 21 lessons · 15 references · 19 learning-records.** NEXT offered: run the job-first shape-check across the 4 artifacts against the 6 jobs → flag/fix list, no code.
- **L16 (done): "Lock the spine, dock the rest"** — taught 2026-06-25 (Roman Urdu). Learner asked the *name* for "core editor design doesn't break when features are added — Add button same position every version; core central, helpers outside." Taught the 3-layer vocabulary (one idea, three altitudes): **spatial stability/consistency** (Nielsen #4, the visible promise) → **stable core + periphery** (structure, = macro of #9) → **pace layering** (Stewart Brand, the deep why: fast features must not move the slow core). Code analog for AI-briefing = **Open–Closed Principle**. One handle = "lock the spine, dock the rest." Worked example = ➕Insert stays rail-slot #1 forever; new element types dock *inside* the Insert panel, never a new rail icon. **Constitution grew to 15** (#15 "Lock the spine; dock additions to the edge", Structure group). `lessons/0016-lock-the-spine-dock-the-rest.html`. Learning-record 0009. **Directly arms the rail-lock:** locking Insert·Media·Pages·Design = locking the spine; the next 6 months of features (CMS/reviews/comments/animations/SEO) must pass the **spine test** (move-anything? → which home? → common/rare? → new slot only for a new user-job) instead of each grabbing a rail icon. NEXT offered: "run the spine test on the rail" — walk the 4 locked slots + 5 incoming features through the test to show what docks where, then the clickable mockup.
- **HOME-MAP RUN DONE (learner: "run the home-map"), 2026-06-23:** full pass output = `docs/reviews/ia-home-map-20260623.md`. All ~100 features placed: 17 backend clusters → **6 user jobs**; each feature one home (Editor/Dashboard) + verdict, grounded in `feature-backend-map.md` (the 2026-06-23 code audit = the live-code confirmation; no fresh browse). Counts ≈ keep 55 · fix 16 · 6 dedup clusters · hide 8 · cut 1 (Export). **Top-level nav: 17 → 5 (dashboard) + 4 (editor rail)** — both skeletons drawn in the doc = the Step-2 input. AI reframed as cross-cutting (not a cluster — lives inside jobs). Auth/states/confirm pulled out as **substrate** (not nav). The `fix` column = the Step-3 worklist, ordered by user pain. HTML reference `ia-home-map.html` now points at the run as canonical. Same honesty caveat as 0005: merge/fix rows touching state-completeness still want a live state-audit before building. NEXT = Step 2 (turn skeletons into real nav) OR draft the redesign plan from this worklist → THEN plan-design-review / plan-ceo-review (the L12 sequence step 4).

## Product facts surfaced (for honesty in lessons)
- Real-time collaboration is DEMO-ONLY / not shippable (6 P1 distributed-systems bugs, prior codex review 2026-06-12). Do not promote it to prime UI. Design the presence spot but gate the feature.

## Build-prompt practice (L11 applied)
- 2026-06-22: learner asked for a full home-page build-prompt modeled on ranklur.com (animations + aesthetics + sample video). Delivered `docs/reviews/homepage-build-prompt-ranklur-20260622.md` — L11 build-prompt skill at FULL-PAGE scale. Studied ranklur via WebFetch (exact sections + copy; visuals/motion specified from premium-SaaS best-practice + brand direction, not a pixel-clone). Beats reinforced: (1) specify EVERY section so the AI can't guess, (2) ONE reusable animation system not per-element motion, (3) explicit ANTI-SLOP guardrails (prefers-reduced-motion, no autoplay-sound, motion-serves-hierarchy) — answers the learner's "AI slop" pain, (4) sample assets + swap-markers so it runs first-build. Formalize as a lesson if revisited.

- **L26 (done): "Prompt the AI to review your Figma file"** — taught 2026-06-30 (Roman Urdu). Founder asked the *meta* skill: "Figma file MCP se access karni hai — usko review/issues-nikalne ke liye kya prompt likhoon?" Used the audit I'd just run on `RmtnWGlZX9Z3idP6f5vmLq` as the live worked-example (per `feedback_founder_prefers_worked_examples`). **Core teachable: MCP sees structure, not intent — the AI finds only what you give it criteria to find; issue-list = criteria-list.** "Review my file" = vague mush; aim it with LENS + CRITERIA + OUTPUT. Taught the 4-slot prompt (ACCESS · LENS · CRITERIA · OUTPUT) + 3 lenses (hygiene = empty/zero-size/overlap, pure structure; brand = fonts/colours vs allow-list; coverage = every job/flow has a frame + ends on a real state). Proved it with the actual table: each of the 4 issues I found (empty frame · scattered captures · 37-font sprawl · 125 zero-size nodes) traced back to ONE supplied rule — drop the font allow-list and the sprawl is never caught. Two honesty beats: (1) MCP scan = coherence+hygiene only (L23) — a spotless scan ≠ validated product, correctness still needs real agencies; (2) `findAll` = structure (tree), `get_screenshot` = the eye (renders frame to an image) — a full review pairs both. New reference `reference/figma-review-prompt.html` (reusable recipe + the worked prompt for his file). `lessons/0026-prompt-the-ai-to-review-figma.html`. Primary source: Anthropic "Be clear & direct." **Course: 26 lessons · 19 references · 28 learning-records.** No new learning-record (skill lesson, not a new meta-insight). NEXT offered: run the worked prompt on his file ("scan it") → table + screenshots; or back to validation (the still-open correctness rung).

- **RECONCILIATION MASTER PROMPT built (founder via /teach, Roman Urdu: "prompt likh do jo her cheez ko pakre meri existing wireframing aur raw designs mein se"), 2026-07-02.** The ask = L26's prompt skill at FULL-PIPELINE scale: codebase → Raw-Design diff → additive Figma fix → happy-path wireframe rebuild → verify. Delivered as worked example (per `feedback_founder_prefers_worked_examples`), NOT a new lesson — it's applied L26 + the whole constitution turned into gates. **New reference: `reference/design-reconciliation-prompt.html`** — 6-phase prompt (0 truth-table from existing inventories + grep spot-verify · 1 Figma read · 2 gap diff w/ the founder's exact 5 verdicts missing/incomplete/duplicated/unclear/misaligned · 3 additive Raw-Design fix · 4 "Wireframe v2 — Happy Path" page: ★ wedge Row-0 + 6 job rows mirroring wireflows' 10 journeys, branch-states drawn below · 5 mechanical+coherence audits clean ×2) with 3 founder GATES (0/2/4) so it can't run as a redesign-button (LR-0012). Criteria baked in = job-first (LR-0018) · zero ghosts #13 · one home #14 · locked rail #15 · tools-near-work #16 · honesty tags 🟡⚠️/dashboard-not-editor · draw-before-claim (LR-0015) · journeys end on done (LR-0026) · code-over-docs (stale-doc trap) · multi-package grep · Pro Figma server + /figma-use first · never delete founder frames. Phase-4 route decision surfaced w/ rec: fix canonical wireflows.html then capture (HTML stays SSOT, LR-0024/0028) vs Figma-native (then banner the HTML superseded — ONE canonical either way). Honest ceiling kept: reconciled ≠ validated (L23/L25 unchanged). NEXT: founder pastes it (rec: "run Phase 0–2 only" first, read-only) — or says "run Phase 0" here.

- **RECONCILIATION MISSION EXECUTED same-day (founder: /goal hit 4000-char limit → "start karo"), 2026-07-02.** The L26-scale prompt ran end-to-end inline (mission spec `docs/reviews/design-reconciliation-mission-20260702.md`, run log `…runlog-20260702.md`). Outcome: **all 6 phases complete** — 74-feature truth table (35 grep checks; 13 Δ corrections incl. interactions 13/13-wired, edit-scope + inline-AI NOT-built, custom-CSS dead-code, domains-verify real); Figma reality mapped (1 page, ~90 frames, 5 bands — prior sessions had already closed fa-flow-audit's 12 critical gaps by drawing); additive fixes (Reality-Sync board + 6 chips + p2-micro-flows + 4 renames); **NEW page "Wireframe v2 — Happy Path"** (★ wedge 13 steps + 7 job rows, 57 cells, states strips, two-axis chip legend, honesty footer); mechanical audit CLEAN ×3 + independent fresh-eyes coherence 20→7→3→1→**PASS** (5 passes — the codex-iterate-until-clean discipline transplanted to a Claude subagent, worked). **Canonical flipped: Figma file now owns the journey set; wireflows.html bannered superseded-for-journeys** (LR-0026 one-canonical rule applied in reverse). Teaching notes: (1) the founder's felt gap ("happy path missing") was structurally real — the file was a 90-frame LIBRARY with a connections-map (m0), no ordered screen spine; the v2 page IS that spine. (2) New reviewer-loop lesson: pass-4 "regression" was my paraphrase, not the canvas — send VERBATIM canvas text to the reviewer, never summaries. Ceiling unchanged: coherent ≠ correct; agency validation (L25) still the open rung.

## Open loops
- A live UX audit is in progress in the project root: `ux-audit-20260621.md` (Sections A–D done; E/F/G/H pending). The redesign work feeds the lessons.
- The learner now critiques designs first (caught a real error in my mockup — see learning-record 0002). Shift to: show a design → learner critiques → compare → lesson from their critique.
- Records: 0001 (worked-examples method) + 0002 (learner reached applied judgment — milestone).
