{
 "slug": "zz-f22-counts",
 "page": "1:3",
 "rows": [
  {
   "op": "rename",
   "id": "1776:8373",
   "name": "07 · Brand · 52",
   "expect": "07 · Brand · 51",
   "why": "F22 — the title claims 51 boards; the section holds 52. Count derived from the section's own children at read time, not hand-typed: the 13:39 baseline said Inspector 52 and a read four hours later said 56, so a typed number is stale the day it is written. Rename only — deliberately NOT order-sections.mjs, which also does s.x=0; s.y=y on every section and would re-lay-out the page."
  },
  {
   "op": "rename",
   "id": "1776:8374",
   "name": "16 · History · 49",
   "expect": "16 · History · 43",
   "why": "F22 — the title claims 43 boards; the section holds 49. Count derived from the section's own children at read time, not hand-typed: the 13:39 baseline said Inspector 52 and a read four hours later said 56, so a typed number is stale the day it is written. Rename only — deliberately NOT order-sections.mjs, which also does s.x=0; s.y=y on every section and would re-lay-out the page."
  },
  {
   "op": "rename",
   "id": "1776:8375",
   "name": "03 · Layers · 35",
   "expect": "03 · Layers · 29",
   "why": "F22 — the title claims 29 boards; the section holds 35. Count derived from the section's own children at read time, not hand-typed: the 13:39 baseline said Inspector 52 and a read four hours later said 56, so a typed number is stale the day it is written. Rename only — deliberately NOT order-sections.mjs, which also does s.x=0; s.y=y on every section and would re-lay-out the page."
  },
  {
   "op": "rename",
   "id": "1776:8376",
   "name": "06 · Content · 49",
   "expect": "06 · Content · 46",
   "why": "F22 — the title claims 46 boards; the section holds 49. Count derived from the section's own children at read time, not hand-typed: the 13:39 baseline said Inspector 52 and a read four hours later said 56, so a typed number is stale the day it is written. Rename only — deliberately NOT order-sections.mjs, which also does s.x=0; s.y=y on every section and would re-lay-out the page."
  },
  {
   "op": "rename",
   "id": "1776:8378",
   "name": "15 · Publish · 25",
   "expect": "15 · Publish · 24",
   "why": "F22 — the title claims 24 boards; the section holds 25. Count derived from the section's own children at read time, not hand-typed: the 13:39 baseline said Inspector 52 and a read four hours later said 56, so a typed number is stale the day it is written. Rename only — deliberately NOT order-sections.mjs, which also does s.x=0; s.y=y on every section and would re-lay-out the page."
  },
  {
   "op": "rename",
   "id": "1776:8379",
   "name": "02 · Insert · 29",
   "expect": "02 · Insert · 28",
   "why": "F22 — the title claims 28 boards; the section holds 29. Count derived from the section's own children at read time, not hand-typed: the 13:39 baseline said Inspector 52 and a read four hours later said 56, so a typed number is stale the day it is written. Rename only — deliberately NOT order-sections.mjs, which also does s.x=0; s.y=y on every section and would re-lay-out the page."
  },
  {
   "op": "rename",
   "id": "1776:8381",
   "name": "08 · Inspector · 56",
   "expect": "08 · Inspector · 51",
   "why": "F22 — the title claims 51 boards; the section holds 56. Count derived from the section's own children at read time, not hand-typed: the 13:39 baseline said Inspector 52 and a read four hours later said 56, so a typed number is stale the day it is written. Rename only — deliberately NOT order-sections.mjs, which also does s.x=0; s.y=y on every section and would re-lay-out the page."
  },
  {
   "op": "rename",
   "id": "1776:8382",
   "name": "17 · Compare · 17",
   "expect": "17 · Compare · 16",
   "why": "F22 — the title claims 16 boards; the section holds 17. Count derived from the section's own children at read time, not hand-typed: the 13:39 baseline said Inspector 52 and a read four hours later said 56, so a typed number is stale the day it is written. Rename only — deliberately NOT order-sections.mjs, which also does s.x=0; s.y=y on every section and would re-lay-out the page."
  },
  {
   "op": "rename",
   "id": "1776:8383",
   "name": "18 · Review · 39",
   "expect": "18 · Review · 38",
   "why": "F22 — the title claims 38 boards; the section holds 39. Count derived from the section's own children at read time, not hand-typed: the 13:39 baseline said Inspector 52 and a read four hours later said 56, so a typed number is stale the day it is written. Rename only — deliberately NOT order-sections.mjs, which also does s.x=0; s.y=y on every section and would re-lay-out the page."
  },
  {
   "op": "rename",
   "id": "1776:8385",
   "name": "01 · Shell · 56",
   "expect": "01 · Shell · 34",
   "why": "F22 — the title claims 34 boards; the section holds 56. Count derived from the section's own children at read time, not hand-typed: the 13:39 baseline said Inspector 52 and a read four hours later said 56, so a typed number is stale the day it is written. Rename only — deliberately NOT order-sections.mjs, which also does s.x=0; s.y=y on every section and would re-lay-out the page."
  },
  {
   "op": "rename",
   "id": "1776:8387",
   "name": "21 · Settings/S7 · 49",
   "expect": "21 · Settings/S7 · 48",
   "why": "F22 — the title claims 48 boards; the section holds 49. Count derived from the section's own children at read time, not hand-typed: the 13:39 baseline said Inspector 52 and a read four hours later said 56, so a typed number is stale the day it is written. Rename only — deliberately NOT order-sections.mjs, which also does s.x=0; s.y=y on every section and would re-lay-out the page."
  },
  {
   "op": "rename",
   "id": "1776:8388",
   "name": "23 · Journeys · S-flows · 109 — S-numbers are a journey narrative, not a screen index",
   "expect": "23 · Journeys · S-flows · 107 — S-numbers are a journey narrative, not a screen index",
   "why": "F22 — the title claims 107 boards; the section holds 109. Count derived from the section's own children at read time, not hand-typed: the 13:39 baseline said Inspector 52 and a read four hours later said 56, so a typed number is stale the day it is written. Rename only — deliberately NOT order-sections.mjs, which also does s.x=0; s.y=y on every section and would re-lay-out the page."
  },
  {
   "op": "rename",
   "id": "1779:3",
   "name": "13 · Command palette · 15 — two palettes ship: shell ⌘K and canvas ⌘⇧P — one decision open",
   "expect": "13 · Command palette · 13 — two palettes ship: shell ⌘K and canvas ⌘⇧P — one decision open",
   "why": "F22 — the title claims 13 boards; the section holds 15. Count derived from the section's own children at read time, not hand-typed: the 13:39 baseline said Inspector 52 and a read four hours later said 56, so a typed number is stale the day it is written. Rename only — deliberately NOT order-sections.mjs, which also does s.x=0; s.y=y on every section and would re-lay-out the page."
  },
  {
   "op": "rename",
   "id": "1779:5",
   "name": "09 · Canvas · 16",
   "expect": "09 · Canvas · 14",
   "why": "F22 — the title claims 14 boards; the section holds 16. Count derived from the section's own children at read time, not hand-typed: the 13:39 baseline said Inspector 52 and a read four hours later said 56, so a typed number is stale the day it is written. Rename only — deliberately NOT order-sections.mjs, which also does s.x=0; s.y=y on every section and would re-lay-out the page."
  },
  {
   "op": "rename",
   "id": "1779:6",
   "name": "22 · Ecommerce · 6 — [not-implemented] ORDERS only. Narrower than first written: the cart and Stripe checkout runtimes exist and are wired into export (StripeInjector.ts via ExportEngine.ts:40), and are inert only because no screen writes config.stripe. What genuinely does not exist is any order, fulfilment or customer model.",
   "expect": "22 · Ecommerce · 5 — [not-implemented] ORDERS only. Narrower than first written: the cart and Stripe checkout runtimes exist and are wired into export (StripeInjector.ts via ExportEngine.ts:40), and are inert only because no screen writes config.stripe. What genuinely does not exist is any order, fulfilment or customer model.",
   "why": "F22 — the title claims 5 boards; the section holds 6. Count derived from the section's own children at read time, not hand-typed: the 13:39 baseline said Inspector 52 and a read four hours later said 56, so a typed number is stale the day it is written. Rename only — deliberately NOT order-sections.mjs, which also does s.x=0; s.y=y on every section and would re-lay-out the page."
  }
 ]
}