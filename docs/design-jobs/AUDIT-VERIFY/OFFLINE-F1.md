# F01–F14 checked against this repo's own record — offline, zero Figma calls

**Scope:** ids `F01`–`F14` of `docs/design-jobs/AUDIT-VERIFY/ALL-FINDINGS.json`
(48 findings; `M01`–`M20` and `F15`–`F28` are somebody else's slice).
**Method:** the local record only — `V2-TO-V1/*`, `findings/*.jsonl`, `applied/`,
`queue-state.json`, the render-defect censuses, plus the parent session's own
fresh `b*.tsv` dumps already sitting in `AUDIT-VERIFY/`. No Figma call was made.

---

## Two facts about the evidence base, before any verdict

### 1. `RENDER-DEFECTS-PAGE-AFTER.txt` is a rate-limit stub. The AFTER data is in the JSON beside it.

The brief points at `V2-TO-V1/RENDER-DEFECTS-PAGE-AFTER.txt` as the post-arc
census. That file is **138 bytes** and its entire contents are:

```
RENDER-DEFECTS-PAGE-AFTER.txt:1  RATE LIMITED — You've reached the Figma MCP tool call limit for your Full seat on the Professional plan. Upgrade your seat or plan for
```

The sweep itself **did** complete and its results are in the state file the
runner writes as it goes: `V2-TO-V1/.render-defects-after.json` (mtime 18:19,
`__v:3`), **376 rows across 27 sections**. That is the number `COVERAGE.md`
itself reports, so the JSON is the authoritative AFTER census:

```
COVERAGE.md:712  before, 29 of 29 sections ......... 413
COVERAGE.md:713  after,  27 of 29 sections ......... 376
COVERAGE.md:714  1938:8372, measured separately ....   6   (unchanged)
COVERAGE.md:724  **382 is the current page total, and every digit of it is a detector reading.**
```

The BEFORE census is `V2-TO-V1/RENDER-DEFECTS-PAGE.txt` / `.render-defects-state.json`
(413 rows, 29 sections, mtime 18:07). **Every `render_defect_evidence` row below
is quoted from `.render-defects-after.json`, with the BEFORE row beside it where
the two differ.** I flattened both to text to diff them; nothing was written back.

### 2. F01–F14 are the audit's **first**-pass findings, not its refreshed pass.

`audit2.txt:941` closes the modal section and opens the editor-wide one:

> *"Correction order: resolve M01–M09 first … **The initial editor-wide findings
> below remain relevant.** This pass audited the design; it did not modify any
> Figma node."*
> `audit2.txt:943` — *"**Initial editor-wide findings**"*

Every `F` finding is `kind: "Visual"`; every `M` finding carries a freshness
label (`Fresh screenshot`, `Retained complete text`). So F01–F14 rest on
screenshots taken in the **first** structural pass, which the audit's own preamble
says was cut short — *"Figma then returned its tool-call limit, preventing the
remaining per-state visual checks"* (`audit2.txt:10`). The V2→V1 arc was writing
to page `1:3` continuously from **04:40** (`queue-state.json` first receipts) to
**18:31**. A first-pass screenshot is therefore potentially many hours behind the
file, and at least one finding below (F05) is demonstrably behind it.

---

## The table

| id | already_known | disposition | render_defect_evidence (AFTER, 18:19) | verdict_offline |
|---|---|---|---|---|
| **F01** Selection disagrees across the editor | **YES** — `findings/C.jsonl:40` `D-C-40` *"On the canonical selected-state board, four surfaces name the selection 'Section' while the Layers panel highlights a heading… Layers row 'heading-that-is-long' is filled/selected; the canvas ring, the selection pill ('Section · Hero'), the inspector header ('Section') and the footer ('Section · Hero · 680 × 250') all say Section."* Measured on `199:205`, the sibling the audit also names. | **never recorded as work.** `D-C-40` appears exactly once in the whole tree — no verdict row, no register row, no checklist row, no queue row. Not in `VERDICTS.jsonl`, not in `REGISTER.md`, not in `CHECKLIST.md`. The V2→V1 register is built from the `UX-*` lanes; the `D-*` audit lane was never carried into it. | `CONTAINER 52:2 S1 · Editor — ASSEMBLED (1440, dra 52:128 "control" right +4` and `… 462:3961 "Canvas toolbar (floa" bottom +8, top +8`; `CONTAINER 199:205 Shell state 3 · Element selected 199:374 "control" right +4` / `… 463:4208 "Canvas toolbar (floa" bottom +8, top +8` / `… 199:404 "selection box" top +18`. **Identical to BEFORE** (`RENDER-DEFECTS-PAGE.txt:350-351`, `:316-318`). No row addresses selection identity — the detector measures boxes, not agreement. | **LIKELY-REAL.** `DISCOVERY.md:85` (`"heading-that-is-long"`), `:72`/`:133` (`"Section · Hero"`), `:156` (`52:58 "⬚ Section"`) confirm all four strings coexist on `52:2` in the 16:20 dump; the arc's own lane measured the same contradiction on `199:205` and nothing ever acted on it. Which Layers row carries the *highlight fill* is not in any text dump — that half is UNDETERMINED offline. |
| **F02** Displayed fill ≠ selected section | **PARTIAL** — the same `findings/C.jsonl:40` records it one board over: *"The inspector also shows BACKGROUND Fill #1A56DB while the hero on the canvas renders white."* For `52:2` specifically the record is **silent**. Related but different: `findings/B.jsonl:32` `D-B-32` and `findings/FIG-J.jsonl:25` are swatch-vs-*value* mismatches (`#1A57DB` chip beside `#1A1A1A`), not swatch-vs-canvas. | **never recorded** for `52:2`; **PENDING** for `199:205` (same orphaned `D-C-40`). | No row for this board on this defect — `52:2`'s only AFTER rows are the two `CONTAINER` rows above. A colour mismatch is invisible to a geometry detector by construction. | **LIKELY-REAL, unverifiable offline.** `DISCOVERY.md:109` confirms `52:131 "#1A56DB"` sits under `52:124 "BACKGROUND"` / `52:127 "Fill"` on the live board, and the arc measured the identical mismatch on the sibling. The canvas hero's actual fill is a paint value in no local dump and there is no `shots/` screenshot of `52:2` — **needs Figma (or a screenshot) to close.** |
| **F03** Audit notes and overlapping blocks obscure the Pages panel | **YES, and the arc caused it.** `plans/pages-nodes-02-tree.json` rows 8 and 9 clone two engineering paragraphs **into** board `140:2` — `note/UX-B-13` and `note/UX-B-32`, each 248×117 — landed `OK` at `queue-state.json:2196,2202`. The record's own stated convention forbids this: `COVERAGE.md:69-71` — *"the file's own convention for commentary is a **caption BELOW the board**, parented to the section — not a node inside the board, which on a full 812-tall auto-layout panel would land out of bounds anyway."* Nothing names the Pages violation. | **landed fix that IS the defect.** `CHECKLIST.md:181` `UX-B-13 pages … IMPLEMENTED VERIFIED — read-back`; `CHECKLIST.md:199` `UX-B-32 pages 140:2 · 1717:17234 … IMPLEMENTED VERIFIED — read-back`. Both read back green; neither read-back could see where the notes landed. | `CONTAINER 140:2 Pages · tree 140:7 "Search" bottom +36` and `TEXTOVER 140:2 Pages · tree 2838:12106 by 36` — **unchanged from BEFORE** (`RENDER-DEFECTS-PAGE.txt:165-166`). `2838:12106` is the `⚂ Structure` link (`queue-state.json:2208`, `plans/pages-nodes-02-tree.json` row 11). | **LIKELY-REAL — confirmed by a post-audit live read.** The parent session's own dump `b09.tsv` (18:35) gives: `2838:12104 [100,16548,248,117] "UX-B-13 — '+ Add page' routes through the uniqueness helpers…"` and `2838:12105 [100,16665,248,117] "UX-B-32 — Pages owns the per-page fact…"`, both `vis=True`, both parented to `Pages · tree`. Fourteen `hotspot/state · …` frames are `v=True` across y 16407–16782 — the "pale rectangular blocks across the lower panel". `2838:12106 '⚂ Structure' [301,16068,66,18]` sits inside `140:7 Search [100,16014,280,36] clips=true`, i.e. **36px below its clipping parent's floor**, and overlaps `140:16 '3'` by 7×6px. Every clause of F03 is arithmetically true. |
| **F04** Media footer actions and accepted-format text overlap | **YES** — the exact pair is in the arc's own triage. `OVERPRINT-LARGE-REVIEW.md:16` lists **248px × 12 boards**: *"`Media · grid`, `· filtered`, `· folder-scoped`, `· uploading`, `· upload-failed`, `· quota-warn`, `· quota-full`, `· empty`, `· load-error`, `· loading`, `· no-results`, `· 145:250`"*, and `:23` — *"**Twelve of the forty-five are one Media panel, cloned across every state it has.**"* The colliding strings are the arc's own writes (`plans/media-05-node-edits.json:79` `"Sort and list view live in the full library ⇱"`, ×12). | **PENDING, explicitly.** `OVERPRINT-LARGE-REVIEW.md:62` — *"**Not done in this arc.** Recorded with the node ids so it is one measurement away from actionable, and explicitly NOT counted as repaired anywhere in `COVERAGE.md`."* `COVERAGE.md:637-639` — *"**45 pairs above 60px**, including twelve at 248px … are **held for a human read**."* | `OVERPRINT 144:2 Media · grid 2838:12022 x 2838:12023 by 185` — **still present**, on all twelve boards (`145:2`, `145:49`, `145:96`, `145:148`, `145:199`, `145:250`, `145:359`, `453:3931`, `777:4093`, `782:4353` all carry the same `by 185` row). BEFORE had **two** pairs per board (`RENDER-DEFECTS-PAGE.txt:47-48`: `2838:12021 x 2838:12022 by 50` **and** `2838:12022 x 2838:12023 by 248`); the ≤60px fixer removed the `by 50` pair and the 248 shrank to 185. Also still live: `TEXTOVER 144:2 Media · grid 1720:17421 by 4`. | **LIKELY-REAL — confirmed by a post-audit live read.** `b09.tsv` (18:35) measures the overlap directly: `185x40px 2838:12023 'Sort and list view live in the full ' >< 2838:12022 'Accepts PNG, JPG, GIF, WebP, AVIF, S'` — the audit's sentence, to the pixel. It also confirms the rest: `2838:12021 'Aa Fonts' [313,21605,50,40]` is **40px tall** in a row of 16–20px labels, i.e. wrapped ("Upload / Stock / Icons / Fonts wrap awkwardly"), and `2435:12154 'Media · local-only assets' vis=True [106,21748,148,12]` is the "local-only navigation label … in the bottom region". |
| **F05** Notification message text overlaps timestamps | **YES — this is the arc's own headline repair.** `COVERAGE.md:531`/`:653` — *"seen \| `165:2` screenshot: row 1's title running into the timestamp column"*; `:484-489` — *"`render-defects.mjs` returned **six OVERPRINTs across the two Notifications boards that have rows, every one exactly 14px**. Uniform overlap is the signature of a formula, and the formula was mine — `relay-panel-width.mjs` grew each title flush to the widened panel's right edge with no gutter."* | **LANDED AND RE-MEASURED.** `COVERAGE.md:531-536` is a six-row closure table ending *"**re-measured** \| `render-defects.mjs 1779:2 --fresh` → **0 defects**"*, with `:538-541` — *"The re-measure matters more than the repair… **A fix with no evidence is not a fix**."* Root cause fixed too: *"the growth now stops 12px short of the nearest right-hand sibling."* | **No row for this board, and a dedicated before/after pair proves it.** `RENDER-DEFECTS-notifications.txt:1-7` (16:17) — *"`--- 1779:2 (6)`"* with six `OVERPRINT … by 14` rows on `165:2`/`165:24`. `RENDER-DEFECTS-notifications-after.txt:2` (16:29) — *"render defects >= 4px: **0** (across 1 of 1 sections)"*. Section `1779:2` then reads `(0)` in **both** whole-page censuses (BEFORE 18:07 **and** AFTER 18:19). | **LIKELY-STALE / CONTRADICTED-BY-RECORD.** The overlap was measured at 6, repaired, and independently re-measured to 0 at **16:29 — an hour and a half before the audit was published at 17:58** — and three later whole-page sweeps agree. The audit is describing a screenshot from its first pass. (Its own `shots/notif-unread_165-2.png`, 15:38, is from the same pre-fix window.) |
| **F06** AI explanatory and recovery content is clipped | **NO — silent.** Nothing in `reports/ai.md`, `reports/ai-gap.md`, `REGISTER.md`, `CHECKLIST.md` or any `findings/*.jsonl` names the clipping on `170:2`, `171:105` or `171:136`. `reports/ai-gap.md` documents a *different* `170:2` defect in detail (the quota meter's silent no-op) and its "What I did NOT cover" section never reaches this. The phrase *"Continue in Inspector"* appears **nowhere** in the record. The only prior trace is one row in the large-overprint triage: `OVERPRINT-LARGE-REVIEW.md:37` *"178px `171:136` AI · not-configured"* and `:43` *"100px `2846:21667` AI · error-provider"* — geometry, no diagnosis. | **PENDING / never recorded.** The 178px and 100px pairs are in the "held for a human read" 45 (`COVERAGE.md:637-639`), and `OVERPRINT-LARGE-REVIEW.md:62` says *"Not done in this arc."* The clipping (`CONTAINER`/`TEXTOVER`) class is explicitly **not** being repaired: `COVERAGE.md:645-648` — *"The other 319 defects — CONTAINER, TEXTOVER, ESCAPES — have no proven fixer in this repo and are **not** being auto-repaired."* | **Every node the finding names still has a row, byte-identical to BEFORE.** `TEXTOVER 170:2 AI · idle 2483:11982 by 4` · `CONTAINER 170:2 AI · idle 170:15 "note" bottom +8` · `TEXTOVER 170:2 AI · idle 170:16 by 8` · `CONTAINER 171:105 AI · error-quota 171:132 "Error" bottom +120` · `TEXTOVER 171:105 2846:21652 by 120` · `TEXTOVER 171:105 2846:21651 by 32` · `CONTAINER 171:136 AI · not-configured 171:163 "Not configured" bottom +106` · `OVERPRINT 171:136 171:165 x 171:166 by 178` · `TEXTOVER 171:136 2846:21650 by 106` · `TEXTOVER 171:136 2846:21649 by 34` · `CONTAINER 2846:21667 AI · error-provider 2846:21677 "Error" bottom +120` · `OVERPRINT 2846:21667 2846:21678 x 2846:21679 by 100` · `OVERPRINT 2846:21667 2846:21679 x 2846:21680 by 56` · `TEXTOVER 2846:21667 2846:21682 by 120` · `TEXTOVER 2846:21667 2846:21681 by 32`. (BEFORE: `RENDER-DEFECTS-PAGE.txt:204-206, 208-214, 218-222`.) | **LIKELY-REAL.** The detector says the recovery text on the quota and not-configured frames stands **106–120px past its clipping "Error" / "Not configured" parent** — which is the finding's *"recovery text flagged outside clipping parents"*, arrived at independently. Unchanged across both sweeps. The only piece not settled offline is whether the *idle* paragraph is visibly cut (the 8px `TEXTOVER` on `170:16` is consistent with it, one 11px line). |
| **F07** History retention warning is cut off | **PARTIAL.** The *string* is in the record verbatim — `plans/history-text.json:208` *"This session only — the last 100 steps, cleared when you reload. Save a version to keep a point."* The *clipping* is not diagnosed anywhere; what the record diagnosed on the same node was a different regression: `INVARIANTS-POST.md:38-39` — *"The separate OOB: the history plan set `163:45` to **width 328 inside a 280-wide board**. Corrected to 248."* | **landed fix, wrong dimension.** `plans/zz-regression-fixes.json:2` resized the **width** to 248; `queue-state.json:2794-2798` shows it `SAME`, `"detail": "248x48"`. The **height** is the defect and nobody looked at it. The height class is in the not-being-repaired 319 (`COVERAGE.md:645-648`). | `CONTAINER 163:2 History · Saves · changes 163:44 "Prune note" bottom +16` and `TEXTOVER 163:2 History · Saves · changes 163:45 by 16` — **unchanged** (BEFORE `RENDER-DEFECTS-PAGE.txt:110-111`). | **LIKELY-REAL — confirmed by a post-audit live read, and it is worse than the audit says.** `b21.tsv` (18:38) gives the arithmetic on **four more boards** the audit also names: `Prune note` frame `2854:12327 [3400,78082,280,40] clips=true`, text `2854:12328 [3416,78090,248,48]` → the text ends at 78138, the clip at 78122, so **16px — one 11px line — is cut**, and the last visible words are *"Save a version to keep a"*. Same numbers on `2854:21739`/`2854:21773`, `2854:21814`/`2854:21848`, `2854:21854`/`2854:21888`. **Five boards, not one.** |
| **F08** Page grouping contradicts comment metadata | **YES** — `findings/W-E.jsonl:4` `W-E-04` (Major): *"The agency board groups client comments by page ('OPEN · HOME', 'MENU') but the client page can never attach a page or an anchor to a comment, so every real client note lands in one 'General' group… Board 156:2 draws 'Sara · client · Home · 2d' and a 'MENU' band; the shipped panel will print 'Sara · client · General · 2d' for every client comment."* Its fix names the audit's remedy as option 2: *"redraw `156:2`'s client rows without page attribution so the panel is not designed around data the client half cannot produce."* | **never actioned.** `W-E-04` appears exactly once in the whole tree — no queue row, no register row, no report row, no verdict. The `W-*` lane fed `COMPILE-*`, and this row did not survive into any of them. | No row for this defect (it is a semantic contradiction, invisible to the detector). `156:2`'s AFTER rows are `TEXTOVER 1720:17415 by 4`, `TEXTOVER 1720:17406 by 4`, `ESCAPES I229:1088;9:19 by 4 past 229:1088`. BEFORE also carried `OVERPRINT 156:2 1753:8434 x 1753:8435 by 60` — **that one was repaired** (`RENDER-DEFECTS-PAGE.txt:290`, absent from AFTER). | **LIKELY-REAL.** `DISCOVERY.md:4192` `"OPEN · HOME"` and `DISCOVERY.md:4214` `I228:1007;17:39 0,22 "Sara · client · Contact · 2d"` — the second comment under the HOME heading says Contact, in the arc's own 16:20 dump. The audit read the board correctly; the repo knew and dropped it. |
| **F09** Reply composer identifies no recipient or thread | **YES, twice, and the two lanes disagree.** `findings/FIG-D.jsonl:28` `FIG-D-28` (Critical) — *"the placeholder `Reply to the client…` promises the opposite, and the board draws both"*; its `edit` asks for the placeholder to become `"Add an internal note…"`. `findings/FIG-N.jsonl:18` `FIG-N-18` (Major) — *"Keep the composer (it works) but change the hint text … to 'Internal note — posts to this page's comments, not under a reply.'"* — i.e. change the hint and leave the placeholder. | **half landed, and the landed half is what the audit is reading.** `FIG-N-18`'s hint shipped: `DISCOVERY.md:4216` `1753:8434 "Internal note — posts to this page's comments, not under a reply."` on `156:2`, and `:4432`/`:4442` on `1705:8566`/`1705:8704`. `FIG-D-28`'s placeholder change did **not**: `DISCOVERY.md:4203` and `:4440` still read `"Reply to the client…"`. The board rename did land — `DISCOVERY.md:4438`: *"`[not-implemented] Review · reply-composer band — comments are flat (no parentId in the schema) and a reply is filed against the round's FIRST comment's page, not the one being answered (ReviewTab.tsx:242)`"*. | `1705:8704` — **no row in AFTER.** BEFORE had `OVERPRINT 1705:8704 [not-implemented] Review · reply-c 1705:8769 x 1705:8770 by 56` (`RENDER-DEFECTS-PAGE.txt:305`) — the ≤60px fixer repaired it. This is the very board that triggered the whole page-wide sweep: `COVERAGE.md:588-590`, `SHOTS.md:174-177`. | **LIKELY-REAL, but half of it is the record's own deliberate answer.** The contradiction the audit sees (`"Reply to the client…"` beside a hint saying it is not a reply) is exactly `FIG-D-28`'s unlanded row — a **PENDING** edit, correctly re-found. The audit's *other* half ("no target thread is selected in this view") is the file telling the truth on purpose: the board name states the schema has no `parentId`. Recommend closing it as `FIG-D-28`, not as a new finding. |
| **F10** Re-send button has insufficient room for its label | **YES, as geometry, never as a finding.** `findings/DEAD-CONTROLS-V2.txt:686` records `A 229:1088 'Re-send for review' 119x32` (and `:689,692,695,698,701,711` — the same 119×32 button on seven boards). No lane filed the overflow. | **never recorded.** No plan, queue row, report row or verdict mentions it. | `ESCAPES 156:2 Review panel · open I229:1088;9:19 by 4 past 229:1088` — **unchanged** from BEFORE (`RENDER-DEFECTS-PAGE.txt:293`). `ESCAPES` is defined in `COVERAGE.md:605` as *"a node past its PARENT's width (the board check is blind to this)"* — i.e. exactly a label wider than its button. | **LIKELY-REAL.** `DISCOVERY.md:4190` `I229:1088;9:19 -4,6 "Re-send for review"` — the label starts **4px left of** its 119px button and the detector says it ends 4px right of it: a 127px string in a 119px control, zero horizontal padding. Same on `157:2` (`DISCOVERY.md:4222`). Detector-measured, still live. |
| **F11** Settings error states offer no recovery | **PARTIALLY, and the record's version is a different defect.** The naming/voice half: `findings/C.jsonl:17` `D-C-17` — *"Forms uses 'error' (1703:9046) and 'action-error' (1703:9208) — both of which are load errors by their own copy ('Failed to load forms.' / 'Failed to load submissions.')."* The layout half: `findings/COVER-1.jsonl:11` `COVER-1-11` (Major, names `1703:9208`) — *"Each of the four save-failure boards draws a single card containing the section title, its description and the error line — the screen reduced to one block… A save failure is never an early return."* **No lane asks for a Retry.** | **CONFLICTED / unresolved.** `CONTRADICTIONS.md:118` — <code>\| `UX-I-32` vs `COVER-1-11` \| one deletes the in-body "Save headers", the other redraws the same boards **with** it \| **both cannot land** — unresolved, flagged \|</code>. `D-C-17` was never carried anywhere. | `ESCAPES 1703:9208 S7 · Settings · Forms · action-err 1703:9273 by 9 past 1703:9271` and `ESCAPES 1703:9046 S7 · Settings · Forms · error 1703:9111 by 9 past 1703:9109` — **unchanged** (BEFORE `RENDER-DEFECTS-PAGE.txt:346-347`). Both are 9px label overflows, unrelated to the recovery claim. | **LIKELY-REAL as an observation, CONTRADICTED as a board defect.** Checked against HEAD: `packages/editor/src/editor/sidebar/tabs/settings/screens/FormsScreen.tsx:127` sets `"Failed to load submissions."` and **there is no retry control anywhere in the file** — so the board is *correct* about the product. Adding a Retry to the board would be drawing an affordance that does not ship, which is the one move the arc's own precedence rule forbids (`reports/content.md:60-66` — *"No proposed affordance is drawn as if it shipped… Drawing any of them on a truth board is the exact defect this arc exists to remove"*). The **real** board defect on `1703:9208` is `COVER-1-11`'s: `FormsScreen.tsx:306-307` renders `subsError` *inline*, with the form `Select` and the four-chip `role="tablist"` (`:268-291`) still above it — so a one-card drawing is wrong, and that is unresolved. F11 should be re-filed as `COVER-1-11`, with the Retry treated as a product proposal. |
| **F12** Dynamic-page config lacks a template-change path | **YES — and the record removed it on purpose.** `reports/content.md:141` `QA-A-01` **Critical**: *"all four dynamic-pages boards draw a TEMPLATE PAGE label + input \| **delete it** from `2429:12111 / 21243 / 21262 / 21281`"*; `:142` `QA-A-02` Major: *"the 'Choose a page…' placeholder promises a picker \| **remove the row**"*. Note `QA-A-01`'s own history: *"`667fd830d` (2026-09-06) claims to have removed it"* — an **invented** picker had already had to be deleted once (`reports/content.md:63-64`). | **landed by design.** `plans/content-dynamic-pages-body.json` board `2429:12111`: `"why": "QA-A-10 (label casing), QA-A-11 (audit notes leave the panel), QA-A-01/02/03 (**no TEMPLATE PAGE control**)"`; board `2429:21281`: *"the no-template WARNING is an extra line under the count line (ContentViews.tsx:683-685), not the state line, and **there is no TEMPLATE PAGE field to carry it**."* Runner receipt: `runlog/dynpages.done` (mtime **16:23**, i.e. before the audit). | `TEXTOVER 2429:12111 Content · dynamic-pages 2435:12148 by 4` — the only row, **unchanged** (BEFORE `RENDER-DEFECTS-PAGE.txt:161`). No row for `2429:21281`. | **CONTRADICTED-BY-RECORD.** F12 asks for exactly the control the arc deleted twice, on a code citation (`ContentViews.tsx:645,662-685`). The founder's precedence rule makes the code the authority on behaviour, so re-adding a picker would re-create the defect `667fd830d` was reverted for. The *product* gap is real and already on file as an absence (`UX-C-09`, `reports/content.md:60-66`); the *board* is not wrong. The audit's second clause — *"a long code-level explanation"* — is also stale: `QA-A-11` moved both 10px audit paragraphs out into `caption/*` nodes (`reports/content.md:67`, `:148`), and the generator that did it ran at 16:23. |
| **F13** Search-result category wraps into a clipped row | **NO — silent, except as two detector rows.** `reports/insert.md:136` states plainly: *"I do not have a single interior node id for `138:2`, `138:53`, …"* — the arc never opened this board. No lane, plan, report or verdict mentions `138:103` or `138:105`. | **never recorded.** The `CONTAINER`/`TEXTOVER` class is explicitly not auto-repaired (`COVERAGE.md:645-648`). | `CONTAINER 138:53 Insert · searching 138:103 "Result · CTA button" bottom +8` and `TEXTOVER 138:53 Insert · searching 138:105 by 8` — **unchanged** (BEFORE `RENDER-DEFECTS-PAGE.txt:187-188`). | **LIKELY-REAL.** A text node standing 8px past the bottom of a clipping row named `Result · CTA button` is precisely "a category label that wrapped to a second line and is cut at the row's lower edge" — one 8px line of an 11px label. The detector found it independently of the audit and it survives the AFTER sweep. Which string it is (`COMPONENTS`) is not in any local dump — that detail alone needs Figma. |
| **F14** Nav corrections not assembled into existing screens | **YES, and the record says it in the first person.** `reports/shell-gap.md:32,35,50,51` mark `2844:12136` (Back to canvas), `2844:12121` (location line) and `2844:12025` (destination list) **IMPLEMENTED** — as *new boards*. `OUTCOME.md:24-30` frames all ten as *"**10 new Shell boards** — the More index, rail selected-vs-open, … the location line, full-page mode…"*. Not one is an edit to an assembled screen. | **landed as boards; assembly never attempted, and said so.** `reports/shell-gap.md:114-116` — *"**I read four sections' direct children and three boards' interiors. I opened no other board.** Every claim above about a board I did not build is its name, read back this session, and nothing more."* `reports/shell.md:173-176` (first pass) — *"**I opened no board's interior.** 73 board-level nodes across my four sections were read by name and geometry only."* And on the Settings half of F14's node list, `reports/settings.md:209-211` — *"**12 of the 46 boards are outside every plan**: `638:2378` General, …"*. | No row for `638:2378`, `2844:12025`, `2844:12121` or `2844:12136` in **either** census. `52:2`'s two `CONTAINER` rows are unchanged. Absence here is expected: the finding is structural, not geometric. | **LIKELY-REAL.** `DISCOVERY.md:164-169` dumps `52:2`'s rail as exactly six labels — `Content`, `Layers`, `Pages`, `Media`, `Insert`, `Brand` — with **no More seat**, at 16:20. `add-rail-more-seat.mjs` is specified (`reports/shell.md:140,147`) and has **no receipt anywhere** — not in `queue-state.json`, not in `runlog/`, not in any report. No location line and no "Back to canvas" string appears in `52:2`'s 101-node text dump. The corrections exist as boards and have not reached the assembly. |

---

## Cross-cutting notes

**Three of the fourteen were caused by this arc, not found by it.** F03's audit
paragraphs (`plans/pages-nodes-02-tree.json` rows 8-9), F04's colliding footer
lines (`plans/media-05-node-edits.json`), and F07's clipped prune note (a
width-only fix at `plans/zz-regression-fixes.json:2`) are all the arc's own
writes, each with a green read-back. `COVERAGE.md:519-521` already names the
pattern: *"a green result describes the rows a tool matched, never the rows it
meant to match."*

**Silence is the finding in four places.** F06 (AI clipping), F10 (Re-send label
overflow), F13 (Insert result row) and F02-on-`52:2` have **no prose entry
anywhere in the record** — their only trace is a row in a detector census nobody
read back. F01 and F08 are worse than silent: both were measured by a lane
(`D-C-40`, `W-E-04`) and then appear exactly once in the entire tree, never
reaching a register, checklist, queue or verdict.

**Two are stale or contradicted.** F05 was repaired and re-measured to zero at
16:29, ninety minutes before publication. F12 asks for a control the arc deleted
twice on a code citation, and its "long code-level explanation" clause was
answered by a generator that ran at 16:23.

**One needs Figma.** F02's core claim — that the hero section on `52:2` renders
light gray while the inspector says `#1A56DB` — is a paint value. It is in no
local dump, there is no `shots/` capture of `52:2`, and the sibling measurement
in `D-C-40` says "white", not "light gray". Everything else above is settled from
files on disk.
