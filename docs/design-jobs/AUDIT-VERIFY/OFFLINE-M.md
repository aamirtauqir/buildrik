# M01–M20 verified against the local record — offline, zero Figma calls

Source of claims: `docs/design-jobs/AUDIT-VERIFY/ALL-FINDINGS.json` (48 rows;
this pass covers the 20 with id `M01`..`M20`).
Checked against: `docs/design-jobs/V2-TO-V1/**` and `docs/design-jobs/findings/**`.
**No Figma MCP call was made.** Every statement below cites a local file and line.

## Clock, so "before/after the audit" means something

All `queue-state.json` stamps are UTC; all file mtimes are local. The offset is
+5 (`queue-state.json:4308` writes at `12:02:40Z`, file mtime `17:02`).

| event | local | UTC |
|---|---|---|
| apply-queue arc: **994 rows attempted, first to last** | 09:38 → **17:02** | 04:38 → 12:02Z |
| `V2-TO-V1/DISCOVERY.md` — the arc's own full re-read of 8 sections | **16:20** | 11:20Z |
| **audit HTML generated** | **17:58** | 12:58Z |
| `RENDER-DEFECTS-PAGE.txt` / `.render-defects-after.json` | 18:07 / **18:19** | 13:07 / 13:19Z |
| `RENDER-DEFECTS-components.txt` (targeted re-measure) | **18:31** | 13:31Z |
| `COVERAGE.md` final overprint arithmetic | **18:32** | 13:32Z |

So: **every queue write predates the audit**, and three detector runs postdate it.
`DISCOVERY.md` is a post-arc, pre-audit text dump — it is the strongest offline
corroborator available, and it covers Brand (`1776:8373`), Templates
(`1084:4527`), Components (`1938:8372`) and Canvas (`1779:5`). It does **not**
cover Content (`1776:8376`), Media (`1776:8372`) or Inspector (`1776:8381`).

## The audit's `kind` field is not a reliable staleness signal

`M16` is labelled **"Retained complete text"**, yet it quotes
*"No records yet. Open Content › this collection to add the first one."* — a
string that did not exist until this arc wrote it at
`queue-state.json:4582-4586` (`gap-b-copy#0`, `06:52:45Z` = 11:52 local, six
hours before the audit). A "Retained" label therefore means "not re-fetched in
this pass", not "captured before today's writes". Weight the **write ledger**,
not the label.

---

## The table

| id | already_known | disposition | staleness_risk | verdict_offline |
|---|---|---|---|---|
| **M01** | **YES** — `V2-TO-V1/plans/media-04-state-boards.json:63`: *"the only delete-confirm board is the >20-file typing-gate bulk one, which is the wrong dialog for the single asset"*; the exact repairs are authored at `plans/media-06-post-clone-unresolved.json:458-520` (`"nodeNameEquals": "Delete 34 files?"` → `"Delete file?"`; hide `"Type DELETE to confirm:"`; `"Delete 34 files"` → `"Delete"`) | **Clone landed, re-content NEVER RAN.** `queue-state.json:4306-4310` — `media-04-state-boards#7` `SAME`, *"board exists: 2850:22371 'Media · delete-confirm · single file'"*. `media-06` has **0 rows in `queue-state.json`** and **0 rows in `queue.json`** — it was never ingested (it is a `groups` dict, not a row list); its own header says `"_status": "unresolved-id"` (`media-06…json:3`) because *"the Figma MCP seat quota was exhausted account-wide part-way through this pass"* (`:2`). `reports/media.md:48` files `UX-H-10` as **BLOCKED-ON-QUOTA**. Contradicted by `CHECKLIST.md:173` / `REGISTER.md:183`, which say `IMPLEMENTED · VERIFIED — read-back`. | **Fresh** evidence. Node **created today by this arc** (a bare clone of `1175:4827`) and **never written to since**. So the inherited bulk text is exactly what is expected to be there. | **LIKELY-REAL** — the arc cloned the bulk dialog and its own re-content plan never executed; the audit is describing the arc's known unfinished step. |
| **M02** | **YES** — `plans/media-04-state-boards.json:84-88` creates the rollback clone; `plans/media-06-post-clone-unresolved.json:829-870` authors precisely the audit's fix: `"2 replaced, 1 failed."` → `"Nothing replaced — all 3 updates failed. No changes committed."`, `"▸ Failed elements (1)"` → `"▸ Show errors (3)"`, `hide "Retry failed"`, **`hide "result-clean"`**. Filed as `COVER-1-25` (`findings/COVER-1.jsonl:23`). | **Same as M01 — clone landed, re-content never ran.** `queue-state.json:4858-4863` — `media-04-state-boards#10` `SAME`, *"board exists: 2881:12608 in section 1776:8372"*. `reports/media.md:62` files `COVER-1-25` as **BLOCKED-ON-QUOTA**. | **Fresh.** Node created today, never edited after. The audit's observation that *"current children remain result-clean and result-partial"* names the two nodes `media-06` was written to hide. | **LIKELY-REAL** — verbatim match to an authored-but-unrun repair row. |
| **M03** | **YES** — `plans/insert-new-boards.json:62-80` (`insert-new-boards#1`, `"kind": "add-inline-error"`) specifies the missing band: `:78` *"Can't add Button — a heading doesn't take children. Select a container first."* | **Clone landed, `postClone` never ran.** `queue-state.json:4720-4725` — `insert-new-boards#1` `SAME`, *"board exists: 2850:21560 in section 1776:8379"*. The only insert text plan that executed is `insert-text-fixes` (10/10 `OK`) and it targets the **old** boards; `insert-text-fixes-unresolved#0-2` all returned `NOTTEXT`/`FRAME`. `reports/insert.md:82` files `UX-A-02` as **BLOCKED-ON-QUOTA**, while `CHECKLIST.md:101` / `REGISTER.md:49` say `IMPLEMENTED · VERIFIED — read-back` — the "read-back" was `insert-truth-marks#2`, a **rename of the source board `1706:8501`**, not a change to the clone. | **Fresh.** Created today; no write since. | **LIKELY-REAL** — and it exposes a checklist over-claim: `UX-A-02` is marked VERIFIED on the strength of a rename to a different node. |
| **M04** | **YES** — `plans/pages-text-08-newboards.json:5-55` authors all four rewrites against the clone's inherited strings: `"equals": "Discard unsaved SEO changes?"` → `"Delete “Pricing”?"` (`:8-11`), the body → the inbound-link sentence, `"Keep editing"` → `"Cancel"` (`:36-38`), `"Discard changes"` → `"Delete page"` (`:52`). Source of the clone is `1171:4820` (`plans/pages-boards-07-new.sh:47`). | **Board created, all four text rows NEVER-ATTEMPTED.** `pages-text-08-newboards#0-3` and `pages-nodes-08-newboards#0` (resize) have **no entry in `queue-state.json`**; `UNRESOLVED-ROWS.md:82` lists `8 \| pages-text-08-newboards.json` among the 104 selector rows blocked on a resolver call nobody made. `REGISTER.md:61` records `UX-B-04` as **PARTIAL** — *"1 landed, 0 refused, 1 missing — UX-B-04 — IMPLEMENTED. Board 2898:22178 … created in section 177…"*. | **Retained** evidence, but the ledger says **no write ever landed on this node**, so retained ≈ current. | **LIKELY-REAL** — the clone still holds its source's unsaved-SEO copy because the four retitle rows were authored and never run. |
| **M05** | **YES** — the exact pair is in `plans/overprint-small.json` / `plans/overprint-batch-1.json` as `{"a":"1712:8430","b":"1712:8431","by":15,"board":"1170:4777"}`, and again in `plans/overprint-pairs-page.json`. `DISCOVERY.md:5197` records `T 1712:8430 520,698 "Cancel"`, matching the audit's `x=520–563, y=698–714`. | **Repair attempted and REFUSED by the fixer's own floor.** `COVERAGE.md:703` — *"`1938:8372` · Components \| **6** \| **yes** — `1712:8430 x 1712:8431`, a 15px overprint on `Components · create (modal)` \| **yes, targeted**"*, then `:707-709`: *"The targeted read came back: **6 defects, and the pair is still overlapping by 15px.** That repair did not land — it was one of the sixteen the fixer's floor refused, and I had been carrying it in the '32 repaired' count without evidence."* | **Fresh** — and **re-measured 33 minutes AFTER the audit**: `RENDER-DEFECTS-components.txt:7` (mtime 18:31) still reads `OVERPRINT 1170:4777 Components · create (modal) 1712:8430 x 1712:8431 by 15`. | **LIKELY-REAL** — independently confirmed still-present by a detector run that postdates the audit. |
| **M06** | **YES, as a geometry defect** — `RENDER-DEFECTS-PAGE.txt:92-93`: `CONTAINER 2850:22371 … 2850:22373 "in-use-warn" right +8` and `ESCAPES 2850:22371 … 2850:22374 by 8 past 2850:22373`. Same node, same class of defect; the numbers differ (+8px overflow vs "cuts off the final word") because one is a container read and one is a screenshot. | **Measured, not repaired.** Both rows survive into `.render-defects-after.json` (mtime 18:19) under section `1776:8372`. `OVERPRINT-LARGE-REVIEW.md:62-64`: *"**Not done in this arc.** Recorded with the node ids so it is one measurement away from actionable, and explicitly NOT counted as repaired anywhere in `COVERAGE.md`."* | **Fresh.** The node is a today-created clone; the defect is confirmed by a detector run **21 minutes after** the audit. | **LIKELY-REAL** — same root cause as M01: the clone inherited a 408-wide warning box sized for the bulk string. |
| **M07** | **YES, and the record predicted this exact failure** — `findings/COMPILE-B.md:80`: *"`1172:4854` (whole `chg` row frame, holding `1172:4855` 'shadow/raised' and `1172:4857` 'removed') … **Delete this row, then the four compiled rows in `plan-text-B.json` … make the board self-consistent at three rows.** **Apply the deletion and the text together or the counts lie.**"* Upstream: `FIG-F-25` / `FIG-F-26` (`findings/FIG-F.jsonl:25-26`), `W-G-06` (`findings/VERDICTS-W.jsonl:102`). | **The text half landed, the deletion half did not.** `applied/plan-text-B.json:281-287` applied `"Review 3 staged changes"` and `"Apply 3 changes"`. No delete op for `1172:4854` exists in any plan, queue or applied file — the only other hit is `BRAND-CONFORMANCE.txt:25`, which shows the row still present and merely renamed `[not-implemented] shadow/r…`. | **Fresh** — and corroborated offline: `DISCOVERY.md:4051-4064` (16:20 local, post-arc) dumps the board with **four** rows (`color/accent`, `type/heading`, `spacing/lg`, `shadow/raised` + `removed`) over `"Apply 3 changes"` and `"Review 3 staged changes"`. | **LIKELY-REAL** — the counts lie, exactly as `COMPILE-B.md:80` warned they would if the deletion were skipped. It was. |
| **M08** | **NO for the claim; YES for the raw text.** `LEDGER.jsonl:243` (2026-09-02) records the board verbatim: *"head 'Choose an image' 14/600 + 'for Hero · Image' 11/400 … foot '2 selected' 11/500 + 'Use Selected' accent"*. But **no finding files the single-vs-multi contradiction.** All 11 local findings on `1164:4713` are about other things: `W-A-16` (3 undrawn tabs), `W-A-17` (footnote), `W-A-46` (dead tab row), `W-A-37` (dead List toggle), `FIG-E-32` (alt-text on replace), `D-A-40`, `D-A-44`, `ARR-A-01`. | **Never recorded as a finding.** The only write this arc made to the board is `media-02-text-fixes#27` (`OK`) — the footnote, `"Upload tab accepts image/video/audio · max 10 MB each"`. The footer was untouched. | **Retained** evidence, but the footer has had **no write** since the 2026-09-02 ledger measurement that agrees with it. Low staleness risk. | **LIKELY-REAL** — the strings the audit quotes are independently in `LEDGER.jsonl:243`; only the contradiction between them is a new observation. |
| **M09** | **NO.** Nothing in the record dumps this board's text. Searched `€9`, `9.00`, `Price is required`, `Can't publish` — **zero hits outside the audit file.** The nearest neighbours are `W-C-09` (`findings/W-C.jsonl:9`, `btn/Publish` is dead so the board is orphaned) and `W-C-24` (`:24`, the record form has no error state at all) — neither is this claim. | **Silent.** No queue row, plan row or applied row targets `1706:8451`. Its `+ Add record` is listed as a dead control at `findings/DEAD-CONTROLS-V2.txt:407`. | **Retained**, and Content (`1776:8376`) is one of the sections `DISCOVERY.md` does **not** cover — so there is no local text to compare against. No write landed either, so it is unlikely to be stale; it is simply unverifiable offline. | **UNDETERMINED-NEEDS-FIGMA** — the record can neither corroborate nor refute the quoted strings, and this is the only M-row in that position. |
| **M10** | **PARTIAL.** The dead-control half is filed: `findings/DEAD-CONTROLS-V2.txt:403-404` — `1706:8437 'Modal · CMS records · none-published'` → `A 1706:8443 '+ Add record' 99x28` (unwired). The **"wrong recovery action for 12 existing records"** framing is not filed anywhere. `FIG-A-05` (`findings/FIG-A.jsonl:5`) is about this board but concerns the banner's *causal* claim, not its CTA. | **Banner rewritten; CTA untouched.** `applied/plan-text.json` rewrote `1706:8397` to *"No records published yet. Dynamic pages generate only from published records — and only once a template page is bound."* (`QA-C-31` confirms). No row ever targeted `1706:8443`. | **Retained.** Note the audit quotes the **short** form *"No records published yet"*, which is a prefix of the new string — so the quote cannot distinguish pre- from post-rewrite. The CTA claim is unaffected either way. | **LIKELY-REAL** — the CTA is corroborated locally and was never changed; only the framing is new. |
| **M11** | **NO.** `1706:8433` appears only in structural findings — `ARR-A-22` (`findings/ARRANGE.jsonl:52`, family split) and `W-C-27` (`findings/VERDICTS-W.jsonl:96`, the record form is undrawn). No claim about its CTA or its detour copy. | **Silent.** No queue/plan/applied row targets it. It is also **absent from the dead-control candidate list** (`findings/DEAD-CONTROLS-V2.txt:390-408` lists candidates for its four siblings and none for `1706:8433`), consistent with "only a close symbol". | **Retained**, but the dimension is corroborated by a **local** Figma read: `plans/content-section-read-2026-09-07.json` lists `{"id":"1706:8433", … "w":720,"h":80, "name":"Modal · CMS records · no-collections"}` — exactly the audit's *"complete 720×80 dialog"*. No write since. | **LIKELY-REAL** — geometry corroborated locally, no write landed, and an 80px-tall dialog cannot hold the action the audit asks for. |
| **M12** | **YES, twice** — `REGISTER.md:89` `UX-G-08`: *"If the generation ever does succeed, the reward is a raw JSON blob in a scrollbox with Discard and Retry. There is no Accept…"*; and `findings/B.jsonl:6` `D-B-06` (Critical): *"The success state of 'Generate component with AI' offers no way to accept the result."* | **Registered `IMPLEMENTED`, but implemented ELSEWHERE — and a later measured pass says the board is CORRECT.** `CHECKLIST.md:38` = `IMPLEMENTED · VERIFIED — read-back`; `REGISTER.md:89`'s evidence names board **`2846:21621`** (an AI decision board), not `1706:8483`. And `FIG-F-32` (`findings/FIG-F.jsonl:32`, confidence `measured`, action `none`) states: *"All four match. **In particular the success board correctly draws NO Accept button** — the modal's `onAccept` is optional and its only mount omits it"* (`AIPromptModal.tsx:40-44`, `DesignSystemTab.tsx:888-899`). | **Retained**, corroborated: `DISCOVERY.md:4137-4142` (post-arc) shows exactly `✕ / Retry / Discard / {PricingCard JSON} / "Generate component with AI"`. The only write to this node this arc was `grid-snap-rows#76`, a **move** — no content change. | **CONTRADICTED-BY-RECORD** — the *observation* is right, but per the founder precedence rule (behaviour → CODE contract) the board faithfully mirrors a modal that ships no Accept; `FIG-F-32` measured that and closed it `action: none`, and the product-level gap is already registered as `UX-G-08` and dispositioned. |
| **M13** | **YES** — same `FIG-F-32` row enumerates it: *"error ('AI service not configured' + Retry / Cancel)"*, and *"The error board's string is the code's literal"* (`AIPromptModal.tsx:75`). | **Closed as conforming.** `FIG-F-32` `action: "none"`, `severity: "Minor"`, *"No edit. Coverage recorded."* Adjacent register rows `UX-G-01` (`REGISTER.md:31`) and `UX-G-23` (`:100`) cover the not-configured recovery story and are both `IMPLEMENTED`. | **Retained**, corroborated by `DISCOVERY.md:4171-4176`: `✕ / Retry / Cancel / "AI service not configured" / "Generate component with AI"`. Only write: `grid-snap-rows#77`, a **move**. | **CONTRADICTED-BY-RECORD** — the board draws the code's literal Retry/Cancel; the audit's fix is a product change already measured and deliberately left alone. |
| **M14** | **NO for the width claim.** `ARR-A-22` (`findings/ARRANGE.jsonl:52`) files the same five boards but for **placement** (*"separated from its own four states by two board rows"*), never width. | **Silent on width; never planned, never written.** No queue row targets any of the five (the only rows near them are `content-captions-new#6/#7`, captions placed *below* `1170:4713`/`1170:4749`, and `content-marks#6/#7` renames). | **Retained**, and **fully corroborated by a local read**: `plans/content-section-read-2026-09-07.json` gives `1170:4749` = `640×350`; `1706:8444` = `720×188`; `1706:8433` = `720×80`; `1706:8437` = `720×162`; `1706:8451` = `720×180`. Numbers match the audit exactly. | **LIKELY-REAL** — the geometry is verified offline from the arc's own section read; nothing has resized these boards since. |
| **M15** | **PARTIAL, and the record disagrees on the fix.** `FIG-A-04` (`findings/FIG-A.jsonl:4`) quotes the same node `1173:4830` verbatim: *"Slug pattern — /blog/{slug} · Template — blog/_template · SEO title — {title} — Blog"* — but its complaint is that ONE caption stands in for **four TextInputs**, and its evidence shows the blog strings are the **code's literal placeholders** (`CMSCollectionSetupModal.tsx:433-438`). `FIG-A-03` (`:3`) adds that the whole block generates zero pages. | **The slug sub-claim LANDED as a caption; the interior edit did not.** `content-captions-new#6` = `OK` (`2838:12013`, 500×162 @900,706) and its text says *"…the Slug field this wizard's own `/{slug}` default depends on cannot be created here."* No row ever targeted `1173:4830`, so the caption line is unchanged. | **Retained.** A caption was added under this board today (before the audit), which the audit does not mention — so its evidence is at least partly pre-caption. | **LIKELY-REAL** (the mismatch is real and `FIG-A-04` quotes the same string) — **but its prescribed fix conflicts with the record**: "use a consistent restaurant-menu example" would overwrite the code's own placeholders. The "expose editable template/URL fields" half agrees with `FIG-A-04`. |
| **M16** | **YES** — `W-I-13` (`findings/W-I.jsonl:13`): *"DEAD — '+ Create Collection' has no reaction, **and it is also the wrong action for this state**: the user is inside a collection that has no RECORDS."* Also `UX-C-10` (`UX-FLOW-MAP.md:232`) and `FIG-J-28` (`findings/FIG-J.jsonl:28`). | **Copy half LANDED today; CTA half did not.** `queue-state.json:4582-4586` — `gap-b-copy#0` `OK` at `06:52:45Z`, writing `1707:8424` to *"No records yet. Open Content › this collection to add the first one."*, with the receipt at `plans/gap-b-advisories.json` (`gap-b-advisories#0`): *"IMPLEMENTED · 1707:8427 / 1707:8424 — the popover's no-records dead end no longer names a command-palette entry."* Nothing touched `1707:8425` (`+ Create Collection`). | **Labelled "Retained" but it is NOT** — the audit quotes the string this arc wrote six hours earlier. Its evidence is post-write. Nothing has changed since. | **LIKELY-REAL** — the remaining half (CTA resolves the wrong prerequisite) is the still-open half of `W-I-13`. |
| **M17** | **YES, adjacent but not identical.** `FIG-D-25` (`findings/FIG-D.jsonl:25`) covers this exact modal and finds a **missing Description field** and a missing door caption; it does **not** raise the missing visible label on the name field. | **Never planned, never written.** No queue/plan/applied row targets `1169:4753` or `1169:4756`. `FIG-D-25`'s own edit (add a Description field, grow the frame to ~210) also never ran. | **Retained**, corroborated by `DISCOVERY.md:4970-4975` (post-arc, pre-audit): the board's complete text is `Cancel`, `Save template`, `Autumn menu layout`, `Save page as template`, and the tokens hint — **no field label**, exactly as claimed. | **LIKELY-REAL** — independently confirmed by the arc's own text dump; the specific label claim is new. |
| **M18** | **YES** — `W-K-23` (`findings/W-K.jsonl:23`, Critical, `VERDICTS-W.jsonl:9` `CONFIRMED`) and `UX-G-11` (`REGISTER.md:91`): *"When it fails it prints the raw server string… While it is thinking there is no Stop."* `FIG-K-03` (`findings/FIG-K.jsonl:3`) asks for a new `AI · error-provider` board. | **`UX-G-11` marked `IMPLEMENTED · VERIFIED — read-back` (`CHECKLIST.md:41`) — but the evidence is a hotspot** (`REGISTER.md:91`: *"OK: 2876:12485 over 2846:21471 -> 2476:12001"*), not a change to the specimen. No row targets `1707:8455`. | **Retained**, corroborated by `DISCOVERY.md:5469-5470`: `BOARD 1707:8455 … 248x40 … Popover · Canvas · AI · error` with the single text node `"The AI service didn't respond."` — the audit's 248×40 measurement is exact. | **LIKELY-REAL** — the specimen is still a dead-end sentence; the register's `IMPLEMENTED` refers to a wire elsewhere. |
| **M19** | **PARTIAL.** The *specimen* measurements are corroborated: `LEDGER.jsonl:125` measures `1172:4840` as *"title 'Review 4 staged changes' 13/600 … foot 1172:4865 h27 right-aligned: … 11/500"* — 11px action text, 27px footer, exactly as claimed. The **reference-kit numbers (46:2, "16/24 titles, 13/20 action text") are nowhere in the record**; `46:2` is only ever named in passing as one of the *"Modal instance sheets"* (`V2-TO-V1/slices/_board-level.json`). No modal type-scale conformance finding exists. | **Not filed, and the record's own decision points the other way.** `LEDGER.jsonl:125`'s `proposal` moves the **CODE** to the board's compact scale — *"buttons size='xs' tw:h-7 tw:text-[11px]"*, *"title tw:text-sm -> tw:text-[13px]"* — i.e. it treats 11px/27px as the target, not the drift. The conformance scanner also passes 11px: `BRAND-CONFORMANCE.txt:1` reports `sub11=0`. | **Fresh.** The single-delete specimen's scale is inherited from `1175:4827` by today's clone (see M01), so the "drift" is a by-product of the unfinished clone. | **LIKELY-REAL** as a measurement (two of the three numbers are corroborated offline) — **but its direction is contested**: the record's standing proposal makes 11px/27px canonical and moves the code to it. Settling this needs a decision, not a read. |
| **M20** | **NO.** Nothing in the record raises viewport height for this modal. The nearest entry is `BLOCKERS.md:272` (`H1`, RESOLVED 2026-09-02), which explains **why it is 720×740**: *"The producer is `CreateComponentModal.tsx` with `<ModalContent size=\"lg\">`, and `SIZE_WIDTH_CLASS.lg` is **720**. 1170:4777 rebuilt at 720×740 with all six field groups verbatim from source."* | **Silent on the constraint; the size is a deliberate, code-derived rebuild.** No queue row targets `1170:4777` (only the two overprint pair entries, and neither landed — see M05). | **"Measured geometry"** — and the geometry is verifiable offline: `DISCOVERY.md:5170` records `BOARD 1170:4777 2060,220 720x740 layout=NONE Components · create (modal)` with absolutely-positioned groups and `1712:8430 "Cancel"` at `520,698`. 740 > 720 is arithmetic, not a read. | **LIKELY-REAL** — the height and the absolute layout are confirmed locally; the claim is genuinely new and the 720 width is load-bearing (do not "fix" it by narrowing). |

---

## MUST be settled by a fresh Figma read

**Only one row is truly undetermined offline:**

- **M09** — `1706:8451` (`Modal · CMS records · publish-error`). The Content
  section is absent from `DISCOVERY.md`, no queue row ever touched this board,
  and the strings the audit quotes (`Margherita €9.00`, `Can't publish: Price is
  required`) appear **nowhere** in the local record. Needs one text read of
  `1706:8451`.

**Two rows are decided offline but carry a named residual read**, worth folding
into the same call if a read happens anyway:

- **M19** — one read of `46:2` (`Modal instance sheets`) to confirm the "16/24
  title / 13/20 action" reference scale. The two *specimen* measurements are
  already corroborated by `LEDGER.jsonl:125`. The bigger blocker is a decision,
  not a read: the record's standing proposal makes 11px/27px canonical.
- **M06** — the +8px container overflow is measured locally
  (`RENDER-DEFECTS-PAGE.txt:92-93`, re-confirmed at 18:19); only the audit's
  screenshot-level claim ("cuts off the final word") is unverified, and it does
  not change the fix.

## Three things worth carrying up beyond the per-row verdicts

1. **Five of the twenty are one root cause.** `M01`, `M02`, `M03`, `M06` and half
   of `M19` all describe boards this arc **cloned today and never re-contented**.
   `media-06-post-clone-unresolved.json` was authored in full, was **never
   ingested into `queue.json` at all** (it is a `groups` dict, not a row list —
   `normalize-plans` skipped it), and has zero rows in `queue-state.json`. Running
   that one plan plus `insert-new-boards#1`'s `postClone` and
   `pages-text-08-newboards` closes `M01`–`M04` and `M06`.

2. **`CHECKLIST.md` over-claims on exactly these rows.** `UX-H-10`, `UX-H-01` and
   `UX-A-02` read `IMPLEMENTED · VERIFIED — read-back` (`CHECKLIST.md:101,164,173`)
   while `reports/media.md:40,48` and `reports/insert.md:82` file the same ids as
   **BLOCKED-ON-QUOTA**. The "read-back" in each case verified a **rename of the
   source board**, not the content of the new clone. The external audit found the
   gap the checklist hides.

3. **Two audit rows would undo deliberate, code-grounded decisions.** `M12` and
   `M13` ask for Accept/Configure actions on boards that `FIG-F-32`
   (`findings/FIG-F.jsonl:32`, confidence `measured`) verified as faithful to
   `AIPromptModal.tsx`. `M15` asks to replace the code's own literal placeholders
   (`CMSCollectionSetupModal.tsx:433-438`) with restaurant sample data. `M20`'s
   720 width is the code's `SIZE_WIDTH_CLASS.lg` (`BLOCKERS.md:272`) — fix the
   height, not the width.
