[
  { "id": "306:2261", "expect": "Drop .json", "text": "Drop tokens.json — JSON only", "width": 216, "why": "UX-H-28 · the zone invites a format the parser cannot read. ImportCard.tsx:391-393 accepts .ts/.js, :98-104 names the file back as 'Tailwind config (AST)', and the only parser is parseImportJSON. Board on 306:2232 Brand · export · exported." },
  { "id": "306:2294", "expect": "Drop .json", "text": "Drop tokens.json — JSON only", "width": 216, "why": "UX-H-28 · same string on 306:2265 Brand · export · imported." },
  { "id": "306:2327", "expect": "Drop .json", "text": "Drop tokens.json — JSON only", "width": 216, "why": "UX-H-28 · same string on 306:2298 Brand · export · error." },

  { "id": "152:145", "expect": "Applying a starter replaces", "text": "Selecting a starter previews it on the canvas. Applying replaces your colour, type and spacing tokens and clears per-token undo; the other eleven kinds keep their values. The confirm names every staged edit it will overwrite before it does.", "width": 248, "why": "UX-H-30 · StartersSection.tsx:43 fires applyStarter(id) straight from onSelect, so the card IS the commit. The board must draw select-then-apply; the warning band is the line that currently stands in for the missing confirm." },
  { "id": "306:2191", "expect": "Applying a starter replaces", "text": "Selecting a starter previews it on the canvas. Applying replaces your colour, type and spacing tokens and clears per-token undo; the other eleven kinds keep their values. The confirm names every staged edit it will overwrite before it does.", "width": 248, "why": "UX-H-30 · same band on 306:2186 Brand · starters · applied." },

  { "id": "155:60", "expect": "6-card gallery", "text": "6-card gallery, a drill-in rather than the old modal. Selecting a card previews it; a footer 'Apply <name>' commits, and the confirm names the token count and the staged brand edits it will discard. Colour, type and spacing only — the other eleven kinds keep their values.", "width": 248, "why": "UX-H-30 · caption/Brand · starters must describe the fixed interaction, not the one-click one." },

  { "id": "155:58", "expect": "Value · used-in · replace-on-delete", "text": "Value · used-in · replace-on-delete. Editing here repaints every consumer immediately, which is why used-in sits next to the value. Rename opens an in-app modal that validates against the existing token ids and states how many elements will be re-pointed — never the browser's prompt. It stays disabled for type and spacing tokens, where the rename call is still inert.", "width": 248, "why": "UX-H-31 · TokenDetailView.tsx:250-259 uses window.prompt with a TODO above it; NoNativeDialogs.test.tsx treats that as a defect elsewhere in the repo. FIG-F-09/FIG-F-70 already established rename is inert for type and spacing." },

  { "id": "155:56", "expect": "Nine sections", "text": "Nine sections, each a 32h row with a count. Above them: the palette swatches, then a live specimen — a card at the brand's radius, border, shadow and spacing carrying heading, body and a primary button — so a change to any of the fourteen kinds is answered on this screen instead of on the canvas.", "width": 248, "why": "UX-H-32 · BrandPreview.tsx takes colours only (props at :27-30, MAX_SWATCHES 10 at :34, SPECIMENS is Heading + Body at :36-39). Twelve of the fourteen kinds have no preview anywhere." }
]
