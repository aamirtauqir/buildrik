{"id":"W-CO-01","module":"Brand","node":"—","screen":"Brand · Starters / Presets","q":"Q4","kind":"missing-screen","severity":"Major","finding":"The server ships a theme router with applyPreset/list/save/delete and the editor calls NONE of them — the only editor→dashboard calls are cms, media stock, siteComponents, siteDetail.domains, siteVersions, userTemplates and webhooks. Brand starters and presets are client-only.","evidence":"server/trpc/routers/theme.ts (12 procedures, key `theme` per server/trpc/router.ts); zero matches for client().theme.* across packages/editor/src; sibling sync services exist for cms (services/cmsSync.ts), components (componentSync.ts) and templates (templateSync.ts) — there is no themeSync.ts","fix":"Decide whether brand presets are meant to be shared across a workspace. If yes, the design needs a board for a SERVER-side preset list (someone else's preset, an org preset) which no board currently draws; if no, the four theme procedures are dead server code and should be recorded as such.","buildFrom":"—"}
{"id":"W-CO-02","module":"Settings","node":"—","screen":"S7 · Settings · Localization","q":"Q3","kind":"missing-field","severity":"Major","finding":"Per-page translations have a complete server API — pages.setTranslation / getTranslation / removeTranslation — with no caller anywhere in the repo, while a Settings · Localization board exists. The screen is designed and the API is built; nothing joins them.","evidence":"server/trpc/routers/pages.ts declares setTranslation, getTranslation, removeTranslation; a scan of all 225 distinct tRPC paths actually invoked across packages/, app/, lib/, components/ and server/ contains none of them","fix":"Either the Localization board must draw the per-page translation surface those procedures serve (which page, which locale, what happens to an untranslated page), or the procedures are dead. Check with the founder which.","buildFrom":"—"}
{"id":"W-CO-03","module":"Shell","node":"—","screen":"—","q":"Q4","kind":"flow-gap","severity":"Minor","finding":"sites.saveProjectData and sites.getProjectData have no caller in the repo — they exist only in their own router and its unit test. The shipping save path is saveProjectFromEditor. Dead server code, not a missing door: do not design screens for them.","evidence":"grep for saveProjectData|getProjectData across the repo returns only server/trpc/routers/sites.ts:516,541 and server/trpc/routers/__tests__/site-limit-message.test.ts:41-42, which mocks saveProjectFromEditor alongside","fix":"Record as dead server code. No design work.","buildFrom":"—"}
{"id":"W-CO-04","module":"Media","node":"—","screen":"Media · folders","q":"Q4","kind":"missing-screen","severity":"Minor","finding":"media.moveFolder has no caller — moving a folder (as opposed to moving assets INTO a folder) has a server procedure and no UI or board.","evidence":"server/trpc/routers/media.ts declares moveFolder; not present among the 225 invoked tRPC paths. Note the editor DOES call media.searchStockPhotos/searchStockVideos, so the media client wiring exists and this is a specific gap, not an unwired module.","fix":"Confirm with the Media agent's board walk whether any board draws folder reorganisation. If not, this is a designed-in-the-API, undesigned-in-the-file capability.","buildFrom":"—"}
{"id":"W-CO-05","module":"AI","node":"—","screen":"AI · error-quota","q":"Q3","kind":"missing-field","severity":"Minor","finding":"ai.getQuotaStatus has no caller, yet the AI section has an error-quota board. The board depicts a quota state the editor never asks the server about.","evidence":"server/trpc/routers/ai.ts declares getQuotaStatus; absent from the 225 invoked paths. The editor's only AI network calls are the literal paths /api/trpc/ai.summarize and /api/trpc/ai.milestoneSuggest (packages/editor/src/editor/panels/version-history/useAISummary.ts:10, :180 region)","fix":"Either wire the quota board to the procedure that serves it, or establish how the quota state is actually reached and correct the board's premise.","buildFrom":"—"}
{"id":"W-CO-06","module":"Components","node":"—","screen":"Components · usage","q":"Q4","kind":"missing-screen","severity":"Major","finding":"siteComponents.usage has no caller while its four siblings (list/get/upsert/delete) are all called by componentSync. 'Which pages use this component?' is the question a user must answer before editing or deleting a master, the server can answer it, and nothing asks.","evidence":"the 225 invoked paths include siteComponents.list/get/upsert/delete and NOT siteComponents.usage; server/trpc/routers/site-component.ts declares it","fix":"A usage/impact board before destructive component edits. Build it from the detach-confirm modal 1170:4792, which is the nearest existing 'this affects other things' surface.","buildFrom":"1170:4792"}
{"id":"W-CO-07","module":"Templates","node":"—","screen":"—","q":"Q4","kind":"missing-screen","severity":"Minor","finding":"templates.cloneFromSite has no caller — 'start a new site from an existing one' is built on the server and has neither UI nor board. templates.generate.{create,cancel,status} ARE called, so the AI-generation half of this router is wired and the clone half is not.","evidence":"invoked paths include templates.generate.create / .cancel / .status; templates.cloneFromSite absent","fix":"Confirm whether cloning is in scope for the editor at all — it may belong to the dashboard. If dashboard-only, it is out of this file's scope and should be recorded as such rather than boarded.","buildFrom":"—"}
