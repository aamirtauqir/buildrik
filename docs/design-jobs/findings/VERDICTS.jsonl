{"id": "D-A-01", "verdict": "CONFIRMED", "method": "figma text-dump 137:2 / 1069:4707 / 1069:4529 + read catalog/groups.ts:1-18,48-54", "note": "Both expanded boards carry 'TEMPLATES | 10'; 137:2 draws only ELEMENTS/BLOCKS/COMPONENTS/MINE; buildInsertGroups() returns exactly those four."}
{"id": "D-A-02", "verdict": "CONFIRMED", "method": "figma reaction walk on 137:2 + parent lookup on the four targets", "note": "137:28 COMPONENTS -> 1069:4790 and 137:36 MINE -> 1069:4970, both parented to SECTION 1090:4527 (REVIEW holding pen); ELEMENTS/BLOCKS targets sit in 1776:8379. Codex refuted this by describing live panel behaviour, which is a different question."}
{"id": "D-A-06", "verdict": "UNSUPPORTED", "method": "read catalog/catalog.ts (6 category strings) + BlockPickerModal.tsx:187 + elements/constants.ts:86-94", "note": "The modal does not render catalog.ts at all - it renders <ElementsTab>, whose live taxonomy is the 7-item NEW_CATEGORY_ORDER. The board's 4 diverge from BOTH, so the cited comparison set ('the six the live catalog has') is the wrong one."}
{"id": "D-A-11", "verdict": "CONFIRMED", "method": "figma parent lookup on 642:3112, 1170:4777, 1138:13394", "note": "642:3112 'Components · create' is parented to SECTION 1084:4527 (the REVIEW holding pen) while 1170:4777 'Components · create (modal)' sits in SECTION 1776:8379 (Insert) - two competing create designs, as filed. The 'reachable from nothing' clause was not separately checked."}
{"id": "D-A-22", "verdict": "CONFIRMED", "method": "figma absolute-bbox probe on 782:4353", "note": "Banner 'Couldn't reach the rest of your library' T83-B123 physically overlaps the folder row T109-B129; the no-results copy is on the same board. Overlap and conflation both real."}
{"id": "D-A-23", "verdict": "CONFIRMED", "method": "figma absolute-bbox probe on 145:250", "note": "Banner lines overlap (T722-B754 vs T742-B774) and Upload renders at full strength. Correction: only the Stock label overruns the 812 frame (T782-B862); the Upload label (T782-B802) sits inside it."}
{"id": "D-A-29", "verdict": "CONFIRMED", "method": "figma text-dump 1162:4617", "note": "'24 files · Last added 2h ago', JPG/PNG/SVG/MP4 chips and TAGS menu/team/food coexist with every count at 0 and 'No images or files yet.' Verbatim as filed."}
{"id": "D-A-37", "verdict": "CONFIRMED", "method": "read StockBrowserOverlay.tsx:282,288", "note": "Code makes 'Loading 8 more…' and 'Load more' mutually exclusive, which is exactly the finding's point about the board drawing both. Codex's REFUTED restates the finding as a refutation."}
{"id": "D-A-39", "verdict": "UNSUPPORTED", "method": "read StockBrowserOverlay.tsx:282,288,303", "note": "The blanket 'no coded empty, searching, failed or quota state' overreaches: :303 renders an items-empty branch and :282 a loading line. Only failed and quota are genuinely absent."}
{"id": "D-D-01", "verdict": "REFUTED", "method": "figma-shot 23:21 (page 1:6) + 1339:7171 + frame-name read", "note": "The canonical client viewing screen IS designed - 23:21 'S5.5 · landing-viewing' on page 1:6 draws the agency header, the frozen site render and the Request-changes/Approve footer. 1339:7171 is named 'echo of 1:6 (canonical); kept because Figma rejects cross-page NAVIGATE'. The audit measured a deliberate prototype proxy and read it as the module's undesigned archetype."}
{"id": "D-D-02", "verdict": "REFUTED", "method": "figma text-dump 23:21 / 114:2 (canonical) vs 1339:7171 / 1340:7162 (echoes)", "note": "Canonical A (23:21) and B (114:2) both draw the full screen INCLUDING the verdict footer. The 'two mutually incompatible partial drawings' is an artefact of the two labelled echo stubs on page 1:3."}
{"id": "D-D-03", "verdict": "CONFIRMED", "method": "read review-client.tsx:374-461 + figma regex sweep of 16085 TEXT nodes across pages 1:3, 1:6, 1:4", "note": "Three copy variants, the note-failed line and the 'Not yet'/'Go back' split are all in code; no board anywhere carries 'Yes, approve' / 'Not yet' / 'Send and request changes'. Matcher returned unrelated hits, so it is a live matcher, not a dead one."}
{"id": "D-D-04", "verdict": "CONFIRMED", "method": "read client-review.service.ts:273-292 + review.service.ts:481,589", "note": "resolveReviewByToken - the path the CLIENT takes - updates the row and returns; notifyReviewResolved is called only from review.service.ts:481 (the admin path). Codex REFUTED this by citing notifyReviewResolved without checking which call site reaches it."}
{"id": "D-D-05", "verdict": "CONFIRMED", "method": "read review-client.tsx:89-92,250-252", "note": "Shell hardcodes 'is asking for your feedback' and every terminal state renders through Shell. Correction to the evidence: the CANONICAL boards (117:58, 122:3) do not carry that subtitle - the contradiction is code-side, not board-side, and the cited nodes are echoes."}
{"id": "D-D-06", "verdict": "REFUTED", "method": "figma text-dump 117:58 and 122:3 (canonical) + review-client.tsx:271-273", "note": "Canonical D (117:58) DOES render 'Signed as Sara Khan · sara@bellacucina.com'. Only canonical C (122:3) omits it. The claim that both D and C omit it is drawn from the echo stubs."}
{"id": "D-D-10", "verdict": "CONFIRMED", "method": "figma reaction walk on 156:2 / 1717:17246 / 1717:17235 + read ReviewTab.tsx:497-521", "note": "Compare hotspot -> loading; loading's only edges are back to 156:2 and to load-error; load-error's only edge is back to loading. Code has three branches and the third (<ApprovedCompareView>) has no board."}
{"id": "D-D-15", "verdict": "CONFIRMED", "method": "read packages/shared/schemas/reviews.ts:19-26", "note": "REVIEW_PILL_STATES is exactly the six states cited. Which two lack a panel board was not separately re-counted."}
{"id": "D-D-18", "verdict": "CONFIRMED", "method": "read prisma/schema.prisma:509-554, ls app/api/cron (17 routes), ls emails/*review* (3), review.service.ts:555-580", "note": "No remindedAt/openedAt on ReviewRequest, no review cron, three review templates, and notifyReviewSubmitted filters the requester out and returns recipients.length. Minor: the constant is TOKEN_TTL_DAYS, not 'tokenExpiry'."}
{"id": "D-D-19", "verdict": "CONFIRMED", "method": "figma text-dump 165:2/165:24/165:71/165:44 + count of NOTIFICATION_TYPE_CATEGORY + every createNotification/notifyWorkspaceOwner call site", "note": "COUNT ERROR: 7 distinct types are emitted, not 6 - SECURITY_2FA_CHANGED is written at account.service.ts:378 and :415 and was missed. The 21-key count and the board rows are exact, and the headline (no review/comment/approval type exists) holds."}
{"id": "D-D-21", "verdict": "CONFIRMED", "method": "figma text-dump 165:2 vs 165:24", "note": "Both return the identical string sequence; the boards differ only in tint."}
{"id": "D-D-22", "verdict": "CONFIRMED", "method": "figma text-dump 165:2 + read NotificationPanel.tsx:193", "note": "The board carries no mark-all-read control, as filed. Note for whoever acts on it: the shipping editor panel already renders 'Mark all read' (NotificationPanel.tsx:193), so this is design-behind-code, not a product gap."}
{"id": "D-E-01", "verdict": "CONFIRMED", "method": "figma text-dump 949:4474 and 1704:8309 + read publish.service.ts:433-466,559-563", "note": "Footer says 'Every publish is restorable'; sibling empty state says 'the last 20'; PUBLISH_HISTORY_RETAINED=20 and the prune nulls the payload beyond it."}
{"id": "D-E-12", "verdict": "CONFIRMED", "method": "figma text scan of 641:2652, 784:4250, 784:4326, 784:4403, 912:4520 + read usePublishSnapshot.ts:174-179", "note": "All five boards carry 'Preview' and 'brk-preview.vercel.app'; the hook ships preview.value = null under a comment saying the row stays because the board draws it."}
{"id": "D-E-14", "verdict": "CONFIRMED", "method": "figma child-name list of SECTION 1776:8378 (14 frames) + read PublishService.ts:231-241, PublishTab.tsx:179-186, sites.ts:428-444", "note": "Unpublish is fully built end to end and no frame in the Publish section mentions it."}
{"id": "D-E-15", "verdict": "CONFIRMED", "method": "read usePublishJob.ts:33,97,228-231,305 + grep PublishTab.tsx for a cancel control", "note": "cancel() is declared, implemented against cancelPublishCall and produces uiState 'cancelled'; PublishTab renders no cancel affordance - the only occurrence of the word is a comment at :153."}
{"id": "D-E-18", "verdict": "CONFIRMED", "method": "figma child-name list of SECTION 1779:4", "note": "Six frames, names verbatim as filed - three [not-implemented], three [design-ahead]. No board for the shipping PreviewOverlay in this section."}
{"id": "D-E-22", "verdict": "REFUTED", "method": "figma child-name list of SECTION 1779:3", "note": "No board name contains 'chord unavailable'. All six read '… the SHELL palette (Cmd+K, shell/modals/CommandPalette.tsx, which cites these boards by id). Ships today.' The section name itself already states the finding's whole thesis, including the two-palette split. The premise the finding attacks is not in the file."}
{"id": "D-E-23", "verdict": "CONFIRMED", "method": "read StudioHeader.tsx:249-267 and PagesTab.tsx:125-137 at the call sites; grep for key===\"k\" across packages/editor/src", "note": "Both listeners real, neither stops propagation. TWO CORRECTIONS: (1) there is a THIRD ⌘K listener at media/LibraryManager.tsx:83, also preventDefault-without-stopPropagation, so with the media library open the chord both focuses its search and toggles the shell palette; (2) SECTION 1779:3's own name already records this exact bug with these exact file:line refs - it is not a new finding."}
{"id": "D-E-27", "verdict": "REFUTED", "method": "figma child-name list of SECTION 1776:8377 (Pages, 13 frames)", "note": "'no board in this section or any other' is false: 1171:4767 'Pages · command-palette (⌘K)' is a board in the Pages section. The residual finding - that the third palette is boarded outside the Command-palette section - is Minor, not Major."}
{"id": "D-E-29", "verdict": "CONFIRMED", "method": "figma-shot 817:4950, read the PNG", "note": "Four score discs render with no numerals visible and four savings pills render as solid green with no text. Confirmed by eye, independent of the fill probe."}
{"id": "D-E-30", "verdict": "CONFIRMED", "method": "figma absolute-bbox probe on 162:2 and 949:4474", "note": "162:2 'Named milestones' T76-B89 with the active-underline RECTANGLE at T82-B84 - inside the text band. Same geometry on 949:4474 (T75-B88 vs T82-B84). Codex refuted this from React/CSS, which is not what the board draws."}
{"id": "D-E-31", "verdict": "CONFIRMED", "method": "figma absolute-bbox probe on 162:2 and 949:4474", "note": "'50 versions kept…' spans L16-R344 in a 280px frame (64px overrun); 'Every publish is restorable…' does the same on 949:4474."}
{"id": "D-E-32", "verdict": "CONFIRMED", "method": "figma absolute-bbox probe on 949:4474 vs 162:2", "note": "Four ⋯ nodes at L320-R331 in a 280px frame on 949:4474; the same glyph sits at L216-R227 on 162:2. Positive control included."}
{"id": "D-E-38", "verdict": "CONFIRMED", "method": "figma-shot 807:8663, read the PNG", "note": "Mobile 375px is selected while the page renders at roughly 780px, the bezel is a transparent outline on top, the headline runs past its right edge and Done sits inside the bezel. Codex refuted this from PreviewOverlay.tsx - a code answer to a board question."}
{"id": "D-E-40", "verdict": "CONFIRMED", "method": "figma text+bbox dump of 162:2 rows", "note": "Three list rows carry an 'Auto-save' title at T307/T363/T475; the row at T419 has a ⋯ and no title text, matching the bare-flag claim."}
{"id": "D-X-01", "verdict": "CONFIRMED", "method": "independent per-section reaction walk of page 1:3, counting frames with a rail-named carrier (both 'any reaction' and 'reaction with a destination')", "note": "COUNT ERRORS in four modules: Media is 5/40 not 8/40; Brand is 0/34 not 3/34; Layers is 0/18 not 2/18; Pages is 0/13 not 1/13. Settings 39/45, Shell 2/17, Journeys 1/74 and the thirteen zeros all reproduce. The conclusion is unaffected and in fact stronger than filed."}
{"id": "D-X-04", "verdict": "REFUTED", "method": "figma name-scan of every frame on page 1:3 for /component/i + parent lookup", "note": "'authoring, detaching, updating and the detail screen have no board anywhere' is false. 641:2546 'Components · library', 641:2599 'Components · detail', 778:4173 loading, 781:4433 load-error and 642:3112 'Components · create' sit in SECTION 1084:4527; 1138:13394 'Components · empty' and 1170:4777 'Components · create (modal)' sit in the Insert section; 1170:4792 'Components · variant-picker · detach-confirm' sits in Notes. The true, smaller finding is that Components has no section of its own and its boards are scattered across a review holding pen, Insert and Notes. Agent A's D-A-11 cites three of these boards by id - the coordinator contradicted its own wave."}
{"id": "D-X-05", "verdict": "CONFIRMED", "method": "figma section list + find packages/editor/src/editor/sidebar/tabs/templates -type f -not __tests__", "note": "COUNT ERROR: 24 non-test files under tabs/templates, not 14. The headline (no Templates product module in the file) holds."}
{"id": "D-X-08", "verdict": "CONFIRMED", "method": "figma-shot 817:4950, read the PNG", "note": "Independent of the sibling-fill probe: the discs render with no numerals and the pills with no text. The methodological warning in the row about ancestor-walks is correct and worth keeping."}
{"id": "D-X-09", "verdict": "CONFIRMED", "method": "read useAgentRunner.ts:275-287 and applySetStyle.ts:642 at the call sites", "note": "approve() calls applyAiEdit once per approved step and applyAiEdit opens exactly one beginTransaction('ai-edit'), so an N-step run is N undo entries. The copy also ships at a third site the row does not cite: shell/modals/CommandPalette.tsx:455."}
{"id": "D-X-11", "verdict": "CONFIRMED", "method": "figma child list of SECTION 1776:8381", "note": "All ten cited nodes (159:99, 159:102, 159:123, 160:2, 160:105, 160:208, 160:313, 160:412, 160:512, 189:2) are now children of 1776:8381, so the move landed. FLAG: this is a Figma WRITE performed inside an audit whose brief says 'You are READ-ONLY. Do not rename, move, delete or create any Figma node.'"}
{"id": "D-X-12", "verdict": "CONFIRMED", "method": "read LeftSidebar.tsx:105,628,638 and tabsConfig.ts:348-356", "note": "RAIL_FIGMA's ids are add/layers/pages/assets/content/design - 'settings' is not among them, so SETTINGS_DIRTY_SET can never match a rendered rail tab."}
{"id": "D-G-01", "verdict": "CONFIRMED", "method": "independent re-walk of SECTION 1776:8388 (74 FRAME + 33 TEXT children), reactions read on frame + every descendant, instances force-warmed via getMainComponentAsync before the measuring pass", "note": "Reproduces exactly: 210 outbound edges, 67 carried by the FRAME ITSELF, 84 by hotspot/*, 28 by btn/ai (all -> 170:17), 31 by a named control, 19 of those on 52:2. The 12 other named-carrier frames are the exact 12 listed. One slip: FIVE of those 12 carriers are named 'Frame'/'Header' (1156:4593, 295:1972, 817:5262, 878:4518 = 'Frame'; 301:1979 = 'Header'), not 4. TRAP for the next verifier: a cold run of the same walk returns 205 edges / 7117 carriers because instance sublayers load lazily - the four 52:2 topbar carriers and 129:223's pill/in vanish. Warm the instances or you will under-report."}
{"id": "D-G-04", "verdict": "CONFIRMED", "method": "destination filter over the same warmed 210-edge set", "note": "Zero edges into 784:4250, 914:4507 or 784:4326. Publish is reached only at 833:4518 (from 52:2, 130:798, 307:2213), 641:2652 (from 52:2 + exactly the five S6 boards 807:8787/817:5220/817:5262/878:4518/1156:4593) and 1168:4732 (from 52:2). Only zero-outbound frames are 1306:2 and 1345:7162, both RETIRED - as filed."}
{"id": "D-G-02", "verdict": "CONFIRMED", "method": "figma node read 1345:7162 (name/size/text/reactions) + parent lookup on all 11 cited archive boards + page-wide destination scan of 519 frames on 1:3", "note": "1440x480, 6 TEXT nodes, RETIRED in its own name, zero reactions on frame+descendants and zero in-edges page-wide. All 11 superseded boards sit in SECTION 957:4474 with the exact '[dashboard-flow] S2.x … SUPERSEDED 2026-08-28 by 1345:7162' names. One scope note: 'the AI journey has no live board' is true of S2 only - the AI MODULE section 1776:8380 holds 22 live boards and 29 journey edges land on 170:17."}
{"id": "D-G-03", "verdict": "CONFIRMED", "method": "figma text dump of 807:8756 + name lookup on the other five S6 boards + page-wide in/out edge scan", "note": "Exact: 'Connect domain' / 'Step 2 of 3 · Verify DNS'; in-edges are 10 and every one is a Settings·Domains board (639:3092, 1702:6931/7095/7261/7425, each contributing @FRAME + a hotspot); out-edges are 639:3092 x2 and 1688:7195. The five other S6 boards are 807:8787 and 817:5220 [not-implemented], 817:5262 / 878:4518 / 1156:4593 [design-ahead], as filed."}
{"id": "D-G-09", "verdict": "CONFIRMED", "method": "figma text dumps of 131:201 (1440x900) and 1168:4713 (520x262) + read StaleApprovalModal.tsx:2, :7-8, :33-50, :205,:212", "note": "Both boards reproduce verbatim, including 131:201's ELEMENT rows ('Hero heading'/'Opening hours', 'edited 2h ago') against 1168:4713's PAGE rows (Home/Menu/Contact). Code cites 131:201 in its header, but ChangedPage is {path, kind} and the shipped CTA at :205 reads 'Request fresh review' - 1168:4713's label, not the 'Re-send for approval' its own docblock quotes. The contradiction is if anything sharper than filed."}
{"id": "D-G-10", "verdict": "CONFIRMED", "method": "figma text dump of 807:8723 + read review-client.tsx:9-10, :322-369", "note": "807:8723 carries the numbered pin '2', 'Sara M. · 2 min ago', 'Reply or click to add comment...', '3 comments · 1 unresolved' and 'View all comments' - verbatim. review-client.tsx has a 320px 'Your notes' aside with a textarea and 'Add note'; grep for pin/reply/unresolved across the file returns one incidental substring hit inside the word 'typing' and nothing else."}
{"id": "D-G-11", "verdict": "CONFIRMED", "method": "figma in-edge scan over all 519 frames on page 1:3 + text dump of 303:1997 + read ImageEditorModal.tsx:2", "note": "146:2 -> 1124:4527 via 'List row' AND via 'hotspot/row · Edit image'; 146:2 -> 303:1997 via 'hotspot/state · S3.6 · media · editing'. 303:1997 is 280x812, 21 TEXT nodes, differing from its sibling only in the banner string. ImageEditorModal.tsx:2 names 1124:4527 as its board."}
{"id": "D-G-12", "verdict": "CONFIRMED", "method": "same page-wide edge scan + text dump of 303:2032 + read OptimizationPanel.tsx:2-14", "note": "303:2032 is 303:1997's 21 strings with 'Optimizing -> WebP…' swapped in. Its only edges page-wide are 303:1997 -> 303:2032 and 303:2032 -> 303:1997 - a closed 2-cycle, confirmed against every frame on 1:3, not just Journeys. OptimizationPanel.tsx:2 names 1124:4562 and its docblock lists the chip row, quality slider, Original/Optimised rows and CTA that 303:2032 does not draw."}
{"id": "D-G-17", "verdict": "CONFIRMED", "method": "figma text dump of 130:201 and 130:400 + read ReviewBar.tsx:113-133", "note": "Both boards carry the literal '0 open'. The code comment ('A zero here was a count where a sentence belongs…') and the three-way render ({count>0 ? `${count} open` : CHANGES_REQUESTED ? 'Changes requested — nothing left open' : 'Sent — waiting on your client'}) are verbatim as filed."}
{"id": "D-G-18", "verdict": "CONFIRMED", "method": "figma text-sequence equality test + non-TEXT node signature diff + per-child geometry read on 130:201 vs 130:400", "note": "TEXT sequences are byte-identical (88 nodes each, same order). Non-TEXT signature 151 vs 149 counting the frame itself - the exact numbers filed - and the only two extras on 130:201 are the invisible frames 'hotspot/state · S5.2 · none' and 'hotspot/alt · S1 · Editor — ASSEMBLED'. Every body frame differs by a uniform 66px (Middle band/Rail/Drawer/Canvas/Inspector 702 vs 768). No badge, dot, avatar or timestamp difference exists."}
{"id": "D-G-33", "verdict": "CONFIRMED", "method": "S-number extraction from all 107 Journeys children + name regex for histor|restor|rollback|version|undo|recover|revert + existence check on every cited code path", "note": "Journeys carries S1.x, S2.0, S3.x, S5.x, S6.x and no S-number for restore. The four name matches are crash-recovery (297:2027), offline-restored (807:7000), force-refresh (813:4888) and undo-redo-toast (814:7027) - none walks a user from a mistake to a restored site. Every cited file exists (VersionHistoryPanel is at editor/panels/, not shell/), siteVersion.create/list/get/delete are at site-version.ts:24,33,40,47, SiteMenu.tsx:200 'Version history', useEditorShortcuts.ts:124 ctrl/meta+H, sites.ts:451/466/481 publishHistory/publishDiff/rollback."}
{"id": "D-G-34", "verdict": "CONFIRMED", "method": "figma read of caption 817:4855 (name + characters) + page-wide frame-name sweep for /share.?(preview|link)|S4\\.1/ + read share-link.service.ts, site-detail.ts:309, app/share/[token]/, SiteMenu.tsx:276", "note": "The caption's own NAME reads 'caption/S4.1 - share-preview-link — NO BOARD ANYWHERE (the only genuine phantom S-slot…)' and its body carries all five features verbatim. Code confirms the shipping journey: bcrypt hash, plan-capped expiresInDays, allowPasswords plan gate, revoke, public app/share/[token]/page.tsx, editor door at SiteMenu.tsx:276. One qualifier the finding omits: a stub frame 817:4774 exists, named '[not-implemented · superseded by 988:2 · Site · Access] Preview · share link' - not a design, but not literally nothing either."}
{"id": "D-H-16", "verdict": "CONFIRMED", "method": "figma child-tree + geometry read on all ten cited boards + read review-client.tsx:39-75", "note": "'The real built site' is 880x560 on every one of the ten, the only tab row is the Mode strip's Preview|Comment, 'Menu|Book|Find us' sits inside the built-site frame, and no node on 23:21 or 114:2 is named scroll/scrollbar. Code renders a per-page tab strip when pages.length>1 over an h-[600px] iframe at :71. ONE EVIDENCE SLIP: the WRAPPER 'Site preview — view only' is not identical - it is 1280x694 on 23:21/113:3/122:3/120:50 and 1280x728 on the other six. The 880x560 crop, which is the load-bearing number, is identical."}
{"id": "D-H-17", "verdict": "CONFIRMED", "method": "figma child-tree on 114:2/122:3/117:2 + read review-client.tsx:322-369,:337,:437 and client-review.service.ts:229-243, schemas/reviews.ts:100-106", "note": "114:2 draws pin/1 24x24 with a sibling 'comment composer' 220x132 carrying 'Sara' / 'this photo is too dark' / 'Post'; 122:3 draws pin/1 and pin/2; 117:2 carries '3 pinned comments included'. Code has the 320px 'Your notes' aside with maxLength 2000 and 'Add note', accepts pageId/x/y/targetSelector in the schema and service, and both call sites send only {token, body}. A pin/reply/unresolved grep over the file returns nothing."}
{"id": "D-H-24", "verdict": "CONFIRMED", "method": "figma frame inventory of page 1:6 (24 frames) + width set + read review-client.tsx breakpoint greps and layout.tsx", "note": "Exactly 24 FRAME children; the only widths on the page are 1280 and 1440. Code: sm:/md:/lg:/xl: = 0, @media|matchMedia|useMediaQuery = 0, flex-col = 5 at exactly :87,:93,:200,:209,:218 all unconditional; the viewing row is max-w-[1100px] flex-1 gap-6 p-6 at :307 with a w-[320px] shrink-0 aside at :322 over an h-[600px] iframe at :71; app/layout.tsx declares no export const viewport."}
{"id": "D-H-30", "verdict": "CONFIRMED", "method": "figma text dump of 121:25 + read review-client.tsx:16-32,:126,:166-167", "note": "Board 121:25 carries 'We couldn't open this right now.' / 'Nothing is wrong with your link. Try again in a moment.' / 'Try again' verbatim. DEAD_LINK_COPY has only EXPIRED/REVOKED/NOT_FOUND/CONFLICT, so an INTERNAL_SERVER_ERROR falls through the `?? DEAD_LINK_COPY.NOT_FOUND` chain at :167 to 'This link doesn't work' / 'Try clicking the link in your email again rather than pasting it.' The query is retry:false at :126 and no retry control exists in the file."}
{"id": "D-H-31", "verdict": "CONFIRMED", "method": "figma string-presence test across all sixteen S5.5 canonical boards + read review-client.tsx:89-92,:148,:169", "note": "16/16 carry 'Ali's Studio', 16/16 carry 'Reviewing:  Bella Cucina · v3' (note the double space), and 0/16 carry 'is asking for your feedback'. Code renders {agency} then that literal on every branch, with <Shell agency=\"Loading\"> at :148 and <Shell agency=\"Buildrick\"> at :169. The correction this finding makes to D-D-05 is sound: the defect is entirely code-side."}
{"id": "D-H-32", "verdict": "CONFIRMED", "method": "figma text dumps of all six echo/canonical pairs + reaction counts + sizes", "note": "All six pairs reproduce. 1339:7162 vs 112:2 share 0 of 5 strings, echo CTA 'Look at {Site}'. 1340:7162 draws 'Your notes'/'Add note' while 114:2 draws pin/1 + a 220x132 composer with 'Post' - two different comment models. 1339:7193 has zero reactions on frame+descendants and says '…ready for you' where 122:3 says '…ready'. 1340:7174's 'If you were sent a new link, use that one' contradicts 120:50. Every echo is 1280x720, every canonical 1280x900."}
{"id": "D-H-10", "verdict": "CONFIRMED", "method": "figma Mode-strip child visibility read across ten S5.5 boards + figma-shot of 122:3, PNG read", "note": "122:3's 'Comment mode' child is visible=false while 117:58's is visible=true, and the rendered PNG shows only the Preview pill with both blue pins still drawn. NOTE FOR THE NEXT VERIFIER: a text dump ALONE refutes this finding - findAll returns hidden nodes, so 'Comment' appears in 122:3's strings. It is invisible, not absent. Two boards the finding does not name do the same thing: 120:2 and 120:50 also hide the Comment tab."}
{"id": "D-I-02", "verdict": "CONFIRMED", "method": "figma effect read on all 17 cited focus variants + text read of 1034:751 + read tokens.generated.css:203, a11y.css:98-105, buttonTheme.ts", "note": "All four definitions reproduce exactly. 1034:751 verbatim; all 17 variants carry DROP_SHADOW #1a56db a=0.25 spread=3 radius=0 offset 0,0; the token is 0 0 0 2px rgba(26,86,219,0.30); a11y.css comments 'NO halo' then sets outline 2px + offset 2px while buttonTheme.ts re-adds tw:focus:[box-shadow:var(--bk-shadow-focus)] on link and ghost. Scope note: 20 nodes on the page are named State=focus - the three the finding omits (14:10, 14:18, 14:29) are Atoms-section controls."}
{"id": "D-I-03", "verdict": "CONFIRMED", "method": "figma fill + opacity read on 981:668/674/692/698 and 9:12/24/84/96 + read buttonTheme.ts:18-31", "note": "981:668/674 are #1a56db and 981:692/698 are #c81e1e, all four at 50%, against the editor set's #e5e7eb chip with a #9ca3af label. The buttonTheme.ts comment and the tw:disabled:bg-[var(--bk-bg-subtle)] emit are verbatim. MECHANISM CORRECTION: the 50% is the NODE's opacity, not the fill's (fill opacity is 1 on all eight) - so the white label is dimmed too, and the editor's compliant variants carry the same 0.5 node opacity; their compliance is the grey fill, not the absence of opacity."}
{"id": "D-I-20", "verdict": "CONFIRMED", "method": "figma node walk of page 1:2 + regex sweep of all 568 TEXT nodes + cross-check against SECTION 1938:8372 on page 1:3", "note": "The 118 count is exactly right under the natural reading: 25 COMPONENT_SETs + 93 standalone COMPONENTs = 118 (276 COMPONENT nodes total once set variants are counted). The regex returns exactly 35 hits and every one is design-system vocabulary, Save-status sample copy, or the single Brand-drawer row 20:85. The file agrees with itself: page 1:3's SECTION 1938:8372 is named '10 · Components · 10 — the editor's saved-components panel; page 1:2 is the LIBRARY'."}
{"id": "D-B-01", "verdict": "CONFIRMED", "method": "figma 154:132 subtree + 1175:4804 + read DSModeContext.tsx:1-20 & grep DSModeToggle", "note": "154:186 carries the exact copy; the only hotspot 1175:4864 -> 1175:4804 'Upgrade modal (403 gate)'; DSMode is a local beginner|pro display pref (buildrik:ds-mode), 0 hits for plan/upgrade/entitle; no DSModeToggle child."}
{"id": "D-B-02", "verdict": "CONFIRMED", "method": "figma child-lists of 1333:7162 / 154:26 / 154:78 / 154:132 / 306:2217", "note": "Headline true - none of the four carries strip 1747:8390, DSModeToggle 1747:8395 or footer 1748:8390. TWO evidence errors: 154:132's text DOES contain 'Pro' ('Switch to Pro to unlock', the very string D-B-01/34 quote), and 154:78's preview is Swatches(4)+Aa Heading+Aa Body, structurally identical to 1333:7162 - not a '3-swatch preview'."}
{"id": "D-B-03", "verdict": "CONFIRMED", "method": "figma 1751:8403 fill/geometry + 1748:8390 + read DesignSystemTab.tsx:761-784, tokens.generated.css:91-93", "note": "dirty-dot visible, 8x8, #8e4b10, x=240 y=232 (inside the Tokens row band y222-274) while footer-clean at y=690 reads 'Brand is up to date'. Code renders it at tw:size-[5px] var(--bk-warning); #8e4b10 is neither #C27803 nor #723B13."}
{"id": "D-B-04", "verdict": "CONFIRMED", "method": "figma 152:112 + 1334:7182 + read StylePresetRegistryContext.tsx:18-21, StylesRouter.tsx:6", "note": "Board rows are exactly Buttons/Cards/Sections/Forms/Nav/Footers; PRESET_CATEGORIES is the 11 named; docstring says '11 category rows'; root row reads 'Presets 18' (6x3)."}
{"id": "D-B-05", "verdict": "CONFIRMED", "method": "figma name sweep /preset/i over all 898 top-level boards + read StylesRouter.tsx:31-33, PresetDetailPane.tsx", "note": "No detail board exists. Sub-error: 306:2111 is NOT text-identical to 152:112 - it adds 'Bound to elements'."}
{"id": "D-B-06", "verdict": "CONFIRMED", "method": "figma 1706:8483 full subtree + frame reactions", "note": "ModalFooter holds only Button-Retry and Button-Discard; single frame reaction returns to 1706:8476 (generating). Codex adds the code twin: AIPromptModal renders Accept only when onAccept is passed, and the Brand panel does not pass it."}
{"id": "D-B-07", "verdict": "REFUTED", "method": "figma getNodeByIdAsync('1706:8492').parent", "note": "Parent is now '07 - Brand - 51' (1776:8373), not Notes. The 2026-09-04 refile moved it; the four AI-prompt modal states are all in Brand today."}
{"id": "D-B-08", "verdict": "CONFIRMED", "method": "figma 775:4305 child list vs 1333:7162 child list", "note": "775:4305 = Panel header 280x44 + seven skeleton rows 280x32 + spacer 280x544; the loaded root is header 44 + strip 56 + toggle 40 + preview 82 + nine 52px rows."}
{"id": "D-B-09", "verdict": "CONFIRMED", "method": "figma 1704:8575 / 1704:8607 subtree + edge walk", "note": "Neither frame contains a button-named node; copy matches verbatim; out-edges are exactly 153:29 and 153:57."}
{"id": "D-B-10", "verdict": "CONFIRMED", "method": "figma 1334:7199 + read DesignSystemTab.tsx:144", "note": "Board row reads 'Components'; NAV entry is { id:'components', label:'Component styles', hint:'What the brand ships' } - hint matches, label drifted."}
{"id": "D-B-11", "verdict": "CONFIRMED", "method": "figma page-wide reaction index (2341 edges)", "note": "152:137 out = 153:2, 1333:7162, 154:78 exactly; 306:2186 in = 963:4474 + 52:2, out = 52:2; 154:2 out = 154:26, 1333:7162 - no edge either way to 306:2217."}
{"id": "D-B-12", "verdict": "CONFIRMED", "method": "figma 57:2 name+text + 1138:13376 in-degree", "note": "57:2 is named 'Empty states - all 11 - copy is final, not placeholder' and carries the No-brand-set copy; 1138:13376 is 'Brand - empty - RETIRED 2026-09-02 (no producer)' with in-degree 0."}
{"id": "D-B-13", "verdict": "CONFIRMED", "method": "figma sweep /picker/i + read TokenDetailView.tsx:193,386", "note": "ColorPicker is inline under the value row; the only picker boards are Inspector token-pickers (1176:4804, 1707:8406/8417) and Media pickers - no Brand colour-picker board."}
{"id": "D-B-14", "verdict": "REFUTED", "method": "figma section 1776:8381 child list + parent lookup on all 15 cited nodes", "note": "All ten 'Reference' Inspector screens and all five 'Notes' popovers are now children of 1776:8381, which holds 37 children. The 2026-09-04 refile resolved this."}
{"id": "D-B-15", "verdict": "CONFIRMED", "method": "figma 824:5095 edges + read InspectorTabContent.tsx:1-14, grep editor/inspector", "note": "824:5095 is 300x552, in-degree 1 from 876:4532, out-edge returns there; grep for role=tablist|activeTab|TabBar across editor/inspector returns nothing."}
{"id": "D-B-16", "verdict": "CONFIRMED", "method": "figma uppercase-header dump of all six profile boards + ls editor/inspector/sections", "note": "INTERACTIONS/VISIBILITY/ELEMENT PROPERTIES/CSS CLASSES/LINK appear in every profile's header list but never inside the 2-4 open sections; all five ship as real sources."}
{"id": "D-B-17", "verdict": "CONFIRMED", "method": "figma text dump of 807:8342/8412/8475/8521/8567/8614", "note": "Captions are exactly '2 of 12','4 of 13','2 of 13','2 of 12','2 of 13','2 of 12' - the finding's list is verbatim."}
{"id": "D-B-18", "verdict": "CONFIRMED", "method": "figma 1707:8456 text + read InspectorErrorBoundary.tsx:40,65", "note": "Board text is exactly 'Inspector Error / Unknown error / Try Again'; :40 is the 'Unknown error' fallback and :65 the literal heading."}
{"id": "D-B-19", "verdict": "REFUTED", "method": "figma section 1779:5 child list", "note": "The Canvas section now holds 10 boards, seven of them real canvas surfaces (breadcrumb bar 1175:4849, inline-edit toolbar 1176:4824, hover levels 1176:4925, four AI popovers). The 'three boards, none of them canvas screens' state no longer holds."}
{"id": "D-B-20", "verdict": "CONFIRMED", "method": "figma name sweep /toolbar/i across all 898 top-level boards", "note": "Exactly three hits - 817:4649 (its own drawn title is 'Canvas toolbar - 7 toggle states'), 1176:4824 (inline-edit) and caption 817:4722. None is UnifiedSelectionToolbar."}
{"id": "D-B-21", "verdict": "CONFIRMED", "method": "figma name sweep /block|swap|replace element/i", "note": "Exactly two hits, 893:4518 and 1069:4707, neither the BlockPickerModal; the modal ships from UnifiedSelectionToolbar.tsx:322 / controls/index.ts:26."}
{"id": "D-B-22", "verdict": "CONFIRMED", "method": "figma 57:2 text + name sweep /blank/i + read CanvasEmptyCTA.tsx:50, CanvasLoadingSkeleton.tsx:2", "note": "57:2 carries the exact CTA copy; no frame is named for the blank canvas; the loading skeleton's docstring does cite board 65:412."}
{"id": "D-B-23", "verdict": "CONFIRMED", "method": "ls editor/ecommerce + figma text of 1719:8391/8421 + caption 304:2107", "note": "editor/ecommerce/ holds only CollectionSetupModal.tsx; the binding UI is inspector/components/BindingBanner+BindingPopover; board copy is verbatim."}
{"id": "D-B-24", "verdict": "CONFIRMED", "method": "figma text dump of 1719:8421 vs 160:105", "note": "All three divergences exact: chain-glyph vs link-glyph, 'Text' vs 'Section' header, This/Desktop/Base pill row on 160:105 only."}
{"id": "D-B-25", "verdict": "REFUTED", "method": "figma parent lookup + edge index", "note": "1719:8443, 1719:8450 and 1719:8414 are all children of '22 - Ecommerce - 5' now, not Notes. Also 1719:8443 has edges both ways with 807:8342 (an Inspector profile), so 'neither has an edge to the inspector profiles' is wrong too."}
{"id": "D-B-26", "verdict": "CONFIRMED", "method": "figma type-aware sweep for /S3.12b/ + FRAME-only S3.* list in Journeys", "note": "303:2082 and 303:2098 are TEXT nodes in Notes; no FRAME named S3.12b exists anywhere. (Journeys' FRAME set is S3.1/2/3/4/6/7/8/9/10/11 - S3.14 has moved out, a detail the evidence gets wrong.)"}
{"id": "D-B-27", "verdict": "CONFIRMED", "method": "figma 1719:8391 text+edges; codex read CollectionSetupModal.tsx:82,118", "note": "Board carries the three assurances, the live sample-products checkbox and Skip/Create; reactions go to 137:2 and 1719:8414 only - nothing covers the post-Skip block."}
{"id": "D-B-28", "verdict": "CONFIRMED", "method": "figma 152:112 text dump", "note": "Zero chevron glyphs on the board; every row is '<Category>' + '3 variants'. Every other Brand list draws the chevron."}
{"id": "D-B-29", "verdict": "CONFIRMED", "method": "figma 153:120 text dump + read ImportCard.tsx:8,43,325", "note": "Board stacks EXPORT, empty dropzone, parsed summary, CSS preview, paste-JSON and Detected/Valid/Errors/Conflicts + Apply all at once. The docstring-vs-type mismatch is real: :8 says 3 strategies, :43 defines 2. (Codex REFUTED this by reading ImportCard's branch logic - a code answer to a question about a drawing.)"}
{"id": "D-B-30", "verdict": "CONFIRMED", "method": "figma dropzone text on 306:2232/2265/2298 + 153:120 + read ImportCard.tsx:272,276", "note": "All three states say 'Drop .css or .json'; parent says 'Drop .json or .ts'; accept is application/json,.json,.ts,.js."}
{"id": "D-B-31", "verdict": "CONFIRMED", "method": "figma 817:4723 text + read shared/constants/canvas.ts:360", "note": "Flyout lists 25/50/75/100/150/200/400; the same board's subtitle says 'Range: 10%-400%' and its first explainer row is the 10% min-zoom; ZOOM_PRESETS has eight entries including 10."}
{"id": "D-B-32", "verdict": "CONFIRMED", "method": "figma fill reads on the profile boards, chip rect paired to the hex TEXT by absolute y", "note": "807:8342 value #1A1A1A / chip #1a57db; 807:8567 #1A56DB / #1a57db; 807:8614 #E2E5F8 / #1a57db. #1A57DB is one digit off the canonical accent."}
{"id": "D-B-33", "verdict": "CONFIRMED", "method": "figma uppercase-header dump of 807:8342 + siblings", "note": "Twelve uppercase section headers, of which the caption says 2 are open - leaving exactly the ten collapsed rows named, with the '2 of 12 sections apply' note the only differentiator."}
{"id": "D-B-34", "verdict": "CONFIRMED", "method": "figma 154:132 subtree vs 1333:7162 subtree", "note": "154:132 draws nine rows with labels/hints/counts identical to the Pro-capable root - nothing is hidden; 1333:7162's toggle caption 'Friendly - hides token IDs and empty foundations' is the accurate description."}
{"id": "D-B-35", "verdict": "CONFIRMED", "method": "figma text diff 154:26 vs 1333:7162", "note": "154:26's text dump contains no string absent from the root (same rows, same 4/18/6/2 counts); no banner, badge or highlighted Lint row."}
{"id": "D-B-36", "verdict": "CONFIRMED", "method": "figma text dump of 153:29 and 1704:8607", "note": "153:29 draws six built-ins plus 'Generate with AI'; 1704:8607 draws only the 'YOUR SAVED COMPONENTS - 0' header and the dashed empty box, no button node, one frame-level hop to 153:57. (Codex REFUTED this from ComponentsSection.tsx - the code, not the board.)"}
{"id": "D-B-37", "verdict": "CONFIRMED", "method": "figma geometry of 333:2360 vs the preview frame + Lint row text", "note": "Badge 'Warnings suppressed' 105x22 at x=16,y=96 sits inside the Brand preview band (y=44-126), over Heading (y84-102) and clipping Body (y110-128); the Lint row still reads '2'. (Codex REFUTED this from DesignSystemTab.tsx - again code for a drawing.)"}
{"id": "D-G-19", "verdict": "CONFIRMED", "method": "figma visible-TEXT position probe on 130:201/130:400/130:599 + read StudioHeader.tsx REVIEW_PILL map", "note": "'In review · 3 open' at x433,y19 and '0 open' at x16,y68 on both pending boards; 130:599 draws '3 open' in both slots. REVIEW_PILL is exactly the six entries quoted (none:null … 'Approved · edited since') with no count in any label."}
{"id": "D-G-22", "verdict": "CONFIRMED", "method": "carrier + destination filter over the warmed 210-edge Journeys table", "note": "The five-hop ring is exact and every hop is @FRAME. Its own evidence is better than its headline, though: there are THREE escapes, not 'one small header hotspot' - 301:1979's 'Header' -> 52:2, 301:2186's hotspot/back -> 199:205, and 301:2393 -> 1176:4824."}
{"id": "D-G-23", "verdict": "CONFIRMED", "method": "page-wide edge scan over all 519 frames on 1:3", "note": "303:1997 <-> 303:2032 is the complete reaction set of both. 303:1997's only outside in-edge is 146:2's hotspot; 303:2032 has none - checked page-wide, not just within Journeys."}
{"id": "D-G-24", "verdict": "CONFIRMED", "method": "edge filter on 130:599/130:997 + read review.service.ts:195-245 + board-name lookup on 131:415 and 131:201", "note": "Both boards' only non-btn/ai edge is @FRAME -> 130:201. The service derives opened-not-acted from PENDING+reviewerId and approved-edited-since from APPROVED+isApprovalStale; neither decays back to pending. 131:415 'S5.6 · re-sent' and 131:201 'S5.6 · approved-edited-since' both exist and neither is a destination of the two boards."}
{"id": "D-G-27", "verdict": "CONFIRMED", "method": "board-name read on all five S5.3 boards + edge filter for the ring and its in-edges", "note": "All five names carry 'SUPERSEDED 2026-09-02 by …, ReviewTab.tsx:…' verbatim; the ring and both 132:2 escapes are exact; in-edges into 132:2 are exactly 307:2203, 807:8723, 817:5006 and 133:2. COUNT CORRECTION: the chain carries 12 outbound edges, not ten - or 7 once the repeated btn/ai instance is excluded. Neither reading gives ten."}
{"id": "D-G-28", "verdict": "CONFIRMED", "method": "carrier classification over the warmed 210-edge table", "note": "Exactly 28 of 210 edges (13.3%) are carried by a node named btn/ai, from 28 DISTINCT boards, and all 28 resolve to 170:17. The 27 carriers listed are exact; the 'and others' is one board, 52:2."}
{"id": "D-G-30", "verdict": "CONFIRMED", "method": "figma frame fill + child geometry read on 296:1972/296:1999/296:2030", "note": "All three are 1440x900 on flat #f3f4f6 with one 340x263 'checklist' at x=1068,y=604 = 6.9% of board area (93.1% empty). Each also carries one invisible hotspot child, so 'single child frame' is true of content only. The x=1068-1408 span against a 1140-1440 inspector column holds arithmetically."}
{"id": "D-H-07", "verdict": "CONFIRMED", "method": "per-frame reaction sweep of all five share/transfer boards + page-wide destination set on 1:6", "note": "854:30, 854:41 and 869:7005 have zero reactions; 854:35's one edge is Accept handover -> 854:41; 869:6999's three hotspots point into the review dead-link boards. The page's destination set contains 854:41 and none of the other four. COUNT CORRECTION: that makes FOUR of the five unreachable (854:30, 854:35, 869:6999, 869:7005), not three - 854:35 has no inbound edge either, which the finding's own destination evidence shows but its headline misses."}
{"id": "D-H-09", "verdict": "CONFIRMED", "method": "figma x/y read of every frame and every caption on page 1:6 + header text and position read", "note": "Frame pitch 1520 at y=60/1300/2540/3780/5020/6000/6980/7960 and caption pitch 1380 at y=2016/3016/4016/5016 - both exact. 'A0 · empty' at (0,2016) sits inside 114:2's box; the y=5016 caption row against the y=5020 frame row mismatches exactly as filed; 'Review Flow · 1 screens' does sit above 854:30 (Share); claimed total 21 against 24 frames; and exactly EIGHT frames have no caption (854:30/35/41, 869:6999/7005, 1736:8389/8397/8405). COUNT CORRECTION: there are 16 captions, not 20 - the finding's own id list (122:51-64 + 125:96/97) is 16."}
{"id": "D-I-01", "verdict": "CONFIRMED", "method": "full-depth node walk of page 1:2 + header text read", "note": "25 COMPONENT_SET + 93 standalone COMPONENT = 118, and the six index header pairs are exactly 1054:13380-13391 (Editor Component Sets / Editor Components / Dashboard Component Sets / Dashboard Components / Icons / Documentation & Showcase)."}
{"id": "D-I-10", "verdict": "CONFIRMED", "method": "figma header text read + set/component classification + parent lookup on all twelve cited buried nodes", "note": "Headers read '14 items' and '7 items'; true non-Dashboard COMPONENT_SET count is 19, and the five uncounted are exactly Toggle 14:14, Radio 14:20, Select 14:37 (in 14:45), Nav item 16:26 (in 16:27), Comment row 17:40 (in 17:41). All seven uncounted components sit inside 14:45/16:27/17:41 as filed. Dashboard 6/12 accurate. COUNT NOTE: the headline says 'ten real components' where its own evidence lists twelve (5 sets + 7 components)."}
{"id": "D-I-11", "verdict": "CONFIRMED", "method": "figma absolute-position + parent lookup on 1695:755 and 1695:760", "note": "Both are COMPONENTs parented directly to page 1:2 (not to 91:6), at absolute (1104,0) and (1140,0) - inside the Editor Component Sets header band at 0,0, hundreds of px from the icon band."}
{"id": "D-I-18", "verdict": "CONFIRMED", "method": "full-depth type census of page 500:2", "note": "COMPONENT=0, COMPONENT_SET=0, INSTANCE=0, FRAME=152 - exact. One child, 500:3 'Element' 1440x1024. Worth flagging to the caller: the page has since been RENAMED to '🗄️ ARCHIVE — foreign UI kit reference … audited 2026-09-04', so this finding has already been acted on in the file."}
{"id": "D-I-19", "verdict": "CONFIRMED", "method": "full-depth type census of page 510:2", "note": "COMPONENT=0, COMPONENT_SET=0, INSTANCE=0 - exact - and the six SECTIONs carry exactly the cited names. COUNT CORRECTION: FRAME=8346, not 4902; the filed number looks like a walk that did not descend through the page's 3286 GROUPs. Also renamed to '🗄️ ARCHIVE — purchased admin template … audited 2026-09-04', i.e. already acted on."}
{"id": "D-C-01", "verdict": "CONFIRMED", "method": "figma nav-model census of all 45 frames in 1776:8387 + read SettingsTab.tsx:288,829-831, settings.css:24-33,61-67", "note": "Census reproduces 39/45 exactly (12 detail + 26 states + 1 locked all draw Rail@x0w60 + 'Settings nav (140)'@x60 + 'Settings pane'@x200w1240; only 1688:7195 differs, with rows at x=40 w=560 and x=640 w=560 and no Rail). Code is the drill-in stack: both screens position:absolute inset:0, .bd-set-snav is flex:1 root content."}
{"id": "D-C-02", "verdict": "CONFIRMED", "method": "figma text of 1344:7162 + read tabsConfig.ts:190-200, SettingsTab.tsx:400/473/870", "note": "Note text is verbatim including 'One column, drawer width - never a two-pane master-detail'; frame name carries RETIRED 2026-09-02. Width claim is stale (mode:'fullpage'); the model claim is exactly what push/pop + DrillInHeader implement."}
{"id": "D-C-03", "verdict": "CONFIRMED", "method": "figma 1688:7195 text + read SettingsTab.tsx:767,841-843,845-856", "note": "Root board text ends at 'Webhooks'; code renders the WORKSPACE group ('opens dashboard'), the Tour row and a per-row Pro badge. All 12 detail boards do draw WORKSPACE/Members/Billing."}
{"id": "D-C-04", "verdict": "CONFIRMED", "method": "figma text of 640:3849/1703:9694/1703:10352/927:4474 + read WebhooksScreen.tsx:94-97 + find dashboard routes", "note": "Board copy verbatim; the permission-denied branch is at :94-97; the only dashboard 'webhooks' route is the Stripe API endpoint, not a management UI."}
{"id": "D-C-05", "verdict": "CONFIRMED", "method": "read IntegrationsScreen.tsx:37 + ls dashboard integrations route", "note": "Every row is hardcoded status=\"soon\" with a docs link-out; the working connect flow is packages/dashboard/app/dashboard/settings/integrations."}
{"id": "D-C-06", "verdict": "CONFIRMED", "method": "figma text of 1138:13436 + grep -i hsts|csp across dashboard app+components", "note": "The Custom-code lock board carries Headers' subtitle AND body; 0 hits for hsts/CSP anywhere in the dashboard - Headers has no second door."}
{"id": "D-C-07", "verdict": "CONFIRMED", "method": "figma text of 640:3135 + read FormsScreen.tsx:62,272,290,299", "note": "Board does stack 'PROVIDER / Form provider / Formspree' above SUBMISSIONS with Export CSV in the save slot. Correction worth carrying: the shipped screen has NO provider block at all - it is inbox + a four-way filter tablist + pagination + Export CSV, so the two jobs are a board-only pairing."}
{"id": "D-C-08", "verdict": "CONFIRMED", "method": "read SettingsTab.tsx:728-740, defaultCommands.ts:441, SiteMenu.tsx:211, StudioHeader.tsx:815, useComposerInit.ts:447, StudioModals.tsx:166", "note": "Every cited line is exact: the Settings screen is a three-line signpost + 'Open exporter', and the modal has four other doors."}
{"id": "D-C-09", "verdict": "CONFIRMED", "method": "read ProjectSettingsModal.tsx:120-124,223; SiteSettingsScreen.tsx:115-122; useEditorShortcuts.ts:119-122; SiteMenu.tsx:195", "note": "Both surfaces write seo.siteName under different labels; ctrl/cmd-, and the Site menu both open the MODAL; the 45-board tab is only on the rail shortcut."}
{"id": "D-C-10", "verdict": "CONFIRMED", "method": "read SettingsTab.tsx:135-138,148-154,209 + tabsConfig.ts:179,312 + SiteMenu.tsx", "note": "Comment says branding spans two canonical homes; BRANDING_FIELD_MAP has exactly five rows with jumps to general and seo; the CTA reads 'Open Palette' while the rail says Brand and the tool says Styles."}
{"id": "D-C-11", "verdict": "CONFIRMED", "method": "read SiteSettingsScreen.tsx:123-130 + dashboard seo/settings tabs + BuildrikSyncProvider + publish-html.ts", "note": "Favicon URL is a General field writing seo.favicon; the dashboard writes the Site column; the sync provider mirrors touchIcon only. Three surfaces, no stated winner."}
{"id": "D-C-12", "verdict": "CONFIRMED", "method": "figma text dump of 1138:13436", "note": "Board renders 'Custom code' with subtitle 'CSP, HSTS, security policy' - Headers' subtitle. (Codex REFUTED this by reading LockedScreen.tsx, which is a code answer to a question about the drawing.)"}
{"id": "D-C-13", "verdict": "CONFIRMED", "method": "read DomainsScreen.tsx:211-212,242 + WebhooksScreen.tsx:97,189-190", "note": "Both strings are literal in code ('Is the dashboard running?') and both render into .bd-set-section-d with no role=alert and no retry."}
{"id": "D-C-14", "verdict": "CONFIRMED", "method": "figma string search over all 45 frames of 1776:8387", "note": "Zero hits for /retry|try again/i across the whole module. Exact."}
{"id": "D-C-15", "verdict": "CONFIRMED", "method": "figma string search over all 45 frames + read SettingsTab.tsx:883-904,909-986", "note": "Zero hits for 'unsaved' and zero for 'discard'; the code ships both the savebar and the ConfirmDialog."}
{"id": "D-C-16", "verdict": "CONFIRMED", "method": "figma 1703:8882 + codex located the string at FormsScreen.tsx:248", "note": "The empty-state sentence is real product copy carrying an engineering caveat, while 640:3135 shows captured submissions with no caveat."}
{"id": "D-C-17", "verdict": "CONFIRMED", "method": "figma state-board name census + read DomainsScreen/LocalizationScreen/RedirectsScreen error strings", "note": "Forms uses 'error'/'action-error' where five siblings use load-error/save-error; the voice split (Couldn't vs Failed to) is in the code strings too."}
{"id": "D-C-18", "verdict": "CONFIRMED", "method": "figma per-screen state census of 1776:8387", "note": "Counts reproduce exactly: Domains 4, Forms 6, Webhooks 5, Redirects 4, Localization 3, Headers 3, Analytics 1 validation, six screens 0. Correction: the closing rationale is wrong - IntegrationsScreen ships no OAuth connect at all (every row is status=\"soon\"), which the same agent's own D-C-05 says."}
{"id": "D-C-20", "verdict": "CONFIRMED", "method": "figma footer text search across all 45 frames", "note": "40 of 45 carry 'Section - Hero / 680 x 250 / Desktop - 100%'. The evidence says 'all 45'; the five without are the two design-ahead health panels, the two note boards and the modal - i.e. every full-shell board does carry it, but the stated count is 5 too high."}
{"id": "D-C-21", "verdict": "CONFIRMED", "method": "figma 1688:7195 row dump + read SettingsTab.tsx:78-91", "note": "Rows are title+subtitle+chevron only. Count correction: TEN of the 13 pass IntegrationsIcon, not 8 - and the finding's own list names all ten, so the number contradicts its own evidence."}
{"id": "D-C-22", "verdict": "CONFIRMED", "method": "figma section child list + read types.ts:58-72, SettingsTab.tsx:767", "note": "SCREEN_PLAN_REQUIREMENTS gates custom-code and integrations; only 640:3488 exists for Integrations and it is not a lock board; the docstring records the id mismatch verbatim."}
{"id": "D-C-23", "verdict": "CONFIRMED", "method": "figma census + per-board out-degree walk of 1776:8387", "note": "26 state frames, all 1440x900, 26/45 = 58%. Out-degree correction: measured 23-25, not the stated 25-27."}
{"id": "D-C-24", "verdict": "CONFIRMED", "method": "figma 1688:7195 nav rows + read SiteMenu.tsx:220-222", "note": "No Health row in the 13-row nav; the product's door opens the dashboard #site-health in a new tab."}
{"id": "D-C-25", "verdict": "CONFIRMED", "method": "grep SeoScreen.tsx (0 hits) + read dashboard seo-tab.tsx:104-105,111-112,119", "note": "robotsTxt/allowIndexing/canonicalUrl have zero editor hits and live in the dashboard, whose own comment calls them 'server-side knobs the editor doesn't own'."}
{"id": "D-C-26", "verdict": "CONFIRMED", "method": "read pages.ts:71,77,90 + grep for UI callers", "note": "getTranslation/setTranslation/removeTranslation exist and have no caller in either app; the dashboard's own orphan checker records them as having no i18n UI."}
{"id": "D-C-27", "verdict": "CONFIRMED", "method": "read AnalyticsScreen.tsx:280-299 + lib/publish-html.ts:22-28", "note": "cookieConsent is written and read by nothing (the screen says so in its own comment) while injectAnalyticsBeacon adds a localStorage+sendBeacon tracker to every published page unconditionally."}
{"id": "D-C-28", "verdict": "CONFIRMED", "method": "grep siteDetail.sharing + templates.cloneFromSite + read SiteMenu.tsx:266-275", "note": "The lifecycle jobs do lack editor surfaces. Correction: 'sharing/access' is not doorless - SiteMenu ships 'Share preview link', which hands off to the dashboard's ShareDraftModal rather than calling siteDetail.sharing itself."}
{"id": "D-C-29", "verdict": "CONFIRMED", "method": "figma 640:2440 text + grep server/trpc/routers for sitemap|notFound + read publish-files.ts:70-76, SitemapGenerator.ts", "note": "No tRPC procedure for a sitemap or custom 404 - true. Correction: a sitemap IS produced outside tRPC (SitemapGenerator + publish-files writes robots.txt with a sitemap pointer), so only the custom-404/suggester half of the gap is real."}
{"id": "D-C-30", "verdict": "CONFIRMED", "method": "read DomainsScreen.tsx:159,222,287 + figma Domains board census", "note": "Both cited disables carry ADMIN_REASON tooltips and :159 routes a remove failure into loadError; Domains' only state boards are loading/empty/load-error/save-error. (Codex REFUTED this by re-stating the finding's own premise.)"}
{"id": "D-C-31", "verdict": "CONFIRMED", "method": "figma topbar string scan of every child of 1776:8385 + read SaveStatus.tsx:41-52, StudioHeader.tsx:552-562", "note": "Every enumerated board carries 'Saved 2m ago' - including Loading, Offline and 'Saving -> conflict'. Count correction: the section holds 15 full-shell boards carrying it (963:4474 too), not 12; the evidence itself lists 14."}
{"id": "D-C-32", "verdict": "CONFIRMED", "method": "figma fill+opacity read on 66:640's scrim child", "note": "Layer is named 'scrim rgba(15,23,42,.4)' but has nodeOpacity 1 and a SOLID fill #111827 at opacity 1. Both the alpha and the hex contradict the name."}
{"id": "D-C-33", "verdict": "CONFIRMED", "method": "figma text dump of 66:640 + read ConflictModal.tsx", "note": "Board actions are Review both / Save backup / Keep mine with the exact quoted copy; the shipped set is Reload latest / Save a backup / two-step Overwrite."}
{"id": "D-C-34", "verdict": "UNSUPPORTED", "method": "figma parent lookup on 1172:4804 only", "note": "One of its five examples is now false - 1172:4804 sits in the Shell section 1776:8385, not Notes. I did not check the four Journeys/Reference examples, so the finding is neither confirmed nor refuted as a whole."}
{"id": "D-C-35", "verdict": "REFUTED", "method": "figma section 1776:8385 child list", "note": "1172:4804 'Exit - guard - dirty - risky-offline - stranded' is a child of the Shell section now, not of Notes. The filing complaint no longer holds (its content and inbound-edge observations still do)."}
{"id": "D-C-36", "verdict": "CONFIRMED", "method": "figma in/out edge walk on 963:4474 + codex read LayoutShell.tsx:267", "note": "In-degree exactly 1, from 876:4532 'Reference - index'. Out-degree correction: 55 edges, not 59. drawerPinned removal confirmed."}
{"id": "D-C-37", "verdict": "CONFIRMED", "method": "figma Middle-band geometry of 65:412 vs 199:205/200:2 + read StudioFooter.tsx:171", "note": "65:412 = Rail 60 / Canvas 1080 / Inspector 300, no drawer - a drawer-closed shell mid-load, matching neither the StudioSkeleton boot state nor the loaded state."}
{"id": "D-C-38", "verdict": "CONFIRMED", "method": "figma footer text nodes on 199:205/638:2378 + read StudioFooter.tsx:227-237 + CanvasFooterToolbar.tsx:318", "note": "Both footer readouts are bare TEXT on the board; in code the zoom readout is a button opening a flyout, and the device name is not the switcher."}
{"id": "D-C-39", "verdict": "CONFIRMED", "method": "figma 1175:4804 name+text+edges + grep openUpgrade across editor/src", "note": "Board is literally named 'Upgrade modal (403 gate)' and says 'Premium templates requires the Pro plan.'; openUpgrade has exactly two call sites, TemplatesTab.tsx:177 and :188, both premium-template clicks; no 403 interceptor exists. Codex marked this REFUTED while citing the same two call sites that prove it."}
{"id": "D-C-41", "verdict": "CONFIRMED", "method": "figma frame sizes in 1776:8385", "note": "66:640 is a full 1440x900 shell with scrim and card; 1175:4804 is 420x225, 927:4474 520x216 and 1172:4867 560x244 - bare cards with no shell and no scrim."}
{"id": "D-C-42", "verdict": "CONFIRMED", "method": "figma text scan of 199:205, 66:441, 66:225, 200:2, 200:213, 199:409", "note": "All six carry the Home/close-glyph tab pattern."}
{"id": "D-C-43", "verdict": "REFUTED", "method": "figma walk of EVERY reaction carrier on every FRAME in 1776:8385", "note": "There is no btn/exit reaction on any Shell board, and 963:4474's edge to 65:412 is carried by a node named 'hotspot/state - Shell state 12 - Loading', not an exit button. The only Exit-named carrier in the section is on 927:4474 -> 1172:4804. The real state is stronger than the claim (wired on zero boards, not one) but the stated evidence is wrong on both carrier and destination; '17 Shell boards' also matches neither the 18 frames nor the 15 full-shell ones."}
{"id": "D-C-44", "verdict": "REFUTED", "method": "figma /activity/i census over all 898 top-level boards", "note": "Board 817:5114 'S5.10 - activity-log' (FRAME 360x812, Journeys) exists. The census searched the string 'activity log' with a space and missed the hyphen - the classic too-narrow-matcher absence claim."}
{"id": "D-C-46", "verdict": "CONFIRMED", "method": "figma full text + Middle-band geometry of 66:441 vs 200:2/66:4 + read SaveStatus.tsx:50, StudioHeader.tsx:552-553", "note": "66:441 contains no 'offline'/'reconnect'/'not saved' anywhere, carries 'Saved 2m ago', and its band is byte-for-byte the default layout (Rail 60 / Drawer pinned 280 / Canvas 800 / Inspector 300). Code ships a yellow 'Offline - not saved' pill that outranks every other save state."}
{"id": "D-C-47", "verdict": "CONFIRMED", "method": "figma Middle-band geometry of 199:2 and its five siblings", "note": "199:2 = Canvas@x60w1080 with 'Drawer (transient)'@x60w280 on top; 199:205/66:441/200:2/66:4 = 'Drawer (pinned)'@x60w280 + Canvas@x340w800. Two models in one section."}
{"id": "D-C-48", "verdict": "CONFIRMED", "method": "figma text dump of 1703:10352 and 1702:7261 + read DomainsScreen.tsx:211-212,242 / WebhooksScreen.tsx:189-190,221", "note": "'Add endpoint' is drawn in the header of the forbidden board whose body says only an admin can manage webhooks; both blocking messages render as plain .bd-set-section-d body text. (Codex REFUTED this from current code - a code answer to a question about the drawing.)"}
{"id": "D-C-49", "verdict": "CONFIRMED", "method": "read LeftSidebar.tsx:105,171,628,638 + tabsConfig.ts:349-350", "note": "SETTINGS_DIRTY_SET is passed to FigmaRail, whose RAIL_FIGMA ids are add/layers/pages/assets/content/design - 'settings' is not among them, so the dirty dot can never render on the shipping rail."}
{"id": "D-F-01", "verdict": "CONFIRMED", "method": "figma text dump of 148:2 + read tabsConfig.ts:254", "note": "148:2 has exactly 21 text nodes (Content, COLLECTIONS 2, five rows, DATA, three rows) and no purpose line; ariaLabel is 'Data sources and collections for dynamic content'."}
{"id": "D-F-02", "verdict": "CONFIRMED", "method": "figma non-text inventory of 148:2 + read ContentViews.tsx:204-206", "note": "The row glyphs are a plus-vector pair; the 'New collection' row uses a TEXT '+'; the shipped rows use Database / Braces / GitBranch."}
{"id": "D-F-03", "verdict": "CONFIRMED", "method": "figma reaction carrier on 148:2 + text of 151:2 + edge index for 1170:4713", "note": "Carrier 'Row - New collection' navigates to 151:2, whose back link reads 'Menu items - fields'; the setup modal 1170:4713 is reachable only from a hotspot on 149:50. (Codex REFUTED this from ContentTab's onCreateCollection wiring - the code, not the prototype.)"}
{"id": "D-F-04", "verdict": "CONFIRMED", "method": "figma name sweep /dynamic|slug pattern/i over all 898 boards + read ContentTab.tsx:299, useContentPanel.ts:28, cms.ts:92", "note": "Zero board hits; 149:50's 'Dynamic pages' row carries no reaction while the product ships the drill-in, the view kind and generateDynamicPages."}
{"id": "D-F-05", "verdict": "CONFIRMED", "method": "figma text of 149:50 and 1170:4749 + read ContentTab.tsx + CMSRecordsModal.tsx", "note": "149:50 is a 280px list with chevrons and '+ Add'; 1170:4749 is a 640px modal with per-row Unpublish/edit/delete plus Import JSON and Add record. Both ship."}
{"id": "D-F-06", "verdict": "CONFIRMED", "method": "figma text of 151:87 + read ContentViews.tsx:1044-1096 + grep editor/inspector", "note": "ConditionsView rows are keyed by elementId and 'Select element' is the only way to see the subject; the Inspector has no conditions section."}
{"id": "D-F-07", "verdict": "CONFIRMED", "method": "figma geometry+text of 149:50", "note": "Header says '24 records' over four rows; 'Fields' and 'Dynamic pages' sit flush at the bottom of the 812px panel with a large void above and no scroll behaviour specified."}
{"id": "D-F-08", "verdict": "CONFIRMED", "method": "figma text of 775:4241 + read LayersTab.tsx:213-216", "note": "The loading board prints 'COLLECTIONS 2' and 'DATA' with every row a skeleton; the Layers code states the opposing rule verbatim ('a count of a list that did not load is not a fact')."}
{"id": "D-F-09", "verdict": "CONFIRMED", "method": "read cms.ts:69, cmsSync.ts:189-197, CollectionManager.ts:154, CollectionStorage.ts:129 + exhaustive grep of packages/editor/src", "note": "deleteCollection has no UI caller anywhere: useContentPanel only calls deleteContentItem/deleteField, and useCmsSync.ts:87 is a LISTENER on the delete event, not a trigger. Full stack, no handle."}
{"id": "D-F-10", "verdict": "CONFIRMED", "method": "read CMSRecordsModal.tsx:154 (0 'confirm' hits in file) + ContentViews.tsx:306,381,573", "note": "The modal deletes directly; the panel wraps the same delete in ConfirmDialog, and two more confirms ship. The code also answers the finding's open question - its own comment records that CMS records are not in the undo stack."}
{"id": "D-F-11", "verdict": "CONFIRMED", "method": "figma text+geometry of 1170:4749", "note": "The 'Width stays 640: F15 adds the matching kind...' note is a real TEXT node in the board body and the four state hotspots sit inside the modal body, overlapping the record rows."}
{"id": "D-F-12", "verdict": "REFUTED", "method": "grep 'bulk-mode' across packages/editor/src + read PagesTab.css", "note": "The premise is false. `.bulk-mode` is applied as a class at PagesTab.tsx:214 but NO CSS rule anywhere keys off it - PagesTab.css has no .bulk-mode selector at all. Three separate comments (PageRow.tsx:6, :207, PagesTab.tsx:211) assert a CSS gate the stylesheet does not implement, so the shipped panel shows the checkbox column permanently, exactly as the board draws it. The evidence read the comment, not the call site."}
{"id": "D-F-13", "verdict": "CONFIRMED", "method": "figma label geometry on 141:78 + read BulkToolbar.tsx:93, PagesTab.tsx:131-133", "note": "Measured: 'Duplicate'@x96 w54 and 'Move to...'@x126 w56 - a 30px pitch for a 54px label, so they collide; no Cancel/close/Clear text in the band. Correction: 'the shipped exit is the Escape key alone' is wrong - BulkToolbar.tsx:93 renders a 'Clear selection' close button."}
{"id": "D-F-14", "verdict": "CONFIRMED", "method": "figma /delet|confirm|discard|remove/i census over all 898 top-level boards + read PagesTab.tsx:122,345-349", "note": "Both confirms ship (the bulk one names every page and promises one undo) and ZERO matching frames sit in Pages - the headline holds. Count correction: the census returns 19 frames across 12 sections, not 29; the finding's own enumeration sums to 13."}
{"id": "D-F-15", "verdict": "CONFIRMED", "method": "figma frame-size census of section 1776:8377 + text of 1717:17217", "note": "1717:17217 is 280x320 with no search box, no Listings toggle, no folders, a bare '+' and an implementation note as body text, against 280x812 siblings. Count correction: there are 11 such siblings, not 12."}
{"id": "D-F-16", "verdict": "CONFIRMED", "method": "figma text of 141:207 + read PagesTab.tsx:251-258,260", "note": "Four SEO columns in a 280px drawer; the shipped panel had to add the 'Pages' back link the board omits; 'Open full listings' is wired to onExpandToggle, which widens the same panel. (Codex REFUTED this by restating the finding's own evidence.)"}
{"id": "D-F-17", "verdict": "CONFIRMED", "method": "figma text of 141:165 + codex read PageList.tsx:239,368", "note": "The board carries 'Couldn't load your pages.', 'Try again' AND '+ Add page'; in code loadError replaces only the list body while AddPageButton renders outside that branch."}
{"id": "D-F-18", "verdict": "CONFIRMED", "method": "figma frame-size census of 1776:8377 + read StudioPanels.tsx (PageTabBar mount)", "note": "435:2348 is 1080x920 in a section of 280x812 panel screens and names itself PageTabBar.tsx; it is canvas chrome mounted by the shell, not a Pages panel screen."}
{"id": "D-F-19", "verdict": "CONFIRMED", "method": "read PagesTab.tsx:126-129 + StudioHeader.tsx:249-251", "note": "Both bind cmd/ctrl-K, one on window and one on document, and neither stops propagation - with Pages mounted one keypress reaches both palettes."}
{"id": "D-F-20", "verdict": "CONFIRMED", "method": "figma text of 140:2 + read panels/layers/index.tsx:388", "note": "140:2's whole text is Pages / Search / Listings / rows / + Add page - no purpose copy; the Layers sibling now ships one."}
{"id": "D-F-21", "verdict": "CONFIRMED", "method": "figma geometry+text of 142:2 and 140:2", "note": "Both 280x812; both title an 11px Medium TEXT at x16,y14; both put search below it; neither board carries a purpose line. The product has fixed only the Layers side."}
{"id": "D-F-22", "verdict": "CONFIRMED", "method": "read useLayerTree.ts:97, panels/layers/index.tsx:431, LayersEmptyState.tsx:21,32", "note": "setLayers([buildTree(rootElement,0)]) always emits a root row, so layers.length===0 means no page/composer - yet index.tsx:431 renders LayersEmptyState, whose copy is this board's, on exactly that condition."}
{"id": "D-F-23", "verdict": "CONFIRMED", "method": "figma first-rows dump of all twelve tree boards + read useLayerTree.ts:97, index.tsx:440", "note": "Only 1082:4640 draws the root ('Page'); the others omit it and the code never skips depth 0. Correction: of the eleven named, 143:2 (filtered) begins at 'Button' and 775:4130 (loading) draws skeletons, so 'all begin at Section' is true of nine."}
{"id": "D-F-24", "verdict": "CONFIRMED", "method": "figma render/text of 143:60 + read panels/layers/types.ts:56, index.tsx:388", "note": "types.ts:56 is exactly 'before' | 'after' | 'inside'; the purpose line advertises nesting; the board shows only a sibling insertion line."}
{"id": "D-F-25", "verdict": "CONFIRMED", "method": "figma text dump of 143:119", "note": "Both nodes are present as claimed: the tooltip TEXT reads 'A button can't contain a section' and '(empty slot - reserved so selecting a row cannot reflow the tree)' is drawn inside the panel over the tree rows."}
{"id": "D-F-26", "verdict": "CONFIRMED", "method": "figma text of 1171:4829 + read LayerDisplaySettings.tsx:34,41,53,64", "note": "Board offers Show hidden layers / Show lock badges / Compact rows / Highlight CMS-bound; the shipped popover offers Show HTML tags / Show element IDs / Compact rows under a 'Display Settings' header. Exactly one option in common."}
{"id": "D-F-27", "verdict": "CONFIRMED", "method": "figma text diff 143:237 vs 142:2", "note": "Byte-identical text - same eight rows, same '12 layers' footer; only a glyph fill differs."}
{"id": "D-F-28", "verdict": "CONFIRMED", "method": "figma text of 775:4130, 143:2, 782:4260, 781:4217 + read LayersTab.tsx:213-216", "note": "Loading prints '12 layers' over skeletons and filtered prints '12 layers' beside one matching row, while the two error boards print nothing - and the code's own sentence condemns the first."}
{"id": "D-F-29", "verdict": "CONFIRMED", "method": "figma text of 1082:4527 + read LayerContextMenu.tsx:57-59,81", "note": "Board menu is Cut/Copy/Paste/Duplicate/Delete/Rename/Group selection/Move to page.../Copy link with no enabling condition stated; the code omits Move to page... deliberately and ships Copy link fourth. Minor: 'Move to page...' is the board's EIGHTH item, not the ninth."}
{"id": "D-F-30", "verdict": "CONFIRMED", "method": "figma first-text dump of 1082:4640 vs the eleven siblings", "note": "1082:4640 is the only tree board with header controls (a refresh and a close glyph) and the only one drawing the root row 'Page'."}
{"id": "D-F-31", "verdict": "CONFIRMED", "method": "figma text of 170:2 + 171:67 + read EmptyThread.tsx:52, applySetStyle.ts:642, useAgentRunner.ts:280", "note": "Verified at the CALL SITE, not the docstring: applyAiEdit opens composer.beginTransaction('ai-edit') once per call, and useAgentRunner.approve calls it once per approved STEP - so a three-step run really is three undo entries. The promise sits at y=304 immediately above the DRAFT row (y=348) that launches that run, and 171:67's own footer says the opposite."}
{"id": "D-F-32", "verdict": "CONFIRMED", "method": "figma text+reactions of 170:29 + read Composer.tsx:43-46", "note": "170:29's entire content is 'Thinking...' with one frame-level reaction and no control of any kind, while the product renders a Stop button whenever streaming. (Codex REFUTED this by citing the very Stop button the finding says the board is missing.)"}
{"id": "D-F-33", "verdict": "CONFIRMED", "method": "figma text of 171:67 + read AgentPlan.tsx:176", "note": "171:67 draws three ticked steps, a live 'Stop', AND a '3 changes proposed / Discard / Apply' card - both run and pending. Code gates Stop to planning|running, exactly as the finding says. (Codex REFUTED this by restating that gate.)"}
{"id": "D-F-34", "verdict": "CONFIRMED", "method": "figma frame census of section 1776:8380 + read StudioPanels.tsx:298, tokens.generated.css:131, AITab.css:1-6", "note": "Exactly eleven AI frames, every one 360x776, ids matching the list; the panel mounts in the 300px inspector column and .bd-ai-tab declares no width."}
{"id": "D-F-35", "verdict": "CONFIRMED", "method": "figma text diff 170:17 vs 170:2 + read ScopeChip.tsx, EmptyThread.tsx", "note": "170:17 keeps the band and drops TRY and DRAFT entirely; the code renders the band on every state and the TRY/DRAFT blocks whenever the thread is empty, regardless of scope."}
{"id": "D-F-36", "verdict": "CONFIRMED", "method": "read ChatThread.tsx:20-36, DiffRows.tsx + figma text of all eleven AI boards", "note": "ChatThread maps a messages array and scrolls on change; every one of the eleven boards draws a single prompt and a single response."}
{"id": "D-F-37", "verdict": "CONFIRMED", "method": "figma text+reactions of 171:105 and 171:136", "note": "Each board has exactly one control and exactly one reaction, both returning to 170:2 (AI - idle); neither reaches Settings/S7. Correction worth carrying: in CODE those two buttons window.open dashboard pages (AITab.tsx:273,290), so the dead end is a board defect, not a product one."}
{"id": "D-F-38", "verdict": "REFUTED", "method": "grep ai.summarize across packages/editor/src", "note": "ai.summarize DOES have an editor caller: editor/panels/version-history/useAISummary.ts:109 fetches '/api/trpc/ai.summarize' directly. A raw fetch to the tRPC HTTP endpoint, which a grep for the typed client property misses - the same too-narrow-matcher absence claim as D-C-44. The other six procedures in the row check out."}
{"id": "D-H-25", "verdict": "CONFIRMED", "method": "read review-client.tsx:334-352 + client-review.service.ts:257-270 + figma text scan of page 1:6", "note": "'Add note' calls comment.mutate({token, body: draft}) with no status change and no cap; listClientComments is scoped to the SITE and ordered createdAt asc, so round-1 notes reappear after a re-send. No board on 1:6 draws two or more notes, a note count, or a come-back-and-decide prompt."}
{"id": "D-H-26", "verdict": "CONFIRMED", "method": "grep of 'resolve.' across review-client.tsx (independent) + codex exec cross-check, agreeing line-for-line", "note": "resolve. appears at exactly :129 (hook), :292/:299/:447 (disabled), :407/:425 (mutate), :451/:456 (pending labels) - never .error. comment.error IS surfaced twice, at :353-354 and :390-394. Codex, asked the same question cold, returned the same seven render/disable sites and the same verdict."}
{"id": "D-H-29", "verdict": "CONFIRMED", "method": "read client-review.service.ts:97,:149 + grep data.note in review-client.tsx + codex cross-check", "note": "getReviewByToken selects note at :97 and returns review.note at :149; 'data.note' occurs zero times in review-client.tsx. Codex independently returned the same."}
{"id": "D-H-35", "verdict": "CONFIRMED", "method": "read client-review.service.ts:273-293 + its import list + grep for every notifyReviewSubmitted/notifyReviewResolved call site + codex cross-check", "note": "resolveReviewByToken does one prisma.reviewRequest.update and returns; the file imports only node:crypto and @/lib/prisma and contains zero notify/sendEmail/logAuditEvent/webhook/createNotification. notifyReviewSubmitted/Resolved are defined at review.service.ts:544/:589 and called only at :111 and :481 - both agency-side paths. Codex confirmed independently."}
{"id": "D-H-03", "verdict": "CONFIRMED", "method": "figma size + text dump of 1736:8389/8397/8405 and 121:2 + hotspot child read", "note": "The three are 1280x720 carrying '{Agency}' / 'is asking for your feedback'; every one of the sixteen canonical S5.5 boards is 1280x900 carrying 'Ali's Studio' / 'Reviewing:  Bella Cucina · v3'. 121:2 carries hotspots 1737:30/34/38 named exactly 'hotspot/state · dead-link · revoked|not-found|conflict', so it is already the parent of all three states."}
{"id": "D-H-06", "verdict": "REFUTED", "method": "raw reaction JSON read on 1736:8396 and 1736:8404 (not the node name)", "note": "Both back hotspots have destinationId: NULL - {\"type\":\"NODE\",\"destinationId\":null,\"navigation\":\"NAVIGATE\"}. They do not navigate to the echo stub 1339:7162 or anywhere else; they are dead hotspots. The finding appears to have matched the hotspot's NAME ('hotspot/back · Client · A0 identify') to the echo stub's name rather than reading the destination - and its own premise says Figma rejects cross-page NAVIGATE, which is why the field is null. The residual true parts stand: 1736:8405 has no back hotspot, and the frame-level ON_CLICK -> 121:2 is the only real path out of all three. The defect is real but it is 'the back control is unwired', not 'it points at the wrong page'."}
{"id": "D-H-08", "verdict": "CONFIRMED", "method": "full reaction sweep of 125:2 and 125:49", "note": "125:2 carries two identical @FRAME ON_CLICK -> 125:49 rows and 125:49 two identical @FRAME -> 23:21 rows. 125:2 additionally carries 1665:30 'hotspot/alt · S5.5 · brand-colour-fails-contrast' -> 125:49, making the frame-level pair redundant twice over."}
{"id": "D-H-15", "verdict": "CONFIRMED", "method": "figma footer + composer child geometry on 23:21/114:2/117:2/117:58/112:2", "note": "The sticky footer is 1280x72 on all four and holds 'Request changes' INSTANCE 148x32 next to Approve 125x32; 114:2's comment composer is 220x132 over a 196x44 input with pin/1 24x24; 112:2's identify fields are Input 340x36. Every number filed reproduces."}
{"id": "D-H-20", "verdict": "CONFIRMED", "method": "figma fill probe on 125:2/125:49 + independent recomputation of both relative luminances and contrast ratios + grep of the review path for any accent/contrast code", "note": "125:49 puts #f59e0b on both the agency name and the brand rule while the Approve INSTANCE reverts to #1c64f2; 125:2 uses #0f766e for all three. My own luminance maths returns 0.4389 for #f59e0b (2.15:1 on white) and 0.1426 for #0f766e (5.45:1) - the filed 0.439/2.15 and 0.142/5.47 are right. The accent is hardcoded in review-client.tsx and no contrast function exists in the dashboard, server, lib or shared packages."}
{"id": "D-H-34", "verdict": "CONFIRMED", "method": "reaction sweep of the four cited terminal boards", "note": "122:3 (444:4041), 120:2 (1665:34), 120:50 (444:4039) and 125:49 (1665:32) each carry a 'hotspot/back · S5.5 · landing-viewing' -> 23:21, and 122:3's own name declares it TERMINAL. The node ids filed are exact."}
{"id": "D-I-04", "verdict": "CONFIRMED", "method": "figma fill probe on 9:6/9:8/9:18/9:20 and 981:664/666/670/672 + read tokens.generated.css:81-82", "note": "Boards rest on #1c64f2 and hover to #1a56db on BOTH halves; the tokens are --bk-accent #1A56DB / --bk-accent-hover #1E429F. The boards' hover value is the token's rest value - the ramp is shifted one Flowbite step, exactly as filed."}
{"id": "D-I-05", "verdict": "CONFIRMED", "method": "figma variantGroupProperties read on 9:102 and 981:700 + read Button.tsx:5-10,:24 and buttonTheme.ts", "note": "9:102 Kind = [primary, secondary, ghost, destructive] and 981:700 Kind = [Primary, Ghost, Danger] - exact. Code's ButtonVariant is the five-role set with a real `link` recipe in buttonTheme.ts, so the role ships and neither set draws it."}
{"id": "D-I-06", "verdict": "CONFIRMED", "method": "full-depth COMPONENT walk of 1:2 filtered on /^icon\\s*\\/\\s*/ + two normalisations (raw name, and size-suffix-stripped)", "note": "67 icon COMPONENTs, 38 spaced 'Icon / name' and 29 tight 'icon/name' - exact. Size-suffix-normalised, exactly 11 glyphs are drawn 2+ times and the eight size-variant pairs are precisely the ones filed. Three are identical-size 24px duplicates: chevron-left 91:30/681:4334, ellipsis 91:49/681:4343, bell 91:67/681:4338. NOTE: a raw-name dedupe finds only 4 - the 11 requires stripping the -16/-18/-32 suffix."}
{"id": "D-I-07", "verdict": "CONFIRMED", "method": "figma stroke + effect probe on 14:10/14:18/14:29/697:434/10:8/12:24", "note": "Geometry A (DROP_SHADOW #1a56db sp3) on Input/Checkbox/Icon button; Geometry B (2px OUTSIDE stroke #1c64f2) on Toggle/Radio/Select; 697:434 carries BOTH a #1a56db w2 OUTSIDE stroke AND the sp3 halo. Three accent blues in focus treatment alone: #1a56db, #1c64f2, and #3f83f8 on Input 10:8's focus stroke."}
{"id": "D-I-08", "verdict": "CONFIRMED", "method": "repo-wide grep for each symbol across packages/editor and packages/dashboard, excluding its own file, chrome-ui/index.ts and __tests__", "note": "TreeRow 0 hits; MediaCard's only hit is a prose comment at UpgradeModal.tsx:114; NavItem's are comments at Row.tsx:25,40 plus an unrelated `type NavItem` at dashboard shell/nav.ts:17; SiteCard 0 in the editor. All four are exported from the barrel and drawn in Figma."}
{"id": "D-I-09", "verdict": "CONFIRMED", "method": "figma node lookup on 17:9 and 997:705 + grep of both SiteCard implementations", "note": "Both nodes exist (17:9 'Card / site' inside 17:41, 997:705 'Dashboard / Site card'). Dashboard's site-card.tsx:32 is consumed at recent-sites.tsx:6,26; the editor's chrome-ui/SiteCard.tsx has zero production consumers."}
{"id": "D-I-12", "verdict": "CONFIRMED", "method": "figma variantGroupProperties read across both halves", "note": "Editor: Button Kind/State all lowercase with State=[rest,hover,focus,disabled,loading]; Input State=[rest,focus,filled,error,disabled]; Icon button State=[rest,hover,focus,disabled]. Dashboard: Button Kind=[Primary,Ghost,Danger] State=[Default,Hover,Disabled,Focus]; Pill Tone=[Neutral,Success,Warning,Error,Accent]; Section tab State=[Default,Active]. Every value reproduces."}
{"id": "D-I-13", "verdict": "CONFIRMED", "method": "figma height measurement on 9:6/9:18 and 981:670/981:664", "note": "Editor Button sm=28 md=32; Dashboard Button sm=32 md=40. Dashboard sm equals editor md, and 'md' means 32 or 40 depending on the half - exact. 40 is flowbite's stock h-10."}
{"id": "D-I-14", "verdict": "CONFIRMED", "method": "ls packages/dashboard/components/auth + figma name sweep of page 1:2", "note": "Exactly the seventeen .tsx files listed (plus a __tests__ dir). Page 1:2 carries no OTP input, password-strength meter, resend timer, social button or Turnstile widget node."}
{"id": "D-I-15", "verdict": "CONFIRMED", "method": "ls packages/dashboard/components/states + ls chrome-ui + grep for denied|ErrorState in chrome-ui", "note": "Dashboard ships state-empty/error/denied/loading/action (5); the editor's chrome-ui has EmptyState.tsx and Skeleton.tsx only, and the denied|ErrorState grep across chrome-ui returns nothing."}
{"id": "D-I-16", "verdict": "CONFIRMED", "method": "figma fill + stroke probe on all five Badge variants and all five Dashboard / Pill variants", "note": "Every hex reproduces exactly, Badge carries a stroke on all five and Pill on none, neutral (#E5E7EB vs #F3F4F6) and warning (#FDF6B2 vs #FDFDEA) differ while success and danger/Error agree, and the property keys are Kind vs Tone."}
{"id": "D-I-17", "verdict": "CONFIRMED", "method": "figma height measurement on all five Badge variants", "note": "neutral 20, success 20, warning 22, danger 20, pro 20 - the warning variant is the 2px outlier, exactly as filed."}
{"id": "D-I-21", "verdict": "CONFIRMED", "method": "figma child read of 20:84 + parent lookup + read tabsConfig.ts:338,:349", "note": "20:84 is a FRAME 'row' inside 20:56 '320 — Brand' whose children are TEXT 'Components', a chevron frame, TEXT '27' and TEXT '›'. tabsConfig.ts:338 carries the 'Open Components panel … folds into Brand · components' comment verbatim and RAIL_FIGMA is at :349."}
{"id": "D-I-22", "verdict": "CONFIRMED", "method": "figma effect probe on 9:6/9:8 vs 981:664/981:666 + read tokens.generated.css:200", "note": "9:6 and 9:8 each carry DROP_SHADOW #000000 sp0 r2 off0,1 PLUS DROP_SHADOW #000000 sp-1 r3 off0,1; the Dashboard twins have no effects at all. --bk-shadow-raised is a single 0 1px 2px rgba(0,0,0,0.08)."}
{"id": "D-I-23", "verdict": "CONFIRMED", "method": "figma text read of 1034:751 + grep for --bk-focus-ring-width/-offset across packages/editor/src excluding tokens.generated", "note": "The tokens exist at tokens.generated.css:156-157 and have exactly ZERO consumers repo-wide. The rule frame cites a token nothing reads."}
{"id": "D-I-24", "verdict": "CONFIRMED", "method": "figma full-depth name sweep of page 1:2 for each symbol + ls/grep of chrome-ui/index.ts exports", "note": "All eighteen symbols are exported from index.ts and exist as files; none has a component node on 1:2. The only near-hits are decoys: 'Footer' matches 'Footer action (optional)' and three Modal sub-frames, 'kbd' matches a sub-frame inside a Search ⌘K showcase. Drawer 19:46 carries only Layout=[list,grid,table], as filed. The finding's own scope note (page 1:2 only) is honest and I did not widen it."}
{"id": "D-I-11-supplement", "verdict": "CONFIRMED", "method": "figma descendant COMPONENT count inside 91:6 + size read on the two orphans", "note": "Supplement to D-I-11: 91:6 'Icons — 30 Lucide' holds exactly 30 descendant COMPONENTs, and 1695:755/1695:760 are both 24x24 and parented to the page, outside it."}
{"id": "D-G-05", "verdict": "CONFIRMED", "method": "figma text-array equality test 130:798 vs 131:2 + string-set diff 131:415 vs 130:201 + read publish-approval.ts PublishApprovalBlock", "note": "130:798 and 131:2 are 84 TEXT nodes each and byte-identical in sequence; 131:2's name concedes it. PublishApprovalBlock is exactly the four reasons filed. COUNT CORRECTION on the third clause: 131:415 carries 82 TEXT nodes, not 'the same 88 as 130:201' - it is a strict subset, missing '‹ Exit', 'Saved 2m ago', 'In review · 3 open' and 'Publish'. It does still carry 'Re-send'."}
{"id": "D-G-06", "verdict": "CONFIRMED", "method": "page-wide in-degree census over every frame on 1:3 with instances warmed", "note": "52:2 = 84 in-edges and 130:201 = 59, both exact, and 84/59 is the 1.4x factor filed. CORRECTION: 52:2 is not 'the file's highest' - 927:4474 takes 85. Everything else in the evidence reproduces."}
{"id": "D-G-07", "verdict": "CONFIRMED", "method": "S-number extraction from all 107 Journeys children + page-wide S7 frame-name sweep with parent lookup", "note": "Journeys contains ZERO S7 frames - its S-set is S1.x, S2.0, S3.x, S5.x, S6.x. Every S7-prefixed frame I can match sits in SECTION 1776:8387 (Settings/S7) or 957:4474 (Archive). My regex counts 44 where the finding says 45; the load-bearing claim is exact."}
{"id": "D-G-08", "verdict": "CONFIRMED", "method": "edge filter on 55:2 and 58:2 over the warmed Journeys table + parent lookup on 876:4532", "note": "Each has exactly two out-edges: @FRAME -> 876:4532 and the boilerplate btn/ai -> 170:17. 876:4532 is 'Reference · index' in SECTION 862:6859. Both board names are the QA-fixture names filed."}
{"id": "D-G-13", "verdict": "CONFIRMED", "method": "read SiteMenu.tsx:225-226, overview-tab.tsx:306, ls activity-log.service.ts", "note": "SiteMenu's 'Activity log' row calls openDashboard(`/dashboard/sites/${siteId}#activity-log`) and the dashboard renders <SectionCard id=\"activity-log\" title=\"Recent Activity\">. activity-log.service.ts exists. The editor has no in-chrome activity panel; the board draws one at 360px in a 280px drawer."}
{"id": "D-G-14", "verdict": "CONFIRMED", "method": "page-wide in-degree census on 1:3", "note": "302:1978 has exactly 13 in-edges, the number filed. SCOPE: I verified the COUNT page-wide, not each of the thirteen sources individually; the finding's source list is consistent with a Pages-module origin but I did not re-derive it row by row."}
{"id": "D-G-15", "verdict": "CONFIRMED", "method": "figma text-array equality test on 130:798 vs 131:2", "note": "Zero differences across 84 TEXT nodes, and 131:2's name is verbatim as quoted."}
{"id": "D-G-16", "verdict": "CONFIRMED", "method": "read SiteMenu.tsx:134-140 and useEditorShortcuts.ts:164-166 + figma text dump of 815:4518", "note": "The SiteMenu comment - including the measured-in-the-editor quotes about '?' drawing SELECTION and ⌘/ drawing PANELS - is verbatim, and Cmd/Ctrl+/ -> modals.setShowShortcuts(true) is at :164-166. PATH NOTE: the hook is at editor/shell/hooks/useEditorShortcuts.ts; the finding gives no directory, so this is not an error, just worth knowing."}
{"id": "D-G-20", "verdict": "CONFIRMED", "method": "figma text scan + btn/publish fill probe across six editor boards + read ReviewBar.tsx:115 and PublishTab.tsx", "note": "52:2 carries 'In review · 3 open' and NONE of the four review-bar strings. Its btn/publish is #e5e7eb with a #9ca3af label while 130:2, 130:201, 130:798, 130:997 and 131:415 are all #1c64f2 on #ffffff. 130:2 carries none of the six toolbar strings checked. The PublishTab comment 'the panel's CTA opens the gate rather than firing the deploy' is verbatim. COUNT SLIP: 52:2 has 100 TEXT nodes, not 97."}
{"id": "D-G-21", "verdict": "CONFIRMED", "method": "figma text dump of 807:8756 + read DomainsScreen.tsx:1-11 + case-insensitive step|wizard grep of that file", "note": "The board's 'Step 2 of 3 · Verify DNS' and the DNS table reproduce exactly. DomainsScreen.tsx's docblock names the six states verbatim and the file contains ZERO occurrences of step or wizard. No ssl-provisioning, step-1 or step-3 board exists in the S6 set."}
{"id": "D-G-25", "verdict": "CONFIRMED", "method": "edge filter on 295:1972 + parent lookup on 807:7252, 295:1989, 295:1994 + page-wide in-degree", "note": "295:1972 has exactly one out-edge, carrier 'Frame' -> 807:7252 'S1.3b · new-page · template-picker'; 295:1989 and 295:1994 are both self-labelled RETIRED 2026-09-02 with 0 in-edges page-wide. NOW STALE, not wrong: 807:7252's parent today is SECTION 1084:4527 '11 · Templates · 18', not a REVIEW scratch section - it was re-filed in the reorganisation. The edge still leaves S1's own section."}
{"id": "D-G-26", "verdict": "CONFIRMED", "method": "edge filter over the warmed Journeys table + parent lookup on 817:4856", "note": "52:2's chip/M -> 807:8663, 807:8069 -> 807:8663, and 807:8663's ONLY edge is @FRAME -> 817:4856 '[not-implemented] Preview · interaction test' in SECTION 1779:4. Exact."}
{"id": "D-G-29", "verdict": "CONFIRMED", "method": "figma child + fill + opacity probe on 307:2193, 307:2203, 307:2213 and 131:201", "note": "All three gate boards carry a RECTANGLE named 'scrim', 1440x900 at 0,0, fill #111827, node opacity 1, fill opacity 1, visible, over a #f3f4f6 frame whose only other content child is a 520x151 card (6.06% of the board). NUANCE on the last clause: 131:201 is NOT the same construction - it draws a full Topbar / Middle band / Footer beneath. The scrim sits above them in z-order at full opacity, so the RENDER matches; the construction does not."}
{"id": "D-G-31", "verdict": "CONFIRMED", "method": "read OnboardingChecklist.tsx:15,:179,:215 + figma text-node count on the three S1.4 boards", "note": "The docblock line 'Each step: click to expand → description + optional CTA button', the aria-expanded at :179 and the step.description render at :215 are all present. The three boards carry only bare checkbox rows - no description text and no CTA button node."}
{"id": "D-G-32", "verdict": "CONFIRMED", "method": "figma frame-relative TEXT coordinate probe on 130:201", "note": "'Bella Cucina' (the canvas site header) at (428,209) and 'Section · Hero' (the selection chip) at (414,229) - the chip starts 14px left of and 20px below the header text, exactly as filed. Same offsets on 130:400 at its 33px-shifted y."}
{"id": "D-G-38", "verdict": "CONFIRMED", "method": "figma per-child geometry + status-bar coordinate read on 130:201 vs 130:400", "note": "130:201: Middle band/Canvas/Rail/Drawer/Inspector all 702 tall and the status row at y=810 in a 900-tall frame, leaving 90px of frame fill. 130:400: all 768, status row at y=876, 24px left. Every number reproduces."}
{"id": "D-H-01", "verdict": "CONFIRMED", "method": "read review.service.ts:62-68, client-review.service.ts:212,:222 + review-client.tsx:182 + figma text check on 113:3", "note": "submitReview creates a NEW ReviewRequest row whose data carries no reviewerId; reviewerId is written only inside the current round (:212, and the resolve path); review-client.tsx:182 is exactly `const signedIn = Boolean(data.reviewer)`, so round 2 renders the identify form. 113:3 does carry 'Welcome back, Sara.' and 23:21 does not."}
{"id": "D-H-02", "verdict": "CONFIRMED", "method": "figma text-array comparison across 23:21/113:3/125:2/125:49 + fill probe", "note": "All four are full 1280x900 copies of the landing screen; the only copy difference is 113:3's 'Welcome back, Sara.', and 125:2/125:49 differ from 23:21 only in the three fills (agency name, brand rule, Approve button) measured under D-H-20."}
{"id": "D-H-04", "verdict": "CONFIRMED", "method": "figma text dump of 121:2 + grep of the whole app/review/[token] directory for mailto:|href=|localStorage + read client-review.service.ts:33 and review-client.tsx:169", "note": "121:2's card carries exactly the three strings filed and no CTA. Every file under app/review/[token] returns ZERO hits for mailto:, href= and localStorage. TOKEN_TTL_DAYS = 90 at :33. The dead-link branch renders <Shell agency=\"Buildrick\">, so the live screen reads 'Buildrick is asking for your feedback'."}
{"id": "D-H-05", "verdict": "CONFIRMED", "method": "figma reaction + child-id read on 869:6999", "note": "Its three hotspots are exactly 1736:8412 / 1736:8416 / 1736:8420, named 'hotspot/state · dead-link · revoked|not-found|conflict', pointing at 1736:8389 / 8397 / 8405 - the client-review dead-link boards. The transfer flow runs through app/transfer/accept and its own token; client-review.service.ts is not in that path."}
{"id": "D-H-12", "verdict": "CONFIRMED", "method": "figma parent + stroke read on 117:58's 'View site' TEXT + full reaction sweep of 117:58 + read review-client.tsx:250-275", "note": "'View site' sits inside a bordered 110x36 frame directly under 'Ali can now publish it live.'; 117:58's ONLY reaction is 117:111 -> 120:2, so in the prototype the button returns the client to the same frozen snapshot. The code's terminal branch renders heading, paragraph and the Signed-as line with zero controls."}
{"id": "D-H-18", "verdict": "CONFIRMED", "method": "read client-review.service.ts:194-201 and routers/client-review.ts:81,:95", "note": "identifyReviewer throws EMAIL_MISMATCH with the exact string 'That is not the address this link was sent to.' on a strict `normalised !== review.invitedEmail` comparison - no plus-addressing tolerance. identify runs on strictClientLimit = createRateLimitedProcedure(5, 15*60*1000), i.e. 5 per 15 minutes."}
{"id": "D-H-19", "verdict": "CONFIRMED", "method": "figma frame-relative child coordinates on 854:41 and 869:6999", "note": "854:41's 'Button · Sign in' 132x36 sits at (32,122) while its card 'Frame' 520x134 sits at (460,240) - the button is outside the card, top-left. 869:6999's TEXT 'Ask Vortex Studio to send a new invitation.' sits at (32,124), likewise outside the card. The filed coordinates were screenshot estimates (~24,92 / ~24,94); the exact values are (32,122) and (32,124). The defect is as described."}
{"id": "D-H-23", "verdict": "CONFIRMED", "method": "figma text dump of 854:30 + wc -l on the three share-route files", "note": "854:30 carries exactly two strings - 'Bella Cucina — draft preview' and 'Shared by Vortex Studio · read-only · expires in 14 days' - with no header, no agency identity and no navigation, against 23:21 which draws the whole site. app/share/[token]/page.tsx is 57 lines, password-gate.tsx 95, not-published.tsx 40 - exact."}
{"id": "D-H-27", "verdict": "CONFIRMED", "method": "wc -l on password-gate.tsx, not-published.tsx and the verify-password route + figma frame inventory of page 1:6", "note": "95, 40 and 93 lines respectively - all three exact. Page 1:6's only share frames are 854:30 (draft preview) and 869:7005 (expired link); neither draws a password or unpublished state."}
{"id": "D-H-28", "verdict": "CONFIRMED", "method": "figma text dump of 854:35 + case-insensitive grep for decline|reject|No thanks across app/transfer/accept/page.tsx and workspace-transfer.service.ts", "note": "854:35 offers one button and one reaction (854:39 -> 854:41). Both code files return ZERO hits for any decline path."}
{"id": "D-H-11", "verdict": "CONFIRMED", "method": "figma text dumps of 117:58 and 122:3 + read review-client.tsx:262-271", "note": "117:58's last string is 'Signed as Sara Khan · sara@bellacucina.com'; 122:3's is '…They'll send a new link when the changes are ready.' with no Signed-as node. In code the Signed-as paragraph sits OUTSIDE the approved/changes ternary, so it renders on both branches. This is the surviving half of D-D-06, exactly as the finding claims."}
{"id": "D-H-13", "verdict": "CONFIRMED", "method": "figma text dump of 120:50 + read client-review.service.ts editedSinceApproval + schemas/reviews.ts changeSummary", "note": "The board says '⚠ Ali has changed 2 things since you approved.' over the same 880x560 snapshot. editedSinceApproval is a pure boolean (status APPROVED && lastEditedAt > resolvedAt) - no count, no diff - and changeSummary is designer-typed z.string().max(500) that appears on no board."}
{"id": "D-H-14", "verdict": "CONFIRMED", "method": "figma text dump of 117:2 + read review-client.tsx:376-398", "note": "The board is exactly 'What should Ali fix?' / note / '3 pinned comments included' / 'Cancel' / 'Send to Ali' and never says the action is irreversible. All three unboarded variants are in the code verbatim: the approve confirm ('This tells your designer the design is settled…'), the no-note variant ('Your designer will not be told what to change…') and the in-dialog failure line ('Your note could not be sent, so nothing was submitted')."}
{"id": "D-H-21", "verdict": "CONFIRMED", "method": "figma parent + fill + stroke read on 121:25's 'Try again' TEXT", "note": "'Try again' is a bare TEXT at #1a56db whose parent is the card itself - no button frame, no fill, no border - against filled/bordered INSTANCE buttons everywhere else in the family. GEOMETRY SLIP: the card measures 460x148, not the 366x120 filed."}
{"id": "D-H-22", "verdict": "CONFIRMED", "method": "figma child geometry + reaction read on 121:48 and its destination", "note": "121:48 stacks three full-width 880-wide skeleton rows inside an 880x368 well over a 1280x844 body, and its AFTER_TIMEOUT reaction resolves to 23:21 - a 1280x56 branded bar, an 880x560 site card and a 72px sticky footer. SIZE SLIP: the rows are 880x44 / 880x180 / 880x120, not the 880x36 / 880x140 / 880x96 filed; the shape claim is right."}
{"id": "D-H-33", "verdict": "CONFIRMED", "method": "figma cookie-copy scan of all 24 frames on 1:6 + read global-providers.tsx:11, cookie-consent.tsx:142, review-client.tsx:106 + ls app/review/layout.tsx", "note": "0 of 24 frames carry cookie copy. CookieConsent is mounted globally at global-providers.tsx:11, there is no app/review/layout.tsx to opt out, and the banner is fixed at z-[9998] (cookie-consent.tsx:142) while the review footer already compensates with bottom: var(--cookie-inset, 0px) at :106. The offset footer state is genuinely unboarded."}
{"id": "D-G-35", "verdict": "CONFIRMED", "method": "read useAiActionGate.ts:9-13,:44,:69 and action-confirmation.service.ts:1-11,:11 + page-wide frame-name sweep of 1:3 for propose|privileged|action-gate|agent-approv|confirm-gate", "note": "The gate ships exactly as described: propose -> consequence dialog -> confirm, single-use grants with TTL_MS = 5 minutes and an actor-match check on confirm. The name sweep returns ZERO frames, and 170:97 is 'AI · step-gate' inside the AI section - the per-step apply gate, a different object, as the finding itself says."}
{"id": "D-G-36", "verdict": "CONFIRMED", "method": "read ConflictModal.tsx:1-18,:36 + page-wide frame-name sweep for /conflict/i with section membership", "note": "The three branches (Reload latest / Save a backup / Overwrite) and the confirmOverwrite gate are verbatim. The only conflict boards are 66:640 'Shell state 11 · Saving → conflict' (Shell - the banner, as filed) and 807:6965 'S1.2d - Conflict - Review both', which sits in SECTION 862:6859 (Reference) and is about Compare, not the decision. The five S1 boards named all exist with those names. Journeys has no conflict-decision board."}
{"id": "D-G-37", "verdict": "CONFIRMED", "method": "figma node lookup on all thirteen cited caption ids + Brand-section name sweep for /token/i", "note": "Every caption exists with the exact name filed, and the two series are genuinely distinct namespaces. The closing claim checks out: the OTHER S4.1 ('S4.1 · Tokens — add') does have real Brand boards - 152:52 'Brand · tokens' and 306:2049 'Brand · tokens · add'. TWO NOTES: the evidence lists thirteen ids, not the 'twelve' the last line claims; and the cited y-bands no longer hold (Series A now sits at y≈102917, Series B at y≈99861-100317) because the page was re-gridded after this was written - the structure is unchanged."}
