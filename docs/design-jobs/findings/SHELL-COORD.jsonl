{"id": "SH-CO-01", "area": "lifecycle", "node": "65:2", "kind": "board-code-mismatch", "severity": "Major", "finding": "SH-A-07 and SH-C-02 both report the ENTRY POINT board drawing no drawer and Active=None while the code boots the Insert drawer open. Adjudicated: the BOARD is the considered artifact here and the CODE is the gap — this is a first-run experience that was designed and never built, not a stale board.", "evidence": "Board 65:2's own caption 204:2 reads: 'Rail icons only, no drawer, canvas full-bleed. A one-time coach sits over the six rail icons and the empty canvas carries its own CTA — the only time the shell teaches itself.' That is a deliberate, reasoned first-run treatment. The code has no coach-mark layer and no first-run branch: useStudioState.ts:225 defaults leftPanelTab to 'add' and :235 defaults isLeftPanelOpen to true ('Always open on session start'), unconditionally, so every user's first session opens mid-tool.", "fix": "Do NOT change the board to match the code — that would delete a designed onboarding moment on the grounds that it was never implemented. Either build the first-run branch (a one-time flag gating isLeftPanelOpen and mounting the coach), or retire the treatment deliberately and record why. Both are founder calls; the defect is that neither has happened and the two artifacts have silently disagreed.", "buildFrom": "—"}
{"id": "SH-CO-02", "area": "topbar", "node": "1776:8385", "kind": "dead-control", "severity": "Major", "finding": "OPEN QUESTION, deliberately not executed: 54 topbar instances drawing Publish=disabled carry an ON_CLICK to 307:2193 'S5.4 · gate · pending'. The code gives the disabled branch onClick={() => {}} (Topbar.tsx:311-320), so in the product that button does nothing at all.", "evidence": "Census from SH-B-01, corroborated by SH-A-01: ready 23/0 wired, disabled 80/55 wired (54 -> 307:2193, 1 -> 833:4518), anyway 2/0. I wired the 23 live ones this pass and left these 54 untouched.", "fix": "Two defensible readings and they need a decision, not a guess. (a) The edges are a prototype affordance meaning 'click to see WHY publishing is blocked', which is useful to a reviewer walking the file and harmless because the destination is the reason. (b) They are simply wrong, because a disabled control that navigates teaches a designer the opposite of what ships. If (b), remove all 54. Note the product does surface the reason — as a TOOLTIP on the disabled button, not by navigating — so a faithful board would draw the tooltip rather than carry an edge.", "buildFrom": "—"}
{"id": "SH-CO-03", "area": "rail", "node": "2034:8519", "kind": "variant-gap", "severity": "Major", "finding": "The Rail component set has 7 variants and the file exercises 2. Active=Insert, Pages, Media, Content and Brand have ZERO instances across all 102 rail instances on page 1:3 — five of the six tools have never been drawn in their own active state.", "evidence": "SH-C-01, resolved via getMainComponentAsync against set 2034:8519: Active=Layers 45, Active=None 57, all five others 0. The variants were built (they exist and are correct); nothing has ever pointed at them.", "fix": "CORRECTED — my first recommendation ('set the variant on boards that already draw a non-Layers drawer') was wrong and I proved it by trying it. A scan of every board in the six rail-tool sections set ZERO rails, because those sections hold 280-wide DRAWER boards with no rail instance at all. Of the 57 Active=None instances, only 7 show a drawer and 5 of those are SUPERSEDED. Exactly one was fixable: 807:6558 'Start blank (Insert drawer opens)' is now Active=Insert, taking the census to Layers 45 / None 56 / Insert 1. The remaining four variants (Pages, Media, Content, Brand) are unused because no full-shell board draws those drawers — that is a missing-board gap, not a wrong-variant gap, and it cannot be closed by editing what exists.", "buildFrom": "—", "status": "PARTIALLY FIXED 2026-09-06 — 1 of 5 variants exercised; the other 4 need boards that do not exist"}
{"id": "SH-CO-04", "area": "lifecycle", "node": "1172:4891", "kind": "board-code-mismatch", "severity": "Major", "finding": "REFUTES SH-D-11's second limb. The edge from the exit destination 'back into the guard the user just confirmed leaving' is carried by a node named `hotspot/state · Exit · guard` — the file's reviewer-chip convention, not a flow edge.", "evidence": "927:4474's only two edges are 927:4484 '← Back to Site settings' -> 638:2378 and 1172:4891 'hotspot/state · Exit · guard' -> 1172:4804. The convention was established by measurement earlier in this session: 386 `hotspot/state` nodes are reviewer chips parked on hub boards, against 36 `hotspot/alt` which are real alternate-outcome edges on a producer. A state chip is how a reviewer jumps to a state; it is not a step a user takes.", "fix": "No change. SH-D-11's first limb stands and is unresolved: 927:4474 is a Settings->workspace hand-off (80 of its 87 inbound edges come from Settings, 3 from Shell) now serving as the destination for 104 topbar exits. That needs a board saying what leaving the editor actually does — the code goes to ${DASHBOARD_URL}/dashboard/projects — and no such board exists on page 1:3. A cross-page destination is not an option: Figma rejected one outright when I probed it (\"destination ... was rejected\"). So this one needs a board drawn, and its copy is the founder's to write.", "buildFrom": "927:4474", "status": "RESOLVED 2026-09-06. The destination existed but was unreachable: 398:14 'Dashboard · Sites' carries caption 398:4263 'Dashboard · Sites — /dashboard/projects', exactly where StudioHeader.tsx:481-483 sends the user — but it lives on page 397:2 and Figma rejects a cross-page prototype destination (probed twice, most recently from a plain scratch frame; both 997:1923 and 398:14 came back 'was rejected'). Page 1:3 holds no dashboard board except superseded archives. So a board was built by cloning 927:4474 rather than authored: heading unchanged (already correct), body given the source's own sentence shape with the subject the code and the file agree on, the Settings-specific third line HIDDEN rather than rewritten, and a back link mirroring the source's. New board 2272:11904 'Exit · Editor (leaves to your sites)', 4 guard Leave edges re-pointed to it, back link -> 199:2. 927:4474 keeps its 84 Settings edges and goes on serving Settings."}
{"id": "SH-CO-05", "area": "lifecycle", "node": "2272:11904", "kind": "board-code-mismatch", "severity": "Minor", "finding": "Self-caught while building the exit board: the cloned back link was relabelled '← Back to the editor' while its inherited edge still pointed at S7 · Settings · General — a control saying one thing and doing another, which is the exact defect class this audit catalogued.", "evidence": "The re-point loop scanned the board's DIRECT children; the reaction sits on 2272:11914, the label's parent, so it was missed. Caught by reading the new board's edges back rather than trusting the script's own success line.", "fix": "Fixed — 2272:11914 now points at 199:2. Recorded because the lesson generalises: a clone inherits every edge, and a rename does not follow one. After cloning-and-relabelling, re-read the clone's edges rather than the edges you meant to write.", "buildFrom": "—"}
