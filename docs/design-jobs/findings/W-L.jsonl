{"id": "W-L-01", "module": "Shell", "node": "1172:4804", "screen": "the ‹ Exit control on every Shell board → Exit · guard", "q": "Q1", "kind": "flow-gap", "severity": "Critical", "finding": "The job \"leave the editor without losing my work\" has no first step: the topbar ‹ Exit carries no reaction on 16 of the 17 topbar-bearing Shell boards, so the guard that protects the exit is reachable only from 927:4474 — the screen you land on AFTER leaving. The flow is drawn backwards.", "evidence": "Reaction walk over every descendant of all 20 frames in 1776:8385: btn/exit has a reaction of its own on exactly one board (963:4474 → 65:412 'Loading', itself a wrong destination); on the other 16 it is rx=0 and only an ancestor — the board's own frame-level catch-all — is wired (e.g. 199:2's frame → 199:205, so ‹ Exit lands on 'Element selected'). Root cause is one node: Topbar master 681:27 'Publish=disabled, Review=open' carries rx0 on btn/exit, btn/publish, btn/notifications, icon/bell, btn/more and pill/saved, so no instance can inherit an edge. In-edge scan of all 27 sections on 1:3: 1172:4804 in-degree = 1, from 927:4474 'hotspot/state · Exit · guard'. Code: StudioHeader.tsx:481-483 exitToDashboard → guardNavigation (:393-419) → the three-kind dialog at :944-1010.", "fix": "Put the reaction on the Topbar master 681:27 (btn/exit → 1172:4804) so all 99 topbar-bearing boards inherit one edge, and delete 963:4474's btn/exit → 65:412. If the master must stay inert, overlay a hotspot/state · Exit · guard on each Shell board the way 963:4474 already does for nine other controls.", "buildFrom": "—"}
{"id": "W-L-02", "module": "Shell", "node": "66:640", "screen": "Shell state 11 · Saving → conflict", "q": "Q1", "kind": "flow-gap", "severity": "Critical", "finding": "The conflict dialog — the one moment a colleague's work can be destroyed — has no wired outcome: all three actions resolve to the board's frame-level catch-all, so 'Keep mine', 'Save backup' and 'Review both' all land on the same screen and the design cannot say what any of them does.", "evidence": "Reaction dump of 66:640: the only self-carrying reactions are btn/ai (→170:17), the six rail items, two parked hotspot/alt rows (→807:6965, →813:4888), the frame itself (CLICK→199:2) and an AFTER_TIMEOUT→52:2. The three action labels are TEXT at board-relative y=498 ('Keep mine', 'Save backup', 'Review both') inside a group that reads 'Save backup=anc' — no reaction of its own. Code ships three distinct consequences: modals/ConflictModal.tsx:35 rendered at AquibraStudio.tsx:684-708 — Reload latest / Save a backup (downloads JSON, then reloads) / Overwrite… behind a deliberate two-step confirm.", "fix": "Wire each action separately: 'Reload latest' → 65:412 (Loading) then the reloaded shell, 'Save a backup' → a download-then-reload state, 'Overwrite…' → its second-step confirm. Until the confirm step is drawn the destructive door is one unlabelled click.", "buildFrom": "1172:4804"}
{"id": "W-L-03", "module": "Shell", "node": "2162:11838", "screen": "Shell state 15 · Save failed / Shell state 14 · Saving", "q": "Q1", "kind": "flow-gap", "severity": "Critical", "finding": "The two newest boards are correct in content and unreachable in the file: both have in-degree 0 across all 27 sections, and the retry loop they exist to document (Save failed → retry → Saving → Saved) has none of its three edges.", "evidence": "Page-wide in-edge scan of every reaction carrier under all 27 sections of 1:3: 154 edges land on this module's boards and not one lands on 2162:11660 or 2162:11838. Their own out-edges are the 9-row board template (frame catch-all → 199:205, zoom hotspot, back → 52:2, six rail items). Copy verified correct against code: topbar text reads 'Saving…' (2162:11660) and 'Save failed — retry' (2162:11838), matching SaveStatus.tsx COPY at :41-52; the grey/red dots match DOT_CLASS at :70-77. The pill is an actionable button in exactly these two states — SaveStatus.tsx:24-29 documents onRetry as 'the pill becomes the button for the two states a user can act on' — and pill/saved is rx=0 on both boards.", "fix": "Add three edges: a state hotspot on 199:2 (or on the Topbar master's pill) → 2162:11660; 2162:11660 → 199:2 for the success path and → 2162:11838 for the failure path; and pill/saved on 2162:11838 → 2162:11660 for the retry. Without the third the board draws a button whose whole purpose is retry and shows no retry.", "buildFrom": "—"}
{"id": "W-L-04", "module": "Notifications", "node": "165:2", "screen": "the bell → Notifications · unread", "q": "Q1", "kind": "flow-gap", "severity": "Critical", "finding": "Notifications cannot be opened from the Shell: the bell is drawn on all 17 topbar-bearing Shell boards and wired on none of them — the module's only two doors are a Reference artifact and a Journeys board.", "evidence": "Per-board control walk of 1776:8385: btn/notifications and icon/bell read rx=0 on every board (only 963:4474 carries a separate hand-placed 'hotspot/state · Notifications' → 165:2). Topbar master 681:27 is rx0 on both. Page-wide in-edge scan: 165:2 in-degree 7 = 963:4474 'Shell · 360 overlay panel (slot proof)', 52:2 'S1 · Editor — ASSEMBLED' (Journeys), and five within-module. 963:4474 is already recommended for the Reference section (D-C-36) — when it moves, Shell has zero routes into Notifications. Code: the bell is Topbar-owned and opens the panel at StudioHeader.tsx:765 (onOpenNotifications) with the panel mounted at :839-845.", "fix": "Add the edge on the Topbar master 681:27 (icon/bell → 165:2) so every board that draws the bell can open the panel.", "buildFrom": "—"}
{"id": "W-L-05", "module": "Notifications", "node": "165:2", "screen": "Notifications · unread", "q": "Q1", "kind": "flow-gap", "severity": "Major", "finding": "The panel's only route to the rest of the user's notifications — 'See all notifications' — is not drawn on any board, so the design shows a list of five with no way to reach the ones the badge is counting.", "evidence": "Full text dump of 165:2 and 165:24 returns only 'Notifications', '✕', the day bands and three rows plus their ages; there is no footer row on either, nor on 165:44 / 165:51 / 165:71 / 453:4051. Code renders it whenever state==='ready' && rows>0 — NotificationPanel.tsx:309-320 (label at :317), calling seeAll() at :162-168 → /dashboard/notifications — and its own comment at :293-297 records why: 'the panel is the five most recent while the bell counts every unread one — 25 against 5 on the site this was walked on … twenty of them were counted in the badge and reachable from nowhere in the editor.' The destination board exists: 1023:5568 'Notifications' on page 988:2.", "fix": "Draw the bordered footer row 'See all notifications' on 165:2 and 165:24 (only those two — the code hides it when the list is empty, loading or errored) and wire it to 1023:5568.", "buildFrom": "—"}
{"id": "W-L-06", "module": "Shell", "node": "199:409", "screen": "Shell state 5 · Drawer closed", "q": "Q1", "kind": "flow-gap", "severity": "Major", "finding": "Closing the drawer — the shell's most-used layout move — has no drawn control: the drawer panel's ✕ is rx=0 on every Shell board, and 'Drawer closed' is reachable only from the two hub boards' state switchers.", "evidence": "Reaction walk of 1776:8385: no carrier on any board targets 199:409; its in-degree is 2, both 'hotspot/state · Shell state 5' rows on 963:4474 and 52:2. Clicking the drawn ✕ on 199:2 fires the frame catch-all and lands on 199:205 instead. Code: the panel-header ✕ is the only close control — LeftSidebar.tsx:590-594 passes onClose: onDrawerToggle into every tab, LeftSidebar.tsx:664 records that the standalone ls-panel-close was removed in favour of it, and it drives isLeftPanelOpen (StudioPanels.tsx:418, useStudioState.ts:235). Re-clicking the active rail tool does NOT close it — activeTab is a non-nullable GroupedTabId (LeftSidebar.tsx:61).", "fix": "Wire the drawer PanelHeader ✕ → 199:409 on the shell boards (or on the Panel header component master, which is already shared), and add the return edge rail/Layers → 199:2.", "buildFrom": "—"}
{"id": "W-L-07", "module": "Notifications", "node": "165:2", "screen": "every notification row", "q": "Q1", "kind": "flow-gap", "severity": "Major", "finding": "No board shows where a notification goes. The module draws six states of the panel and boards the failure of the jump (165:71) without ever boarding the jump, so the one job a notification exists for cannot be walked.", "evidence": "All 16 out-edges of section 1779:2: 165:2→165:24/165:51/165:71/52:2, 165:24→165:44, 165:44→52:2/453:4051/165:2, 165:51→165:2/52:2, 165:71→165:2/52:2, 453:4051→165:2/165:44/52:2. Every cross-module destination is 52:2, the editor shell. Code: openRow marks the row read then calls onNavigate(n.actionUrl) — NotificationPanel.tsx:135-147 — which is navigateFromNotification, StudioHeader.tsx:495-503, resolving to ${DASHBOARD_URL}${actionUrl}. Six event types are actually emitted (SITE_PUBLISHED / SITE_PUBLISH_FAILED, FORM_SUBMISSION_RECEIVED, MEMBER_JOINED, SECURITY_PASSWORD_CHANGED, PAYMENT_FAILED), so the real destinations are dashboard boards that exist on 988:2.", "fix": "Wire at least the two rows the product can really emit: 'Publish failed — Osteria' → the publish-result screen, and a form-submission row → the submissions inbox. One worked jump makes the panel a door instead of an announcement.", "buildFrom": "—"}
{"id": "W-L-08", "module": "Notifications", "node": "165:24", "screen": "Notifications · all-read", "q": "Q1", "kind": "flow-gap", "severity": "Minor", "finding": "The all-read board is a one-way street: it has a single outbound edge, no ✕ wiring and no route back to the editor, so a walker who reaches it can only go deeper into the module.", "evidence": "Out-edges of 165:24: exactly one — the frame itself, CLICK → 165:44 'Notifications · empty'. Its four siblings each carry a parked 'hotspot/back · to editor' → 52:2 (nodes 1671:7265/7267/7269/7271 at board-relative y≈724); 165:24 has none. Its ✕ is a bare TEXT node (165:27) with no reaction. In-degree 1, from 165:2's frame catch-all — which also means the board asserts that clicking anywhere on the unread list marks everything read.", "fix": "Add the 'hotspot/back · to editor' → 52:2 row this board is missing, matching its four siblings.", "buildFrom": "165:44"}
{"id": "W-L-09", "module": "Shell", "node": "199:2", "screen": "the three declared drawer modes", "q": "Q1", "kind": "flow-gap", "severity": "Minor", "finding": "Asked directly: the transition between the three declared drawer modes is drawn nowhere, and it cannot be — the shipped shell has one drawer mode, so two of the three names have no code subject to transition from.", "evidence": "No reaction on any board in 1776:8385 moves between a pinned and a transient/overlay drawer; the only drawer-shaped edges are the six rail items (which open a tool, never change the mode) and the two hub boards' state switchers into 202:2. Code: the drawerPinned prop that produced the overlay mode was removed — LayoutShell.tsx:267-272 states it 'had no door for its whole life: the single production caller never passed the prop' — and grep for drawerPinned across src returns zero. 202:2 'pin auto-released (overlay)' is already annotated RETIRED 2026-09-04. Filed as the transition question the brief asked, not as the mode inconsistency, which is D-C-47's row.", "fix": "Nothing to wire. Record on 199:2's caption that 'Drawer (transient)' is a drawing convention with no shipped counterpart, so the next reader does not go looking for the toggle.", "buildFrom": "—"}
{"id": "W-L-10", "module": "Shell", "node": "1309:2", "screen": "Exit · guard — the stranded variant", "q": "Q2", "kind": "dead-control", "severity": "Critical", "finding": "DEAD — both. The stranded variant of the exit guard has neither outcome wired, while its two siblings on the same board have all five of theirs, so the one dialog that fires when work has not reached the server is the one with no door out.", "evidence": "Reaction dump of 1172:4804: dirty (1172:4805) — btn/Stay 1172:4808 → 199:2, btn/Leave anyway 1172:4810 → 927:4474, btn/Save & leave 1172:4812 → 927:4474; risky-offline (1172:4816) — btn/Stay 1172:4819 → 66:441, btn/Leave and lose changes 1172:4821 → 927:4474; stranded (1309:2) — btn/Leave anyway 1309:9 rx=0, btn/Stay 1309:7 rx=0. Screenshot confirms all three cards draw a full footer. Code: guardNavigation's third branch, StudioHeader.tsx:410-413, fires on totalPendingMirrors() > 0; Stay is setExitDialog(null) — i.e. back to the state you were in — and Leave anyway is leaveAnyway (:445-450) → bypassAndNavigate(nav) → the same dashboard exit as the other two.", "fix": "btn/Stay 1309:7 → 199:2 (stranded fires on a CLEAN project save state, so the shell behind it is the ordinary 'Saved' one, not 66:441), and btn/Leave anyway 1309:9 → 927:4474, matching its two siblings.", "buildFrom": "—"}
{"id": "W-L-11", "module": "Shell", "node": "1175:4825", "screen": "Upgrade modal (403 gate) · btn/Upgrade to Pro", "q": "Q2", "kind": "dead-control", "severity": "Major", "finding": "DEAD. The paywall's primary action has no reaction, and the screen it needs exists — the code opens the dashboard billing page.", "evidence": "1175:4825 rx=0 (its only sibling hotspot, 1175:4872, is a back row). Code: handleUpgrade at UpgradeModal.tsx:83-88 — window.open(`${DASHBOARD_URL}/dashboard/settings/billing`, '_blank') then handleClose(). Name census across every page of the file: 'Settings · Billing' 1008:3466 and 'Settings · Plans' 1010:4035 on page 988:2, plus 'Workspace · Billing' 398:3965 on 397:2. The file also has its own editor-boundary convention for exactly this — 927:4474 'Exit · Workspace (leaves the editor)', whose copy reads 'Members, billing and team permissions are workspace-level' and which already absorbs 85 inbound billing/members edges.", "fix": "Wire 1175:4825 → 927:4474 (consistent with the 85 existing billing edges) or cross-page to 1008:3466. Because the code opens billing in a NEW TAB and closes the modal, the board should also show the caller still standing behind it.", "buildFrom": "—"}
{"id": "W-L-12", "module": "Shell", "node": "1175:4823", "screen": "Upgrade modal (403 gate) · btn/Maybe Later + hotspot/back", "q": "Q2", "kind": "dead-control", "severity": "Major", "finding": "DEAD, and the dismiss path that does exist is stale: 'Maybe Later' has no reaction, and the board's only back hotspot still returns to a board that is no longer its caller.", "evidence": "1175:4823 rx=0. The board's single wired node is hotspot/back 1175:4872 → 154:132 'Brand · pro-locked'. But the modal's only inbound edge in the whole file is now 641:2487 'hotspot/state · Templates …' (page-wide in-edge scan: 1175:4804 in-degree 1), after the Brand edge was moved during the D-B-01/D-X-33 fix. Code agrees the caller is Templates: handleClose is the Maybe-Later handler (UpgradeModal.tsx:126-128) and openUpgrade has exactly two call sites, both premium-template clicks (TemplatesTab.tsx:177,188).", "fix": "Point both the dismiss button 1175:4823 and the back hotspot 1175:4872 at 641:2487's board (Templates · gallery), so 'Maybe Later' returns the user to where they were.", "buildFrom": "—"}
{"id": "W-L-13", "module": "Shell", "node": "681:27", "screen": "the Topbar component master — every Shell board", "q": "Q2", "kind": "dead-control", "severity": "Major", "finding": "DEAD, and missed by the census because it skips instance-nested nodes: every control in the shared topbar is inert on every board. btn/publish, btn/more (Site menu), pill/saved and icon/bell all resolve to whatever the board's frame catch-all points at.", "evidence": "Reactions read on Topbar master 681:27 'Publish=disabled, Review=open': btn/more=rx0, btn/publish=rx0, btn/notifications=rx0, icon/bell=rx0, pill/in=rx0, pill/saved=rx0, btn/exit=rx0. Instance-level walk over all 20 frames of 1776:8385 agrees (each reads 'anc', the ancestor being the board frame). 16 of the 17 topbar-bearing boards carry a frame-level catch-all, so each of these clicks lands somewhere unrelated — on 199:2 every one of them goes to 199:205. btn/publish carries a real reaction on only two boards, 202:2 (RETIRED) and 963:4474 (Reference). The doors behind them are all drawn: Site menu 642:3401 (~24 rows, SiteMenu.tsx), Publish pre-checks 833:4518, save-indicator 813:4836, Notifications 165:2.", "fix": "Wire the master once — btn/exit → 1172:4804, icon/bell → 165:2, btn/more → 642:3401, btn/publish → 833:4518, pill/saved → 2162:11660 — and 99 boards inherit a working topbar instead of 99 copies of the same edge.", "buildFrom": "—"}
{"id": "W-L-14", "module": "Notifications", "node": "786:4301", "screen": "Notifications · load-error · Try again", "q": "Q2", "kind": "dead-control", "severity": "Minor", "finding": "Missed by the census because it is a bare TEXT link, not a btn/ node: the error state's only recovery control has no reaction of its own, so it is indistinguishable from clicking the 600px of empty panel beneath it — and it skips the state the code guarantees.", "evidence": "453:4051's carriers are the frame (CLICK→165:2), hotspot/back · Notifications · unread 453:4058 → 165:2, hotspot/back · Notifications · empty 453:4062 → 165:44 and hotspot/back · to editor 1671:7271 → 52:2, all parked at board-relative y≥154. 'Try again' is TEXT 786:4301 at y≈122, covered by nothing but the frame. Code: the handler is load(), NotificationPanel.tsx:232-244 (the onClick at :243), and load() sets state to 'loading' before it fetches (:113-119) — so the next screen is 165:51, not the populated list.", "fix": "Put a hotspot on 786:4301 → 165:51 (loading), and let 165:51's existing edge carry on to 165:2.", "buildFrom": "—"}
{"id": "W-L-15", "module": "Notifications", "node": "165:5", "screen": "the ✕ on all six Notifications boards", "q": "Q2", "kind": "dead-control", "severity": "Minor", "finding": "Missed by the census (bare TEXT glyphs): the panel's close button is drawn six times and wired zero times, so the design never shows the panel being dismissed back onto the editor it floats over.", "evidence": "✕ nodes 165:5, 165:27, 165:47, 165:54, 165:74 and 453:4054 are TEXT with no reaction; the module's route back to the editor is instead a parked 'hotspot/back · to editor' row on four of the six boards (and absent on 165:2 and 165:24 — see W-L-08). Code: PanelHeader onClose={onClose} at NotificationPanel.tsx:181-185, plus an Escape handler at :127-132, both returning to the shell with the panel gone.", "fix": "Wire each ✕ → 52:2 (or, once W-L-04 lands, back to the shell board the bell was clicked on).", "buildFrom": "—"}
{"id": "W-L-16", "module": "Shell", "node": "—", "screen": "census candidates dismissed as FINE (v2 basis)", "q": "Q2", "kind": "dead-control", "severity": "Polish", "finding": "Recorded for the ledger: v2's Shell section names 5 candidates — 4 are DEAD (W-L-10 x2, W-L-11, W-L-12), 1 is a false positive, and 0 are FINE. The unwired controls I dismissed instead are all on boards carrying their own explanation.", "evidence": "v2 Shell candidates: 1175:4825, 1175:4823, 1309:9, 1309:7 (all DEAD) plus 1175:4805 'Upgrade Your Plan' (bare text) — that is the modal's h1 at 16,14 sitting above the badge and body copy, not a control; the bare-imperative heuristic cannot tell a title in the imperative from a button. Dismissed as FINE on their own annotation: 202:2 'RETIRED 2026-09-04' (btn/more, btn/notifications, icon/bell, pill/saved, btn/exit all rx=0 AND no frame catch-all, so nothing resolves at all — correct for a retired board); 642:3696 '[design-ahead] Shell state 13 · Presence (multiplayer)', where CLAUDE.md forbids NEXT_PUBLIC_FEATURE_COLLAB in production so there is no shipped flow to wire; 65:2 ENTRY POINT, whose zero in-degree is not an orphan. 927:4474's own two controls are both wired.", "fix": "No change. The split: 4 DEAD from v2, 1 v2 false positive, 0 FINE — plus 6 classes v2 could not see (W-L-13, W-L-14, W-L-15), for the mechanical reason in W-L-24.", "buildFrom": "—"}
{"id": "W-L-17", "module": "Shell", "node": "1175:4804", "screen": "Upgrade modal · benefit list", "q": "Q3", "kind": "missing-field", "severity": "Major", "finding": "The paywall's first selling point does not exist as a product: the board offers 'Unlimited exports', which the code deleted from the list because there is no export limit to lift.", "evidence": "Board benefit rows read 'Unlimited exports' (1175:4811), 'Premium templates' (1175:4814), 'AI-powered features' (1175:4817), 'Priority support' (1175:4820). Code ships ['Custom domain','Premium templates','AI-powered features','Priority support'] with the reason written into UpgradeModal.tsx:47-54: 'there is no export limit anywhere in PLAN_LIMITS, so Free exports without limit too and the line sells nothing. Custom domain is a real Free→Pro delta in that table'. Confirmed in lib/constants/plan-limits.ts — customDomains 0 (FREE) → 3 (PRO) at :30 and :51, and no export key in PlanLimitKey (:3-6).", "fix": "Replace the 'Unlimited exports' row with 'Custom domain'. This is a plan-limits fact, not a copy preference — the board currently promises something the Free plan already has.", "buildFrom": "—"}
{"id": "W-L-18", "module": "Notifications", "node": "165:2", "screen": "Notifications · unread / all-read — panel header", "q": "Q3", "kind": "missing-field", "severity": "Major", "finding": "'Mark all read' ships in the panel header in exactly the state this board draws, and no board draws it — so the only bulk action on the panel has no design.", "evidence": "Header text on 165:2 and 165:24 is 'Notifications' plus '✕' and nothing else (full text dump, nodes 165:4/165:5 and 165:26/165:27). Code renders it as a PanelHeader action whenever state==='ready' && ordered.length>0 — NotificationPanel.tsx:186-196 — with its own note that gating on 'ready' alone once put the button above the empty state. It calls markAll() (:149-159), which optimistically flips every row and raises an error toast 'Couldn't mark read' on failure (NotificationPanel.tsx:157).", "fix": "Draw 'Mark all read' as a right-aligned header action on 165:2 and 165:24 only, and add a board or caption for the failure toast the code raises.", "buildFrom": "165:2"}
{"id": "W-L-19", "module": "Shell", "node": "1172:4805", "screen": "Exit · guard — dirty variant", "q": "Q3", "kind": "missing-field", "severity": "Major", "finding": "The exit guard has a fourth line the code emits and no variant draws: when the last save has already failed, the dirty dialog shows an alert saying so — which is the difference between 'Save & leave' being a promise and being a lie.", "evidence": "guardNavigation sets error: 'The last save failed.' whenever saveStatus === 'error' (StudioHeader.tsx:400-404), and the dialog renders it as <p className='bk-exit-dialog__error' role='alert'> above the footer (StudioHeader.tsx:977-981). The dirty variant 1172:4805 draws title 1172:4806, one body line 1172:4807 and the three-button foot 1172:4814 — there is no error row between them, on any of the three variants. This is the exact state board 2162:11838 'Save failed' leads into.", "fix": "Add a fourth variant (or an error row on 1172:4805) carrying 'The last save failed.' in error tone, and wire 2162:11838's ‹ Exit to it — otherwise the design shows 'Save & leave' offered with no hint that saving just failed.", "buildFrom": "1172:4805"}
{"id": "W-L-20", "module": "Shell", "node": "1175:4808", "screen": "Upgrade modal · reason line", "q": "Q3", "kind": "missing-field", "severity": "Minor", "finding": "The modal's reason line is a parameter in code and a hard-coded sentence on the board, so the design has no answer for any feature except premium templates — including the empty case the code ships a fallback for.", "evidence": "Board text 1175:4808 is the literal 'Premium templates requires the Pro plan.' Code composes it from the event detail: `${feature} requires the ${requiredPlan} plan.` with the fallback 'This feature requires the Pro plan.' when no feature is passed (UpgradeModal.tsx:120-125), and the plan name is also a parameter on the button label ('Upgrade to {requiredPlan}', :100) and the badge (:117).", "fix": "Mark 1175:4808, the badge and the button label as slots, and draw the no-feature fallback once so the modal is not read as a templates-only screen.", "buildFrom": "—"}
{"id": "W-L-21", "module": "Shell", "node": "—", "screen": "the editor boot screen (StudioSkeleton)", "q": "Q4", "kind": "missing-screen", "severity": "Critical", "finding": "The first screen of every editor session has no board. While the engine is constructing, the whole viewport is a skeleton — not the half-loaded shell that 65:412 draws — and nothing in the file describes it.", "evidence": "AquibraStudio.tsx:462-464 returns <StudioSkeleton/> whenever composer is null, before any shell chrome exists. chrome-ui/Skeleton.tsx:5 documents it as the 'full-viewport boot screen shown while the engine loads' and :91-115 draws its own topbar, rail and body. Name search across every frame of page 1:3 for /skeleton|boot|splash|opening|launch/ returns one hit, and it is 65:2's ENTRY POINT annotation, not a boot board. 65:412 'Shell state 12 · Loading' is a different thing — a drawer-closed shell with a populated inspector (D-C-37). The gap is already producing drift: the skeleton renders five rail items ([1,2,3,4,5].map, Skeleton.tsx:106) where the shipping rail has six.", "fix": "Draw 'Shell state 0 · Boot' from 65:412, replacing the populated inspector and canvas with skeleton blocks, and give it the six-item rail. Wire it as the entry ahead of 199:2 so the loading sequence can be walked.", "buildFrom": "65:412"}
{"id": "W-L-22", "module": "Shell", "node": "—", "screen": "a notification jump taken with unsaved work", "q": "Q4", "kind": "missing-screen", "severity": "Major", "finding": "Clicking a notification while the project is dirty raises the exit guard — a collision between two of my module's flows that neither module draws, and the state that decides whether the user's edits survive their own notification.", "evidence": "NotificationPanel.openRow calls onNavigate(actionUrl) (NotificationPanel.tsx:143-146); StudioHeader passes navigateFromNotification (:842), which is guardNavigation(() => window.location.href = …) at :495-503 — the same guard as ‹ Exit. The comment at :373 names it: 'notification row jumps'. So a row click can produce any of the three dialogs on 1172:4804. Neither section draws it: the Notifications module's only cross-module edges go to 52:2, and 1172:4804's in-degree is 1.", "fix": "Wire a row on 165:2 → 1172:4804 (dirty variant) and its Stay button back to 165:2, so the design says what happens to unsaved work when a notification is followed. Same board, one new edge each way.", "buildFrom": "1172:4804"}
{"id": "W-L-23", "module": "Shell", "node": "927:4474", "screen": "Exit · Workspace (leaves the editor)", "q": "Q4", "kind": "flow-gap", "severity": "Minor", "finding": "The board every exit lands on describes the wrong destination: it explains workspace settings, while the editor's exit goes to the projects list, and a board for that list already exists.", "evidence": "927:4474 body reads '↗ Leaving the editor', 'Workspace settings open in the dashboard.' and 'Members, billing and team permissions are workspace-level — they are not part of this site'; its only onward move is '← Back to Site settings' → 638:2378. Code: exitToDashboard navigates to `${DASHBOARD_URL}/dashboard/projects` (StudioHeader.tsx:481-483). Name census of the dashboard pages: 'Sites · list' 997:1923 and 'Dashboard · Home' 988:3 on page 988:2. The board's 85 inbound edges are Settings 'Nav · Billing ↗' / 'Nav · Members ↗' rows, which its copy fits — the exit-guard edges are the ones it does not.", "fix": "Either give the exit its own landing (a second card naming the projects list, or a cross-page edge to 997:1923) or widen 927:4474's copy so it covers both reasons for leaving. Today the three guard buttons land on a screen about billing.", "buildFrom": "927:4474"}
{"id": "W-L-24", "module": "Shell", "node": "453:4051", "screen": "the v2 census over sections 1776:8385 and 1779:2", "q": "Q2", "kind": "dead-control", "severity": "Critical", "finding": "v2 has v1's blind spot with the sign flipped: instead of counting a board-level catch-all as WIRED, it appears to drop the whole board. It returned candidates on 2 of the 4 Shell boards with no frame reaction and on 0 of the 16 that have one — so v2's Shell section is byte-identical to v1's and misses every dead control I found by hand.", "evidence": "Controlled test on three load-error boards carrying an identically shaped bare-text 'Try again': 453:4051 Notifications — frameRx=1, node 786:4301, chain 'Try again:rx0 < Empty state:rx0' — NOT in v2. 781:4372 Templates — frameRx=0, node 781:4432, chain 'Try again:rx0 < Load error:rx0' — IN v2. 781:4433 Components — frameRx=0, node 781:4488, same chain — IN v2. The only variable is frameRx. Frame-reaction census of my sections: 1776:8385 has frameRx>0 on 16 of 20 (65:2, 65:211, 65:412, 66:4, 66:225, 66:441, 66:640, 199:2, 199:205, 199:409, 200:2, 200:213, 642:3696, 963:4474, 2162:11660, 2162:11838) and frameRx=0 on 4 (202:2, 927:4474, 1175:4804, 1172:4804) — v2's two hits are two of those four. 1779:2 has frameRx>0 on 5 of 6 and v2 returned nothing for the section. The catch-all is this file's dominant board convention, so any section built to it reports near-zero.", "fix": "Treat the board frame as neither wiring nor disqualifying: exclude the board's own reaction from the ancestor test but still descend it. Re-run before any section's Q2 count is quoted — v2's 171/61/22 totals are drawn only from boards that happen to lack a frame reaction. Shell's real list is W-L-10 through W-L-15: 6 topbar control classes x 16 boards, 3 conflict-dialog actions, the drawer close, a Try again, and six panel closes.", "buildFrom": "—"}
{"id": "W-L-25", "module": "Shell", "node": "2162:11660", "screen": "every in-flight → outcome flow the shell owns", "q": "Q1", "kind": "flow-gap", "severity": "Critical", "finding": "Confirming the coordinator's hypothesis, and it is worse in Shell than in Publish: the file draws in-flight and outcome states as pictures and connects them as steps nowhere. Four independent lifecycles in my two sections have the shape, and the save lifecycle has it more severely than the publish one.", "evidence": "SAVE — 2162:11660 'Saving' has no edge to either outcome, and BOTH it and 2162:11838 'Save failed' have in-degree 0 across all 27 sections (page-wide scan of 154 inbound edges); Publish's equivalents at least have in=1 (W-D-01). CONFLICT — 66:640 is the producer and all three of its outcomes are unreachable: 'Review both', 'Save backup', 'Keep mine' are TEXT at board-relative y=498 with no reaction, so the frame catch-all sends all three to 199:2 (W-L-02). NOTIFICATIONS — a row is the producer and no jump destination is wired anywhere in 1779:2's 16 edges (W-L-07). EXIT GUARD — the mirror image rather than the same shape, and worth distinguishing: its outcomes ARE wired on two of three variants (Stay → 199:2 / 66:441, Leave → 927:4474); what is missing is the producer edge, because ‹ Exit is rx=0 on 16 of 17 boards and the guard's in-degree is 1, from the post-exit screen (W-L-01). The file already contains one worked counter-example: History's [design-ahead] chain 879:4518 restore-confirm → 879:4526 restoring → 879:4531 restored / 1156:4665 restore-failed.", "fix": "Adopt the History chain as the file's pattern for every in-flight decision, and add the missing producer/outcome edges named in W-L-01, W-L-02, W-L-03 and W-L-07. This is one convention, not four bugs.", "buildFrom": "879:4518"}
{"id": "W-L-26", "module": "Shell", "node": "66:640", "screen": "what happens after a Shell modal is answered", "q": "Q4", "kind": "missing-screen", "severity": "Major", "finding": "Shell does not have Settings' failure-only asymmetry — it has the reverse one, and only on modals: all three of my modals draw the decision and none draws its consequence, so no board says what the editor looks like after a conflict is resolved, an exit is taken or a plan is upgraded.", "evidence": "Name search across every frame of page 1:3 for /reload|overwrit|backup|kept|resolved|upgraded|left the editor|restored/ returns 30 boards and not one belongs to Shell: History owns 8, Review panel 2, Client sign-off 8, Journeys the rest. Conflict has three consequences in code (modals/ConflictModal.tsx:35 via AquibraStudio.tsx:684-708 — reload, download-then-reload, two-step overwrite) and one board. The upgrade modal has a post-upgrade state (UpgradeModal.tsx:83-88 opens billing in a new tab and closes the modal, leaving the caller standing) and no board. Elsewhere the asymmetry runs the other way and is also wrong: the save pill draws success on ~14 boards and each failure once, and Notifications draws the mark-all SUCCESS (165:24) but not the failure toast the code raises ('Couldn't mark read', NotificationPanel.tsx:157).", "fix": "Draw one outcome board per modal decision, copying History's confirm → in-flight → resolved/failed chain. The conflict outcomes are the ones that matter: after 'Overwrite' a colleague's work is gone and no board says so.", "buildFrom": "879:4531"}
{"id": "W-L-27", "module": "Shell", "node": "697:461", "screen": "verification of the coordinator's two changes", "q": "Q3", "kind": "missing-field", "severity": "Polish", "finding": "Both changes verified against code. The offline-pill correction is right at the master and I could not fault it. The two new save boards are right in content and wrong in wiring — the half already filed as W-L-03.", "evidence": "Save status COMPONENT_SET 697:461 on page 1:2 carries six variants whose copy matches chrome-ui/SaveStatus.tsx:41-52 leaf for leaf: 697:444 saved 'Saved 2m ago', 697:448 saving 'Saving…', 697:452 unsaved 'Unsaved changes', 697:456 conflict 'Conflict — reload', 697:460 offline 'Offline — not saved', 701:2 error 'Save failed — retry'. The rejected wording 'saved locally' appears in no variant — the master fix is real. Board copy matches too: topbar text reads 'Saving…' on 2162:11660 and 'Save failed — retry' on 2162:11838, 'Offline — not saved' on 66:441 (screenshot), 'Unsaved changes' on 65:2. So all six shipped states are now boarded, which was the stated goal. The unverified remainder: whether the pills on 2162:11660/11838 are instances of 697:461 or hand-drawn text — not checked, and it decides whether W-L-13's fix is one master edit or two board edits.", "fix": "Nothing to correct in the copy. Close the change out by adding the three edges in W-L-03 — six boarded states with two of them unreachable is a component set that is complete and a flow that is not.", "buildFrom": "—"}
