{"id": "COVER-1-01", "module": "settings", "action": "fix-board", "severity": "Critical", "confidence": "measured", "target": "1776:8387 / 1702:6931 1702:7095 1702:7261 1702:7425 1703:7127 1703:7286 1703:7445 1703:7606 1703:7760 1703:7914 1703:8070 1703:8232 1703:8394 1703:8556 1703:8720 1703:8882 1703:9046 1703:9208 1703:9370 1703:9532 1703:9694 1703:9857 1703:10022 1703:10185 1703:10352 1703:10515 640:2440 640:2789 640:3135 640:3849  (all 30 boards I opened; the same header sits on the 12 not opened)", "claim": "Every Settings screen and state board draws a pane header made of a title, a grey subtitle and a right-aligned primary Button — 'Add domain' (1702:6998, 1702:7162, 1702:7328, 1702:7492), 'Add locale' (1703:7194, 1703:7353, 1703:7512), 'Save changes' (1703:7673, 1703:7827, 1703:7981, 640:3098, 1703:10582), 'Add redirect' (1703:8137, 1703:8299, 1703:8461, 1703:8623, 640:2749), 'Export CSV' (1703:8787, 1703:8949, 1703:9113, 1703:9275, 1703:9437, 1703:9599, 640:3444) and 'Add endpoint' (1703:9761, 1703:9924, 1703:10089, 1703:10252, 1703:10419, 640:4158).", "truth": "The Settings section header has no subtitle and no action slot at all. A drilled-in section renders DrillInHeader, whose entire header is a 'Back to Settings' button, a two-item breadcrumb (GROUP / Title) and PanelHeaderActions (pin/help/close). Nothing else can be placed there — the component takes no children, no action and no actions prop. Save/Discard live in a sticky savebar at the BOTTOM, disabled when clean; 'Add domain', 'Add redirect', 'Connect endpoint' and 'Export CSV' all live INSIDE the pane body, and 'Export CSV' is additionally disabled until submissions exist.", "evidence": "DrillInHeader.tsx:12-47 is the complete props interface (title, parentName, breadcrumb, onBack, isDirty, onBackAttempt, isExpanded, onExpandToggle, onHelpClick, onClose, focusTarget, enableDocumentEscape) — no action/actions/children. Render: DrillInHeader.tsx:121-171 emits exactly the Back button (:124-133), the breadcrumb nav (:136-166) and PanelHeaderActions (:168-171). Mounted at SettingsTab.tsx:870-879 with breadcrumb={[GROUP_LABELS[current.group], current.title]}. Savebar SettingsTab.tsx:883-903 ('{dirtyCount} unsaved' + Discard + Save, both disabled={!screenIsDirty}). In-body controls: DomainsScreen.tsx:219-227 (Add domain), RedirectsScreen.tsx:225-227 (Add redirect), WebhooksScreen.tsx:195-197 (Connect endpoint), FormsScreen.tsx:292-300 (Export CSV, disabled when total===0). FIG-M-21 filed this against 2209:11816 alone; it is on 30 more boards.", "edit": "On all 30 boards replace the 'Pane header' frame with the shipped header: a 'Back to Settings' row, a breadcrumb reading '<GROUP> / <Section>' (e.g. 'DISTRIBUTION / Domains'), and the pin/help/close icon trio at the right. Delete the primary Button node from each header (ids listed in the claim) and delete the subtitle text node. The subtitle text is the nav row's own subtitle (SettingsTab.tsx:80-90) and belongs on the root board 1688:7195, not here. Where the deleted header button has a real in-body twin (Add domain / Add redirect / Connect endpoint / Export CSV) the twin is already drawn; where it does not ('Save changes'), the savebar already at the foot is the control."}
{"id": "COVER-1-02", "module": "settings", "action": "mark-unimplemented", "severity": "Critical", "confidence": "measured", "target": "1776:8387 / 640:2440  (S7 · Settings · Redirects) — inner card '404 SUGGESTER' (640:2778)", "claim": "The Redirects screen carries a second card, '404 SUGGESTER' (640:2778), with a 'Suggest redirects from 404s' switch (640:2780) and two live suggestions — '/pizza-menu → /menu' (640:2784) and '/contact-us → /contact' (640:2787) — each with an 'Accept' button (640:2785, 640:2788).", "truth": "No 404 suggester exists anywhere in the product. A repo-wide grep for suggestRedirect / 404Suggest / suggester across packages/editor/src and server/ returns exactly ONE hit, and it is the nav subtitle string, not a feature. RedirectsScreen is 281 lines and contains a notice, an Add-redirect form and an Active-redirects list; there is no 404 log, no suggestion source and no accept action. There is also no 404 collection anywhere on the server to derive suggestions from.", "evidence": "grep -rn 'suggestRedirect|404Suggest|suggester' packages/editor/src server -> SettingsTab.tsx:87 only: { id: \"redirects\", title: \"Redirects\", subtitle: \"301 / 302 + 404 suggester\", group: \"plumbing\" }. RedirectsScreen.tsx full file: :184-190 notice, :191-228 Add redirect, :230-266 Active redirects. No other section. FIG-M-10 already proposes fixing the same phantom in the nav SUBTITLE on root board 1688:7195; nothing proposes anything for the card that draws the feature.", "edit": "Rename frame 640:2778 from 'Card · 404 SUGGESTER' to 'NOT IMPLEMENTED — no 404 log and no suggester exist; the only trace of this feature is a nav subtitle string'. Do NOT delete the card. Add a caption text node under it in the 11px muted BOARD_CAPTION style reading: 'There is no producer. A repo-wide search for suggestRedirect / 404Suggest / suggester returns one hit — the nav subtitle at SettingsTab.tsx:87. RedirectsScreen.tsx has three blocks and this is not one of them.' Pairs with FIG-M-10, which removes the same promise from the nav subtitle."}
{"id": "COVER-1-03", "module": "settings", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8387 / 640:2440  (S7 · Settings · Redirects) — row buttons 640:2763, 640:2769, 640:2775", "claim": "Every redirect row carries an 'Edit' button (640:2763, 640:2769, 640:2775), so a rule can be changed in place.", "truth": "A redirect cannot be edited. The row renders one action and it is Delete. There is no update mutation on the client and no edit state in the screen; changing a rule means deleting it and adding a new one through the form above.", "evidence": "RedirectsScreen.tsx:241-262 is the whole row: PATH_COL with fromPath / arrow / toUrl (:243-247), a TYPE_BADGE (:249), then a single Button (:250-260) whose label is 'Delete' (:259) and whose aria-label is `Delete redirect from ${r.fromPath}` (:254). The screen's mutation surface is redirects.create (:125) and redirects.delete (via handleDelete, :160-167) — no update. grep 'redirects.update' across packages/editor/src and server/trpc/routers returns nothing.", "edit": "On 640:2440 change the three row buttons 640:2763 / 640:2769 / 640:2775 from 'Edit' to 'Delete', styled in the error tone the code uses (RedirectsScreen.tsx:255 sets tw:text-[var(--bk-error)]). Keep the table shape as drawn."}
{"id": "COVER-1-04", "module": "settings", "action": "fix-board", "severity": "Critical", "confidence": "measured", "target": "1776:8387 / 1703:8070 1703:8232 1703:8394 1703:8556  (Redirects · loading / empty / load-error / save-error)", "claim": "All four Redirects state boards draw the screen as ONE card titled 'ACTIVE REDIRECTS' whose description reads 'Send visitors from an old URL to a new one. 301 (permanent) preserves SEO; 302 (temporary) signals a short-term move.', with the state line beneath it.", "truth": "Three things are wrong and one of them is the module's honesty banner. (1) The screen ALWAYS opens with a role=\"status\" notice reading 'Saved, not yet live. Redirect rules are stored but aren't served on your published site yet — visitors hitting the old URL won't be forwarded until deployment wiring ships.' No Redirects board anywhere draws it. (2) The quoted description belongs to the 'Add redirect' card, not to 'Active redirects' — 'Active redirects' has a title only, and its title carries a live count: `Active redirects (N)`. (3) None of these four states is an early return: the notice and the whole three-field Add-redirect form stay on screen in all of them. Only the !projectId branch early-returns.", "evidence": "RedirectsScreen.tsx:184-190 the notice (unconditional, first child of <Screen>); :191-193 <Section title=\"Add redirect\" desc=\"Send visitors from an old URL to a new one. 301 (permanent) preserves SEO; 302 (temporary) signals a short-term move.\">; :230 <Section title={`Active redirects${rows.length ? ` (${rows.length})` : \"\"}`}> with no desc; :231 loading, :232-234 loadError, :235-237 empty inside it. The only early return is :170-177 (!projectId).", "edit": "On all four boards: add the 'Saved, not yet live.' notice as the first block in the pane body; retitle the existing card to 'ADD REDIRECT' and keep its description where it is; add a second card below it titled 'ACTIVE REDIRECTS' with no description, and move the state line (Loading… / No redirects yet. Add one above. / Failed to load redirects. / the validation error) into whichever card the code puts it in — the three list states into ACTIVE REDIRECTS, the validation error into ADD REDIRECT (see COVER-1-05)."}
{"id": "COVER-1-05", "module": "settings", "action": "add-state", "severity": "Major", "confidence": "measured", "target": "1776:8387 / 1703:8556  (S7 · Settings · Redirects · save-error) — clone source", "claim": "1703:8556 draws one submit failure, 'From path must start with / (e.g. /old-page)' (1703:8717), placed inside the ACTIVE REDIRECTS card above an 'Add redirect' button.", "truth": "There are three submit-time validation errors, not one, and all three render inside the ADD REDIRECT form directly above its submit button — not in the list card. The other two are never drawn. A fourth, server-side failure ('Failed to add redirect.') also exists and is never drawn. A delete failure is drawn nowhere either and lands in the LOAD-error slot, so a failed delete looks like a failed load.", "evidence": "RedirectsScreen.tsx:106-110 `From path must start with / (e.g. /old-page)`; :112-114 `To URL is required.`; :117-120 `To URL must be a path (/new-page) or full URL (https://example.com/new).`; :136-137 `Failed to add redirect.`; all four land in submitError, rendered at :221-223 INSIDE the form, above the submit Button at :225-227. Delete failure :166 setLoadError(... 'Failed to delete redirect.') — the load-error slot at :232-234.", "edit": "Move the error line 1703:8717 out of the ACTIVE REDIRECTS card and into the ADD REDIRECT card, immediately above the 'Add redirect' button (1703:8718). Then clone 1703:8556 twice for the two undrawn validation strings, and once more for a delete failure showing 'Failed to delete redirect.' in the ACTIVE REDIRECTS card with the rows still listed."}
{"id": "COVER-1-06", "module": "settings", "action": "mark-unimplemented", "severity": "Critical", "confidence": "measured", "target": "1776:8387 / 640:3135  (S7 · Settings · Forms) — inner card 'PROVIDER' (640:3448)", "claim": "The Forms screen opens with a 'PROVIDER' card (640:3448) holding a 'Form provider' select set to 'Formspree' (640:3451), an 'Endpoint URL' input holding 'https://formspree.io/f/mzbqwerty' (640:3457) and a 'Spam protection' field (640:3460) — i.e. forms are configured here.", "truth": "FormsScreen has no provider configuration of any kind. It is 456 lines and contains exactly two sections: a form selector with four filter chips, and a submissions inbox. A Formspree integration does exist in the product, but it lives in the EXPORT engine and its settings type sits in the SEO types file — not on this screen and not behind any control this board draws. Worse, the screen's own copy says submissions are never captured at all, so a provider endpoint typed here would have nothing to configure.", "evidence": "FormsScreen.tsx:262-288 <Section title=\"Form\" desc=\"Select a form to view its submissions inbox.\"> + the filter tablist; :290 <Section title={`Submissions...`}>. Those are the only two Sections in the return. The real Formspree code: engine/export/FormspreeInjector.ts:15 FORMSPREE_BASE_URL, :32 class FormspreeInjector; shared/types/seo.ts:15 provider: \"formspree\" | \"custom\", :16 Formspree form ID, :22 custom email subject. The screen's own honesty line, FormsScreen.tsx:249-251: 'A Form block renders on the published page, but its submissions are not captured yet — this inbox stays empty until that is wired.' Explained at :240-247: inserting a Form exports a <form> with no action and no submit script, the public endpoint is never called, and nothing creates a FormBlock row except site duplication.", "edit": "Rename frame 640:3448 from 'Card · PROVIDER' to 'NOT IMPLEMENTED — no provider configuration exists on this screen; Formspree is wired in the export engine, not here'. Keep the card. Add a caption beneath it: 'FormsScreen.tsx has two Sections — a form selector (:262) and a submissions inbox (:290). The Formspree integration lives at engine/export/FormspreeInjector.ts:32 with its settings type at shared/types/seo.ts:15, and no UI on this screen writes it.'"}
{"id": "COVER-1-07", "module": "settings", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8387 / 640:3135  (S7 · Settings · Forms) — card 'SUBMISSIONS' (640:3464)", "claim": "Submissions are a four-column table — FORM / FROM / RECEIVED / STATUS (640:3466-3469) — whose rows carry a status Badge reading 'NEW' (640:3474, 640:3480) or 'DELIVERED' (640:3486).", "truth": "The inbox is not a table and has no status column. It is a list of expandable rows: a one-line summary derived from the first field of the submission payload, a mono timestamp, an optional source URL, and a left border that is blue when unread and grey when read. Expanding a row reveals a definition list of every field plus five actions. 'DELIVERED' is not a submission state anywhere — a submission has isRead, isSpam and isArchived, surfaced as four filter chips, not as a badge. There is also a pagination footer and a per-page cap the board does not draw.", "evidence": "FormsScreen.tsx:310-370 the <li> row: read/unread border :315, summary span :330-332 (summarize(s.data), :411 returns the first entry sliced to 80 chars), timestamp :334, sourceUrl :336; expanded detail :338-368 with <dl> :341-349 and actions :350-366 — 'Mark spam' / 'Not spam' / 'Archive' / 'Unarchive' / 'Delete'. Filter chips :272-288, values [\"inbox\",\"unread\",\"spam\",\"archived\"] (:273). Pagination :373-400. grep 'DELIVERED' across packages/editor/src returns nothing in any forms path.", "edit": "On 640:3135 replace the SUBMISSIONS table (640:3464) with the shipped shape: a four-chip filter row (Inbox / Unread / Spam / Archived) under the Form selector, then a list of rows each showing a summary line, a right-aligned mono time and a coloured left border, with one row expanded to show the field <dl> and the five action buttons. Delete the FORM/FROM/RECEIVED/STATUS header (640:3466-3469) and the NEW/DELIVERED badges (640:3474, 640:3480, 640:3486)."}
{"id": "COVER-1-08", "module": "settings", "action": "fix-board", "severity": "Minor", "confidence": "measured", "target": "1776:8387 / 1703:9532 1703:9370 1703:9208  (Forms · inbox-empty / inbox-loading / action-error)", "claim": "The three inbox-state boards show a single FORMS card with a status line and no filter controls; 1703:9532 reads 'No submissions in inbox.' (1703:9693) as a fixed string.", "truth": "The inbox states only occur AFTER a form is selected, and by then the screen is showing a Form select and a four-chip filter tablist above the Submissions section. The empty string is a template that names the ACTIVE chip, so it also reads 'No submissions in unread.', '… in spam.' and '… in archived.' — a board with no chips cannot show which one produced the line.", "evidence": "FormsScreen.tsx:307 `No submissions in {filter}.`; filter is state seeded from the tablist at :272-288 over (['inbox','unread','spam','archived'] as const) at :273. The inbox states live inside <Section title={`Submissions (${total})`}> at :290 — the Form <Section> at :262 and its chips are rendered above them in the same return.", "edit": "On 1703:9532, 1703:9370 and 1703:9208 add the Form selector card and the four-chip filter row above the Submissions card, with 'Inbox' shown active, so the state line reads against the chip that produced it."}
{"id": "COVER-1-09", "module": "settings", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8387 / 640:3849  (S7 · Settings · Webhooks) — 640:4169 and card 'RECENT DELIVERIES' (640:4178)", "claim": "The Events field is a select whose value reads 'site.published, review.approved' (640:4169), and a 'RECENT DELIVERIES' card (640:4178) lists per-delivery rows with EVENT / ATTEMPTED / RESPONSE / STATUS columns and response codes '200 OK' and '502 Bad Gateway' (640:4185-4200).", "truth": "Neither event name exists. The webhook offers exactly two events and they are `site.publish` and `form.submit` — and the second carries a label that says it never fires. They are checkboxes, not a select. There is also no delivery log: the status object exposes only lastDeliveryAt, failures24h and a recentFailures array of {error, createdAt}, and the screen renders ONE summary line from them, never a table, and never an HTTP response code.", "evidence": "WebhooksScreen.tsx:27-33 const EVENTS = [{ id: \"site.publish\", label: \"site.publish — fires after every successful publish\" }, { id: \"form.submit\", label: \"form.submit — ready, but nothing sends it yet (form capture is unbuilt)\" }]. Rendered as Checkbox rows at :206-220 inside <Field label=\"Events\">. Status shape :37-45 including recentFailures: Array<{error: string; createdAt: string}> (:44). renderDeliveryLine :155-168 returns exactly one of: 'Never fired yet — publish the site or submit a form to see the first delivery.' (:158), '⚠ {n} failed deliver{y|ies} in the last 24h · last attempt {ago}' (:161-164), '✓ Delivering — last delivery {ago}.' (:167).", "edit": "On 640:3849 replace the Events select (640:4169) with two checkbox rows carrying the code's own labels verbatim, 'site.publish — fires after every successful publish' and 'form.submit — ready, but nothing sends it yet (form capture is unbuilt)'. Replace the RECENT DELIVERIES table (640:4178) with a single status line ('✓ Delivering — last delivery 2h ago.') plus, below it, the failures list the code actually renders from recentFailures (a timestamp and an error string per row, no response code column)."}
{"id": "COVER-1-10", "module": "settings", "action": "fix-board", "severity": "Minor", "confidence": "measured", "target": "1776:8387 / 640:3849  (S7 · Settings · Webhooks) — text node 640:4176", "claim": "The signing-secret field reads 'whsec_8f21c0a94b7e… · rotate every 90 days' (640:4176), stating a rotation policy.", "truth": "There is no rotation policy, no expiry and no reminder. The secret is a value with a hint and three buttons; nothing schedules or prompts a rotation.", "evidence": "WebhooksScreen.tsx:254 <Field label=\"Signing secret\" hint=\"Verify deliveries by recomputing the HMAC with this secret\">; actions at :293-295 are 'Edit', 'Regenerate secret', 'Disconnect', with a confirm at :271-276. grep for 'rotate' / '90 days' across packages/editor/src and server returns nothing in any webhook path.", "edit": "On 640:3849 change 640:4176 to drop the '· rotate every 90 days' clause and put the shipped hint under the field instead: 'Verify deliveries by recomputing the HMAC with this secret'."}
{"id": "COVER-1-11", "module": "settings", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8387 / 1703:7914 1703:7445 1703:10185 1703:9208  (Headers / Localization / Webhooks · save-error and Forms · action-error)", "claim": "Each of the four save-failure boards draws a single card containing the section title, its description and the error line — the screen reduced to one block.", "truth": "A save failure is never an early return. The whole form stays on screen and the error renders at the foot of it, or inline in the section that failed. Headers keeps five cards (CSP, HSTS, X-Frame-Options, Referrer-Policy, Permissions-Policy) plus its 'Not yet enforced' notice; Localization keeps the default-locale card, the enabled-locales list, the add-locale row and its routing note; Webhooks keeps the open edit form with the URL field and the two event checkboxes; Forms keeps the Form selector, the four filter chips, the Submissions heading and its Export CSV control. Only the loading and load-error branches early-return, and only on Headers, Localization and Forms.", "evidence": "HeadersScreen.tsx:158-163 notice, :165 CSP, :183 HSTS, :206 X-Frame-Options, :226 Referrer-Policy, :252 Permissions-Policy, then :268-271 <Section title=\"\"> holding saveError and :272-279 the 'Save headers' button. LocalizationScreen.tsx:206-260 the cards, :288-297 the note + saveError. WebhooksScreen.tsx:200-225 the edit form with submitError at :221. FormsScreen.tsx:262-288 selector + chips, :290-300 heading + export, subsError at :303-305. Early returns: HeadersScreen.tsx:137-145 (loading), :147-155 (loadError); LocalizationScreen.tsx:181-189, :191-199; FormsScreen.tsx:215-223, :225-233. FIG-M-03's edit note asks a NEW forbidden board to keep 'the full four-card form'; nothing proposes fixing these four existing boards.", "edit": "Redraw all four boards as the full screen with the error in its real position: 1703:7914 = five Headers cards + the 'Not yet enforced' notice, error at the foot beside 'Save headers'; 1703:7445 = the three Localization cards + routing note, error at the foot beside 'Save locales'; 1703:10185 = the open Webhooks edit form (URL + two event checkboxes), error above the Connect/Cancel pair; 1703:9208 = the Form selector + chips + Submissions heading + Export CSV, error inside the Submissions card. Note that Headers has FIVE cards, not four — see COVER-1-20."}
{"id": "COVER-1-12", "module": "settings", "action": "add-state", "severity": "Major", "confidence": "measured", "target": "1776:8387 / 1702:7095  (S7 · Settings · Domains · empty) — clone source for five new boards", "claim": "Settings has 26 state boards covering loading, empty, load-error, save-error, validation and forbidden, so every way a screen can open is drawn.", "truth": "The single most common non-happy opening is drawn nowhere: no project id. Five screens each early-return their own distinct copy for it, before any fetch runs, and it is exactly what the standalone editor and the demo project show. Domains alone gets a different sentence from the other four.", "evidence": "DomainsScreen.tsx:193-201 — <Section title=\"Custom domain\" desc=\"Open a real site to connect a domain.\"> with body 'The demo project can't have a custom domain.' RedirectsScreen.tsx:170-177 'Open this site from the dashboard to manage redirects.' LocalizationScreen.tsx:172-179 'Open this site from the dashboard to manage locales.' HeadersScreen.tsx:128-135 'Open this site from the dashboard to manage headers.' FormsScreen.tsx:206-213 'Open this site from the dashboard to manage forms.' Every one is the FIRST branch in its screen, tested before loading.", "edit": "Clone 1702:7095 into five boards named 'S7 · Settings · <Screen> · no-project' (Domains, Redirects, Localization, Headers, Forms), each a single card carrying that screen's own sentence verbatim and no controls at all. Domains gets the title 'Custom domain', the description 'Open a real site to connect a domain.' and the line 'The demo project can't have a custom domain.'; the other four get their screen title and their own one-liner."}
{"id": "COVER-1-13", "module": "settings", "action": "add-state", "severity": "Major", "confidence": "measured", "target": "1776:8387 / 1703:7445  (S7 · Settings · Localization · save-error) — clone source", "claim": "Localization has one error board and it carries the server-side string 'Failed to save locales.' (1703:7603).", "truth": "Two of Localization's three error strings never reach the server at all — they are client-side refusals raised before the mutation, and they are the ones a user will actually hit, because they guard the two destructive edits on the screen. Neither is drawn.", "evidence": "LocalizationScreen.tsx:116 setSaveError('Cannot remove the default locale. Change the default first.'); :120 setSaveError('At least one locale must remain enabled.'); both set and return before the save path at :131-155, whose catch produces the drawn string at :154 ('Failed to save locales.'). All three render in the same slot at :295-297.", "edit": "Clone 1703:7445 twice, keeping the full screen per COVER-1-11: one board named 'S7 · Settings · Localization · cannot-remove-default' showing the error 'Cannot remove the default locale. Change the default first.' with the default locale's Remove button just clicked, and one named '… · last-locale' showing 'At least one locale must remain enabled.' with a single locale left in the list."}
{"id": "COVER-1-14", "module": "settings", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8387 / 1703:10515  (S7 · Settings · Analytics · validation) — card 'CONSENT' (1738:8390)", "claim": "The validation board draws two cards, ANALYTICS (1703:10672) and CONSENT (1738:8390), the latter holding a field labelled simply 'Cookie Consent' (1738:8392).", "truth": "Two problems. (1) The screen always renders FIVE sections — Google Analytics, Meta Pixel, Microsoft Clarity, Google Tag Manager, Cookie Consent — so a validation state showing two cards is showing 40% of the screen. (2) The consent control's label is not 'Cookie Consent': the switch is titled 'Show cookie banner (stored, not yet shown)' and is followed by an information block stating that Buildrick renders no consent banner and that the analytics load without waiting for consent. Dropping both halves turns a deliberate compliance disclaimer into an ordinary setting.", "evidence": "AnalyticsScreen.tsx:109 <Section title=\"Google Analytics\">, :153 \"Meta Pixel\", :196 \"Microsoft Clarity\", :237 \"Google Tag Manager\", :278 \"Cookie Consent\"; SwitchRow title at :280 'Show cookie banner (stored, not yet shown)'; the block at :298-303 'Records the preference only. Buildrick does not render a consent banner yet, and the analytics above load as soon as the page does — they do not wait for consent. If you need GDPR consent today, add your own banner in Settings → Custom code.' The rationale at :283-297 records that the earlier copy promised GDPR compliance that neither half of the code delivers. The GA error copy the board carries (1703:10678) is verbatim correct — AnalyticsScreen.tsx:132-137.", "edit": "On 1703:10515 add the three missing cards (Meta Pixel, Microsoft Clarity, Google Tag Manager) between ANALYTICS and CONSENT. In the CONSENT card change the field label 1738:8392 to 'Show cookie banner (stored, not yet shown)' and add the disclaimer paragraph beneath it verbatim from AnalyticsScreen.tsx:299-303. Leave the GA validation error as drawn — it matches."}
{"id": "COVER-1-20", "module": "settings", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8387 / 640:2789  (S7 · Settings · Headers)", "claim": "Headers is four cards — CONTENT SECURITY POLICY (640:3102), X-FRAME-OPTIONS (640:3108), REFERRER-POLICY (640:3116) and HSTS (640:3124) — with HSTS holding an 'Enable HSTS' toggle (640:3126) beside a 'Max age' select (640:3131).", "truth": "Five cards, in a different order, with no Enable toggle and with a warning banner the board omits. The order is CSP, HSTS, X-Frame-Options, Referrer-Policy, Permissions-Policy; the fifth card is missing from the board entirely. HSTS has one control, a 'Max age' Field — there is no enable switch. And the screen opens with a notice saying the headers are not enforced, which is the single most important thing this screen says.", "evidence": "HeadersScreen.tsx:158-163 the notice ('Not yet enforced. Values save to your site config but …'); :165 'Content Security Policy', :183 'HSTS (HTTP Strict Transport Security)' with its only Field at :186 <Field label=\"Max age\">, :206 'X-Frame-Options', :226 'Referrer-Policy', :252 'Permissions-Policy' with <Field label=\"Header value\" hint=\"e.g. camera=(), microphone=(), geolocation=(self)\"> at :255. FIG-M-02 filed the same phantom Enable-HSTS toggle against 2209:11816 and FIG-M-22 the same missing not-enforced banner and Permissions-Policy card; both defects are on this board too and neither row names it.", "edit": "On 640:2789: add the 'Not yet enforced' notice as the first block; reorder the cards to CSP, HSTS, X-Frame-Options, Referrer-Policy; add a fifth card 'PERMISSIONS-POLICY' with a 'Header value' field and the hint 'e.g. camera=(), microphone=(), geolocation=(self)'; and delete the 'Enable HSTS' toggle 640:3126, leaving 'Max age' as the card's only control. Same three edits FIG-M-02 / FIG-M-22 specify for 2209:11816."}
{"id": "COVER-1-15", "module": "settings", "action": "none", "severity": "Minor", "confidence": "measured", "target": "1776:8387 / 1702:6931 1702:7095 1702:7261 1702:7425 1703:9694 1703:9857 1703:10022 1703:10185 1703:10352  (Domains + Webhooks state boards)", "claim": "Coverage record: the Domains and Webhooks state copy is correct and does not need re-auditing.", "truth": "Read character by character against the screens. Domains: 'Loading domains…' = DomainsScreen.tsx:211; 'No custom domain. Using the free buildrick.app address until you connect one.' = :215-217; 'Couldn't load domains. Is the dashboard running?' = :98; 'Couldn't connect the domain.' = :147; the field label 'Domain', the hint 'e.g. bellastudio.com or www.bellastudio.com' and the placeholder 'yourdomain.com' = :234-240; 'Connect' / 'Cancel' = :245, :248. Webhooks: 'Loading…' = WebhooksScreen.tsx:189; 'No endpoint connected.' + 'Connect endpoint' = :194-197; 'Couldn't load the webhook. Is the dashboard running?' = :97; 'Couldn't save the webhook.' = :125; the hint 'HTTPS endpoint that receives POST deliveries' = :203; and the forbidden body 'Only an admin can manage webhooks — the signing secret is part of the configuration. Ask a workspace admin to set this up.' = :174-176, verbatim. The card descriptions on both families also match (:208-210 and :186-188).", "evidence": "Board strings read live from the file at nodes 1702:7094, 1702:7258, 1702:7260, 1702:7424, 1702:7588-7599, 1703:9856, 1703:10019-10020, 1703:10184, 1703:10347-10350, 1703:10514. Code lines as cited. The header button on every one of these boards is a separate defect — COVER-1-01.", "edit": "No change beyond COVER-1-01. Recorded so these nine are not re-read."}
{"id": "COVER-1-16", "module": "settings", "action": "none", "severity": "Minor", "confidence": "measured", "target": "1776:8387 / 1703:7606 1703:7760 1703:7127 1703:7286 1703:8070 1703:8232 1703:8394 1703:8720 1703:8882 1703:9046  (Headers / Localization / Redirects / Forms loading, empty and load-error)", "claim": "Coverage record: the loading, empty and load-error copy on these ten boards is correct.", "truth": "Verbatim matches. 'Loading…' = HeadersScreen.tsx:141 and LocalizationScreen.tsx:185 and RedirectsScreen.tsx:231; 'Failed to load headers.' = HeadersScreen.tsx:84; 'Failed to load locales.' = LocalizationScreen.tsx:97; 'Failed to load redirects.' = RedirectsScreen.tsx:91; 'No redirects yet. Add one above.' = :236; 'Loading forms…' = FormsScreen.tsx:219; 'Failed to load forms.' = :99; 'No forms yet.' = :252 together with the card description 'A Form block renders on the published page, but its submissions are not captured yet — this inbox stays empty until that is wired.' = :250, which the board carries in full at 1703:9043 — the one place in the section where the board already tells the truth about an unbuilt capability.", "evidence": "Board nodes 1703:7759, 1703:7913, 1703:7285, 1703:7444, 1703:8231, 1703:8393, 1703:8555, 1703:8881, 1703:9043, 1703:9045, 1703:9207 read live; code lines as cited. Two caveats recorded rather than filed: the Headers and Localization loading/load-error boards add a card DESCRIPTION that those early-return branches do not render (HeadersScreen.tsx:140, :150; LocalizationScreen.tsx:184, :194 pass a title only) — left alone because copy on screen is the board's call per the brief's precedence rule. The Redirects four are a separate defect, COVER-1-04.", "edit": "No change. Recorded so these ten are not re-read."}
{"id": "COVER-1-17", "module": "settings", "action": "none", "severity": "Major", "confidence": "measured", "target": "1776:8387 / whole section — the two-column nav+pane layout on all 45 non-root boards", "claim": "Every Settings board except the root draws a persistent 140px 'Settings nav' column beside a 1240px pane; the shipped component is a drill-in stack in which the nav is the whole root screen and a section covers it completely.", "truth": "TRUE, and already filed and confirmed — not re-filed here. D-C-01 measured the geometry on all 45 frames and VERDICTS.jsonl marks it CONFIRMED. I re-derived the code half independently while reading these boards and it holds: .bd-set-screen is position:absolute inset:0 for BOTH root and section, .bd-set-snav is flex:1 root-screen content rather than a column, and the root is aria-hidden + inert whenever a section is open.", "evidence": "settings.css:24-33 (.bd-set-screen absolute, inset:0), :52-58 (.bd-set-screen--section translateX in/out), :61-67 (.bd-set-snav flex:1). SettingsTab.tsx:288 isRoot, :829-831 root aria-hidden/inert, :862-869 the section screen. Existing rows: C.jsonl D-C-01 (Critical, 39 of 45 boards) and VERDICTS.jsonl D-C-01 = CONFIRMED.", "edit": "No new row. Cite D-C-01. Recorded because FIG-M did not turn D-C-01 into a Figma action and a reader of the FIG-* files alone would not know the biggest defect in this section is already adjudicated."}
{"id": "COVER-1-21", "module": "media", "action": "fix-board", "severity": "Critical", "confidence": "measured", "target": "1776:8372 / 1162:4617 1163:4641 1163:13695  (Media · fullpage · empty / list-view · bulk-select / unused-scope · context-menu) — status pills 1162:4826, 1163:4850", "claim": "Every fullpage library board ends its status bar with a permanent warning pill reading '⚠ This device only' — including 1162:4617, where the same bar reads '0 assets · 0 MB / 500 MB'.", "truth": "That string was deleted from the code as a defect and replaced with a conditional count. It was hardcoded and unconditional, so in production — where the blob token is required and assets DO sync — it told every user the opposite of the truth. The bar now renders '{n} not on the server' and ONLY when at least one asset actually failed to mirror. On the empty board it is doubly impossible: with zero assets localOnlyCount is zero, so no pill of any kind renders.", "evidence": "LibraryManager.tsx:441-448 is the removal note in the source ('This was a hardcoded string … it said \"This device only\" unconditionally — so in production … it told every user the opposite of the truth'); :449-455 the replacement — {localOnlyCount > 0 && <span className=\"mgr-sync-pill\"><AlertCircle size={10}/>{localOnlyCount} not on the server</span>}. The '0 MB / 500 MB' half is CORRECT: :437 renders formatQuotaSize(state.storage.used) / formatQuotaSize(state.storage.total) and the FREE tier is 500 MB (lib/constants/plan-limits.ts:32 storageMB: 500, served through server/services/media.service.ts:491).", "edit": "Delete the '⚠ This device only' pill from 1162:4617 (node 1162:4826) entirely — an empty library cannot show it. On 1163:4641 and 1163:13695 replace the pill text with '⚠ 2 not on the server'. Leave the '{used} / {total}' halves alone."}
{"id": "COVER-1-22", "module": "media", "action": "mark-unimplemented", "severity": "Major", "confidence": "measured", "target": "1776:8372 / 1162:4617 1163:4641 1163:13695  (fullpage boards) — the Trash rows 1162:4694, 1163:4718, 1163:13772", "claim": "The folder column ends with a '🗑 Trash' row, drawn identically to the folder rows above it, so deleted assets are recoverable from a trash view.", "truth": "Clicking Trash opens nothing. Its entire handler is a toast reading 'Trash coming soon'. There is no trash view, no soft-delete and no restore — ConfirmDeleteModal's copy says deletion breaks the elements that use the file, which is the opposite of a recycle bin.", "evidence": "LibraryManager.tsx:356-358 onTrashClick={() => addToast({ description: \"Trash coming soon\", tone: \"info\" })}. The row is rendered by FolderTree with that handler as its only action. Deletion is immediate — ConfirmDeleteModal.tsx:71-72 warns '⚠ {n} file(s) is/are currently used on the canvas. Deleting will break those elements.'", "edit": "On all three fullpage boards rename the Trash row's frame to 'NOT IMPLEMENTED — Trash toasts \"Trash coming soon\"; there is no soft-delete, no trash view and no restore (LibraryManager.tsx:356)'. Keep the row drawn. Do NOT restyle it as disabled — the shipped row looks live, and the board should show what the user sees."}
{"id": "COVER-1-23", "module": "media", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8372 / 1716:8391  (Media · stock · photos · results) — attribution nodes 1716:8437 8440 8443 8446 8449 8452", "claim": "The six result tiles carry mixed provider credits — 'Ana Ruiz · Unsplash', 'J. Lin · Pexels', 'M. Okoye · Unsplash', 'S. Patel · Pixabay', 'T. Grant · Unsplash', 'K. Mori · Pexels' — i.e. one search returns results from three providers, matched by the three provider pills above (1716:8406/8408/8410).", "truth": "A photo search can only ever return Unsplash. The server photo path reads UNSPLASH_ACCESS_KEY and hardcodes source: \"unsplash\" on every row; Pexels is the VIDEO provider and Pixabay has no server implementation at all. FIG-E-13 already established that the provider pills are unreachable dead wiring; this row is the consequence one board further on — the pills cannot switch provider, so the results cannot be mixed, and a single search cannot produce a Pexels or a Pixabay credit on a photo tile.", "evidence": "server/services/stock.service.ts:20 the photo result type pins source: \"unsplash\"; :55 const key = process.env.UNSPLASH_ACCESS_KEY; :29 the video type pins source: \"pexels\" with its key at :102. server/trpc/routers/media.ts:301-308 — the searchStockPhotos input schema is { query, page, orientation, color }: there is no source parameter to send. packages/editor/src/services/stock/StockService.ts:31 declares extras.source ('Provider override; defaults to \"unsplash\" when unset') and :74-77 never forwards it. Existing row: FIG-E-13 (fix-board, Major, target 1716:8557).", "edit": "On 1716:8391 change all six attribution strings to '· Unsplash' (keeping the six author names), and mark the provider pill group 1716:8406/8408/8410 the way FIG-E-13 marks it on 1716:8557 — one marker, one spelling, both boards. Leave the Photos/Videos/Icons/Fonts tab row alone; those four tabs are real (StockSourceModal.tsx:44-49)."}
{"id": "COVER-1-24", "module": "media", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8372 / 1716:8453  (Media · stock · searching) — orientation row 1716:8475-8481", "claim": "The searching board shows the Photos/Videos/Icons/Fonts tabs above an orientation filter (All / Landscape / Portrait / Squarish) and provider pills, implying the filter applies to whichever tab is active.", "truth": "Orientation applies to photos only, and is silently discarded for videos. The editor's searchVideos signature accepts an orientation argument, names it _orientation to mark it unused, and sends only { query, page }; the server's video input schema has no orientation field either. So on the Videos tab the four orientation buttons are live controls that change nothing. Icons and Fonts are not stock searches at all — both are fed by hardcoded four-item arrays in the engine.", "evidence": "packages/editor/src/services/stock/StockService.ts:84-88 async searchVideos(query, page, _orientation, extras) { … client().media.searchStockVideos.query({ query, page }, …) }. server/trpc/routers/media.ts:313-314 .input(z.object({ query, page })) — no orientation, no color. Icons/Fonts: engine/media/MediaManager.ts:1571-1583 getIcons() and :1588-1600 getFonts() are hardcoded four-item arrays; StockSourceModal.tsx:312 offers 'Browse full icon library' as the way out.", "edit": "On 1716:8453 add a note beside the orientation row: 'Photos only — the Videos request carries neither orientation nor colour (StockService.ts:84; media.ts:313).' On 1716:8541 (stock · icons · empty) rename the frame to lead with '[not-implemented] — the Icons and Fonts tabs are not stock providers; both read hardcoded four-item arrays (MediaManager.ts:1571, :1588)'; its 'No icons found.' copy is verbatim correct and should stay."}
{"id": "COVER-1-25", "module": "media", "action": "add-state", "severity": "Major", "confidence": "measured", "target": "1776:8372 / 1174:4849  (Media · modal · replace-across · result-states)", "claim": "The result strip draws three arms: 'Replacing…' (1174:4851, already marked [no-producer]), a clean result 'Replaced 3 uses ✓' + Done (1174:4855-4856), and a partial result '2 replaced, 1 failed.' + Failed elements (1) + Close/Retry failed (1174:4858-4862).", "truth": "There is a fourth arm and it is the one with the surprising behaviour: total failure. When every update fails the operation ROLLS BACK and commits nothing, and the copy says so — a materially different outcome from the partial case, which does commit what succeeded. It also offers Close only, with no Retry, because there is nothing to retry against.", "evidence": "engine/media/MediaCommandLayer.ts:49-53 ReplaceAcrossResult { replaced, failed: Array<{elementId, error}>, clean: boolean }; :280-287 and :414-418 the all-failed rollback (reason: \"all-failed\"). ReplaceAcrossDialog.tsx ResultView :258-347 — clean arm :273/:286 'Replaced {n} use(s) ✓' + 'Done'; total-failure arm :296-311 'Nothing replaced — all {n} update(s) failed. No changes committed.' + 'Show errors ({n})' + 'Close'; partial arm :322-344 '{replaced} replaced, {failed} failed.' + 'Failed elements ({n})' + 'Close' / 'Retry failed'.", "edit": "Add a fourth arm to 1174:4849, cloned from the partial arm: title 'Nothing replaced — all 3 updates failed. No changes committed.', a collapsed 'Show errors (3)' disclosure, and a single 'Close' button with no Retry. Widen the frame from 948 to fit it."}
{"id": "COVER-1-26", "module": "media", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8372 / 1163:4641  (Media · fullpage · list-view · bulk-select) — details column VERSIONS block 1163:4819-4827", "claim": "The details rail shows a VERSIONS list holding 'hero-kitchen_v4127 · current' and 'hero-kitchen_v2210 · Aug 2' — the same server-backed version history the 280 drill-in draws on 146:32.", "truth": "They are two different mechanisms and only one is real version history. The drawer's drill-in reads MediaAssetVersion rows from the server. The FULLPAGE rail has no such call: it derives its list by string-matching sibling assets whose filenames share a base after stripping a `_v<digits>` suffix — so the two rows on this board are two separate uploaded files that happen to be named alike, not two versions of one asset. The drawer deliberately removed that heuristic; the fullpage manager still runs it.", "evidence": "Fullpage heuristic: LibraryManager.tsx:141-151 — const baseName = selectedItem.name.replace(/_v\\d+$/, \"\"); then filters all items whose stripped name equals baseName and whose type matches, sorted by createdAt. Real versions: prisma/schema.prisma:1066-1078 model MediaAssetVersion; packages/editor/src/services/MediaVersionService.ts:34 listAssetVersions, :49 restoreAssetVersion; consumed by AssetDetailOverlay.tsx:462-540, whose header note at :14-16 records that the filename heuristic was removed from the drawer.", "edit": "On 1163:4641 rename the VERSIONS block frame (1163:4819) to 'VERSIONS — filename heuristic, not the version table: the fullpage rail groups siblings by a _v<n> filename suffix (LibraryManager.tsx:141-151), while the drawer drill-in reads MediaAssetVersion rows (MediaVersionService.ts:34)'. Do not change the two rows; they are a fair drawing of what the heuristic produces."}
{"id": "COVER-1-27", "module": "media", "action": "none", "severity": "Minor", "confidence": "measured", "target": "1776:8372 / 146:68 1704:8396 453:3931 782:4353 1205:4816 1716:8541 1164:4738 147:2  (used-in / versions·empty / load-error / no-results / import-url·invalid / stock·icons·empty / replace-across-site / icon-picker)", "claim": "Coverage record: eight boards opened and found correct against the code.", "truth": "146:68 used-in — the page-grouped hit list with 'Home › hero' crumbs and a 'Jump ›' per hit is exactly AssetDetailOverlay.tsx:541-587 (page header :556-561, 44h hit rows :562-571, Jump at :580, handler :169-177). 1704:8396 — 'No saved versions yet. Edits create restore points automatically.' = AssetDetailOverlay.tsx:535-539 verbatim. 453:3931 — \"Couldn't load your media.\" + 'Try again' + a stock link = SlimLauncher.tsx:312-334 (:323 Try again, :332 Stock). 782:4353 — 'Nothing matches ‘logo dark’.' + 'Clear search' = SlimLauncher.tsx:398-412, and the board's extra strip 'Couldn't reach the rest of your library' (1314:21) is the real search-scope failed arm at SlimLauncher.tsx:215-221 / useLibraryState.ts:379,388. 1205:4816 — 'That is not a web address. It needs to start with http:// or https://.' = ImportUrlModal.tsx:83-91 verbatim, validator fetchUrlAsFile.ts:21-28. 1716:8541 — 'No icons found.' = StockSourceModal.tsx:331 (see COVER-1-24 for the tab it sits in). 1164:4738 — the body sentence, the per-page checkbox list with '{n} use(s)' and the button 'Replace 3 uses on 2 pages' are ReplaceAcrossDialog.tsx:173-177, :196-220, :232-234. 147:2 — the picker matches IconBrowserOverlay.tsx (back row :152, search :160, categories :169-213, RECENT band :219-224) and its frame already carries the correct [not-implemented] marker about the inserted icon rendering as an empty span.", "evidence": "Board strings read live from the file at the nodes named; code lines as cited, from an independent source read. One deliberate non-finding: 147:11 says 'Search 370 icons' and shared/constants/icons.ts holds 369 entries — sample data, not a contract, per the brief's rule that board sample values are never conformed to literally.", "edit": "No change. Recorded so these eight are not re-read."}
{"id": "COVER-1-28", "module": "media", "action": "add-state", "severity": "Major", "confidence": "measured", "target": "1776:8372 / 1162:4617  (Media · fullpage · empty) — clone source", "claim": "1162:4617 is the fullpage library with nothing in it: an empty grid, a 'No images or files yet.' block with Upload and Browse stock, and every folder count at 0.", "truth": "Correct as far as it goes, and the copy is verbatim. What no fullpage board draws is the state directly adjacent to it: a load that is still in flight, and a library whose SMART scope is selected and empty. The fullpage manager has no loading branch of its own — it renders the same empty state while the engine is still initialising — and the Recent / In use / Unused scopes each filter to zero without their own copy, so an 'Unused (0)' scope shows the same 'No images or files yet.' as a genuinely empty library.", "evidence": "Empty copy verbatim: the board's 1162:4853/4854 = SlimLauncher.tsx:382 'No images or files yet.' and the fullpage grid's own equivalent. Smart scopes: FolderTree.tsx:31 SmartFolder = null | \"recent\" | \"in-use\" | \"unused\"; rows :289-318; the filter at LibraryManager.tsx:103-116 with unused at :112-114 items.filter(i => (usageMap.get(i.key) ?? 0) === 0). grep for a fullpage loading branch: LibraryManager.tsx has no libraryLoading gate — the drawer's skeleton lives at SlimLauncher.tsx:336-358 and is not reused here.", "edit": "Clone 1162:4617 twice: 'Media · fullpage · loading' with the grid replaced by the six-cell skeleton the drawer uses, and 'Media · fullpage · smart-scope · empty' with 'Unused' selected in the SMART group, its count at 0, and the same empty block — which is the point of the board: the product cannot tell the two apart."}
{"id": "COVER-1-31", "module": "layers", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8375 / 775:4130  (Layers · loading) — count footer 776:4094", "claim": "The loading board draws an empty tree body under the toolbar, with the count footer still reading '12 layers' (776:4094).", "truth": "During loading the footer reads '0 layers'. The footer is a SIBLING of the tree body, not a child of it, so it renders throughout the load — but its number comes from a state seeded at zero and only ever written by an event the tree emits, and the tree is not mounted while loading. The source even records this as the bug that produced the skeleton: the earlier code 'showed an empty tree and a footer reading \"0 layers\" for the whole load'. The skeleton was added; the footer was not addressed, so '0 layers' is still what a user sees beside it.", "evidence": "LayersTab.tsx:113 const [stats, setStats] = React.useState<{total: number; selected: number}>({ total: 0, selected: 0 }); :115-127 the only writer is the composer event 'layers:stats-change', emitted by LayersPanel; :194-209 LayersPanel mounts only when composer && !projectLoading, else :209 <LayersLoadingSkeleton />; :210-221 the footer renders whenever !treeFailed, outside that ternary. The comment at :186-193 names the original defect verbatim.", "edit": "On 775:4130 change the count footer 776:4094 from '12 layers' to '0 layers'. Also replace the empty tree area with the shipped skeleton — seven rows on the tree's row height with an indent ladder (LayersStateBlocks.tsx:12 cites this very board, 775:4130, as its spec). Text-only read: I could not see whether skeleton rectangles are already present as non-text nodes, so verify the second half by eye before acting on it."}
{"id": "COVER-1-32", "module": "layers", "action": "none", "severity": "Minor", "confidence": "measured", "target": "1776:8375 / 781:4217 143:295 782:4260 1082:4739  (load-error / multi-select / no-results / component-instance)", "claim": "Coverage record: four Layers boards opened and found correct.", "truth": "781:4217 — 'Couldn't load the layer tree.' / 'The page is fine — only this list failed.' / 'Try again' = LayersStateBlocks.tsx:60, :61, :70 verbatim, and the board correctly OMITS the count footer, which is exactly what LayersTab.tsx:210 does ({!treeFailed && …}) with the reason recorded at :212-214: 'a count of a list that did not load is not a fact'. 143:295 — footer '3 selected of 12' = LayersTab.tsx:218-219 (stats.selected >= 2 ? `${selected} selected of ${total}`). 782:4260 — 'Nothing matches ‘hero’.' + 'Clear search'. 1082:4739 — the ◇ instance glyph on three rows, and the frame already carries the correct NOT A STATE marker naming LayerTreeItem.tsx:115,256-264.", "evidence": "Board strings read live; code lines as cited. Two observations recorded rather than filed: (a) 1082:4640 and 1082:4739 use a different panel-header component (a 'Layers / ↻ / ✕' instance) from the rest of the family, which draws 'Panel header/INSERT :: Layers' — that is D-R-13's 'the panel header is five different components', already filed. (b) The tree-row component's own name records the missing depth axis ('Has NO depth axis, while the product indents at 12 + depth×16'), so the indentation gap is already annotated in the file.", "edit": "No change. Recorded so these four are not re-read."}
{"id": "COVER-1-33", "module": "review", "action": "add-state", "severity": "Critical", "confidence": "read", "target": "1776:8383 / 1705:8494  (Review · comment-update-failed) — clone source; the same gap is on every Review board", "claim": "'Couldn't update that comment. Try again.' (1705:8564) is drawn as a transient network failure, in grey, in the progress band.", "truth": "It is also what a VIEWER sees, every time, for a reason that is not a network failure. The panel renders Resolve, Reopen, Reattach, the reply composer, Send, Re-send and Revoke live and unexplained for a VIEWER; each fails server-side with FORBIDDEN, and the resolve failure surfaces as this exact grey line with no role, no reason and no retry that could ever succeed. Exactly one control in the panel is gated with a reason, and it is Send-for-review.", "evidence": "ReviewTab.tsx:146 isViewer is defined and referenced at only two call sites, :479 and :885, both passing disabledReason=\"Viewers can't send for review — ask an editor\" to SendForReview. The ungated controls: Resolve/Reopen :558-562, Reattach :712-719, composer + Send :815-830, Re-send :859-873, Revoke :903-912. Server gates: comments.resolve and comments.reattach require EDITOR (server/trpc/routers/comments.ts:91, :74); reviews.revoke and reviews.submit require EDITOR (reviews.ts:205, :64). Ranks server/services/permission.service.ts:4-11 VIEWER 0 / EDITOR 1 / DESIGNER 1 / ADMIN 2 / OWNER 3, failing with PermissionError('FORBIDDEN','Insufficient permissions') at :127. The catch that produces the drawn string: ReviewTab.tsx:264-266. Related: FIG-N-11 already asks for a VIEWER board; this row adds the specific defect the board must show — the failure is reported as a comment-update error.", "edit": "Clone 1705:8494 into 'Review · viewer · forbidden' and draw what the code actually does: the full control set enabled, Resolve just clicked, and this same grey line 'Couldn't update that comment. Try again.' — then caption it 'A VIEWER's FORBIDDEN is reported as a comment-update failure. Only Send-for-review carries a reason (ReviewTab.tsx:479); the other six controls are ungated (:558, :712, :827, :859, :903).' Pairs with FIG-N-11."}
{"id": "COVER-1-34", "module": "review", "action": "none", "severity": "Minor", "confidence": "measured", "target": "1776:8383 / 1705:8636 1705:8566 1717:17235 1717:17246 161:3 161:5 161:7 161:9 161:10 161:12 186:12 453:4005  (round-history · load-error, reply-failed, compare · load-error, compare · loading, and eight captions)", "claim": "Coverage record: twelve Review nodes opened and found correct.", "truth": "1705:8636 — 'Couldn't load the history.' + 'Try again' = ReviewTab.tsx:780-786; the strip label 'Round 3 of 3 ▾' = :775-776; and the round-history load IS independent of the panel's own load (load() at :186-196 never calls fetchRounds; rounds are lazy-loaded at :178-184), which is what justifies the separate board. 1705:8566 — 'Couldn't send that reply. Try again.' = :824, placeholder 'Reply to the client…' = :820. 1717:17246 — 'Loading approved snapshot…' = :506-507. 1717:17235 — \"Couldn't load the approved snapshot\" / 'The dashboard didn't answer. Try again.' / 'Retry' = :512-514, and the frame name already records the 280/700 mount geometry correctly. Captions: 161:3 all-resolved matches :602-609; 161:7 re-send-confirm matches :841-846 including 'the old one stops working immediately'; 161:9 revoke-confirm matches :658-663; 161:10 revoked matches :591-596; 161:12 empty matches :632-634; 161:5 resolved-expanded matches the collapsible group at :740-760 defaulting closed (:148); 186:12 reattach 'Offered, not forced' matches the pick-mode round trip (CommentLayer.tsx:171-177, :348-369); 453:4005 matches the load-error branch at :434-443.", "evidence": "Board strings read live from the file; code lines from an independent source read. Instrument note: my reads are TEXT-ONLY, so on 1138:4527 (Review panel · loading) I saw a panel header and no text, which is consistent with the code's five SkeletonBlock rows (ReviewTab.tsx:354-364) but is NOT evidence either way — skeleton rectangles carry no characters. I make no claim about that board and it should not be counted as verified. FIG-N-26 already files it as none.", "edit": "No change. Recorded so these twelve are not re-read."}
{"id": "COVER-1-35", "module": "review", "action": "add-state", "severity": "Major", "confidence": "measured", "target": "1776:8383 / 1717:17235  (Review · compare · load-error) — clone source", "claim": "Compare has exactly two non-happy states, a loading board and a load-error board, so a failure to build the comparison is covered.", "truth": "There is a third outcome and it is silent. Compare loads two sides: the approved snapshot, whose failure produces the drawn error, and the CURRENT pages, whose failure is swallowed into an empty array. An export failure therefore renders as a comparison in which the current side is simply blank — no error, no retry, and nothing saying the current side failed rather than being empty.", "evidence": "ReviewTab.tsx:310-326 openCompare; :317 the current-side export result falls back to [] on failure; :524 renders that array directly. The approved side's failure path is the one that sets compareState to the error branch at :508-515. The compare button itself is disabled when !onExportCurrentPages with the title 'Compare isn't available here' (:416).", "edit": "Clone 1717:17235 into 'Review · compare · current-side-empty': the same toolbar, the approved side rendering, and the current side blank with no error and no retry. Caption it 'Not an error state — the current side's export failure is swallowed into [] (ReviewTab.tsx:317) and renders as an empty panel.'"}
{"id": "COVER-1-36", "module": "sign-off", "action": "add-state", "severity": "Critical", "confidence": "measured", "target": "1776:8384 / 1339:7214  (Client sign-off · F · dead-link · not-found) — the board a network failure is routed to", "claim": "'This link doesn't work / It may have been copied incompletely. Try clicking the link in your email again rather than pasting it.' is the screen for a token that does not exist.", "truth": "It is also the screen for every transport failure. The dead-link dispatcher reads a reason off the error and falls back to NOT_FOUND when there is none — and a network error, a 500, or an unreachable dashboard carries no reason. So a client whose wifi drops is told their designer sent them a broken link, and the advice offered ('try clicking the link in your email again rather than pasting it') is advice they cannot act on. There is no offline branch anywhere on the page.", "evidence": "packages/dashboard/app/review/[token]/review-client.tsx:165-167 — the reason is read from error.data.cause.reason, then the code, then `?? DEAD_LINK_COPY.NOT_FOUND`; :25-28 the NOT_FOUND copy. The three real throws are the only reasons that can ever be present: client-review.service.ts:119 INVALID_TOKEN, :121 REVOKED, :124 EXPIRED. grep for navigator.onLine / isOffline across the page returns nothing. Second, smaller instance of the same shape: comments.error is never read (the notes list at :356 gates on comments.data && length > 0), so a failed notes fetch renders as 'you have no notes'. Related but different: FIG-N-33 files the canonical page's own S5.5 · load-error board as mark-unimplemented.", "edit": "Clone 1339:7214 into 'Client sign-off · F · unreachable' drawing what the client is shown today — the NOT_FOUND screen — and rename it to lead with 'MISROUTED — a network failure lands here: the dispatcher falls back to NOT_FOUND when the error carries no reason (review-client.tsx:167)'. Keep 1339:7214 itself as the genuine bad-token screen. Pairs with FIG-N-33."}
{"id": "COVER-1-37", "module": "sign-off", "action": "add-state", "severity": "Major", "confidence": "measured", "target": "1776:8384 / 1339:7162  (Client sign-off · A0 · identify) — the loading state that precedes it", "claim": "Section 19 opens on A0 · identify; FIG-N-30 has already established that no loading board exists.", "truth": "Filed here because the missing board hides a visible defect, not just a gap. The loading screen renders the page Shell with the agency name hardcoded to the literal string 'Loading', and the Shell's header template reads '{agency} is asking for your feedback' — so for the whole of the load the client is greeted by 'Loading is asking for your feedback'. A loading board drawn from the code would show that sentence; drawn from imagination it would not.", "evidence": "packages/dashboard/app/review/[token]/review-client.tsx:146-154 the loading branch, :148 <Shell agency=\"Loading\"> with the body 'Opening your review…'; the header template at :91 '{agency} is asking for your feedback'. The real fallback for a missing agency name is a different string, :181 data.agencyName ?? 'Your design team'. Existing row: FIG-N-30 (add-state, Critical) asks for the loading board without naming this.", "edit": "When FIG-N-30's loading board is drawn, draw the header as 'Loading is asking for your feedback' and caption it 'The agency slot is hardcoded to the literal \"Loading\" (review-client.tsx:148); the page has a real fallback, \"Your design team\" (:181), and does not use it here.' Do not invent a corrected header — the board should show the defect."}
{"id": "COVER-1-41", "module": "inspector", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8381 / 161:15  (caption/Inspector · multi-select) — captions board 159:123", "claim": "The caption opens 'Header reads the count; sections give way to align/distribute (6 + 2) and the shared-property subset.'", "truth": "There is no header in multi-select. The inspector short-circuits BEFORE the header, before the scope/breakpoint/state pill row and before every banner, returning the MultiSelectToolbar alone — and the count '{n} selected' lives inside that toolbar, not in a header. The rest of the caption is right: six align actions, two distribute actions, and a five-field shared-property panel.", "evidence": "ProInspector.tsx:298-310 — const hasMultipleSelected = selectedIds.length > 1 || isMultiSelect; the branch returns only <MultiSelectToolbar>. Count at MultiSelectToolbar.tsx:214 `{n} selected`; align band :234 with six aria-labels :240-285; distribute band :295 with two (:301, :317); BatchStylePanel :331, whose five fields are Background (:98), Text color (:103), Radius (:108), Padding (:114), Font size (:120).", "edit": "Change 161:15's first clause from 'Header reads the count' to 'The toolbar reads the count — the header, the pill row and every banner are short-circuited (ProInspector.tsx:298)'. Name the five shared fields, since 'the shared-property subset' is exactly five and the board can show them."}
{"id": "COVER-1-42", "module": "inspector", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8381 / 161:18  (caption/Inspector · breakpoint-override) — captions board 160:208", "claim": "'Context-bar dot, plus a cobalt left bar and ↺ on every overridden control, and the Base value named. Overrides you cannot see are overrides you cannot undo.'", "truth": "'Every overridden control' is the part that is not true, and the caption's own closing sentence is the reason it matters. The per-control override dot is passed by exactly SEVEN controls in the whole inspector — six in Size and one in Position. Every other section — Typography, Flexbox, Grid, Spacing, Border, Background, Effects — ignores the flag, so an override on any of them is invisible at the control. The left bar, the ↺ and the 'Base is …' line belong to the separate BreakpointOverrides list, which renders one row per override in its own block; they are not decorations on the live controls.", "evidence": "Per-control dot: shared/controls/InputControls.tsx:15-17 OverrideDot, rendered at :68/:238/:360 on isOverridden; CSS styles/inspector.css:917-925. The only call sites passing isOverridden: sections/SizeSection.tsx:189, :222, :253, :269, :290, :306 and sections/layout/PositionControls.tsx:203. The override LIST: components/BreakpointOverrides.tsx:96 the left accent bar, :108-119 the revert control, :124 'Overridden on {breakpointName} — Base is {base[property] || \"not set\"}'; mounted ProInspector.tsx:501-505; :40 short-circuits on desktop. The context-bar dot: components/BreakpointPill.tsx:76-86, fed ProInspector.tsx:170.", "edit": "Rewrite 161:18 to distinguish the two mechanisms: 'A dot on the breakpoint pill, and a separate Overrides block listing each overridden property with a cobalt left bar, a ↺ and the Base value. On the controls themselves the dot reaches only Size and Position — seven controls in total (InputControls.tsx:15; SizeSection.tsx:189-306; PositionControls.tsx:203). An override in Typography, Spacing, Border, Background, Flexbox, Grid or Effects is invisible where you edit it.'"}
{"id": "COVER-1-43", "module": "inspector", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8381 / 161:19  (caption/Inspector · pseudo-state) — captions board 160:313", "claim": "'Same treatment, and the column header tints — the signal that you are not editing Base.'", "truth": "Not the same treatment, and there is no header tint. Pseudo-states get three marks and none of them is on a control: a 5px dot on the state pill, a 5px dot beside each option in the dropdown that has overrides, and a banner reading 'Editing :hover — not Base'. The left accent bar, the ↺ revert and the 'Base is …' line the caption borrows from breakpoints are breakpoint-only — BreakpointOverrides reads getBreakpointStyle and knows nothing about pseudo-states. So a hover override cannot be reverted per property and its Base value is never named.", "evidence": "components/StateDropdown.tsx:27 ALL_STATES = ['normal','hover','focus','active','disabled']; labels :19-25 (Base / :hover / :focus / :active / :disabled); pill dot :65-70; per-option dot :120-125 fed by ProInspector.tsx:175-179 computeStatesWithOverrides. Banner ProInspector.tsx:489-499, copy :497 `Editing {pseudoStateLabel(currentPseudoState)} — not Base`. Writes: hooks/useStyleHandlers.ts:234-238 composer.styles.setRule(sel, {...}, { pseudo: `:${currentPseudoState}`, mediaQuery: mq }). BreakpointOverrides.tsx:41 reads composer.styles.getBreakpointStyle(elementId, breakpoint) only.", "edit": "Rewrite 161:19: 'A dot on the state pill, a dot beside each state in the dropdown that carries overrides, and a banner reading \"Editing :hover — not Base\". No column tint, and none of the breakpoint treatment: a pseudo override has no left bar, no ↺ and no named Base value, because BreakpointOverrides reads getBreakpointStyle only (BreakpointOverrides.tsx:41).'"}
{"id": "COVER-1-44", "module": "inspector", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8381 / 189:104  (caption/Inspector · reach-whole-site) — captions board 189:2", "claim": "'ReachScopeStrip routes it to the Styles tab rather than editing in place, so this frame documents a hand-off, not an edit.'", "truth": "Both names are wrong. ReachScopeStrip was deleted — the source file that replaced it says so in its own header — and the component is ScopeDropdown. And the hand-off does not go to a Styles tab: the whole-site takeover's button is 'Open Brand' and it emits ui:switch-tab with tab \"design\". The dropdown's own option subtitle still reads 'colors & fonts — Styles tab', so the board is repeating a stale string the code also carries; the button is the thing that runs.", "evidence": "components/ScopeDropdown.tsx:16 — 'The three-card `ReachScopeStrip` it replaced ate ~150px of vertical space'; repo-wide grep for ReachScopeStrip returns that comment and nothing else. Whole-site takeover ProInspector.tsx:518-537: :523 'Editing the whole site — every page', :526-527 'Site-wide colours, fonts and spacing live in the Brand panel — change them once, everywhere updates.', :531 'Open Brand' → :530 composer?.emit(\"ui:switch-tab\", { tab: \"design\" }), :534 'Back to this element'. The stale option subtitle: ScopeDropdown.tsx:174 'colors & fonts — Styles tab'.", "edit": "In 189:104 replace 'ReachScopeStrip' with 'ScopeDropdown' and 'the Styles tab' with 'the Brand panel', and add the note that the dropdown's own subtitle still says 'Styles tab' while its button opens Brand (ScopeDropdown.tsx:174 vs ProInspector.tsx:530). The rest of the caption — the third reach, no count, a hand-off not an edit — is correct."}
{"id": "COVER-1-45", "module": "inspector", "action": "fix-board", "severity": "Critical", "confidence": "read", "target": "1776:8381 / 161:21  (caption/Inspector · ai-agent-run) — captions board 160:512", "claim": "'The AI panel borrows the 300 column with a ‹ Inspector back row. The live selection is preserved and restored on return — losing it would make the panel cost more than it saves.'", "truth": "Two problems, and the second is the board's whole premise. (1) The takeover renders no back row: it is an 'AI' label, the running step's summary, and the line 'Your selection is kept and restored when you go back.' (2) That line is a promise the product does not keep — the conformance manifest marks this very board unreachable BECAUSE entering the AI tab clears the selection, and with no selected element the inspector never reaches the takeover at all. The caption states the reassurance as fact; the file's own manifest states the opposite.", "evidence": "ProInspector.tsx:510-517 is the whole takeover — data-testid=\"inspector-ai-run\" (:511), 'AI' (:512), {agentRun.summary || \"Working…\"} (:513), 'Your selection is kept and restored when you go back.' (:515). No back control. Producer: sidebar/tabs/ai/hooks/useAgentRunner.ts:153 composer.emit(\"ai:agent-run\", { running: true, summary: … }) with running:false at :136,:226,:259,:269,:299,:307 — and \"ai:agent-run\" is a raw string on both sides, absent from shared/constants/events.ts (whose AI_* constants at :413-416 and :628-636 are referenced nowhere in the AI panel). Board 160:512 carries the conformance-manifest marker `unreachable-selection-cleared-on-ai-tab-entry`.", "edit": "Rewrite 161:21 to say what the takeover renders — an 'AI' label, the running step summary, and the reassurance line — and to carry the contradiction rather than the promise: 'The panel says the selection is kept (ProInspector.tsx:515). Entering the AI tab clears it, which is why this board is marked unreachable — so the state the reassurance was written for is the one that cannot occur.' Do not delete the board."}
{"id": "COVER-1-46", "module": "inspector", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8381 / 161:22  (caption/Inspector · loading) — captions board 159:102", "claim": "'Header renders, sections skeleton. The header first, so the column has an identity before it has content.'", "truth": "The header does not render — a 48px skeleton BAR stands where it will be, and the real header is not mounted. More consequentially, the caption omits the condition: the loading state is gated on there being NO selected element. Open a project with something already selected and the inspector never shows a loading state at all; it renders the profile against whatever the engine has so far.", "evidence": "components/InspectorLoading.tsx — a 48px header bar plus ROWS = 6 (:27), data-testid=\"inspector-loading\" (:36), aria-label=\"Loading\" (:35), SkeletonBlock at :39/:46/:47. The branch: ProInspector.tsx:315-317 `if (projectLoading && !selectedElement) return <InspectorLoading />`. Loading source ProInspector.tsx:210 useProjectLoading(composer) → shell/hooks/useProjectLoading.ts:22 composer?.isProjectLoading(), :31 composer.on(EVENTS.PROJECT_LOAD_STATE, onChange); constant shared/constants/events.ts:565; producer engine/Composer.ts:892.", "edit": "Rewrite 161:22: 'A 48px header bar and six skeleton rows — the real header is not mounted yet (InspectorLoading.tsx:27). Only reached with nothing selected: ProInspector.tsx:315 gates on projectLoading && !selectedElement, so a load that begins with a selection shows the profile, never this.'"}
{"id": "COVER-1-47", "module": "inspector", "action": "add-state", "severity": "Critical", "confidence": "measured", "target": "1776:8381 / 159:123  (Inspector · multi-select) — the board the dead badge was drawn for", "claim": "The inspector marks values that differ across a multi-selection with a 'Mixed' badge, drawn on the multi-select board.", "truth": "The badge cannot render. MixedValueBadge is wired into roughly sixty controls and fed by a mixed-key set that detectMixedValues returns EMPTY for anything under two elements — so it is non-empty only at 2+ selected, and at 2+ selected ProInspector short-circuits to MultiSelectToolbar before InspectorTabContent, which is the only thing that consumes mixedKeys, is ever rendered. The one live 'Mixed' convention in the product is a placeholder string on five BatchStylePanel fields, which is a different component with different copy. This will read as implemented from any board-versus-code diff; it is not.", "evidence": "shared/MixedValueBadge.tsx:51 the literal 'Mixed', :20 title='Mixed — values differ across selected elements', :21 aria-label='Mixed value'. Fed ProInspector.tsx:244-247 → shared/detectMixedValues.ts:10 `if (elements.length < 2) return new Set();` → tabs/InspectorTabContent.tsx:211 mixedKeys, :212 isMultiSelect; consumed at sections/typography/FontControls.tsx:145, sections/flexbox/AlignmentSection.tsx:50, sections/layout/PositionControls.tsx:64, sections/EffectsSection.tsx:212 and ~56 more. The short-circuit: ProInspector.tsx:298-310. The live convention instead: BatchStylePanel.tsx:89-93 placeholder 'Mixed' on Background/Text color/Radius/Padding/Font size, footer :127-128 'Editing a Mixed field applies it to all {n}.'", "edit": "Add a caption to 159:123 recording the dead apparatus so nobody rebuilds a board around it: 'MixedValueBadge is unreachable — detectMixedValues returns a set only at 2+ elements (detectMixedValues.ts:10) and at 2+ the inspector returns MultiSelectToolbar before InspectorTabContent renders (ProInspector.tsx:299). The Mixed the user sees is a placeholder on five BatchStylePanel fields (BatchStylePanel.tsx:89).' Draw the five fields with 'Mixed' placeholders on the board; do not draw the badge."}
{"id": "COVER-1-48", "module": "inspector", "action": "none", "severity": "Minor", "confidence": "measured", "target": "1776:8381 / 161:13 161:16 161:17 161:20 429:2524  (captions: no-selection, instance-selected, bound-to-CMS, reach-all-like-this, animation editor)", "claim": "Coverage record: five of the eleven unread Inspector captions are correct.", "truth": "161:17 bound-to-CMS is the strongest of the five and is right in every clause — the Lucide-link 'Bound' chip in the header (ProInspector.tsx:390-398), the banner naming collection.field with Unbind (BindingBanner.tsx:75, :87, label built :33-37), nothing below made read-only, and the engine wiring is real (BaseBindingManager.ts:100/:124/:131/:140, live-refresh on binding:created / binding:removed, events.ts:192-193). 161:20 reach-all-like-this is right about the three reaches and the peer count (ScopeDropdown.tsx:142/:156/:173, count built :70) and about the fan-out being per-property (useStyleHandlers.ts:255-266 writeOne over reachPeerIds). 161:13 no-selection matches InspectorEmptyState.tsx:84. 161:16 instance-selected matches VariantSection.tsx:114-152. 429:2524 is verified against both files it cites — the panel does print a second, shorter CSS string above the written one (AnimationEditor.tsx:205-211).", "evidence": "Caption text read live from the file; code lines from an independent source read. Three additions recorded rather than filed as defects: (a) 161:13 omits the '✦ Ask AI ›' link the empty state renders (InspectorEmptyState.tsx:89-93). (b) 161:17 omits that the binding banner is hidden during BOTH the whole-site and ai-agent-run takeovers (ProInspector.tsx:454). (c) board 429:2350's own trigger row — if it draws one — is stale: the trigger controls were deleted 2026-05-18 because the engine never honoured them (AnimationEditor.tsx:184-188); I did not open 429:2350 and make no claim about it.", "edit": "No change. Recorded so these five are not re-read, and so lead (c) is checked by whoever opens 429:2350."}
{"id": "COVER-1-51", "module": "ai", "action": "fix-board", "severity": "Critical", "confidence": "measured", "target": "1776:8380 / 172:38  (caption/AI · step-gate) — captions board 170:97", "claim": "'The run PAUSES before anything destructive and waits rather than guessing. This is the line between an assistant and something that edits a client's site unattended.'", "truth": "The line is a checkbox. The panel offers 'Auto-apply steps (skip per-step approval)' directly above the plan, and with it on the runner takes the branch that applies each step without ever setting a step to 'awaiting' — so the gate the caption calls the line between an assistant and an unattended editor never appears. The source names the conflict itself. A separate, genuinely non-defeatable gate exists for privileged actions such as Publish, and it is a different mechanism.", "evidence": "AgentPlan.tsx:134-135 the 'Auto-apply steps (skip per-step approval)' control; backing flag hooks/useAgentRunner.ts:79 (default false) with the two branches at :172 and :174-189; the gating step transition that the auto path skips is :191 setStep(i, { status: \"awaiting\", edit }) with no advance(). The gate UI itself: AgentPlan.tsx:189-207, role=\"alertdialog\" aria-label=\"Step needs approval\" (:190), 'Skip' (:199) / 'Approve' (:202), footnote 'The run waits rather than guessing.' (:206) — the sentence the caption quotes. The conflict is acknowledged at AgentPlan.tsx:36-39. The non-defeatable gate: hooks/useAiActionGate.ts:14 'gate is ALWAYS an explicit dialog — never folded into agent auto-apply'.", "edit": "Rewrite 172:38 to keep the principle and name the escape hatch: '… waits rather than guessing (AgentPlan.tsx:206). One checkbox turns it off — \"Auto-apply steps (skip per-step approval)\" (AgentPlan.tsx:134; useAgentRunner.ts:79), after which no step is ever set to awaiting. Only privileged actions keep an undefeatable dialog (useAiActionGate.ts:14).' Then draw the auto-apply checkbox on 170:97 — no AI board shows it, and it is the control that decides whether this screen exists."}
{"id": "COVER-1-52", "module": "ai", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8380 / 172:36  (caption/AI · planning) — captions board 170:41", "claim": "'The plan is shown BEFORE anything runs, and it is editable. An agent that plans invisibly cannot be corrected.'", "truth": "The first half is true; the second is not. The plan is shown before the run, and there is no way to change it. The steps array is written only by the runner — seeded when the plan returns, patched per step during the run, and cleared on reset — and AgentPlan renders it read-only. The corrections available are Skip and Approve at each gate, Stop, and Retry after a failure; none of them edits a step.", "evidence": "hooks/useAgentRunner.ts:75 const [steps, setSteps] = React.useState<RunStep[]>([]); the only writers are :131 (per-step patch from setStep), :222 (failure), :244 and :308 (clear), :264 (seed from the returned plan). Nothing exported from the hook allows a caller to edit a step, and grep for editPlan / onEditStep / contentEditable / 'Edit plan' across AgentPlan.tsx and useAgentRunner.ts returns nothing. The real correction affordances: AgentPlan.tsx:199 'Skip', :202 'Approve', :184 'Stop', :235 'Retry'.", "edit": "Change 172:36's second clause from 'and it is editable' to 'and it is read-only — the correction is per-step: Approve or Skip at each gate, or Stop (AgentPlan.tsx:199, :202, :184). Nothing edits a step; setSteps is written only by the runner (useAgentRunner.ts:131, :264).' Keep the closing sentence — it is the right argument for building the edit, just not a description of today."}
{"id": "COVER-1-53", "module": "ai", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8380 / 172:37  (caption/AI · running) — captions board 170:70", "claim": "'Step rows 40h with state dots: queued ○ · running ● · done ✓ · skipped ⊘ · failed ✕. Stop is always reachable.'", "truth": "Five of seven. StepStatus has seven members and the caption names five, missing the two that carry the most meaning: 'awaiting', which is the step-gate pause the neighbouring board is entirely about, and 'nochange', a completed step that changed nothing — which has its own word in the panel's own status vocabulary, alongside skipped and failed. A run that ends with three 'no change' steps looks, on this caption's five-dot legend, exactly like a run that applied three.", "evidence": "hooks/useAgentRunner.ts:32-39 export type StepStatus = \"pending\" | \"running\" | \"awaiting\" | \"applied\" | \"skipped\" | \"nochange\" | \"failed\". The panel's own words: AgentPlan.tsx:77-81 STEP_WORD = { skipped: \"skipped\", nochange: \"no change\", failed: \"failed\" }; glyphs :67-75. 'nochange' is set at useAgentRunner.ts:194; 'awaiting' at :191. 'Stop is always reachable' is correct — AgentPlan.tsx:184, shown during running and planning.", "edit": "Extend the legend on 172:37 to all seven: pending ○ · running ● · awaiting (paused, the step-gate) · applied ✓ · skipped ⊘ · no change · failed ✕, using the panel's own STEP_WORD spellings (AgentPlan.tsx:77-81). Add a 'no change' row to board 170:70 — it is the outcome most likely to be misread as success."}
{"id": "COVER-1-54", "module": "ai", "action": "add-state", "severity": "Critical", "confidence": "measured", "target": "1776:8380 / 171:105  (AI · error-quota) and 171:2  (AI · step-failed) — clone sources", "claim": "AI has three failure boards — step-failed, error-quota and not-configured — so every way a run can die is drawn.", "truth": "Four gaps, and the first two are the same defect seen twice. (1) The quota and not-configured branches exist ONLY on the chat path. The agent path collapses every stream error to a bare Error with the message and DROPS the tRPC error code, so errorKind is never computed during a run — an agent run that dies on quota renders as a generic 'step failed', with none of the 'AI is out of credit / Nothing was changed / See plans' treatment. (2) The same is true of the second AI surface, the in-canvas prompt popover, which prints the raw error string with no errorKind routing at all. (3) There is a third top-level failure the boards do not name — a provider failure, whose own source comment calls it 'the third state the boards do not draw'. (4) Planning-failed is a different shape from step-failed: no step number, no 'Nothing after step N ran' line, and Undo all disabled, because nothing applied.", "evidence": "Chat-only classification: hooks/useStreamPrompt.ts:43 errorKind: \"not-configured\" | \"quota\" | \"other\" | null, discriminated :135-142 (err.data?.code; TOO_MANY_REQUESTS → quota, PRECONDITION_FAILED → not-configured). Agent path: hooks/runPromptOnce.ts:102-107 collapses onError to new Error(err.message ?? \"Stream failed\"); errorKind appears nowhere in useAgentRunner.ts or runPromptOnce.ts. Popover: editor/canvas/controls/AiPromptPopover.tsx:83-86 prints {stream.error} raw. Provider failure: AITab.tsx:299-329 — 'The AI service didn't respond.' (:306), 'Nothing was changed. This is usually the model provider, not your site — try again in a moment.' (:308-309), 'Try again' (:327), with the comment at :300-304 saying it is the state the boards do not draw. Planning-failed: useAgentRunner.ts:258 \"Couldn't break that into steps. Try a more specific build request.\", rendered AgentPlan.tsx:213-216 with failedIndex === -1 and Undo all disabled at :229. Server quota message: server/trpc/routers/ai.ts:299-303 `Daily limit reached (${quota.limit}). Resets at ${quota.resetsAt.toISOString()}.`", "edit": "Four boards. 'AI · provider-error' cloned from 171:105 with the amber ground swapped for the neutral one and the three strings above. 'AI · planning-failed' cloned from 171:2 with no step number, the line \"Couldn't break that into steps. Try a more specific build request.\", and Undo all disabled. 'AI · agent · quota-during-run' cloned from 171:2 showing what actually renders — a generic step-failed card — captioned 'the quota treatment is chat-only; runPromptOnce.ts:102 drops err.data.code'. And a caption on 171:136 noting the canvas prompt popover shows neither of these two states (AiPromptPopover.tsx:83)."}
{"id": "COVER-1-55", "module": "ai", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8380 / 172:41  (caption/AI · done) — captions board 171:67", "claim": "'A diff, not a result. Discard and Apply are equally weighted because the run is a proposal.'", "truth": "True of the chat path, false of the agent path, and 'done' is an agent phase. When an agent run reaches done the changes are already applied — the card counts them and offers 'Ask something else', with ⌘Z as the way back. Discard and Apply are the chat message's buttons on a pending diff, which is a different component and a different moment. The caption describes the proposal contract on the board named for the state where the proposal has already been accepted.", "evidence": "RunPhase: hooks/useAgentRunner.ts:47 \"idle\" | \"planning\" | \"running\" | \"done\". The agent done card: AgentPlan.tsx:107 `Done · ${doneCount} of ${total}`, :270-271 '{appliedCount} change{s} applied{, {n} skipped}.', :275-276 'Each step is its own undo step — ⌘Z takes back the last of the {n}.', :281 'Ask something else'. The chat diff: ChatMessage.tsx:35 '{n} change{s} proposed', :45 'Discard', :50 'Apply', :65 'Apply lands as one undo step.' shown only while pending. Board-to-code index: AITab.css:257 maps 171:67 to the result card.", "edit": "Rewrite 172:41: 'The agent's done card is a result, not a proposal — the steps are applied and the exit is ⌘Z (AgentPlan.tsx:270-276). Discard / Apply belong to the chat path's pending diff (ChatMessage.tsx:45, :50), which is where the proposal contract lives.' If the board draws Discard and Apply, it is drawing the chat state and should be split."}
{"id": "COVER-1-56", "module": "ai", "action": "add-state", "severity": "Major", "confidence": "measured", "target": "1776:8380 / 171:136  (AI · not-configured)", "claim": "The not-configured screen is how the user learns AI is unavailable, and the caption 172:43 says it exists so the failure is loud.", "truth": "It is loud, and it arrives late. There is no client-side capability probe of any kind: the rail entry, the tab and the composer all render unconditionally, and the panel only learns the workspace has no key by sending a prompt and reading a tRPC error code off the response. So the honest screen the caption is proud of is preceded by a screen that looks entirely functional. Two further facts the caption misses: the trigger is a server env check on OPENAI_API_KEY or OLLAMA_BASE_URL, not a feature flag, and NEXT_PUBLIC_FEATURE_DS_AI does not gate this panel at all — its only consumer is the design-system path.", "evidence": "Chain: AITab.tsx:266 the branch ← useStreamPrompt.ts:136-138 (PRECONDITION_FAILED → 'not-configured') ← server/trpc/routers/ai.ts:288-295 assertProviderConfigured ← server/services/ai.service.ts:481-491 (`if (isOpenAIModel(model) && !process.env.OPENAI_API_KEY) throw …`, with the Ollama arm on OLLAMA_BASE_URL). No probe: rail entry editor/rail/tabsConfig.ts:87-95 is unconditional, TabRouter.tsx:141-142 has no flag, and grep dsAi|FEATURE_DS_AI under sidebar/tabs/ai/ returns nothing (the flag is defined runtimeEnv.ts:87-88, surfaced featureFlags.ts:15, and consumed only at editor/shell/hooks/useComposerInit.ts:132). Copy verified verbatim: AITab.tsx:268 'AI drafting isn't configured yet.', :270-271 'No API key is set for this workspace, so nothing here will run. This is the real message — not a silent fallback that pretends to work.', :277-279 'Open workspace settings'.", "edit": "Add to 172:43: 'Reached only by sending a prompt — there is no capability probe, so the panel looks live until the first round trip (AITab.tsx:266 ← useStreamPrompt.ts:136 ← ai.service.ts:481). The trigger is OPENAI_API_KEY / OLLAMA_BASE_URL on the server, not a feature flag; NEXT_PUBLIC_FEATURE_DS_AI does not gate this panel.' Also fix the rail's stale aria-label lead recorded at tabsConfig.ts:87-95 — it says 'chat with Claude' while the model enum is gpt-4o-mini | ollama (packages/shared/schemas/ai.ts:16-22) — that one is a code string, so it is a note for the coordinator, not a board edit."}
{"id": "COVER-1-57", "module": "ai", "action": "none", "severity": "Minor", "confidence": "measured", "target": "1776:8380 / 172:33 172:34 172:35 172:39 172:40 172:42 172:43  (captions: idle, scoped, thinking, step-failed, stopped, error-quota, not-configured)", "claim": "Coverage record: seven of the eleven unread AI captions are correct as written.", "truth": "172:33 quotes the panel's own contract line almost verbatim (EmptyThread.tsx:55-56). 172:35 'Before a plan exists' matches the gate on ChatMessage.tsx:24 ('Thinking…' shown while streaming && !text). 172:39 step-failed matches AgentPlan.tsx:216-235 — work kept, the stop point named, Undo all offered not forced. 172:40 stopped matches :103 and :248-258, including 'What already ran is kept.' 172:42's central claim is right and checkable: the limit IS daily, and the server says so — 'Daily limit reached (N). Resets at <ISO>.' 172:43's history claim matches the code's own comment. 172:34 'scoped' is right in substance — the scope is the canvas selection and clearing it widens to the page.", "evidence": "Code lines as cited above from an independent source read; caption text read live. Three things recorded rather than filed: (a) 172:42's parenthetical 'Free 10 / Pro 200, resetting at midnight UTC' is NOT verified — the number and the reset boundary are server-side and lib/constants/plan-limits.ts carries no AI row; treat those three values as unchecked. (b) 172:34 omits that page scope is not a chat turn at all — AITab.tsx:99-127 upgrades it to a batch edit over at most 200 elements (.slice(0,200) at :114) and flips the intent to 'style-command' (:125); the 200 cap is drawn nowhere. (c) 'scoped' is a board word, not a code identifier — the concept is AIScope (types.ts:6-9) plus ScopeChip, and the lock state during a run is AIScopeStatus 'locked' (types.ts:11, ScopeChip.tsx:24-28).", "edit": "No change. Recorded so these seven are not re-read, and so (a) is either verified or struck by whoever next touches 172:42."}
{"id": "COVER-1-61", "module": "layers", "action": "fix-board", "severity": "Critical", "confidence": "measured", "target": "1776:8375 / 143:179 143:237  (Layers · hidden / Layers · locked) — the eye and lock glyphs on every row of both boards", "claim": "Each tree row ends in an eye and a padlock, and the two boards are named 'hidden' and 'locked' — the ordinary reading is that the eye hides the element and the lock protects it, on the site.", "truth": "Neither flag touches the published site, and the eye is not a hide. Its own tooltip says so: 'Dim in editor — the element still publishes.' Both flags are per-page localStorage keys that write data-hidden / data-locked onto the canvas node and nothing else; the source records the measurement — 'toggled it, then read the export — the element was still in the body'. The two also behave differently on the row: hidden dims it to 45%, and locked has NO row treatment at all — the only signal is the darker glyph, the aria-label suffix and draggable=false.", "evidence": "LayerTreeItem.tsx:298-323 the eye — titles 'Dim in editor — the element still publishes' / 'Show in editor — this element publishes either way' (:301-305), aria-label 'Dim in editor' / 'Show in editor' (:306). Lock :325-345, title/aria 'Lock element' / 'Unlock element' (:328-329). The measurement note is at :285-297. Persistence: panels/layers/hooks/layersPersistence.ts:8-9 keys buildrick-layers-{pageId}-hidden / -locked, :15-23 read, :83-91 write data-hidden / data-locked to the canvas node. Row styling: hidden → rowClassNames adds bdc-hidden (LayerTreeItem.tsx:131) → opacity .45 (styles/layers-v2.css:56); locked has NO entry in rowClassNames (:127-138) and grep 'locked' in layers-v2.css returns only a comment at :159. Drag: canDrag = !!(composer && layer.depth > 0 && !isLocked) (:99).", "edit": "On 143:179 and 143:237 add a caption in the 11px muted style: 'Editor-only. The eye is Dim, not Hide — \"the element still publishes\" (LayerTreeItem.tsx:301). Both flags are per-page localStorage (layersPersistence.ts:8-9) and write only data-hidden / data-locked on the canvas node; the export is unchanged (:285-297).' And on 143:237 remove any row dimming — locked rows carry no row-level treatment, only a darker glyph and draggable=false."}
{"id": "COVER-1-62", "module": "layers", "action": "fix-board", "severity": "Major", "confidence": "measured", "target": "1776:8375 / 143:119  ([not-implemented] Layers · invalid-drop) — the DragTooltip 143:178", "claim": "The refusal is a tooltip that follows the drag, reading 'A button can't contain a section' (143:178) — a sentence written for a person.", "truth": "The board's frame name is already right that no drag-time refusal exists; the tooltip it draws is wrong in three further ways. The refusal is not a tooltip and not beside the cursor — it is a transient alert bar at the TOP of the panel, it appears only AFTER the drop, and it clears itself after three seconds. And the copy is not written for a person: four of the five messages interpolate raw internal type names, so the real sentence is closer to 'button cannot contain children' or 'section cannot be nested inside button'.", "evidence": "panels/layers/index.tsx:400-404 the alert element (.bdc-layers-drop-alert), CSS styles/layers-v2.css:483-492; auto-clear at index.tsx:90-95 (3000ms). The five messages, all in handleLayerDrop: :125 'Cannot drop inside a locked container'; :131 `${parentType} cannot contain children`; :135 `${sourceType} cannot be nested inside ${parentType}`; :145 'Cannot drop next to elements in a locked container'; :151 `${sourceType} cannot be placed in ${parentType}`. handleDragOver (:238-270) never calls canNestElement or canHaveChildren, which is why the indicator always looks valid — the board's frame name already records this.", "edit": "On 143:119 move the refusal out of the cursor and into a full-width alert bar at the top of the panel, below the toolbar, and change the copy to the shipped shape — 'button cannot contain children' — with a note that it appears after the drop and clears after 3s (index.tsx:90-95, :400-404). Keep the always-valid-looking insertion line exactly as drawn; that is the defect the board is documenting."}
{"id": "COVER-1-63", "module": "layers", "action": "add-state", "severity": "Major", "confidence": "measured", "target": "1776:8375 / 143:295  (Layers · multi-select)", "claim": "Multi-select is shown as three highlighted rows plus a footer reading '3 selected of 12' (143:352) — and nothing else.", "truth": "At two or more selected the panel grows a selection toolbar the board does not draw, and it holds the only bulk actions Layers has. It reads '{n} selected' and carries four controls — Group, Dim in editor, Delete and Done — and Delete opens an inline confirm inside the panel rather than a browser dialog. Escape exits the selection.", "evidence": "panels/layers/components/LayerSelectionBanner.tsx:33-68 — '{count} selected' (:36), 'Group' (:37), 'Dim in editor' with title 'Dim in editor — these elements still publish' (:47-51), 'Delete' (:58), 'Done' (:63); role=\"toolbar\" aria-label=\"Selection actions\" (:35); Escape at :26-28. Inline confirm: panels/layers/index.tsx:412-418 'Delete {n} layers?' with 'Delete' / 'Cancel'. The footer switch the board DOES draw is correct — LayersTab.tsx:218-220. Modifiers: LayerTreeItem.tsx:189 reads shiftKey and metaKey||ctrlKey; meta toggles (useLayerSelection.ts:80-84), shift takes a contiguous range (:85-107).", "edit": "Add the selection toolbar to 143:295, above the tree: '3 selected' with four controls — Group, Dim in editor, Delete, Done. Then clone the board as 'Layers · multi-delete-confirm' showing the inline 'Delete 3 layers?' row with Delete / Cancel, which is a distinct state and currently drawn nowhere."}
{"id": "COVER-1-64", "module": "layers", "action": "add-state", "severity": "Major", "confidence": "read", "target": "1776:8375 / 1082:4527  (Layers · context-menu) — board not opened by me; claim is from the manifest name plus the code", "claim": "The Layers context menu is a board in the section and, per the manifest, a driven one.", "truth": "Recorded here for whoever opens it, because the code carries two facts a drawing will not: the live menu is exactly eight items in two groups — Cut, Copy, Paste, then Copy link, Duplicate, Delete, then Rename, Group selection — with Paste disabled when the clipboard is empty and Group selection disabled below two selected, each carrying its own reason as a title. And the LayerAction union has fifteen members: hide/show/lock/unlock live on the row buttons, but selectChildren, moveToTop and moveToBottom have working handlers and no UI anywhere. There is also a SECOND, dead LayerContextMenu with four items and no importer outside tests.", "evidence": "Live menu: panels/layers/components/LayerContextMenu.tsx:61-101 in order — Cut (:61), Copy (:64), Paste (:67, disabled with title 'Copy or cut an element first'), separator (:76), Copy link (:80), Duplicate (:83), Delete (:86), separator (:89), Rename (:90), Group selection (:93, disabled with title 'Select 2 or more layers first'); role=\"menu\" aria-label `Actions for ${nodeName}` (:54). Handlers panels/layers/hooks/useLayerContextActions.ts:18-107, with the three orphans at :92-106 and no emitter anywhere under src/editor. Union: panels/layers/types.ts:81-96 (15 members). Dead duplicate: sidebar/tabs/layers/components/LayerContextMenu.tsx (Rename :59, Group :63, lock :65, Delete :72), no non-test importer.", "edit": "Whoever opens 1082:4527: verify the eight items and their order, draw Paste and Group selection in their disabled state with the two reason strings, and add a caption naming the three orphaned actions (selectChildren, moveToTop, moveToBottom — handlers at useLayerContextActions.ts:92-106, reachable from no UI) so they are not mistaken for menu items that were forgotten."}
{"id": "COVER-1-65", "module": "layers", "action": "none", "severity": "Minor", "confidence": "measured", "target": "1776:8375 / 1082:4589 1082:4640 782:4260  (renaming / expanded / no-results)", "claim": "Coverage record: three more Layers boards opened and found correct, with three code facts recorded for their captions.", "truth": "782:4260 is verbatim — 'Nothing matches ‘hero’.' + 'Clear search' = LayersStateBlocks.tsx:83, :92, curly quotes included. 1082:4589 renaming: the inline field commits on ENTER and on BLUR and cancels on ESCAPE, is reachable three ways (double-click an unlocked row, F2, the context menu), and an empty string DELETES the custom name rather than saving one — reverting the row to its type label. 1082:4640 expanded: expansion is persisted per page, and the board's fully-expanded 24-row tree is a legitimate drawing of a restored state rather than a first load.", "evidence": "Rename: LayerTreeItem.tsx:234-252 (blur commits :245, Enter :247, Escape :248), entry points :195-197 (double-click), :144-148 (F2); commit useLayerActions.ts:180-201 with the empty-string delete at :188-190 and EVENTS.ELEMENT_RENAMED at :197. Expansion: useLayerTree.ts:48 read / :153-156 write, key buildrick-layers-{pageId}-expanded; indent 12 + depth*16 (LayerTreeItem.tsx:124). Search results render FLAT while searching — panels/layers/index.tsx:346-359 — which is why 782:4260 shows no hierarchy.", "edit": "No board change. Add one clause to whichever caption serves 1082:4589: 'Custom names are localStorage-only (layersPersistence.ts:74-80) — they are not part of the project model and do not travel with the site.' Recorded so these three are not re-read."}
{"id": "COVER-1-66", "module": "layers", "action": "none", "severity": "Major", "confidence": "measured", "target": "scripts/conformance/boards.json / 775:4130  (Layers · loading) — a stale manifest note, not a board defect", "claim": "The conformance manifest records 775:4130 as dead: 'skeleton NEVER renders… LayersTab.tsx:182 false branch is dead' (2026-08-31).", "truth": "That note is stale and should not be acted on. The skeleton renders today: the gate was changed to test projectLoading as well as composer, precisely because composer alone is non-null on the first render. The board is live, and its real defect is the count footer — see COVER-1-31.", "evidence": "LayersTab.tsx:194 `{composer && !projectLoading ? (…) : (<LayersLoadingSkeleton />)}` with the blocker-A2 rationale at :186-193 ('composer alone is not \"ready\": useComposerInit sets it synchronously in the effect body, before the site fetch even starts'). Skeleton: panels/layers/components/LayersStateBlocks.tsx:35-50, seven rows, indent ladder pl-4/8/12/12/8/4/8, bar widths 92/96/110/70/86/124/104 (:19-30), data-testid=\"layers-loading\" (:36). By contrast the manifest's OTHER unreachability note, 143:355 Layers · empty, still holds — useLayerTree.ts:97 always seeds one root row, so an empty page reads '1 layer', never zero.", "edit": "No board change. Correct the verifiedNote on 775:4130 in scripts/conformance/boards.json (a code file, so this is a note for the coordinator, not a Figma edit). Leave 143:355's unreachable marker alone — independently re-confirmed."}
