{"id": "FIG-A-01", "module": "content-cms", "action": "none", "target": "1776:8376 / 149:50  (Content · collection — where a collection-bound list would live)", "claim": "No board anywhere in page 1:3 draws a repeater, a collection-bound list of records, or a 'Repeat with collection' control.", "truth": "Correct, and it must stay that way. The repeater is the module's largest doorless feature: RepeaterRenderer.ts:25-47 (287 lines, nested recursion, HTML escaping) is constructed by nothing but its own test, and CMSBindingManager.ts:208 bindCollection / :229 unbindCollection have no caller. The board file has never promised it, so there is nothing to mark and nothing to draw.", "evidence": "Figma: recursive walk of page 1:3 descending INTO instances — 37,273 nodes scanned, keys ['repeat','repeater','archiv'] — 8 hits, all either the CSS literal 'repeat(3, 1fr)' (71:152, 807:8491) or prose in audit/journey captions. NB the first pass used page.findAllWithCriteria({types:['TEXT']}) which did NOT descend into INSTANCE children (it returned 4 texts for 149:50 where the manual walk returns 14) — the null result was the instrument until the deep walk confirmed it. Code, searched by CONSTANT per the brief: EVENTS.CMS_COLLECTION_BOUND/'cms:collection:bound' and CMS_COLLECTION_UNBOUND appear at events.ts:621,623, CMSBindingManager.ts:223,233 (emit) and CMSBindingManager.test.ts:293,324 — nowhere else in packages/editor/src or server/. 'new RepeaterRenderer' / 'expandRepeaters' outside __tests__: RepeaterRenderer.ts itself and engine/cms/index.ts:9 only. DataManager.ts:394 bindCollection is a different, generic-data method.", "edit": "NO EDIT. Do not create a repeater board and do not rename anything. Record as covered so the next wave does not 'discover' the missing screen and design it.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-A-02", "module": "content-cms", "action": "none", "target": "1776:8381 / 824:5095  (Inspector · flat scroll body — the only board that draws a 'Data Binding' group)", "claim": "No inspector board draws the 'Repeat with collection' toggle that the properties registry declares.", "truth": "Board is right. propertiesRegistry.ts:877 declares \"data.repeaterEnabled\": {uiOnly:true, type:'toggle'} and repo grep for 'data.repeater' returns that one line — no renderer, no reader, and CMSCollectionBinding (CMSBindingManager.ts:47-60) has no 'enabled' member. The registry describes a control the UI does not have; the board correctly does not draw it.", "evidence": "Figma deep scan above found zero 'repeat' text or layer names in section 1776:8381. Code: propertiesRegistry.ts:877; MOD-A-23.", "edit": "NO EDIT. The registry row is the thing that is wrong, and the registry is code (read-only for this pass).", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-A-03", "module": "content-cms", "action": "mark-unimplemented", "target": "1776:8376 / 1170:4713  (Content · collection-setup (modal)) — inner frame 1173:4825 'dynamic-pages'", "claim": "The wizard's 'Generate a page per entry' block (1173:4825, switch 1173:4827 + caption 1173:4830) draws dynamic-page generation as a working setup step: flip the switch, type a slug pattern and a template, get one page per record.", "truth": "A collection set up through this block contributes ZERO pages at publish, and nothing anywhere says so. appendDynamicPagesToPublish selects collections by `pageTemplatePath: {not: null}` and then does `pages.find(p => p.path === col.pageTemplatePath)` with `if (!template) continue`. The publish page-set's paths are built by ExportEngine.buildPageHrefs as flat single-segment names — 'index.html' for the home page, '<slug>.html' otherwise, de-duped with '-2'. There is no code path that emits a nested path, so the board's own sample value ('blog/_template') and the code's placeholder ('blog/_template/index.html') both miss the find() and the loop continues silently.", "evidence": "server/services/cms.service.ts:234 `where: {siteId, pageSlugPattern:{not:null}, pageTemplatePath:{not:null}}`, :240 `const template = pages.find(p => p.path === col.pageTemplatePath)`, :241 `if (!template) continue`. packages/editor/src/engine/export/ExportEngine.ts:788-797 `if (p.id === homeId) return [p.id,'index.html']; const slug=(p.slug??'').replace(/^\\/+/,'')||`page-${index+1}`; let name=`${slug}.html``. Figma: 1173:4830 characters = 'Slug pattern — /blog/{slug} · Template — blog/_template · SEO title — {title} — Blog'. MOD-A-10, W-C-22.", "edit": "Rename frame 1173:4825 from 'dynamic-pages' to 'NOT IMPLEMENTED — a typed template path can never match an exported page, so this generates zero pages'. Add a caption text node under the block (matching the 11px muted BOARD_CAPTION style already used at 1173:4830) reading: 'Publish emits none of these. The template must equal an exported filename — index.html or <slug>.html — and this field is free text with a nested-path placeholder (cms.service.ts:240; ExportEngine.ts:788).' Do NOT delete the block: the design is the spec for the picker this needs.", "severity": "Critical", "confidence": "measured"}
{"id": "FIG-A-04", "module": "content-cms", "action": "fix-board", "target": "1776:8376 / 1170:4713  (Content · collection-setup (modal)) — text node 1173:4830", "claim": "1173:4830 compresses the whole dynamic-page setup into ONE static caption line naming three settings: 'Slug pattern — /blog/{slug} · Template — blog/_template · SEO title — {title} — Blog'.", "truth": "The code renders FOUR separate TextInput controls in a grid when the switch is on, and the fourth — SEO description — is on no board at all. A builder reading this board ships three fields, or one, and cannot tell they are inputs rather than a hint line.", "evidence": "packages/editor/src/editor/shell/modals/CMSCollectionSetupModal.tsx:433-438 — `<div className=\"tw:grid tw:gap-2\">` containing four `<TextInput>` with placeholders 'Slug pattern — /blog/{slug}', 'Template page path — blog/_template/index.html', 'SEO title — {title} — Blog', 'SEO description — Read about {title}'; state at :401-405 (pageSlug default '/{slug}', pageTemplate/pageSeoTitle/pageSeoDesc default ''); written at :204-211 updateCollection({pageSlugPattern,pageTemplatePath,pageSeoTitle,pageSeoDescription}).", "edit": "Replace text node 1173:4830 with four stacked input rows inside 1173:4825 (reuse the 'TextInput · name' frame style from 1753:8392 — 8px gap), placeholder text verbatim from the four lines above, in that order. Also correct the switch label 1173:4829 from '✓ Generate a page per entry' to 'Generate a page per entry' — the ✓ is baked into the label text and the code's ToggleSwitch label carries no tick (CMSCollectionSetupModal.tsx:429).", "severity": "Major", "confidence": "measured"}
{"id": "FIG-A-05", "module": "content-cms", "action": "fix-board", "target": "1776:8376 / 1706:8437  (Modal · CMS records · none-published) — text node 1706:8397", "claim": "'No records published yet — this collection generates a page per entry, but dynamic pages won't generate until at least one record is published.' The board tells the user that publishing a record is what unblocks page generation.", "truth": "Publishing a record is necessary and nowhere near sufficient, and the banner's own trigger proves it: it fires on `Boolean(collection?.pageSlugPattern)` alone and never looks at pageTemplatePath — the field the publish pipeline actually filters and matches on. A user who does exactly what this sentence says still deploys zero generated pages, and no pre-publish check mentions CMS at all.", "evidence": "packages/editor/src/editor/shell/modals/CMSRecordsModal.tsx:102 `const hasDynamicPages = Boolean(collection?.pageSlugPattern)`, :317 `{hasDynamicPages && publishedCount === 0 && (`, :330-331 the copy verbatim. Publish side: server/services/cms.service.ts:234,240-241 (see FIG-A-03). server/services/publish.service.ts:50-105 — the complete check list is Vercel connected, Pages ready, SEO configured, Domain connected, Empty pages, Favicon; grep -i 'cms|collection' over that file hits only the appendDynamicPagesToPublish import (:5) and call (:329).", "edit": "Rewrite 1706:8397 to: 'No records published yet. Dynamic pages generate only from published records — and only once a template page is bound.' Then add a second, warning-toned line beneath it: 'No template page is bound, so publishing emits none of these yet.' (that string already exists in the product at ContentViews.tsx:684 and is the honest half). Keep the board's height by absorbing the extra line into the 162px frame.", "severity": "Critical", "confidence": "measured"}
{"id": "FIG-A-06", "module": "content-cms", "action": "create-board", "target": "1776:8376 / new board, cloned from 1705:8286 (Content · collection · empty, 280x812), placed after 151:2", "claim": "The Content section has 33 boards and none of them is the Dynamic pages screen, even though 149:50 draws a 'Dynamic pages ›' row (list-row instance I233:1309) that opens it.", "truth": "DynamicPagesView is fully built and reachable — CollectionView renders the row at ContentViews.tsx:660 and ContentTab wires onOpenDynamicPages at :245 → view {kind:'dynamic-pages'} at :291-311 — and it produces six distinct bodies plus a savebar and a discard confirm, none of which is drawn anywhere. It is also the screen that carries the module's two most consequential warnings.", "evidence": "ContentViews.tsx:600-726 DynamicPagesView. States, each a literal string in that file: :672 'No pattern set — this collection generates no pages.'; :663 'No records yet — nothing to generate.'; :665 '{n} records, none published. Dynamic pages only generate from published records.'; :668 'Generates {n} pages from published records.'; :679-681 'This collection has no field called \"{key}\", so every record resolves to the same URL.'; :684 'No template page is bound, so publishing emits none of these yet.'; :697-704 ConfirmDialog 'Discard changes?' / 'The page pattern has unsaved changes. Going back throws them away.'; :707-724 savebar 'Unsaved changes · Discard · Save'. Crumb label at :638 `${collection.name} · dynamic pages`. Input at :645-651 — a TextInput, mono font, placeholder '/menu/{slug}', aria-label 'URL pattern'. Prior screen finding: W-C-28.", "edit": "Create 'Content · dynamic-pages' (280x812) plus four state siblings — '· no-pattern', '· none-published', '· unknown-key', '· no-template' — all cloned from 1705:8286's panel-header + crumb + scroll skeleton. Body: crumb '‹ Menu items · dynamic pages'; FIELD_LABEL 'URL pattern'; a mono TextInput with '/menu/{slug}'; the 11px muted hint 'One page per record. Use a field slug in braces — {slug} — to build the URL.'; then the state line, warning-toned (var(--bk-warning)) for the three warning variants and muted for 'Generates 4 pages from published records.'. Add the SAVEBAR from 149:132 to the dirty variant. Also add caption boards (280x54) for each, and note on the base caption that the 'Generates N pages' count is computed from LOCAL IndexedDB records while generation reads SERVER entries, so a queued sync makes it overstate (ContentViews.tsx:623 vs cms.service.ts:198-202; MOD-A-24).", "severity": "Critical", "confidence": "measured"}
{"id": "FIG-A-07", "module": "content-cms", "action": "wire-edge", "target": "1776:8376 / 149:50  (Content · collection) — list-row instance I233:1309 'Dynamic pages'", "claim": "The 'Dynamic pages ›' row on 149:50 carries no reaction; the board's only prototype exits are the three hotspot/state frames parked on it (1170:4811, 1670:7257, 1720:17390).", "truth": "In the product this row opens a real screen. The edge is missing only because its destination does not exist in the file.", "evidence": "Figma structure dump of 149:50: `I 233:1309 List row` → children Label 'Dynamic pages' + '›', no reaction; the three hotspot/state frames are the only wired exits. Code: ContentViews.tsx:660 `{onOpenDynamicPages && <ListRow label=\"Dynamic pages\" chevron onClick={onOpenDynamicPages} />}`; ContentTab.tsx:245 passes it. W-C-28.", "edit": "After FIG-A-06 lands, add an On-click → Navigate-to edge from I233:1309 to the new 'Content · dynamic-pages' board. Note the row carries NO count in either place (the 'Fields' row above it shows 8 from collection.fields.length, ContentViews.tsx:659) — do not add one.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-A-08", "module": "content-cms", "action": "fix-board", "target": "1776:8376 / 155:52  (caption/Content · fields)", "claim": "'The schema, not the data. Changing a field type touches every row, so the count belongs on this screen.' The caption asserts a per-field count is on the Fields screen.", "truth": "No count is on that screen, on the board or in the code — and the count that would matter cannot be computed at all. Board 151:2 draws eight field rows as name · type · optional 'required' · '⋯' with no number. FieldsView renders the same. And the thing a user needs before deleting a field — how many elements bind it — has no query: bindings are stored element→binding only, with no by-collection or by-field lookup anywhere, so deleteField calls straight through and the bound elements silently fall back to ''.", "evidence": "Figma 151:2 rows 151:9/151:14/151:19/151:23/151:27/151:31/151:35/151:40 — every one is {name, type, [required], ⋯}. Code: ContentViews.tsx:458-598 FieldsView; the delete confirm at :576-578 says '\"{name}\" and its values on every record will be removed.' and mentions no bindings. BaseBindingManager.ts:72 `private bindings: Map<string, T[]>`; :140 getBindings(elementId) is the only accessor. CollectionManager.ts:221-230 deleteField → updateCollection → emit CMS_COLLECTION_UPDATED, whose only listeners are ContentTab.tsx:96 (panel reload) and useCmsSync.ts:92 (server mirror) — CMSBindingManager.ts:75-77 subscribes to content:created/updated/deleted only. MOD-A-06, MOD-A-20.", "edit": "Rewrite 155:52 to: 'The schema, not the data. Deleting a field removes its values from every record — and silently empties every element bound to it, because nothing can count those (no by-field binding lookup exists).' Removing the count promise is the point: leaving it invites the next wave to design a number the product cannot produce.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-A-09", "module": "content-cms", "action": "fix-board", "target": "1776:8376 / 155:50  (caption/Content · record)", "claim": "'Field-driven form. Per-record publish lives here, not in the list.'", "truth": "Per-record publish lives in BOTH places, and the second one is on a board in this same section. The records modal renders a Publish/Unpublish button on every row, and the collection list itself carries a publish dot per record.", "evidence": "Figma 1170:4749 rows: 1744:8396 'btn/Unpublish', 1744:8403 'btn/Unpublish', 1744:8410 'btn/Publish', each beside btn/Edit record and btn/Delete record. Code: CMSRecordsModal.tsx:398 `onClick={() => setStatus(item.id, item.status === 'published' ? 'draft' : 'published')}`, :403 `{item.status === 'published' ? 'Unpublish' : 'Publish'}`. The panel's own toggle is RecordView's 'Published' row (ContentViews.tsx, board 149:103).", "edit": "Rewrite 155:50 to: 'Field-driven form. The Published toggle here is one of two publish doors — the records table (1170:4749) publishes and unpublishes per row too.'", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-A-10", "module": "content-cms", "action": "fix-board", "target": "1776:8376 / 149:84  (Content · record)", "claim": "The record board draws back-crumb, three field rows and a Published toggle, and no destructive action at all.", "truth": "For an existing record the panel renders a 'Delete record' link in error red under the fields, which opens a 'Delete record?' confirm. The board omits the control entirely, so the only delete affordance a reviewer sees in this module is on the records modal — and the sidebar is the door people actually use.", "evidence": "ContentViews.tsx:372-377 `{record && onDelete && (<Button className={`${LINK_BTN} tw:mx-3 tw:my-2 tw:text-[var(--bk-error)] ...`} onClick={() => setConfirmDelete(true)}>Delete record</Button>)}`; :382-390 ConfirmDialog title 'Delete record?' confirmLabel 'Delete record'. ContentTab.tsx:270-281 passes onDelete only when view.recordId is set. Figma 149:84 children: Back row, Field·Name, Field·Price, Field·Description, Row·Published, spacer — no delete. W-C-30 covers the undrawn confirm dialog; this row is about the missing trigger.", "edit": "Add a link-style text row 'Delete record' in var(--bk-error), left-aligned at 12px from the panel edge, between 149:107 (spacer) and the frame bottom, on 149:84 and on 149:108 (unsaved-record). Do NOT add it to 1705:8408 (record · save-error) — that board draws a NEW record ('‹ Untitled record'), and the code hides delete when recordId is null.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-A-11", "module": "content-cms", "action": "none", "target": "1776:8376 / 149:50  (Content · collection) and 155:49 (its caption)", "claim": "Record rows carry a two-state publish dot — ● published, ○ draft — and no board anywhere in the file draws an 'archived' record.", "truth": "Correct and exactly right. 'archived' is in the type union and has no writer anywhere: nothing sets it, CollectionManager branches on 'published' only, and the server mirror collapses anything non-published to DRAFT. The boards draw the two statuses the product can actually reach.", "evidence": "Figma: deep 37,273-node scan for 'archiv' returned three hits, all unrelated captions (957:4474 the Archive section name, 817:5194 activity-log, 817:5219 J4 addendum) — none in section 1776:8376. Code: shared/types/cms.ts:86 `status: 'draft' | 'published' | 'archived'`; grep for 'archived'/'ARCHIVED' across packages/editor/src + server returns FormsScreen.tsx:54,119,273 (form submissions, different feature) and sites.service.ts:440,555 / dashboard.service.ts:34 (site status, different entity) — no CMS writer. CollectionManager.ts:317-323 branches on 'published' only. cmsSync.ts:214 `status: item.status === 'published' ? 'PUBLISHED' : 'DRAFT'`. Figma 155:49 caption: 'Record rows carry a publish dot: ● published · ○ draft.'", "edit": "NO EDIT. Do not add an archived state to any board; the type union is the thing that over-promises and it is code.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-A-12", "module": "content-cms", "action": "fix-board", "target": "1776:8376 / 775:4241  (Content · loading) — text node 776:4097", "claim": "The loading board's COLLECTIONS section header carries the count '2' beside it while the six skeleton rows are still resolving.", "truth": "The panel cannot know that number yet — that is the whole reason the loading state exists. The loading branch renders `<SectionHeader tint>Collections</SectionHeader>` with no count, and the count only appears once collections have arrived. Drawing a real number over a skeleton says the data is already here.", "evidence": "Figma 775:4241 → `F 776:4095 Group · COLLECTIONS` → `T 776:4096 'COLLECTIONS'` + `T 776:4097 '2'`; the DATA group (776:4098) correctly has no count. Code: ContentTab.tsx loading branch renders SectionHeader with the bare label for both bands and three SkeletonRows each; the count on the real root comes from RootView's collections array (board 148:2, text 148:9 '2').", "edit": "Delete text node 776:4097 ('2') from 775:4241, or replace it with an 8px-wide skeleton pill matching sk 775:4289. The DATA band is already correct — match it.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-A-13", "module": "content-cms", "action": "add-state", "target": "1776:8376 / new board, cloned from the toast catalog 1177:4859 (Reference · toast catalog — 5 tones · sticky class), placed after 453:4010", "claim": "Nothing in the Content section draws the CMS sync failure. The section has a load-error (453:4010, 'Couldn't load your collections') for the READ side and no state at all for the WRITE side.", "truth": "The product raises a sticky, error-toned toast with a Retry action whenever a CMS mirror to the server fails, and it retracts itself when the queue drains. It is the only signal a user gets that their content is on this device and nowhere else — and it is drawn nowhere.", "evidence": "packages/editor/src/services/cmsSync.ts:35-48 SyncRetryQueue + getCmsSyncPendingCount + onCmsSyncError. useCmsSync.ts (editor/shell) toast: title 'Some content changes didn\\'t sync', description `${pending} CMS change${pending === 1 ? ' is' : 's are'} saved on this device but not yet on the server. Retry now, or leave it — a reconnect replays the queue.`, tone 'error', duration Infinity, action label 'Retry now'. Retraction on pending===0 is unit-tested in useCmsSync's own __tests__ ('takes the notice down when the queue drains on its own').", "edit": "Create 'Content · sync-failed (toast)' at the toast catalog's error-tone size (360w per 1719:8420's note: Toast.tsx bottom-right, 360w). Title 'Some content changes didn\\'t sync'; body '2 CMS changes are saved on this device but not yet on the server. Retry now, or leave it — a reconnect replays the queue.'; one action link 'Retry now'; no dismiss timer (sticky class). Add a 280x54 caption naming that it retracts itself when the queue drains, so nobody designs a manual dismiss.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-A-14", "module": "content-cms", "action": "none", "target": "1776:8381 / 160:105  (Inspector · bound-to-CMS) — cross-module; owner is the Inspector agent", "claim": "160:105 draws a live bound state — 'Text is bound to menu.name', 'Edit the record in Content, or unbind to type here.', an 'Unbind' action — and caption 161:17 adds a 🔗 chip in the header with bound fields going read-only.", "truth": "DO NOT MARK THIS UNIMPLEMENTED. MOD-A-03 says bindings live only in two in-memory Maps and are in no persistence path, which would make this board's whole state unrestorable after a reload. That is true of HEAD (54544a6ac) and is ALREADY FIXED in the working tree: ProjectData now carries a cmsBindings field, importProject restores both halves, exportProject writes them, and a round-trip test exists. The change is uncommitted, so a Figma write made on MOD-A-03's strength would be wrong within a day.", "evidence": "git show HEAD:packages/editor/src/shared/types/project.ts | grep cmsBindings → no match. Working tree: shared/types/project.ts adds `cmsBindings` to ProjectData with a comment naming BaseBindingManager.ts:72 and CMSBindingManager.ts:68; Composer.ts:569-572 `if (data.cmsBindings) { ... this.cms.bindings.import(...); this.cms.bindings.importCollectionBindings(...) }` placed before applyProjectSettings; Composer.ts:628-632 `...(this.cms ? {cmsBindings: {field: this.cms.bindings.export(), collection: this.cms.bindings.exportCollectionBindings()}} : {})`; new file engine/__tests__/Composer.cmsBindings.test.ts asserts the field binding survives exportProject → importProject. `git diff --stat` shows both files modified and unstaged. The bound state itself is read from the same map at SelectionBoxOverlay.tsx:74-75 and useCMSPreview.ts:58, and written only at BindingPopover.tsx:161 bindToField. RUN, not just read: 'npx vitest run src/engine/__tests__/Composer.cmsBindings.test.ts' -> 1 file passed, 2 tests passed (vitest 4.1.2, 2.98s). The round trip holds in the working tree.", "edit": "NO EDIT on the persistence question. Re-check after the founder commits; if the change is abandoned, this becomes a mark-unimplemented on 160:105 + 161:17. FIG-A-15 covers a separate defect on the same board that is NOT affected.", "severity": "Critical", "confidence": "measured"}
{"id": "FIG-A-15", "module": "content-cms", "action": "fix-board", "target": "1776:8381 / 161:17  (caption/Inspector · bound-to-CMS) — cross-module; owner is the Inspector agent", "claim": "The caption describes the bound state as a safe, live link: a 🔗 chip, read-only fields, the collection and field named, and an unbind action.", "truth": "Two things it does not say, both of which reach the live site. (1) A binding to a DRAFT record publishes that draft's text: resolveBinding applies no status filter and static resolution is the publish default. (2) Publishing or unpublishing that record does not refresh the canvas: CollectionManager emits CMS_CONTENT_PUBLISHED *instead of* CMS_CONTENT_UPDATED on that transition, and neither the binding manager nor the canvas preview subscribes to it.", "evidence": "(1) CMSBindingManager.ts:110-139 resolveBinding — queryContent called with `{collectionId, filter:{}}`, no status anywhere; CollectionManager.ts:355-358 applies a status filter only when options.status is set. ExportEngine.ts:700-702 `mode: options.cmsMode ?? 'static'`; editor/shell/exportPublishPages.ts:39 `exportAllPages({format:'html', minify:true})` passes no cmsMode. Contrast the dynamic-page path which does filter `status:'PUBLISHED'` (cms.service.ts:198-202). (2) CollectionManager.ts:317-323 — the three emits are mutually exclusive; CMSBindingManager.ts:75-77 subscribes to 'content:created','content:updated','content:deleted'; useCMSPreview.ts:123-124 to 'content:updated','content:created'. useCmsSync.ts:96-97 subscribes to PUBLISHED and UNPUBLISHED and is the model. MOD-A-04, MOD-A-15.", "edit": "Append to 161:17: 'Two things this state does not tell you: a binding to a DRAFT record publishes that draft\\'s text (no status filter in resolveBinding, and publish resolves statically), and publishing the record does not refresh the canvas — the publish transition emits its own event that neither the canvas nor the binding manager listens for.'", "severity": "Critical", "confidence": "measured"}
{"id": "FIG-A-16", "module": "content-cms", "action": "none", "target": "1776:8377 / 141:207  (Pages · listings) and its caption 155:24", "claim": "No board in the Pages family draws CMS-generated pages. 'Pages · listings' is the SEO table (PAGE · TITLE · DESC · SCORE), and its caption records that an earlier board described CMS-generated pages here and was corrected.", "truth": "The board matches the code exactly, and matching is the problem — but it is not a board problem. The Pages panel has zero CMS references, and cms.dynamicPages / generateDynamicPages have no editor caller at all, so a page-generating collection is invisible in the one place a user looks at their pages. Drawing it would be design-ahead.", "evidence": "Figma 141:207 children: Search + '⊞ Listings' toggle, table header PAGE/TITLE/DESC/SCORE, three listing rows, 'Open full listings ›'. Caption 155:24 verbatim: '(The card board described CMS-generated pages here; cargo-sheets §2 defines Listings as the SEO table.)'. Code: grep -ri 'cms|collection' over packages/editor/src/editor/sidebar/tabs/pages/ returns nothing; 'dynamicPages'/'generateDynamicPages' outside tests hit only server/trpc/routers/cms.ts:81,92 and server/services/cms.service.ts:152,187. MOD-A-09.", "edit": "NO EDIT. Record that the absence is faithful, and that the fix is an editor call site, not a board.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-A-17", "module": "content-cms", "action": "none", "target": "1776:8378 / 833:4518  (Publish · pre-checks)", "claim": "The pre-checks board draws exactly six rows — Vercel connected ✓, Pages ready ✓, SEO configured !, Domain connected !, Empty pages !, Favicon ! — and no CMS or dynamic-pages check.", "truth": "Board is a faithful, row-for-row match of runPrePublishChecks. The product genuinely has no CMS check, which is why a publish goes green over a collection that will emit nothing (FIG-A-03/05), but adding a seventh row here would draw a capability the code does not have.", "evidence": "Figma 833:4518 rows 833:4531/4543/4536/4548/4570/892:4518 with those six labels and the summary '⚠ 4 warnings — none block. Client approval is a separate gate.' Code: server/services/publish.service.ts:59-105 pushes exactly VERCEL_CHECK_LABEL, 'Pages ready', 'SEO configured', 'Domain connected', 'Empty pages', 'Favicon', then `const hasFail = checks.some(c => c.status === 'fail')`. resolveDynamicPages (cms.service.ts:152) already returns the row shape a seventh check would need and is called by nothing. MOD-A-11.", "edit": "NO EDIT to 833:4518 or 893:4518. The dynamic-pages truth goes on the Content boards instead (FIG-A-03, FIG-A-06).", "severity": "Major", "confidence": "measured"}
{"id": "FIG-A-18", "module": "content-cms", "action": "fix-board", "target": "1776:8376 / 1170:4749  (Content · records (modal)) — text node 1744:8394", "claim": "The records table draws a visible 'Actions' column header above the Publish/Edit/Delete buttons.", "truth": "The product renders that header as screen-reader-only, so the column is unlabelled on screen. The board would have a builder ship a visible fifth heading.", "evidence": "packages/editor/src/editor/shell/modals/CMSRecordsModal.tsx:357-359 `<th scope=\"col\" className=\"tw:px-3 tw:py-2\"><span className=\"tw:sr-only\">Actions</span></th>`, immediately under the comment 'The board draws no row actions; the code has publish, edit and delete'. The other headers are real: :344-352 maps `columns` = collection.fields.slice(0, MAX_FIELD_COLUMNS) (:97-100) plus a literal 'Updated'.", "edit": "Set text node 1744:8394 ('Actions') to hidden, or restyle it to the board's own visually-hidden convention. Leave the three row buttons — they are real (CMSRecordsModal.tsx:398-403 publish/unpublish, plus edit and delete).", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-A-19", "module": "content-cms", "action": "fix-board", "target": "1776:8376 / 1706:8444  (Modal · CMS records · empty) — text node 1706:8399", "claim": "The empty records modal draws the table header as 'NAME    PRICE    STATUS    Updated'.", "truth": "There is no STATUS column. The header is the collection's own first fields (capped) followed by 'Updated' and an unlabelled actions cell; status is shown as the Publish/Unpublish button, not a column. The populated board (1170:4749) already gets this right — 'Title · Price · Photo · Updated' — so the two boards in the same family contradict each other.", "evidence": "CMSRecordsModal.tsx:97-100 `const columns = (collection?.fields ?? []).slice(0, MAX_FIELD_COLUMNS)`; :344-359 thead = columns.map(f => f.name), then 'Updated', then the sr-only actions cell. Figma 1170:4749 thead children: 1170:4752 'Title', 1170:4753 'Price', 1170:4754 'Photo', 1170:4755 'Updated', 1744:8393 cell/actions.", "edit": "Change 1706:8399 to 'TITLE    PRICE    PHOTO    Updated' so the empty state matches its own populated sibling, or better, redraw it as separate th text nodes mirroring 1170:4751's structure. Drop 'STATUS'.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-A-20", "module": "content-cms", "action": "fix-board", "target": "1776:8376 / 1170:4713  (Content · collection-setup (modal)) — Select value 1753:8419", "claim": "The wizard's fourth field row offers type 'Rich text' in its Select.", "truth": "The wizard's Select has exactly five options and Rich text is not one of them, so this row cannot be produced. The Fields screen one step later does offer richtext — that is the screen the board is borrowing from.", "evidence": "packages/editor/src/editor/shell/modals/CMSCollectionSetupModal.tsx:24 `type FieldType = 'Text' | 'Number' | 'Image' | 'Date' | 'Boolean'` and :46 `const FIELD_TYPES: FieldType[] = ['Text','Number','Image','Date','Boolean']`; the typeMap at :167-173 maps exactly those five. Contrast ContentViews.tsx:441 `const FIELD_TYPES = ['text','textarea','richtext','number','boolean','image','date','slug','reference']` — the Fields screen. Prior screen finding: W-C-20.", "edit": "Set text node 1753:8419 from 'Rich text' to 'Text' (or 'Date' if the sample wants variety). The other three rows — Text, Number, Image — are already legal.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-A-21", "module": "content-cms", "action": "none", "target": "1776:8376 / 149:50  (Content · collection) — the absent delete-collection affordance", "claim": "No board in the Content section draws a way to delete a collection, or a confirm for one.", "truth": "Correct, and it must not be drawn. deleteCollection has NO caller anywhere in packages/editor — the whole chain below it exists (storage delete, the CMS_COLLECTION_DELETED event, the server mirror, the tRPC procedure) with no door, so a collection created in this product can never be removed from it. MOD-A-05 describes the consequences of a delete that the editor cannot perform.", "evidence": "grep -rn 'deleteCollection' over packages/editor/src excluding __tests__ returns exactly three lines, all definitions: CollectionStorage.ts:129, CollectionManager.ts:154, and :159 (the manager calling the storage). Zero call sites under editor/. By contrast deleteContentItem IS reachable — useContentPanel.ts:179 and CMSRecordsModal.tsx:168. useCmsSync.ts:93 subscribes to CMS_COLLECTION_DELETED and syncCollectionDelete exists, so the mirror is wired to an event nothing fires.", "edit": "NO EDIT. Do not add a delete-collection row or confirm to 149:50 or 148:2. Record the one-way door so the next wave does not read the absence as an oversight and design it.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-A-22", "module": "content-cms", "action": "none", "target": "1776:8376 / 148:2  (Content · root) — coverage row for the silent failures", "claim": "Four measured CMS defects produce no visible state at all, so no board can be right or wrong about them.", "truth": "Each one degrades silently: (a) a binding to a record outside the 50 most recently updated resolves to the fallback, because resolveBinding does an id lookup through a paged query that defaults to 50; (b) cross-device hydration is additive by collection id, so a device that has already seen a collection never re-reads it or its entries; (c) a CMS-only edit never marks the project dirty and never stamps site.lastEditedAt, so the publish flag and the client-approval staleness check both read as if nothing changed; (d) CMS_STORE_REFRESHED reaches only the Content panel, so an already-mounted binding popover, records modal or canvas preview keeps showing pre-hydration state. None of them renders an error, a badge or a banner.", "evidence": "(a) CMSBindingManager.ts:120-126 `queryContent({collectionId, filter:{}})` then `items.items.find(i => i.id === itemId)`; CollectionManager.ts:383 `const limit = options.limit || 50`; CollectionStorage.ts:185 sorts by updatedAt desc. (b) cmsSync.ts:124-126 `if (localIds.has(rc.id)) continue;` with the entries fetch at :145-154 inside the skipped branch. (c) CollectionManager.ts:35 — the class takes no composer and never calls markDirty (contrast CMSBindingManager.ts:222 which does); server/services/cms.service.ts:33-58 upsertCollection and :82-99 upsertEntry write cms_* tables only; publish.service.ts:174 and client-review.service.ts:171-172 both read site.lastEditedAt. (d) CollectionManager.ts:100 emits CMS_STORE_REFRESHED; the only listener is ContentTab.tsx:102. MOD-A-14, MOD-A-16, MOD-A-07/08, MOD-A-25.", "edit": "NO EDIT. Filed so the coordinator can see that four Criticals/Majors from MOD-A have no Figma action available — they need code, not a board.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-A-23", "module": "content-cms", "action": "none", "target": "1776:8376 / 1170:4749  (Content · records (modal)) — frame 1170:4771", "claim": "The modal's footer control 1170:4771 is already named '[not-implemented] Import JSON — CMSRecordsModal.tsx:396-402 documents it as not built'.", "truth": "Correct, correctly labelled, and the pattern this whole pass should copy. Keep it. W-C-14 flagged that the board is the only place the control exists; the naming convention is what stops that being a trap.", "evidence": "Figma 1170:4749 → `F 1170:4771 [not-implemented] Import JSON — CMSRecordsModal.tsx:396-402 documents it as not built` containing text 1170:4772 'Import JSON'. Code: CMSRecordsModal.tsx around :427 carries the matching comment. W-C-14.", "edit": "NO EDIT. Use this layer's naming as the template for the FIG-A-03 rename.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-A-24", "module": "content-cms", "action": "none", "target": "1776:8376 / 453:4010 and 775:4241  (Content · load-error, Content · loading)", "claim": "Both hydration states are drawn and their copy is exact.", "truth": "Verified string for string. 453:4010 carries 'Couldn't load your collections.' / 'This is a connection problem, not a change to your data.' / 'Try again'; 775:4241 keeps both section headers over three plus three skeleton rows, each with a row glyph. The code renders precisely that, for precisely the reason the caption gives.", "evidence": "Figma 453:4014, 789:4299, 453:4015; 775:4241 → 776:4095 COLLECTIONS + skeletons 775:4287/4290/4293, 776:4098 DATA + skeletons 775:4296/4299/4302. Code: ContentTab.tsx load-error branch (data-testid 'content-load-error', role='alert') with those three strings and `onClick={() => void retryCmsHydration()}`; the loading branch renders SectionHeader 'Collections' + three SkeletonRow widths w-40/w-24/w-44, SectionHeader 'Data' + w-28/w-20/w-36, each SkeletonRow being a 3.5-size glyph block plus a label bar. Only defect is the stray count — see FIG-A-12.", "edit": "NO EDIT beyond FIG-A-12.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-A-25", "module": "content-cms", "action": "wire-edge", "target": "1779:3 / 1177:4804  (Canvas · command palette ⌘⇧P) -> 1776:8376 / 1170:4749 (Content · records (modal))", "claim": "The records modal hangs off a reviewer state chip on 1170:4749 and has no inbound edge from any real door; the canvas command palette board draws no route to it.", "truth": "The command palette is the modal's ONLY door in the product — one producer, one consumer, verified by constant. The palette entry is id 'cms-records', label 'Manage CMS records', category 'Tools', keywords ['cms','content','collection','records','data']. Nothing else emits the event, and the inspector's binding popover explicitly tells users to go there.", "evidence": "EVENTS.CMS_MANAGE_RECORDS declared at shared/constants/events.ts:622 ('cms:manage-records'); emitted at editor/canvas/hooks/useCanvasCommandPalette.ts:171 `handler: () => composer.emit(EVENTS.CMS_MANAGE_RECORDS, {})` inside the entry defined at :165-172; consumed at editor/shell/hooks/useEditorEventListeners.ts:90-96. Searched by CONSTANT, not by call shape — those four lines are every occurrence in packages/editor/src. BindingPopover.tsx:294 'No records yet. Add records via the command palette -> \"Manage CMS Records\".' (note the product's own label is sentence-case 'Manage CMS records'). W-C-33, W-O-24.", "edit": "Add a 'Manage CMS records' result row to 1177:4804's results list (Tools category, 🗃 glyph) and wire On-click -> Navigate-to 1170:4749. This is the canvas palette (⌘⇧P), NOT the shell ⌘K family at 166:* — useCanvasCommandPalette is the canvas one. Do not wire a door from the Content rail: none exists in code.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-A-26", "module": "content-cms", "action": "none", "target": "1776:8376  (section 06 · Content — coverage row for FIG-CO-01's 4 outbound cross-section edges)", "claim": "Section 06 · Content has 109 inbound and 4 outbound cross-section edges, none reaching Pages, Publish, Preview or Inspector (coordinator measurement, FIG-CO-01).", "truth": "Only ONE of those four missing destinations is a navigation the product actually performs, and it is the reverse direction: the canvas command palette opens the records modal (FIG-A-25). Content -> Pages does not exist in code (the Pages panel has zero CMS references and nothing calls cms.dynamicPages). Content -> Publish does not exist (runPrePublishChecks has no CMS check). Content -> Inspector does not exist (bindings are written only from the inspector's own popover, never from the Content panel). The absent edges are faithful; wiring them would draw navigation the product cannot perform.", "evidence": "grep -ri 'cms|collection' over packages/editor/src/editor/sidebar/tabs/pages/ returns nothing; 'dynamicPages' returns zero hits in packages/editor/src (coordinator-confirmed). server/services/publish.service.ts:50-105 lists six checks, none CMS. The only element->field write in the product is BindingPopover.tsx:161 bindToField; the Content panel has no binding call at all. The one real cross-module hop is useCanvasCommandPalette.ts:171 -> useEditorEventListeners.ts:90-96.", "edit": "NO EDIT beyond FIG-A-25. For the Module Interaction Map board: the Content module's honest outbound degree is ONE (palette -> records modal), plus two in-section modal opens (root '+ New collection' -> 1170:4713, collection 'Dynamic pages' -> the board FIG-A-06 creates). Everything else the founder's chain implies is a code gap, not a prototype gap.", "severity": "Major", "confidence": "measured"}
