{"id": "D-X-01", "module": "(cross-module)", "node": "1776:8387", "screen": "rail wiring across the whole file", "q": "Q3", "verdict": "KEEP", "severity": "Critical", "finding": "The product's primary navigation \u2014 the six-tool rail \u2014 is prototyped on Settings boards and almost nowhere else. 39 of 45 Settings frames carry rail-wired hotspots; the rest of the file is Media 5/40, Brand 0/34, Layers 0/18, Shell 2/17, Pages 0/13, Journeys 1/74, and ZERO on Insert, Content, Publish, History, Review, AI, Inspector, Compare, Client sign-off, Notifications, Preview, Canvas and Ecommerce.", "evidence": "Per-module scan of reactions on frames + descendants, carriers named rail/Insert, rail/Layers, rail/Pages, rail/Media. 1090 cross-module edges across 161 distinct pairs; ~409 of them originate in Settings. COUNT CORRECTION (verifier): four of my seven per-module numbers were wrong and all four were too HIGH \u2014 Media 5 not 8, Brand 0 not 3, Layers 0 not 2, Pages 0 not 1. Settings 39/45, Shell 2/17, Journeys 1/74 and the thirteen zeros reproduce. The conclusion is strengthened, not weakened: the rail is prototyped even less widely than I said.", "recommend": "Wire the rail on one representative board per module at minimum, or extract the rail into a single component instance whose reactions travel with it. Until then, anyone walking the prototype can only navigate from Settings, and the file's own edge counts read as 'Settings is the product hub', which it is not."}
{"id": "D-X-02", "module": "(cross-module)", "node": "1776:8384", "screen": "Client sign-off \u00b7 10 screens", "q": "Q3", "verdict": "RETRACTED", "severity": "Minor", "finding": "RETRACTED. I filed Client sign-off as a terminal island because it is the only module with zero outbound cross-module edges. Agent D checked the code: the terminal state is CORRECT BY DESIGN. review-client.tsx:246-249 \u2014 'D \u00b7 approved, and E \u00b7 approved but edited since. Both are terminal for the client' \u2014 the status!==PENDING branch renders a heading, one paragraph and 'Signed as', zero controls, preview gone. A client who has decided is done. Zero outbound edges is the right shape, not a defect.", "evidence": "review-client.tsx:246-249 and :287-298. My original evidence (the cross-module matrix) was accurate; the INFERENCE from it was wrong. A module with no exit is only a defect if its user has somewhere to go, and this user does not.", "recommend": "Two real things survive. (1) Label 1339:7186 'TERMINAL by design' the way its sibling 1339:7193 already is, so the next graph audit does not re-file this. (2) The genuine gap is the AGENCY side, filed by agent D as D-D-18: no review event reaches notifications at all, and notifyReviewSubmitted (review.service.ts:568) filters the requester out of the recipients, so a one-seat agency is told nothing when a client approves."}
{"id": "D-X-03", "module": "(cross-module)", "node": "\u2014", "screen": "Settings/S7 as apparent hub", "q": "Q3", "verdict": "KEEP", "severity": "Minor", "finding": "The raw edge matrix reports Settings/S7 as the file's hub (94 edges to Journeys, 80 to Shell, ~39 to each of Pages/Insert/Layers/Media/Content/Brand). That is an artifact of D-X-01, not an IA fact: the counts are the repeated rail instance on 39 Settings boards.", "evidence": "39 of 45 Settings frames are rail-wired vs 0-8 elsewhere; the ~39-40 counts to each rail destination match the rail's six buttons across those boards.", "recommend": "Recorded so the next reader of the edge matrix does not conclude the product is settings-centred. Any future graph analysis should exclude rail-component reactions or count them once per component, not once per instance."}
{"id": "D-X-04", "module": "(missing)", "node": "\u2014", "screen": "Components \u2014 no module exists", "q": "Q5", "verdict": "RETRACTED", "severity": "Minor", "finding": "RETRACTED, twice over. I filed 'Components has no module and no boards anywhere'. The verifier found Components boards on page 1:3 itself (641:2546 library, 641:2599 detail, 642:3112 create, 778:4173 loading, 781:4433 load-error, 1138:13394 empty, 1170:4777 create-modal, 1170:4792 detach-confirm). Then I found the larger error: the file has ELEVEN pages and I audited one. Page 1:2 is '\ud83e\udde9 Components' with 93 children. Components is one of the best-covered areas in the file, not the worst.", "evidence": "figma.root.children: 0:1 Foundations(2), 1:2 Components(93), 1:3 Editor(27 sections), 1:4 Site(113), 1:5 Portfolio(70), 1:6 Client review(50), 1:7 Archive(159), 397:2 Dashboard spine(54), 500:2 Components 1(1), 510:2 Components 2(6), 988:2 Dashboard v2(175).", "recommend": "The surviving finding is much smaller and is filed as D-X-13: Components has no SECTION on the editor page, so an editor-page reader cannot find it. The real lesson is scope: I read CLAUDE.md's 'page 1:3' as the file's extent and never listed the pages. Every 'no board anywhere' claim in this audit was scoped to one of eleven pages."}
{"id": "D-X-05", "module": "(missing)", "node": "\u2014", "screen": "Templates \u2014 no product module exists", "q": "Q5", "verdict": "BUILD", "severity": "Major", "finding": "Templates has a 14-file editor surface including a preview modal and an apply-progress overlay, and the only Templates section in the file is a review scratch section (1084:4527, '\ud83d\udd0d REVIEW \u00b7 Templates + Components'), not a product module.", "evidence": "packages/editor/src/editor/sidebar/tabs/templates/ \u2014 14 files incl. TemplatePreviewModal.tsx, ApplyProgressOverlay.tsx, ApplyProgressOverlay.css; server/trpc/routers/templates.ts exposes 8 procedures; first run offers 'Browse templates' (807:4299).", "recommend": "Promote a real Templates module: browse, filter, preview, apply-with-progress, applied-result, and the failure state. Applying a template is destructive to existing content \u2014 that confirm has no board."}
{"id": "D-X-06", "module": "(missing)", "node": "\u2014", "screen": "Canvas comments \u2014 no module", "q": "Q5", "verdict": "BUILD", "severity": "Major", "finding": "Canvas comment mode is a shipping editor feature with a keyboard chord and its own canvas layer, and it has no boards. The Review module covers review THREADS in a panel, which is a different object from a pin dropped on the canvas.", "evidence": "editor/canvas/comments/CommentLayer.tsx; the entry point is the plain 'C' key (useEditorShortcuts.ts:106) plus a StudioHeader control; server/trpc/routers/comments.ts exposes 5 procedures.", "recommend": "Board the pin lifecycle: place, typing, posted, replied, resolved, and what a pin looks like to a client in the shared view. A feature whose only entry is an unlabelled keyboard chord is also worth an IA finding in its own right."}
{"id": "D-X-07", "module": "(checked, no gap)", "node": "\u2014", "screen": "Forms", "q": "Q5", "verdict": "KEEP", "severity": "Polish", "finding": "server/trpc/routers/forms.ts (5 procedures) has NO editor-side surface, so the absence of a Forms module in the editor file is correct, not a gap.", "evidence": "grep for FormSection / form-element / FormFieldInspector across editor/inspector and editor/sidebar returns nothing. Forms are a dashboard-side concern.", "recommend": "No action. Recorded so the next audit does not re-file it \u2014 a router without an editor surface is not an editor design gap."}
{"id": "D-X-08", "module": "Preview", "node": "817:4950", "screen": "Preview \u00b7 audit report", "q": "Q4", "verdict": "KEEP", "severity": "Critical", "finding": "INDEPENDENTLY CONFIRMED (agent E's D-E-29). The four headline scores on the Preview audit report are painted in the exact hex of the ellipse directly behind them \u2014 91/95/88 are #0e9f6e on a #0e9f6e disc, 72 is #c27803 on #c27803. Contrast 1.00:1. The screen that reports the site's accessibility score fails the check it reports.", "evidence": "Sibling-shape probe: text fill vs the topmost small shape containing the text centre. 91 text=#0e9f6e behind=#0e9f6e (Ellipse) SAME=true; same for 95, 88; 72 text=#c27803 behind=#c27803. All visible:true, opacity 1.", "recommend": "White or near-white numerals on the tinted disc. NOTE FOR FUTURE PROBES: my first check compared the text fill to the nearest ANCESTOR with a solid fill and returned bg=#ffffff, same=false \u2014 it would have cleared this defect. The disc is a SIBLING, not a parent. An ancestor-walk is the wrong instrument for 'what is behind this text'."}
{"id": "D-X-09", "module": "AI", "node": "170:2", "screen": "AI \u00b7 idle", "q": "Q4", "verdict": "KEEP", "severity": "Critical", "finding": "CONFIRMED at the call site (agent F's D-F-31): the copy 'Apply lands as one undo step' is true of the single-message path and FALSE of the multi-step agent run. useAgentRunner.ts:275-287 calls applyAiEdit once per approved step, and applyAiEdit opens exactly one transaction (applySetStyle.ts:642 beginTransaction('ai-edit')). One approve = one transaction = one undo entry, so a three-step run is three. The promise sits directly above the DRAFT row that launches the multi-step run.", "evidence": "useAgentRunner.ts:275-287 (approve -> applyAiEdit per step); applySetStyle.ts:642 (the single beginTransaction); the copy ships at EmptyThread.tsx:52 and ChatMessage.tsx:65.", "recommend": "Scope the copy to the path it describes \u2014 'A single edit applies as one undo step; a multi-step run applies one step at a time.' A user leaning on this before pressing Apply on a client's live site is being told the wrong thing in the expensive direction."}
{"id": "D-X-10", "module": "AI", "node": "\u2014", "screen": "AgentPlan.tsx:30-33", "q": "Q4", "verdict": "KEEP", "severity": "Minor", "finding": "The code comment that documents the undo behaviour is itself wrong about the mechanism. AgentPlan.tsx:30-33 says 'each approved step applies in its own transaction (applySetStyle opens one per edit)'. applySetStyle opens NO transaction \u2014 applySetStyle.ts:128-133 is just getElement + el.setStyle. The transaction is opened once by applyAiEdit, per approve call.", "evidence": "applySetStyle.ts:128-133 vs applySetStyle.ts:642. The conclusion the comment draws is right; the reason it gives is not.", "recommend": "Correct the comment to name applyAiEdit. This is the project's recurring defect class \u2014 a comment that outlives the code it describes. Two findings in this same audit were overturned because someone (me, twice) trusted a comment instead of the call site."}
{"id": "D-X-11", "module": "Inspector", "node": "1776:8381", "screen": "(rearrangement, executed)", "q": "Q3", "verdict": "FIXED", "severity": "Major", "finding": "CONFIRMED and ACTED ON (agent B's D-B-14). Ten boards named 'Inspector \u00b7 <state>', all 300x812, sat in the \ud83d\udcc4 Reference section instead of the Inspector module \u2014 including 159:99 'Select something on the canvas to edit it', the state the editor boots into. The Inspector module held six element-type profiles and a fallback; every state-bearing screen was filed elsewhere.", "evidence": "Parent-section probe: 159:99, 159:102, 159:123, 160:2, 160:105, 160:208, 160:313, 160:412, 160:512, 189:2 \u2014 all 300x812, all parented to \ud83d\udcc4 Reference. Moved into 1776:8381 with appendChild, which preserves node id, history and prototype links. Prototype edges before 2489, after 2489. Inspector is now 20 screens and its name says so.", "recommend": "Done. Remaining, not yet moved: five Inspector popovers/modals still in Notes, and 3 of Ecommerce's 5 screens. Those need a Q1 decision first \u2014 a popover may belong with its parent screen rather than in the module."}
{"id": "D-X-12", "module": "Shell", "node": "\u2014", "screen": "unsaved-settings dot", "q": "Q3", "verdict": "KEEP", "severity": "Major", "finding": "CONFIRMED at the call site (agent C's D-C-49). The unsaved-settings dot cannot render on the shipping rail. LeftSidebar.tsx:105 defines SETTINGS_DIRTY_SET as new Set([\"settings\"]) and passes it to the Figma rail at :628/:638; the rail renders a dot with dirtyTabIds?.has(tab.id) at :138 while iterating its OWN tabs, and RAIL_FIGMA (tabsConfig.ts:350) is exactly [add, layers, pages, assets, content, design]. No tab has id \"settings\", so the predicate is never true.", "evidence": "LeftSidebar.tsx:105,138,628,638; tabsConfig.ts:349-355. Settings is not on the rail at all \u2014 it opens from the site menu \u2014 so the prop is dead by construction, not by accident of ordering.", "recommend": "Decide where an unsaved-settings signal belongs now that Settings left the rail \u2014 the site menu row is the obvious candidate \u2014 then either wire it there or delete SETTINGS_DIRTY_SET and the prop. Do not just delete the dot without deciding: unsaved settings with no indicator is the worse half of this."}
{"id": "D-X-13", "module": "Components", "node": "1084:4527", "screen": "Components boards have no editor-page section", "q": "Q3", "verdict": "KEEP", "severity": "Major", "finding": "What survives of the retracted D-X-04. The editor page's Components boards (641:2546 library, 641:2599 detail, 642:3112 create, 778:4173 loading, 781:4433 load-error) live inside a section named '\ud83d\udd0d REVIEW \u00b7 Templates + Components', which is a review scratch area, not a product module. Two more sit in Insert and one in Notes. A reader of the editor page cannot find Components as a module because it is not filed as one.", "evidence": "Name-scan of page 1:3: the five boards are children of SECTION 1084:4527; 1138:13394 and 1170:4777 are in Insert; 1170:4792 in Notes. Page 1:2 '\ud83e\udde9 Components' (93 children) is the component LIBRARY page, a different concern from the editor's Components panel.", "recommend": "Promote a Components section on the editor page and move those eight boards into it, the way the ten Inspector states were moved (D-X-11). Check first whether page 1:2 already covers the panel's states, so the move consolidates rather than duplicates."}
{"id": "D-X-14", "module": "(scope)", "node": "\u2014", "screen": "the audit covered 1 of 11 pages", "q": "Q3", "verdict": "KEEP", "severity": "Critical", "finding": "SCOPE ERROR, mine. Six waves and 262 findings were scoped to page 1:3 because CLAUDE.md names that page as the rebuild checklist. The file has ELEVEN pages. Every 'no board anywhere' claim in this audit is therefore 'no board on one of eleven pages', and at least three findings were already refuted on exactly that basis (D-D-01, D-D-02, D-D-06 audited echo stubs while the canonical client screens live on page 1:6).", "evidence": "figma.root.children \u2014 0:1 Foundations 2, 1:2 Components 93, 1:3 Editor 27 sections/898 children, 1:4 Site 113, 1:5 Portfolio 70, 1:6 Client review 50, 1:7 Archive 159, 397:2 Dashboard spine 54, 500:2 Components 1, 510:2 Components 6, 988:2 Dashboard v2 175. Total outside the audited page: ~700 top-level nodes.", "recommend": "Audit 1:6 Client review (50) and 1:2 Components (93) first \u2014 they invalidate live findings. Then 1:4 Site, 1:5 Portfolio, 397:2 and 988:2 Dashboard. Until then, no cross-page absence claim in this audit may be quoted without the words 'on the editor page'."}
{"id": "D-X-15", "module": "(cross-module)", "node": "\u2014", "screen": "accent conformance, whole editor page", "q": "Q4", "verdict": "KEEP", "severity": "Major", "finding": "The editor page draws TWO accent blues. #1a56db (the token, --bk-accent) appears on 1355 nodes across 386 boards. #1c64f2 \u2014 flowbite blue-600, one step off \u2014 appears on 153 nodes across 82 LIVE boards, on nodes named Button and Checkbox, including Components \u00b7 library/detail/loading/load-error/create and Modal \u00b7 dirty. DESIGN.md allows one accent.", "evidence": "Full-page fill sweep of every descendant of every board, blue-ish fills only, then split by whether the parent board's name carries RETIRED/SUPERSEDED/not-implemented/UNBUILDABLE/dashboard-flow. 153 nodes / 82 boards are on live boards; 31 more are on retired or other-scope boards and are excluded. Token value from tokens.generated.css:81. CAUSE FOUND independently by agent I (D-I-04): it is not random drift but a one-step ramp shift. The boards run rest #1C64F2 -> hover #1A56DB; the tokens run rest #1A56DB -> hover #1E429F. The boards' HOVER is the token's REST, so every primary control in the file is drawn one step light on the blue ramp.", "recommend": "Shift the whole ramp back one step rather than repainting 153 nodes ad hoc: board rest becomes #1A56DB and board hover becomes #1E429F. Agent G measured this on 6 boards, I swept it to 153 across 82 live boards, and agent I explained it \u2014 three passes to get one finding right."}
{"id": "D-X-16", "module": "(checked, no defect)", "node": "183:2", "screen": "pure-blue hotspot rectangles", "q": "Q4", "verdict": "KEEP", "severity": "Polish", "finding": "NOT A DEFECT, recorded so it is not re-filed. A fill sweep flags 291 rectangles of pure #0000ff across 205 boards with paint alpha 1 \u2014 which reads as 291 solid blue blocks drawn on the boards. They are prototyping hotspots at node opacity 0.001.", "evidence": "183:2 'Modal \u00b7 open', node 'hotspot/alt \u00b7 S1 \u00b7 Editor \u2014 ASSEMBLED', 220x32, visible=true, opacity=0.0010000000474974513, frame itself visible=true opacity=1. All 291 are named hotspot/*.", "recommend": "No action on the file. The lesson is the instrument: my visibility check tested opacity === 0 exactly, and 0.001 is not 0, so a near-invisible node reported as fully visible. Any future fill sweep must treat opacity below ~0.01 as invisible."}
{"id": "D-X-17", "module": "(checked, no defect)", "node": "\u2014", "screen": "purple swatches on Media stock boards", "q": "Q4", "verdict": "KEEP", "severity": "Polish", "finding": "NOT A DEFECT, recorded so it is not re-filed. The fill sweep flags #a855f7 (purple-500) on 4 nodes, and DESIGN.md bans purple/violet/indigo. All four are named 'swatch' on Media \u00b7 stock \u00b7 photo / search / no-results / quota \u2014 a colour swatch representing a stock photograph is sample CONTENT, not chrome.", "evidence": "4 nodes, 4 boards, all named swatch, all on Media stock boards. The brief's own rule: sample data is never a finding, the SHAPE is the contract.", "recommend": "No action. Filing this would have been a banned-colour finding against a picture of a colour."}
{"id":"D-X-18","module":"Components","node":"1938:8372","screen":"(rearrangement, executed)","q":"Q3","verdict":"FIXED","severity":"Major","finding":"Executed D-X-13 on agent I's direct answer. The editor's eight Components boards were scattered across three homes — five in the '🔍 REVIEW · Templates + Components' scratch section, two in Insert, one in Notes. They are now a module: section 1938:8372, 'Components · 8 screens'.","evidence":"Moved 641:2546 (library), 641:2599 (detail), 642:3112 (create), 778:4173 (loading), 781:4433 (load-error) from 🔍 REVIEW; 1138:13394 (empty), 1170:4777 (create modal) from Insert; 1170:4792 (detach-confirm) from Notes. Prototype edges before 2489, after 2489.","recommend":"Wire the existing door — 20:84 'Components · 27 ›' in the Brand drawer on page 1:2 — to 641:2546. Agent I checked the alternative and ruled it out: page 1:2 is a component LIBRARY (118 component nodes, 0 product screens) and searching all 35 component-vocabulary TEXT hits found no saved-component card, library grid, create flow or panel state. Redirecting the module there would have left the panel undesigned."}
{"id":"D-X-19","module":"(file hygiene)","node":"500:2","screen":"two abandoned lookalike pages","q":"Q1","verdict":"FIXED","severity":"Minor","finding":"Pages '500:2 Components 1' and '510:2 Components 2' are not Buildrik libraries and never were. Neither contains a single COMPONENT or COMPONENT_SET — 500:2 is 152 plain frames of a third-party kit (a date picker reading 'August 2024', Button/Tonal and Button/Gray vocabulary this product does not use); 510:2 is 4902 frames of a purchased admin template (Sweet Alert, Swiper, Tippy, Lightbox, Nestable, Google Maps). Their names sit next to the real '🧩 Components' page and invite exactly the confusion that produced my retracted D-X-04.","evidence":"Agent I's full-depth node census of both pages: COMPONENT=0, COMPONENT_SET=0, INSTANCE=0 in each. 510:2's 4902 frames are the largest single source of noise in file-wide search.","recommend":"Done, as renames only — both now read '🗄️ ARCHIVE — <what it actually is> — audited 2026-09-04'. Never deleted, per the standing rule. Read back verified."}
{"id":"D-X-20","module":"Client review","node":"—","screen":"the client's page header","q":"Q4","verdict":"KEEP","severity":"Critical","finding":"CONFIRMED at the call site, and it is two defects, not one (agent H's D-H-31). review-client.tsx:90-91 renders `{agency}` followed by the literal 'is asking for your feedback'. Line 148 passes agency=\"Loading\", so the loading state of the external client's screen reads \"Loading is asking for your feedback\". Line 169 passes agency=\"Buildrick\", so the error state reads \"Buildrick is asking for your feedback\" — and the comment directly above the header says \"The agency's name leads, not ours. The client hired them, not us.\" The error path violates the principle written two lines above it.","evidence":"packages/dashboard/app/review/[token]/review-client.tsx:88-92 (header), :148 (Shell agency=\"Loading\"), :169 (Shell agency=\"Buildrick\"), :189 and :253 (the real agency). No canonical board on 1:6 carries this sentence at all; all sixteen S5.5 boards specify 'Reviewing: {Site} · v{n}'.","recommend":"Use the board's subtitle. Failing that, a skeleton for the loading state and the real agency name — never a placeholder string — in the error state. This is the one screen in the product seen by someone who is not a user, and it currently introduces the agency as 'Loading'."}
{"id":"D-X-21","module":"Foundations","node":"481:3979","screen":"Elevation · Motion · Z · Focus","q":"Q4","verdict":"KEEP","severity":"Critical","finding":"The focus ring has FIVE definitions and the root spec is the one being ignored. Page 0:1 Foundations — the file's foundation sheet — states 'FOCUS: 2px accent border/outline, no halo'. a11y.css:99-103 cites that spec BY NAME and obeys it (outline: 2px solid var(--bk-accent); outline-offset: 2px). But tokens.generated.css:203 defines --bk-shadow-focus as a halo (0 0 0 2px rgba(26,86,219,0.30)), buttonTheme.ts:36,40 applies that halo on every button, and all 17 focus variants drawn on page 1:2 use a DROP_SHADOW halo of spread 3. So on a button BOTH apply: an outline the spec asked for and a halo it forbade.","evidence":"Screenshotted 481:3979 and read it: 'FOCUS / 2px accent border/outline / no halo'. a11y.css:99 comment quotes the same words. tokens.generated.css:203; buttonTheme.ts:36,40 (tw:focus:ring-0 tw:focus:[box-shadow:var(--bk-shadow-focus)]). Agent I independently found the 17 drawn variants and page 1:2's own rule frame 1034:751, which states a THIRD shape (2px white offset + 2px accent).","recommend":"Foundations is the root and the most-repeated interaction state in the product should match it. Either change --bk-shadow-focus to the outline the spec describes, or amend Foundations — but one of the five has to become the source. Note the token is GENERATED from Figma, so fixing it means fixing the Figma variable, not the CSS."}
{"id":"D-X-22","module":"(page audit)","node":"1:7","screen":"🗃️ Archive page","q":"Q1","verdict":"KEEP","severity":"Polish","finding":"AUDITED, NO ACTION. Page 1:7 is a correctly-maintained archive: 158 of its 159 children carry a supersession marker in their own name, and the one exception is the page's own 'Archived Screens' label. Per the audit's own rule — a board that names its own retirement carries its explanation — none of these is a defect.","evidence":"Name scan of all 159 children: 158 match /\\[superseded|\\[retired|SUPERSEDED|RETIRED/, dated 2026-07-21 (31 with a parseable date) and 2026-08-05 (5). Unmarked: 1 TEXT node, 'Archived Screens'.","recommend":"No action. Recorded so this page is not re-audited: it is 159 nodes that would otherwise look like a large unexamined surface."}
{"id":"D-X-23","module":"(page audit)","node":"0:1","screen":"📕 Foundations page","q":"Q1","verdict":"KEEP","severity":"Minor","finding":"AUDITED. Two frames: a Foundations sheet (67:2) and 'Elevation · Motion · Z · Focus' (481:3979). The second is a genuine root spec and the product mostly follows it — motion fast 100 / base 160 / slow 240, ease-out cubic-bezier(0.2,0,0,1), 'no springs', which matches DESIGN.md's minimal-motion rule. Its z-scale (canvas 0 · chrome 10 · drawer 20 · topbar 30 · popover 40 · overlay 50 · modal 60 · cmdk 70 · toast 80 · tooltip 90) is a real contract that nothing in this audit has checked against the code.","evidence":"Screenshotted and read 481:3979.","recommend":"Two follow-ups: the focus rule on this sheet is being ignored by the token and the button theme (D-X-21), and the z-scale deserves a conformance sweep — a modal at the wrong layer is the kind of defect that only shows up when two overlays meet."}
{"id":"D-X-24","module":"(cross-module)","node":"481:3979","screen":"z-scale conformance","q":"Q4","verdict":"KEEP","severity":"Major","finding":"The Foundations sheet declares a ten-step z-scale — canvas 0 · chrome 10 · drawer 20 · topbar 30 · popover 40 · overlay 50 · modal 60 · cmdk 70 · toast 80 · tooltip 90 — and the code escalates straight past the top of it. Live values include 10000 (twice), 2000 (twice), 1000 (six times), 301, 300, 199, 100 (three times), plus the tell-tale one-above hacks 91 and 51. Layer order is being decided by escalation, not by the contract.","evidence":"Full sweep of z-index in CSS and tw:z-* in TSX across packages/editor/src. Above the scale: TemplatePreviewModal.css 1000, layers-v2.css 1000 x2, Canvas.css 1000 and 100, MediaTab.css 300/301/100, history.css 100, PageTabBar.tsx:471 tw:z-[10000], ImageEditorModal.tsx:306 tw:z-[10000], MediaContextMenu.tsx:137. One-above hacks: PagesTab.css:564 z-index 91 (one over tooltip), LibraryManager.css:457 z-index 51 (one over overlay). On-scale values do exist — 10, 20, 40, 50, 60, 70, 80, 90 — so the scale is known and partly used.","recommend":"The 91 and 51 are the diagnostic ones: someone hit a stacking conflict and added one rather than asking which layer the element belongs to. Map each off-scale value to its intended tier and replace with the token. Until then two overlays meeting is undefined behaviour — a tooltip at 90 loses to a modal at 1000, and an image editor at 10000 beats everything including the toast that would tell you it failed."}
{"id":"D-X-25","module":"Command palette","node":"1177:4804","screen":"Canvas · command palette (⌘⇧P)","q":"Q3","verdict":"KEEP","severity":"Critical","finding":"The canvas command palette ships an UNGATED 'Start collaboration session' command. Its handler calls composer.collab.manager.startSession(siteId, 'Editor') directly, with no feature check anywhere in the file. The gate exists and is honoured exactly one place — StudioHeader.tsx:217 reads isFeatureEnabled('collab') for the Collaborate CTA — so this is a bypass of an existing guard, not a missing one. Real-time collaboration is demo-only by founder decision: last-write-wins with six known non-convergence P1 bugs, and CLAUDE.md's env table says of NEXT_PUBLIC_FEATURE_COLLAB, 'Leave off — never in production'.","evidence":"editor/canvas/hooks/useCanvasCommandPalette.ts:180-190 — the command object has id/label/category/icon/keywords/handler and no guard; grep for features.collab / isFeatureEnabled(\"collab\") / flags.collab across packages/editor/src returns exactly one hit, StudioHeader.tsx:217. The command is 1 of the 25 the board's own footer counts, and the palette opens on ⌘⇧P for every user.","recommend":"Gate the command behind isFeatureEnabled('collab') the way the header CTA is, or drop it from the registry. As it stands a user in production can press ⌘⇧P, type 'collab', and start a multiplayer session on a client's live site — through a feature the founder has said must never ship. The handler also ends in .catch(() => {}), so if it fails the user is told nothing."}
{"id":"D-X-26","module":"Dashboard","node":"397:2","screen":"the two Dashboard pages","q":"Q1","verdict":"FIXED","severity":"Critical","finding":"CONFIRMED and CORRECTED (agent L's D-L-01/02). The page labels were INVERTED: '🏠 Dashboard — spine (code-canonical)' is not code-canonical, and 'Dashboard v2 — screens' is. Verified myself on two of L's three proofs — nav.ts lists sidebar Home/Getting started/Sites/Agency/Media/Settings plus Marketplace/Learn/Resources/Templates in a top nav, which is what v2's 988:3 draws and what the spine's shared shell 853:209 contradicts (it puts Templates and Notifications in the sidebar and promotes Members and Billing); and tab-nav.tsx defines 8 site-detail tabs (access, analytics, domains, feedback, overview, redirects, seo, settings) which v2's 991:203 draws in order while the spine's 398:3809 draws five accordion rows, three named things that are not tabs in code. v2's own caption 1025:5902 calls 397:2 'the old spine'.","evidence":"packages/dashboard/components/dashboard/shell/nav.ts; packages/dashboard/.../tab-nav.tsx; agent L additionally matched settings-sections.ts (14 sections in 6 groups) to v2's 993:360 while the spine has no Settings directory at all.","recommend":"Both page names corrected in place, with the evidence in the name. The correction is load-bearing: a designer opening this file would have trusted the label and built the wrong dashboard. NOT archived — see D-X-27."}
{"id":"D-X-27","module":"Dashboard","node":"397:2","screen":"old spine — do not archive yet","q":"Q5","verdict":"KEEP","severity":"Major","finding":"The old spine cannot simply be retired: it is the file's ONLY record for 11 shipping jobs. A name sweep across all eleven pages returns a single hit each for /auth/signup, /auth/forgot-password, /auth/check-inbox, /auth/verify-email, /auth/magic-link/sent, /auth/workspace-select, /auth/2fa, /onboarding/ai/preview, plus AI generation-failed, Media empty and two sign-in sub-states. Archiving it on the strength of the supersession verdict would delete coverage, not duplication.","evidence":"Agent L's cross-page name sweep. All eight /auth/* routes ship.","recommend":"Drain first, archive second: port those 11 screens into v2 (or a dedicated Auth page), verify each against its route, and only then mark the spine. The page name now carries 'DO NOT ARCHIVE YET' so the next person does not act on the supersession alone."}
{"id":"D-X-28","module":"Portfolio","node":"—","screen":"BrandPush · theme.push","q":"Q5","verdict":"KEEP","severity":"Critical","finding":"CONFIRMED at the schema (agent K's D-K-24), and the risk gradient is INVERTED. theme.push overwrites the brand of every site it touches. Its input makes siteIds OPTIONAL with NO maximum (packages/shared/schemas/theme.ts:22 — z.array(z.string().min(1)).min(1).optional()), and theme.service.ts:151 applies the id filter only when it is present (...(siteIds ? { id: { in: siteIds } } : {})), so omitting it targets EVERY site in the workspace. The service's own comment at :132 says as much. Meanwhile the far safer sites.bulk operation IS capped: packages/shared/schemas/sites.ts:47 is .min(1).max(25). The destructive call is unbounded and defaults to all; the tame one is bounded at 25.","evidence":"schemas/theme.ts:22 vs schemas/sites.ts:47; theme.service.ts:132 (comment), :140, :151. Every BrandPush board draws a tidy 4-site selection, so nothing in the design shows the unbounded case.","recommend":"Cap theme.push, and make the all-sites case explicit rather than the default of an absent field — an operation that rewrites every client site in a workspace should require naming that it is doing so. The design should draw the unbounded case too; today it only draws four sites."}
{"id":"D-X-29","module":"Portfolio","node":"178:512","screen":"BrandPush · type-to-confirm","q":"Q4","verdict":"KEEP","severity":"Critical","finding":"The type-to-confirm gate has no disabled state, and the board argues for the lock it then fails to draw. 178:512 (empty field) and 179:2 (field typed 'PUSH') are pixel-identical in the action row — the same saturated red Push button at 896,625. The board's own annotation reads 'A checkbox is not enough here — this is the only surface that can damage a site the designer is not looking at.' Filed by agent K as D-K-12; paired here with D-X-28 because they compound: an unbounded destructive call behind a confirm that never appears locked.","evidence":"Agent K screenshotted and compared both boards; the action row is identical in position and colour.","recommend":"Draw the disabled state, and show which sites are affected — previewSharedThemePush already returns them per site, and the screen names counts (4 sites / 27 pages / 312 elements) without ever naming a site."}
{"id":"D-X-30","module":"Site","node":"1:4","screen":"caption grid vs board grid","q":"Q4","verdict":"FIXED","severity":"Critical","finding":"CONFIRMED and FIXED (agent J's D-J-16). On page 1:4 the boards were drawn on a 1520px x-step and the captions on their own grid, so 24 of 66 text nodes sat ON TOP of a board — and every sampled one named a DIFFERENT board than the one it covered: 'caption/Domains · none' was drawn over 'Export · format-selected', 'caption/Domains · adding' over 'Export · generating', 'caption/Domains · pending-dns' over 'Export · idle'. A caption sitting on the wrong board does not read as untidy, it reads as a description of that board.","evidence":"Overlap probe before: 24 captions intersecting a board, board x-step 1520 vs caption x-step 82. After the layout pass: boards=47, text=66, captions=32, paired=32, unmatched=0, overlaps=0, captions-on-the-wrong-board=0.","recommend":"Done, by the same rules used on the editor page: exact normalised name match only, duplicate names dropped rather than guessed. The fix also covers the 34 non-caption text nodes — leaving them out of the first pass left them free to overlap the boards it had just tidied, which is how the defect would have partly returned."}
{"id":"D-X-31","module":"Portfolio","node":"1:5","screen":"page layout","q":"Q4","verdict":"FIXED","severity":"Minor","finding":"Page 1:5 gridded with the same tool as a preventive pass: 33 boards, 37 text nodes, 27 captions all paired to the board whose name they match, 0 overlaps, 0 captions on the wrong board. Page 0:1 Foundations checked too and needed nothing (2 boards, no captions).","evidence":"layout-page.mjs run on 1:5 and 0:1; counts re-derived after the pass.","recommend":"No further action. Recorded so the three section-less pages are known to have been laid out, not merely audited."}
{"id":"D-X-32","module":"Pages","node":"—","screen":"bulk-mode checkbox gate","q":"Q4","verdict":"KEEP","severity":"Major","finding":"A REFUTATION EXPOSED A PRODUCT BUG. Verifying D-F-12 (which claimed a board draws a checkbox column the product hides) showed the finding false and the CODE broken: PagesTab.tsx:214 applies `.bulk-mode` to the panel when a selection exists, and NO stylesheet in packages/editor/src keys off that class — grep for 'bulk-mode' across every .css file returns nothing. Three separate comments assert the gate: PagesTab.tsx:211, PageRow.tsx:6 ('The bd-pg-panel.bulk-mode parent gates the row checkbox via CSS'), and PageRow.tsx:207 ('visible when .bd-pg-panel.bulk-mode is active').","evidence":"grep -rn 'bulk-mode' --include='*.css' packages/editor/src -> no matches. The class is applied at PagesTab.tsx:214 and documented three times. Board and shipped code agree, which is why D-F-12 was refuted — the board draws the ungated state because the ungated state is what ships.","recommend":"Either write the rule the comments describe or delete the class and the three comments. Right now the codebase documents a behaviour it does not have, which is how D-F-12 came to be filed against the design instead of the code — the auditor trusted the comment, exactly the failure mode that has bitten this audit repeatedly."}
{"id":"D-X-33","module":"Brand","node":"1175:4804","screen":"the paywall modal and its only producer","q":"Q3","verdict":"KEEP","severity":"Critical","finding":"TWO AGENTS FOUND THE TWO HALVES OF ONE DEFECT AND NEITHER KNEW. D-B-01 says Brand board 154:132 hangs a Pro paywall hotspot on a free localStorage display preference, targeting 1175:4804. D-C-39 says 1175:4804 is misnamed a '403 gate' and its ONLY inbound edge is that same hotspot from 154:132. Together: the Brand board is the sole producer of a modal that only two premium-template clicks (TemplatesTab.tsx:177,188) are meant to open, and no 403 interceptor exists anywhere.","evidence":"D-B-01 + D-C-39, filed independently by agents B and C, neither citing the other; the merge was found by the verification pass. DSModeContext.tsx documents the mode as DISPLAY-ONLY, persisted per-user under buildrik:ds-mode.","recommend":"Fix them as one change. Removing the paywall from 154:132 alone orphans the modal; renaming the modal alone leaves a paywall on a free preference. This is the argument for a verification pass that reads ACROSS waves — no single module agent could have seen it."}
{"id":"D-X-34","module":"(method)","node":"—","screen":"edge counts under-report on a cold walk","q":"Q3","verdict":"KEEP","severity":"Major","finding":"Instance sublayers load lazily, so a section-scoped reaction walk under-reports. The G/H/I verifier measured 205, 210, 205 edges on three runs of the same section before forcing getMainComponentAsync() on every instance in a warm-up pass; four topbar carriers on 52:2 and a pill on 129:223 silently vanished on cold runs. MY OWN page-wide count is unaffected, and I measured that rather than assuming it: cold 2489 edges / 36257 carriers, warm 2414 instances, warm 2489 / 36257 — delta 0.","evidence":"Verifier's three-run variance on the Journeys section; my warm-vs-cold probe over page 1:3, run after their report.","recommend":"Any future edge count on this file warms instances first. The page-wide walk used for every rearrangement verification in this session happens to touch every node via findAll, which forces the load — but that is a property of that walk, not a general guarantee, and a narrower probe will silently give a smaller number."}
{"id":"D-X-35","module":"Settings","node":"—","screen":"agent C contradicts itself on Integrations","q":"Q5","verdict":"KEEP","severity":"Minor","finding":"D-C-05 establishes that the editor's Integrations screen is six static status='soon' link-outs with no connect flow (IntegrationsScreen.tsx:38). D-C-18 then names 'Integrations, whose OAuth connect is the most failure-prone job in the module' as the module's real state-coverage gap. There is no OAuth connect there to fail. D-C-18's state census is exact; only its conclusion rests on the premise D-C-05 already demolished.","evidence":"Found by the verification pass reading both rows together; neither cites the other.","recommend":"Keep D-C-18's census, drop its conclusion, and re-point the state-coverage argument at a job that exists. Recorded because a single agent contradicting itself across 49 findings is a real risk of large per-agent batches."}
{"id":"D-X-36","module":"(design system)","node":"2034:8519","screen":"Rail component","q":"Q4","verdict":"FIXED","severity":"Critical","finding":"The product had no Rail component, so the editor's left rail existed as 99 hand-made copies — change it once, change it ninety-nine times. Measured: the editor page carried 102 instances of a shared component against 1,619 detached copies. The topbar IS componentised (681:27, five Publish/Review variants, 102 instances); the rail was not, and the only rail-shaped thing on the Components page (11:36 'Rail icons') is a 312x72 FRAME of loose icons, not a component. Built the Rail set (2034:8519, 7 Active variants) from a REAL board rather than drawing a new one, and swapped all 99 copies to instances.","evidence":"Shell census before: instances 102 / local copies 1619, including 99x rail (FRAME 60w) and 99x each of its six buttons. After: instances 201, and the local rail frames are gone (56 Active=None + 43 Active=Layers = 99). Page invariants held throughout: 29 sections, 0 loose, 0 out of bounds, 0 overlaps.","recommend":"Done for the rail. The same disease remains in two places: 615 local 'nav' FRAMEs (140w) on the Settings boards, and page headers drawn locally at four widths on Dashboard v2 (1440/1067/642/401)."}
{"id":"D-X-37","module":"(design system)","node":"2034:8519","screen":"rail navigation","q":"Q3","verdict":"FIXED","severity":"Critical","finding":"D-X-01 IS FIXED AS A SIDE EFFECT. The rail was prototyped on Settings boards and almost nowhere else — 53 of 99 rails carried edges, 234 of them in Settings. Before swapping I checked whether the rail means the same thing everywhere: across those 53, EVERY button had exactly ONE distinct destination (Insert->137:2, Layers->142:2, Pages->140:2, Media->144:2, Content->148:2, Brand->1333:7162), so the wiring is a property of the rail and not of the board. That let the component own the navigation. Now all 99 boards with a rail can navigate, not 53.","evidence":"Destination census across all wired rails: 6 buttons x 1 distinct destination each over 53 edges apiece. After the swap: 99 boards with a Rail instance, 594 rail edges, and 99 of 99 fully wired. Page edges 2489 -> 2807, which reconciles exactly as 2489 - 318 (old rail edges) + 594 (99x6) + 42 (the component set's own).","recommend":"None. Recorded because the reconciliation is the proof: an unexplained edge delta after a bulk swap would mean wiring was silently lost."}
{"id":"D-X-38","module":"(design system)","node":"2041:19572","screen":"Settings nav row component","q":"Q4","verdict":"FIXED","severity":"Critical","finding":"615 frames named 'Nav · <Section>' existed as hand-made copies — 585 across the 45 Settings boards (15 rows each) and 30 on archived boards. Each is 140x30 with one TEXT child; active is a #ebf5ff box plus a #1a56db label, inactive is no fill and #4b5563. Built ONE component with an Active axis and the label as a TEXT OVERRIDE rather than 15 labels x 2 states = 30 variants: the label is content, the state is design. All 585 live rows swapped, each keeping its own label.","evidence":"Census: 585 in Settings, 30 in Archive. 15 distinct labels — General, Branding, SEO, Export, Domains, Analytics, Localization, Custom code, Redirects, Headers, Forms, Integrations, Webhooks, Members ↗, Billing ↗. After: swapped=585, labelled=585, and the General board renders all 15 rows with the correct labels and grouping.","recommend":"Done for the editor. The cross-page question remains open and is a design decision, not a cleanup: THREE nav-row designs serve one job across the product — 140x30 here, 240x32 on Site/Portfolio (component 16:19, ~900 instances), 265x30 on Dashboard v2 (component 975:615, 251 instances). One component with a filling width would serve all three; whether the product wants that is the founder's call."}
{"id":"D-X-39","module":"(method)","node":"—","screen":"I destroyed 547 prototype edges","q":"Q3","verdict":"FIXED","severity":"Critical","finding":"MY OWN ERROR, recorded because the recovery is only luck-adjacent. Before swapping the RAIL I measured whether its copies carried prototype edges and whether their destinations agreed — they did, so the component could own the wiring. I then swapped the 585 Settings nav rows WITHOUT running that check. They carried 547 edges. The page went 2807 -> 2260 in one call.","evidence":"Edge count immediately after the nav swap: 2260, against 2807 before. Recovery was possible only because the swap was scoped to the live Settings boards, leaving the 30 copies on ARCHIVED boards untouched — and those preserved the entire label->destination map, all 15 labels with exactly one destination each. Rewired: 573 instances, plus 12 correctly skipped as self-referencing (the active row on each board points at that board, and Figma rejects a NAVIGATE to the same top-level frame). Final 2833, above the 2807 baseline because rows that were never wired now are.","recommend":"The check that saved the rail must run before EVERY bulk swap: does this element carry edges, and do its destinations agree? Note the two answers differ by element — the rail's six buttons have one destination each so the COMPONENT owns the wiring; a nav row serves 15 destinations so the INSTANCE must. Scoping a swap narrowly is what made this recoverable at all."}
{"id":"D-X-40","module":"Shell","node":"681:103","screen":"topbar save/presence states","q":"Q4","verdict":"FIXED","severity":"Critical","finding":"The Topbar component ships two INSTANCE_SWAP properties — Save (6 states: saved, saving, unsaved, conflict, offline, error) and Presence (5) — and ALL 102 instances used the default. Nine designed states, zero uses. So the board named 'Shell state 10 · Offline' and the board named 'Shell state 11 · Saving → conflict' both read 'Saved 2m ago', and so did First run, a project that has never been saved. Two independent audits reached this from different directions: a Shell module pass found the string on all twelve full-shell boards, a chrome-consistency pass found the properties untouched at 102/102.","evidence":"componentPropertyDefinitions on set 681:103: Save#698:0 INSTANCE_SWAP default 697:444, Presence#695:0 default 692:421. Save status set 697:461 has 6 states; Presence set 692:472 has 5. Applied to 10 boards whose OWN NAME declares a state; the Offline board now reads 'Offline — saved locally'.","recommend":"Scope was deliberately narrow — name-declared boards only, because inferring a save state from a board's contents is guessing. The Presence axis is still unused at 102/102 and needs the same treatment once someone decides which boards are meant to show collaborators."}
{"id":"D-X-41","module":"(method)","node":"—","screen":"read the whole board name","q":"Q4","verdict":"KEEP","severity":"Minor","finding":"A near-miss worth recording. My rule matched /offline/ and would have set 'S1.2e - Offline - Connection restored' to the offline state — the board is the RECOVERY state and its own title says so. A guard now skips names containing restored/reconnect/recovered/resolved.","evidence":"Dry run listed 11 boards; the guard reduced it to 10. The excluded board is the one whose name continues past the word the rule matched.","recommend":"Any name-driven bulk edit reads the WHOLE name, not the first token that matches. This is the same shape as the earlier defect where a rank rule matched 'drawer' inside 'Shell state 5 · Drawer closed'."}
{"id":"D-X-42","module":"(design system)","node":"9:102","screen":"the second accent, root cause and fix","q":"Q4","verdict":"FIXED","severity":"Critical","finding":"THE SECOND ACCENT IS GONE — 153 nodes across 82 live boards to ZERO. The root cause was two variant fills: the Button component set bound Kind=primary/State=rest to flowbite/blue/600 (#1C64F2) and State=hover to flowbite/blue/700, exactly one rung below the token chain, where color/accent resolves to blue/700 (#1a56db) and color/accent-hover to blue/800 (#1e429f). The boards' hover WAS the token's rest. 16 more library nodes had the same disease — Row active bars (5 sizes), Checkbox checked and indeterminate, Toggle on, Radio on, Slider track — and 7 hand-drawn buttons on Settings boards were never instances at all.","evidence":"Alias chains resolved before touching anything: color/accent -> flowbite/blue/700 = #1a56db, color/accent-hover -> flowbite/blue/800 = #1e429f, matching --bk-accent and --bk-accent-hover in tokens.generated.css. Sweep after each step: 153 nodes/82 boards -> 53/23 (Button master, 4 fills) -> 7/7 (16 library nodes) -> 0/0 (7 detached buttons). Page invariants unchanged: 29 sections, 0 loose, 0 out of bounds, 0 overlaps, 2833 edges.","recommend":"Done. Three separate audits had to converge before this was fixable: one measured 6 boards, one swept 153, one named the ramp shift, and the typography agent finally located the two fills that caused it. Instances were deliberately skipped when rebinding — rebinding an instance bakes in an override that hides the next drift."}
{"id":"D-X-43","module":"Client review","node":"2059:7457","screen":"client-review chrome componentised","q":"Q4","verdict":"FIXED","severity":"Critical","finding":"Page 1:6 — the external client's entire experience, the one surface in this product seen by someone who is not a user — carried 39 chrome bars and ZERO instances. Now 39 instances, 0 local copies. Four sets built, with variant axes chosen from measured content variance rather than guessed: Branded bar (16 bars, 2 signatures -> Greeting = none/returning), Mode strip (10 bars, 1 signature -> no variants), Sticky footer (10 bars, 4 signatures -> State = pending/approved/approved-edited/changes-requested, matching four real review states), Agency header (3 bars, 1 signature).","evidence":"Census before: Branded bar 1280x56 x16, Mode strip 1280x44 x10, Sticky footer 1280x72 x10, Agency header 1280x56 x3, 0 instances. After: chrome instances=39, local copies=0. Wiring: 8 bars carried 11 edges, captured before each swap and replayed onto the instance — 11 of 11. The canonical board 23:21 renders unchanged, with the Approve button now carrying the unified accent.","recommend":"Two things this surfaced and did not fix. The Sticky footer's four states map 1:1 to review states the code implements, so the component is now the place to check that coverage. And 'Agency header' is 1280x56 like 'Branded bar' but draws the literal placeholder {Agency} — the board-side twin of the shipping defect where review-client.tsx passes agency=\\"Loading\\". Whether they are one component is a design decision, so they were built as two rather than merged silently."}
{"id":"D-X-44","module":"(method)","node":"—","screen":"capture wiring before, not after","q":"Q3","verdict":"FIXED","severity":"Major","finding":"The method that failed on the Settings nav is now built into the swap. There, wiring was recovered AFTER the fact and only because copies happened to survive on archived boards — luck, not method. Here each bar's reactions are read immediately before it is replaced and re-applied to the instance that takes its place. 11 of 11 edges survived.","evidence":"swap-client-chrome.mjs captures edges per bar inside the same loop iteration that replaces it. Dry run reported swapped=39 edgesReplayed=11 before any write; the applied run matched exactly.","recommend":"Also fixed a second-order bug worth naming: removing a node while iterating a descendant snapshot leaves the loop holding ids that no longer resolve ('The node with id 23:4 does not exist'). Collect every target first, then mutate."}
{"id":"D-X-45","module":"(design system)","node":"—","screen":"type-style adoption","q":"Q4","verdict":"FIXED","severity":"Major","finding":"Type-style binding raised across every product page, with zero visual change verified independently. Editor 46.6% -> 59.6%, Components 17.4% -> 60.6%, Site 68.6% -> 70.3%, Dashboard v2 0% -> 21.2% (by the typography agent), Portfolio already 89.9%. Binding was performed ONLY where every typographic property already equalled the style's; mismatches were skipped and counted, never coerced, because binding a node whose values differ is a redesign wearing a cleanup's clothes.","evidence":"After the pass, 10,221 bound nodes re-read across Editor, Components and Dashboard v2 and compared property-by-property against their style: 0 diverged. Instance children were deliberately skipped throughout — binding one bakes an override that hides the next drift.","recommend":"The remaining unbound are genuinely unbindable without a design decision: AUTO line-heights, populations that sit 1-2px off a style, and instance children whose masters must be fixed instead."}
{"id":"D-X-46","module":"(design system)","node":"—","screen":"ui/14 · panel title leading","q":"Q4","verdict":"FIXED","severity":"Major","finding":"CORRECTED A STYLE TOWARD THE DOCUMENTED SPEC, and it is a deliberate 1px visual change. The Figma style 'ui/14 · panel title' was at 14/21. DESIGN.md:148 states the ramp as 'ui/14 panel title (14/20, 600)', and --bk-leading-20 exists in the generated tokens. So the STYLE was the outlier, and the largest unbound exact combination in the file — 857 nodes at Inter Medium 14/20 — missed it by one pixel. Corrected to 20.","evidence":"DESIGN.md:148 quoted verbatim; tokens.generated.css:174 --bk-leading-20: 20px. A --bk-leading-21 token also exists (line 175), so 21 is a legal value — it is simply not what the ramp specifies for this style. The typography-adoption agent found the mismatch and correctly declined to make the change unilaterally, since it moves 86 already-bound nodes by 1px.","recommend":"Stated plainly because it IS a visual change: 86 nodes lost 1px of leading. It was made because CLAUDE.md treats DESIGN.md as the approved visual spec, so aligning Figma to it is a correction and the previous state was the deviation. If the founder wants 21, DESIGN.md:148 is the thing to change, and the style should follow it rather than lead."}
{"id":"D-X-47","module":"Inspector","node":"1776:8381","screen":"profile boards given row structure and auto-layout","q":"Q4","verdict":"FIXED","severity":"Major","finding":"Six of the seven element-type profile boards were layoutMode=NONE with 36-58 loose absolutely-positioned children — TEXT labels and unnamed control frames at bare coordinates — while the seventh, CONTAINER, was already VERTICAL auto-layout with children named 'row'. CONTAINER was the pattern and the other six were the legacy form. All six now have row structure and vertical auto-layout: TEXT 58 children -> 27 rows, FLEX 49 -> 26, BUTTON 38 -> 21, GRID 38 -> 21, MEDIA 38 -> 20, INPUT 36 -> 20. The Inspector section went from 10 NONE / 15 VERTICAL to 4 NONE / 21 VERTICAL; the 4 remaining are modals and popovers, which are not row-structured surfaces.","evidence":"Done in two verifiable steps rather than one — group into rows at existing coordinates (structure appears, nothing moves), then switch to auto-layout. The BUTTON board was screenshotted before and after step 1: pixel-identical. Page invariants held: 0 loose, 0 out of bounds, 0 overlaps, 2833 edges.","recommend":"This is the prerequisite agent N named for an Inspector component: rows now exist to become an 'Inspector / Section row' set with State = collapsed/expanded. The seven profiles match elementProfiles.ts 1:1, so the component's variant axis is Profile and the SSOT stays in code."}
{"id":"D-X-48","module":"Inspector","node":"32:2","screen":"two mistakes in the auto-layout pass","q":"Q4","verdict":"FIXED","severity":"Minor","finding":"Recording two errors of mine rather than only the result. (1) The first auto-layout attempt shifted the whole BUTTON board up 8px: my band sizing resized newly-created rows to absorb the gap to the next row but SKIPPED bands that were already a single row/header frame, so one 8px gap survived and auto-layout collapsed it. Caught by comparing child y-positions before and after, reverted from the captured positions, fixed, re-run. (2) My height-normalising filter then caught 'Inspector · profile · CONTAINER', which was legitimately 776, and resized it to 812.","evidence":"Position strings captured before the reflow made the revert exact — restored 300x812 with every row back at its original y, verified by string equality. CONTAINER at 812 renders correctly with no clipping and is now consistent with its 17 siblings at 300x812, so it was left rather than reverted.","recommend":"Both are the same lesson in different clothes: a bulk geometry edit needs its own before/after comparison, not just a screenshot at the end. The gap bug was invisible in a single screenshot and obvious in a y-position diff."}
{"id":"D-X-49","module":"(design system)","node":"249:6","screen":"Format row adopts the Radio component","q":"Q4","verdict":"FIXED","severity":"Minor","finding":"A forms/tables census found three library components with real variant sets and ZERO product instances while the shapes they describe are hand-drawn across the file: Radio (14:20, off/on/focus/disabled), Slider (92:30, rest/dragging/disabled), Status dot (10:27, five states). The Format row MASTER drew its radio by hand — a 16x16 frame holding a white 16x16 ellipse and an 8x8 accent dot — so one master edit reaches every Format row instance. Swapped for a Radio instance.","evidence":"Radio State=on is 16x16 with an 8x8 accent ellipse, structurally identical to the drawing. Screenshotted before and after; the corrected result is pixel-identical to the original.","recommend":"Two atoms remain unadopted and are the same cheap shape of win: ~50 hand-drawn sliders against Slider (92:30) and 3 local dots against Status dot (10:27). The census also names the larger job — 955 of 1,153 form controls and 222 of 262 table structures are hand-drawn, and Row (8:47) already carries the exact Size axis the code tokenises at tokens.generated.css:123-127."}
{"id":"D-X-50","module":"(method)","node":"14:20","screen":"z-order decides what a drawing looks like","q":"Q4","verdict":"FIXED","severity":"Minor","finding":"I swapped the hand-drawn radio for Radio/State=ON because the layer tree showed a white 16x16 ellipse AND an 8x8 accent dot — the anatomy of a checked radio. The before/after screenshots disagreed: the original rendered UNCHECKED, because the white ellipse sat ABOVE the accent dot and hid it. Corrected to State=off, which matches the original exactly.","evidence":"Before: empty white circle. After (wrong): filled blue radio. After (corrected): empty white circle, identical to before.","recommend":"A layer tree lists what exists, not what is visible. Two shapes in a parent tell you nothing about which one the eye sees — z-order does, and it is not in the tree dump. Any component swap justified by 'structurally the same' needs the render either side, which is the only reason this was caught."}
{"id":"D-X-51","module":"Client review","node":"—","screen":"my swap destroyed 3 edges by calling setReactionsAsync per edge","q":"Q3","verdict":"FIXED","severity":"Critical","finding":"THE FINAL QA CAUGHT A BUG IN MY OWN SCRIPT, and proved it rather than suspecting it. swap-client-chrome.mjs called setReactionsAsync INSIDE the per-edge loop. That call replaces the whole reaction array, so a bar with N edges ended with one while the counter incremented N times — which is exactly why the run reported 'edgesReplayed=11' when 8 survived. 3 edges destroyed by a script written specifically to stop edges being destroyed.","evidence":"QA measured 8 surviving edges against my claim of 11 and located the cause at swap-client-chrome.mjs:75-83. Script fixed to accumulate the list and write once. Restoration narrowed to the exact signature of the damage — a pending footer whose ONLY edge is the Approve one — so footers that were never wired were left alone rather than given invented navigation. Client chrome now carries 12 edges over 8 bars; page 1:6 has 76 edges, 0 dangling, 0 off-page destinations.","recommend":"Stated as a RECONSTRUCTION, not a recovery: the exact lost destinations are gone. What was restored is what a pending footer means — it draws both Request changes and Approve, so it must reach both. One of the four repaired footers (brand-colour) probably had one edge originally and now has two, which is a gap of the same kind rather than a restoration."}
{"id":"D-X-52","module":"(design system)","node":"16:6","screen":"panel header — QA's recommendation corrected","q":"Q4","verdict":"KEEP","severity":"Major","finding":"The final QA's headline recommendation was 'build one Panel header component and instance it in all thirteen panels — it is currently five components'. Measured, that overstates it: 130 headers across Brand (29), Media (23), Review (17), Content (16), Layers (14), Pages (11), History (10) and Insert (10) are ALREADY instances of Panel header (16:6). Only 8 local frames named 'Panel header' at h=44 remain, and none of them is drift — they carry DIFFERENT CONTROLS. The component draws title + expand + close. The four Insert locals draw title + close only. The four Layers locals draw title + refresh + close. Swapping any of them would add or delete a control from the design.","evidence":"Instance-vs-frame census per module; the 8 locals listed with their text children ([Insert,✕] x4, [Layers,↻,✕] x4); the component screenshotted to confirm it renders an expand icon and a close icon. All 8 carry 0 prototype edges, so wiring was not the blocker — anatomy is.","recommend":"The absorbing change is an icon-slot axis on 16:6 (close only / expand+close / refresh+close), then swap. That is a design decision about which controls a panel header may offer, not a cleanup, so it is filed rather than done. What DOES stand from QA's finding is the type inconsistency: panel titles run 11/16 in the majority, 14/21 in AI and Notifications, 12/AUTO in Inspector, and only Compare is bound to ui/14 · panel title."}
{"id":"D-X-53","module":"Inspector","node":"807:8342","screen":"degenerate trailing row repaired","q":"Q4","verdict":"FIXED","severity":"Minor","finding":"The final QA found damage from my auto-layout pass: 807:8342 ended with a 1px row at y=820 on an 812-tall board. Cause: my band sizing computes height as (next band top - this band top), and for the LAST band it used the board height — but that band starts at 820, past the 812 frame, so the height came out negative and was clamped to 1. Sized it to its own content instead.","evidence":"The five children in that row are all 'hotspot/state' nodes — invisible prototype hotspots that were ALREADY past the frame before the rowify, so no visible content was ever affected. Row is now 300x28, board remains 300x812, and the render is unchanged.","recommend":"The general fix for the script is to clamp the last band to the frame rather than to the next band's top. Recorded because the QA found it and I would not have: a 1px row is invisible in a screenshot and obvious in a child-geometry dump."}
{"id":"D-X-54","module":"Shell","node":"—","screen":"floating footers seated","q":"Q4","verdict":"FIXED","severity":"Major","finding":"The final QA reported 'the rail bottom lands at five different y values on identical 1440x900 boards — a 64px spread inside Shell alone'. Measured, the rails are innocent: all 99 fill their band EXACTLY (0 of 99 mismatched). The variance is in the bands, and its cause is a floating footer — 68 boards use one form (topbar 56, band 812, footer at y=868 reaching the bottom at 900) while 12 had the same parts with the footer stranded above the edge at 836, 834, 828 or 804, leaving a strip of dead canvas beneath. 11 seated; every one converged to 812, or 768 for the family whose band starts at y=100, which is the same dominant form.","evidence":"Rail-to-band fit: 99 checked, 0 mismatched. Band+footer census: 68x (56,812,footer 868), 10x (100,768,footer 868), 5x (56,844,no footer), then 12 one-offs. After seating: bands 780->812, 748->812, 778->812, 772->812, 736->768. Board 66:4 screenshotted before and after — the dead strip is gone and the footer spans the bottom edge.","recommend":"One board skipped deliberately: S1.1d has a 252px gap, which is a different drawing rather than a misplaced footer. QA's finding was right about the symptom and wrong about the cause, which is worth more than either alone — it took the rail measurement to show the rails were already correct."}
{"id":"D-X-55","module":"(design system)","node":"—","screen":"group labels cannot be bound without a tracking decision","q":"Q4","verdict":"KEEP","severity":"Major","finding":"QA reported 407 hand-set group labels. Measured across the live modules there are 718 unbound 11px all-caps labels, and NONE can be bound, because they differ from ui/11 · section header (Medium 11/16, +8%) in more than one property at once. The recipes: 318 Regular AUTO +0%, 159 Medium AUTO +0%, 75 Semi Bold 16 +0.5px, 64 Medium 16 +0.5px, 28 Regular 16 +0.5px, 25 Regular 18 +0.5px, plus a Bold and a Semi Bold AUTO tail.","evidence":"Census of unbound 11px TEXT whose characters are all-caps and under 24 characters, excluding instance children and archived sections. 477 of the 718 carry AUTO line-height, which no --bk-leading-* token can express.","recommend":"Two decisions unlock it, and neither is a cleanup: whether the section-header tracking is +8% or +0.5px (they are 0.88px and 0.5px at this size), and whether AUTO leading is admitted anywhere in the ramp. Added to DECISIONS-OPEN.md rather than forced — coercing 718 nodes onto one recipe is a redesign of every group label in the product."}
{"id":"D-X-56","module":"(design system)","node":"2100:11651","screen":"Panel header gains an icon-slot axis","q":"Q4","verdict":"FIXED","severity":"Major","finding":"QA's first structural task is done, and done without inventing anything. Panel header is now a COMPONENT_SET with three variants — Icons=expand-close (the original, 130 instances), Icons=close, Icons=refresh-close — and those three are exactly the control sets already drawn on real boards. The eight local headers that could not be adopted before, because swapping them onto the single-variant component would have deleted a control, are now instances of the variant that matches their own controls.","evidence":"Base component renamed in place so its 130 instances keep pointing at it; the two new variants built by cloning real board headers (1069:4791 title+close, 1082:4641 title+refresh+close). All 8 targets verified to carry 0 prototype edges before writing — the script refuses if any edge is found. Titles travel as text overrides. Board 1069:4790 screenshotted after: 'Insert' + close, unchanged.","recommend":"One Figma constraint worth recording: combineAsVariants requires every node to be on the SAME PAGE as the parent, so clones taken from the editor page must be moved to the library page before the combine, not after."}
{"id":"D-X-57","module":"(design system)","node":"2100:11651","screen":"all 280x44 panel headers adopted","q":"Q4","verdict":"FIXED","severity":"Major","finding":"43 local panel headers adopted onto the new variant set, not 8. Widening the match from the exact name 'Panel header' to also accept 'Header' at the same 280x44 found 35 more in Journeys and Shell — the name differed, the thing did not. Zero local 280x44 headers remain; Journeys went from 25 local frames to 32 instances, Layers from 14 to 18.","evidence":"Census before: 130 instances + 8 'Panel header' + 36 'Header' locals. After: 0 local 280x44 headers. Width is part of the match so a full-page header at another size cannot be caught by it. Page invariants unchanged: 0 loose, 0 out of bounds, 0 overlaps, 2833 edges.","recommend":"The edge guard earned its place here. The narrow 8-target set carried zero edges; widening to 43 pulled in 3, and the script refused to run rather than destroying them — so capture-and-replay was added and all 3 survived. That guard exists because an earlier swap in this session destroyed 547 edges by not having it."}
{"id":"D-X-58","module":"History","node":"—","screen":"drawer height and clipped text","q":"Q4","verdict":"FIXED","severity":"Major","finding":"History disagreed with itself before it disagreed with anyone: 3 boards at 280x812, 7 at 280x776, 5 at 360x776. The seven 280x776 are now 812, so 280x812 holds 154 boards across twelve modules and the 280x776 size no longer exists. Only the height was touched and only on boards already 280 wide — growing a frame cannot clip its contents, which is what separates this from the 360-wide question.","evidence":"Drawer census before: 147x 280x812, 22x 360x776, 18x 300x812 (Inspector, legitimately the right panel), 7x 280x776. After: 154 / 22 / 18 / 0. Screenshotted History · Saves before and after — identical content, 36px more space at the foot.","recommend":"The 22 boards at 360x776 (AI 11, Notifications 6, History 5) are the remaining drawer question and are NOT the same kind of change: 360 -> 280 is a width reduction that can reflow and crop. Still in DECISIONS-OPEN.md."}
{"id":"D-X-59","module":"(cross-module)","node":"—","screen":"28 clipped text boxes fitted","q":"Q4","verdict":"FIXED","severity":"Major","finding":"QA found 100 clipped TEXT nodes, the clearest being History's retention note — a 328px box in a 280px panel, cut mid-word at 'prune oldest first; nam'. 28 boxes that START inside their panel and run past its right edge were narrowed to the panel's content width with textAutoResize=HEIGHT, so the sentence wraps and stays whole instead of being truncated.","evidence":"The first detector found 50 and was wrong about 22 of them: annotations parked to the RIGHT of a board also 'overflow' it — one measured 326px over on a 280 board because it begins at x=468 and was never inside. Narrowing those would have moved real annotation text. The detector now requires the box to start inside the panel. History · Saves screenshotted after: the note reads in full across two lines.","recommend":"Watch the second-order effect, which bit here: growing those boxes taller and resizing the History boards to 812 pushed five boards into their own captions, because captions sit at board.y + board.height + 20 and the boards had grown. Re-running the section layout re-seated them. Any height change in this file needs the section re-laid afterwards."}
{"id":"D-X-60","module":"(design system)","node":"165:2","screen":"360-wide drawers — tested, not assumed","q":"Q4","verdict":"KEEP","severity":"Major","finding":"I had been calling the 360->280 drawer change 'risky' without testing it. Tested on Notifications · unread: narrowing to 280 leaves SEVEN nodes outside the new edge — the close button pinned at x=345, every timestamp at x=326, the titles at x=300 — and the rendered panel came back with no close button and no timestamps. Reverted to 360x776, 0 overflow, controls restored and verified by screenshot.","evidence":"narrow-drawer.mjs reports what still reaches past the new edge after the reflow, which is what turned an assumption into a measurement. The cause is structural: these boards are VERTICAL auto-layout whose ROWS are not, so setting children to FILL reflows the column and leaves right-pinned content where it was.","recommend":"This is a redraw of three modules (AI 11, Notifications 6, History 5), not a resize, and the cost is now known: roughly 7 right-pinned nodes per board to re-anchor. Recorded in DECISIONS-OPEN.md as decision 8 with the test result attached. The HEIGHT half of the same QA recommendation was safe and is already done — 280x776 no longer exists."}
{"id":"D-X-61","module":"(method)","node":"1159:4593","screen":"a number equals its own uppercase","q":"Q4","verdict":"FIXED","severity":"Major","finding":"MY DETECTOR WAS WRONG AND I APPLIED IT BEFORE READING ITS OUTPUT. The group-label finder tests ch === ch.toUpperCase() to identify all-caps text — which is TRUE OF EVERY NUMERAL. On the pilot board 7 of the 9 'labels' were count badges (6, 14, 10, 24, 8, 5), and binding them to ui/11 · section header gave count badges a section-header style: subtle on screen, wrong in the file, and it would have mislabelled every count in the product had I rolled it out.","evidence":"The dry run printed the candidates — '6' Regular, '14' Regular, '10' Regular — and I ran the apply in the same command instead of reading them first. Reverted the 7 numerals to Regular/AUTO/0%; the 2 genuine caps labels on that board stayed bound. Board re-screenshotted and correct.","recommend":"The 718 figure is an upper bound, not a population — it contains an unknown number of numerals, badges and codes. A group-label detector needs to exclude pure numerals and require at least one letter. Recorded as decision 9 rather than carried forward as a number, because quoting 718 again would repeat the error at scale."}
{"id":"D-X-62","module":"(design system)","node":"—","screen":"the 360 drawers, resolved","q":"Q4","verdict":"FIXED","severity":"Critical","finding":"QA's second structural task is DONE, and it was not the three-module redraw everyone including me had assumed. A naive resize destroyed controls, which I proved and reverted. But the failure was mechanical: right-pinned content keeps its x when a VERTICAL auto-layout column reflows, because the ROWS are not auto-layout. Capturing each node's RIGHT INSET before the resize and restoring it after puts every control back the same distance from the new edge. All 29 remaining 360-wide boards are now 280x812 with stillOverflowing=0 on every one.","evidence":"Per-board classification by measurement — full-bleed (both insets ~0) resized, right-pinned (small right inset, large left inset) shifted, rest untouched — plus a trim pass for boxes still wider than the panel. Notifications · unread screenshotted through all three states: original 360, naive 280 (no close button, no timestamps), re-anchored 280 (everything present). Drawer census after: 177 boards at 280x812 across fifteen modules, 360x776 gone. Page: 0 loose, 0 out of bounds, 0 overlaps, 2833 edges.","recommend":"Two second-order effects were caught by the invariant check and fixed by re-laying sections: boards growing 776 -> 812 pushed five into their own captions, and captions still 360 wide overhung their now-280 boards in Reference. Any width or height change in this file needs the section re-laid afterwards."}
{"id":"D-X-63","module":"(design system)","node":"—","screen":"group labels re-counted with a working detector","q":"Q4","verdict":"FIXED","severity":"Major","finding":"The group-label population is 278, not 718. The original detector tested ch === ch.toUpperCase(), true of every numeral, so 440 of its hits were counts, glyphs and codes rather than labels. The corrected test requires at least two letters and no lowercase. Of the 278, the largest tractable subset was 35 already at Inter Medium 11/16 differing from ui/11 · section header in tracking alone — and only 6 of them can take the style without clipping.","evidence":"Recipes: 103 Medium/AUTO/0%, 75 Semi Bold/16/+0.5px, 35 Medium/16/+0.5px, 24 Regular/16/+0.5px, 41 in a tail. Measured per node before deciding: the style's 8% is 0.88px at 11px against 0.5px, so a nine-character label grows ~3px, and 28 of 34 boxes are sized too tight to hold it. 6 bound; page invariants unchanged.","recommend":"What remains is ONE narrow decision rather than a bulk job: is section-header tracking +8% (537 bound labels, the style as written) or +0.5px (134 unbound)? Whichever loses needs its boxes re-measured. Separately, 103 labels carry AUTO leading, which needs the question of whether AUTO is admitted to the ramp at all."}
{"id":"D-X-64","module":"(design system)","node":"92:30","screen":"Slider cannot be adopted as built","q":"Q4","verdict":"KEEP","severity":"Major","finding":"The forms audit named three zero-use atoms whose adoption would convert ~90 hand-drawn shapes 'with no further design work'. Radio was true and is done. SLIDER IS NOT: its knob position and filled-track width encode the VALUE, and an instance cannot override relative-transform — Figma rejects it outright ('This property cannot be overridden in an instance: relative-transform'). So every instance can only ever show the master's 62%. Swapping the Inspector's 'Opacity 100' control silently rewrote it to 62, which the before/after screenshots caught.","evidence":"50 candidate controls found, all 172x28 or 160x28 holding slider+input, 0 prototype edges. The pilot swap on 32:2 changed a board's stated opacity from 100 to 62 and the knob from the far right to 62%. Attempting to restore it threw on knob.x. Detached the instance, restored text=100, filled=118, knob at the end; the board renders exactly as before.","recommend":"Slider needs a value-carrying design before it can be adopted — either a component property driving the fill and knob, or a variant per stop. Until then the 50 hand-drawn controls are CORRECT and should stay. Naming this precisely matters: 'zero uses' looked like neglect and is actually a component that cannot express what the boards need."}
