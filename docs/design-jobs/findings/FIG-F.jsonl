{"id": "FIG-F-01", "module": "brand", "action": "fix-board", "target": "1776:8373 / 155:69", "claim": "caption/Brand · pro-locked reads 'Beginner DS-mode hides depth, not rows. Locked rows stay visible with their badge: a feature you never see is a feature you never buy.' — and it captions a board that does not exist in the section.", "truth": "Beginner/Pro is a display-density toggle, not a plan. It is persisted per USER in localStorage 'buildrik:ds-mode', never read from a plan, entitlement or subscription. Nothing anywhere renders a 'locked' row or a lock badge. And Beginner does the opposite of what the caption says: it REMOVES empty foundation kinds from the list behind a 'More token kinds' disclosure, and drops the Classes and Components counts from the root.", "evidence": "DSModeContext.tsx:19-20 STORAGE_KEY='buildrik:ds-mode', DEFAULT_MODE='beginner'; :32-39 readPersisted reads only localStorage. Repo-wide grep for useDSMode/isPro/isBeginner (packages/editor/src, non-test) returns 8 consumers: DesignSystemTab.tsx:208,315,337,755,807; TokensSection.tsx:80-81,307,427,495; TokenDetailView.tsx:131,285,365-366,541-549; TokenRow.tsx:154; GenericTokenList.tsx:128,168; DSBindingChip.tsx:94,141; DetachInstanceButton.tsx:57,84; ComponentDetailScreen.tsx:92,367. None reads a plan. Grep for 'lock' in design-system/ui returns nothing. The fold-away is TokensSection.tsx:303-323 + 393-406 ('More token kinds'); the count drop is DesignSystemTab.tsx:337-347. Manifest 07-brand-51.txt lists 13 Brand · * boards and 14 caption/Brand · * captions — there is no 'Brand · pro-locked' board for 155:69 to caption.", "edit": "Rewrite TEXT node 155:69 to: 'Beginner is a display density, not a plan — it is a per-user localStorage flag, and no row is ever gated by billing. What it hides: token IDs and CSS vars, the delete action on a token, and empty foundation kinds (folded behind \"More token kinds\"). Pro shows them.' Then rename the caption node from 'caption/Brand · pro-locked' to 'caption/Brand · beginner-vs-pro' so it stops promising a board that was never drawn.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-02", "module": "brand", "action": "fix-board", "target": "1776:8373 / 1333:7162 + 154:132", "claim": "Both root boards label the fifth destination row 'Components'.", "truth": "The shipped label is 'Component styles'. It was renamed deliberately because four doors in this product were called Components and led three different places.", "evidence": "DesignSystemTab.tsx:139-146 `{ id: \"components\", label: \"Component styles\", hint: \"What the brand ships\" }` plus the comment above it naming the collision. Read live from both boards 2026-09-06: 1333:7162 and 154:132 each draw TEXT 'Components' over the hint 'What the brand ships'.", "edit": "On 1333:7162 and on 154:132, set the fifth row's title TEXT from 'Components' to 'Component styles'. Hint string is already correct on both ('What the brand ships') — do not touch it.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-03", "module": "brand", "action": "fix-board", "target": "1776:8373 / 1333:7162", "claim": "The Pro root draws a count on Tokens (4), Presets (18), Starters (6) and Lint (2), and NO count on Classes or Component styles.", "truth": "In Pro the code renders a count on Classes and on Component styles too — Classes is the number of distinct class names carried by the element tree, Component styles is CATALOG.length (27). Only Beginner suppresses those two. The code's own docblock already asserts 'Board 1333:7162 (Pro) shows both', so the two families disagree today.", "evidence": "DesignSystemTab.tsx:337-347 — `if (isBeginner) return { tokens, styles, starters }` else `return { tokens, styles, classes: classNames.size, starters: STARTER_DS_REGISTRY.length, components: CATALOG.length }`; rendered at :783-787. The claim about the board is at :330-336. Live read of 1333:7162 (2026-09-06): the Classes and Component styles rows carry a chevron and no numeral, unlike Tokens/Presets/Starters/Lint.", "edit": "On 1333:7162 add a mono tabular numeral to the Classes row and to the Component styles row, in the same style/position as the Tokens row's '4' (mono, tabular-nums, --bk-ink-soft, immediately left of the '›'). Sample values: Classes 12, Component styles 27 (27 is CATALOG.length, the real number). Leave 154:132 (Beginner) countless on those two rows — that is correct.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-04", "module": "brand", "action": "mark-unimplemented", "target": "1776:8373 / 153:57", "claim": "Brand · typography draws, below ACTIVE FONTS: a '+ Add a font' row, a 'Drop .woff2, or browse' drop zone, and an 'I have a licence for this font' checkbox.", "truth": "None of the three ships and none can. TypographySection renders the ACTIVE FONTS list only. There is no font-upload UI anywhere, `FontManager.uploadFont` has zero non-test callers, its output is an in-memory Map of data URLs that `Composer.exportProject` never serialises, and the export path emits font-family declarations + Google Fonts <link>s from tokens only — no @font-face for a custom file. Founder call 2026-08-16.", "evidence": "TypographySection.tsx:9-13 records the scope call verbatim ('the board's \"+ Add a font\", `.woff2` drop zone and \"I have a licence for this font\" checkbox do not [ship]'); the component (:104-170) renders FONT_SLOTS + weights and nothing else. `grep -rn uploadFont packages/editor/src` → FontManager.ts:291 (the definition) and 3 hits in FontManager.test.ts; no product caller. Composer.ts:255 constructs FontManager, Composer.ts:1118 destroys it, and `grep -rn 'composer.fonts'` finds no other use. Export: Composer.ts:668 `siteTokensCSS(designTokens) + siteFontCSS(fonts)`; ExportHelpers has no @font-face emitter (grep '@font-face' → AssetBundler.ts:102 only, which REWRITES urls it finds, it does not author rules).", "edit": "CONVENTION: this file renames with a `[not-implemented] ` PREFIX (see 306:2111/2136/2161), not a leading 'NOT IMPLEMENTED —'. Apply that prefix form to the rename below. Rename board 153:57 from 'Brand · typography' to 'Brand · typography — NOT IMPLEMENTED (add-a-font, .woff2 upload and the licence checkbox: no font storage and no publish-time @font-face)'. Do NOT delete the design. Keep the ACTIVE FONTS list, which ships. Add the note to caption 155:63 (see FIG-F-05).", "severity": "Critical", "confidence": "measured"}
{"id": "FIG-F-05", "module": "brand", "action": "fix-board", "target": "1776:8373 / 155:63", "claim": "caption/Brand · typography says the screen is 'active fonts (56h rows, family name set in its own face) · add a font · custom upload. The licence confirmation is a checkbox on upload, not a footnote.'", "truth": "Only the first of the three blocks ships. See FIG-F-04.", "evidence": "Same evidence as FIG-F-04. TypographySection.tsx:9-13 and :139-168.", "edit": "Rewrite 155:69's sibling TEXT node 155:63 to: 'Active fonts only (56h rows, family name set in its own face). The add-a-font and .woff2 upload blocks below are NOT IMPLEMENTED — there is no font storage and no publish-time @font-face, so a drop zone here would accept a file it cannot keep.'", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-06", "module": "brand", "action": "fix-board", "target": "1776:8373 / 153:57", "claim": "Each ACTIVE FONTS row on 153:57 ends in a '⋯' overflow button.", "truth": "The shipped row is name + role pill + weight count, with no control at all — no menu, no button, not even an onClick.", "evidence": "TypographySection.tsx:145-166: the <li> renders a family <span>, a role pill <span> and a weights <span>. No Button, no onClick, no menu import in the file. Live read of 153:57 (2026-09-06) shows a '⋯' TEXT after each of the three weight counts.", "edit": "Delete the three '⋯' nodes from the ACTIVE FONTS rows on 153:57, or give them the disabled-with-reason treatment 153:120's Figma row already uses. Preferred: delete — there is no menu behind them and none is specified.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-07", "module": "brand", "action": "none", "target": "1776:8373 / 153:57", "claim": "Board 153:57 headlines the screen 'ACTIVE FONTS' — the fonts THIS SITE uses.", "truth": "The board is right; the code is not. TypographySection falls back to DEFAULT_TOKENS whenever no `tokens` prop is passed, and DesignSystemTab mounts it with `composer` only — so the screen always lists the SEED font stack, never the site's own type tokens. Recorded here because the Figma-truth pass is read-only on code and this is a code defect, not a board defect.", "evidence": "TypographySection.tsx:105 `const source = tokens ?? DEFAULT_TOKENS;` and :34 `tokens?: readonly DesignToken[]`. DesignSystemTab.tsx:842 `{activeSection === \"typography\" && <TypographySection composer={composer} />}` — no `tokens` prop at the only mount site.", "edit": "No Figma edit. Hand to the code owner: TypographySection needs `tokens={type.tokens}` at DesignSystemTab.tsx:842.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-08", "module": "brand", "action": "add-state", "target": "1776:8373 / 152:83", "claim": "Brand · token-detail draws exactly one variant: name, id 'color-primary', CSS var, Light value, Dark value, 'Used by ▸ 34 elements', 'Lint ✓ pass', and three live buttons Replace value / Rename ID / Delete token.", "truth": "In Beginner — the DEFAULT mode for every new user — that screen looks different in four places: the token id and the CSS var are hidden, Delete is DISABLED, and a blue notice renders under the button row reading 'Delete blocked in Beginner mode.' / 'Pro shows replace-with / cascade-clear when N elements bind.'", "evidence": "TokenDetailView.tsx:131 `const isPro = dsMode?.mode === \"pro\"`; :365-366 `{isPro && <span className={ID_MONO}>{token.id}</span>}` / `{isPro && <span className={CSS_VAR}>{cssVarName}</span>}`; :541-542 `aria-disabled={!isPro || undefined} disabled={!isPro}` on Delete; :549-561 the `{!isPro && ...}` notice with `data-beginner-notice` and that exact copy; :285 `if (!isPro) return; // Beginner-blocked.` DSModeContext.tsx:20 `DEFAULT_MODE: DSMode = \"beginner\"`.", "edit": "Clone 152:83 into a new 280x812 board 'Brand · token-detail · Beginner' in section 1776:8373, placed immediately right of 152:83. Changes from the clone: delete the 'color-primary' id line and the CSS-var line under the name; set the 'Delete token' button to disabled (same treatment as 153:120's Figma row); add a notice block under the button row, --bk-accent-tint plate, --bk-accent-text, 12px, two lines: bold 'Delete blocked in Beginner mode.' then 'Pro shows replace-with / cascade-clear when 34 elements bind.'", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-09", "module": "brand", "action": "fix-board", "target": "1776:8373 / 152:83", "claim": "'Rename ID' is drawn as a live, equal peer of Replace value and Delete token.", "truth": "Rename is dispatched by token kind and only colour + the eleven newer kinds have a rename API. For TYPE and SPACING tokens — two of the three kinds a user touches most — the call lands on `undefined?.()` and returns silently, so the button looks live and does nothing.", "evidence": "TokensSection.tsx:287-296 handleTokenRename: `if (k === \"color\") { color.renameToken(id, newId); return; } const r = newKindRegistry(k as TokenKind); r?.renameToken?.(id, newId);` — and the comment at :281-285 states it outright: 'Type + spacing currently have no rename API (no DS UX entry for those) so renames there silently no-op via optional chaining.' newKindRegistry covers radius/shadow/motion/border/opacity/zindex/breakpoint/grid/sizing/icon/imagery only.", "edit": "On 152:83, keep 'Rename ID' as drawn (the board's token is a colour, where it works) and add a second 280x812 variant board 'Brand · token-detail · type or spacing' in which 'Rename ID' carries the disabled treatment plus the inline reason 'Rename isn\\u2019t available for type and spacing tokens yet' — the same disabled-with-reason pattern 153:120 already establishes for the Figma export row.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-10", "module": "brand", "action": "add-state", "target": "1776:8373 / 152:83", "claim": "The Lint field on 152:83 draws only the passing state: 'Lint ✓ pass'.", "truth": "The failing state ships and is the one that matters: a '△ <message>' line plus an 'Ignore' button that suppresses the token's issues. An 'Auto-fix' button sits next to Ignore in the code but can never render, because no linter rule produces the `autoFixHint` it is gated on.", "evidence": "TokenDetailView.tsx:301-341 — the pass branch renders `✓ pass` (data-lint-status=\"pass\"), the fail branch renders `△ {issue.message}` with `{issue.autoFixHint && <Button>Auto-fix</Button>}` and an unconditional `<Button>Ignore</Button>` (:329-339) calling `lintState?.suppress(token.id)` (:239). Producer search for the constant: `grep -rn autoFixHint packages/editor/src` returns only consumers — LintState.ts:34 (the optional field), IssuesPanel.tsx:53, AquibraStudio.tsx:318,631-632, TokenDetailView.tsx:216,318, useStudioState.ts:91, contrastFix.ts:10. DSLinter.ts constructs 7 issue objects (:65,89,100,112,120,130,138,148,183) and none sets autoFixHint; contrastLint.ts:82 constructs the 8th ('contrast') and does not either.", "edit": "Clone 152:83 to a new board 'Brand · token-detail · lint fail' (280x812) in section 1776:8373. Replace the 'Lint / ✓ pass' row with a two-line warning block in --bk-warning-text: '△ Contrast 3.1:1 against color-background — needs 4.5' and, beneath it, ONE ghost button labelled 'Ignore'. Do not draw an Auto-fix button on this or any Brand board.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-11", "module": "brand", "action": "fix-board", "target": "1776:8373 / 306:2049", "claim": "Brand · tokens · add draws the token KIND list (color/spacing/radius/typography/shadow/motion) with an 'Add token' badge at the foot of the screen — implying you add a token of any kind from here.", "truth": "There is no Add affordance on the kind list. The only '+ Add token' in the product lives one level deeper, inside the colour list, and the modal it opens hard-codes category 'colors', type 'color' and group 'brand' — it can create a COLOUR token and nothing else. Thirteen of the fourteen kinds have no create path at all.", "evidence": "TokensSection.tsx:325-406 renders the kind list: kind rows plus the 'More token kinds' disclosure; no Add button. `onAddTokenClick` reaches exactly one consumer: `<ColorTokenList ... onAddToken={() => onAddTokenClick?.()} />` (TokensSection.tsx:418). DesignSystemTab.tsx:582-593 handleAddToken builds `{ id: generateColorTokenId(name), value: hex, category: \"colors\", cssVar: generateColorCssVar(name), type: \"color\", group: \"brand\" }` and calls `color.addToken(newToken)`. Token kinds are 14 (engine/designSystem/types.ts:64-72).", "edit": "On 306:2049, move the 'Add token' control off the kind list and onto the colour kind's own screen, and relabel it '+ Add colour token'. Rename the board 'Brand · tokens · color · add' so its name no longer says any kind can be added. If the kind-list Add is wanted as a design intent, keep it as a separate board named 'Brand · tokens · add — NOT IMPLEMENTED (only colour tokens can be created)'.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-12", "module": "brand", "action": "mark-unimplemented", "target": "1776:8373 / 154:2", "claim": "Brand · lint draws a 'Fix ›' action on its first row and 'Open' on the other two, under a closing note 'Auto-fix only appears where a nearest token exists.'", "truth": "No Fix can ever appear: the Fix path is gated on `LintIssue.autoFixHint` and no rule in the product sets that field. 'Open' has no counterpart at all — the shipped rows are a severity dot, the message and '<rule label> · <tokenId>', with no control of any kind. LintSection already records this and carries the opposite closing note.", "evidence": "LintSection.tsx:9-27 states it: '**The board draws a `Fix ›` link on two rows and `Open` on the third. Neither ships here, and that is deliberate.** … re-checked 2026-08-18: **no rule anywhere sets one.**' The shipped row is :70-91 (dot + message + label·tokenId, no Button). Producer search by constant as in FIG-F-10: DSLinter.ts constructs its 7 issues at :65,89,100,112,120,130,138,148,183 with no autoFixHint; contrastLint.ts:73-90 likewise. Consumers only: AquibraStudio.tsx:631-632, IssuesPanel.tsx:53, TokenDetailView.tsx:318.", "edit": "CONVENTION: this file renames with a `[not-implemented] ` PREFIX (see 306:2111/2136/2161), not a leading 'NOT IMPLEMENTED —'. Apply that prefix form to the rename below. Rename 154:2 to 'Brand · lint — NOT IMPLEMENTED: Fix / Open row actions (no rule produces an autoFixHint, so the Fix branch is unreachable)'. Keep the design. Grey the three action links to the disabled treatment used by 153:120's Figma row and put the reason inline once, under the list.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-13", "module": "brand", "action": "mark-unimplemented", "target": "1776:8373 / 154:2", "claim": "Two of the three lint rows on 154:2 are ELEMENT findings: 'Hard-coded #2D6DFF · Section · Hero background' and 'Font not in the brand · Arial on Footer note'.", "truth": "The linter never looks at an element. `DSLinter.lint` takes a token list plus a preset list and produces eight rule ids, all of them scoped to a token or a preset binding: contrast, banned-hue, pure-black, empty-value, missing-dark, unresolved-binding, alias-depth-exceeded, semantic-needs-alias. There is no 'hard-coded value on an element' rule and no 'font not in the brand' rule, and nothing walks the element tree for lint.", "evidence": "DSLinter.ts:12-24 is the whole LintRuleId union (8 ids). Rule construction sites: :65 semantic-needs-alias, :89 alias-depth-exceeded, :100 empty-value, :112/:120 banned-hue, :130/:138 pure-black, :148 missing-dark, :183 unresolved-binding; contrastLint.ts:82 contrast. useDSLint.ts:26-36 feeds it `useColorRegistry + useSpacingRegistry + useTypeRegistry` tokens — a token array, never elements. LintSection.tsx:38-47 RULE_LABEL enumerates the same 8.", "edit": "CONVENTION: this file renames with a `[not-implemented] ` PREFIX (see 306:2111/2136/2161), not a leading 'NOT IMPLEMENTED —'. Apply that prefix form to the rename below. On 154:2 replace the two element-scoped rows with rows the linter can actually produce, keeping the same row shape: 'Token \"color-accent\" uses a purple/violet/indigo hue (\"#8b5cf6\")' with sublabel 'Banned hue — purple, violet or indigo · color-accent', and 'Color token \"color-muted\" missing darkValue' with sublabel 'No dark variant · color-muted'. Keep row 1 (contrast) as drawn — that rule is real.", "severity": "Critical", "confidence": "measured"}
{"id": "FIG-F-14", "module": "brand", "action": "fix-board", "target": "1776:8373 / 154:2 + 155:66", "claim": "154:2's closing note reads 'Auto-fix only appears where a nearest token exists.' and caption 155:66 reads 'Issues with one-click auto-fix, but only where a nearest token exists — the rest open for a human decision.'", "truth": "Both promise a conditional Fix. The shipped note says the opposite, and it is the honest one.", "evidence": "LintSection.tsx:93-96 renders verbatim: 'Auto-fix isn\\u2019t available yet — the linter reports what is wrong, not what to replace it with. Edit the token in Tokens.' Reason at :20-27 (the `darken-22`/`lighten-22` hint grammar is not guaranteed to reach AA, while `suggestContrastFix` binary-searches to target).", "edit": "Set 154:2's closing TEXT to: 'Auto-fix isn\\u2019t available yet — the linter reports what is wrong, not what to replace it with. Edit the token in Tokens.' Set caption 155:66 to: 'One row per finding — severity dot, message, and the token it is about. No Fix action: no rule produces an auto-fix hint, so a Fix link here would claim what nothing can deliver.'", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-15", "module": "brand", "action": "fix-board", "target": "1776:8373 / 154:2", "claim": "The Lint destination's count and list read as if they cover the brand.", "truth": "The linter is fed 3 of the 14 token registries, so eleven kinds — radius, shadow, motion, border, opacity, z-index, breakpoint, grid, sizing, icon, imagery — can never produce a finding, and the root's Lint count is structurally incomplete. Separately, the banned-hue rule is an exact-match list of 12 Tailwind hex values, so it catches only those twelve strings: the shipped 'Stripe Blue' starter's #635BFF and 'Linear Dark' 's #5E6AD2 both pass it.", "evidence": "useDSLint.ts:26-36 builds allTokens from useColorRegistry + useSpacingRegistry + useTypeRegistry only, and :49 passes that array to composer.dsLinter.lint. DesignSystemTab.tsx:29-45 imports all 14 registries and :783-787 shows the Lint row count from `lintIssues.length`. DSLinter.ts:197-210 `isBannedHue` is `const banned = [\"#6366f1\",\"#4f46e5\",\"#4338ca\",\"#3730a3\",\"#a78bfa\",\"#8b5cf6\",\"#7c3aed\",\"#6d28d9\",\"#c084fc\",\"#a855f7\",\"#9333ea\",\"#7e22ce\"]` with the comment 'finer-grain HSL detection deferred'. starters/stripe-blue.ts:8 value \"#635BFF\"; starters/linear-dark.ts:8 value \"#5E6AD2\".", "edit": "Add a persistent annotation under 154:2's list (11px --bk-ink-muted): 'Lint covers colour, type and spacing tokens. The other eleven token kinds are not checked.' Do not restate the hue list on the board.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-16", "module": "brand", "action": "fix-board", "target": "1776:8373 / 152:112 + 306:2111 + 306:2136 + 306:2161", "claim": "All four Presets boards draw the same six category rows — Buttons, Cards, Sections, Forms, Nav, Footers — each '3 variants'.", "truth": "There are eleven preset categories and two of the board's six are not among them. 'Sections' and 'Footers' do not exist; the shipped set is button, card, form, link, badge, alert, tooltip, modal, nav, table, layout.", "evidence": "engine/designSystem/types.ts:137-140 `export type PresetCategory = \"button\" | \"card\" | \"form\" | \"link\" | \"badge\" | \"alert\" | \"tooltip\" | \"modal\" | \"nav\" | \"table\" | \"layout\";` PresetDetailPane.tsx:17-29 CATEGORY_LABELS gives the display names: Button, Card, Form input, Link, Badge, Alert, Tooltip, Modal, Nav, Table, Layout. DesignSystemTab.tsx:47-52 fans out exactly those eleven registries.", "edit": "On 152:112, 306:2111, 306:2136 and 306:2161 replace the six rows with eleven, in the code's order and under its labels: Button · Card · Form input · Link · Badge · Alert · Tooltip · Modal · Nav · Table · Layout. Keep the row height and the '<n> variants' + '›' shape.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-17", "module": "brand", "action": "mark-unimplemented", "target": "1776:8373 / 152:112", "claim": "Brand · presets draws a drill-in list of preset categories, which the root reaches with a dirty dot and a 'Presets 18' count, and which 306:2161 answers with a 'Draft preset' badge.", "truth": "Nothing in the Presets destination can change anything. The three mutators — updatePreset, addPreset, deletePreset — have zero UI callers; the binding row that CAN edit a binding is dead code with no consumer; and the row that does render says so in its own docblock. Consequently `isDirty` is permanently false for all eleven categories, the root's Presets dirty dot never lights, stylesDirty is always 0, and the 'Draft preset' badge is unreachable.", "evidence": "Producer/consumer search by name across packages/editor/src (non-test): `updatePreset|addPreset|deletePreset` returns only their own definitions and re-exports — usePresetsForCategory.ts:25-27 (types), :69,:82,:86 (definitions), :120-122 (returned), StylePresetRegistryContext.tsx:151-153 (noop defaults). No call site. The editable row, editor/design-system/ui/presets/BindingRow.tsx, has ZERO importers (`grep -rn BindingRow` outside PresetBindingRow → only its own file and a stale mention in TokenPickerPopover.tsx:43). What DOES render is PresetBindingRow.tsx, whose docblock line 5 reads 'Click behaviour is no-op v1 — token picker integration deferred (spec D3).' Persistence is worse: designPresets is written at DesignSystemTab.tsx:543 and read by nothing (grep designPresets → project.ts:259 type + that write only); hydration is localStorage 'buildrick-design-presets-<projectId>-v1' (StylePresetRegistryContext.tsx:55,61,98).", "edit": "CONVENTION: this file renames with a `[not-implemented] ` PREFIX (see 306:2111/2136/2161), not a leading 'NOT IMPLEMENTED —'. Apply that prefix form to the rename below. Rename 152:112 to 'Brand · presets — NOT IMPLEMENTED (read-only: no add/edit/delete reaches the registry, so Draft and the dirty dot are unreachable)'. Keep the design. On 1333:7162 and 154:132, do not draw a dirty dot on the Presets row.", "severity": "Critical", "confidence": "measured"}
{"id": "FIG-F-18", "module": "brand", "action": "none", "target": "1776:8373 / 306:2111 + 306:2136", "claim": "Brand · presets · bound and · unbound are already renamed in the file to lead with 'UNBUILDABLE — elements carry no preset reference; TokenUsageTracker counts tokens'.", "truth": "Correct, and the reason holds on a re-check: no element carries a preset reference of any kind, and TokenUsageTracker records `{elementId, styleProp}` per TOKEN id, never per preset. Interiors read 2026-09-06: 306:2111 carries the pill 'Bound to elements', 306:2136 'Unbound', over the same six-row list, and both already carry the [not-implemented] prefix in their names.", "evidence": "SectionStatusBadge.tsx:18-34 records the finding and the widths that corroborate the two labels (306:2111 'Bound to elements' 128px, 306:2136 'Unbound' 73px). TokenUsageTracker.ts:28,50 — the two regexes are `\\{\\{token\\.…\\}\\}` and `var\\(--buildrick-design-…\\)`; recompute (:75-95) keys `this.refs` by tokenId. SectionStatus type at SectionStatusBadge.tsx:45 is `\"draft\" | \"exported\" | \"imported\" | \"import-failed\"` — bound/unbound deliberately absent.", "edit": "No edit. Coverage recorded so these two are not re-audited.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-19", "module": "brand", "action": "fix-board", "target": "1776:8373 / 306:2161", "claim": "Brand · presets · draft is already renamed 'UNBUILDABLE today — updatePreset/addPreset/deletePreset have zero UI callers, so isDirty is permanently false'.", "truth": "The rename is right but understates it: the editable binding row (ui/presets/BindingRow.tsx) is dead code with no importer, and the row that ships states its own click is a no-op — so this is not a wiring gap that one call site closes.", "evidence": "usePresetsForCategory.ts:69-88 (definitions, no callers); ui/presets/BindingRow.tsx has zero importers; PresetBindingRow.tsx:5 'Click behaviour is no-op v1 — token picker integration deferred (spec D3).' StylesSection.tsx:29-51 useStylesSectionTotalDirty sums 11 registries whose isDirty can never flip.", "edit": "Extend 306:2161's name to '… zero UI callers AND the editable binding row (ui/presets/BindingRow.tsx) has no consumer, so isDirty is permanently false'. No visual change.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-20", "module": "brand", "action": "fix-board", "target": "1776:8373 / 153:92", "claim": "Brand · colour-mode draws a 'Group by / Sort / Filter' toolbar and BRAND / STATE / SURFACE group headers beneath the NO DARK VALUE list.", "truth": "None of them ships. ColourModeSection renders a light/dark toggle, a 'No dark value' header with a count, and a flat unsorted, ungrouped list of colour tokens with an inline 'Set' per row. There is no grouping, sorting or filtering control in the file.", "evidence": "ColourModeSection.tsx:53-136 is the whole component: ColorModeToggle (:56-58), the 'No dark value' header + count (:61-67), the empty branch (:69-72), and the flat `missing.map` list (:74-129). `missing` is `(color?.tokens ?? []).filter((t) => !t.darkValue)` (:40-43) — no sort, no group, no filter. Grep for 'Group by'/'Sort'/'Filter' in design-system/ui returns nothing.", "edit": "Delete the 'Group by / Sort / Filter' toolbar and the BRAND / STATE / SURFACE section headers from 153:92, leaving the toggle, the 'NO DARK VALUE · <n>' header and the flat row list with its per-row 'Set'. Row ids stay mono — the code deliberately renders `token.id`, not the friendly name (ColourModeSection.tsx:83-96).", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-21", "module": "brand", "action": "add-state", "target": "1776:8373 / 153:92", "claim": "153:92 draws only the populated case — four tokens with no dark value.", "truth": "The screen has an empty state that a site with a complete dark palette will always see, and no board draws it.", "evidence": "ColourModeSection.tsx:69-72 — `{missing.length === 0 ? <div …>Every colour token has a dark value.</div> : …}`, centred, 12px, --bk-ink-muted.", "edit": "Clone 153:92 to 'Brand · colour-mode · all-set' (280x812) in section 1776:8373. Keep the header and the Light/Dark toggle, set the header count to 0, and replace the row list with one centred 12px --bk-ink-muted line: 'Every colour token has a dark value.'", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-22", "module": "brand", "action": "fix-board", "target": "1776:8373 / 152:137 + 306:2186 + 155:60", "claim": "Both Starters boards head the grid with 'Applying a starter overwrites your tokens.'", "truth": "A starter stages three of the fourteen token kinds. Radius, shadow, motion, border, opacity, z-index, breakpoint, grid, sizing, icon and imagery are left on the previous brand, so the warning overstates the blast radius by eleven kinds — and hides that the result is a half-mixed brand.", "evidence": "useApplyStarter.ts:63-65 stages colorRegistry, spacingRegistry and typeRegistry only. DesignSystemTab.tsx:29-45 + :291-294 aggregate all 14, and persistAll persists all 14 (TokenRegistryContext.tsx:197). StartersSection.tsx:60-66 renders the warning verbatim.", "edit": "On 152:137 and 306:2186, set the warning TEXT to: 'Applying a starter replaces your colour, type and spacing tokens. Radius, shadow and the other foundations keep their current values.' Keep the --bk-warning-tint plate and placement. Also amend caption 155:60's last clause from 'Applying warns first because it overwrites tokens.' to 'Applying warns first because it replaces colour, type and spacing tokens.'", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-23", "module": "brand", "action": "fix-board", "target": "1776:8373 / 152:137 + 306:2186", "claim": "The six starter cards are named Warm · Clean · Bold · Editorial · Mono · Soft.", "truth": "Six is right — it is STARTER_DS_REGISTRY.length — but none of the six names is a shipped starter. The registry is Buildrik Default, Stripe Blue, Notion Warm, Apple Minimal, Linear Dark, Vercel Mono.", "evidence": "starters/index.ts:18-25 registry order; names at cobalt-default.ts:5 'Buildrik Default', stripe-blue.ts:5 'Stripe Blue', notion-warm.ts:5 'Notion Warm', apple-minimal.ts:5 'Apple Minimal', linear-dark.ts:5 'Linear Dark', vercel-mono.ts:5 'Vercel Mono'. StarterGrid renders from STARTER_DS_REGISTRY via StartersSection.tsx:68.", "edit": "On both boards rename the six cards, in registry order: Buildrik Default · Stripe Blue · Notion Warm · Apple Minimal · Linear Dark · Vercel Mono. This is a closed six-item set, not sample data.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-24", "module": "brand", "action": "fix-board", "target": "1776:8373 / 152:137", "claim": "The card is the control: one click applies a starter, with no confirm, which both Starters boards draw correctly.", "truth": "The board is right about the interaction and silent about its cost. Applying a starter wipes the per-token undo and redo stacks for the colour, spacing and type registries, so the click cannot be walked back token by token — only wholesale, through the footer's Discard, which also throws away every unrelated staged edit in all 25 registries.", "evidence": "StartersSection.tsx:39-45 `choose` calls applyStarter directly from the card, no confirm. useApplyStarter.ts:63-65 → useTokenBase.ts:143-144 `setUndoStack({}); setRedoStack({});`. DesignSystemTab.tsx:559-575 handleDiscard runs discardAll on all 14 token registries and all 11 preset registries.", "edit": "Add a second sentence to the warning note on 152:137 and 306:2186 (same plate, --bk-warning-text, 11px): 'Per-token undo is cleared — the only way back is Discard, which drops every staged brand edit.'", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-25", "module": "brand", "action": "mark-unimplemented", "target": "1776:8373 / 1172:4840", "claim": "Brand · review-changes lists four staged rows across four kinds — color/accent, type/heading, radius/card and shadow/raised — and titles itself 'Review 4 staged changes'.", "truth": "The modal is handed colour, type and spacing only. A radius or shadow edit can never appear in it, and neither can any of the other nine kinds. The mismatch is visible: the footer and the header chip count all 14 registries, so a user with one radius edit sees 'Unsaved brand changes · 1' and then 'Review 0 staged changes'.", "evidence": "ReviewModal.tsx:17-30 props are colorTokens/colorDiff, typeTokens/typeSavedTokens, spacingTokens/spacingSavedTokens — no other kind. :93-94 `const totalChanges = changedEntries.length + changedTypeTokens.length + changedSpacingTokens.length;` drives both the title (:119) and the Apply label (:207). The only mount is DesignSystemTab.tsx:855-863, which passes exactly those six props. The footer's counter is a different sum: DesignSystemTab.tsx:349-352 `tokensDirty = allRegistries.reduce((n, r) => n + dirtyCount(r), 0)` over all 14.", "edit": "CONVENTION: this file renames with a `[not-implemented] ` PREFIX (see 306:2111/2136/2161), not a leading 'NOT IMPLEMENTED —'. Apply that prefix form to the rename below. Rename 1172:4840 to 'Brand · review-changes (modal) — NOT IMPLEMENTED for 11 of 14 token kinds (only colour, type and spacing reach the review)'. Keep the design. In the drawn list, replace the 'radius/card 8 → 12' and 'shadow/raised removed' rows with two rows the modal can render — e.g. 'spacing/lg 24 → 32' and 'color/surface #FFFFFF → #F9FAFB'.", "severity": "Critical", "confidence": "measured"}
{"id": "FIG-F-26", "module": "brand", "action": "mark-unimplemented", "target": "1776:8373 / 1172:4840", "claim": "One review row reads 'shadow/raised — removed', i.e. the review shows deletions.", "truth": "A removal never reaches the review, and never reaches the dirty count either. Both filters require the token to still be present with a changed value.", "evidence": "ReviewModal.tsx:84-92 both filters are `const saved = …find(s => s.id === t.id); return saved !== undefined && t.value !== saved.value;` — they iterate the CURRENT token list, so a token that is gone cannot be a row. DesignSystemTab.tsx:180-184 comment on dirtyCount: 'Removals are not counted here; deleteToken UX is a separate concern.'", "edit": "CONVENTION: this file renames with a `[not-implemented] ` PREFIX (see 306:2111/2136/2161), not a leading 'NOT IMPLEMENTED —'. Apply that prefix form to the rename below. Delete the 'removed' row from 1172:4840 (folded into FIG-F-25's row replacement). If a delete-review is wanted, it needs its own board and its own code arc — do not leave it drawn as if it ships.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-27", "module": "brand", "action": "mark-unimplemented", "target": "1776:8373 / 775:4305", "claim": "Brand · loading is drawn as a loading state of the Brand panel, and its caption 788:4309 says 'Rows keep their 32h so the list does not jump when tokens, presets and counts arrive.'", "truth": "The Brand panel has no loading state and cannot have one. It loads synchronously from `composer.getProjectSettings()` inside a callback — there is no promise, no isLoading flag and no skeleton anywhere in the file. The board itself draws only the panel header on an empty body, so it does not even draw the skeleton rows its caption describes.", "evidence": "DesignSystemTab.tsx:376-411 loadFromComposer is fully synchronous (`const settings = composer.getProjectSettings();` … `resetAllKinds(merged)`), wrapped in try/catch, no async. `grep -n 'Skeleton|isLoading|loading' DesignSystemTab.tsx` → 2 hits, both inside strings/comments (:384, :443), no state and no component. Live read of 775:4305 (2026-09-06): the only content is the 'Brand' Panel header instance.", "edit": "CONVENTION: this file renames with a `[not-implemented] ` PREFIX (see 306:2111/2136/2161), not a leading 'NOT IMPLEMENTED —'. Apply that prefix form to the rename below. Rename 775:4305 to 'Brand · loading — NOT IMPLEMENTED (the panel loads synchronously from project settings; there is no async fetch to show)'. Keep the frame. Fix caption 788:4309 per FIG-F-28.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-28", "module": "brand", "action": "fix-board", "target": "1776:8373 / 788:4309", "claim": "caption/Brand · loading describes 32h skeleton rows holding their height while tokens, presets and counts arrive.", "truth": "The board draws no rows and the code has no async load, so the caption describes something neither family contains.", "evidence": "Same as FIG-F-27: DesignSystemTab.tsx:376-411 synchronous; live read of 775:4305 shows header only.", "edit": "Rewrite TEXT 788:4309 to: 'NOT IMPLEMENTED — the Brand panel reads its tokens synchronously from project settings, so there is no fetch to show. Kept as design intent if brand ever moves behind a request.'", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-29", "module": "brand", "action": "none", "target": "1776:8373 / 781:4311 + 788:4310", "claim": "Brand · load-error draws 'Couldn\\u2019t load your brand system.' / 'Your tokens are safe — only this list failed to load.' / 'Try again'.", "truth": "Matches the shipped screen string for string, including the retry label.", "evidence": "DesignSystemTab.tsx:668-676 `<PanelErrorState title=\"Couldn't load your brand system.\" message=\"Your tokens are safe — only this list failed to load.\" onRetry={…} />`; PanelErrorState.tsx:35-38 renders the retry button labelled 'Try again'.", "edit": "No edit. Coverage recorded.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-30", "module": "brand", "action": "none", "target": "1776:8373 / 154:78 + 155:68", "claim": "Brand · dirty draws a pinned bottom bar: 'Unsaved brand changes' · Discard · Save, and its caption says brand edits do not auto-save.", "truth": "Both are exactly right, including the clean-state wording that 1333:7162 shows.", "evidence": "DesignTabFooter.tsx:20 `export const APPLY_CHANGES_LABEL = \"Save\";`; :64 `{isDirty ? \"Unsaved brand changes\" : \"Brand is up to date\"}`; :79 'Discard'; :87 the Save label. Non-autosave is deliberate: DesignSystemTab.tsx:360-366 explains the panel does NOT raise the project dirty flag so autosave cannot clear it.", "edit": "No edit. Coverage recorded.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-31", "module": "brand", "action": "none", "target": "1776:8373 / 1138:13376", "claim": "Brand · empty is already retired in the file: 'RETIRED 2026-09-02 (no producer)'.", "truth": "Correct. Zero tokens is unreachable: every registry seeds from DEFAULT_TOKENS, and when project settings carry no designTokens the panel keeps the seed and shows a first-run hint instead of an empty state. Interior read 2026-09-06: it draws 'No brand set.' / 'Start from a theme or import your client’s tokens.' / 'Browse starters · Import' — a state the seeded registries make unreachable.", "evidence": "DesignSystemTab.tsx:397-404 — the `else` branch of `if (settings.designTokens && settings.designTokens.length > 0)` sets `setIsFirstLoad(true)` and does NOT clear the registries; :826-833 renders the hint 'These are your site\\u2019s default design tokens. Customize them and click Save to go live.' StylePresetRegistryContext.tsx:61 falls back to DEFAULT_PRESETS.", "edit": "No edit. Coverage recorded.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-32", "module": "brand", "action": "none", "target": "1776:8373 / 1706:8467 + 1706:8476 + 1706:8483 + 1706:8492", "claim": "The four AI-prompt modal boards draw idle, generating, success (schema preview + Retry / Discard) and error ('AI service not configured' + Retry / Cancel).", "truth": "All four match. In particular the success board correctly draws NO Accept button — the modal's `onAccept` is optional and its only mount omits it, so no Accept renders and no schema is silently dropped. The error board's string is the code's literal. Idle and generating read in full 2026-09-06 and match too: 'Describe what you want. The AI returns a structured schema bound to your design system.' with the same placeholder, Cancel + Generate; and 'Generating schema…' with 'Cancel generation'.", "evidence": "AIPromptModal.tsx:40-44 the optional-onAccept contract and why ('It was required, so the only consumer passed an empty function and the Accept button closed the modal and dropped the schema'); DesignSystemTab.tsx:888-899 mounts it with no onAccept and says so. The error string is AIPromptModal.tsx:75 `setState({ kind: \"error\", message: \"AI service not configured\" })`. Idle/generating strings: AIPromptModal.tsx:130 the description, :47-48 PLACEHOLDER, :197-200 Cancel + Generate, :156-158 'Generating schema…', :205-207 'Cancel generation'.", "edit": "No edit. Coverage recorded.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-33", "module": "brand", "action": "fix-board", "target": "1776:8373 / 153:29", "claim": "Brand · components pins '✨ Generate with AI' at the foot of the list as a live call to action.", "truth": "In production that button always fails. The AI client is constructed only when the dsAi feature flag is on, and the shipping editor reads the NEXT_PUBLIC_ half of that pair; with the flag off the service holds a null client and the modal's only reachable state is the error board. The CTA is enabled regardless — it is disabled only when the callback prop is missing, which never happens.", "evidence": "useComposerInit.ts:132-137 `const aiClient = isFeatureEnabled(\"dsAi\") ? new ComponentSchemaAIClient({…}) : null;` → Composer.ts:268 `new AIAssistService(this, config.aiClient ?? null)` → AIAssistService.ts:70-71 `if (!this.client) throw new AIPromptRejectedError(...)`. Flag plumbing: featureFlags.ts:15 `dsAi: FEATURE_DS_AI`; runtimeEnv.ts:87-88 `VITE_FEATURE_DS_AI ?? NEXT_PUBLIC_FEATURE_DS_AI === \"true\"`. The CTA: ComponentsSection.tsx:165-177 `disabled={!onOpenAIAssist}` and DesignSystemTab.tsx:836-839 always passes it.", "edit": "On 153:29 give the '✨ Generate with AI' row the same disabled-with-reason treatment 153:120 uses for its Figma export row: greyed, with the reason inline — 'Available when AI is enabled for your workspace'. Add a second board 'Brand · components · AI enabled' if the live variant is still wanted.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-34", "module": "brand", "action": "none", "target": "1776:8373 / 153:120 + 306:2232 + 306:2265 + 306:2298 + 155:65", "claim": "Brand · import-export draws a Dark-strategy select above four export rows (CSS / JSON / Tailwind with Copy+Download, Figma disabled with its reason inline), a stats line, a collapsible preview, a drop zone with an 'or paste JSON' expander, a Detected/Valid/Errors/Conflicts line and 'Apply N valid only'; 306:2232/2265/2298 draw the exported / imported / import-failed badges.", "truth": "Every one of those ships, in that arrangement, including the deliberate disabled Figma row and its exact reason string, and the three status badges.", "evidence": "ExportSection.tsx:73-84 the four rows with `{ id: \"figma\", label: \"Figma Variables JSON\", desc: \"Coming soon — export JSON and use the Figma Variables importer\", disabled: true }` and the note at :77-81 explaining why the stub emitter was pulled; :187-188 format + darkStrategy state. ImportCard.tsx:124-125 showPaste/pasteBuffer, :179-186 the diff computed BEFORE any write, :400-404 'Apply {applyCount} valid only'. Badges: SectionStatusBadge.tsx:45 `\"draft\" | \"exported\" | \"imported\" | \"import-failed\"`, mounted at DesignSystemTab.tsx:687-699.", "edit": "No edit. Coverage recorded for five boards.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-35", "module": "brand", "action": "fix-board", "target": "1776:8373 / 153:120 + 306:2232 + 306:2265 + 306:2298", "claim": "The import drop zone reads 'Drop .json or .ts' on 153:120 and 'Drop .css or .json' on all three export-status boards.", "truth": "The family disagrees with itself and neither string is true. The file input accepts application/json, .json, .ts and .js — never .css — and the only parser is parseImportJSON, so a .ts or .js drop always lands in the error state and a .css drop cannot be chosen at all. The shipped copy is 'Drop .json or .ts'.", "evidence": "ImportCard.tsx:276 `accept=\"application/json,.json,.ts,.js\"`; :269-272 the board-cited centred line and the rendered string 'Drop .json or .ts'; :93 'unsupported formats (Tailwind .ts AST) currently surface as parse errors'; :26 the only parser import `parseImportJSON`; :170-177 the parse-error branch.", "edit": "Set the drop-zone TEXT to 'Drop .json' on all four boards — 153:120, 306:2232, 306:2265 and 306:2298 — so the family agrees and the string is true. If Tailwind import is to stay as design intent, add it beneath as a second disabled line with its reason, mirroring the Figma export row on the same boards.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-36", "module": "brand", "action": "add-state", "target": "1776:8373 / 1333:7162", "claim": "No board in section 1776:8373 draws any of the Brand panel's four toasts.", "truth": "Four toasts ship off the Brand panel and each carries information that exists nowhere else: the discard toast with its Undo action, the cross-window conflict warning, the canvas-undo/redo reassurance, and the apply failure.", "evidence": "DesignSystemTab.tsx:577-583 `${count} change${…} discarded` with `action: { label: \"Undo\", onClick: … }`; :434-437 'Design tokens changed from another window. Your edits may conflict.' (tone warning); :448-451 'Canvas undo/redo — your unsaved design token edits were kept.' (tone info); :554 success 'Design tokens applied successfully' and :556 error 'Failed to apply tokens. Try again.'", "edit": "Create one 1440x900 board 'Brand · toasts' in section 1776:8373 showing the four toasts stacked in the shell's toast region, cloning the existing toast component: (1) info + Undo action, 'N changes discarded'; (2) warning, 'Design tokens changed from another window. Your edits may conflict.'; (3) info, 'Canvas undo/redo — your unsaved design token edits were kept.'; (4) error, 'Failed to apply tokens. Try again.'", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-37", "module": "brand", "action": "add-state", "target": "1776:8373 / 152:52", "claim": "No Brand board draws a reset-to-defaults path.", "truth": "Spacing — and only spacing — has a factory reset that stages the seed values for Review/Apply and raises its own toast. It is a real, reachable state on the Tokens → spacing screen and nothing in the section draws it.", "evidence": "SpacingTokenList receives `onResetToDefaults={() => onResetSpacingToDefaults?.()}` (TokensSection.tsx:465) which reaches DesignSystemTab.tsx:577-580 handleResetSpacingToDefaults → `spacing.stageDefaults(DEFAULT_TOKENS)` + toast 'Spacing reset to defaults — review and Apply to save.' useSpacingTokens.ts:116 writes the defaults straight onto document.documentElement.", "edit": "Clone 152:52's spacing drill-in to a new 280x812 board 'Brand · tokens · spacing · reset' in section 1776:8373: the spacing list with a 'Reset to defaults' ghost action in its header, and the info toast 'Spacing reset to defaults — review and Apply to save.' Note on the board that this action exists for spacing only.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-38", "module": "brand", "action": "mark-unimplemented", "target": "1776:8373 / 1333:7162", "claim": "The Brand root's first strip says 'Everyday styling lives here. The brand syncs from your workspace shared theme.' with an 'Open Shared theme ↗' button.", "truth": "The shared theme does not carry a brand. Capture reads and push writes `Site.projectStyles`, which holds StyleEngine CSS rules keyed by element id, while a site's brand lives in `projectSettings.designTokens` — a column the push never names. So an agency push changes no colour, font or spacing on any client site, and it OVERWRITES each target's own responsive and pseudo-state rules with rules targeting element ids that do not exist there. The capture error even tells the user to set brand colours and type, which writes the field the capture does not read.", "evidence": "theme.service.ts:79 `select: { projectStyles: true }`; :184 `projectStyles: styles`; :91 the error 'That site has no styles to capture yet — open it, set your brand colours and type, then capture.' Brand's Apply writes elsewhere: DesignSystemTab.tsx:539-543 `designTokens: tokenRecords, designTokensSchemaVersion, designPresets`. What projectStyles actually is: BuildrikSyncProvider.ts:310 'projectStyles holds StyleEngine CSS rules ({id, selector, properties})'; StyleEngine.ts:222,245,270 `const selector = \\`[data-buildrick-id=\"${elementId}\"]\\``. The editor link out is DesignSystemTab.tsx:664 `window.open(\\`${DASHBOARD_URL}/dashboard/agency/theme\\`)`. Cross-filed as MOD-F-01/02/03.", "edit": "CONVENTION: this file renames with a `[not-implemented] ` PREFIX (see 306:2111/2136/2161), not a leading 'NOT IMPLEMENTED —'. Apply that prefix form to the rename below. Rename 1333:7162 to 'Brand · root — NEW 2026-08-27 (CURRENT) — NOT IMPLEMENTED: the shared-theme strip (capture/push moves projectStyles CSS rules, not designTokens, so no brand syncs)'. Keep the strip drawn. Add a red annotation beside it naming theme.service.ts:79,184 so the strip is not re-implemented against the wrong column.", "severity": "Critical", "confidence": "measured"}
{"id": "FIG-F-39", "module": "brand", "action": "add-state", "target": "1776:8373 / 1333:7162", "claim": "No editor board shows the workspace half of the brand: whether a shared theme has been captured, whether THIS site is themeLocked, or that a push has landed.", "truth": "All three are server fields the editor could read and none is surfaced anywhere; the editor's only reference to the whole feature is a link out to the dashboard.", "evidence": "theme.service.ts:60-66 (Workspace.sharedTheme / sharedThemeUpdatedAt), :107-113 listThemeTargets, :117-127 (Site.themeLocked). The editor's single reference is DesignSystemTab.tsx:664. Cross-filed as MOD-F-32; W-G-01/02/03 cover the root's own board defects.", "edit": "Add a 280x812 board 'Brand · root · theme-locked' in section 1776:8373, cloning 1333:7162 with the strip in a locked variant: a lock glyph, 'Brand managed by your workspace', a captured-on timestamp, and the destination rows for Tokens/Presets rendered read-only. Draw it only alongside the FIG-F-38 annotation so nobody builds it on the wrong column.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-40", "module": "brand", "action": "mark-unimplemented", "target": "1776:8373 / 433:2391", "claim": "B9.5 · Migration progress modal draws a running and a failed state for the design-system project migration.", "truth": "The modal itself ships and both states are driven by real events — but what the runner migrates is the wrong array. It is handed `data.styles` (the CSS-rule list) as `project.tokens`, so migration 0003 appends a design-token record into the site's CSS-rule array and no token is ever migrated; dsSchemaVersion is then bumped so it never runs again. The modal reports success over a migration that migrated nothing and corrupted the styles list. The modal's drawn shape is correct and fully wired: both states, the step list, the v0→v3 range, 'Snapshot saved', 'Migrations applied 2 of 3', 'Stuck at v3', and both actions render.", "evidence": "useComposerInit.ts:196 `project: { tokens: (data.styles ?? []) as unknown as DesignToken[] }` and :202 writes it back as `styles: result.project.tokens`. 0003-add-color-warning.ts:52-53 appends WARNING_TOKEN to `project.tokens`; its header (:8-9) says the target is 'projects loaded from a saved designTokens payload'. BuildrikSyncProvider.ts:316-325 has already filtered every token-shaped entry out of `styles`. The modal is real: MigrationProgressMount.tsx:5-16 subscribes to migration:started/complete/failed, mounted at StudioPanels.tsx:406. Cross-filed as MOD-F-05. MigrationProgressModal.tsx:77 the two titles, :136-142 the Snapshot saved / Migrations applied / Stuck at rows, :146-158 'Restore snapshot' and 'Retry v{stuckAt}'; MigrationProgressMount.tsx:154-155 passes both handlers when state === 'failed'.", "edit": "CONVENTION: this file renames with a `[not-implemented] ` PREFIX (see 306:2111/2136/2161), not a leading 'NOT IMPLEMENTED —'. Apply that prefix form to the rename below. Rename 433:2391 to 'B9.5 · Migration progress modal — running · failed — NOT IMPLEMENTED as drawn: the runner is fed projectStyles as project.tokens (useComposerInit.ts:196), so no token migrates and success is reported over a corrupted styles array'. Keep both states drawn.", "severity": "Critical", "confidence": "measured"}
{"id": "FIG-F-41", "module": "brand", "action": "none", "target": "1776:8373 / 153:2 + 1704:8575", "claim": "Brand · classes draws a read-only list of '.name / used N×' rows, and Brand · classes · empty draws the empty copy.", "truth": "Both match. ClassesSection is read-only by construction — it counts class names off the element tree, offers no create/rename/delete, and the empty copy is byte-identical.", "evidence": "ClassesSection.tsx:25 the map is built from the tree ('`.name` → how many elements carry it, most-used first'); :59-62 the empty copy 'No classes yet. A class is a name you put on elements so they can share one rule — add one from an element\\u2019s Classes section.'; :76 the row's 'used {count}&times;'. No Button and no onClick in the file.", "edit": "No edit. Coverage recorded for two boards.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-42", "module": "brand", "action": "none", "target": "1776:8373 / 154:26 + 306:2217 + 155:67", "claim": "Brand · lint-warnings and Brand · lint · suppressed are the root with a Lint count and with a 'Warnings suppressed' pill.", "truth": "Both ship. The pill is rendered at the root when the suppressed count is non-zero, and suppression is reachable — the 'Ignore' button on the token-detail lint row calls it.", "evidence": "DesignSystemTab.tsx:213-215 `const suppressedCount = composer?.designSystem?.lintState?.suppressedCount?.() ?? 0;` and :743-752 renders the 'Warnings suppressed' pill when > 0. The producer is TokenDetailView.tsx:239 `lintState?.suppress(token.id)` behind the Ignore button (:329-339); a second producer is AquibraStudio.tsx:639 (Issues panel onIgnore). LintState.ts:117-118 records that board 306:2217 is what suppressedCount() was added for.", "edit": "No edit. Coverage recorded. Note for the coordinator: suppression is session-only — LintState.ts:47 `private suppressed = new Set<string>()` with no persistence — so the pill disappears on reload. No board claims otherwise.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-43", "module": "brand", "action": "none", "target": "1776:8373 / 152:52", "claim": "Brand · tokens draws the kind drill-in with a 'More token kinds' disclosure and the first-run hint.", "truth": "Matches. The disclosure and the hint string both ship verbatim.", "evidence": "TokensSection.tsx:393-406 the 'More token kinds' / 'Fewer token kinds' disclosure; DesignSystemTab.tsx:826-833 the hint 'These are your site’s default design tokens. Customize them and click Save to go live.'", "edit": "No edit. Coverage recorded.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-44", "module": "brand", "action": "none", "target": "1776:8373 / 155:56 + 155:57 + 155:59 + 155:60 + 155:61 + 155:62 + 155:64 + 155:65", "claim": "Eight of the fourteen Brand captions describe their screens.", "truth": "These eight are accurate, read in full 2026-09-06: 155:56 root ('Nine sections, each a 32h row with a count'), 155:57 tokens ('Kind list first (14 kinds), then the token list, then detail'), 155:59 presets, 155:60 starters ('6-card gallery, a drill-in rather than the old modal'), 155:61 classes, 155:62 components ('Generate with AI as a row at the bottom rather than a floating button'), 155:64 colour-mode ('the missing list is the point of the screen'), 155:65 import-export (four rows, Figma disabled with its reason inline, import diffs before writing). The other six captions are filed separately: 155:58 (FIG-F-70), 155:63 (FIG-F-05), 155:66 (FIG-F-14), 155:67 + 155:68 (FIG-F-42/FIG-F-30, both accurate), 155:69 (FIG-F-01), 788:4309 (FIG-F-28).", "evidence": "Nine rows: DesignSystemTab.tsx:133-149 rendered at :770-812 with tw:h-13. 14 kinds: TokensSection.tsx:55-70 KIND_ORDER; three levels: TokensSection kind list → the per-kind lists → TokensRouter.tsx:54-73 TokenDetailView. Starters: StartersSection.tsx:47-70 grid + warning, STARTER_DS_REGISTRY length 6 (starters/index.ts:18-25). Classes usage counts: ClassesSection.tsx:25,76. AI row pinned at the foot: ComponentsSection.tsx:55 AI_ROW + :165-177. Colour-mode missing list: ColourModeSection.tsx:40-43,61-67. Import/export: ExportSection.tsx:73-84, ImportCard.tsx:179-186.", "edit": "No edit. Coverage recorded for eight caption nodes, each read in full.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-45", "module": "components", "action": "mark-unimplemented", "target": "1938:8372 / 641:2546", "claim": "Components · library draws two sections: 'YOUR COMPONENTS' (name · N instances · ›) and 'FROM BRAND' with rows badged 'linked'.", "truth": "There is no brand-linked component and no 'linked' state anywhere in the model. ComponentDefinition has no field that could carry a brand link, the panel renders one section, and the code says the second is deliberately out because it would always be empty chrome.", "evidence": "shared/types/components.ts:19-48 is the whole ComponentDefinition — id, name, description, category, tags, masterTree, thumbnail, createdAt, updatedAt, version, variantProperties, variants, defaultVariantId, prefillFromDs. No brand/source/linked field. ComponentsTab.tsx:231-234 states the call: 'Board 641:2546 (Components · library): no search, no filter chips — one \"YOUR COMPONENTS\" section of 32h rows (name · \"N instances\" · ›)… The FROM BRAND section ships when a brand-linked source exists; today\\u2019s registry has none, so it would always be empty chrome.' The list itself is :239-266 — one section header, one map, one footer button.", "edit": "CONVENTION: this file renames with a `[not-implemented] ` PREFIX (see 306:2111/2136/2161), not a leading 'NOT IMPLEMENTED —'. Apply that prefix form to the rename below. Rename the 'FROM BRAND' section frame inside 641:2546 to 'FROM BRAND — NOT IMPLEMENTED (no brand-linked component exists; ComponentDefinition has no source field)' and grey its two rows to the disabled treatment. Do not delete the section. Keep 'YOUR COMPONENTS' exactly as drawn — it matches.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-46", "module": "components", "action": "fix-board", "target": "1938:8372 / 788:4314 + 778:4173", "claim": "caption/Components · loading says 'Both sections stay so the split between your components and brand components survives the wait', and 778:4173 draws skeleton plates under both YOUR COMPONENTS and FROM BRAND headers.", "truth": "There is one section, not two (see FIG-F-45), and the shipped loading state is four SkeletonListItem rows with 24px avatars rendered above the list — no section headers at all while `isLoaded` is false.", "evidence": "ComponentsTab.tsx:240-244 `{!state.isLoaded && (<div style={{padding:\"12px\"}}>{Array.from({length:4}).map((_,i)=><SkeletonListItem key={i} hasAvatar avatarSize={24} textLines={1} />)}</div>)}`. The section header (:246-250) renders in the same pass but carries no skeleton semantics. FROM BRAND: see FIG-F-45's evidence.", "edit": "On 778:4173 drop the FROM BRAND header and its skeleton plates; keep four skeleton rows with a 24px leading avatar circle and one text line each under a single 'YOUR COMPONENTS' header. Rewrite caption 788:4314 to: 'Component list fetching — four skeleton rows with a 24px avatar under the one section header. The primary button stays live because it does not depend on the list.'", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-47", "module": "components", "action": "mark-unimplemented", "target": "1938:8372 / 641:2599", "claim": "Components · detail draws a 'PROPERTIES' section listing per-node component properties with types: 'Image — instance swap', 'Title — text', 'Price — text · CMS bound'.", "truth": "The product has no component-property concept at all. A ComponentDefinition carries variantProperties — axes of variation like Size/State/Theme — and nothing else that could be a property; there is no per-node property, no 'instance swap' type, and no CMS binding on a component. The detail screen renders Type / Tags / Description instead.", "evidence": "shared/types/components.ts:19-48 (ComponentDefinition) and :55-62 (VariantProperty is `{name, values[], defaultValue}` — an axis, not a bound property). Grep across packages/editor/src for 'instance swap' and 'instanceSwap' returns nothing. The shipped info block is ComponentDetailScreen.tsx:294-310: rows 'Type' (component.category || 'UI component'), 'Tags' (component.tags joined, else 'No tags'), and 'Description' when present.", "edit": "CONVENTION: this file renames with a `[not-implemented] ` PREFIX (see 306:2111/2136/2161), not a leading 'NOT IMPLEMENTED —'. Apply that prefix form to the rename below. Rename the 'PROPERTIES' section frame inside 641:2599 to 'PROPERTIES — NOT IMPLEMENTED (a component has no per-node properties; ComponentDefinition carries variantProperties only)' and keep it drawn. Then add the shipped info rows above it per FIG-F-49.", "severity": "Critical", "confidence": "measured"}
{"id": "FIG-F-48", "module": "components", "action": "mark-unimplemented", "target": "1938:8372 / 641:2599", "claim": "The 'USED ON' section breaks instances down per page — 'Menu 12 instances ›', 'Home 4', 'Catering 2' — each drilling in.", "truth": "The editor can only produce one flat number for the whole site. getInstancesOfComponent reads an in-memory map that rehydrateInstances builds by walking every element on the open site, with no page attribution and no drill-in. The one API that could answer 'which pages use this?' — siteComponents.usage — has no caller anywhere in the editor or the dashboard. That matters beyond the board: workspaceDelete removes a master from EVERY site behind a UI that never asks.", "evidence": "ComponentDetailScreen.tsx:247 `const instanceCount = composer?.components?.getInstancesOfComponent?.(component.id)?.length ?? 0;` → ComponentManager.ts:145 rehydrateInstances iterates `composer.elements.getAllElements()`, storing `{elementId, componentId, …}` with no pageId. Router exists at site-component.ts:84-90 (`usage`); callers: componentSync.ts uses upsert/delete/list/get only, the dashboard uses workspaceList/workspaceRename/workspaceDelete (library-panel.tsx:24,37,46). workspaceDelete is site-component.ts:104-110. Cross-filed as MOD-F-23.", "edit": "CONVENTION: this file renames with a `[not-implemented] ` PREFIX (see 306:2111/2136/2161), not a leading 'NOT IMPLEMENTED —'. Apply that prefix form to the rename below. Rename the 'USED ON' section frame inside 641:2599 to 'USED ON — NOT IMPLEMENTED (instance counts are a single flat site-wide number; siteComponents.usage, which could answer per-page, has no caller)'. Keep the section drawn — it is the right design and the server route already exists.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-49", "module": "components", "action": "fix-board", "target": "1938:8372 / 641:2599", "claim": "The detail screen's action row is two buttons: 'Edit main component' and 'Detach all'.", "truth": "Neither exists. The shipped screen is a preview plate, three info rows, one full-width primary 'Insert Component', then a three-up equal-width secondary row — Duplicate · Update · Delete — and, only when an instance is selected AND the DS mode is Pro, an 'Instance Actions' block with a single 'Detach instance'. detachAllInstances exists in the engine but its only caller is deleteComponent, so nothing in the product performs a 'Detach all'.", "evidence": "ComponentDetailScreen.tsx:269-393 is the whole body: PREVIEW plate (:280-292), Type/Tags/Description (:294-310), `<Button size=\"sm\" onClick={handleInsert} className=\"tw:w-full\">Insert Component</Button>` (:312-314), the `tw:grid tw:grid-cols-3` row with Duplicate (:318-327), Update (:328-341, disabled when nothing is selected) and Delete (:342-355, color=\"failure\"), then `{isInstanceSelected && isProMode && …}` Detach instance (:365-381). detachAllInstances callers: `grep -rn detachAllInstances` → ComponentManager.ts:33 (import), :393 (inside deleteComponent), ComponentInstances.ts:207 (definition). No UI caller.", "edit": "Rebuild 641:2599's lower half: replace the two footer buttons with (1) a full-width primary 'Insert Component', (2) a three-column secondary row Duplicate · Update · Delete with Delete in the failure colour, and (3) a separate 'INSTANCE ACTIONS' section holding one ghost 'Detach instance'. Add the preview plate and the Type / Tags / Description rows above PROPERTIES. Then add a second board 'Components · detail · Beginner' identical but with the INSTANCE ACTIONS section absent — it renders only in Pro (ComponentDetailScreen.tsx:367).", "severity": "Critical", "confidence": "measured"}
{"id": "FIG-F-50", "module": "components", "action": "mark-unimplemented", "target": "1938:8372 / 1170:4792", "claim": "The left half of this board is a 'Switch variant' picker — Default (✓) · Compact · With CTA.", "truth": "A component's `variants` array is always empty, so the picker can never list anything, and its entry point is not rendered at all. The four variant-authoring APIs have zero non-test callers; the menu that opened the picker lives in ComponentRow, which nothing imports; and selecting a value in the Inspector's own VariantSection looks up a variant in an always-empty array and returns silently.", "evidence": "Authoring APIs with no caller: ComponentManager.ts:311 setVariantProperties, :320 addVariant, :331 updateVariant, :347 removeVariant — `grep -rn` finds only their definitions and tests. The picker's opener is useComponentsState.ts:336-378 handleSwapVariant, exposed at :505-507; its only consumer is ComponentRow.tsx:103-ish, and `grep -rn ComponentRow packages/editor/src` outside its own file returns component-library/index.ts:9 (barrel) and __tests__/ComponentRow.test.tsx — ComponentsTab.tsx renders inline <div> rows instead (:250-286). The Inspector half: VariantSection.tsx:78-84 `variants.find(...)` over `component.variants` and ComponentVariantResolver.ts:130 bails on `!component?.variants?.length`. The detail screen's own VariantPicker only sets local React state (ComponentDetailScreen.tsx:124-129). Cross-filed as MOD-F-18.", "edit": "CONVENTION: this file renames with a `[not-implemented] ` PREFIX (see 306:2111/2136/2161), not a leading 'NOT IMPLEMENTED —'. Apply that prefix form to the rename below. Rename 1170:4792 to 'Components · variant-picker — NOT IMPLEMENTED (component.variants is always empty; addVariant/updateVariant/removeVariant have no callers and the menu that opened this is unmounted) · detach-confirm'. Keep both halves drawn — the detach-confirm half is correct (FIG-F-51).", "severity": "Critical", "confidence": "measured"}
{"id": "FIG-F-51", "module": "components", "action": "none", "target": "1938:8372 / 1170:4792", "claim": "The right half draws 'Detach from component?' / 'This copy stops receiving updates from \\'Hero banner\\'. The component itself is untouched.' / Cancel · Detach, with Detach in the accent colour rather than destructive.", "truth": "Matches the shipped confirm word for word, including the non-destructive tone.", "evidence": "ComponentDetailScreen.tsx:441-448 `<ConfirmDialog open={pendingDetach !== null} … title=\"Detach from component?\" message={\\`This copy stops receiving updates from \"${pendingDetach ?? \"\"}\". The component itself is untouched.\\`} confirmLabel=\"Detach\" />` — no `tone` prop, so not destructive, and the comment at :435-440 records that the board draws an accent button.", "edit": "No edit to this half. Coverage recorded.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-52", "module": "components", "action": "mark-unimplemented", "target": "1938:8372 / 1170:4777", "claim": "The create modal's variant block closes with the hint 'You can configure variant values after creation'.", "truth": "There is nowhere to configure them. variantProperties is written on create and never edited again; the four APIs that would edit it have no callers; and the only screen that reads variantProperties renders a picker whose onChange sets local React state and touches nothing.", "evidence": "CreateComponentModal.tsx:88-101 builds variantProperties from VARIANT_PRESETS and :107 passes it to createComponent (ComponentManager.ts:188). Editors with no caller: ComponentManager.ts:311 setVariantProperties, :320 addVariant, :331 updateVariant, :347 removeVariant. The reader: ComponentDetailScreen.tsx:383-393 maps variantProperties to <VariantPicker> whose onChange is handleVariantChange (:124-129) — `setSelectedVariantValues` and nothing else.", "edit": "CONVENTION: this file renames with a `[not-implemented] ` PREFIX (see 306:2111/2136/2161), not a leading 'NOT IMPLEMENTED —'. Apply that prefix form to the rename below. Rewrite that hint TEXT on 1170:4777 to: 'NOT IMPLEMENTED — variant values cannot be configured after creation; nothing in the product edits variantProperties or adds a variant.' Keep the chips drawn.", "severity": "Critical", "confidence": "measured"}
{"id": "FIG-F-53", "module": "components", "action": "mark-unimplemented", "target": "1938:8372 / 1170:4777", "claim": "The create modal draws a checked 'Pre-fill from DS styles' toggle over the promise '2 styles will bind to your DS tokens. Editing tokens later updates this component too.'", "truth": "Nothing binds. The count is computed and then dropped — createComponent is called without it and ComponentDefinition has no bindings field — and the resolver that produces the count matches only `{{token.…}}`, a syntax the product no longer writes, so on a real selection the number is 0. The toggle's own value is stored and read by nothing.", "evidence": "TokenBindingResolver.ts:23 the sole regex `/^\\{\\{token\\.([a-zA-Z0-9._-]+)\\}\\}$/`. The same bug fixed on the other side documents why it is wrong: TokenUsageTracker.ts:33-38 — '`{{token.…}}` only ever appears in template HTML strings, and resolveTemplateTokens substitutes those away before import… Everything a user binds through the UI is written as `var(--buildrick-design-<id>)`' — and matching only the first form 'meant getUsage() returned 0 for every token in real use'. The count's only use: CreateComponentModal.tsx:56 `const bindingCount = selectionContext ? selectionContext.extractedBindings.size : null;` rendered at :245-250; createComponent at :103-111 is called without it. ComponentDefinition (shared/types/components.ts:19-48) has no bindings field; prefillFromDs is stored (ComponentManager.ts:189) and read nowhere — components.ts:44-47 admits 'UI today just records the user\\u2019s intent.' Cross-filed as MOD-F-20.", "edit": "CONVENTION: this file renames with a `[not-implemented] ` PREFIX (see 306:2111/2136/2161), not a leading 'NOT IMPLEMENTED —'. Apply that prefix form to the rename below. Rewrite the hint TEXT under 'Pre-fill from DS styles' on 1170:4777 to: 'NOT IMPLEMENTED — bindings are counted and discarded; a component stores no token bindings, and the resolver matches a syntax the editor no longer writes, so the count reads 0 on a real selection.' Keep the checkbox drawn.", "severity": "Critical", "confidence": "measured"}
{"id": "FIG-F-54", "module": "components", "action": "add-state", "target": "1938:8372 / 1170:4777", "claim": "The create modal says nothing about how much of the selection it saves.", "truth": "On a multi-selection it saves the FIRST element and discards the rest, silently. The canvas action deliberately collects every selected id and the modal takes `selectionIds[0]`.", "evidence": "standaloneActions.ts:23-31 builds `selectionIds` from the whole selection and emits it; StudioModals.tsx:247-248 passes `saveAsComponentContext?.selectionIds[0]` as the modal's single `elementId`, with the acknowledgement in the comment at :238-240 ('a multi-selection contributes its first id'). CreateComponentModal.tsx:83-86 refuses only when elementId is null. Cross-filed as MOD-F-21.", "edit": "Clone 1170:4777 to 'Components · create (modal) · multi-selection' in section 1938:8372 and add a warning row above the Name field, --bk-warning-tint plate, 12px: 'Only the first of 3 selected elements will be saved. Group them first to save the whole selection.'", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-55", "module": "components", "action": "fix-board", "target": "1938:8372 / 642:3112", "claim": "Board 642:3112 is named 'Components · create' and is a full 1440x900 editor frame.", "truth": "It draws the Layers panel open over the canvas, a six-item rail (Insert · Layers · Pages · Media · Content · Brand) and the inspector — and no component-creation affordance of any kind. The two real routes are the canvas right-click item 'Save as component' and the Components panel's own '+ Create component' footer button, and the board shows neither.", "evidence": "Full-depth walk of 642:3112 (2026-09-06): rail labels Insert/Layers/Pages/Media/Content/Brand; open panel header 'LAYERS'; no Components entry and no create control. The routes: standaloneActions.ts:14 `label: \"Save as component\"` (canvas context menu) and ComponentsTab.tsx:275-285 / :195-208 the '+ Create component' footer. tabsConfig.ts:325-340 confirms the six-item rail and that components is off-rail.", "edit": "Either (a) rebuild 642:3112 to show the canvas context menu open on a selected section with 'Save as component' highlighted, which is the route the modal 1170:4777 opens from; or (b) rename it 'Components · create · context (editor frame, no create affordance drawn)' so nobody reads it as the create flow. (a) is preferred — the flow has no full-frame board today.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-56", "module": "components", "action": "none", "target": "1938:8372 / 1138:13394", "claim": "Components · empty draws 'No components yet.' / 'Select an element on the canvas and save it as a component to reuse it everywhere.' / a 'Create component' text link / a bordered footer with '+ Create component'.", "truth": "All four match the shipped empty state exactly, including the left-aligned copy and the footer that keeps the primary button in the same place as the list view.", "evidence": "ComponentsTab.tsx:176-212 — EmptyStateTitle 'No components yet.', EmptyStateDesc 'Select an element on the canvas and save it as a component to reuse it everywhere.', the `data-testid=\"comp-empty-create-link\"` accent link 'Create component' (:194-201), and the bordered footer with `data-testid=\"comp-create\"` '+ Create component' (:203-212).", "edit": "No edit. Coverage recorded.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-57", "module": "components", "action": "mark-unimplemented", "target": "1938:8372 / 781:4433", "claim": "Components · load-error draws the error copy plus a live '+ Create component' footer button, and caption 788:4315 says 'Create component stays live because it does not depend on the list.'", "truth": "The headline and body match the code verbatim, and the retry label is right — but the error branch returns before any footer, so no create button renders when the list fails. The caption's promise is not kept.", "evidence": "ComponentsTab.tsx:88-113 — the `if (state.error)` branch returns `<PanelFrame>` + optional header + `<PanelErrorState title=\"Couldn't load your components.\" message=\"Your components are safe — only this list failed.\" onRetry={…} />` and nothing else; the `{onCreateNew && <div …>+ Create component</div>}` footers live only in the empty branch (:203-212) and the list branch (:275-285). PanelErrorState.tsx:35-38 supplies the 'Try again' label.", "edit": "CONVENTION: this file renames with a `[not-implemented] ` PREFIX (see 306:2111/2136/2161), not a leading 'NOT IMPLEMENTED —'. Apply that prefix form to the rename below. Grey the '+ Create component' footer button on 781:4433 to the disabled treatment and annotate 'NOT IMPLEMENTED — the error branch returns before the footer, so this button does not render today'. Do not delete it: the caption is the better product and the fix is one branch. Leave caption 788:4315 as written.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-58", "module": "components", "action": "none", "target": "1938:8372 / 641:2546", "claim": "Components · library draws no search field and no filter chips, and its rows are name · 'N instances' · ›.", "truth": "Right on all three. The shipped list is a 32px row with the name, the instance count and a chevron, with no search and no filter — and the row is draggable onto the canvas, which no board draws but which changes nothing visually at rest.", "evidence": "ComponentsTab.tsx:250-286 — a `tw:h-8` div per component with `component.name`, `{n} instance{n === 1 ? \"\" : \"s\"}` and '›', plus `draggable` + `onDragStart={(e) => state.handleDragStart(e, component)}` (:255,:259). No TextInput and no filter control in the file. Count source: `composer?.components?.getInstancesOfComponent?.(component.id)?.length || 0` (:251).", "edit": "No edit. Coverage recorded.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-59", "module": "components", "action": "none", "target": "1938:8372 / 641:2546", "claim": "No board in section 1938:8372 draws a rename affordance, and none should.", "truth": "Renaming a saved component ships in code — a full modal with an input, Enter-to-submit and success/error toasts — and is UNREACHABLE. The only thing that calls handleRename is ComponentRow's overflow menu, and ComponentRow is rendered nowhere: ComponentsTab replaced it with inline <div> rows. The same menu was the only door to Duplicate-from-list and Swap variant. Filed as `none` because the boards are correct not to draw it; recorded so it is not mistaken for a missing screen.", "evidence": "The modal: ComponentsTab.tsx:320-352 (ModalRoot bound to `state.renameTarget`, TextInput, Enter → `state.confirmRename`). Its opener: useComponentsState.ts:308-318 handleRename → setRenameTarget; exposed at :502-504. The only consumer of handleRename/onRename is ComponentRow.tsx:24,42,103, and `grep -rn ComponentRow packages/editor/src` outside that file returns only component-library/index.ts:9 and its own test. ComponentsTab renders inline rows at :250-286. `openMenuId` likewise reaches only ComponentRow.tsx:17,35,90,98.", "edit": "No Figma edit. Hand to the code owner: the rename / duplicate / swap-variant menu has no mount since ComponentsTab moved to inline rows.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-60", "module": "components", "action": "create-board", "target": "1938:8372 / 641:2599", "claim": "No board draws the Update-component confirm or what it reports afterwards.", "truth": "'Update from selection' is the reason components exist — change the master, every instance follows — and it is a destructive, two-step flow with three distinct outcomes that nothing in the file draws: the confirm naming the instance count and the override risk, the success toast naming how many instances followed, and the warning toast naming how many overrides were lost.", "evidence": "The confirm: ComponentDetailScreen.tsx:414-432 — `Replace \"<name>\" with the element selected on the canvas? N instance(s) will change to match. Any edits made on an instance are kept where they still fit, and lost where the new version no longer has that part.` + `Undo won't take the component back — it reverts the pages, not the component itself.`, confirmLabel 'Update component', tone destructive. The outcomes: :204-215 the success toast `\"<name>\" updated — N instances followed.` (or 'no instances placed yet'), :208-220 the warning toast `… M overrides couldn't be re-applied and were lost.` Driven by `updateComponentMaster` returning `{updated, instancesSynced, overridesDropped}` (ComponentManager.ts:275-289).", "edit": "Create a 720x220 board 'Components · update-from-selection (modal)' in section 1938:8372, cloning the ConfirmDialog used by 1170:4792: destructive tone, title 'Update component', the two-sentence body above with N=6, buttons Cancel · Update component. Then create a 'Components · update · outcomes' board showing the two toasts stacked — success '\"Menu card\" updated — 6 instances followed.' and warning '\"Menu card\" updated — 6 instances followed. 2 overrides couldn\\u2019t be re-applied and were lost.'", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-61", "module": "components", "action": "create-board", "target": "1938:8372 / 641:2599", "claim": "No board draws the delete-component confirm, although two different ones ship with different copy.", "truth": "Deleting a master detaches every instance and is not undoable, and there are TWO confirms in the code with different wording depending on where you delete from — the list view's and the detail screen's.", "evidence": "List view: ComponentsTab.tsx:287-303 — title `Delete \"<name>\"?`, message 'Instances already placed keep their content; they stop following this component. This cannot be undone.', confirmLabel 'Delete component', tone destructive. Detail view: ComponentDetailScreen.tsx:396-409 — title 'Delete Component', message `This component has N instance(s). Deleting will detach all instances. Continue?` (or `Are you sure you want to delete \"<name>\"?` when N=0), confirmLabel 'Delete'. Both call `composer.components.deleteComponent(id)` (ComponentManager.ts:389-406), which is outside the engine snapshot — the delete is not on the undo stack (cross-filed MOD-F-24).", "edit": "Create a 720x180 board 'Components · delete-confirm (modal)' in section 1938:8372 using the list view's copy — it is the better of the two — and annotate that the detail screen ships a second, differently-worded confirm (ComponentDetailScreen.tsx:396-409) that should converge on it.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-62", "module": "components", "action": "add-state", "target": "1938:8372 / 641:2546", "claim": "No board draws the component-sync failure.", "truth": "A master that cannot reach the server raises a persistent toast (duration Infinity) with a 'Retry now' action, and it retracts itself when the queue drains. It is the only signal a user gets that a component is device-local, and nothing in the file draws it.", "evidence": "useComponentSync.ts:68-94 — `addToast({ title: \"A component didn't sync to the cloud\", description: \"It's saved on this device but not yet shared across your sites. Retry now, or leave it — a reconnect replays the queue.\", tone: \"error\", duration: Infinity, action: { label: \"Retry now\", onClick: … retryComponentSync() } })`, subscribed via onComponentSyncError and cleared at :64-67 when getComponentSyncPendingCount() hits 0.", "edit": "Add a 720x120 board 'Components · sync-failed (toast)' in section 1938:8372 cloning the shell's error toast: title 'A component didn\\u2019t sync to the cloud', body 'It\\u2019s saved on this device but not yet shared across your sites. Retry now, or leave it — a reconnect replays the queue.', one action link 'Retry now', and no auto-dismiss timer bar.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-63", "module": "components", "action": "add-state", "target": "1938:8372 / 641:2546", "claim": "No board draws a component that is out of date on this device.", "truth": "It is reachable and permanent. The cross-device hydrate is additive-only: any componentId already present locally is skipped, so a device that holds v1 keeps v1 forever no matter how many times the master is edited elsewhere. Nothing renders 'this copy is older than the shared one', and the hydrate's own return value only triggers a silent re-read.", "evidence": "componentSync.ts:91-93 `const localIds = new Set((await loadComponents(siteId)).map(c => c.id)); for (const r of remote) { if (localIds.has(r.componentId)) continue; …}` — the pull never overwrites. The push is unconditional (mirrorComponentUpsert :47-62). useComponentSync.ts:39-43 consumes the count only to re-read locally. Cross-filed as MOD-F-22 / MOD-F-33.", "edit": "Clone 641:2546 to 'Components · stale-copy' in section 1938:8372: one YOUR COMPONENTS row carries a small warning dot and a right-aligned 11px --bk-warning-text label 'older than the shared copy', with a one-line banner above the list 'One component on this device is behind the version shared across your sites.' Draw it only alongside a note that the pull side must stop skipping known ids first.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-64", "module": "components", "action": "wire-edge", "target": "1938:8372 / 641:2546", "claim": "The prototype has no edge into Components · library, and the command palette family offers 'Toggle component view' as the way in.", "truth": "'Toggle component view' emits an event with no listener anywhere — it is a dead command with a printed shortcut. The two real doors are the ⇧A shortcut and the topbar ⋯ site menu's Components row; Components has no rail button in the shipping six-item rail.", "evidence": "Dead command: defaultCommands.ts:449-454 `run: () => composer.emit(EVENTS.UI_TOGGLE_COMPONENT_VIEW)`; `grep -rn UI_TOGGLE_COMPONENT_VIEW packages/editor/src` returns events.ts:356 (declaration), defaultCommands.ts:451,453 and one test — no listener. Real doors: SiteMenu.tsx:240 `<MenuItem kbd=\"⇧A\" onClick={run(onOpenComponents)}>`, useEditorShortcuts.ts:17 'Shift+A → left panel · components', tabsConfig.ts:163 `shortcut: \"⇧A\"`. There is also no ⌘K 'Open Components panel' command despite tabsConfig.ts:338 claiming one — grep for that string finds only comments.", "edit": "Wire a prototype edge from the topbar ⋯ site-menu row 'Components' (on the shell board) to 641:2546, and label it ⇧A. Do not wire any edge from the command-palette boards to 641:2546. Separately, remove or disable the 'Toggle component view' row on the ⌃⇧C command-palette board in section 13 — flag to that section's owner rather than editing it here.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-65", "module": "brand", "action": "fix-board", "target": "1776:8373 / 153:29", "claim": "Brand · components draws a drill-in list — one row per component, name left, '›' right — and no footer callout.", "truth": "Two mismatches. The rows carry a chevron and have no onClick at all, and the panel's own footer callout — which ships and the board omits — says 'every action here jumps to the Components panel to author', a jump no control on the screen performs. The row also carries a meta string the board only partly draws: 'N variants · M in use'.", "evidence": "ComponentsSection.tsx:141-157 catalog rows are `<Button color=\"light\" data-catalog-card={component.id} className={ROW}>` with no onClick; :194-209 saved rows likewise; the chevron is at :155 and :208; the meta is :151-154 `{variantCount} variant{…}{instanceCount > 0 ? \\` · ${instanceCount} in use\\` : \"\"}`. The footer callout is :215-223, rendered on every visit: '**Read-only by design:** Components live in their own panel. This summary lets you see what\\u2019s installed without leaving Brand — but every action here jumps to the Components panel to author.'", "edit": "On 153:29: (a) delete the '›' from every row, since nothing happens on click; (b) add the row meta '<n> variants · <m> in use' right-aligned in 11px --bk-ink-muted on each catalog row; (c) draw the footer callout below the saved section — --bk-accent-tint plate, --bk-accent-subtle border, 11px, with the bold lead-in 'Read-only by design:' — but change its last clause, since no control jumps anywhere: '… but authoring happens in the Components panel (⇧A).'", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-66", "module": "brand", "action": "fix-board", "target": "1776:8373 / 153:29 + 1704:8607", "claim": "Brand · components and its saved-empty variant draw the saved-components list as current.", "truth": "It is never current. The saved list is memoised on the composer identity and the per-row instance counts are read during render with no subscription, so creating, renaming or deleting a component leaves both the list and every count stale until the whole editor reloads. Its sibling panel does subscribe.", "evidence": "ComponentsSection.tsx:125 `const savedComponents = React.useMemo(() => getSavedComponents(composer), [composer]);` — composer identity is stable for the session; :139 and :194 call getInstanceCount during render. The file imports no EVENTS and registers no composer listener. The sibling does: useComponentsState.ts:160 `composer.on(EVENTS.COMPONENT_LIST_UPDATED, handleUpdate)`. Cross-filed as MOD-F-26.", "edit": "Add a red annotation beside 153:29 (not on the artboard): 'Saved list does not refresh — memoised on composer identity, no COMPONENT_LIST_UPDATED subscription (ComponentsSection.tsx:125). The counts shown are from panel-mount time.' No visual change to the boards.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-67", "module": "brand", "action": "none", "target": "1776:8373 / 1704:8607", "claim": "Brand · components · saved-empty draws 'YOUR SAVED COMPONENTS · 0' over 'No saved components yet — save a selection from the canvas to start.'", "truth": "Matches the shipped strings exactly, including the uppercase treatment.", "evidence": "ComponentsSection.tsx:179-181 `<div className={SAVED_HEADER} data-saved-header>Your saved components · {savedComponents.length}</div>` where SAVED_HEADER carries `tw:uppercase` (:58-59); :183-186 the empty plate `No saved components yet — save a selection from the canvas to start.`", "edit": "No edit. Coverage recorded.", "severity": "Minor", "confidence": "measured"}
{"id": "FIG-F-68", "module": "components", "action": "add-state", "target": "1938:8372 / 641:2599", "claim": "No board draws an instance that has fallen out of sync with its master.", "truth": "The state exists and the user has no way out of it. syncInstance can return NOT_SYNCED per instance and the failures are swallowed — updateComponentMaster reports only how many succeeded — there is no 'out of date' badge anywhere, syncInstance has no per-instance caller, and the only remaining control, 'Reset to master', throws the instance's own edits away.", "evidence": "ComponentInstances.ts:258-262 `if (!element) return NOT_SYNCED` / `if (!parent) return NOT_SYNCED`, plus the catch at :317; :371 the only caller is syncAllInstances inside updateComponentMaster; ComponentManager.ts:284-289 returns `instancesSynced` only. The toast says 'N instances followed' (ComponentDetailScreen.tsx:213-215) and never says how many did not. The escape hatch: VariantSection.tsx:150 'Reset to master' → resetInstance → ComponentInstances.ts:346 `overrides: []`. Cross-filed as MOD-F-25.", "edit": "Clone 641:2599 to 'Components · detail · instance out-of-date' in section 1938:8372: the INSTANCE ACTIONS block from FIG-F-49 gains a warning row 'This copy is on v1 — the component is on v3' plus two buttons, 'Update this copy' and 'Reset to master', with an 11px note under Reset: 'Reset discards this copy\\u2019s own edits.' Annotate that 'Update this copy' needs a per-instance sync caller before it can be built.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-69", "module": "components", "action": "fix-board", "target": "1938:8372 / 641:2546", "claim": "The 'N instances' figure on each library row reads as the component's usage.", "truth": "It is the usage on the OPEN SITE only, counted off that site's element tree — and a component master is a workspace object that the dashboard can delete from every site at once. So the number a user checks before deleting is scoped narrower than the delete they are about to perform.", "evidence": "The count: ComponentsTab.tsx:251 `getInstancesOfComponent(component.id).length` → ComponentManager.ts:145 rehydrateInstances walks `composer.elements.getAllElements()` — one site. The workspace-wide delete: site-component.ts:104-110 workspaceDelete, called from the dashboard's library-panel.tsx:46. The route that could scope it correctly, siteComponents.usage (site-component.ts:84-90), has no caller. Cross-filed as MOD-F-23.", "edit": "Change the row meta on 641:2546 from 'N instances' to 'N on this site', and add an 11px --bk-ink-muted line under the YOUR COMPONENTS header: 'Counts cover this site. Components are shared across your workspace.'", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-70", "module": "brand", "action": "fix-board", "target": "1776:8373 / 155:58", "claim": "caption/Brand · token-detail reads 'Value · used-in · safe rename · fix contrast. Editing here changes every consumer in one undo step, which is why used-in sits next to the value.'", "truth": "Value, used-in and the live-propagation sentence are right. The other two are not. Rename is a silent no-op on type and spacing tokens, so it is not 'safe' — it is inert; and there is no fix-contrast on this screen or anywhere, because the Auto-fix button is gated on a LintIssue.autoFixHint that no rule produces.", "evidence": "Live propagation is real: useTokenBase.ts:64 `document.documentElement.style.setProperty(next[idx].cssVar, value)` on every updateToken, and bindings are written as `var(--buildrick-design-<id>)` (TokenPickerPopover.tsx:13-14,37). Rename: TokensSection.tsx:281-296 — 'Type + spacing currently have no rename API … so renames there silently no-op via optional chaining'. Fix contrast: TokenDetailView.tsx:318 `{issue.autoFixHint && <Button>Auto-fix</Button>}`; producer search for the constant across packages/editor/src finds only consumers (LintState.ts:34, IssuesPanel.tsx:53, AquibraStudio.tsx:318,631-632, TokenDetailView.tsx:216,318, useStudioState.ts:91) — DSLinter.ts's 7 issue constructions (:65,89,100,112,120,130,138,148,183) and contrastLint.ts:82 set none.", "edit": "Rewrite TEXT 155:58 to: 'Value · used-in · replace-on-delete. Editing here repaints every consumer immediately, which is why used-in sits next to the value. Rename is inert for type and spacing tokens, and there is no auto-fix — no linter rule produces a fix hint.'", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-71", "module": "brand", "action": "fix-board", "target": "1776:8373 / 306:2080", "claim": "Brand · tokens · replace draws the token KIND LIST carrying a status pill 'Replace across usages'.", "truth": "Replace is not a state of the kind list and there is no such pill. It is a MODAL, reached only from a token's detail screen, and only when that token still has consumers: Delete on a token with usage > 0 opens a same-kind replacement picker. The status-pill vocabulary the board is borrowing has four values and 'Replace across usages' is not one of them.", "evidence": "TokenDetailView.tsx:283-291 handleDelete — `if (consumerCount === 0) { onDelete?.(token.id); onBack(); return; } setReplaceOpen(true);` then :563-570 `<TokenReplaceModal open={replaceOpen} token candidates usage onConfirm={handleReplaceConfirm} />`. TokenReplaceModal.tsx:10-13 records that a hard delete is deliberately not offered there. The pill vocabulary: SectionStatusBadge.tsx:45 `export type SectionStatus = \"draft\" | \"exported\" | \"imported\" | \"import-failed\";` — nothing renders a 'Replace across usages' badge anywhere.", "edit": "Rebuild 306:2080 as a modal board, ~460x200, named 'Brand · tokens · replace-on-delete (modal)': title 'Replace “primary” before deleting', body '34 elements still use this token. Pick a replacement — every one of them will follow it.', a same-kind radio list (accent · secondary · muted), and Cancel + 'Replace and delete'. Delete the 'Replace across usages' pill and the kind-list body. Keep the current frame in the archive section if the kind-list drawing is wanted as history.", "severity": "Major", "confidence": "measured"}
{"id": "FIG-F-72", "module": "components", "action": "none", "target": "1938:8372 / 788:4315", "claim": "caption/Components · load-error reads 'Your components are safe — only this list failed. Create component stays live because it does not depend on the list.'", "truth": "The first sentence is the shipped string verbatim. The second describes a button that the error branch does not render — filed against the board, not the caption, at FIG-F-57, because the caption states the better product and the fix is one branch.", "evidence": "ComponentsTab.tsx:104-111 `<PanelErrorState title=\"Couldn't load your components.\" message=\"Your components are safe — only this list failed.\" onRetry={() => state.setError(null)} />`; the branch returns at :112 before any footer.", "edit": "No edit. Coverage recorded.", "severity": "Minor", "confidence": "measured"}
