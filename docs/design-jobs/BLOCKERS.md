# BLOCKERS — what stops a design job

From `LEDGER.jsonl` (376 lines) + `jobs.json` (**519 rows @2026-09-03**: done 83 · fix 60 · fix-figma 12 · todo 36 · unbuildable 60 · verify 268). **`decide` is 0** — every G row is answered.

**Read the counts below as of the 09-02 rewrite; the 09-03 arc moved them.** That arc
closed D1/D3/D6/D9/D10/D13 and H1/H2/H6/H7 in Figma, **withdrew H4 and re-scoped H3**
after reading them (both would have destroyed real work — H4 would have cut 19 live
prototype edges, H3 loses 9 states and 8 flow starts, not the 3 boards the row claimed),
and added **91 new board rows** for states drawn from their producers during the
Figma-only pass. Those 91 sit at `verify`: they are drawn and wired, **not** yet checked
against the running app, and this file does not count them as done.

**2026-09-03 code pass (the Figma-only arc's sibling).** Ten rows moved: **A6,
A12, A14, A-EC1, A-STOCK fixed and A2 partly fixed** in code; **A9 and A13
confirmed real but correctly blocked** (A9's own proposed fix contradicts a
recorded founder decision and would ship a font upload that dies on reload);
**H8 and H9 re-scanned** — three H8 items were already deleted, one count was
wrong by 3, and only 3 of H9's 13 claims are genuine semantic duplication. The
biggest of these was **A14, whose framing was wrong in a way that hid a
production defect**: the token-usage tracker matched only the template
placeholder syntax, so every token bound through the UI counted as unused, and
deleting one skipped the replace-modal entirely. **Counted honestly, 2026-09-03 (this replaces every "55 → N" figure said earlier
today, all of which were wrong).** One fixed rule — a row is CLOSED if it opens
with ✅ or ❌, PARTIAL if ⚠️ or ⏸️, otherwise OPEN:

| | rows |
|---|---|
| total | **94** |
| closed ✅❌ | **48** |
| partial ⚠️⏸️ | 9 — A2, A9, A13, B14, F4, F5, F7, H8, H9 |
| open | 37 |
| **not done** | **46** |

The running "55 → 41 → 36 → 31" I quoted through the day was produced by a
regex I kept editing between passes, against a starting set that itself grew as
new rows were found (A-STOCK, A-EC1, A-EC2, A-PANELSUB, A-CONTRAST, A-WARN,
F-VERSION, D-SG). Every one of those figures flattered the result. **48 of 94
closed** is the number; the drift is recorded rather than quietly corrected,
because a count produced by a method that changes is the same failure as F6's
count-instead-of-a-set.

A second pass took the P1 **harness** rows, which had been left as "tooling, not
product" — wrongly, because a gate that lies is worse than no gate. **F6** was
comparing a *count*, so fixing one failing contrast pair while introducing
another left the number unchanged and the gate passed reporting no change; it is
a keyed set now, and the fix is watched failing by a test that plants exactly
that swap. **F7**'s 30-day spec alarm was too slack to ever fire — the 09-01
redraw of 135 boards landed at day 28.5 — now 14, watched failing in both modes.
**F4** had every drill-in staged 40px wider than the boards it is compared
against. **C5** turned out to be closed already and the row simply stale.

One line per blocker; the owner clears it; classes per PROTOCOL.md. **Row ids are stable
across rewrites** — a gap (A1, C4, D5 …) is a row that moved to Cleared. Owners are
exactly `designer | founder | coordinator-code | coordinator-figma`. `NN:line` =
`docs/prd/editor/NN-*.md`. `D2` here ≠ jobs.json `D-02`.

## Counts

| class | blockers (was) | jobs stopped |
|---|---|---|
| A | 12 (14) | 22 (4 shared with G6/G9) |
| B | 14 (8) | 30 (7 shared with C, 7 with F) |
| C | 5 (8) | 33 (27 shared with F — the F9/F13 boards; 11 with G; C10 counts 4 done rows measured against a split contract) |
| D | 7 (12) | 13 (7 shared with F1/F15) |
| E | 8 (6) | 34 (10 M-jobs ≈ 48 states) |
| F | 15 (8) | 76 (F13 alone 39; 27 shared with C, 9 with G) |
| G | 15 (9) | 34 (11 shared with C, 9 with F) |
| H | 10 (10) | 32 |

Counted from the `stops:` segments (script, not estimate): 213 unique jobs across A–H of 424 in jobs.json; 89 rows (was 76).

Biggest: **F13** flowbite h-10/link leak (39 jobs across 7 families) · **C10** two Layers families (16) · **C11** two Inspector geometries (14) · **F14** Content's five constants (12) · **H3** client sign-off on two pages (10) · **E6** shipped code with no board (10 M-jobs) · **B8** unboarded Brand chrome (9) · **F15** modal delta set (9) · **G8/C7** History at 360 (7) · **H4** Backups vs Versions (7) · **E5** (7) · **F9** the six boards the Figma seat never returned · **G1** Templates and **D13** off-scale modal type (6 each).

## A · product cannot produce the state

- A2 · ⚠️ **PARTLY FIXED 2026-09-03; the row's mechanism was wrong and 2 of its 3 surfaces were already done.** Canvas has rendered `CanvasLoadingSkeleton` gated on `pageIsEmpty && projectLoading` since `Canvas.tsx:529`; Components renders four `SkeletonListItem`s gated on `!state.isLoaded` (`ComponentsTab.tsx:246-252`). **The founder half is moot** — the "one payload-pending producer" the row asks for already exists (`Composer.isProjectLoading()` at `Composer.ts:862-871` + `useProjectLoading`), and Canvas, `ProInspector.tsx:208` and `StudioFooter.tsx:111` all read it. Layers was the only surface that did not. The mechanism was also not `composer:ready` racing the payload: `useComposerInit.ts:396` calls `setComposer` **synchronously in the effect body**, before the fetch starts, so `composer` is non-null on the first render and the user saw an empty tree with a footer reading **"0 layers"** — not a skeleton. **FIXED:** `LayersTab.tsx` now gates on `composer && !projectLoading`, joining the flag its three siblings already agree on.
- A3 · ✅ **RESOLVED 2026-09-03 (Figma), and the fix was to stop the boards promising a door that does not exist.** There is **no generic autofix producer** — `LintSection.tsx` has no fix door at all, and the only one in the product is contrast, at `ColorTokenList.tsx:294-306`. So: on **154:2** the hard-coded-hex issue offered `Fix ›` with nothing behind it and now offers **`Open`**, which is what the font issue already correctly offered; the contrast row keeps its `Fix ›` because that one is real. **164:42** (`Issues · fixing`, "Writing alt text for 6 images…") and **164:57** (`Issues · fix-failed`, "Couldn't fix the contrast automatically") both draw a generic autofix the product cannot perform and are now marked **`[design-ahead]`** with the reason in the name — marked rather than retired, because those boards are the spec if the feature is ever built. What is left of the row is a docs task: say in 06:40 which surface it describes.
- A6 · ✅ **FIXED 2026-09-03.** `ComponentsTab.tsx` passed the raw thrown string (`"Failed to load components"`) as `message` and no `title` at all, so the panel fell back to `PanelErrorState`'s generic `"Something went wrong"` default — the one every panel is meant to override. Board 781:4433 writes `Couldn't load your components.` / `Your components are safe — only this list failed.`, and that is what it renders now. Brand had already done this at `DesignSystemTab.tsx:672`; Components was the outlier.
- A7 · P2 · Publish not-connected unreachable: canPublish = !!onVercelPublish (PublishTab.tsx:159) tests callback presence · stops: J-784:4480 · owner: founder (parked — Vercel out of scope 08-25) · evidence: req 11:16:30Z (11:75; 10:7 prePublishChecks exists) · fix: one prePublishChecks call on panel open when re-scoped
- A8 · P2 · activity log: 8 of 10 row types never recorded (activity_logs 96% site.settings.updated) · stops: J-817:5114 · owner: founder · evidence: req 11:20:10Z (14:423); jobs.json 817:5114 · fix: PRD row naming the 10 producers, or trim the board to the 2 that exist
- A9 · ⏸️ **CONFIRMED REAL 2026-09-03, but the row's own fix must NOT be taken — it contradicts a recorded founder call.** `uploadFont` has 0 callers (5 hits repo-wide: the definition at `FontManager.ts:291` and four in its own test). The finding is larger: **the whole `FontManager` is orphaned** — outside `engine/fonts/`, the only references are its construction at `Composer.ts:254` and the barrel export. The editor's font picker runs on a parallel system, `services/GoogleFontsService.ts` → `typography/FontPicker.tsx`. **Wiring the door would ship a lie:** `uploadFont` stores into an in-memory `Map` (`FontManager.ts:335`) with no persistence, so an uploaded font dies on reload and never reaches the published site — which is precisely what `TypographySection.tsx:9-13` records the founder deciding on 2026-08-16 ("+ Add a font", the `.woff2` drop zone and the licence checkbox do not ship until there is somewhere to put a font file and a publish-time `@font-face`). Blocked on that arc, not on this row. **One free correction it does unblock:** `J-160:512`'s `unbuildable` evidence reads `unreachable-selection-cleared-on-ai-tab-entry`, and no such code exists — `clearSelection` has no caller in `sidebar/tabs/ai/` or `shell/`, and nothing clears selection on tab switch. That board is probably buildable.
- A10 · P2 · fixture cannot reach: Pro tier (Integrations — the 6 cards are CUT at 14:517-519, LockedScreen is the only conformance target), would-mutate (S1.1d), shared fixture (Components empty) · stops: J-640:3488, J-807:6694, J-1138:13394 · owner: coordinator-code · evidence: req 11:19:50Z; jobs.json 807:6694 'unreachable-by-choice-would-mutate', 1138:13394 'shared-fixture' · fix: verify LockedScreen half only; a server-backed fixture per state (949:4474 half of this row → Cleared)
- A11 · P3 · History rollback trio renders only after a confirmed redeploy, forbidden for the run; a no-server-write path exists (route-fulfil sites.rollback with a fake {id} + drive sites.publishStatus; abort at the route for 'failed') and was not executed pending authorisation · stops: J-184:37, J-184:45, J-453:4064 · owner: coordinator-code · evidence: vh 05:42:45Z ×3 (PublishHistory.tsx:165-205, 376-421) · fix: authorise the fake-job harness, then measure; static deltas already proposed (kind='form' 560 → 'question' 440; disc 32→40; footer centred). The Review twins (158:57 re-sending, 158:213 review-closed) are F11
- A12 · ✅ **FIXED 2026-09-03.** `siteFontsFromTokens` and `siteTokensCSS` take `designTokens` straight from a server payload and a default parameter only fires on `undefined` — a `null` or an object reached `.find` / `for…of` and **threw**. The throw happened on the preview path (`Composer.ts:641`), before `DesignSystemTab`'s own catch (`:376`) could ever run, which is exactly why the Brand load-error board had no producer. Both now normalise through one `tokenList()` helper, so a malformed payload degrades to "no site fonts" and the panel's own error state becomes reachable.
- A13 · ⏸️ **CONFIRMED REAL 2026-09-03, and it is not a bug to fix — the write path was never built.** Verified stronger than the row states: `updatePreset` / `addPreset` / `deletePreset` (`usePresetsForCategory.ts:69-88`) have **zero callers repo-wide** outside their own definition and the `noop` defaults at `StylePresetRegistryContext.tsx:151-153`. The only writer with a caller is `hydrateFromExternal`, which sets `presets` and `savedPresets` to the same value and therefore can never dirty. Consequences beyond the badge: the styles contribution to `DraftChip`'s `totalDirty` and the section-tab dot are permanently 0 too. Stays parked with the token-picker pass — this is a feature, not a repair.
- A14 · ✅ **FIXED 2026-09-03 — and the row's framing was wrong in a way that mattered.** This was never a fixture problem. `TokenUsageTracker.recompute` matched **one** syntax, `{{token.…}}` — which only ever appears in template HTML strings, and `resolveTemplateTokens` substitutes those away before import, so a placeholder survives only on a resolution *miss*. Everything a user actually binds is written as `var(--buildrick-design-<id>)` (`placeCatalogComponent.ts:40-49` on every catalog drop; `tokenBindingDetection.ts` is the Inspector's own read contract, spec §6.4). So `getUsage()` returned **0 for every token in real use, in production** — which hard-deletes on `consumerCount === 0` and made the replace modal unreachable, and made "Used by N elements" and every usage chip read 0 for genuinely bound tokens. The row's proposed fix (seed a fixture element bound to a token) would have produced a real binding the tracker still counted as 0, and the null result would have been read as the fixture being wrong again. **FIXED:** `recompute` now counts both syntaxes.
- A15 · ✅ **RESOLVED 2026-09-03 (Figma).** The `Replacing…` committing card on 1174:4849 has no producer — replace-across is **synchronous**, and five samples at 400ms all read `Replaced 3 uses ✓`, so its 260×5 progress bar draws a phase that never happens. Marked `[no-producer]` and dimmed rather than deleted, because deleting it loses the record that an async phase was specced; only the founder adding a real one revives it. On `result-clean`, **`Done` was a 32-tall Button and is now the 11/500 accent link the code renders** — that 32 is F13's flowbite base leaking through, the third board today where a measured height turned out to be the base rather than a design. The other measured delta (`13/400 #0e9f6e` → `12/600 #057a55`) **was already closed** — D1's contrast rebind had made exactly that substitution earlier in the day, so re-reading the node found it correct. The remaining `result-partial` card still needs an element that actually fails, which the fixture cannot produce (F-class, not this row).

## B · feature SHAPE differs board vs code (product decision)

- B1 · P2 · Compare drawn 1080, mounted in the 280 drawer (~140px per pane — 'Side by side' is only real on the board's surface) · stops: J-168:2, J-168:48 · owner: founder · evidence: req 11:12:00Z (ReviewTab.tsx:517; ApprovedCompareView.tsx:47,149; DESIGN.md:289) · fix: recommend chrome-ui OverlayMount at 1080 (Gate 22); list mode stays in-drawer
- B2 · ⚠️ **PREMISE DOES NOT HOLD — verified against the board 2026-09-03, no redraw made.** The row says the board draws an AI **transcript** while the code is prompt-in-input. Read 170:29 directly: it is `AI · thinking`, 360×776, composed of Back row / Header / Scope / **Prompt (72h)** / **Thinking (56h)**, with the text `Scope: Hero section` / `Make the hero warmer` / `Thinking…`. There is **no transcript node on it** — it already draws prompt-in-input, which is what the code and the PRD (12:247-252, single-shot) both describe. Nothing to redraw; the row needs its evidence corrected. *(Caught by checking the board before acting — the fourth time today a row's claim did not survive that step.)*
- B3 · ✅ **ANSWERED 2026-09-02 — the number is SIX.** B3 asked to pick N once across 01:23 (4) · board 813:4836 (5) · `SaveStatus.tsx:18` (6) · census (7). All six code states have a traced producer and a single consumer (`StudioHeader.tsx:525-535`): **offline** ← `AquibraStudio.tsx:332-339` · **saving** ← `useSaveCallback.ts:110`, `useComposerInit.ts:439` · **conflict** ← `useSaveCallback.ts:142`, `useComposerInit.ts:479` · **error** ← `useSaveCallback.ts:170/206/232/241/261` · **unsaved** ← `isDirty` · **saved** ← idle + clean. None is dead, so precedence favours code and 01:23, 813:4836 and the census move to six. · **New, in passing:** `StudioHeader.tsx:368` reads `offline = Boolean(isOffline) || studioSyncStatus === "offline"` and `setSyncStatus` has **no caller anywhere in src** — that OR-half can never be true. `offline` stays reachable through `isOffline`, so the count is unaffected · stops: J-813:4836 · owner: designer (redraw to six)
- B4 · P2 · 'bound' = preset-property→token (06:17) vs element→preset (boards); the no-op picker at 14:310 has no target either way · stops: J-306:2111, J-306:2136 · owner: founder · evidence: req 11:18:20Z (14:308-310) · fix: define 'bound' once
- B5 · P2 · applying: 6 per-section phases (board) vs 4 generic (code) — both theater over one importHTMLToActivePage call (07:29 '⚠ artificial 500ms') · stops: J-642:2832 · owner: founder · evidence: req 11:17:40Z · fix: one 'Applying…' state, or real per-section progress; never conform theater to theater
- B6 · P2 · Layers empty unreachable: useLayerTree seeds the root row so an empty page reads '1 layer' · stops: J-143:355 · owner: founder · evidence: req 11:16:50Z (04:7 'Root excluded everywhere' vs 05:12) · fix: excluded → 0 layers and the board is reachable; included → retire
- B7 · P2 · ⌘P preview has no device frame; board draws one · stops: J-807:8663 · owner: founder · evidence: req 11:20:40Z (04:13 DeviceFramePreview; 12:21 sanitized window) · fix: reuse DeviceFramePreview in the overlay, or redraw frameless
- B8 · ✅ **CLOSED 2026-09-03 — decided by the rule, not the founder; it needed the work, not a decision.** Behaviour → the CODE contract, so Brand chrome the code renders and no board draws is chrome the board **gains**. All eleven pieces are now on their boards. **1333:7162 (root):** the shared-theme strip (`Brand & shared theme`, its two-line note in **ink-soft** — gray-600 not gray-500, which the producer's own comment explains is because the strip sits on `--bk-bg-subtle` — and `Open Shared theme ↗`), the **Beginner | Pro** toggle with Beginner active and its hint (`isBeginner` is `mode !== "pro"`, so Beginner is the resting state), the **clean footer** `Brand is up to date` with **no buttons** (154:78 draws only the dirty state, and the producer records that a clean panel showing two disabled buttons was two dead controls announcing nothing), and the Tokens-row **dirty dot** in warning-text beside that row's count — `DesignTabFooter`'s own comment is the reasoning, the footer dropped its "N previewing" count precisely because a marker belongs beside the destination it counts. **152:52 (tokens):** `More token kinds`, the **usage chip in both states** (`used N×` and the muted `unused` — the half that gates replace-on-delete, the same surface A14 fixed), and the **first-load banner** on accent-tint, which is gated on `isFirstLoad && activeSection === "tokens"` and therefore belongs here rather than on the root; its label is `APPLY_CHANGES_LABEL`, not a literal. **153:92:** the colour toolbar (Group by / Sort / Filter) and the group heads BRAND / STATE / SURFACE. **153:29:** `Your saved components · N` with its empty line, and the per-component variant count. **154:132:** the Basic-mode note was **already there** — checked before drawing, past where an earlier text read had truncated.
- B9 · ✅ **BOTH HALVES SETTLED 2026-09-03 — and they settle in opposite directions, which is why it was never one decision.** Editing a field inline is **behaviour**, so the code contract wins and the board gains it: 1170:4713's read-only chips (`⠿ name type`) are now the **editable rows the code ships** — 54 tall, `TextInput` 332×42 for the name, `Select` 112×42 for the type, drag handle and trash. The **width** is **visual**, so the board wins there — 500, not the 560 that `ModalContent 'form'` was giving it — and that half already landed as the `fields` kind in editor-ed's shared commit. One row, one of each, neither needing a founder.
- B10 · ✅ **DECIDED BY THE RULE ALREADY ON RECORD, AND DONE 2026-09-03 (Figma).** This never needed a founder call — the row says so itself ("working capability the board omits, and rule 1 says the code contract wins"), and `packages/editor/CLAUDE.md` states the precedence: **behaviour → the CODE contract, visual → the BOARD.** A collection Select and a per-record actions cell are behaviour. So the board gains them, and 1170:4749 now draws both, from the producer: the **Select** at 280 wide (`CMSRecordsModal.tsx:259-271`, the code's own `maxWidth: 280`) directly under the title, and an **Actions** column carrying the ghost trio the code renders per record (`:364-380` — `Publish`/`Unpublish` text, `✎` edit, `🗑` delete, right-aligned with gap 4 as at `:363`). Two rows draw `Unpublish` and one `Publish` so both branches of `item.status === "published"` are on the board. The four existing columns were narrowed to 170/56/88/72 to seat a 146 cell inside the 608 table rather than widening the modal. Width stays **640** — the matching `Modal.tsx` kind is F15's half. `Import JSON` (1170:4775) is documented-not-built and stays in E8.
- B11 · ✅ **RESOLVED 2026-09-03 — the dilemma was false, and the fix found a second cause.** The row read as rule-versus-defect: behaviour → code says the board gains the crumb, but the crumb defeats double-click rename. Neither horn was right. `panels/layers/index.tsx:369` mounted it on `selectedIds.size === 1`, so **selecting a row mounted a block above the tree and pushed every row down** — under the pointer, between the halves of a double-click. The crumb was never the bug; **its appearing was**. Fix: reserve the slot unconditionally. editor-ed landed it and found a **second unmount path** neither of us had named — `LayerBreadcrumb` also opened with `if (ancestors.length === 0) return null`, so a **top-level row**, having no ancestors, selected to nothing and jumped the tree identically; fixing only the call site would have left rename broken for every root-level element while looking fixed everywhere else. Both empty paths now render the slot (`aria-hidden`), height `var(--bk-size-row)`, buttons capped at 20. **Board half done here:** the crumb is drawn on 143:179 (populated) and 143:119 (reserved empty slot) at **32, not 53** — the 53 every earlier note quotes was a flowbite `Button`'s own 40 leaking through (F13), not a designed height. Both states are drawn precisely because existing in only one of them was the defect.
- B12 · ✅ **RESOLVED 2026-09-03 — a data contract decided it, not taste.** The board drew a per-round read-only VIEW ('Round 1 · resolved 2d ago · read-only', that round's comments, Compare/Re-send). **Comments carry no round id** (contracts §6.4), so that view cannot be built without a schema change — it is not a shape preference, it is unbuildable from the data. 157:169 now draws what the code actually renders: the **header-only rounds list**, seven lines at 12/400, with the reason on the board. When an older-round comments endpoint exists this becomes buildable and the row reopens on purpose.
- B13 · P2 · Media fullpage folder column: boards 1159:4639 / 1205:4829 draw a '＋ New folder' ROW (1160:40), a TAGS chip section and SMART rows (Recent / In use / Unused with counts) at 27h; code has a 40×18 '+' in the FOLDERS head, no TAGS, 31.5h rows · stops: J-1159:4593, J-1205:4829 · owner: founder · evidence: vm 10:22:06Z, 10:23:09Z (DECIDE) · fix: add the row + TAGS, or FIGMA drops them; row/type sizes are fix-code either way
- B14 · ⚠️ **PARTLY DONE 2026-09-03, and two of its five claims are wrong.** Decided by the rule already on record — behaviour → the CODE contract, so the board gains a code-only affordance; the row says this itself. **Done in Figma:** `1164:4713` gained the **Grid | List** view toggle with Grid active (`MediaLibraryPanel.tsx:71` defaults `viewMode` to `"grid"`, `:204-212` renders the pair). `1163:13695` gained **`Copy alt text`** after Copy URL and **`Replace across pages…`** after Edit image — cloned from a sibling row so height, padding and type match, and inserted at the code's own positions rather than appended, because `MediaContextMenu.tsx:7-9` documents that order. *(The row names only `Copy alt text`; the same comment names `Replace across pages…` as equally ours, so both went in.)* **Claim 3 is FALSE:** `145:300` already draws the bulk-bar **`Done`** — its text reads `4 selected | Move to… | Delete | Done`. **Claim 5 is BACKWARDS:** `1082:4527` already draws **`Copy link`** — `LayerContextMenu.tsx:77` even cites that board as the reason the item exists. The real difference is its **position** (the code puts it after Paste in the cut/copy/paste group; the board puts it after `Move to page…`), and position is **visual**, so the board wins and this is a **fix-code**, not a fix-figma. **Still open:** `156:2`'s reply-composer band (123h, "Replies are internal notes on the thread."), the `Revoke link` ghost 91×32 and the per-row 32h `Resolve` — none is on the base board, though the states themselves were drawn as their own boards (`Review · reply-composer band`, `Review · foot · revoke + invite`) in the 09-02 arc.

## C · board contradicts board / prototype unwired

**All 8 contradictions now have a named contract.** C2, C5-half, C7-half, C9 and C12 were closed by the Figma pass (see Cleared); C10 and C11 were settled by the founder and their waves are drawn (mechanical half for C11). What remains in each row below is designer composition or a body redraw, not an open question.

- C3 · ✅ **CLOSED 2026-09-02 — the code side was already conformant.** A verifier read this family as "NEVER BUILT TO — the code cites 152:2/154:132 and 1333:7162 appears nowhere in src". The second half is true; the first is not. `DesignSystemTab.tsx` **already renders the current root's shape**: `BrandPreview` (:704) sits directly above a nine-row list (:709-751) of `tw:h-13` (52px) Buttons carrying label + hint, and the nine `SECTIONS` labels (:123-133) are the board's nine verbatim. That is 1333:7162 (82h preview, nine **named** 52h rows from y126), not 152:2 (nine anonymous 32h rows from y44, no preview). Only the **citations** were stale — eight comments naming 152:2, now corrected, with 154:132 keeping its number because it is a *state* of the root, not the root. · **Figma half done too:** all four boards rebuilt on 1333:7162 — nine anonymous 32h rows replaced by the 82h preview + nine **named** 52h rows from y126, with each board's own delta preserved (154:132's Basic-mode note, 154:78's Save bar, 306:2217's Badge). The 'Warnings suppressed' badge sat at y60, inside what is now the preview band — moved to 16,96, the y84 move figma-batch-4 skipped.
- C5 · ✅ **CLOSED — verified in the file 2026-09-03; this row was stale.** The ⋯ half was already done, and the second half was fixed by the 09-02 redraw without the row being updated. Re-read just now: `152:83` (280×812) draws **one** token's detail — `primary` / `color-primary`, with `Light value #1A56DB`, `Dark value #76A9FA`, `Used by ▸ 34 elements`, `Lint ✓ pass`, and an Actions row of `Replace value` / `Rename ID` / `Delete token` scoped to it. The three token rows and the three global foot rows (233:1359/1364/1369) are gone, which is exactly what the code contract asked for (`TokenDetailView.tsx:440/528/324` scopes all three actions to one token reached by a row click). Verified by reading the board, not by trusting the ledger line that claimed it.
- C7 · ✅ **FULLY RESOLVED 2026-09-02.** Title unified earlier; the toggle strip landed with wave 1. The seven boards drew either four tabs (⟳ Changes · ▣ Saves · Published · Backups) or two (Changes · Saves); **live draws two tabs with helper text — 'Saves / Named milestones' and 'Published / What's live' — plus chips labelled 'Milestones' and 'All changes', not 'Changes'.** Measured on the running editor, not computed from `history.css`: header 44 · switcher 40 (pad 0/12, 1px border; tabs pad 12/16, label 13/18.57, helper 11/15.71) · filter row 32 (pad 8/12/0, gap 4, chips 24h pad 4/8 12px pill) · search bar 53. Saves chrome 169, Published 84 — `HistoryTab.tsx:232` renders the filter row and search only while `activeView === 'saves'`, so 949:4474 correctly has neither.
- C10 · ✅ **WAVE 3 DRAWN 2026-09-02.** Contract: 1082 geometry + the 16:6 header (founder). The cause was not nine boards drifting apart — the shared `Tree row` **component 243:6 was still 320 wide**, the old drawer, with pl16/pr16/gap8, icon 16 and eye/lock 11px #d1d5db at x274/x293. Set to 280 / pl12 pr8 gap6 / icon 12 / chevron 10 / label 13-400 #111827 FILL / eye-lock 10px **#6b7280** (G3), landing at x246 and x262 — 1082's exact numbers. Its `indent` spacer was removed: **no instance in the family ever overrode it** (all 55 read indent=1, pl=12) and it only cost 7px of label offset; 1082 indents with paddingLeft. Toolbars on all nine: box 16/166 #f9fafb 1px → 12/208 #f3f4f6 no stroke r6; glyph 198/11px #4b5563 → 228/13px #6b7280 pinned 40w. Footers 16/12px → 12/11px #9ca3af. 1082:4527 and 1082:4589 lost their hand-drawn 14/600 headers for 16:6 instances. **Code conformed after** (layers-v2.css gap 1→0, padding-top 4→0); the blocker's other two code claims were stale — `:29` already reads `font: 400 13px` and eye/lock already uses `--bk-ink-muted` at `height:20px`. · **Still open (designer):** the whole 143 family draws a **flat tree** — all 55 rows at depth 0, hierarchy carried only by chevron presence, where 1082 draws three real depths (pl 12/28/44, step 16). Setting depths is content, not conformance.
- C11 · ✅ **WAVE 2 MECHANICAL HALF DRAWN 2026-09-02.** Contract: 807 geometry, 32:2 master (founder). Measured before: control rows 300×32 holding a `control` frame **172×28 at x112 with no fill**; 807:8342 draws control **160×28 #f9fafb at x120** on a **34** pitch (labels y112/146/180/214…). Applied to 160:2, 160:105, 160:208, 160:313, 160:412 and 159:123 — 52 control rows re-pitched and restyled, 30 section headers 32→28, everything re-stacked with the trailing spacer taking the remainder so each board still ends at 812. 159:2 retired; 189:2's copy already fixed to 'Brand panel'. · **Still open (designer, deliberately not drawn):** the header composition (807 holds two 22h chips at x16/x62 + ⋯ at x273; the 160/159 family draws '⬚ Section' 14px + a 128×8 frame + btn/ai at x227 + ⋯ at x273), the collapsed tail and the footer — 32:2 is the master for both, and what goes in them is composition, not a number the 807 boards dictate. Same for G2's three new header controls on 32:2: the x positions are known (pick @136, select-parent @160, bind @223), the glyphs are not. · **Trap:** 160:412's `reach edge` is a 2×812 full-height decoration; a re-stack loop treated it as a row and pushed content to y1624. Exclude full-height decorations from any stacking pass.

## D · board contradicts PRD / DESIGN.md, or asks the product to lie

- D1 · ✅ **RESOLVED 2026-09-02 (Figma).** The two failing pairs were the SURFACE, not the token: ink-muted on white is 4.83 and passes; it only failed on `bg-subtle`. Rebound the offending nodes on the achievement boards (430:2348, 430:2375) — 3 × ink-muted → ink-soft `#4B5563` (4.39 → 6.87 on `#F3F4F6`), 2 × success → success-text `#057A55` (3.39 → 5.36 on white). No variable changed, so nothing else in the file moved.
- D2 · ✅ **MOOT 2026-09-02 — carried over from G1, which I failed to do at the time.** G1 answered *dissolve Templates into New-Page + Insert*, so the rail drawer this row wants moved from x1120 to x60 is **retired**; there is no structural redraw left to do. The consequence was recorded in G1's own row and in the ledger and never carried into this row — exactly the drift `pnpm run audit:rules` exists to catch · stops: J-807:4299 (already `unbuildable`, authority `founder:templates-rail-drawer-retired`)
- D3 · ✅ **RESOLVED 2026-09-02 (Figma).** 14:177 wins — surface shape is behaviour, and the producer opens a dialog. 813:4489 redrawn as `S1.1e · template-preview (modal)`: scrim over the gallery ground, a 1100×780 modal at (170,60) with a header (title / Use this template / ✕), the 900-wide preview clipped inside it, and a footer carrying `3 pages · 6 sections · Responsive`. The full-page `preview-chrome` and its stray caption are gone.
- D6 · ✅ **RESOLVED 2026-09-02 (Figma).** 03:41 wins. 66:4 now draws five batch rows (Background, Text color, Radius, Padding, Font size) with `Mixed` as the **placeholder** — which is what BatchStylePanel.tsx:89-93 actually does; the label never changes. Added the hint `Editing a Mixed field applies it to all 3.` and removed the annotation claiming the fields are hidden.
- D9 · ✅ **ALREADY SATISFIED — verified 2026-09-02, no change needed.** 170:41 is already named `AI · planning` and already draws the plan-level gate: `PLAN · 3 STEPS`, three numbered steps, and an `Edit plan` / `Run 3 steps` action row. The row described a state that had been redrawn in an earlier wave and never re-read.
- D10 · ✅ **RESOLVED 2026-09-02 (Figma).** 14:500 wins and the code follows it. `Field · Privacy policy page` deleted — the `/legal/privacy` select has no producer in SettingsGeneral. The cookie-consent toggle moved to `S7 · Settings · Analytics` (639:3443) as `Card · CONSENT`, **cloned** from the original so its fill, padding and toggle instance cannot drift from its new siblings. `Card · LEGAL` is gone from General — it had no remaining field. Not yet mirrored onto `S7 · Settings · Analytics · validation` (1703:10515).
- D13 · ✅ **RESOLVED 2026-09-02 (Figma).** 25 text nodes below the 11px floor raised to 11 across 1175:4827, 1205:4804, 1170:4713, 1164:4713, 1164:4738 and 1175:4840 (9px and 10px hints, `MEDIA URL`, `FIELDS`, the collection-setup step chips). Disabled states rebuilt as the opaque grey chip DESIGN.md:208-210 requires — `#F3F4F6` ground, `#6B7280` text, opacity 1 — on `Delete 34 files` (1175:4838) and Media · quota-full's Upload (145:250), replacing the alpha treatment.

- A-STOCK · ✅ **FIXED 2026-09-03.** I first recorded that `StockSourceModal.tsx` "has no error branch, so a failed stock search renders nothing" — true of the modal, **wrong about the product**. `useDiscoveryState.ts:114-122` catches and toasts. The real defect was worse than a missing state: `setStockPhotos` is never reached on failure, so the modal fell through to `!isLoading && photos.length === 0 && searchQuery.length > 0` and rendered **`No photos found for "…"`**. A failed request and a genuinely empty result were pixel-identical apart from a toast that auto-dismisses. **FIXED:** the hook now carries a `searchFailed` flag (cleared when a search starts and when the query is cleared), threaded through `useMediaState` → `MediaTab` → the modal, which renders `Couldn't reach the stock library.` + a `Try again` that re-runs the same query. `StockBrowserOverlay` deliberately does **not** take the prop — it draws no empty-state copy, so there is nothing there for a failure to be mistaken for.
- A-EC1 · ✅ **FIXED 2026-09-03.** `BindingPopover`'s `isBound` / `boundLabel` were plain `useState` written only by its own `handleBind` / `handleUnbind`, never seeded from the composer — so the header chip and `BindingBanner` stayed correct while the popover forgot: bind a field, reselect the element, reopen, and the bound row was gone though the element was still bound. It now reads the same `composer.cms.bindings.getBindings(elementId)` the banner reads and re-syncs on `BINDING_CREATED` / `BINDING_REMOVED`, formatted the way this surface names it (`Collection › Field`, with the field's display name recovered from the collection).
- A-EC2 · P2 · `ProductGrid` writes `data-cms-collection="products"` and `data-cms-template="true"` (`blocks/Ecommerce/ProductGrid.tsx:30-31`) and **nothing in the repo reads either attribute** — no repeater, no field-mapping UI, no bound-grid state. The grid inserts as an empty div and renders zero products. Deliberately **not** drawn: there is no bound-grid board because there is no bound grid.

- D-SG · ✅ **TAKEN ON RECOMMENDATION 2026-09-03 — keep the board, exempt transient canvas overlays.** P3 · **the board is right and the code is what DESIGN.md would question.** `S3.11 · smart-guides-snap-indicators` draws its guide at `#FF00FF`, which is exactly what `SmartGuidesOverlay.tsx:39` renders (`backgroundColor: "#FF00FF"`). It is the one purple/magenta left in the file after the 09-03 drain, and it was deliberately **not** changed — repainting it would make the board lie about the product. Magenta snap guides are a strong tool convention (Figma's own), and this is a transient canvas overlay rather than chrome, so the exemption is defensible — but it should be a recorded decision, not a silent pass. **Recommendation taken** rather than left as an open question: the board stays as drawn, because repainting it would make it lie about the product, and a transient canvas overlay is not chrome. The DESIGN.md §Color edit that records the exemption is a **code-side** change and was left for the next code pass — this arc was Figma-only. Reverse it by changing `SmartGuidesOverlay.tsx:39` and the board in the same commit. · owner: founder (to reverse)

- A-PANELSUB · P1 · **live-measured WCAG AA failure in shipped chrome, found 2026-09-03 on 4 surfaces.** `PanelFrame`'s subtitle span (`chrome-ui/PanelFrame.tsx:108`) paints `--bk-ink-muted` `#6B7280` on the header's `--bk-gray-100` `#F3F4F6` at 13px/500 — **4.39 : 1**, under the 4.5 floor for normal text. Measured in a real browser, not inferred: `span.tw:truncate  "All"  rgb(107,114,128) on rgb(243,244,246) @13px/500`, identically on `media-drawer`, `media-bulk-bar`, `media-card` and `media-load-error`. It affects **every panel that renders a subtitle**, not just Media. Fix is one token: `--bk-ink-muted` → `--bk-ink-soft` `#4B5563`, which is **6.87 : 1** on the same ground — the same substitution D1 already applied on the Figma side, so board and code converge rather than diverge. `chrome-ui/` is editor-ed's under the session split, so reported rather than edited. This is also why the four media baselines are left unseeded: seeding would record the failure as accepted. · owner: coordinator-code (editor-ed) · fix: `PanelFrame.tsx:108` `text-[var(--bk-ink-muted)]` → `text-[var(--bk-ink-soft)]`, then seed the four media baselines

- A-CONTRAST · ⚠️ **UPDATED 2026-09-03 — two pairs fixed by editor-ed, and a THIRD found, worse than either.** Re-measured live after their push, three consecutive runs byte-identical. The `ink-muted on gray-100` (4.39) and `success on white` (3.39) pairs are **gone from the coach** — their fix landed. What the run now surfaces is a pair nobody had recorded:
  - **`--bk-gray-400` `#9CA3AF` on white → 2.54 : 1** (floor 4.5). Four instances on `shell-default` alone: the Insert panel's group counts, `"53"` `"50"` `"14"` `"0"` at 11px/500 with `tabular-nums`, from **`GroupSection.tsx:55`** — `tw:text-[var(--bk-gray-400)]`.
  **This one is a category error, not a shade that came out slightly short.** `--bk-gray-400` is the same token as `--bk-border-strong` and `--bk-border-input` (`tokens.generated.css:30,75,76`) — a **border** colour being used as **text**. At 2.54 it is the worst contrast in the editor, and it is on a number the user is meant to read. Fix: `--bk-ink-muted` clears it at 4.83 on white; `--bk-ink-soft` at 7.5+ if the count should read as content rather than chrome. `src/` is editor-ed's under the split, so reported not edited. **Still open from the original pair:** `PanelFrame`'s subtitle (A-PANELSUB) on the four Media surfaces, which their pairing fix did not cover.

- A-WARN · ✅ **BOTH HALVES DONE 2026-09-03 — awaiting merge, not work.** My break from `61b60c05`: `color-warning` went into `editor/design-system/constants.ts` as the G4 answer without its `themes/design-system/design.css` declaration, so the DS baseline parity gate was **failing on main** (`--buildrick-design-color-warning: CSS="(missing in CSS)" vs JS="#8E4B10"`). editor-ed landed the CSS half; parity now reports `OK (77 tokens verified)`. **Second half mine, done:** no migration added the token to existing projects — `0002-seed-dark-color-values` seeds nine and `warning` is not among them. Rendering was never wrong (the `design.css` declaration makes the var resolve globally, so the Alert catalog's warning variant paints on old projects too) — what an old project lost was **ownership**: the Brand panel cannot list or edit a token absent from its payload, editing tokens did not move the variant, and DSLinter's `missing-dark` cannot flag what is not there. Correct pixels with an absent control is a hard split to notice. `0003-add-color-warning.ts` is written and verified on branch `mig-0003` (`b0b37414`): idempotent, **appends rather than inserts** (token order is the Brand panel's render order, so slotting it beside `color-success` would move rows a user has learned), never overwrites an existing `color-warning` whatever its value, `validate()` throws on absence and on a missing `cssVar`, 9 tests. Verified against editor-ed's **merged** main plus this commit: 105 files / 852 tests, unpiped. Clean fast-forward, held only until their full-suite result lands. Checked and NOT needed: a dark-value declaration in `design.css` — that file has no dark block at all; `darkValue` is a token-model field consumed by JS, not CSS.
- F-VERSION · ✅ **FIXED 2026-09-03 — a version constant that eight tests retyped instead of importing.** Adding migration 0003 and bumping `TARGET_PROJECT_VERSION` 2 → 3 broke **8 tests across 3 files** (`runner.test.ts` ×3, `e2e-persist.test.ts` ×3, `MigrationManager.test.ts` ×2), none of them about the migration. Each had written the target as a literal — `expect(result.newVersion).toBe(2)`, `mgr.run({ currentVersion: 2 })`, `toVersion: 2` — where every assertion actually means *"reached the target"*. The literal quietly turned "the chain completed" into "the chain has exactly two steps". **The tempting fix rebuilds the trap:** bumping each `2` to `3` goes green and breaks again on 0004. They import `TARGET_PROJECT_VERSION` now, and the test *names* (`"v0 → v2 round trip"`, `"bumps to v=2"`) were generalised in the same pass, because the next reader trusts a title. Found in an isolated worktree while a teammate's clean full-suite run was in flight — applying it straight to main would have dropped 8 failures into that run and they would have been read as coming from three unverified WIP branches merged just before it. · fix: landed on `mig-0003`

## E · design-ahead — no code exists

- E1 · P2 · publish theater: perf audit ×4 + deploy pipeline draw a no-op (10:19 lighthouseScore always null, DEPLOYING never written; 11:380) · stops: J-817:4950, J-879:6896, J-879:6901, J-1157:4593, J-817:5220 · owner: founder · evidence: req 11:18:50Z · fix: park; a pipeline board may draw only QUEUED→BUILDING→COMPLETED|FAILED
- E2 · P2 · scheduled publish ×4 — no PRD row, router, service or job (grep 'schedul' = PageStatus 02:52 only) · stops: J-817:5262, J-878:4518, J-878:4543, J-1156:4593 · owner: founder · evidence: req 11:19:00Z · fix: PRD row + cron producer before boards
- E3 · P1 · multiplayer presence/cursor boards for a demo-only feature (PRODUCT-OVERVIEW.md:185; 05:63 six P1 OT bugs; NEXT_PUBLIC_FEATURE_COLLAB never in production) · stops: J-642:3696, J-817:5195 · owner: founder · evidence: req 11:19:10Z · fix: park until OT/CRDT
- E4 · P2 · unbuilt detail: 641:2599 styled props + 'used on' (◆ at 14:192, needs an instance→page index); 168:26 change highlights where the code is an opacity blend (ApprovedCompareView.tsx:99,222; no diff-region producer for HTML snapshots) · stops: J-641:2599, J-168:26 · owner: designer · evidence: req 11:13:20Z, 11:12:10Z · fix: redraw to the built shape or keep as E
- E5 · P2 · no PRD row / producer: 433:2391 migration modal (11:28 no UI), 813:4888 force-refresh (0 hits), 807:6965 Review-both (CONFLICT carries no rival snapshot, 10:7), 817:4899 a11y checker (09:7,11-14 cite deleted files), 1339:7221 client conflict (14:448 has no 'conflict'; review-client ignores the resolve error), 294:1992 retrying (14:63 names it, 01:38 FSM lacks it); 66:640 attribution — the CONFLICT payload carries no lastEditedBy (10:7), the half of old D5 that figma-batch-5 could not settle · stops: J-433:2391, J-813:4888, J-807:6965, J-817:4899, J-1339:7221, J-294:1992, J-66:640 (attribution half) · owner: founder · evidence: req 11:19:40Z, 11:16:00Z, 11:15:50Z, 11:18:30Z, 11:20:30Z, 11:15:00Z, 11:15:40Z · fix: PRD row + producer first; strike 09:7, 09:11-14, 12:152
- E6 · P3 · inverse — shipped code with no board, ~48 states across 10 M-jobs, plus the ones wave 2 found: 145:49 empty-folder block ('No assets matching this filter.' + Clear filter → M-05), Content empty copies + the inline condition form (→ M-04), the Review reply composer + Revoke ghost (B14 / M-09) · stops: M-01…M-10 · owner: designer · evidence: jobs.json M-01…M-10 file:line lists; req 11:20:50Z M-10; vm 10:20:21Z; vc 09:51:12Z J-151:62, J-151:87 · fix: draw; M-10 first — delete StructurePopover per 14:533 (AquibraStudio.tsx:731) and wire-or-delete AIPromptModal (06:84) before boarding either; BlockPickerModal, AiPromptPopover (14:155), StockSourceModal (14:199) are PRD-verified and do need boards
- E7 · P3 · Media data the boards draw but the model lacks: 146:2 '2400×1600' — MediaAsset has no width/height columns (item.width/height null for server-hydrated assets); 146:32 author line — versions carry no createdBy · stops: J-146:2, J-146:32 · owner: founder · evidence: vm 10:21:10Z, 10:21:17Z · fix: persist width/height on upload (MediaAsset + createAsset) and createdBy on versions, or FIGMA drops 146:10's dims and 240:11
- E8 · P3 · board actions the code documents as unbuilt: Layers 'Move to page…' (1082:4564 menu; documented as unbacked), Content 'Import JSON' (1170:4775; CMSRecordsModal.tsx:396-402) · stops: J-1082:4527, J-1170:4749 · owner: founder · evidence: vlr 09:58:04Z J-1082:4527; vc 09:51:12Z J-1170:4749 · fix: build, or FIGMA drops the two rows

## F · harness / tooling

- F1 · ❌ **THE ROW'S FIX WOULD HIDE A REAL DEFECT — do not take it. Measured live 2026-09-03.** The diagnosis was right about *where*: shell-default's failing pairs are the onboarding coach — `+`, `→`, `Next up`, `Drop a ready-made section…`, `Step complete`. But the row's fix, "seed onboarding state so the shell is measured **without** the coach", would make the gate stop seeing **five genuine WCAG AA failures**. Hiding a surface from a contrast gate is not a harness fix; it is the gate losing. Live run against `:5050`: five text-contrast failures, and four of them are `rgb(107,114,128)` on `rgb(243,244,246)` at **4.39** — the identical pair as A-PANELSUB — with the fifth `rgb(14,159,110)` on white at **3.39**. Neither is about onboarding. See **A-CONTRAST**: this is two token pairings failing AA wherever they occur, and the coach is merely one place they are visible. Row superseded.
- F2 · ❌ **PREMISE DISPROVED 2026-09-03 by measurement.** The row says "fixture localStorage carries onboarding state". It cannot: `measure.mjs` calls `playwright.chromium.launch()` then `browser.newPage()` — **no `userDataDir`, no `storageState`** — so every run gets a fresh browser and an empty localStorage. Confirmed empirically: three consecutive `shell-default` runs returned **byte-identical** output, 5 targets and the same 5 contrast pairs each time. Nothing accumulates between runs. *(A first two-run attempt printed `6` then `0`, which looked like state persisting — it was the two runs racing each other's browser launch. A null result is the harness until proven otherwise, and re-running it three times with full output rather than a grep count is what proved it.)* So "coach never mounts" and "checklist floor 3" did not come from persisted state; if they were seen at all it was in a hand-driven browser with a real profile, which is a different instrument from the one the rows are measured on.
- F3 · ⚠️ **THE PREMISE IS UNTESTED, NOT PROVEN — checked 2026-09-03.** The row says the exit guard "did not fire on nudge" and concludes `beforeunload` is "**likely undrivable**". That word is doing all the work, and nothing behind it was measured: a repo-wide grep of `e2e/` and `scripts/` for `runBeforeUnload`, `page.on("dialog")` or `page.on('dialog')` returns **nothing**. The harness has never attempted it. Playwright drives exactly this — `page.close({ runBeforeUnload: true })` fires the handler and `page.on("dialog")` receives the prompt. The producers are real and locatable (`StudioHeader.tsx:467` registers `onBefore`; `InteractionRuntime.ts:224` registers the canvas one), and the row itself notes the product fix landed 2026-08-24. So this is not "the browser will not let us"; it is "we did not write the four lines". · fix: drive it with `runBeforeUnload` + a dialog handler before concluding anything about the guard
- F4 · ⚠️ **PARTLY FIXED 2026-09-03, and one of its claims is a misreading.** **Fixed — the biggest one:** `drillHost` (`e2e/probe/probe.tsx:205`) staged every drill-in at **320×840** (`tw:w-80`), while all five boards it hosts are **280** wide — 146:2 asset-detail, 146:32 versions, 146:68 used-in, 147:2 icon-picker, 147:55 stock-browser. Four surfaces were being measured 40px wider than the thing they are compared against, so every width, wrap point and truncation read from them was wrong *at the source*. Now `tw:w-70 tw:h-203` — 280 × 812, the drawer body (900 − 56 topbar − 32 footer), written on the spacing scale because Gate 14 bans layout literals like `[840px]`. **Misreading, no change made:** "media-drawer-single mounts ONE item where 144:2 draws 4" compares the wrong pair. That fixture is deliberate and its own comment says why — "One card, so a conformance target for `Card / media` resolves to exactly one element — measure.mjs refuses ambiguity, and rightly: whichever card happened to be first would be measured silently." It is consumed by `media-card.json`, which targets `card` and `badge`; board 144:2's grid is measured by `media-drawer-grid`, which mounts the full set. **Still open:** the media-drawer `panel` target pointing at an inner div while the outline lives on `chrome-ui/Drawer.tsx` (which the fixture never mounts), the media-card 108-vs-116 delta, and the six block-only cases sitting in hand-built `<div style={{width:280}}>` whose specs copy 'Layers'/'Search' text the probe never renders.
- F5 · ⚠️ **PARTLY FIXED 2026-09-03 — the two gate-blindness halves are done; the coverage volume is not.** **Fixed:** `check-anchors.mjs` could only see a literal `data-testid="x"`. It was blind to the **forwarded** form (a component takes `testId="x"` and renders `data-testid={testId}`) and the **templated** form (`data-testid={\`insert-group-${id}\`}` → ids like `insert-group-layout`). Both failed in the direction that *blocks work*: a real, rendering anchor read as absent, which is why the three per-family recipes the ledger already had written — Insert 137:2, Pages 141:124, Notifications 453:4051 — were stuck waiting on anchors that render fine. The matcher is now `anchorForm(id, haystack)` in `lib.mjs`, returning `literal` / `forwarded` / `template:<prefix>${…}` / null, with the template verdict reported as deliberately weaker (it proves a prefix exists, not that this id is produced — `measure.mjs` settles that). Seven tests including "still returns null for an anchor nothing renders", and the gate watched failing end-to-end on a planted recipe (exit 3) then clean (exit 0). **Also fixed:** `diff.mjs`'s skipped/compared ratchet did nothing when a surface had no baseline entry, and `.conformance-baseline.json` holds **one** key against **nine** surfaces — so eight of nine could stop comparing targets and the run still printed PASS. It now prints `COVERAGE RATCHET INERT for "<surface>"` with the seeding command, rather than being silently inert. Not a hard failure: a new recipe legitimately has no baseline on its first run, and failing there teaches people to skip the gate. **Also fixed — the baselines are seeded.** I had written "blocked on a live app"; that was wrong, I can start one. Probe host up on `:5051`, all **8 probe surfaces measured live, 0 missing targets**, and the 4 clean ones (`content-load-error`, `layers-load-error`, `layers-loading`, `layers-no-results`) seeded through both `measure --update-baseline` and `diff --update-baseline`, each passing its board diff (5 / 7 / 8 / 8 properties). `.conformance-baseline.json` is **1 key → 5**, so the coverage ratchet is live for 5 of 9 surfaces instead of 1 of 9. The other 4 (`media-drawer`, `media-bulk-bar`, `media-card`, `media-load-error`) are deliberately **not** seeded — they all carry one real defect (see A-PANELSUB) and seeding would bake a WCAG failure in as accepted. **One F5 sub-claim is now stale:** "shell-default measures ONE component (4 of 5 targets SKIPPED — rail, sidebar-panel, canvas, breakpoint-switcher)" — measured live against `:5050` it is **5 targets, 0 missing**. All five resolve. **Still open:** the coverage number itself (6/355 boards) and `boards.json` `recipe: null` on 439/439.
- F6 · ✅ **FIXED 2026-09-03 — the baseline is a SET now, not a count.** `contrastFailures: 3` could only answer "is the number bigger", so **fix one pair, introduce another, and the gate passed reporting no change** — and it could never see a known pair getting *worse* (4.39 → 2.10 is still one failure). That is also why the README could baseline "add-page and zoom controls" while no zoom pair was in the measured five: a 3 looks like any other 3. `measure.mjs` now records `contrastPairs` / `nonTextPairs` keyed by `selector | text | color | bg` and reports three outcomes separately — **NEW** (a measured pair with no baseline entry — the swap), **WORSE** (a known pair whose ratio fell; overridable only with an explicit `--accept-regression`, printing old→new), and **FIXED** (an improvement, which asks you to re-record). A legacy numeric-only baseline still works but now says so loudly instead of pretending the count check is equivalent. The comparison is a pure function in `lib.mjs` (`contrastKey` / `contrastSet` / `compareContrast`) so it could be **watched failing**: `scripts/__tests__/contrast-ratchet.test.mjs`, 7 tests, plants the exact one-for-one swap the count missed and asserts the counts are identical while the set catches it.
- F7 · ⚠️ **HALF FIXED 2026-09-03; the other half is a design constraint, not a defect.** **Done:** `MAX_AGE_DAYS` 30 → **14**. The 30-day window was too slack to ever fire — the 2026-09-01 redraw moved 135 boards and landed at **day 28.5**, so neither the pre-push FAIL nor the CI WARN went off for a change that invalidated most of the specs. A calendar alarm that expires after the redraw it exists to catch is decoration. Watched failing in both modes on a throwaway copy: `--mode=prepush` exits 1, `--mode=ci` warns and exits 0. **Not done, deliberately:** the row's `--live` proposal (re-fetch the board through `figma-mcp.mjs` inside the pre-push hook) is the wrong trade. `check-spec-age.mjs`'s own header records why — CI has no Figma access, and putting a network call in a blocking hook means a Figma outage blocks every push in the repo, which is how a gate gets `--no-verify`'d into irrelevance. The `diff.mjs:94-112` raw-vs-spec hash is likewise **not** a defect: its comment already states it catches "a raw file that was refreshed while the spec was not", which it does, and it never claimed to see a board change. The honest gap — nothing automated can ask whether the board actually moved — stays open and stays documented.
- F8 · P3 · History verification harness: the fixture (cmrsur1fp…) has 51 mirrored versions and a PENDING latest review — the approval band needs a site whose LATEST review_request is APPROVED (only test-site-iter5, soft-deleted 07-18; 949:4474's 6 COMPLETED publishes live there too); an 'A comment lost its element' modal (#bk-overlay-root) intercepts pointer events after createVersion; tRPC batches up to 15 queries per GET so one abort kills its batch-mates (reviews.status, sites.myRole); four parallel browsers → 30s goto timeouts and screenshots hung on fonts; the composer is read off the React fiber of .bd-history-container. The versions the verifiers minted on it are Cleared (co 10:18:38Z); the fixture requirement stands · stops: J-162:2 (band re-verify), J-949:4474 (re-verify on an owned live site) · owner: coordinator-code · evidence: vh 05:42:45Z family:History (F), J-949:4474, J-162:2 · fix: one owned, undeleted fixture with ≥1 COMPLETED publish and an APPROVED latest review; run scenes serially; scripts at scratchpad/verify-history/{probe,fetch-board,shot-board}.mjs
- F9 · P2 · Figma seat cap: 'You've reached the Figma MCP tool call limit for your Full seat on the Professional plan' at ~26-28 calls per hour (get_metadata + get_design_context per board, 1s apart). Content lost 303:2083 ×2 and 1170:4713/4749 metadata (recovered ~40 min later); Media lost 21 payloads at 09:1xZ and refetched all 16 clean at 10:05Z; Layers/Review lost six boards that were measured live against siblings only and sit in jobs.json as `verify` with owner verifier-layers-review · stops: J-143:237 (🔒 rest colour vs live #111827), J-143:295 (does the board draw the 37h banner?), J-782:4260 (footer '66 layers' under 'Nothing matches'), J-1171:4829 (⚙ popover anchor + mono header), J-157:221 (13/11 vs live 14/12), J-158:213 (toast or state?) · owner: coordinator-figma · evidence: vc 09:51:12Z H-figma-ratelimit; vlr 09:58:04Z ×6 (blocker F); vm 10:23:37Z media-harness (1) · fix: pre-cache get_design_context per family before launching its verifier; < 28 calls/hour per token; re-read the six and close them from the live numbers already in the ledger
- F10 · P2 · fixture cannot reach: 160:105 bound-to-CMS (the fixture collection reads 'Menu · 0 fields' — seeding a field is a server write; static read gives hint 12→11, Unbind tw:h-4, chip border→tint); 160:2 variant Select rows (no component with variantProperties); 147:55 stock cards (media.searchStockPhotos returns [] without PEXELS_API_KEY / UNSPLASH_ACCESS_KEY); 159:123's producer — shift-click on the canvas and in Layers both left a single selection (useSelectionBehavior.ts:242 reads shiftKey; harness or product unresolved, ⌘A used instead); 1174:4849 partial card (no failing element) · stops: J-160:105 (jobs.json `unbuildable`), J-160:2, J-147:55, J-159:123, J-1174:4849 · owner: coordinator-code · evidence: vi 09:41:45Z J-160:105 (blocker F), J-160:2, J-159:123; vm 10:21:38Z, 10:22:55Z · fix: seed one text field on Menu + bind a paragraph; one component with a variant property; provider keys in the verifier context; the shift-click reading checked by hand
- F11 · P3 · producers the run rules forbid: 158:57 re-sending (only producer is confirming Re-send → reviews.submit; the label lasts the export + failed request ≈1s); 158:213 review-closed (needs an approval mutation — if it is the client-approval toast, its producer is the reviews.status poll at StudioHeader REVIEW_PILL, not ReviewTab). The History rollback trio is A11 · stops: J-158:57 (jobs.json `unbuildable`), J-158:213 · owner: coordinator-code · evidence: vlr 09:58:04Z J-158:57, J-158:213 · fix: authorise a run with reviews.submit / reviews.status aborted at the route
- F12 · P3 · fixture side effects that read as product: the 'A comment lost its element' dialog on load (the P1 in Cleared — until re-measured, pointer clicks need it dismissed; JS clicks bypass it and read as 'works'); an aborted sites.saveProject surfaces as 'Offline — not saved'; a blocked siteVersions.create raises 'A saved version didn't sync'; refusing IndexedDB for Media load-error raised a 53px page banner + '[BuildrikSync] load failed: Failed to save folder'; the fixture's saved grid element loads as type 'container' (header 'Container', Display=Block) — an inserted Grid is the honest producer; Content load-error shows only when the LOCAL store is empty (ContentTab.tsx:196 — a returning browser with an IndexedDB copy never sees it) · stops: none directly — reading hazards for every live verify · owner: coordinator-code · evidence: vm 10:23:37Z (3)(4), 10:21:45Z; vc 09:51:12Z J-453:4010; vi 09:41:45Z J-807:8475 · fix: editor-rig dismisses the dialog and labels an abort as an abort; check why a saved grid loads as container
- F13 · P2 · CROSS-CUTTING — flowbite h-10 / link-variant leak: every text-link or icon slot the boards draw at 16-28h is a flowbite Button (link variant keeps its h-8 = 32h, base h-10 = 40h) or TextInput (36-42h); tw:min-h-6 / tw:min-h-5 are a different property and never win (CLAUDE.md §Overriding a flowbite default), and every such label renders 500 where the boards say 400. **Inspector** 18 (TEXT) / 10 (FLEX) / 35 (GRID) / 42 (INPUT) controls ≥36 tall inside 24-tall rows — Align buttons overlap the Color row by 12px; batch 7 added a two-class cap on flowbite controls inside rows (re-measure pending), while the empty-state action (61.7×32 vs 268×16), Reset to master (91.5×32 vs 16 text), revert 22×32, Bind 32×32 (header 53 vs 48) are still open. **Media** ~10 slots: quota Optimise (+17px band), empty CTA, load-error links, Generate, versions ⋯, Jump ›, footer links (wrap to 2 lines at 280), load-more, Clear search/filter, bulk-bar actions, filter chips 88×32. **Layers** 6: search input 40 in a 28 box, ⊞⊟⚙ 10×40, rename input 36 in a 28 row, load-error / no-results 32h links (block 150 vs 127), settings ✕ 9.5×40, the breadcrumb button. **History** 5: 40h hover buttons on 62.5 rows, 32h buttons vs 28, foot 32h button vs accent link, Cancel/Continue 40 vs 32, FILTER_CHIP 40→24. **Review**: Compare/Re-send full-width 40 vs 28/32 auto, Resolve/Reattach 32, confirm buttons 36 vs 28. **Content**: '+ New collection/field/variable/condition' 32h Buttons with tw:my-2 → 48px blocks vs 32/36 rows, 'Try again' 32 vs 20 · stops: J-807:8342, J-807:8567, J-807:8412, J-807:8475, J-807:8614, J-159:99, J-160:2, J-160:208, J-1175:4841, J-144:2, J-145:199, J-145:250, J-145:359, J-453:3931, J-146:2, J-146:32, J-146:68, J-782:4353, J-145:300, J-147:55, J-142:2, J-1082:4589, J-781:4217, J-782:4260, J-1171:4829, J-162:2, J-184:2, J-453:4031, J-163:167, J-156:2, J-157:2, J-158:2, J-158:105, J-148:2, J-151:2, J-151:46, J-151:62, J-151:87, J-453:4010 · owner: coordinator-code · evidence: vm 10:23:30Z media-shared (chrome-ui Button link variant → tw:h-auto tw:min-h-0 tw:py-0 tw:font-normal closes ~10 at once); vi 09:41:45Z ×5; vlr 09:58:04Z J-142:2, J-1082:4589, J-781:4217, J-1171:4829, J-156:2; vh 05:42:45Z family:History; vc 09:51:12Z J-148:2; co 09:51:50Z family:Inspector (batch 7) · fix: land the link-variant change + size="xs" / tw:h-7 (same-property, wins twMerge) on row controls BEFORE the per-job re-measures — otherwise every surface fix is re-broken by the base
- F14 · P2 · CROSS-CUTTING — Content's five shared constants drive 11 boards: SectionHeader.tsx:16 tw:h-7 (28 untinted vs 32 #f3f4f6 — the tint prop only reaches the skeleton, ContentTab.tsx:217,221); Row.tsx:27 ROW_META_CLASS (Inter 11/400 vs Geist Mono 11/500); ContentViews.tsx:62/181 LINK_BTN + tw:mx-3 tw:my-2 (48px block vs 32/36 row, 13/500 vs 13/400, x12 vs 16); ContentViews.tsx:85 SUB / :83 INLINE_HINT (12 vs 11px); Row size='comment' 64h where the boards draw 44 (fields, variables) and 56 (conditions — size='tall' already is 56) — plus EmptyState.tsx:26 BODY_CLASS beating a per-call tw:text-[13px] (plain element, stylesheet order) · stops: J-148:2, J-149:50, J-149:84, J-149:108, J-151:2, J-151:46, J-151:62, J-151:87, J-775:4241, J-149:7, J-303:2067, J-303:2083 · owner: coordinator-code · evidence: vc 09:51:12Z ×12 · fix: one commit — SectionHeader h-8 + tint at ContentViews.tsx:170,185; mono meta; LINK_BTN tw:font-normal + tw:mx-4 tw:my-0(.5); 11px SUB/hint; a 44 'stack' Row size (tw:h-11 tw:items-center) at :463/:909 and 'tall' at :1056; skeleton rows 32 with a placeholder for the '+ New collection' row so landed y == skeleton y (775:4241 moves DATA −32 on release today)
- F15 · P2 · CROSS-CUTTING — Media/Content/Brand modals share one delta set: Modal.tsx KIND_WIDTH has no 500/520/640 (form=560, lg=720) → 560 vs 500 (1170:4713), 720 vs 640 (1170:4749, 1164:4713), 560 vs 520 (1205:4804), 360 vs 520 (1172:4840); r8 vs r12 + 1px border; pad 20-24 vs px16 py14; H2 16/600 vs 13-14/600; inputs 36-42h white r4 vs 29-31h #f3f4f6 r6 11-12px; footer buttons 40h 13-14/500 r8 vs 27-29h 11/500 r6; footer dividers the boards lack; 1164:4713's forLabel ('for Hero · Image') exists (MediaLibraryPanel.tsx:40) but the Background caller never passes it. Radius waits on G13 · stops: J-1164:4713, J-1164:4738, J-1175:4827, J-1205:4804, J-1205:4816, J-1170:4713, J-1170:4749, J-1172:4840, J-1174:4849 · owner: coordinator-code · evidence: vm 10:22:41Z, 10:22:48Z, 10:23:02Z, 10:23:16Z, 10:23:23Z, 10:22:55Z; vc 09:51:12Z J-1170:4713, J-1170:4749; vb 05:40:25Z J-1172:4840 · fix: add 500/520/640 kinds + a 'compact' modal recipe (rounded-xl, px-4 py-3.5, title 14/600, size="xs" 11/500 footer, sm inputs h-8 bg-subtle) once, then each modal opts in; pass forLabel from the Background section

## G · founder decisions pending (write the answer in one line)

> **Integrity note, 2026-09-02.** The four decisions in G1/G8/G10/G11 and in C10/C11 were retracted at 14:30:39Z by a **second Claude session** (`session_01UiRqRrjrnEV5zNLuHFsnhG`, commit `807af1d6`) on the premise that "the founder answered none of them". That premise was false: the founder answered all four, and later four more, inside the coordinator session's own transcript. The tell that session used — round ten-second timestamps — fires whenever a batch of lines is hand-written in one script, which is what recording several decisions at once looks like. Everything has been restored, each line now **quoting the founder's own selected words** so a session without the transcript can still audit it. The real defect is two sessions writing `docs/design-jobs/` concurrently: `jobs.json` is single-writer by PROTOCOL, and an auditing session cannot see another's founder messages.


- G1 · ✅ **ANSWERED — DISSOLVE into New-Page + Insert** (founder verbatim in-session 2026-09-02: 'Dissolve into New-Page + Insert'). Ch.14 S3.4 already reads 'J3 · DISSOLVED' and DESIGN.md:286 lists Templates under 'Retired from the rail'; Ch.12:77 is the stale side. **D2 is MOOT** — the x1120 drawer it wants moved to x60 is the retired rail drawer. D3 stands. Code follow-up: retire `sidebar/tabs/templates` into New-Page + Insert.
- G2 · ✅ **ANSWERED — KEEP THE FIVE, redraw 32:2** (founder verbatim in-session 2026-09-02: 'Keep the 5, redraw 32:2'). pick @136, select-parent @160, Ask AI @184, bind @223, ⋯ @257 all stay — deleting three 14:232-verified affordances because a board omits them is a behaviour change, not conformance. D-01 closed; J-32:2 → `fix-figma`.
- G3 · ✅ **CLOSED on the standing licence — 20px hit box (code).** layers-v2.css:162-168 wins; the glyph stays 10×10, so the board's badge x219-vs-229 is not a drift. eye/lock rest colour takes the code's #6b7280 — the board's gray-300 is **1.47:1 on white** and cannot be a contract. DESIGN.md gains 'editor chrome — 20px minimum hit target; the glyph inside it may be smaller' beside the 413 marketing rule.
- G4 · ✅ **CLOSED on the standing licence — ADD `color-warning` to the seed.** projectMigrations gains it with a ≥4.5:1 pair per 06:63. catalog.ts:315's green-painted warning is a bug under *either* answer, so the variant must be fixed regardless; keeping it costs one token, dropping it costs a real state.
- G5 · ✅ **TAKEN ON RECOMMENDATION — the accordion (code); redraw 296:1999/2030.** A flat 30px row has nowhere to put 14:51's 'CTA routing', which is the thing the checklist exists to do.
- G6 · ✅ **ANSWERED — MODAL (code), routed through chrome-ui** (founder verbatim in-session 2026-09-02: 'Modal (code), routed through chrome-ui'). AddTokenModal.tsx:47-70 and TokenReplaceModal.tsx:120 stay; the z200 in-panel scrim moves onto chrome-ui Modal for Gate 22; both modals get boards. An inline add row inside a 280 drawer is where the F13 h-10 leak lives.
- G7 · ✅ **ANSWERED — GATE THE FIRST CHECKPOINT BEHIND A REAL EDIT** (founder verbatim in-session 2026-09-02: 'Gate the first checkpoint behind a real edit'). VersionTimelineManager.ts:140-148 stops checkpointing on `project:loaded` — a checkpoint created by opening the site is a version nobody made, and it is why Saves·empty was unreachable on every healthy site. J-163:64 → `fix-code`, and the board becomes buildable.
- G8 · ✅ **ANSWERED — REDRAW THE SEVEN AT 280** (founder verbatim in-session 2026-09-02: 'Redraw the seven at 280') on the 16:6 header; Figma moves, not code. DESIGN.md:289 '280 for all six'; the 135 drawer boards were redrawn at 280 on 09-01; a 360 right column would be the second new shell surface after B1's 1080 OverlayMount. C7's strip lands in the same pass — see Queued redraw waves, wave 1.
- G9 · ✅ **TAKEN ON RECOMMENDATION — add ✕ to chrome-ui Modal.** One shared component beats five board edits, and a modal with no visible close is a support complaint even when Esc works. 453:4064 gets Close **and** Try again, as live already has.
- G10 · ✅ **ANSWERED — RETIRE 159:2** (founder verbatim in-session 2026-09-02: 'retire 159:2'). Renamed in Figma; J-159:2 → `unbuildable`, authority `superseded-by:32:2`.
- G11 · ✅ **ANSWERED — redraw 189:2 as the TAKEOVER** (founder verbatim in-session 2026-09-02: 'redraw 189:2'). ProInspector.tsx:510-529 deliberately clears the sections while reaching whole-site. Copy already fixed: 189:103 now reads '…live in the **Brand panel**…'. Body measured: header 48 · row 32 · Reach note 36 · Site-wide hint 52 · **16 control rows 32h** · Frame 164 — the 16 rows are what the takeover replaces with 'Open Brand' / 'Back to this element'.
- G12 · ✅ **TAKEN ON RECOMMENDATION — FIGMA drops the Upload link.** The drop zone and the footer already carry Upload; a third door on one empty screen. Pill counts 128/6/24/370 → 0 already landed in the C12 pass. J-145:359 → `fix-figma`.
- G13 · ✅ **ANSWERED — SPLIT THE RULE BY SURFACE** (founder verbatim in-session 2026-09-02: 'Split the rule by surface'). Gate 13 A1.3's radius ≤4 stays for **panel chrome**; modals, overlays and swatches get r8/r12. A1.3 was written for chrome and was being applied to overlays, which is why every Media/Content modal board failed a gate that was never about them — change its SCOPE in DESIGN.md §Chrome Axioms, not its number. Unblocks J-152:137, J-1172:4840 and the F15 set. **Note** commit d8d780f5 already landed the modal half (r12, 16/14 padding) from the other session; A1.3's scope must widen or the ratchet reverts it again.
- G14 · ✅ **TAKEN ON RECOMMENDATION — one band (live).** Two stacked bands for one condition is on DESIGN.md's anti-slop list. The tone/size fixes are fix-code either way: amber → error-text, the uncompilable `tw:text-[${isExhausted ? '13px' : '11px'}]` → two literals, line 2 at 11px #6b7280.
- G15 · ✅ **TAKEN ON RECOMMENDATION — keep the divider; add it to 208:271.** It is shared by every 280 drawer header through the 16:6 instance, so removing it from code touches every panel, and a 44h header needs the separation.

## H · the same state drawn twice

Opened 2026-09-02. **Two of the ten rows did not survive being read**, and both
would have destroyed real work: H4 would have cut 19 live prototype edges, H6
would have deleted the only record that a whole family was missing. Both had been
written from a name-similarity scan, and a name is not a duplicate — see
`feedback_matched_boards_to_the_wrong_component`.

- H1 · ✅ **RESOLVED 2026-09-02 (Figma).** 642:3112 drew the create-component modal at **440**. The producer is `CreateComponentModal.tsx` with `<ModalContent size="lg">`, and `SIZE_WIDTH_CLASS.lg` is **720**. 1170:4777 rebuilt at 720×740 with all six field groups verbatim from source: Name\*, Description (`Optional description...`), Category (`e.g., Headers, Footers, Cards`), Tags + `Comma-separated tags for easier searching`, the Variant Options sub-section carrying the three real `VARIANT_PRESETS` chips (Size S/M/L · State Default/Hover/Disabled · Theme Light/Dark) and `You can configure variant values after creation`, and the checked `Pre-fill from DS styles` with its bindingCount hint. Footer `Cancel` / `Create component`.
- H2 · ✅ **AMENDED, THEN CLOSED 2026-09-03 — the row named the wrong board, and the real defect was wider.** The duplicate trio is `130:2` ≡ **`130:798`** ≡ `131:2` (230 content nodes, zero differences). `130:997` is *not* a duplicate — its topbar carries a 136px `Publish anyway` button where the others carry an 81px `Publish`, and the three boards use three different Topbar instances, so a struct-hash that reads through to the master component misses it. That is almost certainly where the false positive came from. **The bigger finding: all four boards drew `In review · 3 open` regardless of their state.** `StudioHeader.tsx:134-141` is the contract, and it is now honoured — `130:2` (state `none`) shows **no pill at all** (`REVIEW_PILL.none` is `null`); `130:798` and `131:2` show `Approved by Sara Whitfield · 2h ago` in success tone (the `reviewerName` branch, `:686-688`); `130:997` shows `Approved · edited since` in warning tone. `130:798` and `131:2` still render identically **and that is the product's answer** — S5.2 `approved` and S5.6 `approved-clean` resolve to the same pill branch, so the difference lives downstream, not in the topbar. Recorded on the board rather than invented. The `match` claim should be revoked on `J-130:798`, not `J-130:997`. (The row's "181 descendants" does not reproduce; the real figure is 230.)
- H3 · ✅ **RE-SCOPED, THEN EXECUTED 2026-09-03 as a MERGE.** "Retire 1:6" was withdrawn on measurement — it would have cost 9 substantive states, 3 stubs and **8 flow starting points**, and 1:6 is the *higher*-fidelity set (43–55 descendants per board with real copy, against 6–17 with `{Agency}` / `{Site}` placeholders on the page marked `(CURRENT)`). **Neither page can absorb the other: Figma rejects cross-page `NAVIGATE`**, and 1:3's set has to stay reachable from the editor prototype while 1:6 owns the standalone journey's 8 entries. So the merge carried what only one side had, in both directions:
  - the three dead-link states that existed **only** on 1:3 (`revoked`, `not-found`, `conflict`) were cloned onto 1:6, renamed into its own `S5.5 ·` convention and wired off `S5.5 · expired-token` (121:2), their real sibling. *(The first wiring pass hung them on `Transfer · expired link` — a different flow that merely shares the word "expired". That is the same name-match error that produced these H rows; caught on re-read and corrected.)*
  - the two **code-truth annotations** that existed only on 1:3 were carried onto their 1:6 counterparts: `TERMINAL by design — the branch renders text + 'Signed as', zero controls (review-client.tsx:287-298,247-272)` onto `122:3`, and the `UNBUILDABLE — clientReview.get throws only INVALID_TOKEN/REVOKED/EXPIRED …` annotation restored onto the carried conflict board, which the clone's rename had silently dropped.
  - all ten 1:3 boards renamed **`— echo of 1:6 (canonical)`**. That answers what the row actually asked ("neither superseded") without deleting a reachable state: 1:6 is the source of truth, 1:3 is the echo the editor prototype must be able to reach, and the split is now recorded as forced by the tool rather than by indecision.
- H4 · ❌ **WITHDRAWN 2026-09-02 — the premise was false.** The seven Backups boards are not duplicates of anything. They are one coherent, already-classified `[design-ahead]` family (879:4518 restore-confirm, 879:4526 restoring, 879:4531 restored, 879:4536 creating, 950:4474 tab, 1156:4620 empty, 1156:4665 restore-failed) and they are **fully wired**: entry from `History · Saves` and `Reference · index`, then restore-confirm → restoring → restored / restore-failed, creating → tab, all returning to the tab. Executing this row would have cut **19 live edges** and left dangling destinations. The only thing named "backup" that is genuinely redundant is `caption/S7.2 - backup-restore` (817:5432) — a TEXT caption, not a board — and its text is stale besides: it says the panel is reached from *Settings → Backups* while every board puts it under History.
- H5 · ✅ **AMENDED, THEN CLOSED 2026-09-03.** The row named 4 boards but there are only **3 surfaces**: `817:5289` and `1157:4649` are the *filled* and *empty* halves of one site-health board (55 shared nodes; one carries the CDN/DNS/SSL incidents and `99.97%`, the other `100%` / `No incidents in the last 30 days.`). Both renamed to say so, so nobody re-files a legitimate pair as a duplicate. The premise held in code: `SiteMenu.tsx:261` hands off with `openDashboard(\`/dashboard/sites/${siteId}?share=1\`)` — **the editor deliberately does not own a share modal.** The destination question is answered: 988:2 had `Site · Sharing` but **no `Site · Access`**, while `app/dashboard/sites/[id]/access/` + `access-tab.tsx` are what actually own share permissions. Three boards drawn from that producer (`Site · Access`, `· new-link`, `· empty`), cloned from `Site · Sharing` (1006:3128) so the nav, sidebar and site header cannot drift, and wired. `817:5069` and `817:4774` are now marked **`[not-implemented · superseded by 988:2 · Site · Access]`** — the `[not-implemented]` half is load-bearing: dropping it made a deliberately-unwired board count as an orphan on the next flow read.
- H6 · ✅ **RECLASSIFIED, THEN CLOSED 2026-09-03.** The three "Commerce" nodes — `S3.13 · Ecommerce — setup` (304:2052), `— sample-added` (304:2085), `— bound` (304:2107) — are **TEXT nodes**, 19px tall: board *labels* for a family nobody drew. Deleting them would have destroyed the only record the family was missing. Ecommerce has a real producer, so the family was **drawn** instead: `Ecommerce · collection-setup (modal)` at **720** (`CollectionSetupModal.tsx`, `ModalContent size="lg"`), `Ecommerce · collection-created · toast` at 360, `Ecommerce · bound · inspector` at 300 (header chip + `BindingBanner`), and the binding popover at 240 in its `collections` and `fields` steps. The labels were rewritten to say what they are. **`sample-added` is not a screen** — it is a checkbox inside the setup modal, checked by default, so it would have been the same drawing; the distinct state is the success toast, and that is what got drawn.
- H7 · ✅ **EXECUTED 2026-09-03 — but two of the five recorded actions were unsafe and one had no evidence.** Measured first over 85,097 nodes on all 11 pages (95 of 164 variables bound, zero `figma.mixed` reads), then written:
  - ✅ **kept `spacing/N`** — though only 6 of its 19 are bound at all (`spacing/12` ×36, `/8` ×22, `/2` ×5, `/4` ×3, `/24` ×2, `/16` ×1).
  - ✅ **added `spacing/28`.** `space/7 = 28` was the *only* value in `space/1-8` without a `spacing/N` twin, so this was a prerequisite for the delete, not an extra. (The row's "8 value collisions" is **7** — that one unique value is the whole reason.)
  - ❌ **`spacing/36` NOT created.** No variable in either collection holds 36 and nothing is bound to a 36-valued variable. The action rested on nothing.
  - ⚠️ **`space/1-8` deleted — but only after rebinding.** All 23 live bindings sat inside `1306:2`, itself already marked RETIRED. Deleting in the recorded order would have **silently unbound 23 padding/spacing properties**. Instead: 23 bindings on 12 nodes rebound to the same-valued `spacing/N`, then the binding count re-measured across every page, then all 8 removed with 0 survivors. The delete was gated on the re-read, not on the rebind's own report.
  - ❌ **`alias 973:*` WITHDRAWN.** Those three live in **Package**, which has two modes, and they are not twins of their claimed Primitives counterparts — they match only in Editor mode: `size/topnav-h` 56/**60**, `size/sidebar-w` 240/**293**, `radius/control` 6/**8**. Aliasing would collapse the entire Dashboard column onto the Editor value. The premise was measured in one mode.
  - ⚠️ **`1044:5219` / `1044:5214` retargeted to `524:56`, not `2:3`.** `color/accent` (2:3) lives in Package and is *itself* an alias to `flowbite/blue/700`; pointing Primitives at it closes a **collection-level dependency cycle** and makes a 1-mode variable resolve a 2-mode one. `524:56` is the same colour, same collection, single mode. (`color/focus-ring` is `#1a56db` at 25% alpha and cannot be folded into a solid — left alone.)
  - ✅ **the 45 paint styles are gone.** The `fillStyleId` scan this row asked for was run: **0 consumers** across every page, and the 1:1 mirror against the 45 `color/*` variables is exact. All 45 removed, nothing kept.
  - ✅ **the dead text and effect styles are gone too (2026-09-03).** 23 of 34 text styles and 2 of 12 effect styles removed, each measured to 0 `textStyleId` / `effectStyleId` consumers across every page. 11 text and 10 effect styles kept because nodes still point at them. *(For the record, the 5 value-twin groups this cleared: **23 of 34 text styles unused**, with 5 value-twin groups (`ui/13 · row label` ≡ `ui/13 · body`; `ui/14 · panel title` ≡ `ui/14 · body medium` ≡ `ui/btn-lg`; `ui/12 · small medium` ≡ `ui/btn-sm`; `ui/13 · row label medium` ≡ `ui/btn-md`), and 2 of 12 effect styles unused (`dash/panel`, `elevation/inset`).)* Not verified: the row's "21 raw colour groups" and "icons in 3 naming conventions".
- H8 · ⚠️ **RE-SCANNED 2026-09-03 — three items are stale, one count is wrong, and the sweep has a landmine.** Already deleted by `d36c96f2`: `ReplaceAcrossModal`, `DSStatusChip`, `ColorModeIconCycle` — strike them from the row. **`ReplaceAcrossDialog` is LIVE** (`MediaTab.tsx:18,295`) and must be protected from any sweep that greps for the similar name; only "LibraryManager has no replace-across door" still stands. **"4 history icons" is 7** — `history/icons.tsx` exports 8, of which `TimeTravelIcon` is live (`ActivityView.tsx:27,445`); the three the ledger missed are a name-collision trap, because `CanvasFooterToolbar.tsx:99,106` and `sidebar/shared/headerIcons.tsx:26` define their own same-named locals. **"11 shell primitives" is the right count** — the row named 9; the two unnamed are `RailSpacer` and `FooterSpacer`. All 11 have zero product renders and no chrome-ui→chrome-ui internal use; the shell the product renders is `editor/rail/LayoutShell.tsx`, which defines its own local `Rail` / `Drawer` / `Footer`. **23 symbols are genuinely dead today**, two of which the row missed (`ControlRow`, `FeatureCardGrid`). **Landmine:** deleting any chrome-ui primitive must amend `scripts/gates/chrome-ui-surface.manifest.json` in the SAME commit — `gate:chrome-ui-surface` fails on barrel/manifest drift. Not swept yet.
- H9 · ⚠️ **RE-COUNTED 2026-09-03 — the counts drift and only 3 of 13 are genuine semantic duplication.** Actual numbers: `Try again` is **43 across 26 files**, not 20; `Something went wrong` is **8 across 6**, not 3; `Nothing matches` (9) and `Clear search` (8) are exact. **Must NOT be extracted** — `Nothing matches` is 9 unrelated empty-state layouts in three different quote encodings, `Clear search` is 2 aria-labels + 6 links + a ternary, and `Try again` has three different shapes (button label, toast sentence, modal `confirmLabel`) with all-different callbacks; its correct abstraction, `PanelErrorState`, already exists and is already reused. That is the repo's own "don't extract things that merely look similar" rule doing its job. **Genuinely duplicated, worth one source each:** (1) the unlink-token block — `SizeSection.tsx:62-77` vs `FontControls.tsx:70-85` differ by **one line** (`spacing token` vs `type token` in an aria-label), with a third drifted relative at `ColorInput.tsx:180-181`; (2) `CHIP_ACTIVE` — byte-identical in `ImageEditorModal.tsx:171` and `OptimizationPanel.tsx:54-55`, and to be extracted **without** its hex fallbacks, since `#EBF5FF` disagrees with the token it backs (`tokens.generated.css:84` is `#E1EFFE`); (3) the two `Discard changes?` dialogs inside `ContentViews.tsx:375-383` and `:670-678` — the `SettingsTab.tsx:977` third is a different rule and stays. **Dead constants confirmed at 0 consumers:** `config.ts:136-138`, `canvas.tokens.ts:184-190` (whole block, and already **drifted** — `topbarHeight: 52` vs token 56, `footerHeight: 40` vs token 32, where 40 is the height `layout.ts:68` explicitly bans), `layout.ts:52 TOOLBAR_H`, and Emotion `GuideLine` (`OverlayStyles.ts:149`). **Two stale-comment corrections worth more than the dedupe:** `PagesTab.tsx:325-328` claims "this door raises no toast (only the page-tab bar does)" — false, `usePages.ts:237-271` raises an Undo toast too, and that stale comment is why the two messages drifted; and `eslint.config.mjs:187` still points developers at five names (`TOPBAR_H`, `HEADER_H`, `FOOTER_H`, `ROW_SM`, `ROW_MD`) that no longer exist in `layout.ts`. Not acted on yet.
- H10 · P3 · CMSRecordsModal.tsx:339 vs the Content tab's records view (ContentViews.tsx:890; board 1170:4749 now measured — 720 vs 640, the modal's only door is ⌘⇧P 'Manage CMS records', useCanvasCommandPalette.ts:170); StockSourceModal + ConfirmDeleteModal mounted on two media hosts (MediaTab.tsx:232/268, LibraryManager.tsx:467/488) · stops: M-03, J-1170:4749 · owner: founder · evidence: dup 05:20:09Z; vc 09:51:12Z J-1170:4749 · fix: decide whether the records modal is still a door; lift the two media modals to one host
  - ⏳ code-side duplicate, untouched by this Figma-only pass.

## Flow integrity (prototype wiring) — audited 2026-09-02, first full walk

`boards.json` scopes the census to page **1:3**, so five other pages carrying flows had never been walked. All six now have numbers.

> **Read this before quoting any earlier figure.** The first pass of this audit used a checker that collected reactions with `frame.findAll(…)` — which searches **descendants only**, so a reaction on the frame node *itself* was never counted. **248 frames on page 1:3 carry frame-level reactions**, and frame-level is precisely how the linear pages express "this state resolves into that one". Every percentage published before the fix was an undercount. Worst of them: 1:4 Site was reported as the file's worst page at 47% with 15 orphans and 28 dead ends — it is **100% reachable with none of either**. The same bug is in the `flow-audit` skill's own reference script.

| page | frames | edges | reachable | orphans | dead ends |
|---|---|---|---|---|---|
| 1:3 Editor | 403 live | 2229 | **100%** | **0** | **0** |
| 1:6 Client review | 21 | 39 | **100%** | 0 | 4, all now marked `TERMINAL by design` |
| 988:2 Dashboard v2 | 79 | 911 | **100%** | 0 | **0** |
| 1:4 Site | 47 | 112 | **100%** | 0 | 0 |
| 1:5 Portfolio | 33 | 90 | **100%** | 0 | 0 |
| 397:2 Dashboard spine | 34 | 162 | **100%** | 0 | 0 |

**Re-audited 2026-09-03, after the state-board arc.** Every board drawn in this arc
arrived as an orphan — **111** of them on 1:3, against 46 dead ends. All 96 live
ones are now wired (the rest carry a `RETIRED` / `SUPERSEDED` / `UNBUILDABLE`
marker and are excluded on purpose): **0 orphans, 0 dead ends, 0 dangling
destinations, 100% reachable**, edges 2063 → 2229. Counted by re-reading the
graph after the write, not from the write's own report.

Entry edges ride an **absolutely-positioned** `hotspot/state ·` on the parent —
auto-layout silently eats a child's x/y otherwise, which has already cost this
file three verify cycles. Return edges are frame-level, the way 248 other frames
on the page express them. Parents came from a segment-prefix rule with explicit
overrides where that rule was wrong (the stock states belong to
`Media · drill-in · stock-browser`, not `Media · grid`) and explicit chains for
the sequenced families (`AI · prompt → streaming → diff / error`; setup modal →
toast; popover `collections → fields → bound inspector`).

**Two of 1:6's four dead ends were real defects, not terminals.**
`Transfer · expired link` (869:6999) still drew an **enabled `Accept handover`**
button on a dead link — removed, replaced with the note its own copy promises.
`Transfer · accepted` (854:41) told the user to sign in and drew no control at
all — the primary `Sign in` button is now there. The other two, `Share · draft
preview` (854:30) and `Share · expired link` (869:7005), are public read-only
pages with nothing to wire.

**What the work actually was.** Of ~185 hotspots added to page 1:3, 15 duplicated an edge the host already carried and were removed; the other ~170 reached destinations nothing else reached — real gaps, whatever the headline number said. The systemic cause was genuine: family hubs wired fully *outward* while the states they lead to carried no edge of their own, so you could reach a screen and never leave it.

**Sibling asymmetry** was the second pattern — one variant of a pair wired, its twin not. On 1:6 that was three of four defects: `brand-colour` reachable and `brand-colour-fails-contrast` not; `post-approval-edited-since` with a way back and `post-approval-unchanged` without; `expired-token` backing out and `load-error` not.

**What was deliberately NOT wired.** A state produced by a condition (a failed upload, a 90-day expiry, an empty result set) gets a `hotspot/state ·` at most — never a fake button. A board whose control does not exist in code gets neither: Media at 100% quota has no exit because `StorageQuotaBar.tsx:93` gates the optimise link on `isNearLimit`; the AI step-gate renders no Stop (`AgentPlan.tsx:174`); `171:2` step-failed has no dismiss. Wiring those would have hidden three product gaps.

**Eight boards were named rather than wired** — no producer at all, so they read as flow gaps when they are missing subjects: `143:355` (the tree always seeds a root row), `1082:5004` (no list view exists), `306:2111`/`306:2136`/`306:2161` (the three preset mutators have zero UI callers, so `isDirty` is permanently false), `1339:7221` (its error code cannot come from its own query — a test hand-built the payload), `165:24` (no such `LoadState`), `1175:4841` (nothing writes the applied-template key).

**Why the 2026-08-28 boards were orphaned:** each **supersedes an older board that still holds the prototype edges**, and the supersession was never carried across — `1342:7162` ⟵ 296:2064/296:2069, `1343:7162` ⟵ 813:4676, `1347:7162` ⟵ 813:4870, `1344:7165` ⟵ 638:2732. All now wired to their real doors. `1344:7162` was **retired**: not a screen at all, a 1440×400 frame with two text nodes describing a model `SettingsTab.tsx:403-421` already implements.

**The last flow defect — resolved, not punted.** 32 frames carried two whole-frame `ON_CLICK`s to different destinations, so which fired was arbitrary (1:4 17 · 1:5 8 · 397:2 6 · 1:6 1). Settled by the file's own convention rather than a coin flip: **a forward transition gets a scoped `hotspot/state ·`; the whole frame means "go back"** — exactly how every hub on 1:3 is already built. The hub-bound edge (chosen by measured in-degree) stayed on the frame, 33 others were scoped onto named hotspots. Two corrections inside that pass: a scoped `AFTER_TIMEOUT` still fires by itself, so those became `ON_CLICK`; and a hub frame should carry no whole-frame click at all, so the two shells had theirs scoped. **Zero nondeterministic pairs remain.** The 13 duplicate-trigger nodes left all point at the *same* destination — benign.

**The missing Settings root — drawn.** `1344:7165` and eleven other S7 screens had no source because the root the family drills in from was never drawn (`1344:7162` described the model in prose and is retired). `1688:7195` is built from `SettingsTab.tsx:75-92` NAV verbatim — thirteen screens in three groups, each row's real title and subtitle — and **cloned from 638:2378** so the topbar, footer and ground are the same objects the family already uses. All 13 rows wired, a door from the Site menu, a back-edge from all 15 S7 screens.

## Queued redraw waves — waves 1 and 3 DONE, wave 2 half done

Three waves, all Figma-lane, all with a named contract. **Code does not move on any of these families until its wave lands** (founder rule, goal point 2).

- **Wave 1 · History ×7 → 280** (G8 + C7 strip) — ✅ DRAWN 2026-09-02 after the live read (standalone editor, Site menu → Version history; the harness has no History surface). Contract measured in code: `PanelFrame.Header title="Version History"` (16:6, 44h) → `view-switcher` role=tablist with **two** tabs, each a label + helper — **Saves** / "Named milestones" and **Published** / "What's live" (`.view-tab` column, gap 1, padding 12/16, label 13px, helper 11px, 1px bottom border) → `FILTER_ROW` (gap 4, pt 8, px 12) with chips **"Milestones"** and **"All changes"** (h-6, rounded-full, 12px — NOT "Changes", which is what all seven boards draw) → `search-bar` (padding 8/12, 1px bottom border, placeholder "Search saves…" / "Search changes…"). Boards to redraw: 162:2, 163:64, 453:4031, 163:269, 163:167, 163:220, 949:4474 — each 360→280, `✕` at x332 replaced by the 16:6 header's own controls, Prune-note text 328→248, Version-row instances 360→280 (their time @236 and ⋯ @296 are MAX-constrained and reflow themselves). **Blocked on one live read**: the three chrome heights (view-switcher, filter row, search bar) must be measured in the running app, not computed from `history.css` — deriving them from CSS is the same arithmetic that has produced wrong boards here before.
- **Wave 2 · Inspector ×8 → 807 geometry** (C11/G10/G11) — ◐ mechanical half drawn 2026-09-02; header/tail/footer composition open. 34 pitch, 28 headers, 12-13 sections + footer, controls 160×28 gray-50 at x120, 32:2 as master with the collapsed tail. Redraw 159:123, 160:2/105/208/313/412, 1175:4841 and 189:2 (as the takeover). 159:2 already retired. Also G2: 32:2 gains the three missing header controls at the live x — pick @136, select-parent @160, Ask AI @184, bind @223, ⋯ @257.
- **Wave 3 · Layers ×9 → 1082 geometry** (C10) — ✅ DRAWN 2026-09-02, code conformed after. search flex-1 #f3f4f6 no border at x12, glyphs 13px #6b7280 at x228, rows pl12 pr8 gap6 icon 12, footer x12 11px #9ca3af; 1082:4589's hand-drawn 14/600 header replaced by the 16:6 instance. Redraw 143:2/179/119/237/295, 775:4130, 781:4217, 782:4260, 1171:4829. Fix-code under the same contract: row label 11/500 → 13/400 20px, row gap 1→0, padding-top 4→0 (layers-v2.css:29,231,236).
- **Also queued, contract already clear:** C3 — rebuild 154:132, 154:78, 154:26, 306:2217 on the 1333:7162 root (82h preview + nine named 52h rows from y126, not nine anonymous 32h rows from y44), then re-apply each state's own delta. C5 — 152:83 draws one selected token's detail with the three actions hanging off it.

## Cleared
**2026-09-02 · Figma-only pass (coordinator-figma).** Five C rows closed by writing Figma, no code touched:

- **C2** — 158:2 `Review panel · re-send-confirm` is the contract. `ApprovedCompareView.tsx` has no re-send control at all (only a mode toggle and 'Re-render the current site' at :173); `ReviewTab.tsx:839` owns the `role="alertdialog"` confirm, matching 11:229/11:259. 169:92 renamed `— SUPERSEDED 2026-09-02 by 158:2`; its two inbound hotspots 444:2386 and 807:6991 cut — Compare cannot prototype a door it does not have.
- **C9** — the full walk is 1469 edges, not the 28 the previous rewrite implied: LIVE 1368 · PARKED 59 (design-ahead/not-implemented, legitimate) · SUPERSEDED 24 · RETIRED 18. **20 of the 24 SUPERSEDED are internal to the Archive section** (an archived flow wiring to archived frames — coherent, left alone); only 4 leaked from a live board. 6 leaks re-pointed and read back: 921:4480 → 170:29, 171:69 → 32:2, 633:2384 + 963:4715 → 638:2378, 645:4351 → 1138:13436, 876:4549 → 32:2. 19 edges into RETIRED no-producer frames cut. Remaining: 876:4549's visible copy still says 'six missing profiles' (all built) — designer.
- **C12** — every board-internal defect fixed and read back. Icon rows 312w-in-280 → the code grid (`IconBrowserOverlay.tsx:116` grid-cols-5, `tw:size-10`): 248 rows at x16, 5 tiles, pad 1.6 gap 11.2, sections 104→152 and 152→200, 30 tiles kept, 0 overflow. Skeleton cells 136@16/128 (24px overlap) → **116 @ x16/x148** — the BLOCKERS proposal said x144; `gap-4` is 16 not 12, and the loaded row 144:27 already reads x0/x132 w116. Bulk bar → `gap-6 px-4`: Move to… 96→100, Delete 136→180, **Done added** at x234 (closes the B14 half). 453:3933 outer band stroke+r6 stripped (twin 144:7 carries them only on the inner box). Badges hug: 333:2344 132→172, 333:2346 106→146. 1314:21 CENTER→LEFT. Pills: empty → `0`, loading and load-error → count hidden (those states cannot know it) — also closes the pill half of G12.
- **C5 (⋯ half)** — `ClassesSection.tsx` (84 lines) renders each class as a plain `<li>` h-11 with `.name` + `used N×` and no menu, confirming `board:153-2-no-menu`. The 5 ⋯ glyphs at x256,y12 removed from 153:9/13/17/21/25.
- **C7 (title half)** — `HistoryTab.tsx:208` `title="Version History"` is the contract; all seven boards now read it (162:2 and 949:4474 were 'History', the other five 'Versions').

**Two traps this pass, both worth keeping** — a Figma reaction carries BOTH the deprecated `action` object and the current `actions[]` array, each with its own `destinationId`: setting only `action` is a **silent no-op**, and a verify that reads `r.action || r.actions[0]` reads the same stale half back and calls it fixed. Write `{actions:[…], trigger}`, read back `actions[0].destinationId`. And a `— CUT` name suffix belongs on a `hotspot/*` node (which exists only for that edge), never on a board that merely lost one dead reaction — it renamed four live boards as if they were cut, and the word RETIRED inside the suffix then made the next scan flag every inbound edge to them.

**New, found by this pass, not yet fixed:** 1314:21 (h40) and 1313:12 (h32) sit inside 22-tall `Search scope` rows — the row cannot show its own text; and `icons/arrows` 147:33 has no section header while `icons/recent` has one.


Moved out this round, with the ledger line that cleared it:

- old A1 · J-775:4053, J-781:4154, J-778:4102, J-774:4044 → renamed '— RETIRED 2026-09-02 (no producer)', read-back ok (co 09:09:02Z figma-batch-4; jobs.json done ×4). Code debt, not a blocker: the three zero-importer blocks (InsertStateBlocks.tsx:37,61; PagesStateBlocks.tsx:28) can now go.
- old A4 · J-183:31, J-183:43 → RETIRED (co 09:09:02Z; jobs.json done ×2). The 14:41 'loading · error' strike is still doc debt.
- old A5 · J-1138:13376, J-138:198, J-169:60 → RETIRED (co 09:09:02Z; jobs.json done ×3). The 14:163 'disabled Soon' strike is still doc debt.
- old C1 · scope chip cloned from 170:22 into AI·idle 170:2 at index 2 (y80, 'Scope: Page', new id 1625:7162) (co 09:12:19Z figma-batch-5; jobs.json J-170:2 'Figma C1 DONE'). Code half re-measures.
- old C4 · 1334:7162 hand-drawn header hidden, replaced by a 16:6 Panel-header instance 1627:7162 at index 0 (280×44, 'Brand', 11px) (co 09:13:42Z figma-batch-6; jobs.json 1333:7162 'Figma C4 DONE').
- old C6 · badges 333:2348/2362/2364/2366/2358 y60→84, all ABSOLUTE (co 09:09:02Z; jobs.json 306:2049/2232/2265/2298/2186 'Figma C6 badge y84'). 333:2360 (J-306:2217) was not in the batch — it rides on C3's redraw.
- old C8 · 184:35 → h32 r8, 184:36 → 14/Medium (co 09:09:02Z; jobs.json J-184:24 'Figma C8').
- old D4 · AI-draft tile 295:1984 hidden + renamed, holder HORIZONTAL, 2 visible tiles (co 09:12:19Z; jobs.json J-295:1972 'Figma D4 DONE').
- old D5 · 'Save backup' cloned from Keep-mine into the conflict footer 66:845 (x203-311 of 440, id 1625:7164) (co 09:12:19Z; jobs.json J-66:640 'Figma D5 DONE'). The attribution half (no lastEditedBy on the CONFLICT payload) → E5.
- old D7 · 807:8554/8555 (LINK header + chevron) hidden on the MEDIA profile board — a first pass hid everything below y392, corrected the same minute, stillHidden = exactly those two (co 09:13:42Z; jobs.json J-807:8521 'Figma D7 DONE').
- old D8 · 171:104 copy → per-step apply + Undo all (co 09:12:19Z; jobs.json J-171:67 'Figma D8 DONE').
- old D11 · 153:154 → 'Drop .json or .ts' (co 09:09:02Z; jobs.json J-153:120 'Figma D11 copy fixed').
- old D12 · 163:112 → 'Versions are created whenever you name one, or when a template is applied.' (co 09:09:02Z; jobs.json J-163:64 'Figma D12 sub-copy fixed'). G7 (the auto-checkpoint decision) stands.
- Review accent (vlr 09:58:04Z J-156:2, blocker D) · 11 nodes filled blue-600 #1c64f2 across the 12 Review boards → the accent #1A56DB (0 left); 157:55 quote 12/18 → 13/20 (co 10:04:41Z). 158:105's reds: the BOARD is right — the swap is code, ReviewTab.tsx:657-671 (fix).
- J-1175:4841 Figma half · board 280 → 300 like its inspector siblings, banner FILL 248 → 268, the 30-minute note wraps at 268, back hotspot 300, overflow 0, h 158 → 169 (co 09:46:08Z). Code half (InspectorEmptyState.tsx:48,51,131-145) still `fix`.
- Inspector shared chrome (vi 09:41:45Z ×5: header 53/48, strip 39/32, pills 26/22 12-500/11-400, the reach pill's white lost to inspector.css:128, section header 30/28, rows label-right 88+8 control x+107 h24 pitch 30 vs label-left x16 control x120 h28 pitch 34, footer 12/500 39h vs 11/400 13h) → fix batch 7 on inspector.css + InspectorTabContent.tsx: strip 6→5 (39→32), pills 22 11/400, reach pill bg in CSS, section header 6/16 (28), grid 88→96 label-left, .bdi-fld 24→28 (var(--bk-size-row-dense)), two-class cap on flowbite controls inside rows, footer 11/400; Gate 14 baseline 206→205, Gate 13 unchanged (co 09:51:50Z family:Inspector). **Fixed in code, re-measure pending** on 807:8342/8567/8412/8475/8614. Bind 32×32 (header 53) was not in the batch → F13.
- P1 false orphan modal (vlr 09:58:04Z J-157:2) · DETACHED on 3 of 6 plain loads while the pinned element WAS in the DOM (commentAnchors.ts:100 `c.pageId === activePageId` true for null/null; CommentLayer.tsx:229 scans 150ms after comments load, before the page mounts) — 'A comment lost its element' blocked the whole editor on a false claim. **Fixed in code, batch 9 (coordinator brief; no ledger line) — re-measure pending**; vm 10:23:37Z (3) still saw the dialog on load, so the re-measure decides. The 157:2 layout drift (105-125h row with dot + buttons vs the board's 64h) stays `fix`.
- P1 icon-picker back row (vm 10:21:31Z J-147:2) · the back Button measured 279×0 (flex-shrink 1 under a 2992px tw:grid sibling), a pointer click on 'Back to media grid' timed out twice, only a JS click closed it. **Fixed in code, batch 11 (coordinator brief; no ledger line) — re-measure pending.** grid-cols-6 overflow (34.5px tracks holding 40px cells, x73..326), no section headers, cell r4 stay `fix`; the 312-wide board rows → C12.
- P1 publish-history overflow (vh 05:42:45Z J-949:4474) · PublishHistory.tsx:69 tw:min-w-80 = 320 in a 279 panel — Live chip clipped, footer cut, rollback button under the canvas. **Fixed in code, batch 2 (coordinator brief; the ledger names batch 2 only for 433:2348's code half, co 08:30:05Z) — re-measure pending on an owned live site (F8).** jobs.json still carries the P1 text.
- fixture-versions autosave leak (vm 10:23:37Z (2): 30 rows 09:39:53–10:01:13Z; vh 05:42:45Z (1)) · the fixture cmrsur1fp… held 50 versions, 40 minted today by the three live verifiers — queried then deleted by id, 10 remain (co 10:18:38Z harness); PROTOCOL.md:74-77 now names the abort pattern (`saveProject|sites.update|siteVersions|publish|rollback|comments|reviews|assets|content|collections`). F8's fixture requirement stands.
- H-09 CI · run 33609481172: lint + tests green; Style parity 20/20 red on linux with diffs only in text-derived width/height (6.60938 vs 7px) → spec compares width/height within 1px, exact elsewhere (co 09:28:29Z; jobs.json done). Caveat kept: no CI run has yet produced a green conformance verdict.
- old C3 (144:7 double-bordered band) · confirmed gone in the 10:20Z payload — 'the band stroke reported 05:11Z is gone' (vm 10:20:07Z). Its twin 453:3933 is C12.

Cleared in earlier rounds (kept for the record): old C3 144:7 strokes 1→0 radius 6→0 (co 08:27:24Z figma-batch-1) · old A4 half J-295:1989/1994 RETIRED (co 08:27:24Z) · old D10 J-1345:7162 model picker RETIRED (co 08:27:24Z; 02:31 / 12:91 strike still open; its 24 prototype edges are C9) · old H4 17 SUPERSEDED top-level frames → Archive 957:4474, supersededStillTopLevel=0 (co 08:27:24Z); 52 nodes re-pointed, stillPointingAtSuperseded=0 (co 08:36:25Z) · old F6 dead frame 396:2338 archived with the 17 · old C7 half 152:147/154/161 rows 288→248, cards 116, thumbs FILL, overflow 0 (co 08:30:05Z; code half StarterGrid.tsx:39-108 still `fix`) · 433:2348 banners #3F83F8 → #ebf5ff + #1a56db@.30 + primary #1a56db (vh 05:42:45Z → co 08:30:05Z; code half in fix batch 2, re-measure) · old A10 half J-949:4474 CORRECTION: History · Published IS reachable on test-site-iter5 (vh 05:42:45Z) · H-08 drillHost (jobs.json done; probe.tsx:208 still tw:w-80 — a note in F4) · D-06 SmartSuggestions deleted d36c96f2 · Figma timeout (dup ran getInstancesAsync 05:20:09Z) · H-01 raw cache (8 files; but see F7) · H-02 fixture widths (probe.tsx:453-486 six hosts at 280; comments :67,:135,:204,:391 still say 320) · H-05 skeleton-row 264 (probe.html:9 body{margin:0}, co 05:17:46Z) · D-03 disabled opacity (co 05:31:01Z; DESIGN.md:192, 208-210) · Templates drawer side → D2.

## Unclassified

- J-1082:5004 (blocked:no-list-view), J-66:225 (unreachable-by-design-paid-openai-call) — `unbuildable`, empty evidence, no ledger line. Note for 66:225: vh 05:42:45Z J-433:2348 answered ai.milestoneSuggest at the route with sample copy, OpenAI never called — the same trick likely reaches an agent run; needs a verifier line before it becomes a row.
- J-817:4856, J-912:4520, J-807:8787, J-817:5006 — name tag only ([not-implemented]/[design-ahead]), no measurement, no ledger line.
- J-164:22 'drift-filter-note-position-BOARD-CONFLICT' (was C4 two rewrites ago) — a tag, no numbers, no ledger line; needs the two boards and the two positions named before it re-enters C.
- J-294:1976 authority 'founder:board-asks-product-to-lie-REDRAW', J-807:7000 'code-wins-syncing-claim-unbacked' — authority tags only, no ledger line, no copy quoted; jobs.json has 294:1976 `todo` and 807:7000 `verify`.
- Prototype wiring beyond C9 (7 NEW boards orphaned, 0 BACK actions, 66:441 duplicate AFTER_TIMEOUT, 784:4250→4326 no edge) — from the flow-audit memory, not the ledger; only the superseded-target half is ledgered (co 08:36:25Z). Needs a flow-audit lens line.
- stale-PRD lines (PRD-ch01/02/03/11/14/overview, req 11:21:00Z-11:21:50Z; plus 14:41 'loading · error', 14:59 'full-screen', 14:163 'disabled Soon', 02:31/12:91 'model picker', 06:35 '+Save selection', 11:164/01:19 v4 step ids, 09:7,11-14, 12:152) — doc debt, owner coordinator-code; 02:9-23 still says 320 drawers.
- P2 product defects wave 2 found that are plain fix-code, not blockers — listed so they are not lost in 143 `fix` rows: 1163:13695 Escape inside the media context menu closes the WHOLE fullpage manager (vm 10:22:27Z); 1082:4589 double-click rename fails on an unselected row (B11's cause, vlr); 157:169 every previous round prints 'Revoked' (B12, vlr); 775:4241 the Content skeleton lands with DATA rows −32px (F14, vc); 143:179 a #d1d5db square painted behind the type glyph on hidden rows (layers-v2.css:55, vlr); 143:119 the invalid-drop alert renders 61px from the drop (vlr); 1171:4829 settings popover anchored to the panel corner, raw shadow literal (Gate 12), Geist Mono header (DESIGN.md data-only) (vlr); 1177:4804 Enter on open runs the wrong palette command (jobs.json, NOT fixed); 156:2 internal author prints 'You · you' (vlr).
- Class-tag disagreement to reconcile at merge: jobs.json `blocker` says **E** where the ledger reads **A** for J-183:31/43 (now retired), J-1138:13376 (retired), J-775:4305, J-154:2, J-153:57, J-778:4102 (retired), J-306:2111/2136, J-1082:5004; `blocker` is EMPTY where the ledger names a class for J-775:4130 (A), J-781:4311 (A), J-306:2161/2080 (A), J-160:105 (F), J-158:57 (F), J-157:169 (B), J-142:2 (C) — this file follows the ledger. J-145:250 is DECIDE in the ledger and `fix` in jobs.json (G14). Ledger job id 'H-08' at bl 05:40:00Z is jobs.json **H-09** (CI); jobs.json H-08 is the drillHost.
- requirements `ts` 11:10-11:21Z on a file written 05:20Z — clock not UTC; do not sort by `ts`. verifier-inspector/-content/-layers-review stamp one `ts` per agent (09:41:45Z / 09:51:12Z / 09:58:04Z) — cite those by job id. jobs.json was regenerated at 10:16:54Z; counts above are from that file. PROTOCOL.md gained its lines 74-77 (the abort pattern) during this rewrite — the coordinator is live; re-read before merging.
