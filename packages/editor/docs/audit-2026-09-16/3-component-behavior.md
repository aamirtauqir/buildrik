# 3 · Component Behavior — Editor v3 · IA (`4418:45431`) — 2026-09-16

Auditor lens: every reusable control family — which states exist in the library (`🧩 Components` `1:2` + the page-local `LIBRARY · Clone-owned editor components` `4418:144789`), which are used on the v3 page, which flows need a state that is missing, which library states are dead, which raw frames are lookalikes of a component, and which instance overrides contradict their variant. Read-only throughout (no writes, no playback). All counts are page `4418:45431` only, **live boards** per the BRIEF rule, top-level instances (instances nested inside another instance are excluded unless stated), visible nodes only unless stated.

## 1. Scope

| What | How it was read |
|---|---|
| Library masters | `componentPropertyDefinitions` + variant anatomy (size, fill, stroke, radius, opacity, text style binding) of all 26 sets and 92 standalone components on `1:2`; the 4 page-local sets on `4418:144789` (Rail `4418:144790`, Topbar `4418:144989`, List row · indented `4418:144963`, Settings nav row `4418:144958`) |
| Instances | Page-wide census of 14,600 INSTANCE nodes → main component, `variantProperties`, reactions, size/fill/opacity overrides, visibility of ancestors; per-family drill-downs for Button (1,721 live visible), Topbar (414), Rail (415), Panel header (415), Input (147), Select (20), Toggle (56), Checkbox (128), row components (List row · indented 1,418, Layers tree row 411, List row 185, Version row 76, Comment row 73, Record row 4), Card / media 66, Icon / eye + lock 132 |
| Raw lookalikes | Two scans over 84,365 FRAME nodes: (a) name-based families (`btn/ chip/ tab/ seg/ pill/ toast menu item row input field footer badge switch radio dropdown Select tooltip Nav ·`), (b) shape-based button heuristic (horizontal auto-layout, 24–44 high, one text child, accent/outlined fill, radius ≠ 0/9999) |
| Behaviour | Raw `reactions` (trigger types, destinations, CONDITIONAL blocks) — page-wide trigger census: ON_CLICK 20,473 · ON_KEY_DOWN 121 · AFTER_TIMEOUT 58 · MOUSE_DOWN 7 · ON_PRESS 5 · **ON_HOVER 0** |
| Screenshots | Home `4418:81300`, Templates catalogue `4418:54134` (scratchpad `cb-shots/`) |
| Prior fixes honoured | A-v/B-v/C-v/D-v/S-systems/COORD/QA3 (2026-09-15) — nothing already fixed there is re-reported; where a prior request is still open (B X4 Panel header, C R2 Assets footers, A X-6 rail master) it is referenced, not duplicated |

**Not audited:** Archive pages and the Dashboard / * components (out of scope); site-content buttons inside canvas pages (exempt); Commerce PROPOSED drawers; visual hover/focus rendering (the prototype never models hover — see CB-25); nested-section liveness is approximate (the `Media QA` nested sections leaked ~30 boards into a few counts, flagged where relevant, ±2 %).

### Library state coverage vs page use (counts per family)

| Family (master) | Library states | Used on page (live top-level) | Dead in library | Raw lookalikes on page |
|---|---|---|---|---|
| Button `9:102` | Kind primary/secondary/ghost/destructive × Size sm 28/md 32 × State rest/hover/focus/disabled/loading (40) | 1,721 visible: rest 1,719 · disabled 2 · hover/focus/loading **0**; ghost 931 · primary 816 · secondary 182 · destructive 49 | 32 of 40 variants never instanced | 213 shape-detected raw buttons (196 wired) + 739 `btn/*` frames (incl. `btn/ai` 367, Assets header trio 150) + 23 `Button ·` frames |
| Input `10:16` | rest/focus/filled/error/disabled (240×42) | 147: rest 105 · filled 41; **108 overridden to 44 high** | focus/error/disabled 0 | `input` 3,542 (Inspector, 28 high, 4 widths) · `field` 443 · raw error inputs 26/30 high ×4 |
| Select `14:37` | rest/open/focus/disabled (180×36) | 20 rest; **19 overridden to 44 high** | open/focus/disabled 0 | `dropdown` 9 (28 high) · `Select · type` 4 (42) · `Select · Preview format` 2 (26) · Inspector `Family ▾` chips |
| Toggle `14:14` | off/on/focus/disabled (44×24) | 56 visible: on 47 · off 9 (61 incl. hidden) | focus 0; disabled 0 **and pixel-identical to off** | `switch` 28×16 ×5 · `switch/on` 32×18 ×1 |
| Checkbox `12:26` | rest/checked/indeterminate/focus/disabled | 128: rest 121 · checked 7 | indeterminate/focus/disabled 0 | `checkbox` 18×18 ×1 |
| Radio `14:20` | off/on/focus/disabled | **0** | all | `radio/Static`, `radio/From CMS` (`4428:149933/149936`) |
| Slider `92:30` | rest/dragging/disabled/focus | 340 rest | 3 of 4 | — |
| Badge `12:16` | neutral/success/warning/danger/pro | 87 (neutral 40 · success 39 · warning 5 · danger 2 · pro 1) | — | `badge` 140 (16-px thumb glyph badges, 20-px count badges) |
| Icon button `697:440` | rest/hover/focus/disabled | 0 top-level (only nested as Topbar `btn/more`) | hover/focus/disabled | Topbar `btn/notifications` raw, Inspector `⋯`, drawer `✕` glyphs, `btn/eyedropper` 28×28, `btn/pick|select-parent|bind` 22×22 |
| Panel header `2100:11651` | Icons expand-close (11 M UPPER) / close (14 SB) / refresh-close | 415: expand-close 240 · close 174 · refresh-close 1 | — | 19 header-less drawers (S-systems §6, still open) |
| Topbar `4418:144989` | Publish ready/disabled/anyway × Review open/none; swaps Presence, Save | 414: ready/open 407 · disabled/open 7 | anyway 0 · Review=none 0 live | — |
| Save status `697:461` | saved/saving/unsaved/conflict/offline/error | via Topbar: saved 383 · unsaved 21 · saving 2 · error 2 · conflict 1 | offline 0 | — |
| Presence `692:472` | solo/few/many/reconnecting/offline | solo 404 | 4 of 5 | — |
| Rail `4418:144790` | Active None/Add/Layers/Pages/Assets/CMS/Brand (items are raw frames inside the set) | 415: None 104 · Layers 141 · Brand 50 · CMS 38 · Pages 30 · Assets 28 · Add 24 | no hover/focus/tooltip/disabled per item | `tooltip/Add` board `4433:46540` (1 of 6 items) |
| Row `8:47` | Size ×5 × State rest/hover/selected/disabled/focus (25) | **0** | all 25 | — |
| List row · indented (`2142:11082`, master on **Archive · Editor v1 Clone**) | Depth 0–5 × rest/selected | 1,418 (84 wired) | — | page-local copy `4418:144963` = 0 instances |
| Layers tree row `243:6` | Chevron, Selected, Glyph swap | 411 (82 wired) | — | — |
| List row `232:6` | Icon/Count/Chevron booleans | 185 (69 wired) | — | `row/🕐 Recent`, `row/📁 …` Assets sidebar rows 50 each |
| Nav item `16:26` | rest/hover/active/focus | **0** | all | `Nav · *` 794 Settings rows (232×36) · ghost-Button nav rows 232×40 ×58 (Templates/Media) |
| Settings nav row `4418:144958` / `2041:19572` | Active Off/On | 0 live (30 in ARCHIVE) | all | the 794 raw `Nav · *` rows |
| Tooltip `14:43`, Empty state `17:18`, Progress row `17:23`, Card / site `17:9`, Format row `249:6`, Integrations row `257:6`, Drawer frame `19:46`, Modal frame `19:79`, Right panel frame `19:47`, Status dot `10:27`, Avatar `691:450` | — | **0 each** | entire component | `tooltip` ×3 raw; dialogs 100 % raw; `Status` 9 raw |
| Card / media `17:6` | Badge boolean | 66 (40 Media) | — | Add block cards `card 1/2/3` 197×100 ×2,485 · template cards raw |
| Comment row `17:40` · Version row `240:6` · Record row `240:14` | Author client/internal · Current · Published | 73 · 76 · 4 | — | — |
| **No component at all** | — | — | — | `chip/*` 7,254 (canvas toolbar) · `tab/*` 1,602 + `tabs` 393 · page tabs 1,586 on 397 boards · `segmented/tier` 354 + `seg/*` 749 · `row` 6,259 Inspector rows + `row/applies-to` 364 + `row/show-all` 360 · `selection label` 1,015 · status bar `Footer` 466 · Inspector `footer` 363 · menu items (5 anatomies) · `toast` 14 · `Crumb` 11 · `hotspot/*` 2,885 |

## 2. Findings (sorted by priority)

| ID | Issue | Category | Affected (node ids) | Current problem (observed) | Why it matters | Recommended fix | Priority |
|---|---|---|---|---|---|---|---|
| **CB-1** | Compact canvas toolbar has dead dropdowns and hidden chips — breakpoint switch and view overlays unreachable from the flow start | Missing state / dead control | 205 live boards with `Canvas toolbar · compact` 760×44 @y824 incl. Home `4418:81300` (`control/breakpoint selector` e.g. `4418:81363` › child, `control/view menu`); vs 189 boards with the expanded 760×72 @y796 toolbar | `control/breakpoint selector` ("Desktop ▾") wired **0/205**, `control/view menu` ("View ▾") wired **0/205**; `group/device` (W/D/T/M chips, wired) and `group/overlays` are **hidden** on all 205; no "Breakpoint menu open" / "View menu open" state exists anywhere | From Home — the flow start — a user cannot switch to Wide/Tablet/Mobile or toggle Snap/Grid/Rulers; the 384 chip/W retargets of pass 5 only work on the 189 expanded boards. Two anatomies for one control set also means two mental models | Owner picks one toolbar (compact is the v3 IA; expanded is the Clone-era). If compact: add two popover states (`STATE · Canvas · breakpoint menu` with W/D/T/M rows carrying the existing chip destinations; `STATE · Canvas · view menu` with the 6 overlay toggles) and wire the two dropdowns on all 205 boards (`OVERLAY`, click-outside CLOSE). If expanded: unhide `group/device`/`group/overlays` and hide the two dropdowns. Then make the toolbar one component (see CB-12) | **Critical** |
| **CB-2** | Disabled-looking buttons that are still wired (contradictory state reachable) | Override contradicts variant | `4418:86268` "Save record" (`State=disabled` → NAVIGATE `4418:86921`, board `Content · Menu items › Margherita · Price required`); `4418:176305` "Connect · coming soon" (`State=disabled` → NAVIGATE `4418:133885`, Integrations › Browse all); `4418:154630` "Apply v2 across site" (rest, opacity 0.4, wired) vs its twin `4418:154639` (0.4, unwired); `4418:99115` "Publish to production" (primary, fill `#E5E7EB`, wired) on `STATE · Publish · published (simulated)` `4418:99089` | A button drawn disabled navigates; the same label on sibling boards is inert | User learns "grey = inactive" then a grey button fires; on the Price-required board the disabled Save must not proceed past validation | Remove the reactions on the four nodes (or restore `State=rest` where the action is genuinely allowed — owner call for `154630`). Rule: `State=disabled` and opacity < 1 ⇒ no reaction; enforce in QA | **Critical** |
| **CB-3** | Button `Size` axis ignored — 40 % of instances carry a height override | Inconsistent sizing | 689 of 1,721 live Button instances: **538 at 44 px** (Assets organisation 129, Assets editing 110, Assets Flowbite 53, CMS · Media field 43, Settings 24, Templates 23, Media upload 23, import 15, Content completion 12, loose dialogs 12, Media 10, Assets selection 10), **64 at 40 px** (Templates sidebar 41, Assets editing 10, Media 7, Phase 4 6), 9 at 14/16/26 px. Example dialog `Assets · Move · Products selected` `4418:155713`: `Action / Choose another folder` 240×44, `Action / Cancel` 120×44, `Action / Move 2 assets` 210×44 stacked vertically | The library defines 28/32; the Assets/Settings/Templates families redraw every button at 44 with width overrides and ghost fill `#F9FAFB`; sizes 26/29/33/40/44 appear across the page (see CB-11) | Three button heights coexist in dialogs that the same user opens seconds apart (Replace Menu 28 vs Assets Move 44); the variant axis says nothing | Reset the 538+64 to `Size=md` (32) inside a right-aligned `foot` row (this is C-v R2 for the ~47 Assets dialogs, extended to the whole family); the 41 Templates ghost rows become `Nav item` instances (CB-5). If the owner wants a 44-px dialog button, add `Size=lg` to the master instead of per-instance overrides | High |
| **CB-4** | Fill/opacity overrides used to fake states the library already has (loading, disabled, secondary, selected) | Override contradicts variant | 319 rest-state fill overrides: fake **loading** `4418:97595` "Publishing…" (primary, fill `#E5E7EB`); fake **disabled** `4418:97814` "No new changes" (fill `#E5E7EB` + opacity 0.4), `4418:98275` (`#F3F4F6`), `4418:154505` "Save version" (0.4); **primary-as-secondary** ×22 in the Image editor (`Crop` `4418:149448/149538/149578/154280/154336/154380/154423/154467/154555`, `3:2` `4418:154540/157189/157233/157277/157321/157365`, `Done`/`Cancel` `4418:154170/154185/154205/154210/154216/159967/149319`); **secondary-as-primary** `4418:175365` "Browse all" (`Kind=secondary`, fill `#1D4ED8`); off-token blues `#1D4ED8` on Templates primaries (`4418:53033…56966`, 15+) and `#1C64F2` on Review primaries (`4418:115824`, `116077`); ghost+`#F9FAFB` on Media "Manage assets ↗" ×7 (`4418:59779…61473`) and on every Assets `Action /` ghost | The master has `State=loading`, `State=disabled`, `Kind=secondary`; instances override fills instead, so the same intent renders 3–4 ways and the accent drifts off `#1A56DB` | Users read three greys and two blues as different meanings; code built from these boards will hard-code hexes | Replace: `97595` → `State=loading` (label "Publishing…"), `97814/98275/154505/154639` → `State=disabled` (opacity 1), Image-editor ratio chips → a Segment (CB-19) and Done/Cancel → `Kind=secondary`/`ghost`, `175365` → `Kind=primary`, Templates/Review primaries → clear fill override (token `flowbite/blue/700`). Policy: no fill/opacity overrides on Button instances | High |
| **CB-5** | Library components with zero uses while raw lookalikes do their job | Lookalikes / dead library | `Nav item` `16:26` (0) vs 794 raw `Nav · *` Settings rows + 58 ghost-Button nav rows (Templates `4418:52996/53000/53109/53211…`, Media); `Radio` `14:20` (0) vs raw `radio/Static` `4428:149933` + `radio/From CMS` `4428:149936`; `Tooltip` `14:43` (0) vs raw `4433:46779`, `4418:101916`, `4612:45018`; `Toggle` 44×24 vs raw `switch` 28×16 `4418:84142/84146/84150/84154/84698` and 32×18 `4428:151374`; `Checkbox` vs raw `4418:145083`; `Select` vs raw `dropdown` `4418:64965…65427` (28 high), `Select · type` `4418:84668…84692` (42), `Select · Preview format` `4418:72749`, `168928` (26); `Input` error/focus vs raw error inputs `4418:86936`, `90105` (30 high), `91820`, `92031` (26 high, `#C81E1E`); `Row` `8:47` (0, 25 variants), `Empty state`, `Progress row`, `Card / site`, `Format row`, `Integrations row`, `Drawer frame`, `Modal frame`, `Right panel frame`, `Status dot` (0 each) | Every one of these controls exists twice: once as a master nobody instances, once hand-drawn at a different size | Each raw copy is a divergence point (three toggle sizes, four select heights, two radios) and the library reads as abandoned | Replace the raw ones with instances where the master is the right control (radio, tooltip, toggle, checkbox, dropdown → Select, error inputs → `Input State=error`, Settings/Templates nav rows → `Nav item` after resizing its master to 232×36); **retire** `Row`, `Empty state`, `Progress row`, `Card / site`, `Format row`, `Integrations row`, `Drawer frame`, `Modal frame`, `Right panel frame`, `Status dot`, `Settings nav row` (both copies) from the library, or move them to an ARCHIVE section on `1:2` | High |
| **CB-6** | Rail `Active` contradicts the open drawer | State contradiction | **64 boards** show the LAYERS drawer with `Active=None`: AI `4418:104454…107044` (15), Inspector `4418:107674…115173` (37), Assets `4418:150780/151046/151238`, Media `4418:160695/160961/162133`, Builder `4418:166009/166252/166495/166733/169143/169389`. **6 boards** show Layers active with another rail panel open: Brand `4418:49448/49685`, Templates `4418:57528/57760`, CMS `4418:87780/88015`. Non-rail drawers (History/Publish/Review/Components/Issues) are inconsistent: 44 boards keep `Active=Layers`, 95 use `None` | The rail is the only always-visible "where am I" cue; it disagrees with the panel title on ~115 of 415 boards | Set `Active` = the open rail panel on the 64+6 boards; adopt the majority rule for non-rail drawers (`None`, 95 boards) and apply to the 44 | High |
| **CB-7** | Menu item — five anatomies, no component | Inconsistent anatomy | canvas ⋯ menu `item/*` h30 w212 pad10 13 px (`4428:143743`); Assets context menu `mi/*` h44 w220 11 px (`4418:58923`); Pages row menu `mi/*` h32 **and** h29 w200 11 px (`4418:91603`, `91609`); Ecommerce `MenuItem/*` h24 w216 (`4418:145141`); breakpoint detail `menu item ·` h34 w320 (`5773:45433`); Site menu rows h24 w280 (`4418:126035`) | The same "≤ 8 actions on an object" pattern renders at 24/29/30/32/34/44 px with three label sizes; disabled items have no shared treatment (⌘K explains disabled rows, menus don't) | Menus are the highest-frequency secondary surface; height/label drift is visible board-to-board | One `Menu item` component (32 high, `ui/13 · row label`, slots: leading glyph, label, shortcut; booleans `destructive`, `disabled` + reason text) replacing all 6 anatomies; menu container = 8 radius + `elevation/popover` (already the rule) | High |
| **CB-8** | Toast — four anatomies, none matches the rule, none auto-dismisses | Inconsistent anatomy / missing state | 14 live toasts: dark `#111827` 36 px at (356,784) ×5 `4418:81751/81969/82188/82407/82626`; dark 36–37 px at (585–1163, 711–820) ×4 `4418:166794` (r6 `#1A1C21`), `4428:146058`, `4428:147549`, `4418:135549`; **light `#F3F4F6` 360×43/47 at (560,744)** ×3 `4612:45008`, `4762:55578`, `4428:148206` (47 px); Review-closed 248×90 two-line `4418:120564` inside a 280×102 wrapper `4418:120563`. AFTER_TIMEOUT on **0/14** boards; Undo on 7 | DESIGN-RULES says "dark pill 360×43 at (560,744)" but the only nodes at that size/spot are light; the dark ones are 36 px and float at five positions | A feedback surface that looks different per module trains nobody; toasts that never clear block the canvas in playback | One `Toast` component (360 wide, title + optional info line + `Undo` boolean, one anchor 560,744), 14 instances; add `AFTER_TIMEOUT 4000 → CLOSE/NAVIGATE back` on each toast board. **Owner decision:** dark (rule) or light (the three "correct-size" ones) | High |
| **CB-9** | Two live row components for the Layers panel, plus a third unused; eye/lock affordances inert; live master lives on an Archive page | Duplicated control / missing wiring / library hygiene | `List row · indented` (master `2142:11082` on *Archive · Editor v1 Clone*; page-local copy `4418:144963` has **0** instances) — 1,418 live, label-only, Depth × rest/selected; `Layers tree row` `243:6` — 411 live (chevron · glyph · name · eye · lock, `Selected` boolean, no Depth); `Row` `8:47` — 0. `Icon / eye` (64) and `Icon / lock` (68) on Layers rows: wired **0**, parents wired 0, although `STATE · Layers · locked` and `Layers · Heading dimmed` exist | The Layers tree is the most-instanced control on the page and is built from two components with different vocabularies (A-3), neither has hover/drag/hidden/locked as states, and the majority master is off-page | Hide/lock cannot be triggered from the panel; a library consumer cannot tell which row is canonical; the Archive page cannot be archived without breaking 1,418 instances | Keep `Layers tree row` (it has the anatomy), add the `Depth` axis its own description asks for and a `State` axis (rest/selected/dimmed/locked), swap the 1,418 `List row · indented` instances to it, retire both `List row · indented` copies and `Row`; wire eye → `Layers · Heading dimmed`, lock → `Layers · locked` on the base boards (`4418:81300`, `4418:79139`) | High |
| **CB-10** | Same destructive action drawn as raw blue primary on four boards and as a destructive instance on one | Lookalike + wrong kind | Templates Replace Home confirms: raw `btn/Replace page` 100×29 `#1A56DB` + raw `btn/Cancel` 61×29 on `4418:54279/54281`, `54292/54294`, `56211/56213`, `56243/56245` vs destructive Button instance `4762:55403` (B-v rebuild) and `4418:53220`; also raw red-outline `act/Delete` 280×40 ×~20 (`4418:59120`, `150481`, `150694`, `151657`, `151865`, `152073`, `152281`, `152697…154153`), raw `btn/Delete token` 120×32 red outline ×6 (`4418:169683`), raw `Button · primary destructive` 172×28 ×3 (`4418:76116`, `78305`), `Button · Try Again` red outline `4418:111883` | "Replace Home" (discards the page) is blue on 4 boards, red on 2; deletes in Assets use an outlined-red kind the library does not have | Colour is the only cue that an action is irreversible; it flips between boards of one flow | Replace the 8 raw Templates buttons with `Cancel` ghost + `Replace Home` destructive instances (copy the gate from `4762:55403`). **Owner decision:** add `Kind=destructive-secondary` (outlined red) to the master — ~27 raw buttons want it — or use ghost + red label | High |
| **CB-11** | Assets/Media header and action-row buttons are raw families (≈ 380 nodes) | Lookalikes | `btn/⭳ Import URL` 120×44, `btn/↑ Upload` 120×44, `btn/+ Add from stock` 152×44 on 50 boards each (`4418:58305/58307/58309` …); `act/Insert to canvas` 280×40, `act/Edit image` 136×29, `act/Rename` 136×29, `act/Replace across site…` 280×40, `act/Delete` 280×40 per asset-detail board; `Dialog actions` ×13; `btn/Cancel` 61×29 ×8 / 68×33 ×4 / 72×33 ×2; `btn/Send` 60×26 ×11; `btn/Save` 52×27; shape scan: 213 raw buttons at 27/29/32/33/40/44 px (Assets selection 65, Assets organisation 41, Media 17, Builder 16, Templates 11, Settings 10, Brand 9) | Six heights, two greys, one label size, none an instance | Every Assets board is a fork of the button system | Convert to Button instances (`Size=md` in a `foot` row for dialogs; `Size=sm` in panel headers); merge with CB-3 into one Assets-family pass (C-v R2 scope + the 150 header buttons) | High |
| **CB-12** | Canvas toolbar chips: 7,254 raw frames, active state encoded two ways | Lookalikes / inconsistent state | ~18 `chip/*` per board on 400 boards; device chips consistent (`#F3F4F6` active, 189 boards); `chip/Snap Guides (active)` = `#DBEBFF` on 172 boards but **`#FFFFFF` (invisible on the white bar) on 17**; Notifications filter chips built as `state/default` + `state/selected` frames + an **invisible Button instance at opacity 0 as hotspot** (`4418:140600/140606/140612/140618` inside `chip/All…`) | No chip component; the "active" look is per-board hand work | 17 boards show no active overlay; a Button instance used as an invisible hotspot is a misuse that hides in every override report | One `Canvas toolbar` component (Breakpoint axis W/D/T/M, six overlay booleans, zoom text) replaces ~7,000 nodes; or at minimum a `Chip` (rest/active) component. Rebuild the 4 Notifications chips as Chip instances with the reaction on the chip | Medium |
| **CB-13** | Tabs — three anatomies, no component, 1,071 unbound labels | Inconsistent anatomy | Inspector `tab/Style|Settings|Effects` 100×36 underline (357 rows, consistent: active blue text + 2-px underline, all texts 12 M **unbound**); Assets `tab/Library|Upload|From URL|Optimize` 57–71×23 ×15 each; Animation settings `tab/active` 68×25 blue **outline** (`4418:109848`, `109919`); CMS `tab/Records|Fields` ×7 | Same control, 36/25/23 px and underline vs outline | Users see three tab languages inside one Inspector column (Style tabs above, Animation tabs below) | One `Tab` component (36 high, rest/active, `ui/12 · small strong`), instances everywhere; bind the 1,071 labels | Medium |
| **CB-14** | Page tabs carry a hidden ✕ in every tab (including the "+" tab) | Dead anatomy | 397 boards · 1,586 tabs · ✕ text child hidden in **all 1,586** (`4418:81324`, `81328`, `81331`, `4430:141151` on Home) | Either pages can be closed (then no board shows it) or they cannot (then 1,586 dead nodes) | Clutter in every board; ambiguous product rule | **Owner decision:** if page tabs are not closable, delete the ✕ children (one script); if they are, design one `Page tab · hover` state showing it. Make the page tab a component (rest/active/changed-dot) | Medium |
| **CB-15** | Input / Select masters do not match their own usage; Inspector inputs are a separate raw system | Inconsistent sizing | Input master 240×42 (r8) — 108/147 instances overridden to **44** (Settings 66); Select master 180×36 — 19/20 overridden to **44**; Inspector `input` 3,542 raw at **28** high in four widths (82 ×2,076 · 74 ×690 · 172 ×355 · 104 ×345); raw error inputs at 26/30; DESIGN-RULES: inputs radius 4, rows 28/32 | Four input heights (26/28/30/42/44) and no shared focus/error state for the 3,542 Inspector controls | Focus/error/disabled exist only in the master; the boards that need them (rename, slug validation, Price required) hand-draw red strokes | Set Input and Select masters to one height (owner: 32 per rule or 44 per usage), add `Size=sm` 28 for the Inspector and make Inspector controls instances (Home's Fill/Padding/Gap rows first); then error/focus states come free | Medium |
| **CB-16** | Master-level state defects | Library anatomy | Toggle `14:14` `State=disabled` = `State=off` (fill `#E5E7EB`, opacity 1, no difference); Button `disabled` = grey fill **and** grey text **and** opacity 0.5 (triple dim; label `#9CA3AF@0.5` on `#E5E7EB@0.5`); Button `loading` = "Working…" at opacity 0.7, no spinner, width grows 66→86; Button `sm` label 13 M unbound while `md` is bound; Button radius 8 vs rule 6; Checkbox stroke 1.5 `#CCCCCC` (non-token, off-rule); Badge `warning` 22 px vs 20 | The states that do exist are either invisible (Toggle disabled), unreadable (Button disabled) or reflow the layout (loading) | A disabled toggle looks "off", a disabled button's label is illegible, loading shifts neighbours | On `1:2`: Toggle disabled → opacity 0.4 (match Checkbox/Icon button); Button disabled → drop the opacity, keep fill/text; loading → keep width, replace text with "…" glyph or a 12-px spinner slot; bind sm label to `ui/13 · row label medium`; owner picks radius 6 or updates the rule to 8; Checkbox stroke 1 `color/border`; Badge warning 20 | Medium |
| **CB-17** | Topbar master: mixed anatomy, double-dimmed Publish, three dead variants | Library anatomy / dead variants | `4418:144989`: `btn/notifications` raw FRAME 32×32 while `btn/more` is an `Icon button`; `Publish=disabled` sets `btn/publish State=disabled` **and** opacity 0.5 (master disabled already 0.5 → ≈ 0.25 effective) on 7 live boards + Viewer `4418:126059`; `Publish=anyway` 0 uses; `Review=none` 0 live; Presence `few/many/reconnecting/offline` 0; Save status `offline` 0; `btn/exit` `State=rest` but unwired on the two Saving boards (`4418:124730`, `125919`) | 6 Topbar variants for 2 in use; a quarter-opacity Publish; an inert rest-state Exit | Dead variants invite drift; the Saving boards show a clickable-looking Exit that does nothing | Collapse Topbar to `Publish ready/disabled` (+ boolean `Review pill`), make `btn/notifications` an `Icon button`, remove the opacity override, set Exit to `State=disabled` on the Saving boards; **owner:** remove Presence (collab is demo-only per CLAUDE.md) or keep `solo` only; add an offline shell STATE or drop `Save status=offline` | Medium |
| **CB-18** | Panel header: three variants, two title styles (B-v X4, still open) | Library anatomy | `expand-close` 11 M UPPERCASE (240 live), `close` 14 SB (174), `refresh-close` (1, in `Reuse site elements · Components` `4418:142142`); title case split 181 upper / 206 mixed page-wide; ⛶ dead on all (B-v) | One component, two title languages, one variant used once | Drawers flip between "INSERT" and "Insert" | One variant, title `ui/14 · panel title`, booleans `Expand`, `Refresh` (default off); fix on `1:2` then instances inherit | Medium |
| **CB-19** | Beginner/Pro and other segmented controls are raw; primary Buttons used as a ratio segment | Lookalikes | `segmented/tier` 354 boards (Beginner active 353 / Pro 1; consistent), `seg/Simple|Advanced` 5, `seg/Side by side|Overlay` 3, `seg/List` 3, `seg/Light|Dark` 2; Image editor ratio `3:2/1:1/16:9` = primary Buttons with grey fills (CB-4) | Six segment families, no component | The tier switch is on every Inspector board and is hand-drawn 354 times | One `Segment` component (2–3 options, `Active` axis), 112×25 → 28 high per grid | Medium |
| **CB-20** | Settings navigation rows raw on all 51 boards while a nav-row component exists | Lookalikes | 794 `Nav · *` rows (232×36; exactly 1 active/board; active fill `#EFF6FF`, text `#1D4ED8` off-token) + 794 `Navigation icon · *`; `Settings nav row` set 0 live uses; `Nav item` 0 | Consistent but not a component; the active text colour is not the accent token | 51 boards × 15 rows to touch for any nav change | Promote the raw row to the `Nav item` master (resize master to 232×36, rest/active only), bind active text to `flowbite/blue/700` | Medium |
| **CB-21** | Bulk select: folder rows ignore their children's selection (indeterminate never used) | Missing state (inferred requirement) | `STATE · Pages · bulk-select` `4418:94471` (and `96537`): `Row · Marketing` `4418:94483` rest, `Row · Legal` `4418:94501` rest while Home/Menu/Contact `4418:94489/94494/94497` checked; no header select-all | The library's `indeterminate` variant exists for exactly this | A folder that contains selected pages reads as unselected | Set folder checkboxes to `indeterminate` (or `checked` when all children are) on the two boards; add a select-all checkbox to the bulk bar if the owner wants one | Medium |
| **CB-22** | Badge: two anatomies (library 20-px pill vs 140 raw 16/20-px badges) | Lookalikes | raw `badge` 22×16/23×16/25×16 thumb-type glyphs (▶ ◆ Aa, 10 px text) in Media library boards `4418:58398…`, 23×20/24×20/26×20 count badges | Count badges and type badges hand-drawn beside 87 Badge instances | Two visual weights for "status" | Add `Size=sm` (16 px, glyph) to Badge or accept thumb glyphs as art; convert the ~80 count badges to `Badge Kind=neutral` | Low |
| **CB-23** | Status bar, selection label, Inspector footer, breadcrumb are raw on every board | Lookalikes (shell) | `Footer` 1440×32 ×466, `selection label` 86×20 ×951 + `selection · Text · Heading` 106×20 ×64, Inspector `footer` 300×40 ×363, `Crumb` 280×32 ×11 | Consistent today (A-v aligned them) but 1,800 hand copies | Any change = page-wide script | Low-cost components with text props (status bar: 3 slots; selection label: 1 slot) — do after the higher items | Low |
| **CB-24** | `btn/ai` raw pill on 367 boards, one destination | Lookalike | `btn/ai` 40×22 ×367, all → `4418:104454` | Same node everywhere, not an instance | — | One `AI` chip/Button instance (ghost sm with ✦ glyph) | Low |
| **CB-25** | Hover/focus/press are not modelled; 19 hover/focus variants are spec-only | Dead library states | ON_HOVER reactions page-wide: **0**; Button hover ×8 + focus ×8, Icon button hover/focus, Toggle/Checkbox/Slider focus, Select open/focus, Input focus, Nav item hover/focus, Row hover/focus — 0 instances; only three hover boards exist (`Canvas · hover · Hero`, `Add · Blocks · hover`, `Insert · element-hover`) | Fine for a click-through prototype, but it means the library's hover/focus variants can never be verified from the boards | Code will invent hover; the file cannot check it | Keep hover/focus in the masters as spec, mark them "spec-only" in the set descriptions, and do not require them on boards; consider collapsing `State=loading` into a boolean | Low |
| **CB-26** | Rail items have no tooltip/focus/disabled and one tooltip board for one item | Missing states | Rail set: items are raw frames (`rail/Add…rail/Brand`) with only the `Active` look; `tooltip/Add` board `4433:46540` reached only from a walkthrough launcher; Viewer board keeps the rail fully active | Icons-only navigation with labels under them, so tooltips are optional; but the one tooltip that exists is unreachable | — | **Owner decision:** drop the tooltip board (labels already exist) or design one `Rail · tooltip` component state; give the Viewer rail a disabled treatment if viewers cannot open editing panels | Low |
| **CB-27** | Hidden clutter: 333 empty-label Button instances and 259 hidden Buttons inside hidden ancestors | Clutter | e.g. `4418:53033/53056/53088` (320×44 primary, no children) inside hidden Templates preview cards; `4418:52993`, `53102` (Panel footer) | No visible effect; inflates every override count | Slows scripts and confuses instance reports | Delete the hidden preview cards' orphan buttons when the Templates boards are next touched | Low |

## 3. Strengths to protect

- **Button is the one control that is genuinely a system**: 1,721 live instances, 1,809 wired page-wide, `Cancel` ×150 and `‹ Back to canvas` ×79 use the same variant everywhere; `Kind=destructive` (49) is used only for destructive labels (0 false positives).
- **Topbar is a component on 414 shells** with Exit (412), Publish (406), notifications (411), ⋯ (411) and Review pill (414) wired; Save status carries real states (saving/unsaved/conflict/error) on the boards that need them; the Viewer shell disables Publish and keeps Exit.
- **Consistent raw families** (do not "fix" the look, only componentise): page tabs (397 boards, one active encoding, changed-dot on Menu), Inspector tabs (357 rows, one encoding), device chips (189 boards), Settings nav (51 boards, exactly one active), Beginner/Pro segment (354 boards), status bar / selection label after A-v.
- **Layers STATE coverage is broad** (26 designed states incl. loading/load-error/empty/no-results/invalid-drop/renaming/dragging/multi-select) — the missing piece is wiring (CB-9), not design.
- **Panel header on 415 drawers**, ✕ wired page-wide after pass 5; `Comment row`, `Version row`, `Record row`, `Card / media` are used as intended.
- **Assets family is internally consistent** (its own 44-px system, its own action rows) — consolidation should move it onto the library, not redesign it.

## 4. Owner decisions / ambiguities

1. **Toolbar generation** (CB-1): compact (v3, Home) with popover menus, or expanded chips. Everything else in CB-1/CB-12 follows from this.
2. **Toast colour** (CB-8): dark pill (rule text) or the light `#F3F4F6` 360×43 (the only nodes at the rule's size/position).
3. **Outlined destructive kind** (CB-10): add `Kind=destructive-secondary` or map the ~27 red-outline buttons to ghost + red label; and whether `Detach all` (×7, secondary — B-v aligned it) and `Discard` (`4428:145034`, secondary) count as destructive.
4. **Input/Select height** (CB-15): 32 (DESIGN-RULES rows) or 44 (108/147 + 19/20 instances say so).
5. **Button radius** 8 (master) vs 6 (DESIGN-RULES) — one of the two documents is wrong.
6. **Page tab ✕** (CB-14): closable or not.
7. **Rail `Active` for non-rail drawers** (CB-6): `None` (majority, 95) or keep the last rail panel (44).
8. **Presence / collab** (CB-17): remove from the Topbar (CLAUDE.md: collab is demo-only) or keep `solo` only.
9. **Layers row canonical component** (CB-9) — relates to A-3 vocabulary decision; and whether the Archive-page master `2142:11082` may be moved/retired.
10. **Hover/focus** (CB-25): keep as spec-only variants (recommended) or add hover states to boards.
11. Rail tooltip (CB-26), Radio vs Select for "Static / From CMS" (`4428:149540`).

## 5. UNVERIFIED notes

- No prototype playback was run; all wiring statements are from raw `reactions`. "Unwired" means no reaction on the node **or** its non-instance parent; reactions on grandparents were not counted (the Layers eye/lock check looked one level up only).
- Liveness follows the BRIEF heuristic; boards inside **nested** sections (e.g. `Media QA · Typed video and SVG insertion`) were counted live in the Button/raw scans (≈ 30 boards, ±2 % on the big counts) and excluded elsewhere.
- Rail-vs-drawer (CB-6) used the `Panel header` title text at x≈60; boards whose drawer has no `Panel header` (the 19 header-less drawers) are not in those counts.
- The "hidden ✕" in page tabs (CB-14) was read as `visible=false` on the text child; whether any board ever exposes it via a variant was not checked (no page-tab component exists).
- Colour readings use the paint's raw hex; nodes bound to tokens show the token's resolved value, so "off-token" claims (`#1D4ED8`, `#1C64F2`, `#EFF6FF`) mean "not the accent's hex", not "unbound".
- The empty-label Button instances (CB-27) have **zero children** in the Plugin API, which should be impossible for an instance of a variant with a text child; cause not investigated.
- One `use_figma` transport drop occurred (loading-state scan); retried once after 60 s and landed. Nothing was written to the file.
