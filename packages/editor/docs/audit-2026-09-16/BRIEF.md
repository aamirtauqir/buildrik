# Buildrick · Editor v3 · IA — deep product audit (2026-09-16)

File `g4GzQFqzNYz5sosz1QtZXC` · **target page: Editor v3 · IA = `4418:45431`** (confirmed with the owner in every previous pass; it is the source of truth). Read-only references: `Archive · Editor v1 Clone` `3397:13062`, `Archive · Editor v1 Clone 2` `3974:26562`, `🧩 Components` (main components). Product: Buildrick, a Webflow-style website editor for agencies; demo site "Bella Cucina" (restaurant). Users: designers building client sites (Beginner / Pro tiers), clients reviewing/approving.

## Your role
You are ONE specialist auditor. Audit the whole v3 page independently through your lens only, and write your findings to your own file (given in your task). You are **read-only**: never write to the Figma file (no `setReactionsAsync`, no property changes, no clones, no renames). Fixes are applied later by the coordinator after the Final Verification agent consolidates all reports.

## Method (mandatory)
- Load the `figma:figma-use` skill before any `use_figma` call. Tools: `mcp__plugin_figma_figma__use_figma`, `mcp__plugin_figma_figma__get_screenshot` (load both with one ToolSearch `select:` call).
- Every script starts with `const page = await figma.getNodeByIdAsync('4418:45431'); await figma.setCurrentPageAsync(page);`. One page switch per call.
- **Do not launch any browser, Playwright, or prototype playback.** The Mac restarts under that load. Judge behaviour from `node.reactions` (read raw: `reaction.actions[0].type === 'CONDITIONAL'` hides destinations in `conditionalBlocks[].actions`; SET_VARIABLE carries state) and from screenshots.
- Page map: the page is organised in SECTION nodes (Editor shell, Canvas, Inspector, Layers, Add/Insert, Brand, Templates, Components, CMS/Content, Pages, Media/Assets, Publish, History, Review, Settings, AI, Notifications, Commands, Commerce, Issues, Preview, v3 Phase sections, STATES index `4418:140114`, START HERE `4418:146476`). Live boards = top-level FRAMEs ≥ 240×180 whose name is not prefixed ARCHIVE/REFERENCE/PROPOSED/LIBRARY/AUDIT/CHANGELOG/QA/FRAGMENT/START HERE/VERIFY ·, and whose section is not parked (/ARCHIVE|REFERENCE|PROPOSED|LIBRARY|QA |AUDIT|CHANGELOG|Specification|analysis|Captions|decisions|fixture/) unless the frame name starts CURRENT DESIGN / STATE / STATES. ~760 live boards. `CURRENT DESIGN · …` = the live design; `STATE · …` = designed states launched from each section's `STATES · … · every designed state` board.
- Key anchors: Home `4418:81300` (flow start, Layers drawer open, Section · Hero selected); drawer-closed shell `4418:123573`; rail set `4418:144790` (Add · Layers · Pages · Assets · CMS · Brand); Pages `4418:90494`; Add `4418:99611`; Assets `4418:59771` / library `4418:58292`; CMS root `4428:140486` / table `4428:143182` / row sheet `4428:144760`; Brand Simple `4428:143804` / Advanced `4428:146876`; Templates catalogue `4418:54134` / replace mode `4428:149355`; Inspector Style `4428:141170` / Settings `4428:141642` / Effects `4428:142686` / Fill picker `4428:142922`; Publish `4418:97118` / gate `4418:120066`; History `4418:73545`; Review `4418:115784`; Settings `4418:128917`; AI `4418:104454`; ⌘K `4418:141220`; Notifications `4418:140492`; Site menu `4418:126034`; New page dialogs `4428:151059` (v3, from the + tab) and `4418:126300` (older, from Pages "Add page").
- Work by archetype: screenshot one board per family plus every board that diverges; read structure/reactions for the rest. Keep scripts light (scope to a section, ≤ 20 boards per call, type-guard `'characters' in n`, `'children' in n`). If a call times out or the transport drops, wait 60 s and retry once.
- Rules established earlier (do not re-litigate; use them as the yardstick): `DESIGN-RULES.md` in `../audit-2026-09-15/` (type scale, tokens, grid, dialog anatomy, interaction-pattern table). Previous findings and fixes: `../audit-2026-09-15/{A-v,B-v,C-v,D-v,S-systems-v,COORD-v,QA3-static}.md` — read the ones relevant to your lens so you do not re-report what is already fixed, and so you can verify whether earlier fixes actually resolved the user problem.
- Evidence: every finding cites node ids + the observed value (reaction destination, count, size, copy). Separate **observed defects** from **inferred requirements** and put consequential ambiguities under **Owner decisions** — never invent product rules.
- Bias of this audit: **remove, merge, simplify, reuse, consolidate.** Do not recommend adding features unless a flow cannot complete without them. Ask of every important control: does the user need it, why here, is this the right location, can it be removed / merged / globalized / made contextual, can the task take fewer steps?

## Finding format (one block per issue, in a markdown table or list — keep it scannable)
`ID` (your prefix + number) · **Issue name** · Category · Affected screen/feature (node ids) · Current problem (observed) · Why it is a problem (user impact) · Recommended fix (concrete: which nodes/boards change, what is removed/merged) · Priority: Critical / High / Medium / Low.

Priority rubric — Critical: the task cannot complete, contradictory state reachable, data loss risk, destructive action without confirmation. High: wrong location/pattern that costs the user steps or context, missing needed state, duplicated control that causes divergence. Medium: inconsistency or clutter with a clear fix. Low: polish.

## Report file structure
1. Scope (sections/boards actually read; anything not audited listed). 2. Findings (format above, sorted by priority). 3. Strengths to protect (things that work — so the redesign plan does not flatten them). 4. Owner decisions / ambiguities. 5. UNVERIFIED notes (anything you could not confirm from the file). Then return a ≤ 40-line summary.
