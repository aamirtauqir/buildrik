# Pass 5 — visual consistency · interaction patterns · IA · verified coverage (draft, numbers filled at the end)

## Kya kiya (method)
- Target confirm: sirf page **Editor v3 · IA** `4418:45431` par likha gaya; baqi pages (Archive Clone 1/2, Components, Editor v1) sirf reference ke liye parhi gayeen. Koi main component nahi chhera, koi board delete nahi hua (hide-only).
- 6 agents alag ownership ke saath: **Systems** (text styles, tokens, grid/radius/stroke, emoji→icons), **A** shell/canvas/inspector/layers, **B** brand/templates/add/components, **C** CMS/pages/assets, **D** publish/history/review/settings/AI; **Coordinator** (main) ne cross-module batches kiye; **QA3** ne read-only dobara check kiya; **playback harness** ne asal prototype viewer mein click kar ke verify kiya.
- Design rules pehle file se nikaale (DESIGN-RULES.md), phir sab ne unhi rules par fix kiya.

## Coverage kaise gina
- "Intended interaction" = ek designed control × trigger × navigation × destination (label|trigger|nav|dest group). Ek hi control ki copies (jaise rail/Add 393 shells par) ek designed connection hain → ek group, instances alag report.
- Pass sirf **observed** hai: NAVIGATE = viewer ka node-id badla; OVERLAY = screen badli, node-id wahi; CLOSE = base screen pixel-identical wapas; AFTER_TIMEOUT = auto-advance land hua; keys keyboard se. Sirf configured connection = pass nahi.
- Conditional branches: jo branch default variables par chala woh observed; doosri branch total mein rehti hai (state chahiye).

## Modules — kya mila, kya theek hua (summary; full tables in the artifact)
**Systems (S):** 56,526 UI text nodes scan; 1,267 text → local style bind (no visual change); 1,274 off-scale sizes snap (10→11 caption 809, 15 SB→16 heading, 17→14 body 294, 22→20 heading lg 81); ≥10,157 raw paints → Primitives/flowbite tokens (translucent paints opacity preserved); 3,481 off-grid paddings, 601 stray radii, 659 1.5-px strokes snapped; 172 colour emoji (👁🔒📁🗑) → library Icon instances. Report-only: 19 header-less drawers (9 History done by coordinator), 15 toasts off-spec (2 fixed), ~14k texts unbound only because line-height is AUTO (needs Type owner sign-off), ~480 near-miss Tailwind hexes (needs Variables owner).
**A (shell/canvas/inspector/layers, 109 boards):** canvas toolbar mispositioned on 7 shells (y715 → y740 760×72); AI pending toolbar clipped; 20 Layers-section headers on the old component → `Panel header` 2100:10963; selected element disagreed across Layers/canvas/Inspector/status on 8 boards (v3 Layers panel + v3 Inspector profile cloned in, Fill swatches corrected); Returning/Exit-saving/AI-pending Inspectors were Clone-era chip strips → v3 profile; breadcrumbs "› Editor" / "· Workspace viewer" → `Site › Page › Panel` on 7 boards; Exit · Editor / Exit · Workspace bare 560-px cards reached by NAVIGATE → 1440×900 "leaving the editor" pages (Workspace back = BACK); S3.4 responsive-viewport board (second breakpoint bar, dead chips) → `Canvas · breakpoints · Tablet` (768-px page); chip/W → Heading · Wide; STATE · no-selection contradiction fixed; dialog radii/widths/scrim aligned; Site menu popover r8 + elevation/popover.
**B (brand/templates/add/components, 149 boards):** 9 token dialogs had a 100-px blank band above the buttons → v3 footer; Components delete-confirm was a wireframe spec card used live → rebuilt 560 v3 card; 12 v1 backup/couldn't-apply dialogs → v3 foot rows; Templates catalogue saved-template text card → real 4th card (4-up grid, "all four"); Add family BLOCKS count 50→8 on 13 boards, MINE → SAVED COMPONENTS; Starters drill-in listed 6 different starters → v3 six, conditional back (Simple/Advanced); Manage saved master ×6 aligned; Reusable styles values overflow fixed; 21 expand-close ✕ wired.
**C (CMS/pages/assets, all 13 sections):** Pages bulk bar overlap; Delete "Menu"? emphasis inverted → ghost Cancel + red Delete; Image editor Resize rendered empty → body cloned; delete-field chain spacer; old row menu → v3 anatomy; breadcrumbs leaked state names on 14 boards; 13 Pages boards lacked the Structure toggle, 25 Structure links → real structure view 95789 (+ ‹ Pages, + Add page); 30 CMS dialogs → v3 footer row; 2 new states `STATE · CMS · Delete collection?` 4757:150118 and `Collection renamed` 4762:55480 wired from trigger + STATES launcher; Generate 4 pages sets pagesGenerated.
**D (publish/history/review/settings/AI, ≈250 boards):** Critical: Unsaved-settings dialog 165478 had white-on-white buttons (empty fills) on the guard every Settings "Back to canvas" hits → re-bound; 40 dialogs → v3 footer anatomy; Publish footers: reactions moved from the container to the button on 5 boards, error states got exits (Reconnect Vercel, Back to editor); Preview Done ×3 → Home; Compare side-by-side sized; Review 118896 aligned to 115784 with conditional Resolve buttons; raw drawer headers on Issues ×3 + History ×2 → Panel header; 35 drawer ✕ wired.
**Coordinator:** 142 dead drawer ✕ (both header variants) → drawer-closed shell; 45 GENERATED-group visibility bindings; 340 chip/W retargets off the Tablet board; 5 clipped lists made scrollable; 69 dialog boards bound to elevation/modal; 9 History drawers → Panel header; toasts/titles snapped; Exit cards elevation. Not applied (owner/limitation): overlay scrim property (read-only in the Plugin API), rail/Layers → Home (vocabulary preference), AI 820/280 shell rebuild.
