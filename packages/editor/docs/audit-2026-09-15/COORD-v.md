# Coordinator · pass 5 (visual consistency · interaction patterns · IA · coverage) — 2026-09-15

Page `4418:45431` only. Module reports in this folder: `S-systems-v.md`, `A-v.md`, `B-v.md`, `C-v.md`, `D-v.md`; QA: `QA3-static.md`; playback harness in `~/buildrik-cov/` (results `results-*.jsonl`, `journeys-results.json`). The session scratchpad was wiped by a machine reboot at 22:20; B/C/D/S/QA3 are reconstructed from the agents' final summaries + their §6 sections (A is complete).

## Cross-module batches applied by the coordinator (each read back in-call)

| # | Request (from) | What was done | Read-back |
|---|---|---|---|
| X1 | B: `Panel header` ✕ (`…;16:13`, Icons=expand-close) dead on 288/381 instances page-wide; D: ~440 outside module D | Sweep over every live board: every visible `;16:13` with no reaction → `NAVIGATE 4418:123573` ("Editor · Home / drawer closed"). 93 wired (two batches; the second timed out at 326 s but landed). | `dead: 0`, `wired → 123573: 147` |
| X1b | QA3: the `Icons=close` variant's ✕ is child `;2100:10962`, not `;16:13` — 48 live boards (the 21 Layers-section headers A swapped + STATE · Layers/Inspector boards) still had a dead drawer ✕ | Same sweep for `;2100:10962` → `NAVIGATE 4418:123573` (49 wired) | `stillDead: 0` |
| R1 | C: GENERATED · MENU ITEMS is 5 sibling nodes per Pages board; `pagesGenerated / menuItems` (`VariableID:4643:45304`, default false) is set by "Generate 4 pages" but nothing listened | `visible` of `rule/generated-top`, `row/generated-header`, `row/generated-sub`, `rule/generated-end`, `Generated pages · source and template` bound to the variable on `4418:90494, 93108, 93381, 93657, 93929, 94200, 164291, 164559, 165158` | 45/45 bound; group hidden until Generate sets the flag; Pages panel re-flows cleanly (screenshot) |
| X-2 | A: chip/W still → `4418:126653` (now the **Tablet** canvas board) on ~340 boards while the 12 v3 shells go to Heading · Wide `4418:166009` | Every `chip/W` reaction on a live board whose destination was `4418:126653` → `4418:166009` (5 batches) — one Wide destination page-wide, Tablet stays on chip/T | `stillOldOnLive: 0`, `chip/W → 166009: 384` |
| Q1 | QA3: `4418:95816` "+ Add page" dead on the structure view `4418:95789` | → `OVERLAY 4418:126300` (same target as its 19 sibling "Add page" rows; v3 dialog `4428:151059` vs `126300` stays an owner decision) | `1 → 4418:126300` |
| Q2 | QA3: `4418:145168` footer 128 px because spacer `4418:145177` was visible | Spacer hidden (B's token-dialog treatment) | footer 61 px |
| S1 | Harness: five drawer/dialog lists clip hotspots below the fold with scrolling off — Insert · elements-expanded `4418:100534` (26 rows), page-settings SEO `4418:132995`, Review anchored Home/Contact/Menu drawers `4418:172808/173069/173330` (Revoke / Re-send / Open review link = D's "reachable only by scrolling" gap) | `overflowDirection = VERTICAL` on the five frames | 5/5 |
| S2 | QA3/B: token dialogs and ~60 other dialog boards carried raw DROP_SHADOW effects instead of `elevation/*` | Every live dialog-sized board (≤ 1000×820) with a raw drop shadow and no effect style → `elevation/modal` (69 boards incl. the 9 token dialogs, Replace Menu `4428:149521`, backup step `4430:141135`; swatch picker `4428:149324` → popover class) | 69/69, 0 errors |
| S3 | Systems §6: toasts 360×47 on `4418:96537`, `4762:55480`; Templates dialog titles left 16 SB unbound after the B rebuild | Toasts `4612:45008`, `4762:55578` → 360×43 @ (560,744) (`4428:44400` no longer has one there); 12 titles `4418:57461…57524` bound to `ui/16 · heading` | read back |
| S4 | Systems §6: 19 drawers without a `Panel header` instance | The 9 History STATE drawers (`4418:74736, 75876, 76095, 76323, 76793, 77604, 77839, 78290, 78526`) got the instance (`4980:45412…45444`, "VERSIONS"/"HISTORY", ✕ carries the raw ✕'s destination), raw 48-px headers hidden. Pages · Rename ×2 (`4418:91805, 92016`, absolute panels), Activity `4418:140587` and the REFERENCE VARIANT / superseded panels (`107268, 120553, 140871`) left as reported — they need re-layout, not a swap | 9/9 + screenshot `4418:77604` |
| A-3-fix | QA3 D2: Exit cards `4762:55416/55427` had `elevation/popover` | → `elevation/modal`, radius 12; padding kept 0 because the header band / body / footer children carry their own spacing (a pad-24 attempt double-padded the card and was reverted) | read back + screenshot |
| X-6 | A: rail master `rail/Layers` → Home `4418:81300` instead of `4418:79139` | **Not applied.** `4418:79139` screenshot-checked: LAYERS header, v3 Inspector, tree with eye/lock — a valid Layers landing; the difference is row vocabulary (glyph + name vs Type · Name) = owner ambiguity A-3. Rail master untouched. | — |
| X2/X-7 | A/B/D: `overlayBackground` NONE on every OVERLAY dialog | **Figma limitation:** `overlayPositionType`, `overlayBackground`, `overlayBackgroundInteraction` are read-only in the Plugin API. The v3 dialogs carry an inline scrim (`#111827` @ 0.5); menus use a click-outside rect → CLOSE. Owner can set the overlay background by hand on the dialog masters. | — |
| D-1 | AI section boards use canvas 820 / inspector 280 (file 800/300), 15 boards | Not applied — structural rebuild; listed as a gap (inferred requirement). | — |
| D-5 | Client review "Add note" raw frame acting as a second primary | Not applied (minor). | — |

## Harness (independent playback QA)
Standalone headless Chrome (SwiftShader) with the owner's Figma session, `~/buildrik-cov/run3.mjs`: one representative hotspot per interaction group (label × trigger × navigation × destination), NAVIGATE proved by URL node-id, OVERLAY/SWAP by screen change with URL unchanged (tolerance-based image compare), CLOSE by return to the base screen, AFTER_TIMEOUT by waiting, keys via keyboard, hotspots below the fold via wheel scroll, SWAP-only dialogs via their opener chain. Machine limit: one browser at a time (three shards pushed the load to 490 and produced false failures). Lessons: the first click after a load is swallowed (focus), `scaling=min-zoom` fits width only (tall boards scroll), after `goBack` the URL updates before the frame re-renders, re-renders drift by ≤ 13 levels (pixel-identical compare is wrong), auto-advancing boards cannot be clicked.
