# Code Review Report V2.1 — new-editor-l2 Editor Module

**Date:** 2026-02-21
**Version:** V2.1 (corrects code_review_report_v2.md)
**Type:** Report correction — no code changes
**Scope:** `packages/new-editor-l2/src/` — static analysis + grep-based truth-table
**Evidence:** 42 tool uses, 115,424 tokens (prior background agent)
**Claim types used throughout:**

- `STATIC_CONFIRMED` — proven via grep/file read
- `RUNTIME_CONFIRMED` — proven via browser execution
- `LIKELY` — high-confidence logical deduction from static evidence
- `REVIEW_REQUIRED` — needs runtime or deeper investigation

---

## A) V2.1 Correction Summary

### What Was Corrected

| #   | Correction Type               | Summary                                                                                                                                                                               |
| --- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Static vs Runtime wording** | 11 "Confirmed" claims replaced with `STATIC_CONFIRMED` or `LIKELY`. No claim was `RUNTIME_CONFIRMED` — none were verified in a live browser.                                          |
| 2   | **Circular dependency scope** | "NO CIRCULAR DEPENDENCIES" verdict (overstated, unscoped) → "No Engine↔UI cycle found (STATIC, best-effort, engine→components direction only). Full project-wide scan not performed." |
| 3   | **Overlay count**             | 16 vs 17 mismatch resolved. 16 = renderable overlays mounted by CanvasOverlayGroup. 17 was incorrectly counting CanvasOverlayGroup itself as an overlay.                              |
| 4   | **Hook count note**           | 41 (V1 estimate) vs 56+ (V2 directory count) discrepancy clarified. Both are valid in different contexts; relationship explained.                                                     |
| 5   | **Listener leak threshold**   | Hard threshold "> 10 = leak" replaced with baseline/delta guidance.                                                                                                                   |
| 6   | **H section wording**         | "now CONFIRMED L2 WIRED" → "STATIC_CONFIRMED L2 wired"                                                                                                                                |
| 7   | **K section truth table**     | "Confirmed" column entries replaced with evidence-type labels                                                                                                                         |

### Why Corrections Were Needed

The V2 report was generated by a static analysis agent (grep + file reads). No browser was launched, no runtime events were observed. The word "Confirmed" in V2 implied a stronger verification standard than was actually performed. V2.1 makes the evidence level visible on every claim so a reader can trust the report without re-verifying what it says was already verified.

### Confidence Level Changes

| Area                        | V2 Confidence                             | V2.1 Confidence                                                     | Why                                                        |
| --------------------------- | ----------------------------------------- | ------------------------------------------------------------------- | ---------------------------------------------------------- |
| Alignment methods L0 status | "Confirmed L0 dead" (implied RUNTIME)     | `LIKELY L0 dead (STATIC)` — downgraded                              | Still needs 1 runtime call to confirm                      |
| Circular dependency check   | "NO CIRCULAR DEPENDENCIES" (full project) | "No Engine↔UI cycle (STATIC, partial direction)" — downgraded scope | madge not available; only 2 grep directions tested         |
| Everything else             | Unchanged                                 | Unchanged                                                           | Claims were already accurate; only label wording corrected |

### Original Claims Downgraded

Two claims were strength-downgraded (scope narrowed, not content changed):

1. **Alignment methods**: `STATIC: 0 UI grep hits` is strong evidence but not the same as `RUNTIME: called and observed to do nothing`. Label changed from "Confirmed L0 dead" → `LIKELY L0 dead (STATIC_CONFIRMED: 0 UI call sites in grep)`.

2. **Circular dependencies**: Checked only `engine → components` and `components → engine` directions via grep. Intra-UI cycles (e.g., `components/Canvas` → `components/Panels` → back) were not checked. Verdict narrowed from "NO CIRCULAR DEPENDENCIES" to "No Engine↔UI cycle found".

---

## B) Claim Accuracy Audit Table

| #   | Original V2 Wording                                                                 | Corrected V2.1 Wording                                                                                                                                                           | Evidence Type | Confidence | Reason                                                                |
| --- | ----------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ---------- | --------------------------------------------------------------------- |
| 1   | "**Confirmed 9**" (tab count)                                                       | "**STATIC_CONFIRMED 9** (3-source: tabs.ts, TabRouter.tsx, LeftRail.tsx)"                                                                                                        | Static        | HIGH       | Grep/file read — correct label needed                                 |
| 2   | "**Confirmed L2 wired** (ELEMENT_CREATED + ELEMENT_DUPLICATED)" for useElementFlash | "**STATIC_CONFIRMED L2 wired** (imported in AquibraStudio.tsx; subscribes to ELEMENT_CREATED, ELEMENT_DUPLICATED events)"                                                        | Static        | HIGH       | File read confirmed wiring; no browser run                            |
| 3   | "**Confirmed L0 dead** (8 methods, 0 UI calls in grep)" for alignment methods       | "**LIKELY L0 dead** (`STATIC_CONFIRMED`: grep of entire src/ returned 0 UI call sites for all 8 alignment method names)"                                                         | Static        | HIGH       | Static evidence is strong; label must reflect no runtime test         |
| 4   | "**Confirmed L2 wired** (StudioHeader + StudioModals)" for Collaboration            | "**STATIC_CONFIRMED L2 wired** (imported by StudioHeader and StudioModals; connected to useCollaboration hook)"                                                                  | Static        | HIGH       | Import chain confirmed by grep; no browser observation                |
| 5   | "**Confirmed L2 wired** (StudioModals)" for ConflictModal                           | "**STATIC_CONFIRMED L2 wired** (imported by StudioModals; triggered by sync conflict event)"                                                                                     | Static        | HIGH       | Same — import confirmed, runtime not observed                         |
| 6   | "**Confirmed L0 stub** (throws on every call)" for EmailService                     | "**STATIC_CONFIRMED L0 stub** (every method body calls `subscribeViaBackendProxy()` which throws `Error('Not implemented')`)"                                                    | Static        | HIGH       | File content explicitly stubs all methods                             |
| 7   | "**56+ hooks enumerated and categorized**" vs V1 "41 hooks"                         | "56+ hooks in current hooks/ directory (current count); V1 estimated 41 active hooks from Canvas.tsx call list. Both are valid in different contexts."                           | Static        | HIGH       | Relationship between the two counts was ambiguous in V2               |
| 8   | "**Confirmed all-barrel redirect**, no physical moves" for features/                | "**STATIC_CONFIRMED**: all 8 features/_.ts files read and confirmed as pure `export _ from` barrel redirects"                                                                    | Static        | HIGH       | Label cleanup only                                                    |
| 9   | "**0 circular dependencies confirmed**" in exec summary                             | "No Engine↔UI cycle found (`STATIC`, best-effort, engine→components direction only). Full project-wide scan not performed."                                                      | Static        | MEDIUM     | Scope overstated — only 2 directions checked, not full graph          |
| 10  | "VERDICT: NO CIRCULAR DEPENDENCIES" in Section D                                    | "STATIC VERDICT: No Engine↔UI cycle found. Scope: engine-imports-components + components-imports-engine directions only. Note: intra-UI cycles not checked (madge unavailable)." | Static        | MEDIUM     | Same — scope correction                                               |
| 11  | "now CONFIRMED L2 WIRED" for PresenceIndicators in Section H                        | "STATIC_CONFIRMED L2 wired (StudioHeader imports PresenceIndicators; collaboration hook wired)"                                                                                  | Static        | HIGH       | Label consistency                                                     |
| 12  | "If > 10, there is a listener leak" in Section L                                    | "Capture baseline listener count on first mount. Compare after repeated tab switches and remounts. Investigate unexpected growth above baseline."                                | Inferred      | N/A        | Hard threshold "> 10" not justified; baseline/delta is safer guidance |

---

## C) Count Reconciliation Table

### Overlay Count (16 vs 17)

| Conflicting Count | Where Found in V2                                  | Correct Count                      | Evidence                                                                                                       | Confidence | Notes                                                                   |
| ----------------- | -------------------------------------------------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------- |
| "16 overlays"     | Section B (line 105): "Mounts all **16** overlays" | **16** renderable overlays         | `CanvasOverlayGroup.tsx` description says "mounts all 16 overlays" — the 16 excludes CanvasOverlayGroup itself | HIGH       | CanvasOverlayGroup is the orchestrator/container, not itself an overlay |
| "17 overlays"     | Sections A, C, N                                   | **INCORRECT** for renderable count | Counting error — CanvasOverlayGroup was mistakenly counted as an overlay, giving 16+1=17                       | HIGH       | Remove from Sections A, C, N                                            |

**Resolution:** Use **16** for "number of renderable overlays". Use "CanvasOverlayGroup orchestrates 16 overlays" (not "17 overlays"). Update all sections to 16.

### Hook Count (41 vs 56+)

| Count   | Context             | Accuracy                | Evidence                                                                             | Notes                                                  |
| ------- | ------------------- | ----------------------- | ------------------------------------------------------------------------------------ | ------------------------------------------------------ |
| **41**  | V1 report estimate  | V1-era inventory        | V1 was counting hooks visible/called in Canvas.tsx call order at time of V1 analysis | This number may reflect an older state of the codebase |
| **56+** | V2 current analysis | Current directory count | Background agent enumerated all files in `hooks/` subdirectory                       | This is the authoritative current count                |

**Resolution:** The hook count discrepancy is not an error in V2 — V2 correctly uses 56+. The relationship to V1's 41 is: **V1 listed ~20 active hooks called from Canvas.tsx as its primary inventory; the total hooks/ directory has 56+ files**. V2's improvement column correctly notes "41 hooks estimate → 56+ hooks enumerated". This is clarified in V2.1 with the note below.

> **Note added in V2.1:** The V1 count of "41 hooks" referred to the agent's initial inventory of `hooks/` directory files during V1 analysis. The V2 count of "56+" is the current hooks/ directory count from the background agent that read the full directory tree. The 20 hooks in Canvas.tsx's call order are a _subset_ of the 56+ total — the rest are called from other components (ProInspector, LeftSidebar, etc.) or are helper sub-hooks called internally.

### Tab Count

| Count           | Status             | Evidence                                                                                                                                           |
| --------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 9               | `STATIC_CONFIRMED` | 3-source reconciliation: `constants/tabs.ts` (9 entries in GROUPED_TABS_CONFIG), `TabRouter.tsx` (9 switch cases), `LeftRail.tsx` (9 icon buttons) |
| 10 (V1 mention) | Superseded         | Was a legacy estimate before components/media tab naming was settled                                                                               |

**Resolution:** 9 tabs. All V2.1 sections use 9 consistently. ✅

---

## D) Corrected Scope Notes

### File Inventory: "Key Files" Not "All Files"

Section B is titled "File Inventory (Key Files)" — this is **correct** in V2. No rename needed.

The inventory covers ~35 key files out of 875 code files in the project. It covers:

- All 10 `Editor/` shell files (complete coverage)
- All 4 `Layout/` domain files (complete coverage)
- All 12 `LeftSidebar/tabs/` tab files (complete coverage)
- 11 key `Canvas/` files (controls, 2 hooks, 1 overlay orchestrator — not all 56+ hooks)
- 9 key `ProInspector/` files (index, 3 tabs, 2 hooks, 2 config, 1 sections group)
- 11 key `Engine/` files (not all 28 managers)

**What is NOT in the inventory:** The 56+ canvas hooks, the 14+ inspector sections, all engine utility files, all block components, all form components, all utils/parsers, services other than EmailService.

### Circular Dependency Analysis Scope

**What was checked:**

1. `grep "from.*components"` in `engine/` → 0 results → engine does not import UI components ✅
2. `grep "from.*engine"` in `components/` → 84 results → components import engine as expected ✅

**What was NOT checked:**

- Cycles within `components/` (e.g., `Canvas` → `Panels` → back to `Canvas`)
- Cycles within `engine/` subdirectories
- Cycles involving `utils/`, `types/`, `constants/`
- Full project dependency graph (would require madge or dependency-cruiser — unavailable)

**Honest verdict:** Best-effort static check, engine↔UI direction only. Not a full project-wide cycle scan.

### Runtime Verification Status

**Nothing in this report is `RUNTIME_CONFIRMED`.** All verification was static (file reads + grep). Runtime verification requires:

- A running browser with the editor loaded
- DevTools open to observe events and listener counts
- Intentional UI interactions to trigger code paths

The Section L Runtime Verification Checklist remains valid as the _plan_ for runtime verification — it was never executed.

---

## E) Corrected V2.1 Report (Clean Final Version)

---

# Code Review Report V2.1 — new-editor-l2 Editor Module (Full Report)

**Date:** 2026-02-21
**Version:** V2.1 (corrects V2)
**Scope:** `packages/new-editor-l2/src/` — static analysis + grep-based truth-table
**Evidence:** 42 tool uses, 115,424 tokens (background agent)
**Claim labels:**

- `STATIC_CONFIRMED` — proven via grep/file read
- `RUNTIME_CONFIRMED` — proven via live browser (none in this report)
- `LIKELY` — strong deduction from static evidence, not runtime-verified
- `REVIEW_REQUIRED` — needs runtime or deeper investigation

---

### A.1) Executive Summary

#### What the module does

`new-editor-l2` is a **visual web page builder** shipped as an NPM package (`@aquibra/editor`). The module exports `AquibraStudio` (the full React editor) and `Composer` (the engine for headless use). Users build multi-page websites via drag-and-drop, edit CSS in an inspector panel, manage a page tree, and export clean HTML/CSS.

#### Why it feels complex

1. **56+ Canvas hooks** — canvas behavior is decomposed into ~20 active hook calls in Canvas.tsx alone. Each hook is well-scoped, but the sheer count creates a high onboarding cost.
2. **`features/` is a navigation trap** — the folder structure _promises_ domain-driven architecture but every `features/*.ts` is an empty barrel that re-exports from `components/`. No files have been physically moved.
3. **16 canvas overlays** — the overlay system is powerful but has significant surface area to understand before making changes near the canvas.
4. **28 engine managers in one orchestrator** — `Composer.ts` instantiates all managers. Reading it requires understanding 28 classes, each in a separate file.

#### What V2 added over V1 (V2.1 corrected labels)

| Area                  | V1                                 | V2.1 Status                                                                                     | Evidence Level |
| --------------------- | ---------------------------------- | ----------------------------------------------------------------------------------------------- | -------------- |
| Tab count             | Estimated 9–10                     | **STATIC_CONFIRMED 9** (3-source reconciliation)                                                | Static         |
| useElementFlash       | Marked REVIEW                      | **STATIC_CONFIRMED L2 wired** (ELEMENT_CREATED + ELEMENT_DUPLICATED)                            | Static         |
| Alignment methods     | Marked REVIEW                      | **LIKELY L0 dead** (STATIC: 8 methods, 0 UI call sites in grep)                                 | Static         |
| Collaboration UI      | Marked REVIEW (L1 suspected)       | **STATIC_CONFIRMED L2 wired** (StudioHeader + StudioModals)                                     | Static         |
| ConflictModal         | Marked REVIEW                      | **STATIC_CONFIRMED L2 wired** (StudioModals)                                                    | Static         |
| EmailService          | Suspected stub                     | **STATIC_CONFIRMED L0 stub** (throws on every call)                                             | Static         |
| Canvas hooks          | "41 hooks" (V1 directory estimate) | **56+ hooks** enumerated and categorized (current directory count)                              | Static         |
| ProInspector          | 3-tab description                  | **14+ sections + config-driven evaluator** confirmed                                            | Static         |
| features/ directory   | Not analyzed                       | **STATIC_CONFIRMED**: all-barrel redirect, no physical moves                                    | Static         |
| Circular dependencies | Not checked                        | **No Engine↔UI cycle found** (STATIC, engine→components direction only; full graph not checked) | Static         |
| storageMigration      | Marked uncertain                   | **STATIC_CONFIRMED valid** (aquibra- → aqb- prefix, 15 keys, completion flag)                   | Static         |

#### Main structural issues

1. `features/` barrel redirect pattern is a half-done migration creating a misleading folder structure
2. `SelectionManager.ts` alignment/distribution methods (8 functions, ~200 lines) — LIKELY L0 dead code (STATIC: 0 UI call sites)
3. `engine/integrations/EmailService.ts` — STATIC_CONFIRMED L0 stub, always throws
4. Canvas hooks at 56+ create high cognitive load. No over-splitting found — each hook is focused — but hook _ordering dependencies_ need runtime verification
5. `LeftRail.css` at 510 lines is the only CSS file approaching the 500-line limit

#### Main runtime risks (all REVIEW_REQUIRED — not yet verified)

1. Hook initialization order in Canvas.tsx — 20 hooks called sequentially; if any earlier hook's state is used by a later hook before it initializes, subtle bugs arise `[REVIEW_REQUIRED]`
2. Event listener accumulation — 56+ hooks each potentially subscribing to Composer events; any hook that fails to clean up in a `useEffect` return will cause memory leaks and double-triggers `[REVIEW_REQUIRED]`
3. Auto-save race condition — `useComposerInit` debounces auto-save at 1500ms; if a user closes the tab within 1500ms of a change, the save may not fire `[LIKELY]`

---

### B.1) File Inventory (Key Files)

> **Scope note:** ~35 key files out of 875 total code files. Complete coverage for Editor shell, Layout, and Sidebar tabs. Partial coverage for Canvas (2 of 56+ hooks shown) and Engine (11 of 28 managers shown).

#### Editor Shell Domain

| File                                            | Type                 | Label        | Lines | Purpose                                      | Confidence |
| ----------------------------------------------- | -------------------- | ------------ | ----- | -------------------------------------------- | ---------- |
| `components/Editor/AquibraStudio.tsx`           | UI orchestrator      | `CORE_FLOW`  | 348   | Main shell, error boundary, hook wiring      | HIGH       |
| `components/Editor/StudioPanels.tsx`            | UI layout            | `CORE_FLOW`  | 648   | 3-column layout, prop distribution           | HIGH       |
| `components/Editor/StudioHeader.tsx`            | UI component         | `CORE_FLOW`  | —     | Top bar, undo/redo, collaboration display    | HIGH       |
| `components/Editor/StudioModals.tsx`            | UI component         | `CORE_FLOW`  | —     | Modal portal (export, conflict, etc.)        | HIGH       |
| `components/Editor/PageTabBar.tsx`              | UI component         | `SUPPORTING` | —     | Page tabs below canvas                       | HIGH       |
| `components/Editor/hooks/useComposerInit.ts`    | Hook (engine bridge) | `CORE_FLOW`  | 220   | Composer bootstrap, event binding, auto-save | HIGH       |
| `components/Editor/hooks/useStudioState.ts`     | Hook (UI state)      | `CORE_FLOW`  | 426   | Panel/tab state, localStorage persistence    | HIGH       |
| `components/Editor/hooks/useStudioHandlers.ts`  | Hook (action bridge) | `CORE_FLOW`  | —     | Block click, template apply, canvas handlers | HIGH       |
| `components/Editor/hooks/useStudioModals.ts`    | Hook (UI state)      | `CORE_FLOW`  | —     | Modal open/close state                       | HIGH       |
| `components/Editor/hooks/useHistoryFeedback.ts` | Hook (feedback)      | `SUPPORTING` | —     | Undo/redo toast notifications                | HIGH       |

#### Layout Domain

| File                                | Type         | Label        | Lines | Purpose                                      | Confidence |
| ----------------------------------- | ------------ | ------------ | ----- | -------------------------------------------- | ---------- |
| `components/Layout/LayoutShell.tsx` | UI layout    | `CORE_FLOW`  | 252   | CSS Grid 4-column container + WCAG skip link | HIGH       |
| `components/Layout/LeftRail.tsx`    | UI component | `CORE_FLOW`  | —     | 9-icon tab strip (top 5 + bottom 4)          | HIGH       |
| `components/Layout/LeftRail.css`    | CSS          | `CORE_FLOW`  | 510   | Rail visual styling (⚠️ near 500-line limit) | HIGH       |
| `components/Layout/DrawerPanel.tsx` | UI component | `SUPPORTING` | —     | Collapsible side drawer wrapper              | MEDIUM     |

#### Sidebar Domain

| File                                                     | Type            | Label       | Lines | Purpose                                    | Confidence |
| -------------------------------------------------------- | --------------- | ----------- | ----- | ------------------------------------------ | ---------- |
| `components/Panels/LeftSidebar/index.tsx`                | UI orchestrator | `CORE_FLOW` | 200   | Tab host + keyboard shortcuts (A/Z/P/D...) | HIGH       |
| `components/Panels/LeftSidebar/TabRouter.tsx`            | Router          | `CORE_FLOW` | 106   | Switch/case → 9 lazy-loaded tab components | HIGH       |
| `components/Panels/LeftSidebar/tabs/BuildTab.tsx`        | UI tab          | `CORE_FLOW` | 504   | Add elements, templates, drill-in          | HIGH       |
| `components/Panels/LeftSidebar/tabs/LayersTab.tsx`       | UI tab          | `CORE_FLOW` | —     | DOM tree hierarchy                         | HIGH       |
| `components/Panels/LeftSidebar/tabs/PagesTab.tsx`        | UI tab          | `CORE_FLOW` | 197   | Page list orchestrator (split from 772)    | HIGH       |
| `components/Panels/LeftSidebar/tabs/ComponentsTab.tsx`   | UI tab          | `CORE_FLOW` | 340   | Component library orchestrator             | HIGH       |
| `components/Panels/LeftSidebar/tabs/MediaTab.tsx`        | UI tab          | `CORE_FLOW` | 395   | Images, videos, fonts                      | HIGH       |
| `components/Panels/LeftSidebar/tabs/DesignSystemTab.tsx` | UI tab          | `CORE_FLOW` | 351   | Global tokens                              | HIGH       |
| `components/Panels/LeftSidebar/tabs/SettingsTab.tsx`     | UI tab          | `CORE_FLOW` | —     | Site config, SEO                           | HIGH       |
| `components/Panels/LeftSidebar/tabs/PublishTab.tsx`      | UI tab          | `CORE_FLOW` | 494   | Deploy flow                                | HIGH       |
| `components/Panels/LeftSidebar/tabs/HistoryTab.tsx`      | UI tab          | `CORE_FLOW` | 243   | Revision history                           | HIGH       |
| `components/Panels/LeftSidebar/tabs/ElementsTab.tsx`     | UI tab          | `CORE_FLOW` | 176   | Block picker (split from 666)              | HIGH       |

#### Canvas Domain

| File                                                     | Type                 | Label        | Lines  | Purpose                                               | Confidence |
| -------------------------------------------------------- | -------------------- | ------------ | ------ | ----------------------------------------------------- | ---------- |
| `components/Canvas/Canvas.tsx`                           | UI orchestrator      | `CORE_FLOW`  | 477    | Canvas entry — 20 hook calls, overlay group           | HIGH       |
| `components/Canvas/CanvasEmptyCTA.tsx`                   | UI component         | `SUPPORTING` | —      | Empty canvas "start building" prompt                  | HIGH       |
| `components/Canvas/CanvasFooterToolbar.tsx`              | UI component         | `SUPPORTING` | —      | Zoom controls + breakpoint switcher                   | HIGH       |
| `components/Canvas/controls/UnifiedSelectionToolbar.tsx` | UI component         | `CORE_FLOW`  | 637 ⚠️ | Selection toolbar with formatting, grouping           | HIGH       |
| `components/Canvas/controls/QuickActionsToolbar.tsx`     | UI component         | `CORE_FLOW`  | 530 ⚠️ | Quick action overlay for selected element             | HIGH       |
| `components/Canvas/controls/CommandPalette.tsx`          | UI component         | `SUPPORTING` | 449    | Cmd+K command palette                                 | HIGH       |
| `components/Canvas/hooks/useCanvasSync.ts`               | Hook (engine bridge) | `CORE_FLOW`  | 79     | Event → HTML re-render bridge (12 events)             | HIGH       |
| `components/Canvas/hooks/useComposerSelection.ts`        | Hook (engine bridge) | `CORE_FLOW`  | 178    | Canonical selection read hook                         | HIGH       |
| `components/Canvas/hooks/useCanvasKeyboard.ts`           | Hook                 | `CORE_FLOW`  | 620 ⚠️ | All canvas keyboard shortcuts                         | HIGH       |
| `components/Canvas/hooks/useCanvasContent.ts`            | Hook                 | `CORE_FLOW`  | —      | HTML content generation with selection injected       | HIGH       |
| `components/Canvas/overlays/CanvasOverlayGroup.tsx`      | UI orchestrator      | `CORE_FLOW`  | —      | Orchestrates 16 renderable overlays with prop routing | HIGH       |

#### Inspector Domain

| File                                                        | Type                 | Label        | Lines  | Purpose                                    | Confidence |
| ----------------------------------------------------------- | -------------------- | ------------ | ------ | ------------------------------------------ | ---------- |
| `components/Panels/ProInspector/index.tsx`                  | UI orchestrator      | `CORE_FLOW`  | 604 ⚠️ | Right panel (layout/style/advanced)        | HIGH       |
| `components/Panels/ProInspector/tabs/LayoutTab.tsx`         | UI tab               | `CORE_FLOW`  | —      | Flexbox, grid, positioning                 | HIGH       |
| `components/Panels/ProInspector/tabs/DesignTab.tsx`         | UI tab               | `CORE_FLOW`  | —      | Colors, fonts, effects                     | HIGH       |
| `components/Panels/ProInspector/tabs/SettingsTab.tsx`       | UI tab               | `CORE_FLOW`  | —      | Element properties, interactions           | HIGH       |
| `components/Panels/ProInspector/hooks/useInspectorState.ts` | Hook (UI state)      | `CORE_FLOW`  | —      | Tab/section/pseudo-state management        | HIGH       |
| `components/Panels/ProInspector/hooks/useStyleHandlers.ts`  | Hook (engine bridge) | `CORE_FLOW`  | —      | Style read/write via StyleEngine           | HIGH       |
| `components/Panels/ProInspector/config/contextEvaluator.ts` | Config               | `CORE_FLOW`  | —      | Determines sections shown per element type | HIGH       |
| `components/Panels/ProInspector/config/elementProfiles.ts`  | Config               | `SUPPORTING` | —      | Element type → inspector tab mapping       | HIGH       |
| `components/Panels/ProInspector/sections/*.tsx`             | UI sections (14)     | `CORE_FLOW`  | —      | Per-property section controls              | HIGH       |

#### Engine Domain

| File                                           | Type                | Label              | Lines  | Purpose                                             | Confidence |
| ---------------------------------------------- | ------------------- | ------------------ | ------ | --------------------------------------------------- | ---------- |
| `engine/Composer.ts`                           | Engine orchestrator | `CORE_FLOW`        | 694    | SSOT hub, 28 managers, EventEmitter base            | HIGH       |
| `engine/EventEmitter.ts`                       | Engine base         | `CORE_FLOW`        | —      | Typed pub/sub event bus                             | HIGH       |
| `engine/SelectionManager.ts`                   | Engine manager      | `CORE_FLOW`        | 523    | Selection SSOT + 8 LIKELY-L0 alignment methods      | HIGH       |
| `engine/HistoryManager.ts`                     | Engine manager      | `CORE_FLOW`        | 471    | JSON patch undo/redo, 500ms coalesce                | HIGH       |
| `engine/elements/ElementManager.ts`            | Engine manager      | `CORE_FLOW`        | 240    | Element CRUD, page management                       | HIGH       |
| `engine/styles/StyleEngine.ts`                 | Engine manager      | `CORE_FLOW`        | 672    | Style mutations, breakpoints                        | HIGH       |
| `engine/drag/DragManager.ts`                   | Engine manager      | `CORE_FLOW`        | 215    | Drag state machine (IDLE→PENDING→DRAGGING)          | HIGH       |
| `engine/export/ExportEngine.ts`                | Engine service      | `CORE_FLOW`        | 650    | HTML/CSS export                                     | HIGH       |
| `engine/commands/CommandCenter.ts`             | Engine registry     | `SUPPORTING`       | —      | Keyboard command registration                       | HIGH       |
| `engine/collaboration/CollaborationManager.ts` | Engine manager      | `SUPPORTING`       | 789 ⚠️ | OT collaboration (STATIC_CONFIRMED L2 wired)        | HIGH       |
| `engine/integrations/EmailService.ts`          | Service stub        | `ORPHAN_CANDIDATE` | 144    | **STATIC_CONFIRMED L0 STUB** — throws on every call | HIGH       |

#### Orphaned / Dead Code

| File                                               | Type          | Label              | Status                           |
| -------------------------------------------------- | ------------- | ------------------ | -------------------------------- |
| `components/surfaces/` (3 files)                   | Entire system | `ORPHAN_CANDIDATE` | **DELETED** this session         |
| `ProInspector/shared/PropertyRenderer.tsx`         | Registry      | `ORPHAN_CANDIDATE` | **DELETED** prior session        |
| `ProInspector/shared/controls/ControlRegistry.tsx` | Registry      | `ORPHAN_CANDIDATE` | **DELETED** prior session        |
| `engine/integrations/EmailService.ts`              | Stub          | `ORPHAN_CANDIDATE` | **Active** — STATIC_CONFIRMED L0 |

---

### C.1) Real Editor Flow Map (Step-by-Step)

```
STAGE 1: ENTRY
  src/index.ts               ← NPM barrel: exports AquibraStudio + Composer + types
  demo/main.tsx              ← Local demo harness
  Consumer code: <AquibraStudio options={...} onReady={...} />
  Confidence: HIGH | Evidence: STATIC_CONFIRMED

STAGE 2: EDITOR SHELL
  components/Editor/AquibraStudio.tsx
    ├── migrateStorageKeys()  ← STATIC_CONFIRMED: runs ONCE on module load (aquibra- → aqb-)
    ├── useComposerInit(config)
    │     → creates Composer instance
    │     → runs loadProject() from storage
    │     → binds 8 event category subscriptions
    │     → sets up 1500ms auto-save debounce
    ├── useStudioState()
    │     → reads panel state from localStorage 'aqb-panel-state'
    │     → provides: activeTab, panelOpen, drawerWidth
    ├── useStudioHandlers()   → block clicks, template apply, canvas events
    ├── useStudioModals()     → modal open/close booleans
    ├── useComposerSelection()← reads SelectionManager (canonical hook)
    ├── useHistoryFeedback()  → undo/redo toast
    ├── useElementFlash(composer) → CSS flash on ELEMENT_CREATED/DUPLICATED [STATIC_CONFIRMED]
    └── renders: StudioHeader + StudioPanels + StudioModals + AIAssistantBar
  Confidence: HIGH | Evidence: STATIC_CONFIRMED

STAGE 3: LAYOUT
  components/Editor/StudioPanels.tsx (648L, 20+ props distributed)
    └── components/Layout/LayoutShell.tsx
          → CSS Grid: LeftRail(56px) | LeftSidebar(280px) | Canvas(1fr) | Inspector(300px)
  Confidence: HIGH | Evidence: STATIC_CONFIRMED

STAGE 4: LEFT RAIL
  components/Layout/LeftRail.tsx
    → 9 icon buttons: 5 top (add/layers/pages/components/assets) + 4 bottom (design/settings/publish/history)
    → visual-only: emits tab change up to StudioPanels → useStudioState.setActiveTab()
    → rail does NOT own state
  Confidence: HIGH | Evidence: STATIC_CONFIRMED

STAGE 5: LEFT SIDEBAR / TAB ROUTING
  components/Panels/LeftSidebar/index.tsx (200L)
    → keyboard shortcuts: A=add, Z=layers, P=pages, C=components, M=assets, D=design, S=settings
    → renders: TabRouter with activeTab prop

  components/Panels/LeftSidebar/TabRouter.tsx (106L)
    → switch(activeTab) → React.lazy() for each of 9 tabs
    → code splitting: only active tab's bundle loads
  Confidence: HIGH | Evidence: STATIC_CONFIRMED (3-source reconciliation — 9 tabs)

  TAB LIST (STATIC_CONFIRMED):
  ┌──────────┬──────────┬────────┬────────────┬──────────┐
  │   add    │  layers  │ pages  │ components │  assets  │
  │  BUILD   │  LAYERS  │ PAGES  │ COMPONENTS │  MEDIA   │
  ├──────────┴──────────┴────────┴────────────┴──────────┤
  │  design  │ settings │publish │  history   │          │
  │  DESIGN  │ SETTINGS │PUBLISH │  HISTORY   │          │
  └──────────┴──────────┴────────┴────────────┘

STAGE 6: CANVAS (editing surface)
  components/Canvas/Canvas.tsx (477L)
    → 20 hook calls (see Section G.1 for full decomposition)
    → receives: composer, content HTML, device, zoom, selected states
    → renders: iframe with content + CanvasOverlayGroup (which mounts 16 overlays)
    → exposes ref: undo(), redo(), getHTML(), getCSS(), exportHTML()

  Canvas re-render path (driven by useCanvasSync.ts):
    Any of 12 Composer events fires
    → composer.elements.toHTML() called
    → content string updates
    → iframe src or srcdoc updates
    → browser re-renders the page DOM
  Confidence: HIGH | Evidence: STATIC_CONFIRMED

STAGE 7: RIGHT INSPECTOR (ProInspector)
  components/Panels/ProInspector/index.tsx (604L)
    → reads selectedElement via useComposerSelection()
    → contextEvaluator.ts determines which sections to show
    → useInspectorState.ts manages tab/section/pseudoState UI state
    → useStyleHandlers.ts reads+writes styles via StyleEngine
    → 3 tabs: Layout | Design | Settings
    → 14+ sections per tab (SpacingSection, TypographySection, FlexboxSection, etc.)
    → scroll position persisted per-element in localStorage
  Confidence: HIGH | Evidence: STATIC_CONFIRMED

STAGE 8: STORE / ENGINE
  engine/Composer.ts (694L) — the SSOT hub
    → All UI reads from Composer via event subscriptions
    → All UI writes go through Composer manager methods
    → Pattern: useEffect subscribe → composer.on(event, handler) → cleanup composer.off()
    → Mutation pattern: composer.styles.setStyle() → StyleEngine → JSON patch → HistoryManager.push()
  Confidence: HIGH | Evidence: STATIC_CONFIRMED

STAGE 9: SERVICES (external integrations)
  services/CloudSyncService.ts (594L)   — save/load, conflict detection  [STATIC_CONFIRMED L2]
  services/GoogleFontsService.ts         — Google Fonts API               [LIKELY L2 — REVIEW_REQUIRED]
  services/ai/AIServiceClient.ts         — AI generation                  [LIKELY L1 PARTIAL]
  engine/integrations/EmailService.ts    — Email marketing signup         [STATIC_CONFIRMED L0 STUB]
  Confidence: MEDIUM (services not individually verified except EmailService)
```

---

### D.1) Dependency & Coupling Report

#### Dependency Tool Availability

No `madge` or `dependency-cruiser` found in `node_modules/.bin/`. This section is **best-effort static analysis** via grep counts and manual import tracing. Full project-wide cycle detection was not performed.

#### Import Count Analysis (`STATIC_CONFIRMED`)

| Target             | Import Count | Coupling Level | Verdict                                                              |
| ------------------ | ------------ | -------------- | -------------------------------------------------------------------- |
| `engine/Composer`  | **84**       | Very High      | Expected — it's the SSOT hub. Every component needs the engine.      |
| `constants/tabs`   | **18**       | Moderate       | Acceptable — tab IDs are needed in rail, router, sidebar, test files |
| `constants/events` | **11**       | Moderate       | Acceptable — event constants imported only where subscribed          |
| `types/*`          | **200+**     | Very High      | Expected — TypeScript type files are always high-import              |
| `utils/*`          | **150+**     | High           | Acceptable — utility imports are diffuse and specific                |

#### Import Density per File (`STATIC_CONFIRMED`)

| File                | Import Lines | Assessment                                  |
| ------------------- | ------------ | ------------------------------------------- |
| `AquibraStudio.tsx` | 25           | High but justified — top-level orchestrator |
| `Composer.ts`       | 36           | High but justified — wires 28 managers      |
| `Canvas.tsx`        | 36           | High — 20 hooks + overlays + utilities      |

#### Circular Dependency Check (`STATIC, best-effort`)

```
Directions checked:
  Engine → Components: 0 imports found ✅ (engine does not import UI components)
  Components → Engine: 84 imports found ✅ (expected — components depend on engine)

Directions NOT checked:
  Components → Components (intra-UI cycles not verified)
  Engine → Engine (intra-engine cycles not verified)
  Utils/Constants internal cycles not verified

STATIC VERDICT: No Engine↔UI cycle found.
Scope: engine-imports-components + components-imports-engine directions only.
Note: Full project-wide cycle scan not performed — madge/dependency-cruiser unavailable.
Confidence: MEDIUM (partial check only)
```

#### Barrel Chain Analysis (`STATIC_CONFIRMED`)

```
features/editor/shell/index.ts
  → re-exports from components/Editor/index.ts
    → re-exports from AquibraStudio.tsx, StudioPanels.tsx, etc.

[Same pattern for: sidebar, inspector, rail, canvas, ai, templates, blocks]

Issue: features/ adds ONE extra indirection level.
A developer searching for "where is AquibraStudio?" may:
1. Find features/editor/shell/index.ts
2. Follow it to components/Editor/index.ts
3. Follow it to AquibraStudio.tsx
→ 3-hop chain vs 1-hop if features/ didn't exist.
```

#### Coupling Hotspots

1. **`Composer.ts`** — imported 84 times. Any rename/refactor cascades to 84 files. This is structural and necessary, but it means any change to Composer's public API is a major breaking change.
2. **`constants/events.ts`** — 90+ event constants. Any event rename cascades. This is the correct pattern (central registry), but the large count means it's a high-churn area when adding/renaming features.
3. **`Canvas.tsx`** — 36 imports + 20 hook calls. Most complex integration point in the UI layer.
4. **`LeftSidebar/TabRouter.tsx`** — Adding a new tab requires changes here + `constants/tabs.ts` + `LeftRail.tsx`. Triple-touch coupling is a mild smell.

---

### E.1) File-by-File Deep Analysis (Key Files)

#### `engine/Composer.ts` (694L)

**Responsibility:** Creates and wires all 28 managers. Exposes unified API: `loadProject()`, `exportHTML()`, `setZoom()`, `undo()`, `redo()`, `beginTransaction()`, `endTransaction()`.
**Key imports:** 28 manager files, EventEmitter, types, constants
**Issues:**

- 694 lines is near the limit. The file is a constructor-heavy orchestrator — justified at this size.
- `emailService` (EmailService.ts) is imported and initialized here. Since EmailService is STATIC_CONFIRMED L0 (stub), this import is dead weight.
  **Confidence:** HIGH

---

#### `components/Editor/hooks/useStudioState.ts` (426L)

**Responsibility:** Panel open/close, active tab, sidebar width — persisted to `localStorage('aqb-panel-state')`. Includes `migrateLegacyPanelState()`.
**Issues:**

- 426 lines is approaching limit.
- Multiple localStorage keys across the app: this file (`aqb-panel-state`), ProInspector (per-element section state), others. No centralized key registry.
  **Confidence:** HIGH

---

#### `engine/SelectionManager.ts` (523L)

**Responsibility:** Selection SSOT. Public API: `select()`, `addToSelection()`, `removeFromSelection()`, `clearSelection()`, `reselect()`, `getSelectedBounds()`, plus 8 alignment/distribution methods.
**Critical issue:** Lines 329–520 implement 8 alignment/distribution methods. **LIKELY L0 dead** (STATIC_CONFIRMED: grep of entire src/ returned 0 matches for any UI call site).
**Recommendation (analysis only):** Either add toolbar buttons, or delete the 8 methods to bring file below 500-line limit. Runtime verification needed before deletion.
**Confidence:** HIGH

---

#### `engine/integrations/EmailService.ts` (144L)

**Responsibility:** Stub for email marketing integration.
**Status:** STATIC_CONFIRMED L0 STUB. Every method body calls `subscribeViaBackendProxy()` which `throw new Error('Not implemented: POST /api/email/subscribe')`.
**Risk:** Any consumer calling `await composer.email.subscribe()` will throw an uncaught error.
**Confidence:** HIGH

---

#### `features/*/index.ts` (8 files)

**Responsibility:** All 8 files are barrel re-exports pointing to `components/`.
**Pattern:** `export * from '../../../components/Canvas/index'`
**Reality:** Physical files have NOT moved. The barrels create a 2-hop import chain.
**Confidence:** HIGH (STATIC_CONFIRMED — all 8 files read)

---

#### `utils/storageMigration.ts` (112L)

**Responsibility:** One-time migration from `aquibra-*` prefix to `aqb-*` prefix (15 keys). Uses `aqb-migration-v1-complete` flag to prevent re-run.
**Status:** STATIC_CONFIRMED VALID and necessary for users on older versions.
**Called by:** `AquibraStudio.tsx` at module load (synchronous)
**Confidence:** HIGH

---

### F.1) Function-by-Function Analysis (Key Functions)

#### `SelectionManager.select(element)`

- **Label:** `ACTIVE_LOGIC` | **Confidence:** HIGH | **Evidence:** STATIC_CONFIRMED

#### `SelectionManager.alignLeft()` (and 7 siblings)

- **Called by:** LIKELY nothing — STATIC_CONFIRMED: grep of entire src/ returned 0 UI call sites
- **Label:** `LIKELY_UNUSED` | **Confidence:** HIGH | **Evidence:** STATIC_CONFIRMED (0 grep hits) + REVIEW_REQUIRED (runtime not tested)

#### `useComposerInit` (hook)

- **Label:** `ACTIVE_LOGIC` | **Confidence:** HIGH | **Evidence:** STATIC_CONFIRMED (imported in AquibraStudio.tsx)

#### `useCanvasSync` (hook)

- **Events subscribed:** 12 Composer events (project:imported, project:loaded, project:changed, page:created/deleted/changed, element:created/deleted/duplicated/updated/moved/resized)
- **Label:** `ACTIVE_LOGIC` | **Confidence:** HIGH | **Evidence:** STATIC_CONFIRMED (file read)

#### `useComposerSelection` (hook)

- **Label:** `ACTIVE_LOGIC` | **Confidence:** HIGH | **Evidence:** STATIC_CONFIRMED (canonical hook, imported in ProInspector, AquibraStudio, Canvas overlays)

#### `useElementFlash` (hook)

- **Events:** EVENTS.ELEMENT_CREATED, EVENTS.ELEMENT_DUPLICATED
- **Mechanism:** Adds `aqb-element-flash` CSS class, waits 500ms, removes it. Uses `requestAnimationFrame + offsetWidth` reflow to restart animation.
- **Label:** `ACTIVE_LOGIC` | **Confidence:** HIGH | **Evidence:** STATIC_CONFIRMED (imported in AquibraStudio.tsx, file read confirmed subscribers)

#### `migrateStorageKeys()` (function)

- **Called by:** `AquibraStudio.tsx` at module load (synchronous, before first render)
- **Label:** `ACTIVE_LOGIC` (no-ops after first run via completion flag) | **Confidence:** HIGH | **Evidence:** STATIC_CONFIRMED

---

### G.1) Canvas Hooks Decomposition Report (Dedicated Section)

> **Count context:** 56+ hooks in the `hooks/` directory; 20 called by Canvas.tsx on each render; the remainder are called from other components (ProInspector, LeftSidebar, etc.) or are internal sub-hooks.

#### 20-Hook Canvas.tsx Call Order

```
1.  useCanvasDragDrop       [DRAG]
2.  useCanvasInlineEdit     [INLINE EDIT]
3.  useCanvasElementDrag    [DRAG]
4.  useComposerSelection    [SELECTION — engine bridge]
5.  useCanvasGuides         [OVERLAY — guides]
6.  useCanvasSync           [ENGINE BRIDGE — HTML re-render]
7.  useCanvasIndicators     [OVERLAY — spacing indicators]
8.  useCanvasMarquee        [SELECTION — rubber band]
9.  useCanvasKeyboard       [KEYBOARD]
10. useCanvasHover          [HOVER]
11. useCanvasContent        [RENDER — HTML content generation]
12. useCanvasContextMenu    [UI — right-click menu]
13. useCursorSync           [COLLABORATION]
14. useSelectionBehavior    [SELECTION]
15. useCursorIntelligence   [COLLABORATION]
16. useCanvasSnapping       [DRAG — snap guides]
17. useCanvasCommandPalette [UI — Cmd+K]
18. useCanvasToolbarActions [UI — toolbar]
19. useCanvasInlineCommands [UI — inline commands]
20. useCanvasSize           [RENDER — container dimensions]
```

#### Hook Table (Key Hooks by Category)

**SELECTION HOOKS**
| Hook | Responsibility | Dep Risk | Complexity | Confidence |
|------|---------------|----------|------------|------------|
| `useComposerSelection` | Canonical selection read from SelectionManager | High (core) | Low | HIGH |
| `useCanvasSelectionBox` | Rubber-band marquee selection | Medium | Medium | HIGH |
| `useSelectionRect` | Selection bounding rect for overlays | Low | Low | HIGH |
| `useSelectionBehavior` | Click-to-select (shift, cmd, area) | Medium | Medium | HIGH |
| `useCanvasMarquee` | Marquee drag box UI state | Low | Low | HIGH |

**DRAG/DROP HOOKS**
| Hook | Responsibility | Dep Risk | Complexity | Confidence |
|------|---------------|----------|------------|------------|
| `useCanvasDragDrop` | Drop zone detection for sidebar-to-canvas drag | High | High | HIGH |
| `useCanvasElementDrag` | Within-canvas element repositioning | High | High | HIGH |
| `useDragSession` | Drag state machine (IDLE→PENDING→DRAGGING) | High | Medium | HIGH |
| `useDragVisuals` | Ghost element + cursor styles | Low | Low | HIGH |
| `useDragAutoScroll` | Auto-scroll when dragging near edges | Low | Low | HIGH |
| `useCanvasSnapping` | Snap-to-guide calculation | Medium | High | HIGH |
| `useCanvasResize` | Element resize handles interaction | High | High | HIGH |

**KEYBOARD HOOKS**
| Hook | Responsibility | Dep Risk | Complexity | Confidence |
|------|---------------|----------|------------|------------|
| `useCanvasKeyboard` (620L) | ALL canvas keyboard shortcuts | High | High ⚠️ | HIGH |

**OVERLAY/VISUAL HOOKS**
| Hook | Responsibility | Dep Risk | Complexity | Confidence |
|------|---------------|----------|------------|------------|
| `useCanvasHover` | Tracks hovered element via DOM hit test | Low | Medium | HIGH |
| `useCanvasGuides` | Guide lines state + drag-to-create | Low | Medium | HIGH |
| `useCanvasIndicators` | Spacing indicator calculations | Low | Medium | HIGH |

**ENGINE BRIDGE HOOKS**
| Hook | Responsibility | Dep Risk | Complexity | Confidence |
|------|---------------|----------|------------|------------|
| `useCanvasSync` | 12 events → toHTML() → iframe update | High (critical) | Low | HIGH |
| `useCanvasContent` | HTML with selection/drop states injected | High | Medium | HIGH |

**COLLABORATION HOOKS**
| Hook | Responsibility | Dep Risk | Complexity | Confidence |
|------|---------------|----------|------------|------------|
| `useCursorSync` | Broadcasts local cursor to collaborators | Medium | Medium | HIGH |
| `useCursorIntelligence` | Predicts remote cursor movement | Medium | High | HIGH |

**UI/UTILITY HOOKS**
| Hook | Responsibility | Dep Risk | Complexity | Confidence |
|------|---------------|----------|------------|------------|
| `useCanvasContextMenu` | Right-click menu state | Low | Low | HIGH |
| `useCanvasCommandPalette` | Cmd+K palette state | Low | Low | HIGH |
| `useCanvasToolbarActions` | Toolbar action handlers | Medium | Medium | HIGH |
| `useCanvasInlineCommands` | Inline text editor commands | Medium | Medium | HIGH |
| `useCanvasInlineEdit` | Double-click to enter text edit | Medium | Medium | HIGH |
| `useElementRect` | DOM rect calculation for overlays | Low | Low | HIGH |
| `useToolbarPosition` | Positions toolbars relative to selection | Low | Low | HIGH |
| `useCanvasSize` | Canvas container dimensions (ResizeObserver) | Low | Low | HIGH |

#### Hook Dependency / Ordering Risks

```
Canvas.tsx hook call order (important):
1. useDragSession          ← MUST run before useCanvasDragDrop (provides dragState)
2. useComposerSelection    ← MUST run before overlay hooks (provides selectedId)
3. useCanvasSync           ← MUST run after composer is initialized
4. useCanvasContent        ← Depends on useCanvasSync.content
5. useCanvasKeyboard       ← Depends on selectedId from #2
6. useSelectionBehavior    ← Depends on composer from #3

RISK: React hooks run in call order. If composer is null on first render
and a hook does not guard for null, it will throw.
All hooks should have: if (!composer) return defaultState;
[REVIEW_REQUIRED — needs runtime test to confirm all 20 hooks guard for null composer]
```

#### Hook Coupling Findings

1. **`useCanvasDragDrop` + `useDragSession` coupling** — useDragSession manages state machine; useCanvasDragDrop reads from it. Merging or re-splitting both would require coordinated changes.

2. **`useCanvasSync` + `useCanvasContent` double-render risk** — Both subscribe to element events and produce HTML content. They must not both call `toHTML()` for the same event. `[REVIEW_REQUIRED — trace needed to confirm single-call pattern]`

3. **`useCanvasKeyboard` (620L)** is a monolith depending on `selectedId`, `composer`, and toolbar action refs. Any refactor must preserve React's hook call order invariant.

#### Canvas Overlay System (16 Renderable Overlays)

```
CanvasOverlayGroup.tsx
  ├── SelectionBoxOverlay        ← selection border + resize handles
  ├── ElementHoverOverlay        ← hover highlight ring
  ├── DropFeedbackOverlay        ← drop indicator (green/red)
  ├── GuidesOverlay              ← draggable guide lines
  ├── RulersOverlay              ← px ruler along canvas edges
  ├── GridOverlay                ← background grid pattern
  ├── MarqueeOverlay             ← rubber-band selection box
  ├── SnappingLinesOverlay       ← snap guide lines during drag
  ├── SpacingIndicatorsOverlay   ← element spacing indicators
  ├── HierarchyOverlay           ← parent/child hierarchy hints
  ├── CollaborationCursors       ← remote user cursors
  ├── ResizeHandlesOverlay       ← per-handle resize controls
  ├── ContextMenuOverlay         ← right-click context menu
  ├── InlineEditOverlay          ← text editing toolbar
  ├── DropZoneHighlightOverlay   ← drop target highlight
  └── CanvasAnnotationsOverlay   ← comment/annotation pins
```

Total: 16 renderable overlays (CanvasOverlayGroup itself is the orchestrator, not counted as an overlay).

---

### H.1) Orphan / Legacy / Dead Code Report (Evidence-Based)

| File / Item                                        | Type             | Reason                                                                                                                 | Label              | Evidence         | Confidence |
| -------------------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------ | ---------------- | ---------- | ----------- |
| `engine/integrations/EmailService.ts` (144L)       | Entire file      | Every method throws `Error('Not implemented')`. Initialized in Composer, no UI callers found.                          | `ORPHAN_CANDIDATE` | STATIC_CONFIRMED | HIGH       |
| `SelectionManager.alignLeft()`                     | Method           | STATIC: grep of entire src/ returns 0 UI call sites                                                                    | `LIKELY_UNUSED`    | STATIC_CONFIRMED | HIGH       |
| `SelectionManager.alignRight()`                    | Method           | Same as above                                                                                                          | `LIKELY_UNUSED`    | STATIC_CONFIRMED | HIGH       |
| `SelectionManager.alignCenter()`                   | Method           | Same as above                                                                                                          | `LIKELY_UNUSED`    | STATIC_CONFIRMED | HIGH       |
| `SelectionManager.alignTop()`                      | Method           | Same as above                                                                                                          | `LIKELY_UNUSED`    | STATIC_CONFIRMED | HIGH       |
| `SelectionManager.alignMiddle()`                   | Method           | Same as above                                                                                                          | `LIKELY_UNUSED`    | STATIC_CONFIRMED | HIGH       |
| `SelectionManager.alignBottom()`                   | Method           | Same as above                                                                                                          | `LIKELY_UNUSED`    | STATIC_CONFIRMED | HIGH       |
| `SelectionManager.distributeHorizontal()`          | Method           | Same as above                                                                                                          | `LIKELY_UNUSED`    | STATIC_CONFIRMED | HIGH       |
| `SelectionManager.distributeVertical()`            | Method           | Same as above                                                                                                          | `LIKELY_UNUSED`    | STATIC_CONFIRMED | HIGH       |
| `components/surfaces/` (3 files)                   | Entire folder    | Zero imports, broken internal imports, duplicate of tab system                                                         | `ORPHAN_CANDIDATE` | STATIC_CONFIRMED | HIGH       | **DELETED** |
| `ProInspector/shared/PropertyRenderer.tsx`         | Entire file      | L0 per CLAUDE.md, never imported                                                                                       | `ORPHAN_CANDIDATE` | STATIC_CONFIRMED | HIGH       | **DELETED** |
| `ProInspector/shared/controls/ControlRegistry.tsx` | Entire file      | L0 per CLAUDE.md, 6/70+ types built                                                                                    | `ORPHAN_CANDIDATE` | STATIC_CONFIRMED | HIGH       | **DELETED** |
| `features/*/index.ts` (8 files)                    | All barrel files | All are pass-through re-exports with no added logic                                                                    | `LEGACY_SUSPECT`   | STATIC_CONFIRMED | HIGH       |
| `utils/sidebarAnalytics.ts`                        | Utility          | Name suggests analytics stub — not investigated                                                                        | `REVIEW_REQUIRED`  | NOT_CHECKED      | LOW        |
| `utils/openai.ts`                                  | Utility          | Unclear if used vs AI service client. Possible duplicate.                                                              | `REVIEW_REQUIRED`  | NOT_CHECKED      | LOW        |
| `components/Collaboration/PresenceIndicators.tsx`  | Component        | Previously marked REVIEW — **STATIC_CONFIRMED L2 wired** (StudioHeader imports it; connected to useCollaboration hook) | `CORE_FLOW`        | STATIC_CONFIRMED | HIGH       |

---

### I.1) Feature Ownership / Namespace Mismatch Report

#### Reality vs Folder Promise

**What `features/` promises:**

```
features/
  ai/ui/                ← "AI feature UI lives here"
  templates/ui/         ← "Templates feature UI lives here"
  editor/
    shell/              ← "Editor shell lives here"
    canvas/             ← "Canvas lives here"
    sidebar/            ← "Sidebar lives here"
    inspector/          ← "Inspector lives here"
    rail/               ← "Rail lives here"
    blocks/             ← "Block library lives here"
```

**What actually lives there (`STATIC_CONFIRMED`):**

```
features/ai/ui/index.ts → export * from '../../../components/AI/'
features/editor/canvas/index.ts → export * from '../../../components/Canvas/'
[... all 8 files are empty barrel re-exports]
```

**Actual file locations (physical reality):**

```
components/AI/               ← AI feature (barrels to features/ai/ui/)
components/Templates/        ← Templates feature (barrels to features/templates/ui/)
components/Editor/           ← Editor shell (barrels to features/editor/shell/)
components/Canvas/           ← Canvas (barrels to features/editor/canvas/)
components/Panels/LeftSidebar/ ← Sidebar (barrels to features/editor/sidebar/)
components/Panels/ProInspector/ ← Inspector (barrels to features/editor/inspector/)
components/Layout/LeftRail.tsx ← Rail (barrels to features/editor/rail/)
components/Blocks/           ← Block library (barrels to features/editor/blocks/)
```

#### Generic Buckets vs Domain Owners

| Folder        | Type              | Problem                                                                           |
| ------------- | ----------------- | --------------------------------------------------------------------------------- |
| `components/` | Generic bucket    | Contains ALL features (Editor, Canvas, AI, Templates, Media, Collaboration, etc.) |
| `utils/`      | Generic bucket    | Contains parsers, html utils, drag helpers, storage utils — all mixed             |
| `constants/`  | Generic bucket    | Events, tabs, storage keys, icons, defaults — all mixed                           |
| `types/`      | Generic bucket    | Types for every domain — all mixed                                                |
| `features/`   | Alias only        | Barrel redirects with no physical ownership                                       |
| `shared/`     | Alias only        | Barrel redirects with no physical ownership                                       |
| `engine/`     | Real domain owner | Self-contained engine with clear domain folders (elements/, styles/, drag/, etc.) |

The `engine/` directory is the **only folder with genuine domain-driven structure**. The UI side (`components/`) has no equivalent — everything is in generic top-level buckets.

---

### J.1) Truth Tables (Uncertain Items — Resolved)

| Item                                     | Exists? | Imported? | Reachable?        | UI-Triggered?    | Runtime-Verified? | Final Status                                   | Evidence                                     | Confidence |
| ---------------------------------------- | ------- | --------- | ----------------- | ---------------- | ----------------- | ---------------------------------------------- | -------------------------------------------- | ---------- |
| **Tab count = 9**                        | ✅      | ✅        | ✅                | ✅               | ❌                | **STATIC_CONFIRMED (9 tabs)**                  | 3-source: tabs.ts, TabRouter, LeftRail       | HIGH       |
| **Tab count = 10** (V1 mention)          | ❌      | N/A       | N/A               | N/A              | N/A               | **Superseded**                                 | Legacy before media/assets split             | HIGH       |
| **useElementFlash**                      | ✅      | ✅        | ✅                | ✅               | ❌                | **STATIC_CONFIRMED L2 wired**                  | File read + AquibraStudio import             | HIGH       |
| **Alignment methods (8)**                | ✅      | N/A       | ✅ (via composer) | ❌               | ❌                | **LIKELY L0 dead**                             | STATIC: 0 grep UI hits. Runtime not tested.  | HIGH       |
| **storageMigration**                     | ✅      | ✅        | ✅                | ✅ (module load) | ❌                | **STATIC_CONFIRMED valid**                     | 15 keys, completion flag                     | HIGH       |
| **EmailService**                         | ✅      | ✅        | ✅                | ❌               | ❌                | **STATIC_CONFIRMED L0 stub**                   | File content explicitly throws               | HIGH       |
| **Collaboration PresenceIndicators**     | ✅      | ✅        | ✅                | ✅               | ❌                | **STATIC_CONFIRMED L2 wired**                  | StudioHeader import + hook wiring            | HIGH       |
| **ConflictModal**                        | ✅      | ✅        | ✅                | ✅               | ❌                | **STATIC_CONFIRMED L2 wired**                  | StudioModals import + sync conflict event    | HIGH       |
| **features/ as domain owner**            | ✅      | ✅        | ✅                | N/A              | N/A               | **Namespace only — no physical ownership**     | All 8 files are barrel re-exports            | HIGH       |
| **Engine↔UI circular deps**              | N/A     | ❌        | N/A               | N/A              | N/A               | **No Engine↔UI cycle (STATIC, partial check)** | grep: engine imports 0 component files       | MEDIUM     |
| **CloudSync wired to UI**                | ✅      | ✅        | ✅                | ✅               | ❌                | **STATIC_CONFIRMED L2 wired**                  | ConflictModal wired; save/load wired         | MEDIUM     |
| **SelectionManager.getSelectedBounds()** | ✅      | REVIEW    | REVIEW            | REVIEW           | ❌                | **REVIEW_REQUIRED**                            | May be used by overlay bounds — not verified | MEDIUM     |
| **useCursorIntelligence**                | ✅      | ✅        | ✅                | ✅ (collab mode) | ❌                | **STATIC_CONFIRMED wired — conditional**       | Only active when collaboration connected     | MEDIUM     |
| **sidebarAnalytics.ts**                  | ✅      | REVIEW    | REVIEW            | REVIEW           | ❌                | **REVIEW_REQUIRED**                            | Not investigated                             | LOW        |

---

### K.1) Runtime Verification Checklist (Next Phase)

> None of the items below have been executed. This is the plan for runtime verification.

#### Smoke Flow Checks (Do These First)

```
□ Boot: Does editor load without console errors?
□ Boot: Does migrateStorageKeys() run without error? (check localStorage after first load)
□ Boot: Is Composer initialized before first render?
□ Tab switch: Click all 9 rail icons → does corresponding tab content render?
□ Tab switch: Does keyboard shortcut for each tab work? (A, Z, P, C, M, D, S)
□ Canvas: Click an element → does selection box appear? Does ProInspector update?
□ Canvas: Move an element → does canvas HTML update? Does inspector show new position?
□ Canvas: Resize an element via handle → does visual update correctly?
□ Inspector: Change padding → does canvas iframe update in real-time?
□ Undo: Cmd+Z → does element revert? Does inspector revert?
□ Redo: Cmd+Shift+Z → does element go forward? Does inspector go forward?
□ Drag from sidebar: Drag a block → does drop indicator appear? Does drop create element?
□ Template apply: From BuildTab, apply a template → does canvas populate?
```

#### Event/Listener Checks (Baseline/Delta Method)

```
□ Capture baseline: After first editor mount, record:
    composer.listenerCount('element:selected')
    composer.listenerCount('element:created')
    composer.listenerCount('drag:start')
  → Typical expected range: 3–7 per event. Note YOUR baseline, don't assume.

□ Remount check: Navigate away and back → compare listener counts to baseline
  → Counts MUST match baseline exactly.
  → Any growth above baseline = missing useEffect cleanup.

□ Tab switch leak test: Switch through all 9 tabs 10 times
  → Listener counts must remain at baseline throughout.
  → Lazy-loaded tab components should mount/unmount cleanly.

□ Investigate unexpected growth above baseline — do not assume a fixed threshold.
  → Find which hook/component is not cleaning up in its useEffect return.
```

#### Hook Null-Guard Check

```
□ Simulate slow init (set composer = null, delay real init by 500ms)
□ Do all 20 Canvas hooks handle null without throwing?
□ Check: useCanvasSync, useCanvasDragDrop, useCanvasKeyboard especially
□ Expected: Each hook returns empty/default state when composer is null
```

#### Store Sync Checks

```
□ After element:created fires, does ProInspector auto-select the new element?
□ After page:deleted, does PagesTab update without stale data?
□ After history:undo, do Canvas + ProInspector BOTH show the reverted state?
  → Both must respond to the same HISTORY_UNDO event
  → If one updates and the other doesn't, there's a listener gap
```

#### Alignment Methods Runtime Verification

```
□ Open browser console → type:
    window.aquibra?.composer?.selection?.alignLeft?.()
□ Does it execute? Does it move elements on canvas?
□ This is the only runtime test needed to confirm or deny the LIKELY L0 label.
□ If it works: add toolbar buttons (the feature is complete, just headless).
□ If it throws or does nothing useful: delete the 8 methods.
```

#### Suggested Debug Instrumentation (Dev-Only)

```typescript
// Add to useComposerInit.ts (dev mode only):
if (process.env.NODE_ENV === "development") {
  const EVENTS_TO_LOG = ["element:selected", "element:created", "drag:start", "drag:end"];
  EVENTS_TO_LOG.forEach((event) => {
    composer.on(event, (data) => devLogger.event(event, data));
  });
}

// Listener count baseline snapshot (run in browser console):
const events = ["element:selected", "element:created", "drag:start"];
events.forEach((e) => console.log(e, composer.listenerCount(e)));
```

---

### L.1) Probable SSOT Candidates

| Domain                | Current Owner                        | Duplication Risk                                                                                                        | Confidence | Recommendation                                                                                                |
| --------------------- | ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------- |
| **Tab metadata**      | `constants/tabs.ts`                  | Low                                                                                                                     | HIGH       | Already SSOT. Keep.                                                                                           |
| **Panel routing**     | `TabRouter.tsx`                      | Low                                                                                                                     | HIGH       | Already SSOT. Keep.                                                                                           |
| **Editor UI state**   | `useStudioState.ts`                  | Medium — ProInspector has own localStorage state                                                                        | HIGH       | Could unify under `useEditorState` owning all panel state keys.                                               |
| **Block registry**    | `components/Blocks/blockRegistry.ts` | Low                                                                                                                     | HIGH       | Already SSOT per CLAUDE.md. Keep.                                                                             |
| **Command registry**  | `engine/commands/CommandCenter.ts`   | Medium — shortcuts also in `useCanvasKeyboard.ts`                                                                       | MEDIUM     | Risk: same shortcut defined in both. Verify which takes precedence.                                           |
| **Event constants**   | `constants/events.ts` (90+ events)   | Low                                                                                                                     | HIGH       | Already SSOT. Any new event MUST be added here first.                                                         |
| **Storage keys**      | `constants/storage.ts`               | MEDIUM — `storageMigration.ts` maintains its own parallel key list; `useStudioState.ts` has `aqb-panel-state` hardcoded | HIGH       | **Gap**: storage.ts and storageMigration.ts both define key lists independently. They should share constants. |
| **Engine bridge**     | `engine/Composer.ts` (public API)    | Low — single bridge                                                                                                     | HIGH       | Already SSOT. The 28 manager namespaces form the interface.                                                   |
| **Alignment actions** | `engine/SelectionManager.ts`         | N/A (LIKELY L0 dead)                                                                                                    | HIGH       | Either add UI entry points or delete all 8 methods. Don't let dead code be the SSOT.                          |
| **CSS design tokens** | `themes/default.css` (--aqb-\* vars) | Low                                                                                                                     | HIGH       | Already SSOT. All CSS must reference --aqb-\* vars, never hardcode.                                           |

---

### M.1) Top 20 Most Important Files to Understand First

#### Tier 1 — Engine Core (Days 1–2)

| Rank | File                                | Why                                                                                  | Confidence |
| ---- | ----------------------------------- | ------------------------------------------------------------------------------------ | ---------- |
| 1    | `engine/Composer.ts`                | Creates all 28 managers. Reading this = understanding all possible editor operations | HIGH       |
| 2    | `engine/elements/ElementManager.ts` | Element CRUD is the heart of all editing                                             | HIGH       |
| 3    | `engine/SelectionManager.ts`        | Selection SSOT. Every click, every inspector render flows through here               | HIGH       |
| 4    | `engine/HistoryManager.ts`          | Undo/redo is core UX. JSON patch + coalesce pattern is important                     | HIGH       |
| 5    | `engine/styles/StyleEngine.ts`      | How CSS gets applied, stored, and resolved per breakpoint                            | HIGH       |
| 6    | `constants/events.ts`               | The complete event vocabulary. Must understand before reading any hook               | HIGH       |

#### Tier 2 — React ↔ Engine Bridge (Days 2–3)

| Rank | File                                              | Why                                               | Confidence |
| ---- | ------------------------------------------------- | ------------------------------------------------- | ---------- |
| 7    | `components/Editor/hooks/useComposerInit.ts`      | How Composer is bootstrapped and all events bound | HIGH       |
| 8    | `components/Canvas/hooks/useComposerSelection.ts` | Canonical selection read. All UI uses this.       | HIGH       |
| 9    | `components/Canvas/hooks/useCanvasSync.ts`        | The 79-line bridge that makes editing visible     | HIGH       |
| 10   | `components/Editor/hooks/useStudioState.ts`       | Panel state persistence. localStorage pattern.    | HIGH       |

#### Tier 3 — Layout + Routing (Day 3)

| Rank | File                                          | Why                                                            | Confidence |
| ---- | --------------------------------------------- | -------------------------------------------------------------- | ---------- |
| 11   | `components/Layout/LayoutShell.tsx`           | The 4-column CSS Grid that defines the editor's physical shape | HIGH       |
| 12   | `components/Editor/StudioPanels.tsx`          | Props distribution from shell to all 4 panels                  | HIGH       |
| 13   | `components/Panels/LeftSidebar/TabRouter.tsx` | Tab routing with code-splitting. All 9 tabs wired here.        | HIGH       |
| 14   | `constants/tabs.ts`                           | The `GroupedTabId` type + `GROUPED_TABS_CONFIG` SSOT           | HIGH       |

#### Tier 4 — Feature Surfaces (Days 4–5)

| Rank | File                                                        | Why                                                                | Confidence |
| ---- | ----------------------------------------------------------- | ------------------------------------------------------------------ | ---------- |
| 15   | `components/Canvas/Canvas.tsx`                              | Canvas entry — 20 hooks + 16 overlays orchestrated here            | HIGH       |
| 16   | `components/Panels/ProInspector/index.tsx`                  | Right panel. How selected element properties are displayed         | HIGH       |
| 17   | `components/Panels/ProInspector/config/contextEvaluator.ts` | Determines which inspector sections show for each element type     | HIGH       |
| 18   | `components/Canvas/hooks/useCanvasKeyboard.ts`              | All canvas keyboard shortcuts (620L). Most shortcut bugs are here. | HIGH       |
| 19   | `components/Blocks/blockRegistry.ts`                        | What elements users can drag/insert. Adding blocks = editing this. | HIGH       |
| 20   | `engine/export/ExportEngine.ts`                             | How editor DOM becomes clean HTML/CSS for production               | HIGH       |

---

### N.1) Final Recommendations (Decision Support)

#### Verify at Runtime First (Before Any Cleanup)

1. **Alignment methods** — `composer.selection.alignLeft()` in browser console. This is the only test needed to decide: add toolbar buttons vs delete 8 methods.
2. **Hook null-guard audit** — Simulate slow init. Do all 20 Canvas hooks gracefully return empty state when composer is null?
3. **Listener count baseline** — Record baseline counts on first mount. Re-check after tab switches and remounts.
4. **useCanvasSync + useCanvasContent** — Do both call `toHTML()` for the same event? Add a temp counter to verify single-call per event.

#### Cleanup Priority (After Verification)

1. **Delete `engine/integrations/EmailService.ts`** — STATIC_CONFIRMED stub. Grep for 0 UI callers first. 144 lines removed. Remove its initialization in `Composer.ts`.
2. **Delete 8 alignment methods in `SelectionManager.ts`** — After runtime confirms never triggered. ~200 lines removed. File drops from 523 → ~310 lines (under limit).
3. **Consolidate storage key constants** — Create single `constants/storage.ts` list; import from it in `storageMigration.ts`. One truth, not two lists.
4. **Resolve `features/` strategy** — Either commit to migration (physical file moves) or revert to `components/` as canonical. Maintaining a half-done migration misleads developers.
5. **Split `useCanvasKeyboard.ts` (620L)** — After alignment methods cleanup, split into 3 semantic groups (edit, nav, select shortcuts). Medium-risk refactor — no state changes, import reorganization only.

#### What NOT to Touch First

- **`engine/Composer.ts`** — The 28-manager system is well-organized. Don't refactor before fully understanding all event contracts.
- **`engine/collaboration/CollaborationManager.ts` (789L)** — STATIC_CONFIRMED L2 and active. Don't split until you understand the OT algorithm and have runtime tests.
- **Canvas hook call order in `Canvas.tsx`** — Hook call order must stay fixed in React. Don't reorder without running the full listener audit first.
- **Type files (`types/index.ts` 803L, `types/media.ts` 783L)** — Type files are verbose by nature. The 500-line limit in CLAUDE.md is for logic files.
- **`CanvasOverlayGroup.tsx`** — 16 overlays have careful z-index ordering. Any reordering changes visual stacking. Read before touching.

---

_Generated: 2026-02-21 | Version: V2.1 (corrects V2) | Analyst: Claude Sonnet 4.6_
_Evidence: 42-tool background agent, 115,424 tokens, 119 seconds (static analysis only)_
_Previous versions: `code_review_report.md` (V1), `code_review_report_v2.md` (V2)_

---

## F) Final Quality Checklist

| Check                                           | Status | Notes                                                                                                                                                                                 |
| ----------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Static vs runtime wording corrected             | ✅     | All 11 "Confirmed" instances replaced with `STATIC_CONFIRMED` or `LIKELY`. No `RUNTIME_CONFIRMED` claims exist (none were runtime-verified).                                          |
| Circular dependency scope wording fixed         | ✅     | "NO CIRCULAR DEPENDENCIES" → "No Engine↔UI cycle found (STATIC, best-effort, engine→components direction only). Full project-wide scan not performed."                                |
| Count mismatches resolved                       | ✅     | Overlays: 16 (not 17) — CanvasOverlayGroup is orchestrator, not an overlay. Hooks: 56+ (current) vs 41 (V1 estimate) — relationship clarified. Tabs: 9 (consistent throughout).       |
| Old/mixed fragments removed                     | ✅     | No V1 fragments present in V2. V2.1 preserves all V2 content with corrected wording only.                                                                                             |
| Inventory scope truthful                        | ✅     | Section B titled "Key Files Inventory" in V2 — already correct. Note added in V2.1 specifying exactly how many of the 875 files are covered.                                          |
| Path/typo issues fixed                          | ✅     | No path typos found. All file paths verified against directory tree.                                                                                                                  |
| Confidence + evidence type applied consistently | ✅     | Every major finding now has an evidence type label (`STATIC_CONFIRMED`/`LIKELY`/`REVIEW_REQUIRED`) and a confidence rating (HIGH/MEDIUM/LOW). No conflicting labels between sections. |
| Listener leak threshold normalized              | ✅     | "> 10 = leak" removed. Replaced with: "Capture baseline on first mount. Compare after remounts. Investigate unexpected growth above baseline."                                        |
| H section wording normalized                    | ✅     | "now CONFIRMED L2 WIRED" → "STATIC_CONFIRMED L2 wired"                                                                                                                                |
| Truth table (K/J section) labels consistent     | ✅     | All truth table rows now use evidence-type labels instead of bare "Confirmed"                                                                                                         |
