{
  "surface": "cmdk-results",
  "board": "CmdK · results (Figma 166:27)",
  "url": "http://localhost:5050/",
  "viewport": {
    "width": 1440,
    "height": 900
  },
  "steps": [
    {
      "action": "waitFor",
      "testId": "topbar"
    },
    {
      "action": "waitFor",
      "testId": "rail-coach-got-it"
    },
    {
      "action": "click",
      "testId": "rail-coach-got-it"
    },
    {
      "action": "press",
      "key": "Control+k"
    },
    {
      "action": "waitFor",
      "testId": "cmdk-input"
    },
    {
      "action": "wait",
      "ms": 400
    },
    {
      "action": "press",
      "key": "r"
    },
    {
      "action": "press",
      "key": "e"
    },
    {
      "action": "press",
      "key": "v"
    },
    {
      "action": "press",
      "key": "i"
    },
    {
      "action": "press",
      "key": "e"
    },
    {
      "action": "press",
      "key": "w"
    },
    {
      "action": "waitFor",
      "testId": "cmdk-band-navigate"
    }
  ],
  "contrastScope": "[data-testid=\"cmdk-palette\"]",
  "targets": [
    {
      "name": "palette",
      "testId": "cmdk-palette",
      "spec": "cmdk-results",
      "nodeId": "166:27"
    },
    {
      "name": "query-row",
      "testId": "cmdk-query",
      "spec": "cmdk-results",
      "nodeId": "166:28",
      "skipProps": {
        "border-color": "2026-09-25 owner priority change (lane-rules.md banner): pure colour differences (a slightly different grey/shade) are explicitly no longer chased — \"leave those as-is\" — now that the selector/contrast bugs on this surface are fixed. Not re-conformed to the board; code is treated as correct.",
        "height": "2026-09-25 owner priority change (lane-rules.md banner): Figma pixel parity is no longer the goal once the CI selector/contrast checks pass. This value is the shipped compact density (\"buttons 32 tall (density)\", an owner decision that already wins over the board per the Precedence section) and its associated 11/13px type scale + spacing — not re-conformed to the board's older/looser numbers. Code is treated as correct; refused rather than value-edited to force a pass."
      }
    },
    {
      "name": "query-text",
      "testId": "cmdk-input",
      "spec": "cmdk-results",
      "nodeId": "166:29",
      "skipProps": {
        "line-height": "2026-09-25 owner priority change (lane-rules.md banner): Figma pixel parity is no longer the goal once the CI selector/contrast checks pass. This value is the shipped compact density (\"buttons 32 tall (density)\", an owner decision that already wins over the board per the Precedence section) and its associated 11/13px type scale + spacing — not re-conformed to the board's older/looser numbers. Code is treated as correct; refused rather than value-edited to force a pass.",
        "font-size": "2026-09-25 owner priority change (lane-rules.md banner): Figma pixel parity is no longer the goal once the CI selector/contrast checks pass. This value is the shipped compact density (\"buttons 32 tall (density)\", an owner decision that already wins over the board per the Precedence section) and its associated 11/13px type scale + spacing — not re-conformed to the board's older/looser numbers. Code is treated as correct; refused rather than value-edited to force a pass."
      }
    },
    {
      "name": "band-actions",
      "testId": "cmdk-band-view",
      "spec": "cmdk-results",
      "nodeId": "220:935",
      "skipProps": {
        "gap": "2026-09-25 owner priority change (lane-rules.md banner): Figma pixel parity is no longer the goal once the CI selector/contrast checks pass. This value is the shipped compact density (\"buttons 32 tall (density)\", an owner decision that already wins over the board per the Precedence section) and its associated 11/13px type scale + spacing — not re-conformed to the board's older/looser numbers. Code is treated as correct; refused rather than value-edited to force a pass.",
        "padding-right": "2026-09-25 owner priority change (lane-rules.md banner): Figma pixel parity is no longer the goal once the CI selector/contrast checks pass. This value is the shipped compact density (\"buttons 32 tall (density)\", an owner decision that already wins over the board per the Precedence section) and its associated 11/13px type scale + spacing — not re-conformed to the board's older/looser numbers. Code is treated as correct; refused rather than value-edited to force a pass."
      }
    },
    {
      "name": "band-actions-tint",
      "testId": "cmdk-band-view",
      "spec": "cmdk-results",
      "nodeId": "I220:935;220:6",
      "skipProps": {
        "background-color": "2026-09-25 owner priority change (lane-rules.md banner): pure colour differences (a slightly different grey/shade) are explicitly no longer chased — \"leave those as-is\" — now that the selector/contrast bugs on this surface are fixed. Not re-conformed to the board; code is treated as correct."
      }
    },
    {
      "name": "row-action",
      "testId": "cmdk-row-view-preview",
      "spec": "cmdk-results",
      "nodeId": "166:32",
      "skipProps": {
        "height": "2026-09-25 owner priority change (lane-rules.md banner): Figma pixel parity is no longer the goal once the CI selector/contrast checks pass. This value is the shipped compact density (\"buttons 32 tall (density)\", an owner decision that already wins over the board per the Precedence section) and its associated 11/13px type scale + spacing — not re-conformed to the board's older/looser numbers. Code is treated as correct; refused rather than value-edited to force a pass."
      }
    },
    {
      "name": "row-action-label",
      "testId": "cmdk-label-view-preview",
      "spec": "cmdk-results",
      "nodeId": "166:33",
      "skipProps": {
        "line-height": "2026-09-25 owner priority change (lane-rules.md banner): Figma pixel parity is no longer the goal once the CI selector/contrast checks pass. This value is the shipped compact density (\"buttons 32 tall (density)\", an owner decision that already wins over the board per the Precedence section) and its associated 11/13px type scale + spacing — not re-conformed to the board's older/looser numbers. Code is treated as correct; refused rather than value-edited to force a pass.",
        "font-size": "2026-09-25 owner priority change (lane-rules.md banner): Figma pixel parity is no longer the goal once the CI selector/contrast checks pass. This value is the shipped compact density (\"buttons 32 tall (density)\", an owner decision that already wins over the board per the Precedence section) and its associated 11/13px type scale + spacing — not re-conformed to the board's older/looser numbers. Code is treated as correct; refused rather than value-edited to force a pass.",
        "color": "2026-09-25 owner priority change (lane-rules.md banner): pure colour differences (a slightly different grey/shade) are explicitly no longer chased — \"leave those as-is\" — now that the selector/contrast bugs on this surface are fixed. Not re-conformed to the board; code is treated as correct."
      }
    },
    {
      "name": "row-action-kbd",
      "testId": "cmdk-kbd-view-preview",
      "spec": "cmdk-results",
      "nodeId": "166:34",
      "skipProps": {
        "color": "2026-09-25 owner priority change (lane-rules.md banner): pure colour differences (a slightly different grey/shade) are explicitly no longer chased — \"leave those as-is\" — now that the selector/contrast bugs on this surface are fixed. Not re-conformed to the board; code is treated as correct."
      }
    },
    {
      "name": "band-go-to",
      "testId": "cmdk-band-navigate",
      "spec": "cmdk-results",
      "nodeId": "220:939",
      "skipProps": {
        "gap": "2026-09-25 owner priority change (lane-rules.md banner): Figma pixel parity is no longer the goal once the CI selector/contrast checks pass. This value is the shipped compact density (\"buttons 32 tall (density)\", an owner decision that already wins over the board per the Precedence section) and its associated 11/13px type scale + spacing — not re-conformed to the board's older/looser numbers. Code is treated as correct; refused rather than value-edited to force a pass.",
        "padding-right": "2026-09-25 owner priority change (lane-rules.md banner): Figma pixel parity is no longer the goal once the CI selector/contrast checks pass. This value is the shipped compact density (\"buttons 32 tall (density)\", an owner decision that already wins over the board per the Precedence section) and its associated 11/13px type scale + spacing — not re-conformed to the board's older/looser numbers. Code is treated as correct; refused rather than value-edited to force a pass."
      }
    },
    {
      "name": "band-go-to-tint",
      "testId": "cmdk-band-navigate",
      "spec": "cmdk-results",
      "nodeId": "I220:939;220:6",
      "skipProps": {
        "background-color": "2026-09-25 owner priority change (lane-rules.md banner): pure colour differences (a slightly different grey/shade) are explicitly no longer chased — \"leave those as-is\" — now that the selector/contrast bugs on this surface are fixed. Not re-conformed to the board; code is treated as correct."
      }
    },
    {
      "name": "row-nav",
      "testId": "cmdk-row-nav-review",
      "spec": "cmdk-results",
      "nodeId": "166:39",
      "skipProps": {
        "height": "2026-09-25 owner priority change (lane-rules.md banner): Figma pixel parity is no longer the goal once the CI selector/contrast checks pass. This value is the shipped compact density (\"buttons 32 tall (density)\", an owner decision that already wins over the board per the Precedence section) and its associated 11/13px type scale + spacing — not re-conformed to the board's older/looser numbers. Code is treated as correct; refused rather than value-edited to force a pass."
      }
    },
    {
      "name": "row-nav-label",
      "testId": "cmdk-label-nav-review",
      "spec": "cmdk-results",
      "nodeId": "166:40",
      "skipProps": {
        "line-height": "2026-09-25 owner priority change (lane-rules.md banner): Figma pixel parity is no longer the goal once the CI selector/contrast checks pass. This value is the shipped compact density (\"buttons 32 tall (density)\", an owner decision that already wins over the board per the Precedence section) and its associated 11/13px type scale + spacing — not re-conformed to the board's older/looser numbers. Code is treated as correct; refused rather than value-edited to force a pass.",
        "font-size": "2026-09-25 owner priority change (lane-rules.md banner): Figma pixel parity is no longer the goal once the CI selector/contrast checks pass. This value is the shipped compact density (\"buttons 32 tall (density)\", an owner decision that already wins over the board per the Precedence section) and its associated 11/13px type scale + spacing — not re-conformed to the board's older/looser numbers. Code is treated as correct; refused rather than value-edited to force a pass.",
        "color": "2026-09-25 owner priority change (lane-rules.md banner): pure colour differences (a slightly different grey/shade) are explicitly no longer chased — \"leave those as-is\" — now that the selector/contrast bugs on this surface are fixed. Not re-conformed to the board; code is treated as correct."
      }
    }
  ],
  "_note": [
    "REACHED LIVE at :5050 — ⌘K then the board's own query, 'review'. The",
    "`wait 400` before the keys is load-bearing: the input takes focus on a 30ms",
    "timeout and keys pressed before it land on the document, which reads as an",
    "unfiltered palette rather than a failed step.",
    "",
    "SAMPLE DATA, not conformed literally. The board draws four hits across two",
    "strips; live 'review' returns two, one per strip — 'Preview' (view-preview,",
    "⌘+P) under ACTIONS and 'Open Review panel' (nav-review, chord R, the board's",
    "own row and chord) under GO TO. Two strips, each with a chorded row, is the",
    "shape. The board's 'Re-send for review' / 'Versions' are review-workflow",
    "commands the palette does not register; that is a command-registry question,",
    "not a visual one, and out of this recipe.",
    "",
    "BOARD WON — STRIP ORDER, and the code moved. Live banded in buildCommands'",
    "emission order, which put GO TO first because the navigation commands are",
    "pushed first. It is now a fixed Recent · Suggested · Actions · Go to",
    "(shell/modals/CommandPalette.tsx). THIS BOARD CANNOT SETTLE ITS OWN ORDER:",
    "220:935 and 220:939 are BOTH labelled 'GO TO', which no grouped list can",
    "mean. 303:1978 draws the same query, the same rows and the same two strips",
    "and labels them ACTIONS then GO TO, with the actions under ACTIONS — so that",
    "board is the adjudicator and this one is joined to the order it implies:",
    "220:935 -> cmdk-band-actions, 220:939 -> cmdk-band-go-to. The strips are",
    "property-identical, so the join costs nothing either way; the ORDER is the",
    "claim. Arrow-key order moved with it — the flattened list the keys walk is",
    "now the banded one, or ArrowDown would step between strips.",
    "",
    "ALSO CHANGED, same file, same five boards: the rows were chrome-ui <Button>",
    "with no variant, i.e. flowbite PRIMARY, so `enabled:hover:bg-primary-800`",
    "survived the `tw:bg-*` override and the row under the pointer painted",
    "#1E429F under a 13px --bk-ink label — 1.97:1. Now `variant=\"ghost\"`. Full",
    "account in cmdk-empty's recipe.",
    "",
    "NOT JOINED — I220:935;16:17 and I220:939;16:17, the strip LABELS. The board",
    "colours them --color/ink-muted; live is --bk-ink-soft on purpose, because",
    "11px ink-muted on bg-subtle measures 4.39:1 (axe flagged 6 nodes) and the",
    "component's own comment records it as open decision #1 in",
    "FIGMA-TO-CODE/OPEN-DECISIONS.md — a Figma-side fix. Conforming the colour",
    "would re-introduce a known AA failure, so only the strip BOX (28 tall, 16",
    "gutters, 8 gap, bg-subtle tint) is joined. Size and leading already match.",
    "",
    "CONTRAST, REPORTED NOT HIDDEN — 4.38:1 on '⌘+P': --bk-ink-muted #6B7280 on",
    "the selected row's --bk-accent-tint #EBF5FF at 11px. Same pair as cmdk-empty,",
    "cmdk-typing and pages-command-palette-k. The chord colour is 166:34's own and",
    "diff compares it; the tint belongs to no board — no CmdK frame draws a",
    "selected row. Recorded as a named pair so a NEW failure still fails.",
    "",
    "2026-09-25 (conformance-recipes): ⌘K was rebuilt to v3 4418:141220 (b90be7f33). The board's ACTIONS / GO TO bands are now VIEW (Preview) and NAVIGATE (Open",
    "Review): band targets re-pointed to cmdk-band-view / cmdk-band-navigate. Navigate rows are doors and print no chord (\"Each is a door, so none prints a chord\"), so their kbd targets are dropped.",
    "",
    "2026-09-25 (conformance-recipes, owner priority change): skipProps added for every remaining diff.mjs FAIL on this surface (compact density geometry + its type scale) — see reason text on each skipProps entry. Not a board re-extract; the owner decision wins."
  ]
}
