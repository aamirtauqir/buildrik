{
  "surface": "inspector-no-selection",
  "board": "Inspector · no-selection (Figma 159:99)",
  "url": "http://localhost:5050/",
  "viewport": { "width": 1440, "height": 900 },
  "steps": [
    { "action": "waitFor", "testId": "rail-coach-got-it" },
    { "action": "click", "testId": "rail-coach-got-it" },
    { "action": "waitFor", "testId": "inspector-empty" }
  ],
  "contrastScope": "[data-testid=\"inspector-empty\"]",
  "targets": [
    { "name": "empty-block", "testId": "inspector-empty", "spec": "inspector-no-selection", "nodeId": "159:100" },
    { "name": "empty-text", "testId": "inspector-empty-text", "spec": "inspector-no-selection", "nodeId": "159:101" },
    { "name": "ask-ai", "testId": "inspector-empty-ask-ai", "spec": "inspector-no-selection", "nodeId": "920:4717" }
  ],
  "_note": [
    "REACHED LIVE at the shell's own 1440x900: on first run nothing is",
    "selected, so this is the state the editor opens in. The only step is the",
    "rail coach mark's 'Got it', which otherwise covers the Insert drawer and",
    "would intercept any later click.",
    "",
    "THE BOARD WON, THREE TIMES, and the third one reverses an earlier read:",
    "",
    "  ALIGNMENT. 159:100 carries `text-center`, and BOTH its text nodes are",
    "  268-wide boxes at `left-[150px] -translate-x-1/2` — the panel's full",
    "  content width (300 - 2x16), centred. The code was `items-start` +",
    "  `text-left`, which shrink-wrapped the sentence to its own 253px and left",
    "  the AI link `self-center`. The comment that justified it reasoned from",
    "  the box's LEFT EDGE ('box starts at x16') and so missed both the",
    "  centring transform and the parent's `text-center`. Now `items-stretch`",
    "  + `text-center`: both children get the board's 268 box, both measure it.",
    "",
    "  THE AI LINK'S TYPE. 920:4717 is 12px Inter Medium in a 16px box; the",
    "  chrome-ui Button's `link` colour key ships 13/20 at `min-h-6` (24). The",
    "  key is 48 call sites wide and one screen board does not move it, so the",
    "  two deltas are corrected at THIS call site — `tw:text-[12px]` and",
    "  `tw:min-h-4`, both the same twMerge groups as the theme classes they",
    "  beat.",
    "",
    "  THE SENTENCE'S LEADING. `leading-normal` computes 19.5 against the",
    "  board's 20 — inside the 0.5px tolerance, so it had been passing while",
    "  being wrong. `tw:leading-5` states it.",
    "",
    "NOT JOINED:",
    "",
    "  920:4717's `line-height`. The board says 0, which is Figma's marker for",
    "  an auto-height text node, not a line box any renderer can produce. It is",
    "  measured and reported UNKNOWN rather than dropped. Related and worth",
    "  knowing: `tw:leading-*` is inert on a chrome Button — the root computes",
    "  `line-height: normal` with the theme's own `tw:leading-5` on it, and",
    "  only the arbitrary-property form `tw:[line-height:16px]` lands. The 16px",
    "  box comes from `min-h-4` instead.",
    "",
    "  159:99 itself (the panel frame, bg-card white). The no-selection branch",
    "  returns InspectorEmptyState directly — there is no `inspector-panel` in",
    "  this state — and the frame's only property is a fill the empty block",
    "  already composites to. Joining it to the same element as 159:100 would",
    "  be one anchor standing for two nodes.",
    "",
    "  1175:4868, a prototype hotspot for the template-applied state. Not a",
    "  control; that state is board 1175:4841's surface, not this one."
  ]
}
