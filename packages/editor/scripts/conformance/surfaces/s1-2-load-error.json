{
  "surface": "s1-2-load-error",
  "board": "S1.2 · load-error (Figma 297:2139) — the state-banner",
  "_note": [
    "This board draws the WHOLE shell with a failed load: rail, topbar, layers",
    "drawer, canvas with a selected hero, inspector, footer, and one thing that",
    "belongs to nobody else — the `state-banner` at 297:2243. Everything above",
    "that list has a family that owns it (Rail 52:2, Topbar 681:26, Layers,",
    "Inspector 159:99, and the footer's own 52:10), and is measured there. This",
    "recipe measures the banner, which is what the row is actually about.",
    "",
    "PROBE, because `loadError` is set only from the `.catch` of a dashboard",
    "round-trip (useComposerInit) — no click reaches it, and the demo has no",
    "siteId so it never calls the dashboard at all. The real component is",
    "mounted in the `network` kind the board draws.",
    "",
    "WHAT MOVED:",
    "  · Retry: 12/6 insets, radius 6, 13px label. flowbite's `size=\"xs\"` was",
    "    supplying h-8/px-3/text-xs and the base rounded-lg; only same-property",
    "    utilities unseat those, so it took `h-auto` (not `min-h`), `py-1.5`,",
    "    `rounded-md` and an explicit 13.",
    "  · `leading-normal` -> `leading-[normal]` on the bar. Tailwind's",
    "    `leading-normal` is 1.5, i.e. 21px; the board says the keyword, and the",
    "    difference is 7px of line box on a one-line banner.",
    "  · the ⚠ the board opens the line with, `aria-hidden` — the banner is",
    "    already `role=\"alert\"` with a name, and a spoken 'warning sign' adds",
    "    nothing to that.",
    "",
    "NOT CONFORMED, and this is the row's headline drift (boards.json:",
    "`drift-banner-position`): the board puts the banner at `top-56`, i.e.",
    "BELOW the topbar, full-bleed at 1440. The shipped banner is a flex child",
    "at the very top of `.bd-studio`, ABOVE the topbar, and moving it means",
    "editing `AquibraStudio.tsx` — which this arc may not stage, because it is",
    "mid-edit in the founder's tree. Reported rather than half-done; the",
    "banner's own box (fill, insets, radius, gap) is joined and passes, so the",
    "outstanding delta is one line of placement, not a rebuild.",
    "",
    "COPY: the board's line stops at \"Couldn't load the latest version of this",
    "site.\" and the shipped one continues \"You're seeing local changes for",
    "now.\" That clause is kept: `useComposerInit` really does call",
    "`loadFromLocalStorage` on this branch, so it is the true half of the story",
    "the board's sample line leaves out. Board sample data is not conformed to;",
    "the SHAPE is.",
    "",
    "NOT JOINED: 297:2245, the flex spacer between message and button — a",
    "1px-high Figma strut standing in for `flex: 1`, which the code expresses",
    "on the message itself."
  ],
  "url": "http://localhost:5051/e2e/probe/probe.html?case=s1-2-load-error",
  "viewport": { "width": 1440, "height": 200 },
  "steps": [{ "action": "waitFor", "testId": "load-error-banner" }],
  "contrastScope": "[data-probe]",
  "targets": [
    { "name": "banner", "testId": "load-error-banner", "spec": "s1-2-load-error", "nodeId": "297:2243" },
    { "name": "message", "testId": "load-error-message", "spec": "s1-2-load-error", "nodeId": "297:2244" },
    { "name": "retry", "testId": "load-error-retry", "spec": "s1-2-load-error", "nodeId": "297:2246" }
  ]
}
