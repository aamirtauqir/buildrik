{
 "surface": "layers-no-results",
 "board": "Layers · no-results (Figma 782:4260)",
 "url": "http://localhost:5051/e2e/probe/probe.html?case=layers-tree",
 "viewport": {
  "width": 280,
  "height": 812
 },
 "steps": [
  {
   "action": "waitFor",
   "testId": "layer-row-lx-section"
  },
  {
   "action": "click",
   "testId": "layers-search"
  },
  {
   "action": "press",
   "key": "h"
  },
  {
   "action": "press",
   "key": "e"
  },
  {
   "action": "press",
   "key": "r"
  },
  {
   "action": "press",
   "key": "o"
  },
  {
   "action": "wait",
   "ms": 500
  },
  {
   "action": "waitFor",
   "testId": "layers-no-results"
  },
  {
   "action": "hover",
   "testId": "layers-count"
  }
 ],
 "contrastScope": "[data-probe]",
 "targets": [
  {
   "name": "panel-header",
   "testId": "panel-header",
   "spec": "layers-no-results",
   "nodeId": "782:4261"
  },
  {
   "name": "panel-header-title",
   "testId": "panel-header-title",
   "spec": "layers-no-results",
   "nodeId": "I782:4261;16:7"
  },
  {
   "name": "toolbar",
   "testId": "layers-toolbar",
   "spec": "layers-no-results",
   "nodeId": "782:4262"
  },
  {
   "name": "no-results-block",
   "testId": "layers-no-results",
   "spec": "layers-no-results",
   "nodeId": "782:4350"
  },
  {
   "name": "no-results-text",
   "testId": "layers-no-results-text",
   "spec": "layers-no-results",
   "nodeId": "782:4351"
  },
  {
   "name": "clear-search",
   "testId": "layers-clear-search",
   "spec": "layers-no-results",
   "nodeId": "782:4352"
  },
  {
   "name": "search-box",
   "testId": "layers-search"
  }
 ],
 "_note": [
  "['782:4260 is the one board in the manifest nobody had ever measured. It had', 'a recipe from 2026-09-01 that mounted `LayersNoResults` ALONE in a static', 'probe case and joined a single target — the block — so it certified the', \"empty state's own padding and nothing about the screen the board draws.\", 'This pass drives the state instead: the `layers-tree` case mounts the REAL', 'tab over a REAL Composer, and the recipe types h-e-r-o into the SHIPPED', 'search field. `useLayerSearch` filters, nothing in the eight-row seed', \"matches, and LayersPanel:433 renders the block — the board's own query and\", \"the board's own branch.\", '', 'CONFORMED to the board in this pass: nothing had to move. Every property', \"the board states and the code can express already agreed — the block's\", '36/32/24/24 insets, its 10px gap, its 13px text, the message in ink-muted', \"and 'Clear search' in accent-text, on the 44px panel header and the 36px\", 'toolbar. What changed is that it is now MEASURED rather than assumed.', '', 'The message paragraph gained `layers-no-results-text` in this pass. It had', 'no anchor, so 782:4351 — the one node that says the message is ink-muted', 'and not ink — had nothing to join to.', '', 'NOT JOINED, with reasons:', '', '  the search box (782:4263) — the fill and stroke agree with what ships', '  (--flowbite/gray/50 on --color/border, the reading `layers-filtered`', '  records for 137:8 / 781:4220 / 782:4263), but the node also states', '  width 166 and left 16 on a 280 panel. The shipped field is flex and the', \"  family's left inset was settled at 12 by `layers-tree` against 142:7,\", '  which four boards agree on. A width drawn around a four-letter sample', '  query is frame geometry, not a box model. Measured unjoined.', '', '  the query TEXT (782:4264) — 13px/20 --color/ink is the value the field', '  ships, but the node is the text INSIDE the input and the only anchor on', '  that control is the `<label>` wrapper (`layers-search`). Joining a label', \"  box to an input's type contract would compare two different elements.\", '', '  the ⊞⊟⚙ run (782:4265) — 11px --color/ink-soft here. 143:10, 143:303,', '  1082:4597 and 1171:4834 all draw the same run at 13px --color/ink-muted,', '  which is what ships and what `layers-filtered`, `layers-hidden` and', '  `layers-locked` each join against. Four boards to one; one board does', '  not re-settle a run the family already agreed on. Measured unjoined.', '', '  the panel ROOT (782:4260) — 1px gray-100 on four sides, the same', '  floating-frame dressing the whole 142/143/782 family carries and the', '  same disagreement with the 1082/1171 card. The shipped drawer is docked.', '', '  the back hotspot (787:4325) — a prototype hitbox, not a rendered thing.', '', 'COUNT FOOTER — CORRECTED IN THIS PASS, because the first draft of this note', 'asserted it instead of reading it. The board draws no footer. The product', \"DOES render one here, and it reads '9 layers' — the page total, not the\", 'match count — because `.bdc-lcount` is gated only on `!treeFailed`', '(LayersTab.tsx:216) and `stats.total` is the whole tree. That is the same', \"reading board 143:2 draws for the FILTERED state ('12 layers' over a\", 'one-result list), so the code is consistent with the family; 782:4260 simply', 'omits the band from its frame. Not joined here — the board has no node for', 'it — and measured on `layers-tree` and `layers-filtered`, which do.', '', 'TWO STRINGS THE COPY CHECK REPORTS, both benign, both verified:', \"  'Nothing matches ‘…’.' reads as two live fragments ('Nothing matches ‘'\", \"  and '’.') because the query is a JSX interpolation and the harness\", '  collects leaf text nodes. The rendered sentence is whole.', \"  'No layers match your search' is live text with no board node: it is the\", '  `aria-live` announcement at LayersPanel:392-397, `.bdc-sr-only`, and a', '  board cannot draw something that is deliberately invisible.'] ROOT ROW REMOVED 2026-09-08 (founder call, BLOCKERS.md B6): the page root is no longer a layer, so `layer-row-lx-root` does not exist and the first row is the first seeded element. ROOT ROW REMOVED 2026-09-08 (founder call, BLOCKERS.md B6). The page root is no longer a layer. Its targets are DELETED rather than re-pointed — there is no row to measure — and the gating `waitFor` now names `lx-section`, the first TOP-LEVEL row, which is visible before any expansion exactly as the root row was.",
  "2026-09-22 (B10, audit G2-058, board 7059:78962): Expand all / Collapse all / Display settings… moved from toolbar glyphs to the header ⋯ menu (layers-panel-menu); steps open the menu first; the targets that measured the retired ⊞/⚙ buttons are dropped, so `compared` falls on the next diff — accept with --update-baseline."
 ]
}
