{
  "surface": "media-bulk-delete-confirm",
  "board": "Media · delete-confirm · bulk >20 — plain confirm (decision #29; Figma 1175:4827 drew a type-DELETE gate)",
  "_note": "Decision #29 (2026-09-21): an assets bulk delete past 20 is a plain confirm; typed DELETE is kept for irreversible AND wide actions only. The board's type-DELETE input (1175:4832) and reason line (1175:4840) no longer exist in code, so the waitFor is on the confirm button. Probe: the >20-file branch needs a real selection over a real library plus a usage scan, so the fixture supplies the payload the modal branches on (34 keys, isBulk, 5 in use). The waitFor is on `media-delete-gate-reason`, which only renders while the typed word does NOT match — i.e. it proves the type-DELETE gate is actually up, which is the whole subject of this board.\n\nNOT joined: 1175:4827 the card itself. MODAL_FRAME_BASE_CLASS is shared by every dialog in the editor and ModalTitle/ModalBody own the insets in this system — the same call export-html-modal recorded for 1172:4825. 1175:4832 the confirm input, boards.json authority=open:input-fill. 1175:4830 (w-388) / 1175:4837 / 1175:4839 — text-frame widths and label sizes inside flowbite's own Button span, a shared primitive.\n\n1175:4829, the in-use warning, is joined for its FILL, PADDING and RADIUS and NOT for its height: the board's 40 holds one sentence, and the shipped block also names each affected file and the pages it is on. That list is why the sentence is checkable — the modal's own comment records the reason — so the board's height is a fact about mock content, not about the block. Its type WAS conformed: 12/18 → the board's 11/16, and `--bk-warning-text` no longer carries a `var(...)` fallback it never needed.\n\n1175:4840, the gate's reason line, is NOT joined: every text node on this frame carries Figma's AUTO line height (`leading-[normal]`), including the ones whose siblings on the Media drawer boards are explicitly 11/16. AUTO is the absence of a decision, not a decision, and the same call was made on media-no-results where board 782:4399 asks for `normal` against a family that is uniformly 13/20. The line is 11px ink-muted either way.",
  "url": "http://localhost:5051/e2e/probe/probe.html?case=media-delete-confirm-bulk",
  "viewport": {
    "width": 1440,
    "height": 900
  },
  "steps": [
    {
      "action": "waitFor",
      "testId": "media-delete-confirm"
    }
  ],
  "targets": [
    {
      "name": "title",
      "testId": "media-delete-title",
      "spec": "media-bulk-delete-confirm",
      "nodeId": "1175:4828"
    },
    {
      "name": "foot",
      "testId": "media-delete-foot",
      "spec": "media-bulk-delete-confirm",
      "nodeId": "1175:4834"
    },
    {
      "name": "cancel",
      "testId": "media-delete-cancel",
      "spec": "media-bulk-delete-confirm",
      "nodeId": "1175:4836"
    },
    {
      "name": "delete",
      "testId": "media-delete-confirm",
      "spec": "media-bulk-delete-confirm",
      "nodeId": "1175:4838"
    }
  ]
}