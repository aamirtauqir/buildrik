{
  "surface": "pages-page-tab-bar-code-pagetabbar-tsx",
  "board": "Pages · page tab bar (Figma 435:2348)",
  "_note": [
    "435:2348 is a SPEC SHEET, not a screen: five labelled sections stacked in a",
    "1080-wide frame, each drawing PageTabBar in one state (default, unsaved,",
    "renaming, context menu, delete confirm). What is joined here is the default",
    "sample's tab anatomy, which is the part that is the same component in every",
    "one of the five.",
    "",
    "PageTabBar returns null without a composer, so the probe stubs the two reads",
    "and two events it makes. The bar's own CSS is all `tw:`, so no stylesheet",
    "needs loading (unlike the Pages panel).",
    "",
    "NOT joined, and why:",
    "  435:2350 (the bar frame) — 1000px wide with a 6px radius, which is the sheet",
    "  drawing a sample of a bar that in the product spans the canvas edge to edge",
    "  as a border-y strip. Its remaining properties are split across two elements",
    "  in the code (BAR carries the fill and border, ROW the 8px gap and the 4/8",
    "  padding), so no single node can be scored against it.",
    "  435:2351 (tablist) — gap 2 matches; the board's 6px radius on a scrolling",
    "  tablist has nothing to round.",
    "  435:2368/2371 (dirty dot) — `useDirtyPages` keeps its state in a",
    "  per-composer store fed by real engine events. A stub could write that store,",
    "  and then the measurement would be of the fixture. Left to a live walk.",
    "  435:2380-2399 (rename: error / warning / valid) and 435:2402-2417 (context",
    "  menu, delete confirm) — driven states behind F2 and right-click. The",
    "  2026-08-31 walk already reports the validation popovers and the delete-confirm",
    "  copy as character-exact; the drift it recorded (the active tab floating 4px",
    "  above the bar's bottom edge) is fixed, and the fix is what `ROW`/`TAB_ACTIVE`",
    "  in PageTabBar.tsx now document."
  ],
  "url": "http://localhost:5051/e2e/probe/probe.html?case=pages-page-tab-bar",
  "viewport": { "width": 1000, "height": 300 },
  "steps": [{ "action": "waitFor", "testId": "page-tab-home" }],
  "contrastScope": "[data-probe]",
  "targets": [
    { "name": "tab-active", "testId": "page-tab-home", "spec": "pages-page-tab-bar-code-pagetabbar-tsx", "nodeId": "435:2352" },
    { "name": "tab-home-glyph", "testId": "page-tab-home-home", "spec": "pages-page-tab-bar-code-pagetabbar-tsx", "nodeId": "435:2353" },
    { "name": "tab-active-name", "testId": "page-tab-name-home", "spec": "pages-page-tab-bar-code-pagetabbar-tsx", "nodeId": "435:2354" },
    { "name": "tab-resting", "testId": "page-tab-about", "spec": "pages-page-tab-bar-code-pagetabbar-tsx", "nodeId": "435:2355" },
    { "name": "tab-resting-name", "testId": "page-tab-name-about", "spec": "pages-page-tab-bar-code-pagetabbar-tsx", "nodeId": "435:2356" },
    { "name": "add-btn", "testId": "page-tab-add", "spec": "pages-page-tab-bar-code-pagetabbar-tsx", "nodeId": "435:2359" }
  ]
}
